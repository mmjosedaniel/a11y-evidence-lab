# Reliability, reproducibility, and operations requirements

## Authority and use

This document is a focused canonical module within [Quality, security, and operations requirements](README.md) and the authoritative requirements baseline indexed by [Project requirements](../../PROJECT_REQUIREMENTS.md). The index defines status vocabulary, priority semantics, ID stability, and precedence. This file describes planned behavior, not implemented behavior; each identified row's recorded status controls.

## Decision history

OD-010, OD-011, OD-015, and OD-018 were resolved on 2026-08-25. Rows below that moved from Proposed to Accepted define only the proportional MVP controls. Rows moved from Proposed to Deferred retain their stable IDs for a later, explicitly authorized performance, support, packaging, or release stage.

`REQ-QUAL-010` was already Accepted, but OD-015 narrows its earlier broad interpretation: MVP runtime validation is limited to the URL input, axe result, actual local or Groq provider response, and persisted `run.json` read boundaries. It does not require a universal schema platform, a separate provider probe, or a capability-preflight subsystem. `REQ-QUAL-009` remains the local-model capacity gate, planned for this portfolio scope as one documented manual smoke run on the reference PC rather than application-owned hardware monitoring or support qualification.

[OD-020](../DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-020--authorized-public-page-analysis-scope) supersedes the synthetic-only and one-rule-per-scan wording retained under stable IDs `REQ-QUAL-003` and `REQ-QUAL-012`. One public-page scan now owns the entire top-level document in its current rendered state at the configured readiness condition for one developer-selected public HTTPS page and the exact three-rule profile, while controlled fixtures remain the fixed evaluation baseline. Downstream work remains sequential for one selected finding at a time: incomplete required evidence or a completed retrieval with `incomplete`, `missing`, or `conflicting` support ends in a terminal application-authored abstention with no provider call or proposal-review decision; a retrieval execution or passage-integrity error instead fails the FindingWorkflow without assigning a support state; and only complete required evidence plus a completed `supported` retrieval permits generation whose validated proposal may enter human review. [OD-021](../DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-021--trusted-operator-url-boundary-for-the-portfolio-mvp) and [ADR-0018](../../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md) replace ADR-0017's hostile-target expansion: `REQ-QUAL-010`, `REQ-QUAL-012`, `REQ-QUAL-013`, and `REQ-QUAL-019` are narrowed below, while the exhaustive bound manifest in `REQ-QUAL-014` and its exact-limit/no-bypass proof in `REQ-QUAL-017` are Deferred. They no longer block development of the trusted-input portfolio slice.

For the portfolio MVP, one `PageAnalysisRun` has one canonical `run.json` aggregate. Findings and the state reached for a user-selected finding are nested in that aggregate; the MVP does not require independently versioned child files, an event store, an audit log, a recovery journal, or a generated Markdown report. Outside the application, the developer installs and starts Ollama, obtains `embeddinggemma` for retrieval in both modes, obtains `qwen3.5:4b` only for Local generation, and supplies a Groq credential only for Groq generation. The application reports unavailable dependencies and actual provider-call failures but does not supervise the runtime, monitor hardware continuously, or run a separate readiness/probe workflow.

## Reliability, reproducibility, and operations

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-QUAL-001 | Each `PageAnalysisRun` must use one canonical `run.json` aggregate. A completed scan and each later selected-finding result, human decision, or comparison update must be written as a complete aggregate before it is treated as durable; interruption must not replace an earlier valid aggregate with partial data. Failure to durably write the initial complete scan aggregate prevents parent completion and leaves the PageAnalysisRun failed. Failure to write a later nested retrieval, generation, review, or comparison update instead leaves the already completed parent and last valid aggregate unchanged, reports that selected action as failed, and must not present the unsaved state as durable. Independently versioned child files, event sourcing, audit records, recovery journals, generated reports, and in-flight resume are not required. | Must | Accepted | Aggregate-write and interruption inspection |
| REQ-QUAL-002 | Completed deterministic scan evidence must remain available when a later retrieval, generation, review, comparison, or optional diagnostic action fails. | Must | Accepted | Stage-failure inspection |
| REQ-QUAL-003 | Each fixed MVP evaluation case must record only the material identities needed to interpret it: controlled-fixture profile and revision, exact three-rule scan profile and selected Finding, browser/scanner versions, corpus snapshot, retrieval configuration, prompt/output-contract version, application revision, and immutable selected mode/provider/model as run configuration. A public-page run records its requested and observed final page identities instead of fixture identity. Only when an actual generation call is attempted, its compact provenance must additionally record the run's provider/model context reference, adapter identifier and version, non-secret endpoint identity, bounded material generation parameters, prompt/output-contract provenance, owning Finding and corpus/passage context reference, non-secret outcome, and validation result. Request time, runtime/model revision, and safe usage metadata are optional when available. Selected mode without a call remains context and creates no invocation provenance. | Must | Accepted | Fixed-manifest, compact actual-call provenance, and public-run provenance inspection |
| REQ-QUAL-004 | Automated corpus update, index replacement, and recovery behavior are deferred. The MVP uses one fixed, approved corpus snapshot; changing it creates a new snapshot and requires rerunning affected fixed cases rather than an in-place production update. | May | Deferred | Reconsider if mutable corpus management is authorized |
| REQ-QUAL-005 | Ordinary local diagnostics must identify the failing stage and exclude secrets, raw provider payloads, excluded page content, credentials, and arbitrary input values. A structured audit subsystem, hosted tracing, LangSmith, telemetry, and analytics are outside the MVP. | Must | Accepted | Content-safe local-diagnostic inspection |
| REQ-QUAL-006 | Before the selected local configuration is used for the portfolio evaluation, the developer must complete one manual representative end-to-end smoke run on the reference machine described in [Local MVP feasibility](../../LOCAL_MVP_FEASIBILITY.md). It passes when the workflow completes without out-of-memory failure or an unusable single-reviewer interface. Continuous monitoring, an in-app hardware profiler, repeated capacity runs, and performance qualification are not required. | Must | Accepted | One documented manual reference-PC smoke run |
| REQ-QUAL-007 | Manual evaluation notes may record whether the smoke run completed, an approximate elapsed time, and any obvious limitation. The application need not generate a diagnostic or Markdown report, and the notes must not claim latency budgets, throughput, percentile values, thermal stability, public performance, or generalized hardware support. | Must | Accepted | Manual evaluation-note inspection |
| REQ-QUAL-008 | The MVP supports one local user and one current item. Multi-user concurrency, high availability, and cloud-scale operation are deferred. | Must | Accepted | Scope inspection |
| REQ-QUAL-009 | A proposed local model must be plausibly within the reference PC's published memory and storage envelope and then pass the one manual smoke run in `REQ-QUAL-006`. This is a one-time development/evaluation decision, not an in-application capacity gate, hardware monitor, benchmark suite, or public support claim. A clearly oversized model is not downloaded for evaluation. | Must | Accepted | Candidate metadata and manual-smoke inspection |
| REQ-QUAL-010 | Basic runtime validation is required where unknown data actually enters the MVP: URL syntax, the axe result, the actual local or Groq provider response, and `run.json` when it is read. Validate the structure and only the semantic fields consumed by the next step. Compile-time types and successful JSON parsing are insufficient, but DNS/redirect admission, generalized schema validation, provider probing, and hostile-target validation are outside the MVP. | Must | Accepted and narrowed through OD-021 | Malformed URL, scanner, actual-provider-response, and run-record inspection |
| REQ-QUAL-011 | The MVP must keep one minimal application-owned TypeScript definition for the `run.json` aggregate and small boundary definitions for actual external responses. Incoming values are treated as unknown and checked before use. Independently versioned child contracts, JSON Schema, code generation, a schema framework, migrations, multiple schema dialects, and compatibility machinery are not required. | Must | Accepted | Aggregate-definition and boundary-check inspection |
| REQ-QUAL-012 | One user-requested operation runs at a time. A runtime scan owns the entire top-level document in its current rendered state at the configured readiness condition for one developer-selected public HTTPS page, one exact three-rule profile, and one complete scan result; a fixed evaluation substitutes one enumerated project-owned fixture revision. Retrieval and generation then operate sequentially for one selected Finding, and review remains one proposal at a time. The parent PageAnalysisRun execution status is only `running`, `completed`, or `failed`; each Finding retains its separate downstream lifecycle defined in [Information and workflow lifecycle](../INFORMATION_AND_WORKFLOW_LIFECYCLE.md). Timeout, shutdown, incomplete coverage, malformed top-level output, or a fatal top-level failure in result validation or evidence capture that prevents the complete bounded scan collection fails the parent and cannot publish partial success. An individual missing, invalid, or withheld allowlisted fact instead preserves its Finding or ScannerReviewObservation with the concise category or sufficiency reason required by `REQ-SCAN-005`; it does not by itself fail the parent. Initial and later persistence failures follow `REQ-QUAL-001`. A user may start a new run after failure; no resume, child-attempt lineage, or audit trail is required. | Must | Accepted and narrowed through OD-021 | Sequential scan, selected-finding, lifecycle, timeout, and visible-failure inspection |
| REQ-QUAL-013 | The MVP uses one local application service with an unprivileged browser UI. Outside the application workflow, the developer manually installs and starts Ollama, acquires `embeddinggemma` for retrieval in either generation mode, acquires `qwen3.5:4b` only for Local generation, and supplies a Groq credential only for Groq generation. For an eligible finding, the application makes the one actual call requested to the selected provider and reports an unavailable runtime, invalid response, or provider failure visibly. It does not add a readiness probe, capability-preflight service, model manager, hardware monitor, connection-level egress proxy, helper supervisor, worker pool, process orchestration, automatic restart, queue, automatic retry, or provider fallback. | Must | Accepted and narrowed through OD-021 | Conditional manual setup, actual-call, failure, and component-scope inspection |
| REQ-QUAL-014 | If production hostile-target hardening is later accepted, it must freeze finite bounds for target and redirect input, navigation and scan duration, browser requests and transferred resources, DOM and axe result size, finding count, retained per-finding evidence, provider input, temporary storage, and cleanup. The portfolio MVP instead requires only its simple configured timeout and the complete-result semantics in `REQ-QUAL-012` and `REQ-QUAL-019`. | Must | Deferred through OD-021 | Reconsider if hostile or arbitrary URL support is required |
| REQ-QUAL-015 | A release-grade exact component inventory, including artifact digests, signing state, removal ownership, audit references, and formal support states, is deferred until packaging or release authorization. | May | Deferred | Release-stage decision |
| REQ-QUAL-016 | Packaged/native matrices, installer lifecycle validation, signing verification, formal exact-host qualification, and public-release evidence are deferred. Deterministic MVP checks and portfolio observations must not be presented as substitutes. | May | Deferred | Packaging or release-stage decision |
| REQ-QUAL-017 | Before production hostile-target support, the project must define how each accepted resource/result bound is enforced and freeze below-limit, exact-limit, and maximum-plus-one verification cases, including cleanup and no-partial-publication expectations. This qualification is not required for the trusted-input portfolio MVP. | Must | Deferred through OD-021 | Reconsider with a hostile-target or production-readiness decision |
| REQ-QUAL-018 | A generated state-machine authority spanning services, serialized records, UI projections, and documentation is deferred. The three-state parent PageAnalysisRun execution lifecycle in `REQ-QUAL-012` is the MVP run authority; [Information and workflow lifecycle](../INFORMATION_AND_WORKFLOW_LIFECYCLE.md) separately defines the nested Finding lifecycle. | May | Deferred | Reconsider if a second lifecycle consumer creates drift |
| REQ-QUAL-019 | A completed scan must record that the entire developer-selected top-level document reached the configured readiness condition and that axe executed exactly `image-alt`, `label`, and `color-contrast` over that document's current rendered state with iframe documents excluded. It must publish every violation node reported by those rules and every relevant native `incomplete` observation returned by the completed scan. Missing rule coverage, unexpected rule execution, malformed top-level output, a fatal failure in evidence capture that prevents the complete bounded collection, or silent result loss must fail the scan and must never appear as a successful zero-finding or truncated result. An individual missing, invalid, or withheld allowlisted fact follows `REQ-SCAN-005`: preserve the Finding or ScannerReviewObservation with its concise category or sufficiency reason. This coverage does not include inactive or non-rendered states. | Must | Accepted and narrowed through OD-021 | Exact-rule coverage, current-rendered-state, malformed-result, result-preservation, and zero-finding inspection |
| REQ-QUAL-020 | Completed minimized evidence for every Finding in a valid scan must remain durable and addressable through its owning `findingId`. Retrieval, evidence-sufficiency abstention, generation, provider failure, or review for one selected Finding must not erase, overwrite, or change the eligibility or result of another Finding. No evidence ID, aggregate partial-success state, fan-out scheduler, parallel worker, or workflow engine is required. | Must | Accepted and narrowed through OD-022 | Finding-isolation, failure, abstention, and preservation inspection |

Each public-page runtime scan remains limited to the entire top-level document in its current rendered state at the configured readiness condition for one trusted developer-supplied non-authenticated public HTTPS page and exactly the three accepted rules; fixed evaluations use only the enumerated project-owned fixtures. Activation of inactive states, link discovery, following navigation as scan targets, crawling, authentication, imported browser state, arbitrary protocols, multi-page intake, scheduled monitoring, and crawler implementation are not reliability obligations. Hostile or private targets are unsupported rather than production-qualified.

## Deferred exhaustive bounds and enforcement proof

OD-021 removes the production hostile-target bound manifest, connection-gate proof, and below/exact/maximum-plus-one matrix from the portfolio MVP readiness gate. The initial implementation needs one simple configured navigation/scan timeout, cleanup on every terminal path, and the complete-result semantics in `REQ-QUAL-012` and `REQ-QUAL-019`. Exact URL, redirect, request, transfer, DOM, finding-count, evidence-size, memory, process, and temporary-storage ceilings are Deferred until hostile or arbitrary URL support, production distribution, or observed resource failure creates a demonstrated need.

## Accepted manual local-capacity smoke

Before the fixed local-generation evaluation, the developer checks the selected model's published artifact and storage information and performs one representative evidence-sufficient finding workflow on the documented reference PC. The manual note records the machine identity, local runtime and model identity, whether the workflow completed without out-of-memory failure, whether the interface remained usable, and any obvious limitation. No application feature samples GPU, CPU, memory, temperature, or paging, and no repeated benchmark or automated capacity harness is required.

The three fixed local happy-path cases remain functional portfolio evaluations after that one-time smoke; they are not three additional capacity qualifications. The public-page check still exercises URL parsing, the fresh context, complete exact-three-rule scanning, all-finding preservation, and visible failure without turning findings into an automatic generation batch. The three Groq cases validate the shared output contract separately and are not local-capacity evidence. Neither mode is ranked against the other, and no performance, provider, hardware-support, or release claim follows from these observations.

## Public-page reliability source basis

- Playwright documents isolated [browser contexts](https://playwright.dev/docs/browser-contexts) and navigation failure behavior in [`page.goto`](https://playwright.dev/docs/api/class-page#page-goto).
- The version-pinned axe-core [API documentation](https://github.com/dequelabs/axe-core/blob/v4.13.0/doc/API.md) defines `axe.run`, result categories, rule selection, and output used at the scanner boundary.

## Documentation navigation

- Previous: [Privacy and security requirements](PRIVACY_AND_SECURITY.md)
- Up: [Quality, security, and operations requirements](README.md)
- [Project requirements index](../../PROJECT_REQUIREMENTS.md)
- [Project documentation index](../../README.md)
