# Reliability, reproducibility, and operations requirements

## Authority and use

This document is a focused canonical module within [Quality, security, and operations requirements](README.md) and the authoritative requirements baseline indexed by [Project requirements](../../PROJECT_REQUIREMENTS.md). The index defines status vocabulary, priority semantics, ID stability, and precedence. This file describes planned behavior, not implemented behavior; each identified row's recorded status controls.

## Decision history

OD-010, OD-011, OD-015, and OD-018 were resolved on 2026-08-25. Rows below that moved from Proposed to Accepted define only the proportional MVP controls. Rows moved from Proposed to Deferred retain their stable IDs for a later, explicitly authorized performance, support, packaging, or release stage.

`REQ-QUAL-010` was already Accepted, but OD-015 narrows its earlier broad interpretation: MVP runtime validation is required at the axe result, Groq response, local-runtime response, and persisted-JSON read boundaries. It does not require a universal schema platform. `REQ-QUAL-009` remains the accepted local-model capacity gate; passing it admits an evaluation candidate and does not qualify a supported release dependency.

[OD-020](../DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-020--authorized-public-page-analysis-scope) supersedes the synthetic-only and one-rule-per-scan wording retained under stable IDs `REQ-QUAL-003` and `REQ-QUAL-012`. One public-page scan now owns one developer-selected public HTTPS main page and the exact three-rule profile, while controlled fixtures remain the fixed evaluation baseline. All downstream retrieval, generation, abstention, and review work remains sequential for one selected finding at a time. [OD-021](../DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-021--trusted-operator-url-boundary-for-the-portfolio-mvp) and [ADR-0018](../../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md) replace ADR-0017's hostile-target expansion: `REQ-QUAL-010`, `REQ-QUAL-012`, `REQ-QUAL-013`, and `REQ-QUAL-019` are narrowed below, while the exhaustive bound manifest in `REQ-QUAL-014` and its exact-limit/no-bypass proof in `REQ-QUAL-017` are Deferred. They no longer block development of the trusted-input portfolio slice.

## Reliability, reproducibility, and operations

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-QUAL-001 | Every validated stage result and human decision must be written as complete canonical data within its workflow-run record before it is treated as durable. An internal stage result is not another operation lifecycle and does not complete the workflow. Interruption must not overwrite an earlier durable record. An explicit retry creates a new linked workflow run; in-flight resume is not required. | Must | Accepted | Interruption and new-run retry inspection |
| REQ-QUAL-002 | Completed deterministic scan evidence must remain available when a later retrieval, generation, review, comparison, or optional diagnostic action fails. | Must | Accepted | Stage-failure inspection |
| REQ-QUAL-003 | Each fixed MVP evaluation case must record the material identities needed to interpret it: canonical controlled-fixture profile and revision, exact three-rule scan profile and selected finding, browser and scanner, corpus snapshot and retrieval configuration, prompt/output contract, selected provider/model/runtime configuration when a call occurred, and application revision. A separate public-page run must instead record its normalized requested and observed final target identities. A global generation mode without an invocation is context, not provider-call evidence. This provenance is portfolio evidence, not release qualification. | Must | Accepted | Fixed-manifest and public-run provenance inspection |
| REQ-QUAL-004 | Automated corpus update, index replacement, and recovery behavior are deferred. The MVP uses one fixed, approved corpus snapshot; changing it creates a new snapshot and requires rerunning affected fixed cases rather than an in-place production update. | Could | Deferred | Reconsider if mutable corpus management is authorized |
| REQ-QUAL-005 | Diagnostics must stay local, identify the failing stage, and exclude secrets, raw provider payloads, excluded page content, credentials, and arbitrary input values. Hosted tracing, LangSmith, telemetry, and analytics are outside the MVP. | Must | Accepted | Content-safe local-record inspection |
| REQ-QUAL-006 | The three local-mode happy-path cases must complete sequentially on the reference machine described in [Local MVP feasibility](../../LOCAL_MVP_FEASIBILITY.md), without out-of-memory failure or making the single-reviewer interface unusable. Observed stalls, failures, and practical limitations must be recorded without converting them into performance or support claims. | Must | Accepted | Reference-PC practical-capacity observation |
| REQ-QUAL-007 | MVP reporting may include bounded descriptive observations such as whether each stage completed, elapsed time observed for that run, and readily observable resource pressure. It must not define or claim latency budgets, throughput, p95 values, thermal stability, public performance, or generalized hardware support. | Must | Accepted | Portfolio-report wording inspection |
| REQ-QUAL-008 | The MVP supports one local user and one current item. Multi-user concurrency, high availability, and cloud-scale operation are deferred. | Must | Accepted | Scope inspection |
| REQ-QUAL-009 | A local model configuration must pass the two-stage reference-PC capacity gate in ADR-0004 before it becomes an evaluation candidate or is recommended for the MVP. Configurations that clearly exceed the metadata envelope are excluded without download; a borderline candidate receives only the bounded on-device preflight. Passing does not create a public support claim. | Must | Accepted | Candidate-list and capacity-gate inspection |
| REQ-QUAL-010 | Runtime validation is required where unknown data crosses the MVP boundaries: basic URL parsing, axe results, Groq responses, local-model-runtime responses, and persisted JSON read back from a run directory. Compile-time TypeScript types, SDK types, successful parsing, and successful transpilation are not runtime validation. DNS, redirect-destination, and hostile-target admission are not MVP validation boundaries. | Must | Accepted and narrowed through OD-021 | Malformed URL, scanner, provider, and persisted-record inspection |
| REQ-QUAL-011 | The MVP must keep a minimal application-owned TypeScript record definition for each persisted or external boundary shape, treat incoming data as unknown, perform structural validation, and apply the few semantic checks required by the consuming stage. JSON Schema, schema code generation, a schema framework, multiple schema dialects, migrations, and a compatibility framework are not required. | Must | Accepted | Record-definition and boundary-check inspection |
| REQ-QUAL-012 | One user-requested operation runs at a time. A runtime scan operation owns one developer-selected public HTTPS main page, one exact three-rule profile, and one complete scan result; a fixed evaluation scan substitutes one enumerated project-owned fixture revision for the developer-supplied target. Afterward, retrieval or generation operates sequentially for one selected finding; review also remains one proposal at a time. Each operation has only `running`, `completed`, or `failed`. The simple configured timeout, shutdown, stale ownership, incomplete coverage, malformed output, or persistence failure fails the current operation without publishing partial success. Retry creates a new linked run or attempt and never overwrites the earlier result. | Must | Accepted and narrowed through OD-021 | Sequential scan, selected-finding, lifecycle, timeout, and failure inspection |
| REQ-QUAL-013 | The MVP must use one local application service with an unprivileged browser UI and an independently owned local model runtime through its adapter. No connection-level egress proxy, helper supervisor, worker pool, process-orchestration layer, automatic restart, queue, or provider failover is required. | Must | Accepted and narrowed through OD-021 | Component and process-ownership inspection |
| REQ-QUAL-014 | If production hostile-target hardening is later accepted, it must freeze finite bounds for target and redirect input, navigation and scan duration, browser requests and transferred resources, DOM and axe result size, finding count, retained per-finding evidence, provider input, temporary storage, and cleanup. The portfolio MVP instead requires only its simple configured timeout and the complete-result semantics in `REQ-QUAL-012` and `REQ-QUAL-019`. | Must | Deferred through OD-021 | Reconsider if hostile or arbitrary URL support is required |
| REQ-QUAL-015 | A release-grade exact component inventory, including artifact digests, signing state, removal ownership, audit references, and formal support states, is deferred until packaging or release authorization. | Could | Deferred | Release-stage decision |
| REQ-QUAL-016 | Packaged/native matrices, installer lifecycle validation, signing verification, formal exact-host qualification, and public-release evidence are deferred. Deterministic MVP checks and portfolio observations must not be presented as substitutes. | Could | Deferred | Packaging or release-stage decision |
| REQ-QUAL-017 | Before production hostile-target support, the project must define how each accepted resource/result bound is enforced and freeze below-limit, exact-limit, and maximum-plus-one verification cases, including cleanup and no-partial-publication expectations. This qualification is not required for the trusted-input portfolio MVP. | Must | Deferred through OD-021 | Reconsider with a hostile-target or production-readiness decision |
| REQ-QUAL-018 | A generated state-machine authority spanning services, serialized records, UI projections, and documentation is deferred. The accepted three-state lifecycle in `REQ-QUAL-012` and [Information and workflow lifecycle](../INFORMATION_AND_WORKFLOW_LIFECYCLE.md) is the MVP authority. | Could | Deferred | Reconsider if a second lifecycle consumer creates drift |
| REQ-QUAL-019 | A completed scan must prove that the developer-selected main document reached the configured readiness condition and that axe executed exactly `image-alt`, `label`, and `color-contrast` over the declared scope. It must publish every violation node reported by those rules and every relevant native `incomplete` observation returned by the completed scan. Missing rule coverage, unexpected rule execution, malformed output, or silent result loss must fail the scan and must never appear as a successful zero-finding or truncated result. | Must | Accepted and narrowed through OD-021 | Exact-rule coverage, malformed-result, result-preservation, and zero-finding inspection |
| REQ-QUAL-020 | Completed minimized evidence for every finding in a valid scan must remain durable and independently addressable. Retrieval, evidence-sufficiency abstention, generation, provider failure, or review for one selected finding must not erase, overwrite, or change the eligibility or result of another finding. No aggregate partial-success state, fan-out scheduler, parallel worker, or workflow engine is required. | Must | Accepted | Finding-isolation, failure, abstention, and preservation inspection |

Each public-page runtime scan remains limited to one trusted developer-supplied non-authenticated public HTTPS main page and exactly the three accepted rules; fixed evaluations use only the enumerated project-owned fixtures. Link discovery, following navigation as scan targets, crawling, authentication, imported browser state, arbitrary protocols, multi-page intake, scheduled monitoring, and crawler implementation are not reliability obligations. Hostile or private targets are unsupported rather than production-qualified.

## Deferred exhaustive bounds and enforcement proof

OD-021 removes the production hostile-target bound manifest, connection-gate proof, and below/exact/maximum-plus-one matrix from the portfolio MVP readiness gate. The initial implementation needs one simple configured navigation/scan timeout, cleanup on every terminal path, and the complete-result semantics in `REQ-QUAL-012` and `REQ-QUAL-019`. Exact URL, redirect, request, transfer, DOM, finding-count, evidence-size, memory, process, and temporary-storage ceilings are Deferred until hostile or arbitrary URL support, production distribution, or observed resource failure creates a demonstrated need.

## Accepted practical local-capacity observation

The fixed MVP manifest exercises one selected evidence-sufficient finding for each of the three rule families, sequentially, using the capacity-screened local configuration and the project-owned controlled fixture revisions. A separate trusted-input public-page check exercises basic URL parsing, the fresh-context boundary, and exact-rule scanning; it is not evaluation-gold input or hostile-target qualification. Every complete scan preserves every returned finding, and the three generation examples do not turn into automatic bulk generation. For each run, record the exact reference-PC and software/model identities, whether all stages completed, any out-of-memory or visibly unusable-interface condition, and observed limitations. Elapsed times and readily observable resource facts may be included as descriptive context only.

The three Groq happy-path cases validate the same application-owned output contract separately and are not local-capacity evidence. Neither mode is ranked against the other. Formal repetitions, percentile statistics, thermal protocols, provider leaderboards, and performance/support thresholds require a later decision and are not inferred from this compact portfolio manifest.

## Public-page reliability source basis

- Playwright documents isolated [browser contexts](https://playwright.dev/docs/browser-contexts), context-wide [network routing](https://playwright.dev/docs/network), navigation failure behavior in [`page.goto`](https://playwright.dev/docs/api/class-page#page-goto), and separate [WebSocket routing](https://playwright.dev/docs/api/class-browsercontext#browser-context-route-web-socket).
- The version-pinned axe-core [API documentation](https://github.com/dequelabs/axe-core/blob/v4.13.0/doc/API.md) defines `axe.run`, result categories, rule selection, and output used at the scanner boundary.
- OWASP's [Server-Side Request Forgery Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html) remains relevant to the Deferred hostile-target boundary; OD-021 does not claim those protections for the portfolio MVP.

## Documentation navigation

- Previous: [Privacy and security requirements](PRIVACY_AND_SECURITY.md)
- Up: [Quality, security, and operations requirements](README.md)
- [Project requirements index](../../PROJECT_REQUIREMENTS.md)
- [Project documentation index](../../README.md)
