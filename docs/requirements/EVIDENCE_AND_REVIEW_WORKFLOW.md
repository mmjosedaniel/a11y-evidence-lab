# Evidence and review workflow requirements

## Authority and use

This document is part of the authoritative requirements baseline indexed by [Project requirements](../PROJECT_REQUIREMENTS.md). The index defines status vocabulary, priority semantics, ID stability, and precedence. This file describes planned behavior, not implemented behavior; each identified row's recorded status controls.

## Current workflow boundary and decision history

**Status:** [OD-020](DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-020--authorized-public-page-analysis-scope) accepts one public HTTPS page, one provider-independent scan with exactly `image-alt`, `label`, and `color-contrast`, a complete variable collection of violation-node Findings, separate native `incomplete` ScannerReviewObservations, one immutable global Local-or-Groq mode context, and one selected FindingWorkflow at a time. [OD-021](DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-021--trusted-operator-url-boundary-for-the-portfolio-mvp) and [ADR-0018](../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md) replace its production hostile-target controls with a trusted developer-input portfolio boundary.

OD-020 supersedes OD-019's synthetic-only, one-profile/one-rule/one-target runtime boundary and the applicable cardinality portion of OD-018. OD-021 supersedes OD-020 only where it required a separate authorization attestation, hostile-target isolation, public-address enforcement, connection-level egress gating, and exhaustive pre-development resource qualification. OD-003 and OD-019 remain preserved as controlled evaluation-fixture history. Rows marked **Superseded** below retain their stable IDs and identify their replacements rather than silently changing accepted history.

The public-page scope does not authorize implementation by itself. The developer owns target authorization, and hostile or private pages are unsupported. The proportional browser, evidence, provider, and data-flow boundaries in ADR-0018 and the owning requirements apply; the production hardening formerly required by ADR-0017 no longer blocks this portfolio slice.

## Workflow-step technical assessments

The requirement rows and statuses in this module are canonical. The linked assessments remain Proposed technical research; they cannot override an identified requirement or accepted decision.

| Planned workflow step | Canonical requirements | Proposed technical assessment |
| --- | --- | --- |
| Scan one trusted developer-input public page with exactly three deterministic checks | [Target authorization and scanning](#target-authorization-and-scanning) | [Authorized deterministic web scan assessments](../architecture/candidates/authorized-scan/README.md) |
| Capture every Finding and the minimized evidence behind it | [Evidence and provenance](#evidence-and-provenance) | [Accessibility finding evidence-capture assessment](../architecture/candidates/ACCESSIBILITY_FINDING_EVIDENCE_CAPTURE_ASSESSMENT.md) |
| Retrieve relevant guidance for one selected Finding | [Corpus and retrieval](#corpus-and-retrieval) | [Accessibility guidance retrieval assessments](../architecture/candidates/guidance-retrieval/README.md) |
| Generate one structured evidence-grounded proposal or abstention | [Generated explanations and remediation proposals](#generated-explanations-and-remediation-proposals) | [Evidence-grounded remediation-generation assessment](../architecture/candidates/EVIDENCE_GROUNDED_REMEDIATION_GENERATION_ASSESSMENT.md) |
| Present one proposal for individual human review | [Human review and manual checks](#human-review-and-manual-checks) | [Human remediation-review assessment](../architecture/candidates/HUMAN_REMEDIATION_REVIEW_ASSESSMENT.md) |
| Rescan the same page and compare conservatively | [Rescan and comparison](#rescan-and-comparison) | [Rescan evidence-comparison assessment](../architecture/candidates/RESCAN_EVIDENCE_COMPARISON_ASSESSMENT.md) |

### Runtime handoff boundaries

| From | To | Minimum handoff |
| --- | --- | --- |
| Target intake | Scan | One basically parsed developer-supplied public HTTPS target, explicit Analyze action, trusted-input limitation, single-page scope, exact three-rule scan profile, and immutable global generation-mode context. |
| Scan | Evidence capture | One runtime-validated transient native axe result plus page/scan/coverage provenance. The scan step creates no durable normalized finding by itself. |
| Evidence capture | Results interface | One complete Finding collection containing every retained violation node, plus a distinct ScannerReviewObservation collection for native `incomplete` results and exact rule-coverage status. |
| Selected Finding | Retrieval | One selected Finding, its minimized sanitized source evidence, exact rule metadata, and approved rule-to-guidance mapping. No sibling finding content enters the query. A Proposed target-correlation descriptor is relevant only to a later comparison. |
| Retrieval | Generation | One completed per-finding retrieval record containing the privacy-safe query representation, corpus snapshot, ranked passages, citation metadata, and support state. |
| Generation | Review | One structurally valid, clearly AI-generated finding-specific proposal with inspectable references, or one deterministic abstention with its manual-review direction. A ProviderInvocation exists only when a call was attempted. |
| Review | Rescan/comparison | One immutable finding-specific review action, accepted plan when one exists, reusable manual-check definitions, and completed result occurrences; none is automated proof. |
| Later scan | Comparison | One complete baseline/later page pair, the Proposed conservative target-correlation evidence for each compared finding, any required later positive observation, and rule-specific measurements. |

### Eligibility and independence gates

| Gate | Minimum rule |
| --- | --- |
| Scan completion | All three accepted rules must execute under the recorded profile and every returned violation node must cross the validated minimized-evidence boundary. Missing, invalid, incomplete, or silently truncated coverage cannot complete. A complete scan may truthfully contain zero Findings. |
| Scanner observation classification | One axe violation node becomes one Finding. A native `incomplete` node becomes a ScannerReviewObservation and is never proposal-eligible. Native `passes` or `inapplicable` results are not general finding-list entries; retain only a narrow positive observation required for an identified comparison target. |
| Finding selection | Every Finding remains visible, but only one FindingWorkflow may be selected and active at a time. No automatic fan-out, combined query, queue, or bulk action follows from the collection. |
| Retrieval to generation | Only complete required evidence plus a completed `supported` retrieval may invoke the run's selected provider. `incomplete`, `missing`, or `conflicting` support creates a finding-specific abstention without a call. A retrieval execution error is a workflow failure and has no support state. |
| Generation to proposal review | Only a validated `proposal` branch with resolvable evidence/passage references and no prohibited claim enters pending review. An abstention is not approvable. |
| Proposal to accepted plan | Every material claim and remediation step must be confirmed by the reviewer, and every applicable blocking manual check must satisfy `REQ-REV-006`. Approval affects only that proposal/Finding. |
| Scan pair to comparison | Accepted boundary: both scans must be complete, ambiguity must never be forced, and absence from a later violation list is never sufficient for `resolved`. Proposed public-page detail: correlation uses an exact unique match under a future frozen versioned descriptor. |

## Target authorization and scanning

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-AUTH-001 | Historical requirement: before the project-owned fixture is scanned, the user must explicitly confirm the fixture ID and that the run is limited to that local synthetic fixture. The scan record must retain the attestation time and scope; no legal-ownership workflow or credential is required for this slice. | Must | Superseded by `REQ-AUTH-005` | Decision-history inspection |
| REQ-AUTH-002 | Historical requirement: the MVP must accept only a selected project-owned synthetic controlled fixture revision and must not accept a live target or arbitrary URL, crawl links, discover additional targets, or contain a crawler implementation. Live-target work requires a later explicit scope decision and the gated requirements for that distinct capability. | Must | Superseded by `REQ-AUTH-006` | Decision-history inspection |
| REQ-AUTH-003 | Historical proposed requirement: the product must record the scenario identifier, fixture and revision identifiers, target identifier, declared scope, time, page-state description, viewport, locale, browser version, scanner version, exact rule configuration, and relevant preconditions for every scan. | Must | Superseded by `REQ-AUTH-005` | Decision-history inspection |
| REQ-AUTH-004 | Historical deferred requirement: any later live-target support must restrict URL schemes, redirects, network destinations, resource use, and browser capabilities according to an approved threat model. | Should | Superseded by `REQ-AUTH-006` and ADR-0017 | Decision-history inspection |
| REQ-AUTH-005 | Historical requirement: before a PageAnalysisRun starts, the user must provide one public HTTPS page URL, explicitly attest that they are authorized to analyze that single page, and activate one **Analyze** action. The run must retain the attestation time/scope, normalized requested and validated final page identities, scan time, page-state/readiness description, viewport, locale, browser/scanner versions, exact three-rule configuration, applied URL/network-policy version, and relevant preconditions without retaining URL credentials or another prohibited URL component. The global generation-mode selection is governed separately by `REQ-LLM-002`. | Must | Superseded by `REQ-AUTH-007` | Decision-history inspection |
| REQ-AUTH-006 | Historical requirement: the MVP must accept only one public HTTPS page per PageAnalysisRun and enforce the public-address, DNS, redirect, browser-capability, resource, navigation, download, and network containment boundary accepted in ADR-0017. It must not accept authentication material or private-network targets, follow links, discover another page, crawl, or contain a crawler implementation. | Must | Superseded by `REQ-AUTH-008` | Decision-history inspection |
| REQ-AUTH-007 | Before a PageAnalysisRun starts, the developer must provide one non-authenticated public HTTPS page URL that they are responsible for choosing and are permitted to analyze, select the global generation mode governed by `REQ-LLM-002`, and activate one **Analyze** action. The local service must perform basic standards-based parsing, reject malformed input, URL credentials, and unsupported schemes, and retain the normalized requested and observed final page identities, scan time, page-state/readiness description, viewport, locale, browser/scanner versions, exact three-rule configuration, and relevant preconditions. It does not independently prove ownership, public reachability, destination class, or hostile-page safety, and it collects no separate authorization attestation, confirmation, or acknowledgement record. | Must | Accepted through OD-021 and ADR-0018 | Initiation, basic-input, provenance, and limitation checks |
| REQ-AUTH-008 | Each PageAnalysisRun must inspect only the developer-selected main document with exactly the three supported rules in a fresh non-persistent managed-browser context without imported cookies, storage state, permissions, or personal browser profile. The application must not discover or follow links as scan targets, submit forms, upload files, interact with controls, permit downloads, crawl, or create another page-analysis target. A simple configured navigation/scan timeout and cleanup path must close the page, context, and managed browser after success or failure. Ordinary page redirects and subresource loading are not a security-isolated boundary; intentionally hostile, authenticated, private, or non-public targets are unsupported. | Must | Accepted through OD-021 and ADR-0018 | Single-page, no-state, no-interaction, timeout, cleanup, and limitation checks |
| REQ-SCAN-001 | The product must run a versioned deterministic accessibility scanner and preserve its source evidence separately from any model alteration. | Must | Accepted | Deterministic test |
| REQ-SCAN-002 | For each controlled evaluation scenario, the product must preserve violations and results requiring manual review. It must also retain exactly the minimal positive same-target observation and rule-specific scanner-emitted values required to correlate the corrected scan; unrelated passes and inapplicable results remain excluded. This row governs controlled evaluation inputs only; `REQ-SCAN-006` governs public-page result completeness. | Must | Proposed | Deterministic test |
| REQ-SCAN-003 | Historical requirement: a failed or coverage-incomplete scan must be labeled as such and must never appear as a successful run with zero findings. | Must | Superseded by `REQ-SCAN-006` | Decision-history inspection |
| REQ-SCAN-004 | Before evaluation, each scenario's failing and corrected content, expected native rule result, stable fixture-target key, viewport/browser profile, and scanner/rule profile must be frozen in a versioned manifest. A change creates a new manifest version rather than silently changing an evaluated case. | Must | Accepted | Manifest inspection |
| REQ-SCAN-005 | Scanner output must be deterministically allowlisted and sanitized before persistence. The record must identify the policy version and any omitted, transformed, or withheld field categories; a separate detailed redaction artifact is required only when content-bearing evidence is actually transformed or withheld. Unredacted output may exist only transiently unless the accepted data policy explicitly permits protected retention. Exact public-page field allowlists remain Proposed and must be frozen before implementation. | Must | Proposed | Privacy and data-integrity test |
| REQ-SCAN-006 | One provider-independent scan must execute exactly `image-alt`, `label`, and `color-contrast`, runtime-validate their results, retain every returned violation node as an independent Finding, retain native `incomplete` nodes separately as ScannerReviewObservations, and record complete per-rule coverage. The product must not silently merge, drop, or truncate findings. If navigation, scanner execution, result validation, evidence capture, timeout, or persistence prevents complete collection, the operation fails; no zero-finding or complete-result claim is permitted. | Must | Accepted through OD-020 and narrowed by OD-021 | Multi-node, valid-zero, timeout, malformed-result, and coverage-failure checks |
| REQ-SCAN-007 | Scanner execution, result collection, and evidence capture must be independent of the PageAnalysisRun's Local-or-Groq context. Provider selection must not cause a provider call, alter the rule set, or suppress a Finding or ScannerReviewObservation. | Must | Accepted through OD-020 | Mode-isolation and network inspection |

## Evidence and provenance

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-EVID-001 | Each PageAnalysisRun, scan, Finding, ScannerReviewObservation, evidence item, positive target observation, corpus snapshot, retrieval run, optional ProviderInvocation, generation result, proposal version, review action, manual check, and comparison must have a stable identifier within its applicable lineage. | Must | Accepted through OD-020 | Record and lineage inspection |
| REQ-EVID-002 | Each retained controlled-fixture finding or corrected-state evidence record must carry its scenario ID, scanner-rule ID, fixture revision and state role, stable fixture-target key, native result type, and only the minimized rule evidence and scanner-emitted values needed for that scenario. A locator or selector may support inspection but is not identity. This Accepted identity and minimization boundary remains the deterministic evaluation baseline; it does not define public-page correlation. | Must | Accepted | Fixture, identity, and privacy demonstration |
| REQ-EVID-003 | Persisted source evidence must be immutable while retained. Normalized or human-authored descriptions may be versioned but must not overwrite their source evidence; MVP run deletion is governed by REQ-SEC-006. Deferred tombstones, ledgers, and recoverable deletion history remain governed separately by REQ-SEC-010. | Must | Proposed | Data-integrity and deletion test |
| REQ-EVID-004 | The interface, and any export that is provided, must label page evidence, guidance, model interpretation, and reviewer-authored content as separate layers. | Must | Accepted | Inspection and user review |
| REQ-EVID-005 | Historical requirement: the synthetic-only MVP must minimize page evidence and must not retain full HTML, screenshots, accessibility trees, browser traces, cookies, credentials, entered values, arbitrary DOM snapshots, network logs, or private URLs. Only evidence permitted by the applicable minimized-evidence policy may cross the durable boundary; the exact rule-specific field allowlist remains Proposed under REQ-SCAN-002 and REQ-SCAN-005. | Must | Superseded by the broader `REQ-EVID-008` | Decision-history inspection |
| REQ-EVID-006 | The product must preserve end-to-end lineage from an accepted remediation plan or comparison outcome back to all material inputs and versions. | Must | Proposed | Traceability audit |
| REQ-EVID-007 | A Finding is exactly one axe violation node for one supported rule and one affected public-page target. Its within-run identity must include its PageAnalysisRun and scan references, exact rule ID, opaque finding ID, native result/check identity, and rule-specific evidence reference. Collection order and display index are not identity. | Must | Accepted through OD-020 | Multi-node identity and collection-order checks |
| REQ-EVID-008 | Public-page and controlled-evaluation evidence must be minimized per rule before persistence or provider use. Durable records must exclude the raw native scanner payload, raw/full HTML, DOM or accessibility-tree snapshots, screenshots, browser traces, cookies, credentials, form values, arbitrary page text, hidden content, network logs, and raw provider payloads. For a public-page run, the normalized target identity may appear only in target/run provenance allowed by the privacy policy; it must not enter Finding evidence, a ProviderInvocation, or a provider payload. Sanitized locator facts must be limited to the minimum allowed by the evidence and Proposed correlation policies. | Must | Accepted; public-page scope narrowed through OD-021 and ADR-0018 | Fixture and public-run durable-record, payload, and negative-content inspection |
| REQ-EVID-009 | A ScannerReviewObservation must preserve its native `incomplete` category, rule/check identity, minimized target evidence, reason when available, and scan provenance while remaining visibly distinct from a Finding. It cannot enter retrieval, proposal generation, proposal review, or accepted-plan state. | Must | Accepted through OD-020 | Classification and proposal-ineligibility checks |
| REQ-EVID-010 | Public-page comparison should use a versioned minimized target-correlation descriptor that is separate from display order and never treated as permanent identity. The exact permitted fields, normalization, stability rules, and ambiguity thresholds must be frozen before implementation; missing, duplicate, changed, or ambiguous descriptors must never be forced into a match. | Must | Proposed | Correlation-design and ambiguity evaluation |

### Supported rule profiles and controlled evaluation inputs

Every runtime scan executes all three profiles and may retain multiple Findings per row. The controlled states are deterministic evaluation inputs only.

| Rule profile | Runtime minimized evidence | Minimum curated guidance | Model and human boundary | Conservative comparison |
| --- | --- | --- | --- | --- |
| `informative-image-alt` — axe `image-alt`, WCAG 2.2 SC 1.1.1 | Per violation node: native result/check identity, minimized target evidence, element kind, bounded sanitized `img`/`alt` state, and exact scanner/evidence provenance. Exact cross-scan descriptor fields remain Proposed. | Normative SC 1.1.1, Understanding SC 1.1.1, H37, and H67. | The model may explain the observed missing alternative and conditional treatments. A person determines actual purpose/context and appropriate final treatment. | Exact matched failure-to-failure is `persistent`; `resolved` needs a unique later same-target non-failing observation plus complete coverage. No `improved`. |
| `form-input-label` — axe `label`, WCAG 2.2 SC 4.1.2 | Per violation node: native result/check identity, minimized target evidence, input type, bounded label/name-source association facts, and no input value. Exact cross-scan descriptor fields remain Proposed. | Normative SC 4.1.2, Understanding SC 4.1.2, and H44. | The model may explain the missing programmatic name and applicable association patterns. A person judges clarity, accuracy, association, and additional instructions. | The same binary boundary applies; a changed but still-failing target is `persistent`, not `improved`. |
| `text-contrast` — axe `color-contrast`, WCAG 2.2 SC 1.4.3 | Per violation node: native result/check identity, minimized target evidence, axe-emitted foreground/background, measured and expected ratios, font size/weight, and measurement provenance. Any native `incomplete` result and reason remain in a separate ScannerReviewObservation. Exact cross-scan descriptor fields remain Proposed. | Normative SC 1.4.3, Understanding SC 1.4.3, and G18. | The model may explain the retained shortfall and conditional color changes. A person verifies text classification, exceptions, background/design meaning, and omitted states. | Native failing-to-non-failing may be `resolved`. Two exact matched failures use the retained contrast margin: higher `improved`, equal `persistent`, lower `regressed`; missing/mismatched evidence is `inconclusive`. |

The narrow rule mappings are grounded in axe-core's versioned [`image-alt`](https://github.com/dequelabs/axe-core/blob/v4.13.0/lib/rules/image-alt.json), [`label`](https://github.com/dequelabs/axe-core/blob/v4.13.0/lib/rules/label.json), and [`color-contrast`](https://github.com/dequelabs/axe-core/blob/v4.13.0/lib/rules/color-contrast.json) metadata and WCAG 2.2 [SC 1.1.1](https://www.w3.org/TR/WCAG22/#non-text-content), [SC 4.1.2](https://www.w3.org/TR/WCAG22/#name-role-value), and [SC 1.4.3](https://www.w3.org/TR/WCAG22/#contrast-minimum). One automated node never proves complete success-criterion or page-level non-conformance.

## Corpus and retrieval

**Source-pack decision:** Accepted through OD-004. Runtime public-page scope does not expand the curated corpus or authorize web search, crawling, or recursive source loading.

The shared first-slice corpus remains the closed eight-artifact W3C guidance pack:

1. Normative [WCAG 2.2 Recommendation dated 12 December 2024](https://www.w3.org/TR/2024/REC-WCAG22-20241212/) sections for SC 1.1.1, SC 4.1.2, and SC 1.4.3 plus needed definitions.
2. [Understanding SC 1.1.1](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html).
3. [Technique H37](https://www.w3.org/WAI/WCAG22/Techniques/html/H37).
4. [Technique H67](https://www.w3.org/WAI/WCAG22/Techniques/html/H67).
5. [Understanding SC 4.1.2](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html).
6. [Technique H44](https://www.w3.org/WAI/WCAG22/Techniques/html/H44).
7. [Understanding SC 1.4.3](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html).
8. [Technique G18](https://www.w3.org/WAI/WCAG22/Techniques/general/G18).

WCAG success criteria are normative. Understanding documents and Techniques are informative and must not be presented as independent conformance requirements. The selected material must follow WAI's [Using WAI Material](https://www.w3.org/WAI/about/using-wai-material/) guidance and the applicable [W3C Document License](https://www.w3.org/copyright/document-license-2023/). Runtime page scanning does not crawl or add target-page content to the corpus.

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-CORP-001 | The product must retrieve only from the accepted eight-artifact source manifest and an identified immutable corpus snapshot. | Must | Accepted | Corpus inspection |
| REQ-CORP-002 | Every source must record title, publisher, exact and canonical URLs, source type and status, relevant standard/version or dated snapshot, publication or revision date when available, access date, copyright/attribution notice, license or use-condition URI, modification notes, content checksum, and corpus version. | Must | Accepted | Metadata validation |
| REQ-CORP-003 | Every passage must retain its exact heading/section locator and derivation lineage; a source or segmentation change must create a new version rather than silently changing an existing snapshot. | Must | Accepted | Rebuild and integrity test |
| REQ-CORP-004 | Source conflicts must be surfaced. Normative text takes precedence over informative guidance, while an unresolved material conflict makes guidance `conflicting` and requires abstention with a manual-review direction. | Must | Accepted | Evaluation cases |
| REQ-CORP-005 | Passage selection and segmentation must be manual, deterministic, and heading-aware, preserving success-criterion, Understanding-section, and Technique identity plus exact URLs and headings. The embedding adapter may vectorize selected passage text but must not select, split, overlap, merge, or rewrite canonical passages. | Must | Accepted | Rebuild and provenance inspection |
| REQ-CORP-006 | The first corpus build must remain closed to the eight accepted artifacts, use no crawling or recursive loader, and occur only after development and source acquisition are authorized. A failed validation or index build must not be represented as a usable snapshot. | Must | Accepted | Boundary and failed-build test |
| REQ-RETR-001 | Retrieval must use one selected Finding and its minimized evidence as structured input and return passages with source metadata, locators, relevance data, corpus version, and visible support state. | Must | Accepted | Retrieval evaluation |
| REQ-RETR-002 | The user must be able to open or inspect the exact passage supporting each material citation. | Must | Accepted | Demonstration |
| REQ-RETR-003 | An evaluation run must record the corpus/index identity, embedding configuration, filters, exact privacy-safe query representation, ranked passage identifiers and metrics, support rule, and result. Future TypeScript record definitions must be runtime-validated at the retrieval and persisted-JSON boundaries; a digest alone is not reproducible, and no JSON-Schema/code-generation framework is required for the MVP. | Must | Accepted | Trace, boundary, and privacy inspection |
| REQ-RETR-004 | A deterministic Finding must remain visible regardless of retrieval outcome. Only a completed `supported` result is proposal-eligible; `incomplete`, `missing`, or `conflicting` support must produce abstention and a manual-review direction without calling the model. | Must | Accepted | Adversarial evaluation |
| REQ-RETR-005 | Retrieval must process only the currently selected Finding. It must not include sibling findings, arbitrary target-page text, another URL, reviewer history, or a combined page-level query, and completion or failure must not change a sibling FindingWorkflow. | Must | Accepted through OD-020 | Isolation, query, and sibling-state checks |

The support states remain `supported`, `incomplete`, `missing`, and `conflicting`. A retrieval execution error is distinct: it fails the selected FindingWorkflow, has no support state, leaves the Finding/evidence visible, and must not be reported as missing guidance.

## Generated explanations and remediation proposals

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-GEN-001 | The application generation-stage output for one selected FindingWorkflow must use a validated discriminated structure whose result type is either **proposal** or deterministic **abstention**. A proposal contains the finding summary, evidence references, user-impact explanation, remediation proposal, material citations, evidence sufficiency, assumptions, and required manual checks. An evidence-sufficiency abstention is application-authored before provider invocation. | Must | Proposed | Schema test |
| REQ-GEN-002 | Before a proposal enters review, every material guidance claim must reference a resolvable retrieved passage and every scanner-observation claim must reference recorded evidence. Mechanical validation must reject missing or invented references and prohibited claims. The reviewer is the MVP semantic-support authority: an unsupported claim must be removed through edit-and-accept or the proposal must be rejected; if the available inputs cannot support a core conclusion, generation must abstain. | Must | Accepted | Citation validation and reviewer evaluation |
| REQ-GEN-003 | A proposal may be generated only from complete required finding evidence and a completed `supported` retrieval. Its separate model-confidence label must use the categories **high**, **medium**, or **low** with stated uncertainty; the product must not display an uncalibrated model probability. **Abstention** is a separate result type, not a confidence level. | Must | Accepted | Eligibility, structure, and labeling review |
| REQ-GEN-004 | Confidence describes the bounded model interpretation, not evidence sufficiency or accessibility status. Every confidence level must preserve assumptions and required manual checks; medium or low confidence must state the material uncertainty. Missing, incomplete, or conflicting core evidence or guidance must block the model call and produce abstention under REQ-RETR-004. | Must | Accepted | Evaluation rubric |
| REQ-GEN-005 | The product must distinguish what the scanner observed from what the model inferred and from what a person must verify. | Must | Accepted | User review |
| REQ-GEN-006 | Generated content must not claim certification, legal compliance, whole-page accessibility, complete success-criterion conformance or non-conformance, or that a finding is fixed solely because automated evidence changed. | Must | Accepted | Prohibited-claim evaluation |
| REQ-GEN-007 | Page content and retrieved content must be treated as untrusted data and must not be allowed to override system rules, invoke tools, or alter the approved workflow. | Must | Proposed | Prompt-injection evaluation |
| REQ-GEN-008 | Model, prompt template, generation parameters, structured-output schema, corpus snapshot, and exact input identifiers must be recorded without exposing hidden model reasoning. | Must | Proposed | Trace inspection |
| REQ-GEN-009 | An abstention result must contain the finding and evidence summary, retrieved support or conflict, missing information, abstention reason, and recommended manual escalation; it must not contain a remediation conclusion. | Must | Proposed | Schema and workflow test |
| REQ-GEN-010 | Every optional ProviderInvocation must reference the PageAnalysisRun's immutable global mode and exactly one selected FindingWorkflow. It may be created only when the user explicitly starts generation and one call is attempted, must never contain sibling Finding content, and must not change provider/model inside the run. Unprocessed findings and abstentions have no ProviderInvocation. | Must | Accepted through OD-020 | Invocation existence, isolation, and no-mixing checks |

## Human review and manual checks

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-REV-001 | Every generated remediation proposal must enter a pending-review state and must be approved, edited and accepted, or rejected by a person before it becomes an accepted remediation plan. | Must | Accepted | Workflow demonstration |
| REQ-REV-002 | Review history must preserve the original output, every human edit, decision, actor, timestamp, reason or note, and superseding version. | Must | Proposed | Audit test |
| REQ-REV-003 | Rejected proposals must remain in the audit record but must never appear as accepted remediation plans. | Must | Proposed | Workflow test |
| REQ-REV-004 | Rejection must not silently trigger regeneration or change the corpus. Regeneration, if later offered, must be an explicit action that creates a new PageAnalysisRun and linked FindingWorkflow/proposal version, without overwriting the prior run, feedback, or proposal. | Must | Proposed | Workflow test |
| REQ-REV-005 | A manual-check result must reference one immutable check definition and separately record execution status, reviewer, time, and evidence or notes. `pending` has no observed outcome or proposal relationship; `completed` requires an observed outcome and exactly one relationship from `supports`, `contradicts`, or `inconclusive`; `not applicable` requires a rationale and has neither an observed outcome nor a relationship. A post-change result may also reference its later scan and comparison. | Must | Proposed | Workflow demonstration |
| REQ-REV-006 | A blocking manual check is complete only when `completed` with a `supports` relationship, or `not applicable` with a rationale. `contradicts` or `inconclusive` blocks acceptance. The first-slice review has no exception workflow; the proposal must be edited when existing evidence supports the correction or rejected when new evidence or a new blocking check is needed. | Must | Proposed | State-transition test |
| REQ-REV-007 | An abstention may be acknowledged, routed to manual triage, or explicitly retried in a new PageAnalysisRun after inputs change. It must never enter an accepted-remediation-plan state. | Must | Proposed | Workflow test |
| REQ-REV-008 | Every proposal, edit, manual check, and review decision applies to exactly one Finding. Approving, editing, rejecting, or failing one FindingWorkflow must not change another. The MVP has no bulk approval/rejection, combined remediation proposal, or page-level accepted-plan state. | Must | Accepted through OD-020 | Individual-action and sibling-isolation checks |

## Rescan and comparison

OD-020 supersedes OD-008's fixed fixture-key **runtime** correlation while retaining the accepted controlled-fixture correlation baseline for evaluation. Exact public-page target correlation and comparison algorithms remain Proposed: correlation must be inspectable and conservative, and ambiguity must never be forced.

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-COMP-001 | For controlled-fixture evaluation, the product must determine whether two scans are comparable by examining scenario/profile, target and fixture-family identity, whether each revision and declared state is valid for its assigned baseline/later role and permitted transition, viewport, browser, scanner, exact rule, measurement profile when applicable, and other material configuration. The two revision identities and state roles need not be equal. | Must | Proposed | Fixture evaluation |
| REQ-COMP-002 | Controlled-fixture target correlation must use the scenario ID, exact rule ID, stable fixture-target key, fixture revision/state role, and minimized rule evidence. A selector or locator may support inspection but must not replace the stable project-owned key. Missing or ambiguous identity must produce `inconclusive` or pair-level `not comparable`, never a forced match. | Must | Accepted | Matching evaluation |
| REQ-COMP-003 | The controlled-fixture supported outcomes must include **resolved**, **improved**, **persistent**, **regressed**, **inconclusive**, and **not comparable** where applicable to the selected scenario, using the scanner-evidence meanings below. **New** remains deferred for the fixed evaluation cases. | Must | Proposed | Gold comparison dataset |
| REQ-COMP-004 | Every outcome must show its matching rationale and before-and-after evidence. Ambiguous matches must be inconclusive rather than forced. | Must | Accepted | User review |
| REQ-COMP-005 | A resolved or improved automated finding must not be translated into a claim that the page, scenario, or site is accessible or conformant. | Must | Accepted | Prohibited-claim evaluation |
| REQ-COMP-006 | Comparison history must retain both source scans and must not overwrite prior review or manual-check results. | Must | Proposed | Data-integrity test |
| REQ-COMP-007 | A public-page scan pair should be comparable only when both scans are complete and the normalized requested/final page identity, exact three-rule profile, viewport, locale, browser/scanner, evidence/sanitizer versions, measurement profile when applicable, and other material preconditions align. Finding correlation should use the exact rule plus one versioned minimized target-correlation descriptor that matches uniquely in both scans. Missing, duplicate, changed, or ambiguous identity yields `inconclusive`; a material pair-level mismatch yields `not comparable`. The exact descriptor and materiality rules must be frozen before implementation. | Must | Proposed | Pair-gate, exact-match, duplicate, and drift evaluation |
| REQ-COMP-008 | Proposed public-page semantics: `resolved` requires a baseline Finding, complete later rule coverage, and a unique later non-failing observation for the correlated target; absence from the later violation list, target removal, or locator failure is insufficient. Matched failures are `persistent` for binary rules; exact comparable contrast failures use the accepted margin for `improved`, `persistent`, or `regressed`. A uniquely correlated retained non-failing baseline followed by a violation may be `regressed`. A later-only unmatched Finding remains visible but is not labeled `regressed`; the `new` outcome remains Deferred until explicitly accepted. | Must | Proposed | Outcome, absence, unmatched-later, and contrast evaluation |

The outcome names describe scanner evidence only. Their controlled-fixture meanings are the existing evaluation baseline; their exact public-page correlation and classification algorithm remains Proposed under `REQ-COMP-007` and `REQ-COMP-008`:

| Outcome | Meaning |
| --- | --- |
| New (Deferred) | A possible future classification for a later-only Finding. The MVP shows it as unmatched later evidence and does not infer regression or causality. |
| Resolved | The baseline Finding is no longer reported, later coverage is complete, and a unique same-target native non-failing observation exists. This does not prove the underlying issue is fully fixed. |
| Improved | A correlated `color-contrast` Finding remains natively failing but its retained contrast margin is strictly higher under an identical measurement profile. It remains unresolved. |
| Persistent | A correlated Finding remains reportable in both scans; binary evidence changes stay visible, while contrast persistence requires equal retained margins. |
| Regressed | Correlated rule-specific evidence worsens, or an explicitly retained same-target non-failing baseline becomes a later violation. |
| Inconclusive | The pair is generally comparable but target correlation or required evidence is missing, duplicate, changed, ambiguous, or conflicting. |
| Not comparable | Two complete scans differ in a material page, authorization, rule, environment, evidence, measurement, or coverage prerequisite. An invalid source scan fails rather than receiving this outcome. |

## Evidence-oriented interface and export

| ID | Requirement | Priority | Status | Planned verification |
| --- | --- | --- | --- | --- |
| REQ-UX-001 | Historical requirement: the primary interface must provide controlled-scenario selection, scan status, the selected finding and evidence, one proposal-or-abstention review view, the applicable review decision and manual checks, and one baseline/later comparison view. A bulk findings list or generic finding-management interface is not required. | Must | Superseded by `REQ-AUTH-007`, `REQ-UX-010`, `REQ-UX-011`, `REQ-REV-001`, and `REQ-COMP-004` | Decision-history inspection |
| REQ-UX-002 | Evidence, guidance, AI interpretation, evidence sufficiency, manual work, and human decisions must be recognizable without relying only on color. | Must | Proposed | Accessibility review |
| REQ-UX-003 | The interface must show the analyzed target, scan configuration, supported-rule coverage, and non-certification limitation at the point where results are interpreted. For a public-page run, `REQ-UX-012` supplies the additional accepted context. | Must | Accepted | User review |
| REQ-UX-004 | Visible states must distinguish scan running/completed/failed, valid zero, Finding unprocessed/selected/failed/abstained/proposal pending/decided, ScannerReviewObservation, provider invocation, and comparison outcomes. | Must | Proposed | Workflow demonstration |
| REQ-UX-005 | Errors must identify the failed stage and provide an explicit safe retry as a new PageAnalysisRun without overwriting completed evidence or reviewed work. The MVP provides no in-place resume, automatic retry, or provider fallback. Active cancellation is required only for a later stage that introduces resumable or long-running work. | Must | Proposed | Failure-injection test |
| REQ-UX-006 | Historical Deferred scope that excluded a bulk findings list together with search, filtering, sorting, and generic finding management. The complete bounded result list is now required by `REQ-UX-010`; the remaining management features are restated in `REQ-UX-013`. | Could | Superseded by `REQ-UX-010` and `REQ-UX-013` | Decision-history inspection |
| REQ-UX-007 | General export is Deferred. The local service may derive an optional human-readable Markdown report from canonical run JSON, but the report is not a separate source of truth and no export service or tracker integration is required. | Could | Deferred | Later export decision |
| REQ-UX-008 | Opening or closing settings, provider, runtime, or model-management views must be side-effect free. Inspection alone must not probe hardware, start a service, download or activate an artifact, change a selected provider, transmit data, or mutate durable state; each such action requires an explicit control and visible outcome. | Must | Proposed | Side-effect and network tests |
| REQ-UX-009 | Progress and readiness interfaces must report measured bytes or validated stage results when those facts are available and must not fabricate percentages, countdowns, or completion estimates for opaque work. Internal stage results are not separate workflow-operation states. Passive updates must preserve keyboard focus, and validation must verify the state perceived through text and assistive technology rather than DOM presence alone. | Must | Proposed | Progress, focus, and screen-reader tests |
| REQ-UX-010 | After a complete scan, the interface must show every Finding in one results list grouped by the three supported rules, plus ScannerReviewObservations in a clearly distinct section or group. Each entry must expose a stable item label, rule, target summary, individual workflow status, and detail action. A complete zero result and a coverage-incomplete/failed scan must be visibly and semantically different. No item may be silently omitted because of display limits. Detailed accessible names, focus, announcements, and assistive-technology behavior are owned by the application-accessibility requirements. | Must | Accepted through OD-020 | Multi-item, grouping, zero/failure, and item-presence checks |
| REQ-UX-011 | The user may select only one FindingWorkflow at a time. The detail view must show the run's immutable global provider context and whether a ProviderInvocation exists for that finding, while keeping sibling states visible and unchanged. There is no generate-all, automatic collection processing, combined proposal, or bulk review action. | Must | Accepted through OD-020 | Selection, invocation, and sibling-isolation checks |
| REQ-UX-012 | For a public-page run, the interface must show the normalized analyzed page identity, exact three-rule scan configuration and coverage, trusted-input and unsupported-hostile-page limitations, global provider context, and non-certification limitation where results are interpreted. | Must | Accepted through OD-020 and narrowed by OD-021 | Public-page result-context review |
| REQ-UX-013 | Search, bulk filtering or sorting, generic finding management, and bulk actions remain Deferred. A complete visible grouped list for one PageAnalysisRun is a bounded core result view, not a general finding-management platform. | Could | Deferred | Later scope decision |

## Documentation navigation

- [Workflow-step technical assessments](#workflow-step-technical-assessments)
- [Architecture index](../architecture/README.md)
- [Project requirements index](../PROJECT_REQUIREMENTS.md)
- [Project documentation index](../README.md)
