# Candidate architecture

## Authority and status

This document is Proposed architecture research referenced by, but not part of, the authoritative requirements baseline indexed by [Project requirements](../PROJECT_REQUIREMENTS.md). It is not an accepted ADR, implementation authority, dependency selection, or release qualification. Accepted architecture decisions remain under [Architecture Decision Records](decisions/README.md).

## Proposed architecture and technology constraints

**Status:** The provider-neutral generation boundary, Groq MVP adapter and dual-mode policy, localhost-browser execution topology, and filesystem run-persistence boundary are Accepted in ADR-0001 and ADRs 0014 through 0016. ADR-0002 is preserved as history but is superseded for the MVP; installer and packaging work are deferred. ADR-0003, ADRs 0005 through 0009, ADRs 0011 through 0013, and ADR-0014's exact Groq model accept named models or technologies only as bounded evaluation configurations; ADR-0010 records the reranker deferral. None of those evaluation choices is implemented, bundled, generally supported, or release-qualified.

The preferred first-slice architecture is one local application service plus an unprivileged React interface opened in Chrome or Edge at localhost: ordinary TypeScript modules own the six workflow steps, LangChain composes bounded retrieval and—only after the application-owned evidence-sufficiency gate—one structured generation call, and canonical JSON records remain on the local filesystem. The application adds no worker process, IPC protocol, queue, agent graph, or workflow engine. Playwright launches its separate managed Chromium; the selected Chroma and Ollama candidates may remain separate local tool services because those are evaluated adapter boundaries, not application microservices. Each eligible generation run explicitly selects the local adapter or the fixed Groq adapter; neither is a fallback. A distributed or hosted multi-user architecture is deferred.

The [accepted MVP portfolio scope](../requirements/PRODUCT_SCOPE_AND_GLOSSARY.md#first-vertical-slice) supplies exactly three independent, project-owned synthetic controlled profiles: `informative-image-alt` (`image-alt`/SC 1.1.1), `form-input-label` (`label`/SC 4.1.2), and `text-contrast` (`color-contrast`/SC 1.4.3). The candidate architecture reuses one sequential path for all three: each operation selects one profile, runs only its rule, retains one rule-appropriate finding, retrieves guidance, then either records deterministic abstention or generates one eligible proposal for one human decision before comparing one baseline/later pair. It does not introduce concurrent multi-rule execution, cross-finding orchestration, another service, an agent, a queue, or a workflow engine. Live pages, arbitrary URLs, authenticated targets, crawling, and crawler implementation remain excluded from the MVP.

| Capability | Current candidate | Status and requirement |
| --- | --- | --- |
| Application language | TypeScript | Accepted only as the initial application-language evaluation baseline in ADR-0011. Require independent strict type checking and minimal runtime validation at the axe, Groq, local-runtime, and persisted-JSON boundaries. No schema framework, code generation, migration system, or release qualification is implied. |
| User interface | React with TypeScript, client-rendered SPA | Accepted only as the initial UI evaluation baseline in ADR-0012. The renderer remains presentation-only over the application-owned loopback interface and runs in an ordinary local Chrome or Edge tab under ADR-0015; it has no direct provider, runtime, Playwright, filesystem, process, or credential access. |
| JavaScript execution runtime and local-service host | Node.js 24 LTS is the dated evaluation candidate | The local-service topology is Accepted in ADR-0015, but Node.js remains Proposed. The exact dated scan seed and rationale are recorded in the [scan technology selection assessment](candidates/authorized-scan/TECHNOLOGY_SELECTION.md#technology-profile); TypeScript still does not select a runtime, web-service framework, package manager, build tool, desktop container, or native-module strategy. |
| Browser analysis | Playwright Library with its pinned managed Chromium; axe-core through `@axe-core/playwright` | Accepted only as separate initial evaluation baselines in ADR-0008 and ADR-0009. The [technology selection assessment](candidates/authorized-scan/TECHNOLOGY_SELECTION.md) records the tool rationale, while the [controlled-fixture execution assessment](candidates/authorized-scan/CONTROLLED_FIXTURE_EXECUTION_AND_SECURITY.md) proposes the fixture, profile, transient observation, and security detail. Release use and broader coverage are deferred. |
| Retrieval/model integration | LangChain | Accepted only as the thin initial two-step RAG integration baseline in ADR-0013. Application code owns queries, records, provenance, citations, eligibility policy, and evaluation. |
| Stateful workflow | Plain TypeScript state | Use an ordinary sequential, single-current-item application flow for one proposal at a time; dependency calls may still be asynchronous. LangGraph is deferred until a measured resume, interruption, or branching need cannot be handled coherently without it. |
| Tracing and evaluation | Application-owned content-safe local records and diagnostics | Sufficient for the portfolio MVP. LangSmith, hosted tracing, telemetry, and analytics are outside the MVP. |
| Generation-provider boundary | Application-owned contract with one selected local adapter and one fixed Groq adapter | Accepted in ADR-0001 and ADR-0014. The user selects the mode per run; there is no fallback, provider registry, dynamic discovery, second hosted provider, or provider leaderboard. |
| Local model runtime | Ollama | Accepted only as the initial evaluation runtime for separate generation and embedding adapters in ADR-0005. The MVP checks the selected model's practical capacity, structured output, explicit acquisition, visible failure, local-mode egress, and exact identity. Release adoption is deferred. |
| External generation | Groq with `openai/gpt-oss-20b` strict Structured Outputs | Groq and the explicit dual-mode/no-fallback boundary are Accepted for the MVP in ADR-0014; the exact model is Accepted only as the fixed evaluation configuration. Groq is the provider even though it exposes a mostly OpenAI-compatible API and the model ID uses an `openai/` namespace. The model is not release-qualified or promised to remain available. |
| Embeddings | `embeddinggemma` | Accepted only as the initial evaluation model in ADR-0006, through the separate embedding adapter and initially on the runtime selected in ADR-0005. MVP evaluation checks practical reference-PC fit, retrieval quality, exact identity, and source/license provenance. Release use is deferred. |
| Initial local capacity screen | `qwen3.5:4b`; one smaller replacement only if needed | Initial screen accepted in ADR-0003 and governed by ADR-0004. Only a configuration that completes the practical reference-PC capacity preflight may enter the compact local MVP evaluation; this is not release or support qualification. ADR-0003's historical `qwen3.5:9b` option and local-model comparison are Deferred for this MVP. |
| Vector storage | Chroma | Accepted only as the initial local persistent evaluation technology in ADR-0007. For the MVP it may hold only a rebuildable corpus index behind an isolated loopback boundary; evaluate startup/failure visibility, deterministic rebuild, deletion, and practical resource use. It is not the canonical run store, and release lifecycle, backup, migration, packaging, and support are deferred. |
| Reranking | None in the initial baseline | Deferral accepted in ADR-0010. A reranker requires retrieval-error evidence, a practical reference-PC capacity screen, measurable improvement, and a later ADR selecting the exact model and runtime. |
| MVP start and UI host | Developer-started local application service; Chrome or Edge opens its localhost/loopback UI | Accepted in ADR-0015. No installer, launcher, Start menu entry, desktop wrapper, or application-controlled webview is in the MVP; packaging is deferred. The user's UI browser is not the Playwright scan target. |
| Run persistence | One local directory per run, canonical JSON, optional Markdown report | Accepted in ADR-0016. The conceptual root is `data/runs/<timestamp-or-run-id>/`; user data stays outside Git. No database, migration framework, backup, synchronization, or browser-storage authority is included. |

The hardware snapshot in [Local MVP feasibility](../LOCAL_MVP_FEASIBILITY.md) is the reference practical-capacity environment, not a universal minimum specification. Model artifact sizes are feasibility evidence, not proof of generation quality, latency, context capacity, or full GPU residency.

## First-slice candidate patterns

**Status: Proposed candidate.** This section records coherent implementation shapes to evaluate after development is authorized. It does not accept the shape, select the named supporting tools, or authorize copying code. The detailed external implementation references and provenance constraints are recorded in [Voxleaf implementation pattern assessment](candidates/VOXLEAF_IMPLEMENTATION_PATTERN_ASSESSMENT.md).

| Candidate pattern | Proposed implementation shape | Adoption gate and explicit non-goals |
| --- | --- | --- |
| Authorized deterministic scan slice | One TypeScript module loads the selected project-owned synthetic profile revision as static page content in a fresh pinned Chromium context, blocks unexpected requests, runs only that profile's axe rule (`image-alt`, `label`, or `color-contrast`), and returns one validated transient native observation in memory. | Step 1 owns execution and the transient observation. [Step 2](candidates/ACCESSIBILITY_FINDING_EVIDENCE_CAPTURE_ASSESSMENT.md) exclusively owns allowlisting, sanitization, rule-specific normalization, digests, durable evidence, and retained deterministic contrast measurements. Live pages, arbitrary URLs, crawling, and crawler implementation remain out of scope. |
| Minimal record boundary | Small application-owned TypeScript record definitions describe the exact handoffs among the six steps, with runtime validation at axe, Groq, the local runtime, and persisted JSON. | Accepted by OD-015 and scoped by ADR-0011. The first slice does not require JSON Schema as a canonical contract, code generation, a schema framework, migrations, multiple schema dialects, or a compatibility framework. |
| Manual micro-corpus preparation | A bounded frozen W3C source pack covering the three selected success criteria and minimum scenario-specific techniques is divided manually into deterministic heading-aware passages with stable source locators and application-owned identities, then embedded once. | OD-004 accepts the bounded pack subject to the W3C/WAI use conditions documented in the [curated guidance corpus assessment](candidates/guidance-retrieval/CURATED_GUIDANCE_CORPUS_ASSESSMENT.md#proposed-deterministic-source-preparation). Public availability does not remove attribution, copyright, status, or update obligations. Axe metadata maps the finding to the corpus but is not embedded guidance. |
| Thin two-step RAG | Application code builds the deterministic privacy-safe query, LangChain composes vector retrieval, application policy admits only complete evidence with `supported` retrieval, and an eligible path makes one structured model call; all other support states abstain before invocation. Deterministic validation checks references and prohibited claims. | ADR-0013 accepts this only as an evaluation baseline. No agent, reranker, query rewriting, critique model, or semantic judge is included. |
| Sequential operation ownership | One current operation at a time carries one selected scenario, rule, finding, and either an abstention or eligible proposal to `running`, then `completed` or `failed`. A retry creates a new immutable run. | Accepted by OD-018. No multi-rule orchestration, queue, replacement controller, cancellation, checkpoint engine, worker, resume, or partial-success state is required. |
| Portfolio evaluation | One frozen happy-path generation case for each of the three scenarios runs locally, and the same three cases run through Groq. Shared deterministic abstention and comparison checks are not duplicated per provider. | Accepted by OD-009 as a compact, non-promotable six-generation-run manifest. It supports no statistical, leaderboard, provider-comparison, broad coverage, compliance, support, or release claim. |
| Safe React projection | Application code converts untrusted records into a closed view model; React renders the four information layers and one proposal decision using native semantics and text by default. | ADR-0012 accepts only React's evaluation baseline. This candidate does not select a router, state library, component kit, desktop container, or workflow library. |
| Minimal local persistence | Persist only the minimized synthetic record graph needed to reopen one run, inspect its evidence and decision, and compare it with one later run. | ADR-0016 accepts one local run directory with canonical JSON and an optional Markdown report. Browser Web Storage is not the durable authority; no database, backup, sync, or migration framework is included. |

## Deferred candidates requiring evidence

The following ideas remain useful research candidates from the Voxleaf assessment, but YAGNI excludes them from the first slice. They may be evaluated only after the named trigger exists.

| Candidate | Trigger before evaluation |
| --- | --- |
| Generated serialized-contract pipeline | More than one producer, persisted schema version, or import/export boundary creates demonstrated drift risk. |
| Identity-first queue, cancellation, and checkpoint controller | Concurrent, replaceable, resumable, or long-running application work is accepted into scope. |
| Supervised helper-process recovery | A selected dependency or live-page threat model requires an application-owned helper process. |
| Managed optional-artifact acquisition | Distribution and application-owned runtime/model installation enter scope; do not duplicate a selected runtime's safe lifecycle. |
| Full backup, migration, and cascading deletion | Private page projects, multiple stored versions, or distributable retention controls enter scope. |
| Exact release inventory, signing, repair, and uninstall evidence | A distributable Windows release is authorized. |
| Formal evaluation authority and support matrix | The project intends to promote results, compare providers publicly, or make support claims. |
| Compatibility/readiness report | The project supports hardware beyond the documented reference PC. |

## Documentation navigation

- [Project requirements index](../PROJECT_REQUIREMENTS.md)
- [Architecture index](README.md)
- [Authorized deterministic web scan candidate assessments](candidates/authorized-scan/README.md)
- [Accessibility guidance retrieval assessments](candidates/guidance-retrieval/README.md)
- [Voxleaf implementation pattern assessment](candidates/VOXLEAF_IMPLEMENTATION_PATTERN_ASSESSMENT.md)
- [Project documentation index](../README.md)
