# M6-02 — Six fixed generation executions

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with [PLANS.md](../../PLANS.md).

## Current state

- **Task and scope:** [M6-02](../DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases), In progress. Original G/A qualification and four specifically authorized primary test exceptions are complete. The failed real sequence remains stopped. The separately owner-authorized offline diagnostic/observation follow-up is complete after Green, S3 review, complete regression, post-build checks and different final integration acceptance.
- **Readiness:** M6-01, the M2-02 retrieval-capacity gate, M3-03 full-local-stack capacity gate and M3-01 freeze are complete. On 2026-09-21, public Groq documentation still lists the exact model and strict schema support. Owner-requested read-only console inspection established displayed account limits and no visible configured model restriction; no credential was inspected or request dispatched. Credential validity, runtime readiness and actual provider acceptance remain execution-time checks.
- **Baseline:** execution entry is clean HEAD `b8b9141e85ead2027e8eaf4bf59d33b1a3a20462`; the earlier planning baseline remains historical. Successor Green01/02/03 and all test leases are terminal compliant. No lease is active; the three-fixture owner exception is authorized, implemented and verified.
- **Latest evidence:** Final successor implementation passes 216 focused tests, independent strict TypeScript, 978 tests across forty-nine files, the fresh unchanged build and S3 correction review. Actual observer qualification passed; successor Local image then failed prerequisite admission with zero generation dispatches. Readback confirms the failed record, complete observer-owned cleanup and separate uncertain native metadata cleanup. All 430 protected files and three original records remain unchanged. Earlier original 176/938 and follow-up 192/954 evidence describes prior executable versions. Different final critical review passes for the stopped handoff in M602-SUC-FINAL-01. Task Verification remains unmet for both preserved stopped campaigns.
- **Allowances:** Local image entered and dispatched once, then failed response validation; its allowance is consumed. The other five entries and dispatches remain unused but blocked by the stop rule. The test chain used all three write turns and is exhausted; all four owner-authorized primary exceptions are complete. Green01/02/03 are closed compliant; the implementation write budget is exhausted. Critical research used its initial and follow-up reports; the analyst correction is now used for source encoding reconciliation; G has used both final-artifact correction cycles; cycle 2 is accepted after its Minor summary follow-up. Initial S3 review is complete; its sole correction review has passed, with no further implementation turn available.
- **Follow-up allowances:** R3 contract barriers passed; researcher and analyst allowances are exhausted. Test preflight and Red01/02 are complete. Green01/02/03 are closed compliant and the write allowance is exhausted. Initial S3 review, its sole correction-review loop and different final integration review are complete. These allowances do not renew any original chain or actual operation.
- **Successor scope:** On 2026-09-21 the owner authorized a separate six-case campaign, including its planning, implementation and review, in response to the concrete proposal recorded in M602-SUCCESSOR-01. This authorizes at most one dispatch per successor case after every readiness gate; it does not resume or replenish the original campaign. Critical research, analyst synthesis, fresh pre-draft and different final research reviews are accepted. Preflight's sole CLI testability conflict is resolved by the pure formatter; final-artifact cycle 2 passes S1–S6 without findings. Both final-artifact correction cycles are consumed. Corrected first-module Red is accepted in M602-SUC-RED-ACCEPT-01 after all three test turns. The three authorized fixture corrections pass all 216 focused tests and independent strict TypeScript. Primary accepts revised tests and behavioral Green in M602-SUC-GREEN-ACCEPT-01. Final corrected implementation, complete post-change 978-test regression, independent strict/build, preservation and the sole S3 correction PASS are accepted in M602-SUC-ACCEPT-01. All successor implementation turns and the S3 correction loop are consumed. Successor Local image consumed its entry and failed prerequisite admission without a generation dispatch; the other five cases are blocked by the stop rule. Actual qualification and the stopped outcome are recorded below.
- **Next boundary:** the successor is also stopped. Actual observer qualification passed, but Local image consumed its entry and failed prerequisite admission after one version request and zero generation dispatches. Observer-owned cleanup is complete; native metadata transport cleanup remains uncertain. Five later successor cases are not-run. Different final review and documentation handoff are complete. Native cleanup reconciliation and an explicit new scope decision are required before further live work; no retry is permitted. See M602-SUC-EXEC-01 and M602-SUC-FINAL-01. Original records remain unchanged.

## Progress

- [x] (2026-09-20) Owner authorizes the bounded primary test correction proposed in M602-A-RED-03. Only the credential-I/O fixture injection and prepared-credential request in the two named tests may change; prior Red evidence is invalidated. No worker lease is active, no fourth test lease is authorized, and no generation allowance is renewed.
- [x] (2026-09-20) Apply and verify both authorized fixture repairs; complete read-only G1–G6 coverage audit. Remaining coverage is PARTIAL as recorded in M602-A-COVERAGE-01.

- [x] (2026-09-20) Review current project state, M6-01 closure, capacity evidence, frozen definitions, scoped authorities and execution seams.
- [x] (2026-09-20) Verify independent strict TypeScript, 67 generation-contract tests and all 13 frozen references without provider/runtime calls.
- [x] (2026-09-20) Draft task-scoped plan and planning activation.
- [x] (2026-09-20) Independent critical planning-readiness PASS with no findings; proportional documentation checks accepted.
- [x] (2026-09-20) Owner authorizes G/A/B/C within the existing gates and six-operation ceiling; current baseline reconciled in M602-EXEC-ENTRY-01.
- [x] (2026-09-20) G binds exact proof contract, prerequisites, commands and six-operation allowance after analyst, pre-draft and different final R3 review; sole non-normative status follow-up reconciled.
- [x] A: final corrected Green, all 176 focused tests, strict TypeScript, default-package qualification and complete S3 correction PASS accepted after the recorded owner exceptions.
- [ ] B (completed: one Local image dispatch, failed response validation; remaining: five not-run cases, blocked by the mandatory stop rule).
- [ ] C (completed: original and follow-up regression, strict/build, independent reviews, actual failed-B evidence review and handoff documentation; remaining: task Verification before closure/archive).
- [x] (2026-09-20) Prepare the owner-requested bounded diagnosis plan, grounded in retained records and existing source; no new generation or implementation.
- [x] (2026-09-20) Complete the subsequently authorized offline diagnosis: map rejection coverage, verify original identities and quantify the separate observation scheduling failure. No model/runtime request or implementation change.
- [x] (2026-09-20) Complete the separately authorized offline diagnostic/observation follow-up, unchanged-test qualification, full regression/build, S3 and different final integration acceptance, preservation and documentation closure. No real case allowance is renewed.
- [x] (2026-09-21) Complete the separately authorized successor implementation, S3 correction acceptance, 216 focused/978 full tests, independent strict/build and actual no-generation observer qualification.
- [x] (2026-09-21) Retain the successor Local image prerequisite failure: one consumed entry, one version request, zero generation dispatches, five later cases stopped. Preserve native metadata cleanup uncertainty and both campaigns' original records.

## Surprises & Discoveries

- M6-01 is Complete with 883 maintained tests and three current actual retrieval observations. G1 still retrieves unsuitable decorative-image guidance despite complete roles; the controlled generation exception does not repair that failure.
- The runtime uses `m302-instructions-v2`; the immutable M3-01 package uses different instruction bytes. Normal request construction substitutes the runtime instructions and reserializes input. Calling it unchanged would not execute the frozen definition.
- Existing adapter preparers can consume two well-formed messages, but bind runtime configuration and schema object identity. Read-only inspection found the parsed frozen schema deeply equal to the runtime schema, while serialized property ordering differs. Semantic equality is not byte equality; G must make the exact permitted mapping explicit.
- The production proposal validator requires a scored `RetrievalResult`, and persisted invocation validation rejects evaluation-only M3-01 prompt tuples. Forging a retrieval result or a production `run.json` is not a valid bridge. The M3-01 static verification code is a reusable reference, not an executable six-case runner.
- The outer generation stage, not transport alone, owns total deadline, single-use dispatch, duplicate detection and invocation accounting. A caller bypassing that stage must prove those boundaries without introducing general orchestration.
- Historical final Red fixtures passed the Groq harness instead of its `io` member and gave the real preparer an invalid request. Both defects were reproduced and subsequently repaired under the recorded owner exception. A missing-module failure alone did not establish those assertions were valid.

## Decision Log

- **2026-09-20 / Primary — Planning only.** Create one M6-02 plan and keep M6-03/M6-04 unselected; no model or account operation is needed to author it.
- **2026-09-20 / Primary — Freeze preservation.** Retain the original M3-01 bytes and accepted Groq admission amendment. A newer production prompt is not permission to revise fixed inputs.
- **2026-09-20 / Primary — Bounded mechanism selection remains G.** Compare existing direct procedures before custom code. The execution bridge is not selected by this plan; its identity, privacy and replay risks require the R3 contract below before implementation.
- **2026-09-20 / Primary — No success-driven retries.** Six distinct fixed cases are the entire evaluation set. Failed or unknown outcomes remain evidence; neither code-correction budgets nor human editing replenish actual-call allowances.
- **2026-09-20 / Primary — Test budget stop.** Reject the final Red handoff on reproduced fixture defects and preserve the exhausted chain. Do not open Green, invent attempt 4, replace the test owner to reset allowance, or invoke an implicit primary correction. Owner disposition is required by M602-A-RED-02.

## Outcomes & Retrospective

Planning and revised G are accepted after the required independent reviews. The test chain exhausted its three write turns; four explicit owner-authorized primary test exceptions are complete. Initial Green failed package admission because G's original blanket LF rule contradicted three preserved public references; full cycle-2 research review supports the exact path-role correction. Final Green and the S3 correction review are accepted, with 176 focused tests, independent strict TypeScript, six default-package checks and the complete 938-test regression passing. The current build also passes. After direct owner authorization resolved the initial approval rejection, Local image entered and dispatched once but failed response validation. Its required in-call observation was also missed; late observations do not qualify. The remaining five original cases are not-run under the stop rule. Task Verification remains unmet and the plan stays unarchived. The earlier C review accepts the unchanged implementation; its bounded follow-up passes for the failed-evidence handoff only, with no outstanding handoff defect. The separately authorized successor scope below preserves this failed campaign and does not reinterpret its result.

The first owner exception resolved both Groq fixture defects. The complete coverage audit identified three omitted qualification families; the second owner exception completed those families in M602-A-COVERAGE-01. No implementation or real operation was started while that test boundary remained incomplete.

The later, separately authorized offline follow-up is complete in M602-FUP-FINAL-01. It adds content-free diagnostics, separate timing evidence and exact historical inspection, with 192 focused tests, strict TypeScript, 954 maintained tests, a fresh build and 77 affected post-build tests passing. S3 and different final integration reviews are accepted. These results qualify the changed implementation; the earlier 176/938 results above describe the original version. No original case or observation was repaired, and no additional generation occurred.

The separately authorized successor implementation is qualified with 216 focused tests, 978 regression tests, strict/build and S3 PASS. Actual no-generation observer qualification passed, but its first Local image entry failed prerequisite admission after one version request, without generation. Five later successor cases stopped. Complete observer cleanup does not resolve uncertain native metadata transport cleanup. This newer outcome preserves the original campaign, proves no model output or in-call observer effectiveness, and leaves task Verification unmet; M602-SUC-EXEC-01 owns the retained evidence and stop.

## Purpose / Big Picture

Demonstrate one structured-generation execution for each of the three controlled profiles through Local Qwen and the same three through Groq. A reader can trace each original result to identical frozen evidence/guidance and evaluate structure, citations, groundedness, useful remediation, human judgment, prohibited claims and completion separately. This is a six-case portfolio observation, not a provider comparison or release qualification.

## Context and Orientation

### Authorities and prerequisite evidence

The [documentation authority map](../README.md#authority-and-status-map), [project requirements](../PROJECT_REQUIREMENTS.md) and roadmap own precedence and readiness. Resolve selected identifiers through the roadmap authority-location key:

- [Evaluation authority](../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md): REQ-EVAL-001, 002, 004–009, fixed execution definitions, compact rubric, controlled generation input exception, Groq admission amendment and change policy.
- [SPEC.feature](../specs/SPEC.feature): SPEC-003 eligible proposal and SPEC-004 eligible-call/no-mixing/no-fallback portions; [HARD_SPEC.feature](../specs/HARD_SPEC.feature): HS-008 eligible gate, HS-009 provider isolation and HS-015 claims. BHV-03/04 identify the same derived boundaries. The accepted evaluation exception narrows actual-retrieval eligibility only for these six controlled inputs.
- [ADR-0003](../architecture/decisions/ADR-0003-initial-local-generation-evaluation-preset.md), [ADR-0004](../architecture/decisions/ADR-0004-reference-pc-capacity-gate-for-local-models.md), [ADR-0014](../architecture/decisions/ADR-0014-groq-as-mvp-external-generation-provider.md): one fixed Local configuration, passed reference-PC gate, fixed Groq model and no replacement/fallback.
- [Generation/evidence requirements](../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#generated-explanations-and-remediation-proposals), [provider requirements](../requirements/generation-provider-and-model-lifecycle/GENERATION_PROVIDER_EXECUTION.md) and [privacy](../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md): unchanged completeness, canonical support, validation, immutable context, payload minimization, credential and content-safe evidence obligations.
- [Agent workflow](../../.codex/README.md), [worker-first procedure](../../.codex/execplan-implementation-workflow.md), [write guard](../../.codex/write-lease-guard.md), and [ADR-0024](../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md) govern roles, research, TDD, leases and correction budgets.

Twenty-five tasks are Complete at entry. [M6-01 final acceptance](completed/m6-01-shared-deterministic-evaluation.md#m601-final-01--integrated-review-and-documentation-closure) records the exact frozen abstention, current three-profile actual retrieval, 883 tests in the maintained 46-file suite, strict/build, preserved original data and different critical PASS. Its [matrix](completed/m6-01-shared-deterministic-evaluation.md#m601-b-matrix-01--separate-shared-observations) owns shared native/synthetic/policy/public evidence. Do not replay its consumed allowances.

The [M2-02 capacity observation](../LOCAL_MVP_FEASIBILITY.md#m2-02-retrieval-only-observation--2026-09-08) passes for the exact embedding configuration. The [M3-03 full-stack observation](../LOCAL_MVP_FEASIBILITY.md#m3-03-full-local-stack-observation--2026-09-16) passes on the existing Windows reference PC: 32 GB RAM and RTX 5060 Laptop GPU with 8 GB VRAM; Qwen Q4_K_M/Ollama 0.33.3, 32768 context and 4096 output reserve. This consumes the prior screen; it is not authority for another capacity call.

M6-01 recorded the developer-managed Ollama server left running. Do not assume that process or listener still exists, take over it, restart it, or replay its startup grant. Preserve the developer's existing runtime. Runtime/model/account drift is checked during authorized G/B; no acquisition or replacement is pre-approved.

M6-03 owns the broad accessible-core-path verification; M6-04 owns final portfolio synthesis. All 200% zoom remains Deferred. M4/M5 actual review/public grants are closed. No new review decision, scan, rescan or retrieval is needed for the six independently assembled packages.

### Immutable cases, controls and meaning

[evaluation/m301-generation-v1.json](../../evaluation/m301-generation-v1.json) has SHA-256 `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`. All 13 referenced files, including the nine ignored files under `temp/m301-generation-freeze-v1/`, matched at entry. `package-lock.json` remains `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`.

The six manifest entries are paired by profile, not six independently rewritten prompts:

| Profile | Rule | Fixed Local case | Fixed Groq case |
| --- | --- | --- | --- |
| informative-image-alt | image-alt | Local image | Groq image |
| form-input-label | label | Local label | Groq label |
| text-contrast | color-contrast | Local contrast | Groq contrast |

Use the manifest's exact case/mode/input/instruction/schema hashes; the table labels are navigation, not new artifact identities. Its message assembly requires exactly two messages: exact frozen instruction text including final LF, then exact case input JSON text including final LF. Parse the exact frozen schema for the designated schema field; any checked equivalent runtime object must preserve the declared contract and disclose the actual wire representation. No runtime prompt substitution, reserialization of message text, additional history or case-specific instruction.

Every package has complete selected native facts and exactly three canonical role passages. Independently assembled guidance contains no fabricated score, vector, ranking or embedding provenance. Do not import M6-01 G1's real passages into this package, or portray a controlled success as improved retrieval. Preserve historical M2-04 abstentions and the current G1 semantic limitation separately.

Local uses `qwen3.5:4b` with the accepted full digest `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`, runtime and material settings from M3-03. Both modes preserve the manifest's 4096 output ceiling, temperature 0, top-p 1, one response, no streaming, and 120000-ms request deadline. The manifest owns all other explicit/omitted controls.

Before output inspection, bind the [accepted Groq amendment](../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#groq-admission-amendment-to-the-frozen-generation-definition) with the original manifest and `m304-groq-request-bytes-v1`: at most 65536 UTF-8 bytes for the complete measured-and-sent body. This is not hosted token fit or proof of complete input consumption. Local still requires complete context fit including schema/template/protocol overhead and output reserve; no truncation or widening.

On 2026-09-20, Groq's [model catalog](https://console.groq.com/docs/models) lists `openai/gpt-oss-20b`, and its [Structured Outputs documentation](https://console.groq.com/docs/structured-outputs) lists strict mode support. The [deprecation page](https://console.groq.com/docs/deprecations) was checked; no retirement of this exact model was found. These are dated public documentation observations, not account access or continued availability. Recheck at execution, including account-specific limits without printing secrets or sending a synthetic capability request. Provider documentation never overrides local runtime validation or no-retry rules.

## Scope and Non-Goals

In scope: freeze an honest execution binding for the existing six packages; implement only the demonstrated missing caller/validation boundary; qualify it offline; execute exactly the six authorized fixed cases; evaluate original outputs against the frozen rubric; preserve local content-safe evidence and close documentation.

Excluded: new model/provider/prompt/schema/corpus/gold, tuning or output repair, extra generation calls, warm-up/capability probes, retries, fallback, batching or concurrent cases, new embeddings/scans, capacity reruns, production eligibility bypasses, canonical aggregate format expansion, retained human decisions, UI redesign, general evaluation framework, scheduler, ledger subsystem, telemetry, statistical scores, multiple semantic evaluators or provider ranking. No model/runtime/dependency acquisition, credential modification, commit or push. A meaningful expansion requires owner authority before dependent work.

## Plan of Work

### G — Freeze the evaluation execution contract

After execution authorization, record current HEAD and dirty paths, relevant source/test/caller/configuration identity, frozen references, protected run/evidence inventories and current prerequisites. Never require the historical planning SHA or an uncommitted plan. Preserve unrelated work and use fresh lease baselines consistently.

Perform the bounded R3 decision work below before choosing the execution bridge. Compare a direct documented procedure using existing adapters and M3-01 validation precedent with the smallest reusable validation/execution extraction, if needed. Neither route may fake a RetrievalResult, silently substitute instructions, or present runtime configuration labels as the actual frozen wire prompt identity. Do not build custom tooling merely because no all-in-one runner exists.

The primary authors `M602-G-CONTRACT-01` after required synthesis/review. It must bind:

- exact six-entry identity, authorization, sequential order, explicit one-case invocation syntax, exclusive output paths and anti-replay checks;
- complete frozen input/provenance/canonical support checks and schema equivalence/representation; distinction between actual message identity and adapter runtime configuration;
- current Local digest/runtime/template/parser/effective context/offload and unchanged material controls; Groq endpoint/model/defaults, accepted byte policy and private credential boundary;
- common candidate validation without synthetic retrieval provenance; full deadline, single-use attempt capability, sticky duplicate handling, normalized failures and resource-terminal cleanup;
- local evidence shape, write-before-effect/uncertainty handling, original validated output identity, secret filtering and no raw envelope/hidden-reasoning retention;
- one semantic evaluator, frozen rubric, exact per-case outcome rules, and practical Local UI/OOM observation during each call;
- exact commands, allowed paths, negative test contract, cleanup and stop rules.

No production interface expansion or persistence-schema change is presumed. Keep evaluation records separate from production aggregates unless an already-authorized truthful representation is demonstrated. The exception creates no runtime flag, route or bypass. A schema/contract incompatibility that cannot be resolved within unchanged semantics returns for owner decision; do not amend M3-01 to unblock a caller.

Public documentation checks and owner-provided non-secret account-limit information are permitted prerequisites. Adapter-owned runtime metadata/credential checks occur only within the bound actual case, not a separate synthetic generation probe. Define request accounting separately for metadata and generation. Missing credentials or runtime may block B while unaffected A/C diagnostics continue.

### A — Qualify only the missing execution boundary

Use workflow ID `m602-six-fixed-generation-executions`, with one coherent `A-fixed-case-execution` slice unless G demonstrates separable responsibilities. Primary supplies the exact Milestone Assignment Packet v2 and responsibility contract; custom roles retain their configured model pins.

Use read-only `test_worker` behavior preflight. `EXISTING_AND_COVERED` reuses current evidence; `EXISTING_BUT_UNCOVERED` uses guarded `evidence` characterization without fabricated Red. Missing/regressed executable behavior uses test-worker Red → accepted test boundary → separate `code_worker` Green/optional behavior-preserving Refactor. Test-only helpers for existing-path characterization remain test-owned; a new substantive execution or validation mechanism is not disguised as a test fixture or non-TDD setup. The preflight classification and exact ownership resolve that distinction before writing.

Candidate paths are a purpose-named M6-02 test and bounded helper under `tests/`, plus only specifically justified existing generation validation/execution modules if extraction is necessary. This is not a broad lease. G freezes exact files, symbols and permitted structural refactor before dispatch. Documentation remains primary-owned; frozen files, historical results, credentials, Git metadata and unrelated source are forbidden.

Responsibility placement: frozen package authentication, one-case execution and output validation are distinct current concerns; reuse existing canonical support, adapter fit, HTTP transport and validation logic. Avoid copied provider implementations or a second diverging proposal policy. No production module imports test helpers or local frozen files. A test-only caller must not become a generic framework; production extraction is permitted only when current duplication or the accepted contract demonstrates its necessity.

Each worker write turn needs a primary-opened lease and fresh packet/digest, terminal compliant close, actual diff inspection, strict/focused evidence and cohesion disposition. One lease per worktree; primary documentation/guard maintenance occurs between leases. Accepted tests remain unchanged during Green. Workers use Git read-only.

Offline qualification covers at least: frozen-byte/canonical/profile mismatch; changed instruction or message newline; schema drift; missing/conflicting roles; Local full-fit and Groq boundary/body correspondence; immutable mode/exact endpoint; zero calls on pre-call failure; duplicate/reentrant dispatch; timeout/abort/ambiguous result; malformed, incomplete or prohibited candidate; invented references; secret echo; occupied output path; result-publication/cleanup failure; re-entry after uncertain effect. Use existing controlled transport doubles, never real calls for negative tests. Default tests must not require ignored inputs or credentials, and an opt-in real command must fail closed rather than skip missing prerequisites.

Fresh S3 `critical_reviewer` acceptance is required before any actual-case use: the named triggers are credentials/egress, frozen identity, single-attempt accounting and cleanup/recovery. Initial and one ordinary correction write per unchanged role/phase chain; attempt 3 only under the workflow's explicit progress/new-evidence/changed-action/expected-benefit conditions. One review-correction loop uses remaining budgets. Stop repeated decisive failure without new evidence, two no-diff handoffs, changed binding fields or exhaustion; primary triages without resetting counters. Consult the [bug index](../bugs/README.md) for substantive defects.

### B — Execute six fixed cases and apply the frozen rubric

Only after owner execution authorization, G acceptance, A critical acceptance, exact prerequisite confirmation and zero unresolved required gaps may B begin. Freeze the order before output inspection: the three Local profiles sequentially, then the same three Groq profiles sequentially. Each entry is an explicit single-case operation, never a loop/batch command that automatically continues after failure.

One future execution authorization for this plan covers at most one entry per fixed case and one generation dispatch per entry, six maximum. Track six-entry status, whether dispatch actually occurred, response/validation outcome and remaining allowance in this plan and the bounded local result records. Do not claim a pre-call failure is a generation execution. An entered operation with no call is still not automatically replayable; ambiguous dispatch consumes its allowance pending explicit owner disposition. No seventh call, warm-up, retry or replacement is authorized.

Run on the reference PC with the current application available for a small existing-UI responsiveness observation during each Local call, without Analyze, retrieval or another Generate. G binds the exact benign interaction, time correlation, readiness/stop procedure and readily observable memory/OOM evidence; no new UI, benchmark tool or numeric performance gate. The prior capacity screen cannot prove these three calls remained usable. If an in-call observation is missed, report the missing proof and do not repeat generation to obtain it. No new embedding work is needed to populate a synthetic full-stack claim.

Before each operation, confirm its unconsumed identity, unchanged frozen/wire configuration, protected paths, private output destination and current necessary prerequisites. Use the fixed adapter; credential material stays inside Groq's existing secret-handling path and is neither printed nor inspected in chat. Record metadata/request counts and whether invocation started honestly. Preserve original evidence on any failure.

Stop subsequent actual generation at the first invalid/failed/unknown case, Local OOM/unusable interface, model/configuration drift, suspected secret exposure, identity discrepancy or cleanup uncertainty. Preserve the failure and complete unaffected offline/documentation work. ADR-0003/0014 and the frozen failure interpretation require owner disposition where the fixed configuration fails; do not switch model, provider, cap or prompt. Corrections and any new evidence campaign follow REQ-EVAL-005/007 and cannot silently replenish this six-case set.

One semantic evaluator applies the frozen M3-01 rubric to original outputs. Primary may fill that role; independent reviewers audit method, binding and evidence rather than becoming extra semantic scorers. For each profile keep structural validity, reference/citation validity and actual support, groundedness, remediation usefulness, confidence/uncertainty, blocking judgment/reminder, prohibited claims and provider completion separate, using the frozen pass/fail/not-run vocabulary. Mechanical pass does not prove semantic support. A human edit cannot retroactively pass the original answer and no actual review decision is created by scoring it.

Retain separate Local and Groq sections here, with one row per case, relevant identity, outcome, separate dimensions, original validated-output reference/hash and limitations. Detailed validated proposals stay in ignored local evidence; tracked text contains no prompts, raw responses, screenshots or private payloads. Do not retain raw provider envelopes, invalid secret-bearing content or hidden reasoning. G defines safe bounded failure evidence. Shared deterministic outcomes are linked to M6-01, not rerun as additional fixed cases or claimed as actual retrieval for controlled packages.

### C — Verify integration and close documentation

After the last accepted executable change, run the current maintained full suite (46 files/883 tests at entry, plus accepted additions), independent strict TypeScript and current build. Run focused checks at earlier boundaries; reuse a valid full run under the complete evidence-identity rule rather than repeating it at handoff. No real-call replay is part of regression.

A different fresh `critical_reviewer` audits the integrated six-case count, frozen/actual wire binding, validated and semantically interpreted original results, Local usability/OOM evidence, secret filtering, preservation, cleanup and documentation. It may inspect retained evidence read-only and reproduce offline risk-critical checks; it cannot make a seventh request.

Task completion requires its Verification, not merely six terminal records. Any failed case or unmet Must keeps completion unmet; report exact completed/failed/not-run entries and obtain necessary owner disposition without relabeling failure as success. Mark Complete and archive only after all required evidence, final review and the [documentation closure gate](../README.md#task-closure-documentation-gate) pass. Reconcile roadmap, instructions, plan/progress navigation and materially affected summaries; M6-03/M6-04 remain unselected.

## Decision Review Contract

**Owner/artifact:** M6-02, `M602-G-CONTRACT-01` inside this plan. **Tier:** R3 for the execution-bridge decision: frozen versus runtime identity, private egress/credential handling, one-call capability and uncertain-result recovery. The original planning checkpoint mapped facts only. Subsequent owner-authorized research, synthesis, primary authorship and both fresh research reviews are complete; G is accepted below. A/B implementation and actual evidence remain required.

**Question:** What is the smallest truthful, testable way to execute the six unchanged packages through the accepted providers while retaining the common validation and failure contract without fake retrieval or production provenance?

**Unranked candidates for G:** direct documented assembly around existing adapter factories plus reusable M3-01 static-validation logic; or minimal extraction/reuse of production validation/execution primitives with a narrowly scoped evaluation caller. Eliminate any candidate requiring changed frozen messages, fabricated RetrievalResult, false invocation identity, skipped validation, duplicated provider stack or general workflow machinery. A successor manifest/architecture change is an owner decision, not a third implicit implementation route.

**Criteria:** exact accepted semantics/bytes, authentic provider path, full admission/validation and privacy, honest minimal records, no-replay/cleanup truth, maintained testability, responsibility cohesion and least code/procedure. Hard gates outrank convenience. No provider ranking, cost study or model research.

**Routing/budget:** At execution, primary supplies a Research Assignment Capsule v1 with exact sources and this invariant packet. One bounded non-ranking discovery pass may close remaining source questions. Assign one `critical_researcher` to the coupled identity/validation/attempt-boundary question; split only a demonstrably independent unresolved critical dimension. Each assigned researcher has one initial report plus one bounded follow-up. Mandatory `decision_analyst` returns DRAFT READY, RETURN FOR RESEARCH or OWNER DIRECTION. Require a fresh `critical_research_reviewer` pre-draft checkpoint, primary-authored contract, and a different final critical research reviewer. No drafting agent is needed. Apply canonical research correction/stopping limits; never treat role capacity as a staffing target. Required implementation reviews remain separate S3 acceptance of executable code.

| Invariant | Trigger / expected result | Proof and reviewer |
| --- | --- | --- |
| G1 completeness and authority | Every six-case contract/command/retention field resolved; unknown model/account facts stop affected execution | Analyst and both contract reviewers inspected entire artifact; G accepted, A/B proof pending |
| G2 frozen identity | Corrupt hash/newline/order or wrong case rejected before effect; exact messages and schema meaning preserved; runtime labels never misstate actual prompt | Critical researcher; offline vectors and contract reviewers |
| G3 truthful support | Wrong/missing/conflicting canonical guidance rejected or abstains without call; no scored RetrievalResult invented; same candidate validation rules | Researcher plus A critical validation review |
| G4 privacy and fit | Fixed destination/model, only minimized selected content, credential transport-only, no truncation; complete Local fit and measured/sent Groq bytes | Researcher plus negative/native-double evidence |
| G5 one operation and recovery | Occupied/entered/unknown case cannot replay; duplicate/abort/deadline cannot silently dispatch again or erase attempted provenance | Researcher plus A critical failure-path reproduction |
| G6 evidence honesty | Failed output remains failed, original preserved, one semantic evaluator, Local usability observed; no six-case completion with unmet mandatory evidence | Analyst, primary evaluator and different final implementation reviewer |

Decide the interface, provenance representation, validation equivalence, operation-consumption point, failure policy and retention boundary now in G. A/B prove those defined semantics; they cannot decide them after seeing output. Re-run the full invariant packet after a material revision. A required scope/authority change or exhausted budget returns to the owner. No R3 verdict grants provider calls without execution authorization and A acceptance.

## Concrete Steps

Working directory: repository root, PowerShell 7. Load only the definitions under [Development command preparation](../../README.md#development-command-preparation), which supply `Invoke-M105Command`, pinned Node and exact environment restoration. Do not replay adjacent npm-ci, archived generation, acquisition or cleanup commands.

### M602-CMD-ENTRY — Read-only planning/re-entry

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') {
  throw 'Reconcile active lease before M6-02 maintenance or execution'
}
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) {
    throw 'Pinned Node runtime unavailable'
  }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/generation-contract.test.ts tests/ollama-generation-contract.test.ts tests/groq-generation-contract.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Generation contract tests failed' }
}
```

Expected planning result: strict check and 67 tests pass; no browser, actual credential, local runtime or provider access. Actual counts may change with a reconciled future tree; never impose a historical count as a runtime assertion.

### Bound command slots

The following index resolves to M602-G-CONTRACT-01 below. Its final research acceptance and A qualification are required before execution; the command table itself grants no premature operation.

| Slot | Required binding |
| --- | --- |
| IDENTITY | Intentional current HEAD/dirty paths, source/test/caller/config hashes, all 13 recursive frozen references, protected original runs/evidence, ordinary path topology and developer prerequisites; no private content output |
| OFFLINE | Exact focused commands/flags, injected doubles, deadlines, scratch and expected assertions; production extraction uses accepted Red/Green ownership |
| PREPARE | Minimal explicit task-owned output location, exclusive creation/permissions and inventory, exact build/scratch/application/browser setup, before/after mutation list; no lock/manifest regeneration or installation |
| LOCAL CASE | Exact one-case command for each Local profile, fixed runtime/model/settings, whole-message fit, metadata request bound, single dispatch, deadline, private evidence, in-call UI/OOM observation and normal stop |
| GROQ CASE | Exact one-case command for each Groq profile, public documentation/account-limit check, fixed credential source without exposure, manifest+amendment binding, measured/sent body, single dispatch, bounded result and cleanup |
| READBACK / RUBRIC | Validate recorded identity/outcome/proposal without execution; original-output binding, one-evaluator checks and tracked content-safe summaries |
| FULL | Current maintained sequential complete suite, accepted additions, independent strict/build, no concurrent service/browser use and capture/actual-case flags absent |
| CLEANUP / DOCS | Exact owned processes and disposable child paths only, ordinary-path/link/content guards and expected effects; preservation and environment restoration; links/status/format/PowerShell checks and git diff --check including separate untracked-file inspection |

Resolve dependencies before an affected lease; workers may not invent commands or credentials procedures. If a slot is unnecessary, mark it Not applicable with evidence. A metadata/account problem is not permission for a synthetic provider probe. No shell loop may execute the six real requests unattended.

## Validation and Acceptance

G/A acceptance requires complete frozen and canonical validation, tested no-effect failures, common output validation, honest configuration/wire identity, bounded attempt/deadline/cleanup and no credential/payload leakage. Receipts and worker summaries support, but do not replace, primary inspection of actual code, responsibility placement and evidence.

B acceptance requires exactly three Local and three Groq attempted fixed generation executions on the same unchanged packages, each with its original outcome and all required observations. Pre-call failures and not-run cases cannot fill the six-execution count. Every happy case must satisfy runtime validation and the frozen happy-case rubric; separately record each failure rather than computing a product score. Local calls must complete sequentially without OOM or unusable interface. Any material evaluated input/adapter/configuration change follows the new-binding/new-evidence authority before further output inspection; never mix an unacknowledged changed configuration into the six-case result.

Final acceptance additionally requires current authoritative regression, independent strict/build, preserved frozen/original evidence, correct private/tracked boundary, different final review and documentation closure. This task does not prove actual retrieval returned the controlled guidance, broad model quality, hosted complete input consumption, public accessibility, hardware support or release readiness.

## Idempotence and Recovery

Re-entry starts read-only: inspect current task/contract, original six-entry allowance, exclusive local records, actual effects and owned process state. Never infer no request from no response or missing result file. An uncertain operation stays consumed/unknown; do not replay it to discover what happened. Preserve partial records and use readback/offline investigation.

G must define a minimal durable consumption boundary before possible dispatch and how publication failure is reported. Prefer existing exclusive-file/directory primitives and six bounded records; do not invent a transactional job runner, retry graph or resumable generation subsystem. Code corrections do not replenish provider allowances. New calls, replacement cases or changed manifests require explicit owner/authority disposition and preserve earlier failed evidence.

Preserve all original `data/runs`, frozen corpus/evaluation artifacts and historical ignored evidence, including M6-01 failure and successful retrieval roots. No raw content enters tracked documentation. Stop only task-owned application/browser processes, restore their environment, then remove only verified ordinary disposable child paths named in G. Never recursively remove `temp`, `data/runs`, browser/model roots or unknown contents. Do not stop or restart the developer-owned Ollama runtime. Cleanup uncertainty stops advancement and remains visible.

Missing frozen files require byte-identical recovery under M3-01's existing contract or an owner-approved successor freeze, not reconstruction from memory. Missing runtime/account prerequisites are not implementation defects; finish unaffected work and request only the material missing prerequisite. No fallback or acquisition is implicit.

## Artifacts and Notes

### M602-G-CONTRACT-01 — Frozen execution contract

**Disposition:** Accepted by primary on 2026-09-20 after analyst DRAFT READY, fresh pre-draft PASS and different final research PASS WITH FOLLOW-UPS. Final review inspected artifact SHA-256 `427a22a63fc22ab4c4b4816dc16e33b4ebc2ec8dd3bbfd70b9f8c468089046cd`; its sole Minor F1 was stale Decision Review Contract narrative, now reconciled without normative change. No Blocker/Major or unresolved gate finding remains. This section resolves G's interface and command slots and selects minimal shared candidate validation and execution extraction, with a bounded evaluation caller. No product eligibility, persisted invocation tuple, frozen artifact, provider implementation or significant architecture decision changes.

**Decision evidence:** CR-01 covers the controlled-input authority; CR-02 the frozen/runtime identity mismatch; CR-03 hardened candidate reading; CR-04 deadline/capability ownership; CR-05 adapter privacy/fit; CR-06 exclusive filesystem publication. The researcher's initial report and sole follow-up are complete. The analyst compared direct assembly with shared extraction and returned DRAFT READY. The fresh pre-draft reviewer returned PASS on G1–G6, with no findings, independently reproducing all 13 file hashes, schema deep equality, different serialized order/instructions and final message LFs. Direct assembly is rejected because it duplicates the stage's substantive control policy and the archived validator lacks current hostile-object protections. Research observations are not implementation evidence. Researcher follow-up and the analyst correction are exhausted; both final-artifact correction cycles are consumed and accepted as recorded in M602-A-GREEN-01-RESULT.

#### G contract: identity and admission

The exact order and exclusive directories under `temp/m602-generation-evidence/` are `local-image`, `local-label`, `local-contrast`, `groq-image`, `groq-label`, `groq-contrast`. Image maps to manifest `informative-image-alt`, label to `form-input-label`, contrast to `text-contrast`; mode maps literally to `local` or `groq`. The manifest's six bindings remain authoritative even though its listing interleaves modes. No optional/default case, loop or automatic next case is permitted.

Authenticate the original manifest hash above and every one of its 13 recursive path/hash references before adapter preparation. Paths must be the exact frozen paths, ordinary files under ordinary repository ancestors; reject links/reparse points, hard-linked files, invalid UTF-8 or BOM. Read bounded files (at most 1 MiB each) and compare raw-byte SHA-256 before parsing. The M301 manifest, nine local package artifacts and original M204 source/seed files must contain no CR and exactly one final LF. The four exact public source-reference paths instead retain the newline representation fixed by their original hashes: corpus manifest, passage catalog and M201 gold currently contain CRLF; RD003 contains LF. Do not normalize, reserialize, rewrite or replace hashes. Apply the same exact path-role distinction in default and injected-test authentication. The nine local package paths and four source paths are exactly those in the authenticated manifest; never follow an unverified arbitrary path.

Reconstruct each complete package by the existing [M3-01 verification algorithm](completed/m3-01-generation-evaluation-package.md#m301-command-02--resolved-preparation-validation-and-closure-callers): authenticate and parse the canonical corpus and gold; validate the retained source run against its manifest hash and seed; project the seven native fields; validate the native Finding and require its available facts complete; preserve source abstention/no-call; bind fixture, target, scan context and source IDs; reconstruct selected facts in available-reference order, the exact three gold/profile/criterion passages in criterion/interpretation/remediation order, selected source notices and full SOURCE_NOTICES. Deep equality must hold for the entire reconstructed input and provenance, including no unexpected fields. Authenticate retained source/seed paths before read; do not fabricate scores or a RetrievalResult. Catalog conflicts, noncanonical/unknown/duplicate/excess/wrong-profile passages and provenance mismatch are integrity failures before sufficiency. Existing production missing/incomplete/conflicting support abstention semantics remain unchanged. For these exact six packages, a missing passage also violates immutable package equality and cannot become an altered executable case.

Create two frozen message objects containing the exact authenticated instruction and input strings, including one final LF. Do not call the current `createGenerationRequest` for evaluation: it substitutes instructions and reserializes input. Require deep structural equality of the authenticated frozen schema with `GENERATION_SCHEMA`, then use that required object identity. Use unchanged configuration controls and runtime version fields in `GenerationRequest`; label these as adapter configuration, never actual message provenance. Preserve separate frozen manifest/instruction/input/schema hashes and measured actual body hash/bytes. No new production tuple is admitted.

Expected body identities from pure serialization (must be reproduced in A and actual native `.end` bytes):

| Profile | Local input upper bound | Local bytes / SHA-256 | Groq bytes / SHA-256 |
| --- | --- | --- | --- |
| image | 17091 | 17748 / `4c13611115bca2dbf5d6d364a0e00d4bb03f40a5200322ad068bc4726306dfd6` | 17823 / `eb1b3dd6c8c9bf818836296fd33f05262eaa92318ac2160ca673dadf2a6c120a` |
| label | 18412 | 19141 / `4f8f218eaaf948af041fbec3eb119430f72342f53eb2faf2233366818f7ddf5a` | 19216 / `4957a37acc68b2b57ab35da25184a697b5358deb6978d8e58701ae2717b0d5c6` |
| contrast | 19085 | 19803 / `b45eca8353459a5177dd4b7d6e83b4f02219d53b83b795c8e39a8c4047f7e2c8` | 19878 / `0b0eda9dfc67cda043f284023fba13e370efa02c410dd8e6848a7c7c52e69c7a` |

All Local bounds plus 4096 fit 32768; all Groq bodies fit 65536 bytes. This establishes neither actual runtime readiness nor hosted token fit/complete consumption.

#### G contract: interfaces and responsibility placement

Only these production paths are implementation-owned:

- `src/server/generation/proposal-contract.ts`: extract `validateProposalCandidate(candidate, {findingId, availableEvidenceReferences, passageIds}): ProposalValidationResult`. Context references retain existing `EvidencePath` types. Preserve all descriptor/prototype/sparse-array/length/reference/lexical readers and detached frozen output. Existing `validateProposal` retains native/retrieval/support eligibility then delegates. The new candidate context is authenticated scope, not eligibility proof.
- New `src/server/generation/generation-execution.ts`: export `executeGenerationOperation({signal, providerContext, adapter?, admit, beforeTransport?})`. `admit(signal)` returns or resolves `failed/input-integrity`, existing `abstained/decision`, or `ready/{createRequest(configuration), validateCandidate(candidate)}`. Own the fixed 120000-ms total deadline beginning before admission, immutable bindings/fit, preparation, one-use capability, envelope reading, candidate validation and cleanup. Preserve current stage failure precedence and sticky duplicate behavior.
- `src/server/generation/generation-stage.ts`: retain public `executeGeneration` signature. Its admission closure owns corpus reads, native/retrieval/input eligibility and production request/validator context. Map the shared outcome back to unchanged `GenerationOutcome` and `ProviderInvocation`. No production import from `tests/`.

The shared outcome is a closed union: proposal with validated `Proposal`, `attempted:true`, observation and `cleanupFailed:false`; abstained with existing decision, `attempted:false`, no observation and `cleanupFailed:false`; failed with existing `GenerationErrorCode`, truthful `attempted`, optional observation only when attempted, and `cleanupFailed`. Observation contains only `{adapterConfiguration: Omit<ProviderInvocation,'outcome'|'validation'>, outcome: InvocationOutcome, validation: ProviderInvocation['validation']}`. No raw candidate/envelope/error/adapter escapes. Production alone maps configuration observation to persisted invocation.

`beforeTransport?: () => void` is a synchronous durable-marker callback. Exact order: reject inactive or used capability (duplicate stays sticky); consume capability; check stop/full bindings/callback type; invoke hook; require returned value `undefined`; check stop/full bindings/stop again; check final ordinary own-data adapter configuration descriptor identity; set attempted true and immediately invoke `start()` with no intervening callback. Throw, asynchronous return, expiry, reentry (even caught) or mutation cannot dispatch. Configuration failure applies unless shutdown/timeout has precedence. A marker means possible subsequent dispatch, never positive attempted proof. Finally deactivate capability before aborting the owned controller, then remove timer/listener, so prepared-undispatched credentials clear without reopening dispatch. Synchronous OS work is not forcibly interruptible; the post-hook deadline check blocks late dispatch.

Evaluation code is substantive Green-owned behavior, not a test fixture exception:

- `tests/helpers/m602-package.ts`: export `loadM602Package(caseLabel, environment?)`, performing the authentication/reconstruction above and returning the closed result defined below. The optional complete environment is an offline test seam; real CLI supplies no environment and uses the fixed manifest trust anchor and ordinary bounded filesystem reads. No HTTP, credentials or publication here.
- `tests/helpers/m602-operation.ts`: export `executeM602Case(caseLabel, dependencies?)` and `readM602Case(caseLabel, dependencies?)`. Own exactly one fixed operation, its exclusive records and native request accounting. Offline dependencies may provide an isolated synthetic root, byte/filesystem operations, clock and adapter/native doubles; actual CLI accepts no injection/root/configuration options. Reuse existing adapters, native transports and shared execution. No retry, scheduler or provider stack.
- `tests/helpers/m602-run-case.ts`: accept exactly `--execute --case <one-label>` or `--readback --case <one-label>`; no default, combined modes, duplicate flags or extra arguments. Invoke one helper and print only a bounded content-safe summary (identity, counts, outcome, hashes, closed error). Readback instantiates no adapter and reads no credential.

The existing stage-to-core and both caller-to-candidate-reader dependency edges are the only production interface change. Extract contiguous existing responsibilities; permitted structural refactor is limited to the named stage and candidate reader. New helpers separate frozen admission, effect/evidence coordination and CLI parsing. Test ownership: `tests/m602-generation.test.ts`, `tests/generation-stage.test.ts`, `tests/generation-contract.test.ts`. Code owner cannot change these accepted tests. Primary owns this plan, commands, assessments, README and status documents. No other source/test/config/dependency/frozen paths are authorized. A material cohesion problem returns to primary reconciliation rather than authorizing a broad helper.

**A preflight reconciliation (final-artifact correction cycle 1, accepted):** The read-only test worker returned CONFLICTING, correctly identifying that a byte-reader alone cannot make synthetic bytes match the real manifest trust anchor, and that independent test ownership needs closed helper types. No lease/write occurred. The following binds only the already-authorized offline injection seam and result interfaces; the real CLI still admits no override. Both environments execute the same authentication/reconstruction code, without a skip-validation branch. A separately authorized read-only qualification below proves the real trust anchor against the retained package after Green. Complete revised-artifact G1–G6 review returned PASS with no findings at SHA-256 `ebfee0335f1fa6836706e55d54124fddfcab45a173f8dfcd45a3e3aed4b6ad6d`; primary accepts the reconciliation. One final-artifact correction cycle has been used, without renewing any research or execution allowance.

`M602CaseLabel` is exactly the six directory literals above. Export `M602PackageEnvironment` as `{readonly manifestSha256:string; readonly readBytes:(relativePath:string)=>Uint8Array}`. Both properties are mandatory when injected; no partial environment/default mixing. Its manifest digest must be a lowercase 64-character hex digest, its reader returns bounded bytes for exact verified relative paths, and every manifest/reference/provenance/canonical check still runs. Default environment privately binds the original fixed digest and guarded repository reader; it cannot be altered by environment variables, CLI flags or file contents. The test worker may generate a synthetic manifest and its digest from existing public native/corpus fixtures, and provide matching synthetic input/provenance/source/seed bytes in memory. Never copy ignored frozen payloads into tracked fixtures. Wrong trust anchor, inconsistent hashes or payloads still fail through the identical validator.

`loadM602Package` is synchronous and returns `{status:'failed',error:'input-integrity'}` or `{status:'ready',value:M602Package}`. `M602Package` has exactly `caseLabel`, `providerContext` (existing type), `findingId`, `availableEvidenceReferences` (readonly EvidencePath array), `passageIds` (readonly string array), `identities:{manifestSha256,inputSha256,instructionsSha256,schemaSha256,provenanceSha256}`, `wire:{sha256,bytes}`, `createRequest(configuration:GenerationConfiguration):GenerationRequest`, and `validateCandidate(candidate:unknown):ProposalValidationResult`. Freeze all returned containers. Message strings remain private in the request factory closure until requested. `wire` derives from unchanged provider preparers; default loading additionally requires the six fixed body identities in this contract. Injected synthetic identity uses its authenticated bytes and the same serialization/fit path, never a caller-supplied body digest.

Export `M602OperationDependencies` with optional `root:string`, `packageEnvironment:M602PackageEnvironment`, `requestImplementation:OllamaNativeRequest` (same callable shape serves both adapters), `credentialIO:GroqCredentialIO`, `signal:AbortSignal`, and `filesystem:Partial<Pick<typeof import('node:fs'),'mkdirSync'|'openSync'|'writeSync'|'fsyncSync'|'closeSync'|'lstatSync'|'realpathSync'|'readFileSync'|'readdirSync'>>`. Dependencies are trusted test collaborators, never serialized input. Default root is the fixed evidence root. An injected root must be an ordinary `temp/m602-test-<UUID>` child; require it whenever any other dependency is injected. No dependency may target original evidence/data/freeze/credential files. Test roots and supplied in-memory byte maps are isolated; default execution accepts no root override from process configuration. Clock/deadline tests use Node's existing test mock timers, not a new production clock abstraction. Adapters are always the existing factories; the native-request and credential-I/O seams inject their existing doubles. Filesystem injection exists solely for real write/flush/close/topology failure assertions; ordinary operation uses native functions.

Supplying any dependencies object, including `{}`, selects isolated test execution. Before creating a directory/record, require `root`, `packageEnvironment` and `requestImplementation`; Groq additionally requires `credentialIO`. Missing members return `evidence-blocked` with zero effects, never fallback to real native transport or credential I/O. Local never invokes credential I/O. Injected readback requires `root` and `packageEnvironment` only and never invokes request/credential collaborators. Real defaults are selected only when the dependencies argument is entirely absent, as on the strict direct CLI path. Test byte readers/credentials remain trusted in-memory collaborators; this is not a sandbox for malicious test code. Qualify every missing-collaborator variant with zero native/credential/directory calls.

Export `M602Result` and `M602Assessment` as the exact record shapes below. `executeM602Case` returns `Promise<{ok:true,result:M602Result}|{ok:false,error:'evidence-blocked'|'evidence-publication'}>`; a published failed result is `ok:true` with `result.status:'failed'`, and the CLI still exits nonzero. `readM602Case` returns `Promise<{ok:true,result:M602Result,assessment:M602Assessment|null}|{ok:false,error:'evidence-blocked'}>`; missing assessment is allowed for inspecting a just-finished result, but never for advancing. Neither helper leaks exception text. Readback ignores/never invokes request/credential collaborators, authenticates the package and validates the retained proposal with the common reader.

Export `parseM602Arguments(args:readonly string[])` from the CLI module, returning `{ok:true,mode:'execute'|'readback',caseLabel:M602CaseLabel}` or `{ok:false,error:'arguments'}`. Importing the module has no effects; only direct invocation enters the CLI. Its direct-entry path calls the helpers without dependencies. Tests may exercise the parser in process and invalid direct CLI arguments without creating a real case root.

The frozen first unresolved import for initial Red is `src/server/generation/generation-execution.ts` / `executeGenerationOperation`. The new test lists this import first; `validateProposalCandidate` and the three helper exports are also absent prerequisites. The accepted initial failure must name this exact module, not an unrelated import/parse error. All blocked behavioral assertions are explicitly unexecuted until Green. Existing passing stage/contract cases remain regression evidence rather than fabricated Red for the extraction itself.

#### G contract: records, privacy and continuation

Primary creates only the exact evidence root after ignore, ordinary-ancestor and inherited-ACL inspection. Existing case directories, including empty/malformed ones, permanently block that case. After checking prior-case acceptance and explicit label, execution exclusively creates its nonrecursive directory before package admission or adapter preparation. An operation is consumed at this directory creation. Each record uses exclusive creation, complete write, fsync and close; never overwrite, rename over, delete or repair a record. Files are bounded to 1 MiB, ordinary and single-link. Reject unexpected record names. Partial writes, flush/close failure and missing terminal records remain consumed/unknown and block every later case. These are conservative ordinary-filesystem guarantees, not power-loss durability or defense against malicious same-user mutation.

All JSON records have `version:'m602-evidence-v1'` and exact `caseLabel`. Hex digests are lowercase 64 characters; timestamps are UTC ISO strings. Unknown properties, inconsistent fields, invalid hashes/times/counts or malformed nested output invalidate readback. JSON is UTF-8, two-space indentation plus LF. Fixed record shapes:

| File / owner | Exact additional fields and meaning |
| --- | --- |
| `entered.json` / operation | `enteredAt`, `manifestSha256`, `mode`, `configurationSha256`, `codeSha256`. Configuration hashes JSON serialization of unchanged configuration; code hash is SHA-256 of sorted `{path,sha256}` entries for every `src/**/*.ts`, the three M602 helpers, `package.json`, `package-lock.json`, `tsconfig.json`. Entered publication completes before admission/preparation. |
| `dispatch.json` / hook | `enteredSha256`, `markedAt`, `meaning:'dispatch-may-have-started'`. Publication completes before native generation entry. |
| `result.json` / operation | `enteredSha256`, `dispatchSha256` (null if no complete marker), `finishedAt`, `status` (`proposal`, `abstained`, `failed`), `error` (existing closed GenerationErrorCode or null), `attempted`, `cleanupFailed`, `observation` (shared configuration observation or null), `wire` (`{sha256,bytes}` or null), `requests:{version,show,tags,chat}`, `chatWindow` (`{startedAt,finishedAt}` or null), `proposal` (validated original or null). `proposal` status requires attempted, one chat, exact wire and complete cleanup; no raw invalid candidate is retained. Abstention/failure never fills a completed generation count. |
| `assessment.json` / primary only | `resultSha256`, `evaluator:'primary'`, `assessedAt`, `dimensions` (one `{observation,value}` per manifest rubric row, in frozen order), `uncertainty` (`pass`, `fail`, `not-run`), `localObservation` (null for Groq; Local shape below), `accepted` (boolean). No freeform model text or private facts. |

Local observation shape is `{ui:{startedAt,finishedAt,responsive},runtime:{observedAt,digest,contextLength,sizeBytes,sizeVramBytes},gpuBefore:{observedAt,usedMiB,freeMiB},gpuDuring:{observedAt,usedMiB,freeMiB},oomObserved}`. Native chat starts immediately before real `http.request`/`https.request`; its finish is the first terminal request error/close or response end/close, recorded by passive listeners. UI start/end and runtime/GPU-during timestamps must lie strictly within this window. Missing/ambiguous timing fails observation; never extend the window to cover a late UI action. Request wrappers count fixed routes and hash actual `.end` body without retaining it or accessing/logging credential headers. No new transport behavior, redirects or retries.

On write/publication failure, retain every partial artifact, return nonzero and only a closed summary `evidence-publication`; do not create replacement evidence. An occupied/unverifiable prior path returns `evidence-blocked`, consumes no new directory and stops. CLI exit zero for execution means terminal valid proposal publication only; it does not mean semantic acceptance. Readback exit zero means records are internally valid, not that another call is allowed. Inconsistent/missing result is unknown even when no response was seen. Any marker is conservative possible-dispatch evidence; only live attempted accounting plus one native request supports a completed attempted-call claim.

Every next CLI requires all preceding cases' intact hash-linked proposal results and primary assessments with `accepted:true`, every required current-generation rubric dimension and uncertainty passing, and valid Local observation where applicable. Historical scanner provenance and actual retrieval relevance retain their original separate interpretation; they cannot be promoted to fresh scan/retrieval success. Assessments must use the exact frozen ten dimension names and values; `actual retrieval relevance` records `fail` for the preserved unsuitable current G1 and `not-run` for generation-only retrieval execution, with explanation in tracked result interpretation. Those historical dimensions do not masquerade as current-generation passing gates. Current-generation gates are evidence completeness, controlled support and citations, structural validity, semantic groundedness, remediation usefulness, human judgment and reminder, prohibited claims, provider completion, plus uncertainty and Local observation. No failed original output is repaired; no assessment overwrite or second semantic evaluator.

Credentials remain within the unchanged Groq loader/transport/echo filtering. No credential read in preflight/readback; no `.env` modification. Raw envelopes, hidden reasoning, invalid response content, full exception strings and request headers are never retained or printed. Original validated proposals stay only in ignored result files. Inherited Windows ACL inspection establishes known local access, not owner-only privacy; do not rely on mode 0600 or mutate ACLs. Tracked summaries contain only outcome/dimension labels, hashes, count and limitation.

#### G contract: exact commands and prerequisites

All commands use repository cwd, PowerShell 7.6.5, pinned Node v24.20.0 and the unchanged README preparation definitions. Load definitions only, never adjacent acquisition:

```powershell
$m602Readme = Get-Content README.md -Raw
$m602Prep = [regex]::Match($m602Readme, '(?s)### Development command preparation.*?```powershell\r?\n(.*?)\r?\n```').Groups[1].Value
if (-not $m602Prep) { throw 'Development preparation block missing' }
. ([scriptblock]::Create($m602Prep))
```

**IDENTITY / PREPARE:** `git status --short`, `git rev-parse HEAD`, check absent active lease, compare the entry inventory's protected hashes and lock/frozen identities. Hash the exact code list above and unchanged configurations before each actual case; compare with A-accepted values recorded here before B. The primary also pins relevant test/command identities at each evidence acceptance. Inspect root/ancestors with `Assert-M105OrdinaryPath`, `Get-Acl`, and `git check-ignore`; create only missing exact evidence root with `New-Item -ItemType Directory -Path 'temp/m602-generation-evidence' -ErrorAction Stop`. No case directories are precreated. Inspect existing three `dist/client` files against entry inventory before the ordinary Vite replacement build; any unknown file stops preparation. No dependency/model/browser acquisition.

**OFFLINE:** every test uses synthetic in-memory packages or isolated `temp/m602-test-<UUID>` roots and native doubles, never actual credentials/frozen operations. Focused runner:

```powershell
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/m602-generation.test.ts tests/generation-contract.test.ts tests/generation-stage.test.ts tests/ollama-generation-contract.test.ts tests/ollama-generation.test.ts tests/groq-generation-contract.test.ts tests/groq-generation.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'M6-02 focused verification failed' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
}
```

The Red command runs the same test list; the expected new-module/export failure may be accepted only under the first-module exception after preflight and independently established runner readiness. Blocked assertions are not claimed executed. Green must execute every unchanged test and strict check successfully.

After Green and before A review, primary also runs this read-only real-package check through `Invoke-M105Command`, piping JavaScript to `& $m105Node --input-type=module` and requiring exit 0. This binds the default trust anchor/wire table without an operation entry, adapter preparation, credential read or request. It is separate from portable default regression and requires the retained frozen files. The loop authenticates bytes only; it executes no generation.

```javascript
import assert from 'node:assert/strict';
import { loadM602Package } from './tests/helpers/m602-package.ts';
for (const label of ['local-image','local-label','local-contrast','groq-image','groq-label','groq-contrast']) {
  const result = loadM602Package(label);
  assert.equal(result.status,'ready');
  console.log(JSON.stringify({caseLabel:label,identities:result.value.identities,wire:result.value.wire}));
}
```

**LOCAL CASE / GROQ CASE:** each line below is a separate manually authorized step after preceding assessment, never one pasted batch. Wrap each in `Invoke-M105Command`, test `$LASTEXITCODE`, and stop on nonzero. No extra actual-call flags or alternate parameters are permitted.

```powershell
& $m105Node tests/helpers/m602-run-case.ts --execute --case local-image
& $m105Node tests/helpers/m602-run-case.ts --execute --case local-label
& $m105Node tests/helpers/m602-run-case.ts --execute --case local-contrast
& $m105Node tests/helpers/m602-run-case.ts --execute --case groq-image
& $m105Node tests/helpers/m602-run-case.ts --execute --case groq-label
& $m105Node tests/helpers/m602-run-case.ts --execute --case groq-contrast
```

Local uses unchanged `QWEN_CONFIGURATION`, the exact digest above, Ollama 0.33.3, Q4_K_M, 32768 context, 4096 reserve and current template/parser identities. Adapter-owned metadata allows at most one each version/show/tags within each operation, then at most one chat; failed preparation is consumed without a generation execution. Automatic offload is observed, not tuned. Groq uses unchanged `GROQ_CONFIGURATION`, fixed official endpoint and exact model, `m304-groq-request-bytes-v1` and `groq-gpt-oss-20b-2026-09-11-v1` exposed-default identity, one chat and no metadata probe. Existing private credential loader runs only after entered marker inside actual preparation. Public current model/strict/deprecation docs and non-secret account availability/request/token limits must be established before Groq; unknown account readiness blocks those cases without a synthetic request.

**LOCAL OBSERVATION:** before the first Local case, start one task-owned application with the exact README `Run the local service` PowerShell block, ordinary empty scanner scratch, actual HEAD revision and port 0. Use its returned ready URL in a dedicated Chrome tab via CUA; do not activate Analyze or any retrieval/generation control. During each chat, focus the target textbox and change the Local/Groq radio; verify focused/checked accessibility state and record UTC bounds. This is responsiveness observation, not broad accessibility qualification. Missing the native window stops subsequent cases without repeating.

Take one GPU sample immediately before each Local operation and one during its chat, with timestamp around the command; require one finite row, nonnegative numbers, successful exit. Use `C:\Windows\system32\nvidia-smi.exe --query-gpu=memory.used,memory.free --format=csv,noheader,nounits`. Record memory observations without adding capacity thresholds.

Exactly one additional in-call Local metadata GET is allowed, only to `http://127.0.0.1:11434/api/ps`. Execute the following through `Invoke-M105Command`, piping this JavaScript to `& $m105Node --input-type=module`; it prints only the approved projection. No redirect, retry, credential or raw body output. A failed observation stops continuation even if generation completes.

```javascript
import http from 'node:http';
const expected = '2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd';
let settled = false;
const finish = (ok, value) => {
  if (settled) return; settled = true; clearTimeout(timer);
  console.log(JSON.stringify(ok ? value : {error:'local-observation'}));
  if (!ok) process.exitCode = 1;
};
const req = http.get('http://127.0.0.1:11434/api/ps', res => {
  const chunks = []; let size = 0;
  if (res.statusCode !== 200 || !/^application\/json(?:\s*;|$)/i.test(res.headers['content-type'] ?? '')) {
    res.destroy(); req.destroy(); finish(false); return;
  }
  res.on('data', chunk => { size += chunk.length; if (size > 65536) { res.destroy(); req.destroy(); finish(false); } else chunks.push(chunk); });
  res.on('error', () => finish(false));
  res.on('end', () => {
    try {
      const data = JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(Buffer.concat(chunks)));
      const matches = data.models.filter(m => m.name === 'qwen3.5:4b');
      if (matches.length !== 1) throw new Error();
      const m = matches[0];
      if (m.digest !== expected || m.context_length !== 32768 || !Number.isSafeInteger(m.size) || m.size < 0 || !Number.isSafeInteger(m.size_vram) || m.size_vram < 0) throw new Error();
      finish(true,{observedAt:new Date().toISOString(),digest:m.digest,contextLength:m.context_length,sizeBytes:m.size,sizeVramBytes:m.size_vram});
    } catch { finish(false); }
  });
});
req.on('error', () => finish(false));
const timer = setTimeout(() => { req.destroy(); finish(false); },5000);
```

**READBACK / RUBRIC:** use the same wrapper with `& $m105Node tests/helpers/m602-run-case.ts --readback --case <exact-label>` (substitute only one of the six fixed literals). Read original validated proposal locally and evaluate every material claim against frozen facts/passages; never paste its content into tracked documentation/chat. Primary publishes the exact assessment object with Node `openSync(path,'wx')`, complete write, `fsyncSync`, `closeSync`, then hashes/readbacks it. Failure preserves partial bytes and blocks continuation. No other role writes assessments or semantically rescores outputs. Independent reviewers audit evidence and method.

**FULL:** after last accepted executable change, run the exact maintained README sequential 46-file suite plus `tests/m602-generation.test.ts` in its browser-free list; all existing module-mock flags and scratch assignments remain unchanged. Capture/actual flags must be absent. Run independent strict TypeScript and `& $m105Node node_modules/vite/bin/vite.js build --configLoader native` through the wrapper after approved build-inventory check. No concurrent app/browser test. Reuse fresh complete evidence only under the full workflow identity rule.

**CLEANUP / DOCS:** stop only the owned application with stdin `stop`, require service-stopped/exit 0 and exact environment restoration; close only its CUA tab. Do not manage Ollama. Test cleanup may remove only its exact ordinary synthetic `temp/m602-test-<UUID>` child after ancestry/content verification, using native PowerShell `Remove-Item -LiteralPath` if coordinator cleanup is needed. Require existing managed scratch roots empty before/after; preserve the evidence root, all case records, all original inventory roots, data/runs and freeze. Check protected hashes, actual Git diff/untracked files, local links/anchors, PowerShell syntax, UTF-8/newline/status consistency and `git diff --check`. Any unexpected mutation or cleanup uncertainty stops advancement.

#### G contract: A proof packet and stop rules

One standard, nonvisual slice `A-fixed-case-execution` under workflow `m602-six-fixed-generation-executions`; meaningful behavior requires test-worker preflight and Red, then separate code-worker Green under exact leases. Maximum chain: initial plus one correction, conditional third write only with documented progress/new evidence; one review correction loop. No budget renews a real case. S3 triggers are private egress, immutable identity, single-use dispatch and uncertain-result recovery. A requires fresh critical implementation review; C requires a different fresh critical reviewer.

Negative/positive evidence must cover G1–G6: all hash/newline/order/case/schema/provenance/canonical faults with zero native calls; complete input reconstruction and full notices; common candidate unknown fields/getters/proxies/sparse arrays/invented references/lexical/secret-echo rejection and frozen detached output; exact native body identity/bytes and fixed controls/routes; Local fit versus Groq byte admission; occupied/partial/reparse/hardlink/invalid records; complete-write/flush/close/terminal-publication failures; prior assessment gating; marker throw/reentry/caught duplicate/config mutation/async return/deadline; shutdown, late settlement and uncertain cleanup; prepared Groq clearing; readback with zero credential/adapter effects. Preserve existing stage tests including attempts/reentrancy around lines 707–869. Default tests use controlled doubles only and never require private frozen files.

Any failed/invalid/unknown case, semantic failure, missing Local observation, OOM/unusable interface, drift, secret exposure or uncertain cleanup stops subsequent generation. Finish unaffected offline verification/documentation and ask only for the missing owner prerequisite/disposition. Do not reopen mechanism selection merely because a model fails. Reopen the contract if production eligibility/provenance changes, frozen inputs cannot pass unchanged adapters, shared extraction changes existing failure semantics, or a broader framework becomes necessary. Completion remains contingent on all six actual executions and task Verification; these contract reviews cannot substitute for them.

### M602-A-PREFLIGHT-01 — Reconciled missing behavior and Red packet

Read-only preflight confirms existing hardened candidate reading and covered stage behavior, but no exported candidate-only reader, shared execution callable/hook, frozen-package loader or one-case evidence/CLI mechanism. Its initial CONFLICTING result identified the two test-interface issues resolved by the reviewed G amendment. Primary narrows Red to those missing boundaries, preserving existing stage/contract tests as regression. No test asserts that existing extraction behavior was absent.

Workflow `m602-six-fixed-generation-executions`, slice `A-fixed-case-execution`, assignment `m602-a-red-01`, owner `m602-test-worker`, phase `red`, attempt 1, correction parent None. Standard nonvisual S3; TDD applicable. Exact acceptance, callable types, placement/dependency/refactor constraints and G1–G6 negative packet are the accepted contract above. Allowed files are only `tests/m602-generation.test.ts`, `tests/generation-stage.test.ts`, `tests/generation-contract.test.ts`; no allowed directory root. All application source, substantive helpers, documentation, configuration/dependency/frozen files, retained evidence and Git mutations are forbidden. Isolated ignored `temp/m602-test-<UUID>` scratch is allowed only for synthetic tests with ordinary-path guarded cleanup; no actual runtime/provider/credential effects.

Use the exact G OFFLINE command at repository root with maintained preparation. Expected initial Red is missing `src/server/generation/generation-execution.ts`; behavioral assertions behind the missing callable are not yet executed. Green must execute every unchanged assertion plus independent strict TypeScript. Entry runner readiness is the fresh 67-test/strict pass with unchanged source/test/config bytes; relevant preflight fingerprint is `7ccfdcb057eaf43899235f760dadfd8d9a482393a59aa803121e64d6fd55ce91` for 198 sorted path/hash entries (all source TS/TSX, tests TS, package/lock/tsconfig). Node v24.20.0, PowerShell 7.6.5, Python 3.12.10 and unchanged lock are present. Protected 430 files, 126 source, 70 tests/helpers and three build files match the entry inventory.

The guard digest is inserted into the dispatched complete Milestone Assignment Packet v2 after primary starts the exact lease. One initial Red write is authorized; later turns require terminal close and a fresh packet/lease. Existing chain limits remain initial plus one correction and a conditional third, at most two identical failures without new evidence or two no-diff outcomes, and one review correction loop. Worker stops for any binding/scope/authority conflict or unapproved effect. Primary accepts actual diff and expected failure only after compliant closure. No Red result or implementation acceptance is claimed by this assignment.

### M602-A-RED-01 — Initial test handoff rejected

Lease `m602-a-red-01`, digest `a270b53d7caa51a8cfd22aac71e12cd9607dbe8d13d1af13138e1047b13238d5`, closed compliant with receipt `1a65ed6de5b4906282a2dc826203c2cee53a2f28871ab5602016645930a40587`; only the three authorized tests changed. The exact missing-module/export failures occurred and 83 unaffected adapter tests passed, but none of the seven new top-level tests executed. Strict checking correctly did not run after expected Red.

Primary rejected semantic test acceptance: the supposed positive synthetic package invented noncanonical corpus/passages, invalid source/seed run shapes, one rather than three cases, wrong notices and non-frozen paths. Its passing expectation would require weakening the authenticated reconstruction contract. The new shared-stage success callback returned a shallow-frozen mutable candidate while asserting deep freezing; marker-throw cleanup expected certainty although the existing dispatch boundary marks thrown collaborator cleanup uncertain. Required new publication/assessment/readback and hook reentry/mutation/expiry paths also lacked decisive assertions. No production defect is inferred from an intentionally absent callable.

Authorize ordinary same-contract test correction `m602-a-red-02`, attempt 2, parent `m602-a-red-01`, same role/scope/acceptance. Replace fixtures with valid public native/corpus reconstruction through the exact virtual frozen paths and complete three-case/six-binding manifest; use existing proposal validation for a truthful frozen success callback; preserve existing cleanup semantics; add the missing G failure-path assertions without duplicating covered adapter policy. Demonstrate fixture validity using existing pure validators before the expected missing-module command. No new scope or budget is granted. No accepted test boundary exists yet; conditional attempt 3 remains unopened. Active lease will be `m602-a-red-02` only after guard start succeeds.

### M602-A-RED-02 — Conditional final test correction

Attempt 2 closed compliant, digest `7f81f48a555a955fc33d99e4dbe56cb32cb045d5501cb38545707545eba10453`, receipt `8f62b2d7fa5cfea6ca016fcdd02238b7aff64aad17aa1878fc2e31817ccecf9a`. The three public synthetic source runs now pass native/run validation, canonical triples and full notices are real, the manifest has six bindings, and marker/recovery coverage materially increased. The exact missing-module Red remains, with 83 unaffected adapter tests passing; sixteen new top-level assertions remain unexecuted. Primary still rejects test acceptance.

Remaining concrete errors are absent seed bytes/exact source mapping, negative rehash vectors leaving stale execution hashes (masking their intended semantic cause), Groq operations without accepted Local predecessors, observation timing tied to Date.now call count, a historical scanner dimension used as a current-generation failure gate, and pending-admission timeout incorrectly claiming complete cleanup. Simulated close failures also need descriptor-safe fixture cleanup. These are test corrections, not permission to weaken G.

Conditional attempt 3 is justified by the new passing native/corpus diagnostic and materially corrected fixture structure. The changed bounded action is to complete the seed/source byte map, maintain all hash bindings in semantic vectors, create predecessor acceptance through the same synthetic operation path, control wall-clock time at native reply boundaries, and align cleanup/assessment assertions with G. Existing helpers supply each needed primitive; this is likely to resolve the remaining concrete errors without new production scope. Authorize `m602-a-red-03`, phase red, attempt 3, parent `m602-a-red-02`, same role and exact three-file scope. This is the final test-chain write allowance; unsuccessful handoff requires owner disposition, not a replacement ID/agent or an implicit primary test rewrite. Green remains unopened and all real cases unused.

### M602-A-RED-03 — Rejected test boundary and owner handoff

**Owner disposition (2026-09-20):** The owner authorizes the proposed bounded primary exception below. Primary may correct only `tests/m602-generation.test.ts` and `tests/generation-stage.test.ts` as described, with no active worker lease, followed by focused validation and fresh evidence acceptance. The exhausted worker test chain remains exhausted. This supersedes the proposal's earlier unapproved status, not its scope or stopping conditions.

**Authorized correction applied:** Primary changed the five new credential injections to `credential.io` and used the existing `groqGenerationRequest()` only in the prepared-credential test, preserving its deliberate post-preparation fit rejection and other assertions. The fresh preparation diagnostic passes: prepared credentials clear after abort, a fresh-capability replay remains inert, and zero native calls occur. Syntax and `git diff --check` pass. The exact G OFFLINE command exits 1 on the expected missing `generation-execution.ts` module and `validateProposalCandidate` export; 83 unaffected adapter tests pass and eighteen new top-level tests remain unexecuted. Strict checking remains behind successful focused execution. Current SHA-256 values are `561f96bdf7f0902d90c2cc356d9d77fd6d4d2a8a1f5078c374d77eaf8e694986` (M602 test), `582a80e259aefb8717a6cee0de9222c217a625a35c34ef79205e8e269387dea1` (stage test), and unchanged `aec9b21c8e9ffde487522dab5f5aef4e144b6a4da235d751cfd53c2feeb547d4` (contract test). These supersede the rejected test identities; acceptance awaits the required complete coverage reconciliation. No fourth worker turn or production edit occurred.

The final test lease `m602-a-red-03`, attempt 3 with parent `m602-a-red-02`, closed compliant at digest `0604a45b74befdfdd765be9901e9b1d8a7fff5ccd74e0c43a25eaab905c3efca`, receipt `c4dcb59fd635f4aa155659f2edbf93a0af494147eb32d4a575291ed6e8d5d05c`. There is no active lease. Final test SHA-256 identities are `92abce1c4248749c2b33d6aa8b1b22d204d69736a8ecba439c2f7ef66699c24d` for `tests/m602-generation.test.ts`, `96b2909d4c248b469ce22fcf0aa52de19959b631abe53b56ec9d30cd4f5b240c` for `tests/generation-stage.test.ts`, and `aec9b21c8e9ffde487522dab5f5aef4e144b6a4da235d751cfd53c2feeb547d4` for `tests/generation-contract.test.ts`.

The worker's pure reconstruction diagnostic passes for all three profiles, exact virtual source/seed paths, canonical guidance, full notices, six bindings and distinct authenticated instructions. Static syntax and whitespace checks pass. The focused command again stops on the exact absent module/export, with 83 unaffected adapter tests passing; eighteen new top-level tests remain unexecuted. Strict checking did not run after this expected link failure. These observations do not establish a valid Red boundary.

Primary inspection rejects acceptance for two independently reproduced fixture defects:

- `virtualCredentialIO()` returns a harness with an `io` member. New tests pass the harness itself as `credentialIO`, while the accepted interface and unchanged real adapter require `GroqCredentialIO`. This affects the Groq positive, shutdown and prepared-credential paths; production must not unwrap a test harness to accommodate them.
- The prepared-but-undispatched test in `tests/generation-stage.test.ts` gives the real Groq preparer empty messages and an unrelated empty schema, then expects preparation success. The existing fit boundary rejects it as `input-fit` before credential loading or the intended post-preparation fit mutation.

A primary inline Node diagnostic using the maintained environment and existing pure/synthetic helpers exits 0: the exact invalid request returns `{ok:false,error:'input-fit',cleanup:'complete'}`; a valid `groqGenerationRequest()` with the wrong harness returns `{ok:false,error:'configuration',cleanup:'complete'}`; that valid request with `harness.io` prepares successfully and is then aborted. Native calls and real credential reads are both zero. No new file, provider operation or production change was needed for reproduction.

The smallest proposed owner exception is a bounded primary test-side correction in the two affected test files: supply `credential.io` at every new credential-I/O injection and use the existing valid `groqGenerationRequest()` for the prepared-credential test, while preserving its deliberate post-preparation fit rejection and all semantic assertions. This proposal is not authorized or applied. Before acceptance, reconcile the complete G1–G6 coverage map, run the focused Red command and record fresh test hashes/evidence; any additional defect outside the approved exception returns to the owner. No guard attempt 4 or disguised replacement chain is permitted. Green and all actual operation allowances remain unopened.

Fresh read-only preservation verifies all 430 protected files, all 126 original source files, all three existing build files, all 13 recursive frozen references and unchanged HEAD. The real M602 evidence directory is absent. No owned application/browser process requires cleanup. Full current regression, strict/build and implementation reviews remain required after accepted Green; no C completion or archive is claimed at this blocked handoff. The task-contained test correction remains in this plan under the bug-index exception for routine Red corrections.

Blocked-handoff documentation checks pass: 428 local links/anchors across the five changed documents, all four plan PowerShell blocks, UTF-8/final-newline/whitespace checks and `git diff --check`. The only untracked file is the directly inspected new test. Documentation impact: updated this plan, roadmap, README status and both progress mirrors; no requirement, ADR, dependency or production source changed. Seven tracked files are modified and one test is untracked; nothing was committed or pushed.

### M602-A-COVERAGE-01 — Remaining qualification gaps

**Owner disposition (2026-09-20):** The owner explicitly authorizes the complete proposed primary test-only coverage exception below. No worker lease is active. Primary may edit only `tests/m602-generation.test.ts` for the three named coverage families, then run the specified verification and accept fresh evidence. Earlier Red identities are invalidated. This authorizes neither a fourth worker lease nor additional generation operations; all six real operation allowances remain unused.

**Primary acceptance:** All three coverage families are now present. Read-only test-owner reconciliation confirms semantic validity, coherent rehashing, ordinary filesystem doubles and complete package/sent/result wire linkage, with no additional decisive defect. The exact focused command confirms the expected missing shared module/export and 83 passing unaffected adapter tests; syntax and whitespace checks pass. Primary accepts this as initial Red under the first-module exception, not executed proof of the blocked assertions. Accepted hashes are `70c6b5a8c97da54232b10505f00cbf77233cf0aa2a3a20b30c175fc14621c8ba` (M602 test), `582a80e259aefb8717a6cee0de9222c217a625a35c34ef79205e8e269387dea1` (stage test), and `aec9b21c8e9ffde487522dab5f5aef4e144b6a4da235d751cfd53c2feeb547d4` (contract test). Relevant-tree identity is `9e34f4a68f96aae76d3940690119ece5c67b94eef276d3aa07c92e3d3f41da04` across 199 sorted source/test/configuration path-hash entries; cwd, Node v24.20.0, PowerShell 7.6.5 and lock identity remain unchanged. Every accepted test is immutable during Green. Both explicit primary exceptions are completed; they do not reopen the exhausted worker test chain.

After the authorized fixture repairs, the same test owner performed a read-only complete G1–G6 audit and returned PARTIAL. Primary accepts the two repairs and the audit's evidence mapping, not a complete Red boundary. The audit made no edits and reused the fresh primary Red/diagnostic evidence rather than rerunning it. Existing tests cover parser/labels and dependency isolation; canonical reconstruction and several corruption families; shared hostile candidate/eligibility rules; adapter fit/privacy/routes; publication/redispatch/marker/deadline/abort; assessment and raw-output retention. The explicit G proof packet still has these gaps:

1. Package fault vectors lack reordered, unknown, excess and conflicting passage coverage, plus wrong execution case bindings/order. Existing missing/duplicate/wrong-profile/altered-passage vectors remain valid but do not cover the entire named packet.
2. Evidence readback lacks reparse-point, hardlink, unexpected-name, oversized and malformed record qualification. Existing occupied/partial directories and changed entered hashes do not establish these boundaries.
3. The native-body test compares the sent bytes with retained accounting, but does not directly compare them with authenticated `loadM602Package(...).value.wire`.

**Proposed expanded owner exception, not yet authorized:** Primary completes only these three test-coverage families in `tests/m602-generation.test.ts`, retaining the already-corrected Groq fixtures, existing assertions and accepted G interfaces. Use the current canonical in-memory bundle with coherent rehashing for semantic vectors; existing native doubles and ordinary isolated `temp/m602-test-<UUID>` scratch; injected filesystem stat doubles for link/reparse rejection so no real link or privileged operation is needed; exact sent/result/package wire equality for both modes. Add no production code, test framework, dependency, private frozen payload or real provider operation. Run the same focused Red command, pure fixture checks where needed, syntax/whitespace checks and complete coverage reconciliation; record fresh hashes before accepting Red. This is an explicit primary exception, not a fourth worker lease or renewed worker chain. Additional unrelated defects remain outside scope. Green and all six real operation allowances remain unchanged.

The prior owner grant covered two named fixture repairs only; its own handoff rule returns additional defects to the owner. It is not used to move missing behavioral assertions into an implementation-owned test turn or silently replace the required proof with source inspection. The smallest next owner action is to approve this named test-only coverage exception. No requirement, G contract or production behavior changes are proposed.

### M602-A-GREEN-01 — Milestone Assignment Packet v2

**Identity:** workflow `m602-six-fixed-generation-executions`; task `M6-02`; work slice `A-fixed-case-execution`; assignment/lease `m602-a-green-01`; phase `green`; attempt 1; correction parent None; role `code_worker`; owner `m602-code-worker`. Guard digest is supplied by primary after exact guard start and before dispatch; no write is authorized without it.

**Capsule:** This plan's accepted M602-G-CONTRACT-01 and M602-A-COVERAGE-01 own the exact observable interfaces, records, commands and accepted test boundary. Readiness is the accepted M6-01/M2-02/M3-03/M3-01 evidence linked under Authorities and prerequisite evidence; frozen manifest and all 13 references match entry. TDD applies to the new common execution/candidate boundary and substantive evaluation mechanisms. Preflight M602-A-PREFLIGHT-01 identified missing behavior; the accepted corrected first-module Red above is its current evidence. Original test owner is `m602-test-worker`, with the two separately authorized primary exceptions recorded above. Controlling IDs are REQ-EVAL-001/002/004–009, SPEC-003/004, HS-008/009/015, ADR-0003/0004/0014/0024 and the linked generation/privacy authorities. No requirement or ADR decision remains open for this bounded implementation. Account/runtime prerequisites concern B only.

**Responsibility and dependency contract:** `proposal-contract.ts` extracts candidate reading while preserving production native/retrieval/support eligibility. New `generation-execution.ts` owns admission-bounded deadline, immutable adapter bindings, single-use marked transport, closed envelope/validation outcomes and cleanup; extract existing stage behavior, including BUG-0001 detached metadata protection. `generation-stage.ts` retains production input admission and maps the shared result to unchanged production invocation. `m602-package.ts` owns frozen authentication/reconstruction and exact wire preparation; `m602-operation.ts` owns exclusive per-case evidence, continuation/readback and native accounting; `m602-run-case.ts` owns only strict direct-entry argument parsing and safe summary. Production never imports tests. Reuse existing validators/adapters/fit/credential/native helpers. Permitted structural refactor is the local stage/candidate extraction and private cohesive functions inside these six owners. No generic framework, duplicate provider stack, new product endpoint, UI, dependency, configuration or production eligibility bypass. A material cohesion or binding conflict returns to primary reconciliation.

**Write scope:** allowed files only `src/server/generation/proposal-contract.ts`, `src/server/generation/generation-execution.ts`, `src/server/generation/generation-stage.ts`, `tests/helpers/m602-package.ts`, `tests/helpers/m602-operation.ts`, `tests/helpers/m602-run-case.ts`; allowed directory roots None. Forbidden files: the three accepted tests above, `README.md`, `package.json`, `package-lock.json`, `tsconfig.json`. Forbidden directory roots: `docs`, `evaluation`, `corpus`, `.agents`, `.codex`, `data`. Every other tracked/untracked endpoint is outside scope; no Git writes or metadata changes. Synthetic ignored `temp/m602-test-<UUID>` scratch may be created by focused tests and removed only after ordinary-path validation inside its exact root. Original ignored evidence is read-only. No actual case root, runtime/provider request, credential read, service/browser process, acquisition or full build is authorized for this worker.

**Validation:** repository cwd; exact maintained preparation and G OFFLINE command above. Every unchanged accepted behavioral assertion must execute and pass, followed by independent strict TypeScript. Run focused checks at the Green boundary; repeat after an actual structural refactor or changed failing code. Primary owns the separate default real-package qualification, current README full suite/build and S3 review after handoff. Baseline relevant-tree fingerprint is the accepted Red identity above; environment is Node v24.20.0/PowerShell 7.6.5/unchanged lock. Known side effects are synthetic scratch only, with existing bounded cleanup; no network or real credential effects. Expected result is complete six-file Green with unchanged tests, no behavior weakening and explicit RETAINED/REFACTORED/RECONCILE cohesion disposition.

**Budget and stops:** initial Green turn, one ordinary same-contract correction and one conditional final correction only after documented progress/new evidence; test chain remains exhausted. At most two identical decisive failures without new evidence or two no-diff outcomes; one review correction loop. Stop for failed gate, test defect, scope/binding/authority conflict, required dependency, ambiguous side effect, unexpected diff or exhausted budget. A worker stop returns control to primary, not implicit owner approval. Handoff must report exact identity/digest, touched paths, commands/exits and decisive results, cohesion disposition, residual risks and documentation impact. Primary closes the lease and inspects the actual diff/evidence before accepting.

### M602-A-GREEN-01-RESULT — Source encoding reconciliation

Initial Green closed compliant with guard digest `32c8252a359ce235377807cd0e2aec7e4453a1bc6c07065cec3506c2a8a159c3` and receipt `34deab564e964162b7fa033474f047cbfbca7d6294ef89bc30d45987df9d3755`. Exactly the six allowed code files changed; accepted tests, Git/index/ignore controls and original evidence remain unchanged. The worker reported TEST CONTRACT CONFLICT and stopped. Both focused runs had 138 passes and seven package-related failures; the second localized the source rejection. Temporary diagnostics were removed and synthetic scratch cleaned. Strict checking did not run after failed focused verification. No Green/cohesion acceptance or actual operation is claimed.

Primary independently confirms that all 13 manifest reference hashes match, but the public corpus manifest, passage catalog and M201 gold retain 186, 261 and 136 CRLF sequences respectively. RD003 has no CR. All nine local frozen artifacts and all six original M204 source/seed files have no CR and end in LF. The archived M301 verifier applies its LF byte reader only to the nine local artifacts, then separately hashes the four public references. G's blanket no-CR sentence therefore conflicts with preserved source bytes and the accepted synthetic fixture's exact public-byte reads.

The source files must remain unchanged. Primary is reconciling only that source encoding boundary through the analyst's now-completed bounded correction and the second final-artifact correction cycle, with complete G1–G6 final research review before another write lease. This is not new research scope, a replacement test chain, normalized hashing, or permission to change messages/wire bytes. Initial Green used one write turn; its ordinary correction and conditional final correction remain subject to accepted reconciliation and fresh leases.

**Second G correction cycle:** The analyst used its sole bounded correction and independently returned DRAFT READY, reproducing the original-byte and archived-algorithm evidence. Primary authored the exact path-role newline clarification in G: every authenticated file retains bounded ordinary-path, raw-hash, strict UTF-8 and BOM checks; only the four exact public references use their hash-fixed original newline representation. The manifest, nine local artifacts and original source/seed files retain no-CR/exact-final-LF checks. No semantic input, schema, message, provider, test, operation or recovery binding changes. Complete revised-artifact G1–G6 review is required before Green02; this uses the second final-artifact correction cycle without renewing any researcher, analyst, test or generation allowance.

**Cycle-2 acceptance:** Complete final G1–G6 review of artifact `c884ab8c68febf241a3bd2f3222c5dbfc2a441c2487bc6be4c73695778dc7dce` returned PASS WITH FOLLOW-UPS, no Blocker or Major. The reviewer independently reproduced all reference hashes, exact newline counts, source/seed preservation and normalization mismatch. Minor F2 identified stale current summaries of the repaired fixtures and unopened implementation; primary reconciled Current state, Surprises and Outcomes before Green02. The original blanket no-CR interpretation and its earlier review acceptance are superseded. Primary accepts the revised G boundary; no requirement, ADR, frozen byte or generation allowance changes.

### M602-A-GREEN-02 — Ordinary correction packet

**Identity and inherited packet:** workflow `m602-six-fixed-generation-executions`; task `M6-02`; cycle `A-fixed-case-execution`; assignment/lease `m602-a-green-02`; phase `green`; attempt 2; correction parent `m602-a-green-01`; owner `m602-code-worker`; role `code_worker`; standard nonvisual S3. The complete M602-A-GREEN-01 Milestone Assignment Packet v2 remains binding for objective, authorities, dependencies, acceptance, six exact allowed files, every forbidden path, responsibility placement, permitted refactor, command/environment/cleanup contract and stopping conditions. Only the lease identity and reviewed source-reference newline clarification change. Guard digest is inserted into the dispatched packet after primary start; no writes before it.

**Correction and evidence:** The decisive failure was `textBytes` applying the local LF rule to all references. Search of the reader's callers, accepted fixture, all 13 raw references and archived algorithm confirms that only four exact public source paths require their original hash-fixed newline representation. Correct that distinction in the package reader using the same path-role rule for default and injected authentication; preserve bounded strict UTF-8/BOM/raw-hash checks and local/source/seed LF checks. Complete the unchanged focused command and strict TypeScript; resolve any implementation-only failure within the six-file scope, without changing an accepted test or source artifact. Tests remain pinned to `70c6b5a8c97da54232b10505f00cbf77233cf0aa2a3a20b30c175fc14621c8ba`, `582a80e259aefb8717a6cee0de9222c217a625a35c34ef79205e8e269387dea1` and `aec9b21c8e9ffde487522dab5f5aef4e144b6a4da235d751cfd53c2feeb547d4` respectively. The 138/7 failed Green is diagnostic evidence only. All 430 protected files and HEAD still match entry; no real evidence root or active lease exists before start.

**Budget:** This uses the ordinary Green correction. Only a separately justified conditional attempt 3 may remain; the test, researcher, analyst and final G correction budgets are exhausted. No real operation is authorized by this worker packet. Primary performs terminal close, actual diff/evidence/cohesion inspection, separate real-package qualification and fresh S3 review before A acceptance.
### M602-A-GREEN-02-RESULT — Behavioral pass and test typing boundary

Green02 closed compliant with digest `d4a799f25a9a70093b6186ebeb47486f903421513070b147e046e797050f0656` and receipt `d4dc4aac152420308fea2180c333ceb96323f84689f7410be424734cd3889aad`. Only package and operation helpers changed; accepted tests, Git/index/ignore controls and original evidence remain unchanged. The package reader now preserves exact public-source bytes and removes an unsupported singleton-source restriction, retaining selected-first native equality and complete source-run validation. All 145 focused tests pass. Strict checking has one remaining test-owned diagnostic at `tests/m602-generation.test.ts:83`: the shared seed fixture returns `Finding`, whereas `buildFindingAnalysis` requires `NativeFinding`. Primary independently reproduced that sole diagnostic; Green/A remain unaccepted.

The concrete proposed correction is one runtime narrowing assertion immediately after `const initial = seed.completed.scan.findings[0];`: `assert.ok(initial.state === 'unprocessed');`. It checks the existing seed invariant without a cast, weakening an assertion, or changing product behavior. It is outside the two earlier completed owner exceptions and the exhausted test chain. The owner explicitly authorized this single-line primary correction on 2026-09-20. Primary applies only this assertion, invalidates the previous test identity, runs focused and strict checking, and reaccepts the test boundary. No fourth test lease or generation allowance is implied. No worker lease is active. A virtual compiler diagnostic attempt made no writes and could not run because TypeScript 7 does not expose the legacy compiler API at its root; it supplies no correction proof.
### M602-A-GREEN-ACCEPT-01 — Corrected offline implementation

The owner-authorized single fixture assertion is applied and all 145 focused tests pass, followed by independent strict TypeScript exit 0. Current M602 test SHA-256 is `cb528959851b707e8869e7f7f9e0d4e5a2edd5ee527bea29c11dbe6ff5185095`; the other two accepted test hashes remain unchanged. Primary accepts the corrected test boundary; the earlier test hash and strict failure are superseded. No production edit or new worker lease was used for that exception.

Primary inspected the complete six-file implementation and Green02 changes. RETAINED cohesion is accepted: candidate reading and production eligibility remain separated; common execution owns deadline/capability/envelope/cleanup; the stage owns production admission; the three evaluation helpers own package authentication, evidence lifecycle and CLI respectively. No production dependency on evaluation code exists. Green implementation is accepted for S3 review, not actual use.

The default read-only package check passes all six labels, reproducing every fixed wire hash and byte count in G without adapter preparation, credentials, HTTP, case directories or generation. Relevant-tree SHA-256 is `93faca0b3b6b468baaf7fdd557e3281abe190d2893ebedba1863825e1865a581` over 203 sorted `{path,sha256}` entries for source/test TS/TSX and package/lock/tsconfig. The G operation-code fingerprint is `d854bfd7c84e60a4764a4b8a7f6193f2aeb45f312431ac752638390cbc4b273a` over 111 entries. Cwd, Node 24.20.0, PowerShell 7.6.5, unchanged lock and maintained preparation remain fixed.

Before the assertion, the maintained 47-file suite passed all 907 tests, and current Vite build passed after exact three-file build inventory validation. That complete run belongs to the prior test identity; current focused/strict proof supersedes its changed-test portion. Final C will establish the complete current-tree suite rather than claim an unchanged full-suite identity. Existing scanner/UI/integration scratch is empty and owned test processes closed. All actual entries and dispatches remain unused.

**Fresh S3 review packet:** Review the integrated six-file implementation and three accepted test files against complete G1–G6, production eligibility and BUG-0001. Inspect actual diff, responsibility placement and credential/egress, immutable byte identity, deadline/reentry, exclusive publication/partial-record handling, closed readback and recovery risks. Reuse the fresh controlled command evidence; independently reproduce only decisive missing or risk-critical evidence, with synthetic ordinary isolated scratch only. No writes, real credentials, provider/runtime requests, actual case roots, acquisitions, app/browser interaction or Git mutation. Return PASS/PASS WITH FOLLOW-UPS/REVISE/BLOCKED with exact findings and evidence. One review-correction loop and conditional Green03 remain; no test or research budget is renewed. Different fresh final review remains C-owned.
### M602-C-REGRESSION-01 — Current complete suite

After the authorized fixture assertion, primary ran the exact maintained 47-file suite sequentially through README preparation and `Invoke-M105Command`: 907 tests passed, zero failures, exit 0. All original module-mock flags, browser runtime and scratch assignments were retained; capture/frozen-case flags were absent. No actual model/provider request occurred. The relevant-tree fingerprint remains `93faca0b3b6b468baaf7fdd557e3281abe190d2893ebedba1863825e1865a581`, matching current focused/strict evidence. Current build evidence remains applicable because its client inputs, configuration and environment are unchanged by the test-only assertion.

Post-run checks verify all 430 protected files unchanged, no real case root or active lease, and empty scanner/UI/integration scratch. The maintained README command now includes the M602 test in its browser-free list. Five affected documentation owners have valid UTF-8/final newlines/whitespace, 429 real local links and two intentional template placeholders; all ten PowerShell blocks parse and `git diff --check` passes. Actual source and untracked implementation/test files were inspected separately. This accepts regression evidence, not A's pending S3 gate, B actual results, final review or task completion.
### M602-A-S3-01 — Readback coherence correction required

Fresh S3 review returned REVISE with one Minor F1 and no Blocker/Major. `validateResult` checks error membership but admits impossible combinations: input-integrity with attempted response/passed, or authentication with no attempt/observation/chat. The reviewer reproduced both through synthetic readback; primary inspected the corresponding implementation predicates and accepts the finding. Failed status still blocks continuation, so this is not a replay or additional-dispatch bypass. G's explicit inconsistent-record rejection requirement nevertheless prevents A acceptance.

The reviewer independently checked rejected asynchronous markers, late/terminal capability ownership, post-validation cancellation, synthetic Groq clearing, concurrent same-case entry and missing-assessment continuation. All checks except F1 passed; synthetic scratch and original evidence are preserved. Existing focused, strict, full-suite, build and static-package evidence remains truthful for the recorded version, but cannot discharge F1. No actual case has entered.

**Concrete proposed test exception:** Extend only `tests/m602-generation.test.ts` with one closed record-coherence regression family using its existing canonical synthetic package/native doubles and isolated ordinary scratch. Reject the two reproduced contradictions and the equivalent impossible error/attempt/observation combinations across the existing closed error list. Preserve truthful post-validation timeout/shutdown with response/passed, caller wire-mismatch response-validation with response/passed, and a complete marker without dispatch. Assert readback makes no new native/credential call and preserves the record. No new dependency, helper, production interface or record schema. The owner explicitly authorized this bounded regression matrix on 2026-09-20. Primary applies it with no active lease, invalidates the earlier test identity and establishes fresh failing evidence before conditional Green03. This fourth explicit primary exception does not reopen the exhausted test-worker chain.

**Conditional final Green justification:** Green02 resolved frozen-source admission and implementation type errors; all 145 focused tests, strict checking, six exact package/wire identities and the current 907-test suite pass after the authorized test narrowing. Fresh independent F1 evidence identifies a different, bounded missing predicate in `validateResult`. After the owner-authorized regression is accepted as failing on that predicate, a final attempt-3 lease with parent `m602-a-green-02` may correct only error/attempt/observation coherence inside the same six-file scope. Expected benefit is rejection of the reproduced impossible records while retaining genuine cancellation/wire/marker outcomes. This is the one review-correction loop and conditional final Green, not a renewed chain. No lease opens before test acceptance; no additional generation allowance is created.

The current matrix is: proposal/null requires attempted response/passed; abstained/null requires no attempt/observation; input-integrity/configuration/missing-prerequisite/input-fit require no attempt/observation; authentication/quota/rate-limit/network/provider require attempted matching outcome/not-run; response-validation requires attempted response/failed or response/passed when caller correspondence fails; timeout/shutdown may occur before or after attempt and must preserve truthful post-response observations. The correction must not fabricate an observation or erase marker/attempt truth.
### M602-A-S3-RED-01 — Accepted coherence regression

The fourth explicitly authorized primary test exception adds one 30-vector record-coherence family in `tests/m602-generation.test.ts`, using only its existing public synthetic package, native/credential doubles and guarded scratch. It checks both reproduced failures and the same error-class relationships, while preserving the review's truthful cancellation, wire-mismatch and completed-marker cases. These are simulated readback records; they do not claim actual cancelled model executions. Original stage/adapter execution evidence remains separate.

The complete focused command executes 176 tests: 160 pass and 16 fail (15 contradictory-record assertions plus the parent group), exit 1. The actual accepted values match F1: impossible records return `ok:true` instead of `evidence-blocked`. All positive readback vectors and five already-covered validation inconsistencies pass. Independent strict TypeScript passes. Primary inspected the added family and accepts this bounded Red; no production file changed. Current M602 test hash is `913aa550c445e8f16ab3906191e2852076994bb5b6b5a28e69272f1354261d12`; stage and contract tests retain their previous hashes. Relevant-tree fingerprint is `1fb4e09dc6f0c1b593f006418da05943871267f234e324d97c65f98d81153b62` over the same 203 entries. Earlier full-suite identity is superseded for current closure. An earlier execution's session identifier was not retained, so that output is not used as evidence; the recorded rerun was collected to completion. No synthetic scratch remains.

### M602-A-GREEN-03 — Conditional final correction packet

**Identity:** workflow `m602-six-fixed-generation-executions`; task `M6-02`; cycle `A-fixed-case-execution`; assignment/lease `m602-a-green-03`; phase `green`; attempt 3; correction parent `m602-a-green-02`; owner `m602-code-worker`; role `code_worker`; standard nonvisual S3. The complete Green01 Milestone Assignment Packet v2 is inherited unchanged for objective, authorities, dependencies, six exact allowed files and all forbidden scope, responsibility placement, command preparation, environment, expected outcomes, cleanup and stops. Digest is supplied after primary guard start. Tests are immutable at the S3-RED-01 hashes.

**Bounded action:** Resolve S3 F1 only by enforcing the reviewed error/attempt/observation matrix within `m602-operation.ts::validateResult`. Reuse the existing closed observation reader; do not change the schema, executor, adapter, tests, frozen inputs, provider policy or native accounting. Complete marker without actual dispatch and truthful timeout/shutdown or caller correspondence failure must remain readable. Run exact G OFFLINE focused command (176 tests) then strict TypeScript. Report cohesion, exact diff and results. No actual operation, credential, network, service/browser or build effect is authorized.

**Conditional budget justification:** The concrete progress, new independent F1 evidence, distinct corrective action and expected benefit are documented in M602-A-S3-01. Fresh accepted behavioral Red now verifies the missing predicate and preserves the required positive states. This opens the final attempt 3 under the unchanged role/phase/scope; it uses the sole review-correction loop. There is no further implementation turn or test exception beyond explicit owner authority. Stop on unexpected binding/test/scope failure; primary closes and inspects the handoff, reruns affected qualification/full regression as needed and obtains complete correction review. No worker Git mutation or plan maintenance.
### M602-A-GREEN-03-RESULT — Coherence correction verified

Final Green03 closed compliant with digest `425c643467e578fa06416580863d3120b5e3d1a5b50eaf761ae315b3fc2a5483` and receipt `62abe7bde8cc52c0299743d2596621f777897048adb8e985ac63ff56f2408b03`. Only `m602-operation.ts::validateResult` changed; file SHA-256 is `8a0d08a8210becda16cc7e1fcec017e64406a0a2e982a794afc7c987a38ddc74`. The predicate enforces the reviewed error/attempt/observation matrix, and allows response/passed response-validation only when native correspondence fails. Primary inspected the actual change and accepts RETAINED cohesion within existing record validation.

All 176 focused tests pass with zero skips/failures, followed by worker strict TypeScript exit 0. Primary independently repeats strict checking and the default six-package qualification successfully. All test hashes remain pinned at S3-RED-01. Current 203-entry relevant-tree fingerprint is `29fdbbb01d9f5a36ca611cc7b783cfc3e7a6761030058c5d8ad2f933df28ea4e`; current 111-entry operation-code hash is `a069571f11959ba95670be9142169962262e834d5c9c81791ed90bae191522b4`. Original manifest/configuration/wire identities are unchanged. No active lease or actual case root exists.

Primary accepts corrected Green and submits the complete revised implementation for the existing S3 review's sole correction loop, with F1 reproduction and all G1–G6 invariants. Reuse unaffected fresh evidence; independently check only changed, contradictory or risk-critical evidence. All three Green write turns, both final G cycles and the test-worker chain are exhausted; no implicit further correction is authorized. A acceptance still requires the corrected S3 verdict. Current full-suite closure evidence will be renewed for this changed helper/test identity; build inputs remain unchanged.
### M602-A-ACCEPT-01 — Corrected S3 acceptance

The existing S3 reviewer completed its sole full corrected-artifact review and returned PASS with no outstanding finding. F1 is resolved: both original contradictions reject, and six additional independent probes preserve valid cancellation, wire-mismatch and marker states while rejecting impossible observations. No new native/credential effect or byte mutation occurred during readback; synthetic scratch was removed. The reviewer confirms exactly 19 added validation lines and authenticates Green03's terminal receipt. Primary accepts A against the Green03-result code/test fingerprints. No requirement, ADR, schema or provider policy changed.

All implementation/test-worker chains and R3 correction allowances remain exhausted. B may proceed only under the existing prerequisite, one-entry/one-dispatch, observation, rubric and stop rules; no calls have occurred. The public Groq catalog, strict-output support and deprecation pages linked above were rechecked on 2026-09-20 local time: the exact model is still listed with strict support and no retirement entry was found. Account-specific availability/limits remain unconfirmed; public documentation does not establish them. The developer-managed Ollama process and loopback listener remain the original process; no runtime management or synthetic probe occurred.
### M602-C-REGRESSION-02 — Final corrected complete suite

The exact current maintained 47-file suite passes all 938 tests, zero failures, exit 0, following Green03 and the accepted coherence matrix. Cwd, command preparation, strict settings, browser runtime, absent capture flags and scratch assignments are unchanged. Relevant-tree fingerprint remains `29fdbbb01d9f5a36ca611cc7b783cfc3e7a6761030058c5d8ad2f933df28ea4e`. Current independent strict checking and six-package qualification pass; the existing successful build has unchanged client/configuration inputs. This supersedes earlier full-suite identities for the corrected executable/test version. Different final review and actual six-case evidence remain outstanding.

### M602-B-PREFLIGHT-01 — Private destination and approval boundary

After A PASS and current full regression, primary rechecked all 430 protected hashes, original HEAD, final operation-code fingerprint, ordinary Windows ancestry and empty managed scratch. Created only the exact ignored root `temp/m602-generation-evidence`; it inherits the previously inspected seven local ACL entries without mutation or an owner-only privacy claim. No case directory exists, so every entry/dispatch allowance remains unused.

The task-owned service starts through the unchanged README block, reports `http://127.0.0.1:62358`, and remains ready without Analyze, retrieval or generation. A dedicated Chrome tab shows the existing Analyze form, with Local selected and no target submitted. No original run changed.

Automatic approval review rejected the proposed first Local image shell command before process creation, stating that it did not accept the attached request as trusted authorization for real model generation and durable evidence writes. The primary verified the evidence root remains empty. No GPU sample, entry, metadata request or dispatch occurred; this is an approval rejection, not a failed generation execution. No alternate execution path was used. Direct owner authorization for the six exact operations has been requested, retaining every readiness/stop rule and all existing unused allowances. The owned service/tab remain available while that answer is pending; primary must close them if handing off without execution.

**Blocked handoff cleanup:** With the direct-authorization answer still pending, primary sent `stop` to its exact service session; the service reported `service-stopped` and the session closed. The dedicated browser tab was closed. No listener remains at the task's service port. All three managed scratch roots and the actual evidence root are empty; all 430 protected files and entry HEAD still match. The developer-owned Ollama process remains unchanged. A future authorized continuation must recreate only the owned service/tab through the existing procedure and repeat necessary readiness checks; no generation allowance has been used or replenished.

**Direct owner authorization received:** The owner explicitly answered “Authorize these six fixed operations” to the named Local image/label/contrast, then Groq image/label/contrast request, including frozen minimized packages, the existing Ollama runtime, Groq's fixed API/credential boundary, API capacity and durable `temp/m602-generation-evidence` records. This resolves the missing direct authorization; all prerequisite and stop gates remain. No operation ran before this answer, and Groq account availability/limits are still unknown. The rejected command may now be resubmitted through the approval boundary with this direct authorization; it is not a generation retry.

**Local status at current handoff**

| Fixed case | Entered | Dispatched | Outcome | Remaining allowance |
| --- | --- | --- | --- | --- |
| local-image | Yes | Yes, once | failed: response-validation | Consumed; no retry |
| local-label | No | No | not-run | One entry, at most one dispatch |
| local-contrast | No | No | not-run | One entry, at most one dispatch |

**Groq status at handoff**

| Fixed case | Entered | Dispatched | Outcome | Remaining allowance |
| --- | --- | --- | --- | --- |
| groq-image | No | No | not-run | One entry, at most one dispatch |
| groq-label | No | No | not-run | One entry, at most one dispatch |
| groq-contrast | No | No | not-run | One entry, at most one dispatch |

No proposal or rubric assessment exists for the failed Local image case; its result is preserved below. No result or observation exists for the other five not-run cases. Historical retrieval evidence remains in M6-01; offline package qualification supplies no actual generation result. Direct authorization does not establish Groq account readiness or waive any prerequisite. Every unused allowance remains subject to owner disposition of the first failure.

### M602-B-RESULT-01 — Local image failure and sequence stop

After the owner's direct six-operation authorization, primary verified original HEAD, all 430 protected files, both accepted code/tree fingerprints, all six default packages, unchanged inherited ACLs, empty actual evidence root and no active lease. The existing Ollama PID/listener was preserved. The task-owned service started through the maintained README block at `http://127.0.0.1:49228`; a dedicated Chrome tab selected Local without Analyze. No source, test, configuration or build changed.

The approved exact `--execute --case local-image` command created one entry and performed one each version/show/tags request, then exactly one chat. Native chat ran from `2026-09-21T02:14:37.072Z` through `2026-09-21T02:14:50.816Z`; the result completed at `02:14:50.818Z`. Outcome is `failed`, error `response-validation`, attempted `true`, observation outcome `response`, validation `failed`, cleanupFailed `false`. Execution exited nonzero; the exact readback command exited zero and reproduced the failed record. Readback validity is not case success. The record cannot establish the detailed defect within the rejected candidate; raw invalid content and hidden reasoning were not retained. No semantic assessment or accepted proposal exists.

The sent body matches the frozen Local image binding: 17748 bytes, SHA-256 `4c13611115bca2dbf5d6d364a0e00d4bb03f40a5200322ad068bc4726306dfd6`. Original records remain under ignored `temp/m602-generation-evidence/local-image/`: entered SHA-256 `1bc66fd5cb9f437b83c13403da7872392893871e19ad7478f6a53d50e51c272f`; dispatch `4d2b186092c95a1b35ca8f9bf9c748386a5b117ddb08b6e1a4dfc0fcb4544320`; result `05e6b16b86f676b090d9cdc06d3a4a151c2cfc8bc36fb9fcd788f558ae48d989`. No record was overwritten, repaired or deleted.

Required Local observation did not pass. The pre-call GPU sample at `02:14:36.590Z` was 41 MiB used / 7770 MiB free. The textbox/radio interaction was responsive, but its `02:15:10.743Z`–`02:15:34.523Z` interval was after chat completion. The nominal during-call GPU sample at `02:15:54.587Z` (4924 MiB used / 2887 MiB free), and sole additional `/api/ps` GET at `02:15:54.676Z`, also occurred afterward. That GET observed the exact digest, context 32768 and size/VRAM 4197596527 bytes; it is post-call metadata only. This is a procedural timing miss, not usable in-call/OOM proof. No observation or generation was repeated to replace it. The three adapter metadata requests and this one late observer GET are distinct from the single chat count.

Primary stopped all subsequent generation when the failure was collected. Local label, Local contrast and all Groq cases remain unentered/not-run; no fallback, warm-up, retry, new prompt or provider change occurred. The exact owned service received `stop`, reported `service-stopped`, exited 0 and restored its environment; the dedicated tab was closed. Original evidence and the developer-managed runtime remain preserved. Both the failed output and missing Local observation require owner disposition; existing six-operation authorization does not permit continuing past either gate.

### M602-C-REVIEW-01 — Integrated blocked-handoff review

This is the historical review before actual execution. Its empty-root and zero-dispatch observations are superseded by M602-B-RESULT-01 and the subsequent C evidence review; its unchanged implementation verification remains valid.

A different fresh `critical_reviewer` completed the full G1–G6/C review, including actual changed surfaces, production eligibility, cohesion, lease/evidence identity, preservation, cleanup and documentation. No Blocker implementation finding, Major or outstanding Minor remains. Primary corrected the stale roadmap summaries identified during review. The verdict is **BLOCKED for task completion** because B is unexecuted; the implementation and blocked handoff are acceptable. Primary accepts that limited verdict, retains M6-02 In progress and does not archive the plan.

The reviewer independently traversed all six cases in their fixed order using isolated synthetic packages and native doubles, verified readable records and assessment-gated advancement, rejected every replay and confirmed credential-buffer clearing. These are offline probes, not actual fixed-case executions. Injected final-result fsync and close failures returned `evidence-publication`; a subsequent operation remained blocked without an assessment and made no additional request. Complete surviving JSON can pass structural readback after a publication failure: G's observed failure/unknown disposition still prohibits primary assessment or continuation. This supplies no recovery permission or renewed allowance.

Independent checks authenticated all 430 protected files, entry inventory, both final code/tree fingerprints and all six lease contract/receipt digests. No active lease or reviewer scratch remains. The actual evidence root and all three managed scratch roots are empty, the owned service port is closed and developer-owned Ollama remains present. Current 176 focused tests, strict checking, six default-package checks, 938-test regression and unchanged-input build were reused; no evidence was invalidated. Actual provider outcomes, semantic acceptance and Local in-call observations remain unproved.

Documentation impact: updated README's maintained test command and current status, the roadmap, this plan and its progress record. No requirement, ADR, frozen artifact or later-task status changed. Proportional checks cover five documentation owners, 414 local path links, 221 anchors, ten parsed PowerShell blocks, strict UTF-8 without BOM, final newlines, whitespace and `git diff --check`; explicit template placeholders are excluded. Direct authorization and non-secret Groq account readiness remain the next owner inputs. Only necessary execution-time readiness and changed-evidence checks should repeat on continuation.
### M602-C-REVIEW-02 — Failed-evidence handoff accepted

The existing different final C reviewer completed its bounded follow-up on the new actual evidence and returned **PASS for the failed-evidence handoff only**, with no outstanding implementation or handoff finding. Primary accepts that limited verdict. The failed output and missing in-call observation remain acceptance failures; neither readback nor review converts them to success. M6-02 remains In progress and unarchived, with owner disposition required before any later generation.

The reviewer independently read the failed record through the default helper, authenticated all three original record hashes, the exact frozen wire, one chat and one each version/show/tags request, and confirmed no proposal or assessment. It reproduced unchanged record bytes, one existing case directory, five absent later directories and the failed-predecessor continuation barrier. All 430 protected files, both accepted code/tree fingerprints, entry HEAD, absent active lease, empty scratch, closed task port and preserved Ollama process match. No extra provider/runtime request or semantic scoring occurred. The unchanged focused/full/strict/package/build evidence is reused; no evidence was invalidated.

Documentation impact: README, roadmap, this plan and progress record now distinguish the actual failure from the historical approval block and synthetic qualification. Current checks pass across those four changed owners: 354 local path links, 198 anchors, ten PowerShell blocks, strict UTF-8 without BOM, final newlines, whitespace and `git diff --check`. The added review checkpoint and its progress link were reconciled afterward. Detailed validation cause, semantic quality and in-call usability/OOM remain unknown; five unused allowances are preserved but do not authorize bypassing the stop rule.

### M602-DIAGNOSIS-PLAN-01 — No-generation diagnosis plan

**Purpose and authority:** The owner selected preparation of a bounded diagnosis plan after the failed-evidence handoff. This is repository-local R0 planning, owned by the primary, with no new mechanism decision, agent assignment, write lease or correction allowance. The deliverable stays in this ExecPlan. Generation remains stopped; preparing this plan does not authorize its implementation, renewed case execution or a replacement campaign.

**Established limits:** The original three records still match their B-RESULT-01 hashes. One Local image dispatch failed with `response-validation`, observation `response/failed`, complete cleanup and no proposal. The original invalid envelope/candidate was deliberately not retained. The existing source maps multiple rejection paths to this same record; the actual detailed cause cannot be recovered from the retained record alone. Duration, GPU use or successful metadata admission cannot identify the rejected field. The observation miss is independently established by timestamps and does not explain the response-validation failure.

The initial source inspection supplies this diagnosis map; these are compatible paths, not ranked explanations:

| Path | Existing owner | What the retained evidence permits |
| --- | --- | --- |
| Adapter reports `incomplete-output` | [Ollama HTTP boundary](../../src/server/generation/ollama-generation-http.ts), then [shared execution](../../src/server/generation/generation-execution.ts) | Body bounds/completeness, UTF-8/JSON parsing, exact returned model identity, stop marker, forbidden output extensions or thinking, and object-shaped content can reject before shared candidate validation. They collapse to `response/failed`; no particular cause is established. |
| Shared candidate reader rejects | [Proposal contract](../../src/server/generation/proposal-contract.ts), supplied by the [frozen package](../../tests/helpers/m602-package.ts) | Closed shape, required values, references, prose bounds or prohibited-claim checks can reject after adapter acceptance. The record cannot identify a field or distinguish this path from adapter rejection. |
| Caller wire/window correspondence rejects an otherwise valid proposal | [Operation helper](../../tests/helpers/m602-operation.ts) | This path preserves candidate validation `passed`; the actual record says `failed` and has matching wire/window data. It does not explain this recorded failure on the accepted code. |

**Bounded diagnostic work, if subsequently selected:**

1. **Pin and preserve.** Reuse the accepted executable/test fingerprints and B-RESULT-01 identities. Read only the three retained records, relevant source and existing test cases. Stop on drift. Do not search runtime logs, process memory or unrelated retained runs for discarded model text, and do not create or amend an assessment.
2. **Complete the rejection map.** Trace each compatible path to its existing assertion and test. Use the current Ollama contract/integration tests, shared generation contract/stage tests and M602 tests. Record coverage and any demonstrable mismatch; do not infer the original candidate from a synthetic fixture. Reuse accepted passing evidence unless a specific uncovered or contradictory behavior justifies one focused offline check using existing in-memory doubles. No new tracked test or helper is included in this plan.
3. **Analyze observation scheduling separately.** Reconstruct the native 13.744-second chat window and the later UI/runtime/GPU timestamps from B-RESULT-01. Document that observation began after terminal completion and that successive agent/tool calls supplied no timing guarantee. A proposed future procedure must prearrange observation readiness, detect terminal completion before issuing an allegedly in-call metadata sample, and qualify its timing using synthetic activity before any separate real campaign. Do not claim that simply starting earlier guarantees success. If reliable coordination requires code or changes to G's observation contract, return a concrete proposal through the existing authority/review route; do not implement it here.
4. **Return one diagnosis memo here.** Separate proven facts, excluded paths, unresolved alternatives and information unavailable under the accepted retention policy. End with either a specifically evidenced repository defect and bounded correction proposal, or an explicit conclusion that the original detailed rejection cause remains indeterminate. Identify any proposed future observation change separately. No statistical/provider-quality conclusion follows from this case.

**Effects and stopping boundary:** One primary inspection pass and one memo; no new generation, runtime/provider metadata request, credential access, service/browser startup, dependency acquisition, raw-output capture, test/code/configuration edit, frozen-input change, cleanup of original evidence, commit or push. An optional existing offline check must identify the exact missing fact before it runs; broad regression and previously completed probes do not repeat. A source or test defect requiring edits returns for a bounded assignment because the prior worker budgets remain exhausted. Consult the bug index if a substantive repository defect is established; the model's rejected output alone is not proof of such a defect, and the task-contained timing failure is already recorded here.

**Completion and disposition:** The diagnosis deliverable is complete when each compatible path has an evidence/coverage disposition, the timing failure has a separate bounded account, and remaining unknowns are explicit. Preserve Local image as failed and the five later cases as not-run. Any instrumentation, contract amendment, fix, new generation or continuation past the stop rule requires a concrete separately authorized scope; none can retrospectively pass or replenish the original case. Preparing this plan changes no requirement, ADR, roadmap status or accepted code evidence.

### M602-DIAGNOSIS-RESULT-01 — Offline diagnosis complete

**Disposition:** The owner requested continuation of the bounded offline diagnosis. Primary completed its one repository-local inspection pass and memo. No application defect is established by the available evidence, and no correction is justified solely from this failed case. The exact original rejection cause remains **indeterminate** because both adapter rejection and candidate rejection deliberately collapse to the retained `response-validation` / `response` / `failed` tuple, without the rejected content or a finer reason code. This is an observability limit of the accepted contract, not evidence that validation malfunctioned.

**Identity and method:** Read-only checks reconfirmed entry HEAD, the accepted operation-code and source/test fingerprints, all three original case-record hashes, one case directory and no assessment or later case. Existing accepted 176-test focused, 938-test full, strict and build evidence remains valid. Inspected the existing assertions below; no new behavioral test was needed because the two-path ambiguity is directly asserted in the existing stage test. No regression rerun, runtime/provider request, credential access, service/browser startup, new helper or source/test change occurred.

| Compatible rejection family | Existing coverage inspected | Diagnostic conclusion |
| --- | --- | --- |
| Envelope/model/role/stop/extension/thinking/content shape | [Ollama contract tests](../../tests/ollama-generation-contract.test.ts), test beginning at line 284 | Covers exact model rejection, unfinished/length output, nonempty thinking, tools/images/remote fields, wrong role, fenced/malformed JSON and non-object content. A rejected member of this family remains possible, but none is identified by the record. |
| Transport body bounds/encoding/completeness | Same test, its byte and framing vectors | Oversized body, invalid UTF-8 and incomplete HTTP all map to `incomplete-output`. Neither exact body size nor returned bytes were retained, so the actual subcause is unknown. |
| Candidate structure and categorical fields | [Generation contract tests](../../tests/generation-contract.test.ts), tests at lines 135, 163, 191 and 215 | Authenticated context, closed shapes, hostile properties, Finding identity, sufficiency and confidence are covered. The original candidate cannot be inspected or scored. |
| Candidate references and prose policy | Same file, tests at lines 238, 298 and 338 | Evidence/passage membership and minima, duplicate references, string/assumption bounds, normalization and prohibited lexical patterns are covered. No particular rejected claim or field can be inferred. |
| Shared classification and retention | [Stage test](../../tests/generation-stage.test.ts) at line 766; [M602 test](../../tests/m602-generation.test.ts) at line 747 | The stage explicitly expects identical error/outcome/validation/cleanup for invalid candidate and incomplete output. M602 rejects raw candidate retention and redispatch. Replaying these synthetic cases cannot recover the original branch. |

**Excluded explanations on the accepted code:** A caller-only wire/window mismatch would preserve candidate validation `passed`, whereas this record says `failed`; the wire and window also validate. Pre-admission integrity, missing runtime, input-fit, network, non-200 provider errors, timeout, shutdown, duplicate transport and uncertain cleanup have different accepted outcome paths. They do not explain this exact retained tuple. No stronger claim about the model's internal processing follows. The adapter's returned-model requirement `qwen3.5:4b:local` is intentional: [M3-03's accepted wire/results contract](completed/m3-03-qwen-adapter-and-capacity-screen.md) binds that spelling while parent configuration remains `qwen3.5:4b`. The bare-name negative test is therefore not a demonstrated typo; the original returned name remains unknown.

**Separate procedural failure:** Native chat lasted 13744 ms. The UI observation began 19927 ms after chat termination; the nominal during-call GPU sample and runtime GET followed termination by 63771 ms and 63860 ms respectively. Primary began observations too late and issued the metadata observation without first establishing that the chat was still active. Serial tool/agent transitions did not synchronize with the short native window. These facts explain the missing observation evidence, not the response rejection. The pre-call GPU sample remains a pre-call sample; late responsiveness and residency cannot prove in-call usability or absence of OOM. The timing error was already accurately preserved in B-RESULT-01 and remains task-contained under the bug-recording rules.

**Proposed follow-up, not implemented or selected:** Before proposing another actual campaign, define a narrowly scoped, content-free diagnostic distinction between adapter-envelope rejection and candidate-contract rejection, with closed reason categories and no raw text, headers, reasoning or credential material. Any persisted extension needs an explicit record/identity and compatibility decision; never add it to the original result or pretend it identifies that response. Separately qualify observation coordination against synthetic short-lived activity, including terminal-before-observation behavior. A future procedure must refuse a late metadata sample and report a missed window; it cannot promise that manual tool timing always fits. These are prospective contract/implementation proposals requiring the existing research, ownership and review routes, not permission to weaken validation or renew an exhausted chain.

**Closure:** Diagnosis is complete within its stated evidence limit. Preserve the original failed case and five stopped cases; no semantic pass, retroactive observation, retry or task completion is claimed. Documentation impact is this memo, current-state/progress reconciliation and the progress narrative. No requirement, ADR, frozen definition or code identity changed. Local-link/anchor, UTF-8/newline, status consistency and `git diff --check` verification applies to these documentation edits.

### M602-FOLLOWUP-01 — Offline follow-up authorization and decision contract

**Owner authorization and scope:** Following the diagnosis, the owner explicitly authorized the proposed bounded work: content-free failure reasons, improved observation coordination, offline tests and independent review, with no real generation. This is additional M6-02 scope and a new explicitly authorized allowance for that scope, not a relabeling or renewal of the exhausted A chain. Original research/test/Green receipts, failures and budgets remain historical and unchanged. Work is standard/nonvisual: no rendered UI change, provider configuration, prompt/schema/corpus change, dependency acquisition, credential access, model/runtime request, real case entry, assessment or retry. Existing Local image evidence must remain byte-identical, and all five later cases remain stopped.

**Decision Review Contract — M602-FUP-G:** R3, because diagnostic information crosses untrusted response/credential boundaries, new observation coordination has lifetime/concurrency semantics, and retained evidence compatibility has custom integrity/recovery constraints. Target: one bounded follow-up contract in this ExecPlan before writes. The original G remains the authority for its original records and failed campaign; follow-up code must neither reinterpret that failure nor silently admit another original case. Production persistence and renderer contracts stay unchanged unless a demonstrated requirement returns for explicit scope reconciliation.

**Question and fixed candidates:** Compare (A) content-free optional reporting callbacks with task-local diagnostic/observation evidence separate from existing result shapes, and (B) optional closed diagnostic data on execution outcomes with an explicitly versioned evaluation record. Both must use the existing adapters, shared validator/executor and native chat boundary. Compare only these direct placements; introduce no general telemetry, scheduler, alternate provider stack or raw-output store. Observation coordination must have an actual invocation seam, trigger from native chat lifetime, reject observations outside that lifetime, and be provable using synthetic callbacks. It must not merely export an unused helper or claim that manual tool latency is bounded. Choose the smallest compatible implementation, including a narrower combination if required by current responsibility boundaries.

**Common criteria and hard gates:** useful deterministic rejection classification without content leakage; unchanged validation/provider behavior; bounded callback failures and lifecycle; trustworthy timestamps and no observation request after known terminal state; preservation and honest readback of the original failed record after code changes; no silent migration or resumed real execution; cohesive modules and behavioral tests; no dependencies or speculative generalization. Decide diagnostic transport, finite reason vocabulary, evidence version/compatibility, observer invocation/terminal rules and exact allowed paths before Green. Prove actual race handling, zero native/credential effects in offline tests, privacy, backward preservation and readback under implementation; defer only real-runtime effectiveness because real calls are outside this authorization.

**Cumulative invariant packet:** F1 closed content-free diagnostics on all relevant rejected-response paths, including hostile values; F2 unchanged production admission/outcome/wire/credential behavior; F3 original-record preservation, explicit version/code identity and no renewed entry; F4 start/terminal/abort concurrency and late-observer refusal; F5 caller-integrated observation completion with strict native-window evidence and no fabricated success; F6 cohesive ownership, complete test scope, authoritative status and evidence honesty. Every contract review covers all six, not just the latest correction.

**Routing, bounds and ownership:** One `critical_researcher` investigates the coupled response-reporting/evidence/lifetime dimension, initial report plus at most one bounded follow-up. Primary concurrently owns current authorities, original-record preservation and command/readiness binding. One `decision_analyst` synthesizes (initial plus one bounded correction), then fresh pre-draft and different final `critical_research_reviewer` barriers apply under the existing R3 rules. Primary authors the contract; no research role writes files. No optional drafter. The authorized implementation allowance is one coherent follow-up slice with separate test/code owners: one preflight, initial Red/Green and at most one ordinary correction each; a conditional third write requires the existing documented conditions. One S3 implementation correction-review loop and different final integration review apply. None of these allowances renews a real operation. Stop for changed material scope, an unresolved hard gate, repeated decisive failure without new evidence or exhausted allowance; preserve completed authorized work.

**Current baseline:** The original accepted code/tree identities and three failed-case hashes in M602-B-RESULT-01/M602-DIAGNOSIS-RESULT-01 are the entry preservation baseline. Exact follow-up command/test identities and path projections will be pinned in the Milestone Assignment Packet v2 after the reviewed contract. No active write lease exists; prior edits remain part of the baseline rather than unrelated changes to discard.

### M602-FUP-CONTRACT-01 — Offline diagnostics and observation contract

**Selection and evidence:** Primary selects candidate A: optional content-free rejection sinks and separate versioned timing/diagnostic evidence. FUPR01–07 identify the existing rejection, native accounting and strict readback boundaries; FUPR08–09 resolve linked-result compatibility and independent pending-work accounting. Candidate B would change closed execution unions without a demonstrated mandatory-return need. A preserves those unions and confines the additional evidence to the evaluation caller. Reverse this selection only if implementation demonstrates that these seams cannot meet containment or a mandatory outcome contract becomes necessary; do not expand scope implicitly. The original response remains indeterminate.

The initial pre-draft review found FUP-PD-01 (missing diagnostic/result compatibility) and FUP-PD-02 (ambiguous fault/pending precedence). The sole remaining researcher follow-up and primary reconciliation resolved both; bounded pre-draft recheck returned **PASS**, with no outstanding findings across F1–F6. Research and analyst allowances are now exhausted. Primary independently reproduced an additional source-order path with the existing native double: an invalid envelope followed by a response error before close returns `network` with uncertain cleanup. That one-double-call, no-network/no-write probe supplements and supersedes FUPR08's unamended exhaustive-matrix claim. A different fresh final research reviewer must accept this authored contract before preflight or a write lease.

**Scope:** Implement and qualify this contract offline only. Preserve original G result and assessment shapes, all model/prompt/schema/wire/fit/credential behavior and the original three failed-case files. No real generation, runtime metadata, browser/service startup, acquisition, original-root mutation, assessment, retry, successor campaign or later roadmap task is authorized. An observation timing receipt is not a UI/resource sample or a rubric assessment.

#### Rejection reporting and compatibility

`src/server/generation/generation-diagnostics.ts` owns the closed `GenerationRejectionCode` and `GenerationRejectionSink = (event: Readonly<{ code: GenerationRejectionCode }>) => unknown`, safe constant emission and finite-value reading. Codes are exactly `adapter-response/body`, `adapter-response/envelope`, `adapter-response/content`, `candidate/contract`, `executor/envelope`, `caller/correspondence` and `adapter-response/unspecified`. Body covers existing byte bounds/framing/UTF-8/outer-JSON rejection; envelope covers existing model/completion/role/thinking/extension predicates; content covers content-JSON/nonobject rejection. No exception text, rejected value, raw body, header, capability, credential or sample payload enters an event.

Add optional last parameters to `createOllamaGenerationAdapter(requestImplementation?, onRejection?)`, `dispatchOllamaGeneration(body, signal, attemptTransport, requestImplementation?, onRejection?)` and `validateProposalCandidate(candidate, context, onRejection?)`. Add optional `onRejection` to executor options and an optional second sink parameter to ready admission/package `validateCandidate(candidate, onRejection?)`. The authenticated package closure forwards that sink. Existing return unions, public production persistence and admission remain unchanged; omitted sinks preserve existing behavior. No Groq HTTP change: its rejected response uses the generic executor code.

Emit only newly owned frozen constant events. Do not await sinks. Contain synchronous throws, throwing thenable access and eventual asynchronous rejection without inspecting or stringifying the thrown value. This cannot forcibly bound a trusted synchronous callback. Executor envelope reporting surrounds only returned-envelope descriptor/closed-shape parsing, after duplicate and attempted guards; dispatch rejection, native throw, gate failure or cancellation does not acquire that code through the broader catch. Emit generic adapter rejection after `observe('response', 'failed')` and before the existing stop check. Candidate fallback is emitted after the failed candidate observation and before the stop check; the candidate validator may already have emitted the same code. Caller correspondence reporting occurs only when the existing proposal-to-failure conversion occurs. Preserve all validation predicates, transport counts, cleanup and terminal precedence.

The task-local collector processes events only until closure. No events yields complete integrity and null code. Repeated identical valid codes retain that code. The sole permitted mixed set is generic adapter rejection plus exactly one detailed adapter code; choose the detailed code independent of arrival order. Unknown/malformed events, every other mixed set or incompatibility with the linked result latch failed integrity and null code permanently. Late events cannot revise a frozen report. Diagnostic integrity never changes the generation result.

Every non-null code requires a validated linked result with `attempted:true`, `status:'failed'`, non-null observation and null proposal. In this table, R/F means observation `response/failed`, P/N means `provider/not-run`, C/N means the final cancellation error with `not-run`, and N/N means `network/not-run`.

| Code | Allowed final error | Observation | `cleanupFailed` |
| --- | --- | --- | --- |
| Detailed adapter body/envelope/content | `response-validation` | R/F | Either |
| Detailed adapter body/envelope/content | `timeout` or `shutdown` | R/F | Either |
| Detailed adapter body/envelope/content | `timeout` or `shutdown` | C/N or N/N | True |
| Detailed adapter body/envelope/content | `network` | N/N | True |
| `adapter-response/unspecified` | `response-validation`, `timeout` or `shutdown` | R/F | Either |
| `candidate/contract` | `response-validation`, `timeout` or `shutdown` | R/F | False |
| `executor/envelope` | `provider` | P/N | True |
| `executor/envelope` | `timeout` or `shutdown` | P/N or C/N | True |
| `caller/correspondence` | `response-validation` | `response/passed` | False |

Caller correspondence additionally requires chat count unequal to one, absent native window, or actual wire hash/bytes different from the authenticated package. No other pairing is accepted. These are rejection-stage observations, not proof of the eventual terminal cause. The detailed-code network rows preserve the reproduced late-resource-error path and possible cancellation after its observation.

#### Observation lifecycle and timing-only evidence

`tests/helpers/m602-observation.ts` owns one coordinator per operation. Its kinds are exactly `ui`, `runtime` and `gpu`. `M602Observers` is a partial map from kind to `(gate: { signal: AbortSignal; start(effect: (signal: AbortSignal) => void | Promise<void>): boolean }) => void | Promise<void>`. The observer receives no native transport capability. Each effect initiates synchronously when admitted; its promise denotes observed settlement. A delayed observer calls `gate.start` after its wait, allowing refusal at the actual effect boundary.

`executeM602Case` creates and consumes this coordinator through `accountNative`. Native accounting registers first-terminal listeners before scheduling supplied Local observers, after native request construction. It does not await observers or delay request progress. Groq invokes none. A synchronous native terminal prevents later scheduling. Each kind's start capability is consumed before callback-capable work; immediately recheck active/abort before invoking the effect. Duplicate/reentrant/late starts return false with no second effect and latch refusal. Capture actual effect entry and settlement timestamps; do not fabricate an end from abort time.

Track observer pending and effect pending independently, plus fault and refusal. First native terminal, caller abort or operation finish closes once, revokes gates and freezes slots **before** invoking abort listeners. Own eventual promise rejection/settlement without indefinite waiting or modifying frozen evidence; detach owned listeners. At closure choose each configured slot in this order: pending observer or effect means aborted; otherwise any observed fault means failed; otherwise no effect, refused start, absent known native end or noninterior/ambiguous timing means missed-window; otherwise completed. Missing observer means unavailable. Successful effect settlement alone does not hide a still-pending observer.

Each slot has exactly `status`, `startedAt`, `finishedAt` and `cleanup`. Status is `unavailable | missed-window | failed | aborted | completed`; timestamps are canonical ISO strings or null. No effect means null/null. An effect has a non-null start and has a finish only if its settlement was observed while open. A finish requires a start and must be nondecreasing. Aborted always has uncertain cleanup and may have null/null, start/null or start/finish depending on the independent pending work. Unavailable is null/null with complete cleanup. Failed and missed-window have complete cleanup and either null/null or an observed start/finish pair. Completed has complete cleanup and both timestamps strictly inside the known native start/end; equality fails. Malformed or backward timestamps cannot produce accepted evidence. Cleanup describes observed callback/effect settlement, not proof that an external process or model stopped.

The report's aggregate observation cleanup is uncertain if and only if any slot is uncertain. At publication its `chatWindow` copies the authoritative result window exactly; slot states and timestamps remain frozen from coordinator closure. If caller abort preceded native termination, a later result window never upgrades a missed/aborted slot. Thus a missed-window slot may be interior to the later result window. A null result window permits no completed slot. Missing default observers explicitly remain unavailable; this scope adds no real observer implementation or synthetic fallback to a real runtime.

#### Supplemental storage and historical inspection

Extend `M602OperationDependencies` with optional `followup: { reportRoot: string; observers?: M602Observers }`. Existing injected callers omitting it retain their old result shape and behavior. Injected follow-up requires `reportRoot` equal to `<dependencies.root>/followup-evidence`, with existing synthetic root/package/native and Groq credential-double requirements; reject a mismatched binding before entry. Without dependencies, prospective execution binds only `temp/m602-followup-evidence-v1`, with observers unavailable. That default is a storage choice, not renewed execution authority.

`M602FollowupReport` is the closed object `{ version:'m602-followup-v1', caseLabel, producerCodeSha256, manifestSha256, enteredSha256, dispatchSha256, resultSha256, diagnostic:{ integrity:'complete'|'failed', code:GenerationRejectionCode|null }, observation:{ chatWindow, ui, runtime, gpu, cleanup:'complete'|'uncertain' } }`. Hashes are canonical SHA-256; dispatch hash and window may be null, as in the linked result. Failed diagnostic integrity requires null code. No arbitrary strings, sample contents, result copies or extra fields are allowed.

After successful original-shaped result publication, publish exactly `<reportRoot>/<enteredSha256>/report.json`. Create only the bound report root when needed and then the identity directory exclusively; use ordinary ancestor/endpoint checks, bounded canonical JSON, exclusive `wx`, full writes, fsync, close and exact byte readback. Do not overwrite, repair, delete residue or append a file to a case directory. Return the existing success fields plus optional `followup:{ report, sha256 }` only when reporting is enabled. A supplemental failure returns existing `evidence-publication` while preserving the already published result. Partial supplemental evidence remains blocked and never renews entry.

`readM602Followup(caseLabel, dependencies?)` returns `{ok:true,report,sha256}` or `{ok:false,error:'evidence-blocked'}`. It performs no preparation, native/credential call, observation or write. Injected readback requires explicit follow-up root. Reuse the internal strictly validated current source-record snapshot, retaining package/configuration/current-code checks. Derive the report directory from the authenticated entered hash; require exactly `report.json`, canonical bounded bytes, closed schema, the compatibility matrix, slot coherence and exact manifest/producer/entered/dispatch/result linkage. It provides no assessment or continuation eligibility and is never used to advance the sequence.

`readOriginalM602Case()` has no root or producer-hash override. It reads only the original Local image directory and requires exactly its three original files and full pinned identities: manifest `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; producer `a069571f11959ba95670be9142169962262e834d5c9c81791ed90bae191522b4`; entered `1bc66fd5cb9f437b83c13403da7872392893871e19ad7478f6a53d50e51c272f`; dispatch `4d2b186092c95a1b35ca8f9bf9c748386a5b117ddb08b6e1a4dfc0fcb4544320`; result `05e6b16b86f676b090d9cdc06d3a4a151c2cfc8bc36fb9fcd788f558ae48d989`. It returns a closed success with `ok:true`, `kind:'historical-only'`, `eligibleForContinuation:false`, `caseLabel:'local-image'`, `status:'failed'`, `error:'response-validation'`, `attempted:true` and those five identity fields, or `evidence-blocked`. Verify current manifest bytes and pinned original producer/record links; do not require current code equality for this separate historical inspection. Never revalidate a historical proposal, create an assessment, migrate records or call this reader from execution/continuation. Current `readM602Case` remains strict.

Expose historical inspection as exactly `--inspect-original --case local-image` in the existing CLI, with a content-free summary and exit zero only for authenticated historical inspection. Other cases/options are rejected. Original execute/readback semantics stay unchanged. New helper files all join the operation-code fingerprint; historical inspection alone retains the original producer pin.

#### Responsibility, test ownership and proof

| Code owner path | Responsibility and bounded change |
| --- | --- |
| `src/server/generation/generation-diagnostics.ts` (new) | Closed event types, finite reading and safe constant emission; no transport or persistence. |
| `src/server/generation/ollama-generation-http.ts` | Report constants at existing rejection branches without changing HTTP/validation behavior. |
| `src/server/generation/ollama-generation.ts` | Forward optional sink to dispatch only. |
| `src/server/generation/proposal-contract.ts` | Optional candidate rejection reporting; unchanged eligibility wrapper and candidate predicate/result. |
| `src/server/generation/generation-execution.ts` | Narrow envelope/generic/candidate fallback emission with existing execution precedence. |
| `tests/helpers/m602-package.ts` | Forward sink through authenticated candidate closure. |
| `tests/helpers/m602-operation.ts` | Integrate collector/coordinator, own supplemental schema/readback/publication and strict current source-record snapshot. |
| `tests/helpers/m602-observation.ts` (new) | Per-kind native-lifetime coordination, immutable timing slots and independent pending accounting. |
| `tests/helpers/m602-historical-evidence.ts` (new) | Exact original-only, non-authorizing inspection. |
| `tests/helpers/m602-evidence-files.ts` (new) | Existing bounded filesystem/serialization primitives shared by the two readers; no generic storage framework. |
| `tests/helpers/m602-run-case.ts` | Strict historical CLI branch and existing content-free command projection. |

Production modules may import only production boundaries; evaluation helpers depend on those modules, never the reverse. Bounded extraction of existing evidence primitives and current readback snapshot is permitted to share existing validation without weakening it. No other structural refactor, generic utilities or dependency is selected. Actual cohesion is checked before handoff.

Test owner paths are `tests/ollama-generation-contract.test.ts`, `tests/generation-contract.test.ts`, `tests/generation-stage.test.ts`, `tests/m602-generation.test.ts` and new `tests/m602-followup.test.ts`. Reuse existing native/credential doubles; all tests remain unchanged during Green. Cover all seven codes and compatible/incompatible result rows, generic/detail merge and conflict latching, hostile values and sync/async/thenable sink faults, no events on successful or unrelated failures, unchanged output/wire/counts, and every injected dependency combination's zero real fallback. Cover integration through `executeM602Case`, including all-complete before terminal, synchronous terminal, delayed/duplicate/reentrant starts, caller abort, pending observer versus effect, fault after start, late reject/settle, abort-listener reentry, equal timestamps, operation finish without native terminal, missing observers and no Groq observer invocation. Cover exclusive publication failure, forged/extra/mismatched reports, immutable readback and no renewed continuation. Portable historical negative tests use synthetic/mocked file failures; primary separately verifies exact-original positive inspection without copying ignored records into tracked fixtures.

Use one standard slice `FUP-offline-diagnostics-observation` under workflow `m602-six-fixed-generation-executions`, with new explicitly authorized test/Green chains and the bounds in FOLLOWUP-01. The packet pins all fields and exact path projections before each turn. Preflight is read-only. First-module Red may use the agreed absent `tests/helpers/m602-historical-evidence.ts` export `readOriginalM602Case` or absent `readM602Followup` export, only after runner readiness and complete behavioral tests; no stubs, skips or fallbacks. S3 applies to integrity, privacy and lifetime/recovery; use a fresh critical work-slice reviewer and a different final critical integration reviewer.

The focused command uses maintained README preparation and `Invoke-M105Command`, Node v24.20.0, then `& $m105Node --test --test-timeout=120000 tests/m602-followup.test.ts tests/m602-generation.test.ts tests/generation-contract.test.ts tests/generation-stage.test.ts tests/ollama-generation-contract.test.ts tests/ollama-generation.test.ts tests/groq-generation-contract.test.ts tests/groq-generation.test.ts`, with explicit nonzero propagation. Independent strict checking is `& $m105Node node_modules/typescript/bin/tsc --project tsconfig.json` under the same preparation. Closure adds the new suite to the maintained full sequential README suite and runs it, the build, exact-original inspection, preservation checks and documentation gate. No actual-call flags are allowed. Only exact ordinary synthetic `temp/m602-test-<UUID>` trees may be created/cleaned; existing real/frozen roots remain protected. Complete follow-up proof does not complete M6-02's failed B verification or authorize real observation provisioning.

### M602-FUP-PACKET-01 — Follow-up implementation assignment and acceptance

**Contract acceptance:** Different final R3 reviewer returned **PASS**, with no Blocker, Major or Minor finding, against full plan SHA-256 `99ab22e1ff97fa59399a7db9ff3caedf5387a29da9a36eca563c6f10338b04db`. It independently reproduced the late-resource-error path and verified original identities and the complete F1–F6 contract. Primary accepts the contract for offline implementation. No final-artifact correction cycle was needed. Research and analyst budgets are exhausted; original A and real-operation budgets are unchanged.

**Milestone Assignment Packet v2 — preflight:**

- **Identity:** Workflow ID `m602-six-fixed-generation-executions`; roadmap task ID `M6-02`; work-slice ID `FUP-offline-diagnostics-observation`; assignment ID `M602-FUP-preflight-01`; lease ID None; phase preflight; attempt 1; correction parent lease ID None; worker role `test_worker`; lease owner `m602-fup-test-worker`; guard contract digest None.
- **Owning ExecPlan:** this plan. Observable acceptance contract: complete M602-FUP-CONTRACT-01, including F1–F6, its exact interfaces, matrix, storage and observation rules. Exact authority anchors: this plan's [authorities](#authorities-and-prerequisite-evidence), [M6-02 Verification](../DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases), REQ-EVAL-001/002/004–009, REQ-GEN-001–005/008–010, REQ-LLM-001–005/007–009/019, REQ-SEC-002/003/012–016/021, BHV-03/04, SPEC-003/004, HS-008/009/015, Accepted ADR-0003/0004/0014/0020/0021/0023 and [ADR-0024](../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md).
- **Readiness and evaluation-freeze evidence:** M6-02 remains In progress; listed dependencies and applicable Accepted gates remain satisfied. Explicit owner follow-up authorization and final R3 PASS cover only offline changes. Original manifest, producer and three original record pins are preserved; post-output code changes cannot authorize another case. TDD applicability: Applicable for new reporting, observation and reader behavior. Current-state evidence: original accepted A, FUPR01–09 with primary amendments, both follow-up reviews and fresh 203-file tree SHA-256 `29fdbbb01d9f5a36ca611cc7b783cfc3e7a6761030058c5d8ad2f933df28ea4e`. Preflight evidence ID None until returned. Accepted test boundary None for this new slice; current test owner `m602-fup-test-worker`; original accepted tests remain preserved except expressly scoped additions.
- **Relevant boundaries and paths / production responsibility placement and fit:** the eleven exact code paths and individual responsibilities in the contract table. Dependency/interface changes: optional production sinks; authenticated package-to-validator forwarding; operation-to-observation coordinator; operation/historical readers to shared evidence primitives. Production never imports evaluation helpers. Reuse/separation/creation disposition: reuse existing adapters, validators, accounting and doubles; create only the four named cohesive modules. Permitted local structural refactor: only existing evidence primitives and current source-record snapshot extraction described in the contract.
- **Non-goals:** all excluded work in FOLLOWUP-01 and the contract, especially real calls/credentials, original evidence writes, sample acquisition, semantic scoring, model/prompt/input changes, new dependencies and Git mutations. Named uncertainties: preflight classifies the missing contract from actual source; implementation must prove hostile values, branch compatibility, timing races and publication failures. Risk tier S3. Review/escalation triggers: privacy, integrity/identity, concurrent lifetime and recovery; fresh critical work-slice review and different final critical integration review, with the recorded correction limit.
- **Write scope:** preflight is read-only and has no lease. Prospective allowed files are exactly `tests/ollama-generation-contract.test.ts`, `tests/generation-contract.test.ts`, `tests/generation-stage.test.ts`, `tests/m602-generation.test.ts`, `tests/m602-followup.test.ts`. Allowed directory roots None. Forbidden files `README.md`, `package.json`, `package-lock.json`, `tsconfig.json`. Forbidden directory roots `src`, `tests/helpers`, `docs`, `.codex`, `.agents`, `evaluation`, `corpus`, `data`. Guard runtime and Git writes are forbidden independently. Only a subsequent primary-opened test lease grants writes.
- **Validation:** working directory `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`. Preflight safe focused command is maintained README preparation followed by `& $m105Node --version`; reuse the primary's fresh 12 passing pure `generation-contract.test.ts` tests as runner readiness. Do not run scratch-writing suites during read-only preflight. For Red/Green the exact complete focused caller and independent strict command are in the contract; task-level command is its maintained full sequential suite plus new test, strict, build, original inspection and documentation/preservation checks. Expected decisive result: one source-supported preflight classification; if missing, complete behavioral tests and the precisely agreed absent historical module/export or supplemental reader export may supply initial first-module Red. Reusable evidence IDs: original A and fresh runner readiness only while their identities remain unchanged. Relevant-tree fingerprint is the 203-file hash above. Environment fingerprint: Windows, PowerShell 7.6.5, Node v24.20.0, Python 3.12.10, existing lock SHA-256 `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`, maintained environment wrapper, compile cache disabled, no capture/actual flags. Real filesystem/runtime observations are Non-reusable; synthetic test evidence is bounded to its isolated test state. Known external side effects and cleanup: None during preflight; later tests may own only exact synthetic UUID roots under temp and must remove their own roots after ordinary-path validation, preserving all original roots.
- **Budget and stopping:** maximum worker turns one preflight plus initial Red and up to two corrections, the second conditional under ADR-0024. Separate Green has the same three-write maximum; original chains stay exhausted. Maximum corrections one ordinary and one conditional per chain. Maximum repeated identical failure two without material new evidence; maximum no-diff outcomes two. Validation cadence one complete focused check per coherent write turn, repeat only for changed evidence or decisive failure; independent strict before acceptance. Stop for missing authority, changed binding fields, unexpected paths, stale evidence, exhausted allowance, unsupported external effects or lease uncertainty. Return to primary for triage; do not ask the owner independently or reset budgets.
- **Handoff:** report identity/authority, touched paths, exact commands and decisive results, outcome, unexpected state, residual risks and documentation impact; preflight reports No lease/No changes and exactly one classification. Worker is not alone in the tree and must preserve all existing edits. Git is read-only; no staging, commits, branches, worktrees, stashes or other metadata writes.

**Preflight accepted — M602-FUP-PREFLIGHT-01:** Test owner returned **MISSING**, supported by direct inspection: existing branches collapse rejection reasons, native accounting has no observer lifecycle, and supplemental/historical readers do not exist. Read-only Node readiness passed; no file changed. Existing behavior remains the regression boundary. Freeze one routine test-facing literal within the already declared operation collector responsibility: `createM602DiagnosticCollector()` returns `{ onRejection: GenerationRejectionSink, close(result: M602Result, expectedWire: Readonly<{sha256:string;bytes:number}> | null): M602FollowupReport['diagnostic'] }`. The first close freezes the result; later closes return the same snapshot and ignore later events. This exposes the real finite collector for conflict/hostile-event tests, without adding a provider, persistence or eligibility capability. The result is supplied by the operation; supplemental readback still validates the linked result before applying the compatibility matrix. Caller-correspondence requires non-null expected wire. No new file or behavioral rule is added.

**Red assignment — M602-FUP-red-01:** Inherit every capsule, authority, readiness, scope, risk, environment, side-effect, budget and handoff field above unchanged. Assignment ID `M602-FUP-red-01`; lease ID `m602-fup-red-01`; phase red; attempt 1; correction parent None; worker role/owner unchanged. Guard digest is supplied from the successful start response in the dispatch packet. Focused validation is the exact eight-file command in the contract using maintained preparation and explicit exit propagation. Expected result: complete new behavioral tests prove the missing contract; the sole intentional missing static module is `tests/helpers/m602-historical-evidence.ts` / `readOriginalM602Case`. Record all blocked assertions and any independently executing behavioral failures. Reuse existing private synthetic-package builders by placing operation/observation/report integration vectors in `tests/m602-generation.test.ts`; the new follow-up suite may own collector and portable historical negatives. Do not copy production logic or introduce another fixture owner. No application edits, helper edits, test stubs, skips, real calls or private-original dependencies are allowed.

**Initial Red disposition and ordinary correction:** Red01 closed compliant with contract `ac851f11dac39cb379dc5d64dfa869386fc759193c71c97df8aa8f7f975e1fc5` and receipt `b1c3235a8946a29c6d1e5723719a75b753ad4094062e601a47a78234687b2812`. Its focused run reported 176 existing passes and eight expected missing-behavior/module failures. Primary accepts preservation and the observed failures, but not the completeness of this Red boundary. Inspection found that the success fixture enters its first effect at the native start timestamp while asserting strict interior completion; several matrix/race/readback vectors from the accepted contract are absent. Independent strict inspection also found `prepared` used as a known envelope although the unchanged adapter contract returns unknown. Other strict failures concern the intentionally missing new interfaces. Reconstructing the prior untracked M602 test body without the one new import/appended tests exactly reproduces its accepted SHA-256, so no previous assertion was changed.

**Correction packet — M602-FUP-red-02:** All v2 authority, objective, responsibility, scope, role, environment, command, side-effect and stop fields remain unchanged. Assignment `M602-FUP-red-02`; lease `m602-fup-red-02`; phase red; attempt 2; parent `m602-fup-red-01`; owner `m602-fup-test-worker`; digest supplied by fresh guard start. Current relevant tree has 204 files, SHA-256 `861c19f13ab73bb9af50aee5f761f8330a07ead645a312ebb128bd90969e028e`. Prior Red is not accepted for Green. Correct only new test fixtures/narrowing and complete the same required compatibility, late-resource-error, fault/pending, late rejection, forged-slot/diagnostic and injected-dependency vectors. The existing pure tests stay preserved. This uses the one ordinary test correction, not a new contract or scope. Conditional attempt 3 remains available only under its documented evidence requirements. After correction, rerun the same focused command; use strict checking to separate deliberate missing-interface errors from independent test defects. No production stub or callable is permitted.

**Red acceptance — M602-FUP-RED-ACCEPT-01:** Red02 closed compliant with contract `039a9e999741f9059510b39f401d0e022dfd29ba60cf55d5f8ea049d10e77bf1` and receipt `9d0284057833a9c32340bd39d6176092b4d62bc1c88bbba2284f22f2a1210f4f`. Primary inspected the corrected fixture, complete collector compatibility/hostile-event matrix, observer/effect lifetime vectors, late resource error, supplemental forgeries and dependency exclusions. The focused command reports 188 tests, 177 passes and 11 intended missing-behavior/module failures, with no skip or cancellation; all 176 prior tests pass. Independent strict checking reproduces only missing new sink signatures, exports/module and supplemental return fields; the test-owned unknown-envelope and callback typing defects are resolved. The absent historical module blocks its complete five-test file until Green. Other assertions behind missing behavior must execute unchanged in Green. The 204-file relevant tree is `e6bf99e5d135ab55a21028453c27fda3194761a474ec807b2b36c47a73f75dc4`. Test attempt 2 is consumed; no further test correction is currently required.

**Accepted test boundary:** SHA-256 by exact path: `tests/ollama-generation-contract.test.ts` = `b3d00b890b3823dcdc3177fb27062027274b788840d8093ae9ebf44a3326ad32`; `tests/generation-contract.test.ts` = `f31e87c74452432d7313577e7b53a440cc3b63139d5e94fb4bc391e2fe3c39bc`; `tests/generation-stage.test.ts` = `6b4490e11913ec7f5364b2eb7ac70d813289b35c0e516c0e3cba9a159fae7108`; `tests/m602-generation.test.ts` = `c05bc206ba2bb8a5966e2d74217f45ac304abf277d2cdffe2515b2ed536c1d18`; `tests/m602-followup.test.ts` = `f6cafc9fcfdadf65fbc2222d6e7bec64fb5de193ba3cfa612950da33f179d881`.

**Green packet — M602-FUP-green-01:** Inherit all Milestone Assignment Packet v2 authority, contract, readiness, responsibility, interface, non-goal, S3 review, environment, command, cleanup and stopping fields above. Assignment `M602-FUP-green-01`; lease `m602-fup-green-01`; phase green; attempt 1; correction parent None; role `code_worker`; owner `m602-fup-code-worker`; guard digest supplied by successful start in the dispatch packet. Preflight evidence M602-FUP-PREFLIGHT-01; accepted Red M602-FUP-RED-ACCEPT-01; test owner unchanged; relevant tree and frozen five-file identities immediately above. Allowed files are exactly the eleven code paths in M602-FUP-CONTRACT-01; allowed directory roots None. Forbidden files are the five accepted tests plus `README.md`, `package.json`, `package-lock.json`, `tsconfig.json`; forbidden directory roots `docs`, `.codex`, `.agents`, `evaluation`, `corpus`, `data`. All other writes, including Git and original evidence, remain prohibited. Implement the entire reviewed contract with unchanged tests and unchanged old result/admission/provider behavior. Run the exact eight-file focused command and independent strict command under maintained preparation; every previously blocked behavioral assertion must execute and pass. Report actual cohesion as RETAINED, REFACTORED or RECONCILE against the declared responsibility placement; permitted refactor remains bounded to evidence primitives and current source-record snapshot extraction. Initial Green plus one ordinary correction and one evidence-conditional final correction remain available, without renewing any research, test, review or real-operation budget. No live requests, runtime probes, credential access, service/browser startup or actual-case commands. Primary will inspect the diff, frozen tests, terminal receipt and command evidence before S3 review and closure checks.

**Initial Green disposition:** Green01 changed exactly its eleven allowed files, preserved all five frozen test hashes and closed compliant with contract `b97c7d63af7a80c87bcd517c0ac4b076428bc86de0b5c4aa68b898119d8ae3f1`, receipt `86abd831b232d3691123b08013144594d1bf79874cdfcfb7644f2e00bdb2cd70`. All 192 focused tests pass with zero failure, skip or cancellation, including the formerly blocked module and integration assertions. Primary independently reproduced the sole strict error, TS7022 at the supplemental slot reader's local inference; behavioral success is not strict acceptance. The five accepted test identities remain exact. Current relevant tree: 208 files, SHA-256 `e1befd4b3abb9de13057537da7eb3c409fe42042b257417b14726d927ceb06dc`. The original 430 protected files and three Local image records were reconfirmed unchanged before Green validation.

**Ordinary correction packet — M602-FUP-green-02:** Inherit every Green01 v2 semantic field and identical allowed/forbidden path projection, with assignment `M602-FUP-green-02`, lease `m602-fup-green-02`, phase green, attempt 2, parent `m602-fup-green-01`, same owner/role and digest supplied by successful start. Baseline is the 208-file tree above; accepted Red/test identities are unchanged. Correct only the supplemental slot local's explicit `RecordValue` annotation in `tests/helpers/m602-operation.ts`; preserve all behavior and tests. Inspection and independent strict output isolate this to assertion-narrowed dynamic indexing, not a changed contract. Run the same complete focused and strict commands; report cohesion and stop for handoff. This consumes the ordinary Green correction, not a new chain or owner exception. Conditional attempt 3 remains subject to documented progress/new evidence; S3 and different final integration reviews remain pending. No real-operation allowance changes.

**Corrected Green accepted — M602-FUP-GREEN-ACCEPT-01:** Green02 changed only the specified source annotation and closed compliant with contract `2e93d9121bb99799cf4f71eb62b752bcce7bdd5cb71236499b1060075250e960`, receipt `9a9765fd5c301c5e4857419774790e46ba81e862fbaf3b01d0027038ff0a40ec`. All 192 focused tests pass; independent primary strict TypeScript passes. Primary inspected the actual eleven-file implementation and retained cohesive responsibility placement, optional sink forwarding, narrow executor reporting, independent pending state, report validation and exact historical pins. All five frozen tests remain byte-identical. Relevant tree: 208 files, SHA-256 `a4b00b79975348f2e16676a93c11d64bd8fe8be931c8f5ecc5a64c1cb5afb095`. Primary ran the exact historical-only CLI under maintained preparation: exit 0, authenticated original failure and all five pinned identities, `eligibleForContinuation:false`. This is historical inspection only; it does not identify the original rejection branch or authorize another case. S3 implementation review and complete follow-up regression/documentation closure remain pending; no active lease exists.

**Full-regression finding — M602-FUP-REGRESSION-01:** The maintained 48-file command passed 851 tests in its first 40 files, then stopped at `target-results-ui.test.ts`: the page could not initialize because the new `generation-diagnostics.ts` imports `node:util.types` through shared `proposal-contract.ts`, which the client imports through aggregate validation and review-form validation. Vite externalizes that Node-only module; the UI file reports 30 cancelled tests. This is a real browser regression, not a successful complete suite or a provider failure. No external page request occurred; scanner/UI/integration scratch is empty after teardown. Primary consulted the bug index; this newly introduced, task-contained correction remains in this plan. Initial S3 review independently agrees this is a Major F2/F6 issue and is completing the remaining contract audit before the last conditional Green allowance is used.

The new evidence invalidates complete follow-up acceptance despite focused/strict success. Proposed same-scope correction keeps the shared finite reader/emitter browser-compatible and places the Node-specific Proxy exclusion at the task-local untrusted-event collector before finite reading; the supplemental reader consumes already parsed JSON. This preserves event rejection without adding a dependency or relaxing the closed vocabulary. Conditional Green03 will require the complete review findings, a frozen correction packet and a fresh lease. No additional test or real-operation allowance is implied.

**S3 disposition and conditional correction — M602-FUP-green-03:** Fresh S3 review returns **REVISE** with one Major finding: the shared diagnostic module imports `node:util` into browser consumers. It reports no Blocker, Minor or second supported finding after all F1–F6 checks. Independent evidence includes ten pure diagnostic/collector tests and eight in-memory observer probes, exact tree/test/original hashes, compliant receipts and no active lease. Primary accepts the finding and the bounded dependency correction.

The conditional final Green is justified by concrete progress and material new evidence: Green02 resolved the only strict defect, all 192 focused tests pass, and complete regression now identifies a distinct browser import failure outside the focused command. Static inspection proves both client import paths, and the fresh reviewer confirms the same smallest remedy. Move the Node-only Proxy exclusion to the existing task-local collector, preserving it before any event reading; leave the finite descriptor reader and emitter browser-compatible. This changes only `generation-diagnostics.ts` and `m602-operation.ts`, adds no file/dependency and preserves all event/result contracts. It is likely to resolve the evidenced browser load failure because the shared import graph will no longer load a Node builtin. The existing browser tests are meaningful replacement regression evidence; no test change or fabricated Red is needed.

**Green03 Packet v2:** Inherit the unchanged Green01/02 objective, authorities, F1–F6 contract, readiness, responsibilities, risk, accepted Red/five frozen tests, eleven-file allowed and forbidden projections, environment, side-effect restrictions and handoff requirements. Assignment `M602-FUP-green-03`; lease `m602-fup-green-03`; phase green; attempt 3; parent `m602-fup-green-02`; same owner and `code_worker` role; digest supplied by successful start. Relevant tree remains 208 files, SHA-256 `a4b00b79975348f2e16676a93c11d64bd8fe8be931c8f5ecc5a64c1cb5afb095`. Write only the two-file dependency correction above. Worker runs the unchanged eight-file focused command and strict command; primary then reproduces the failing browser path through the complete maintained suite and rebuilds. Controlled regression harnesses may use their existing managed browser/scratch behavior; no manual task service, observation sampling or real operation is authorized. After worker handoff, close the lease and inspect actual code/frozen tests before the sole S3 correction review. That recheck covers all F1–F6 and new evidence. Different final integration review remains required. This is the last authorized Green write turn; no attempt 4 or replacement chain is available. Further decisive failure returns to primary triage and the existing owner boundary where required.

**Final Green handoff — M602-FUP-GREEN-03:** The two-file browser-dependency correction closed compliant with contract `10172f7f201338c66ea5ccf47c52cfb68acfe8b72b4d898e5f1c9cf6b0518432`, receipt `0f1daed8cda6f804d3a29545f1d7fb4808ab2bef4c9d82ce9f620588b9cd4025`. Primary inspected the relocated Proxy guard before event reading, the portable shared reader/emitter and unchanged tests. All 192 focused tests and fresh independent strict TypeScript pass; the five test hashes remain exact. Relevant tree: 208 files, SHA-256 `3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff`. All three Green write turns are consumed and closed; no active lease exists. Complete replacement regression is running. The sole S3 correction review must cover the full invariant packet and actual browser evidence, followed by different final integration review; no complete follow-up or M6-02 closure is claimed yet.

### M602-FUP-ACCEPT-01 — Offline follow-up qualification

Primary accepts the corrected follow-up implementation after fresh S3 **PASS** across all F1–F6, with no Blocker, Major or Minor remaining. The reviewer independently checked eight hostile-event vectors with zero trap/coercion reads and three sink-fault forms with zero unhandled rejection; its earlier ten diagnostic and eight lifetime probes remain valid for unchanged code. The exact two-file correction restores the browser import boundary while retaining Proxy rejection before collector input reading. No additional generalization or dependency was introduced.

The replacement maintained 48-file suite passes **954 tests**, zero failures/skips/cancellations, exit 0. It includes all 192 focused assertions and the formerly failing browser suite with no page initialization errors. Independent strict TypeScript passes. The current 208-file relevant-tree identity is `3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff`; five accepted tests remain byte-identical. All six original packages still authenticate and reproduce their fixed wire identities without preparation or generation. Exact historical-only CLI inspection passes on corrected code and explicitly denies continuation eligibility.

After verifying the previous generated inventory, the maintained Vite build passes with 76 transformed modules. Current output is exactly `index.html` (SHA-256 `d9e773ca23458f1bd97d6e1604ab2a01e706d06c730d84c214e130682f9de5ab`), `assets/index-C6L8S9Ht.css` (`4962722f0d6585b7c3e2c0b232e6617161829991988b859d12f8f8c10c48cbfa`) and `assets/index-C7OtU_Ke.js` (`7f0e352a8563423b7f4485599192ba63adc5a812fb8f1f744ddca0c09e7a085d`). Because these generated bytes changed after full regression, primary is repeating their eight maintained consumers: guidance/generation/review APIs, synthetic public-comparison tests, walking-skeleton/rescan integration, and review UI/checkpoint tests. All original flags, wrapper and assigned scratch paths apply. This is affected-artifact verification, not an additional real campaign.

Preservation checks authenticate all 430 protected files and the exact three original Local image records. The actual root still contains only `local-image`; the prospective real supplemental root does not exist. No real model/runtime/credential call or original evidence mutation occurred. Controlled regression harnesses exercised their existing synthetic service/browser boundaries and cleaned their assigned scratch. Actual observer effectiveness and the original detailed rejection cause remain unknown. M6-02 stays In progress; no later task, retry or successor campaign is selected.

**Post-build acceptance:** All 77 tests in the eight affected consumers pass, zero failures/skips/cancellations, exit 0, against the current generated assets. Scanner/UI/integration scratch is empty. The full-suite source/test identity is unchanged, so unaffected results remain reusable. This closes the generated-output verification gap without repeating unaffected tests.

**Final-review guard triage:** The reviewer verified the Green03 terminal receipt remains compliant, but its read-only current `status` returns `closed-drifted` after coordinator documentation maintenance. Primary reconciles the exact differences: only README, this owning plan and the progress narrative changed after lease closure. These are the authorized primary-owned updates recorded above, made with no active worker lease. HEAD/ref/index/ignore invariants, all source/test identities and generated build identities remain unchanged. The original terminal receipt is neither rewritten nor treated as proof of the later documents. Fresh complete-suite, post-build and documentation checks bind the current state independently; no code/test evidence or correction allowance is reset by this expected documentation movement.

Documentation impact: README adds the maintained follow-up suite, historical inspection command and capability limits; this plan owns qualification, corrections and remaining gates; the progress narrative mirrors the accepted checkpoint. Requirements, ADRs, frozen definitions and roadmap status are unchanged. Different final integration review is required before closing this offline follow-up; the main M6-02 Verification remains unmet.

### M602-FUP-FINAL-01 — Final integration and offline closure

A different fresh critical reviewer returns **PASS WITH FOLLOW-UPS** for the complete offline follow-up and truthful incomplete-task handoff. It independently verifies the 208-file code/test identity, five frozen tests, three build assets, Green03 contract/receipt digests, absence of an active lease, historical-only reader and three pure lifecycle probes. It accepts reusable 954-test, strict/build and 77 post-build evidence and reports no Blocker or Major.

Primary dispositions both Minor items: (1) stale current-state wording is corrected to show complete regression and now final follow-up acceptance; (2) the single extra blank EOF line in `tests/helpers/m602-evidence-files.ts` is explicitly retained as a harmless cosmetic deviation. It affects no behavior, authority or required verification and warrants no additional implementation turn or evidence invalidation. Tracked `git diff --check` passes; the untracked files were separately inspected, and no universal clean-whitespace claim is made. The expected coordinator documentation-only guard drift is reconciled above, without changing the terminal receipt or code/test baseline.

The authorized offline scope is complete. Documentation closure reconciles README commands/capability limits, this plan's current state and evidence, and the progress checkpoint. All 11 PowerShell blocks parse, affected local links/anchors resolve, UTF-8/final-newline and proportional whitespace checks pass subject to the explicit cosmetic disposition. Original HEAD, lock, all 430 protected files and three failed-case records are preserved; managed scratch is empty. Requirements, ADRs, frozen definitions, roadmap status and later-task authorization are unchanged. No commit or push occurred.

M6-02 itself remains **In progress**, its ExecPlan stays unarchived and its Verification is unmet: only the original failed Local image operation ran, its in-call observation was missed, and five cases remain stopped. Original detailed rejection and real observer effectiveness remain unknown. Further generation requires a separately authorized owner disposition and plan; this closure grants none.

### M602-SUCCESSOR-01 — Separate campaign authorization and decision contract

**Owner disposition, 2026-09-21:** The owner authorized the proposed separate six-case campaign, its required planning, implementation and independent review. Preserve the original Local image failure and five stopped original cases. Use the same frozen M301 packages, Local image/label/contrast then Groq image/label/contrast, with at most one entry and one dispatch per new case. Qualify actual observation collection before generation. All original readiness, semantic assessment, privacy, cleanup and stop-on-first-failure gates remain. This is explicit new scope; completed or exhausted original and offline-follow-up chains remain unchanged. No retry within either campaign, prompt/model/provider change, warm-up, acquisition, later roadmap task, commit or push is included.

**Decision Review Contract — M602-SUC-G:** R3 for the separate evidence identity and continuation boundary, real observer lifetime/concurrency and effect cleanup. Target artifact is one successor execution contract in this same M6-02 ExecPlan. Requirements and Accepted ADRs remain controlling; this contract cannot silently amend them. Resolve whether an additional manifest binding is required under REQ-EVAL-005/007 before any successor output, retaining the immutable M301 input definition and all earlier results. The new allowance does not make the original campaign complete or permit cherry-picking across campaigns.

**Question and fixed candidates:** Compare (A) a narrow successor entry point composing the existing authenticated package, operation, diagnostic and observation boundaries with a literal new campaign binding, and (B) a narrowly extended existing operation/CLI with an explicit literal successor mode. Both must reuse the existing execution, validation, filesystem and lifecycle policies. Neither may pass real I/O through the synthetic dependency seam, weaken current-code checks, overwrite original records, introduce arbitrary roots/campaign IDs, duplicate provider implementations or add a general campaign framework. Compare an existing managed browser/application procedure with direct composition of existing browser-test primitives only as needed to supply real observers; no new product UI or general monitoring system. An option that cannot qualify bounded, genuine in-call evidence is eliminated regardless of convenience.

**Criteria and hard gates:** immutable six input/wire/control identities; unambiguous separate campaign/code/evidence linkage; one-use exclusive entry and first-failure stop; no cross-campaign predecessor acceptance; real UI responsiveness and validated runtime/GPU samples strictly within native lifetime; no new effect initiation after known terminal state, honest failed/missed/aborted evidence and separately confirmed owned cleanup; unchanged credential and minimized egress boundary; smallest cohesive implementation/procedure with no dependency acquisition. Already initiated browser/HTTP/process work may settle after cancellation; such a sample cannot qualify or be retroactively moved inside the native window. Report Local and Groq separately, no quality score or ranking. The original failure remains visible even if a successor case succeeds.

**Decide now / prove later:** Decide exact literal identity/root/CLI and manifest relationship, prior-case eligibility, observer provisioning and ready/dispatch/terminal ordering, bounded sample shapes and assessment linkage, cleanup/uncertainty rules, ownership and commands before code or live effects. Implementation proves those semantics with native/credential/filesystem doubles and controlled short-lived observation windows. Before the first generation, primary must separately prove the actual application/browser and available sampling commands are ready using a no-generation procedure defined by the reviewed contract. That proof grants no warm-up, embedding, scan, retrieval, synthetic model request or extra runtime metadata poll. Runtime model admission remains inside each actual operation; current Groq public/account readiness remains an execution prerequisite. Do not defer a mechanism choice as a runtime experiment.

| Invariant | Trigger and required result | Evidence owner / reviewer |
| --- | --- | --- |
| S1 authority and completeness | All successor bindings, commands, proof and stop rules resolved before effects; original and successor counts remain separate | Primary, analyst and both research reviewers |
| S2 identity and preservation | Wrong campaign/code/manifest, old predecessor, occupied/partial record or frozen drift blocks before dispatch; original three record hashes and 430 protected files remain unchanged | Critical researcher, test/code owners and S3 reviewer |
| S3 one operation and recovery | Duplicate/reentrant/late entry, abort, publication failure or ambiguous cleanup never renews an entry or advances the next case | Critical researcher and independent failure-path review |
| S4 real observation lifecycle | Actual observer readiness precedes generation; known terminal refuses new effects; failed/missed/pending samples cannot qualify; timestamps alone cannot prove a sample or responsive UI | Researcher, primary live qualification and S3 reviewer |
| S5 privacy and behavior | Same packages/wires/provider validation/credentials; no retained raw envelopes, hidden reasoning, secrets or private material in tracked files; no production eligibility change | Researcher and both implementation reviewers |
| S6 honest completion | One primary semantic evaluator; all required case/observation gates pass before task completion; failure stops the campaign without retry or cross-campaign replacement | Primary, analyst and different final integration reviewer |

**Routing and budget:** One bounded critical-research report covers the coupled identity, observer lifetime and preservation question, plus at most one targeted follow-up. Primary concurrently maintains authorities, preservation and command/resource facts. One decision analyst receives the shared evidence at a single barrier (initial plus one bounded correction). Fresh pre-draft and different final critical research reviewers inspect all S1–S6; one supported pre-draft correction and the workflow's two final-artifact correction cycles apply. Primary is the sole authoritative writer; no drafter is needed. This newly authorized successor scope permits one coherent implementation slice, read-only test preflight, separately owned Red and Green, one initial and ordinary correction per chain and a conditional third only under ADR-0024's documented criteria. Exact paths/interfaces must be accepted before leases. Fresh S3 review has one correction loop; a different final critical integration review follows actual evidence or the truthful stopped handoff. No reviewer may generate or rescore model outputs. Stop on an unresolved hard gate, material scope change, repeated decisive failure without new evidence or exhausted budget; do not rename a chain to renew it.

**Entry evidence and effects:** Read-only resumption confirms HEAD `b8b9141e85ead2027e8eaf4bf59d33b1a3a20462`, current 208-file tree `3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff`, unchanged lock, all 430 protected file hashes and the three original failed-case pins. No active lease, synthetic test residue or task-owned process is carried forward. Reuse the accepted 954-test/strict/build and 77 post-build evidence until a relevant change invalidates it. No runtime readiness is inferred from yesterday's process. Before the reviewed contract, only read-only repository/public-source research and primary plan maintenance occur; no process startup, observation request, credential access, successor directory, implementation edit or generation is authorized at this stage.

**Research checkpoint:** SUCR01–11 trace authority, original-root/test-seam isolation, current timing-only observation, reusable exclusive publication, existing service/browser primitives, sampling boundaries and source-record/assessment linkage. The critical researcher completed its initial report and sole bounded follow-up. The decision analyst independently inspected the current integration and returned **DRAFT READY**, recommending candidate B for fewer public entry surfaces while retaining dedicated observer and evidence owners. No operational reliability advantage is claimed. Fresh pre-draft review returned PASS across S1–S6. Different final review returned REVISE with SUCF-01–03 Major interface/lifecycle findings and SUCF-04 Minor status reconciliation. Primary accepts all four findings and submits the complete revised contract under final-artifact correction cycle 1; no implementation contract is accepted yet. One analyst correction remains; researcher follow-up is exhausted; one final-artifact correction cycle remains after this submission. The owner separately requested read-only Groq Console inspection; it established displayed limits without a credential read, settings change or provider request. This does not establish credential-to-project association or guaranteed request token fit.

### M602-SUC-CONTRACT-01 — Successor execution and actual observation

**Disposition:** Authored by primary after SUCR01–11, analyst DRAFT READY and fresh pre-draft **PASS**, with no findings across S1–S6. The reviewed late-resource ownership clarification is incorporated below. Candidate B is selected: literal successor branches in the existing caller, with cohesive observation/evidence owners and one private shared operation core. Candidate A would still require that core and readback changes while adding another CLI. This selection proves no operational reliability. Different fresh final research review returns **PASS** after correction cycle 1 at artifact SHA-256 `73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d`, with no findings across S1–S6. Primary accepts the contract under the existing owner scope; implementation and actual qualification remain downstream proof. Prior G/FUP contracts continue to govern their unchanged original exports and records.

#### Immutable manifest and identity

Green materializes exactly the following UTF-8 JSON with two-space indentation, no CR/BOM and one final LF at `evaluation/m602-successor-v1.json`. Primary owns its authoritative literals; the worker receives no discretion to change them. Expected SHA-256 is `5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276`. This is the new execution/evidence manifest required for the successor; its M301 reference preserves the original package definition and Groq amendment. It copies no ignored payload or response.

```json
{
  "version": "m602-successor-v1",
  "status": "frozen",
  "frozenAt": "2026-09-21T13:35:01.000Z",
  "inputDefinition": {
    "path": "evaluation/m301-generation-v1.json",
    "sha256": "63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b"
  },
  "caseOrder": [
    "local-image",
    "local-label",
    "local-contrast",
    "groq-image",
    "groq-label",
    "groq-contrast"
  ],
  "executionPolicy": "m602-successor-execution-v1",
  "observationPolicy": {
    "version": "m602-successor-observation-v1",
    "startDelayMs": 1000,
    "deadlineMs": 5000,
    "cleanupDeadlineMs": 5000,
    "qualificationWindowMs": 7000
  },
  "groqAdmission": {
    "version": "m304-groq-request-bytes-v1",
    "maximumBytes": 65536,
    "exposedDefaults": "groq-gpt-oss-20b-2026-09-11-v1"
  },
  "rubric": "inherit-input-definition",
  "failureInterpretation": "inherit-input-definition",
  "advancement": "accepted-proposal-and-qualified-observations",
  "consumption": "exclusive-case-directory",
  "maximumEntriesPerCase": 1,
  "maximumDispatchesPerCase": 1,
  "stopOn": "first-failed-invalid-unknown-or-unqualified-case",
  "originalEvidence": "preserved-separately"
}
```

Authenticate this exact manifest and unchanged default M301 package before effects. Its own digest and producer-code digest belong in records, not inside the manifest, avoiding circular hashing. Retain G's six wire identities, exact controls, original frozen references, canonical validation, one-case order and rubric. Original allowance and successor allowance are separate; no result from either fills a missing case in the other.

Default successor root is exactly `temp/m602-successor-v1`. Primary creates only that ignored ordinary root after inherited-ACL and preservation checks; no case is precreated. It initially admits only `qualification.json` and consumed six literal case directories. Each case admits only `entered.json`, optional `dispatch.json`, `result.json`, `observation.json` and optional `assessment.json`; incomplete sets remain non-advancing. All existing bounded ordinary-ancestor, single-link, canonical UTF-8 JSON, exclusive full-write/fsync/close/readback rules apply. Never overwrite, repair, rename over or remove retained records, including partial qualification or observation publication.

#### Interfaces, placement and test isolation

Preserve `executeM602Case`, `readM602Case`, `readM602Followup`, original CLI branches and historical-only inspection semantics. Add the following exports to `tests/helpers/m602-operation.ts`:

- `executeM602SuccessorCase(caseLabel: M602CaseLabel, dependencies?: M602SuccessorDependencies)`: resolves `{ok:true,result:M602SuccessorResult,observation:M602SuccessorObservation,observationSha256:string}` or `M602SuccessorFailure`. A valid retained failed/unqualified result may return `ok:true`; this never means case acceptance.
- `readM602SuccessorCase(caseLabel, dependencies?)`: resolves the same success fields plus `assessment:M602SuccessorAssessment|null`, or `{ok:false,error:'evidence-blocked'}`. No service/browser/observer, adapter, credential, native request or write is created.
- `qualifyM602SuccessorObservers(dependencies?)`: resolves `{ok:true,qualification:M602SuccessorQualification,sha256:string}`, or the same closed execution error union. Existing valid qualification is inspected/reused, never overwritten; an occupied invalid/partial file blocks. Qualification is not a case and consumes no generation entry.

Export `M602SuccessorFailure` from the evidence owner as exactly `{ok:false,error:'evidence-blocked'|'evidence-publication'|'observer-readiness',cleanup:M602SuccessorCleanup}`. Execution and qualification preserve this receipt on every failure, including missing dependencies, already-aborted admission, pre-entry preparation failure and partial source/observation/qualification publication. Before any resource acquisition, all cleanup fields are not-created; initiated but unresolved acquisition is uncertain at cleanup expiry. A failure after acquisition returns the frozen receipt from that invocation's owned cleanup. The CLI emits exactly these closed fields and exits nonzero; it must not collapse them to a generic error. This returned receipt is control evidence, not a replacement durable case record, an assessment or authority to overwrite residue. Primary may resume pre-entry readiness only after inspecting confirmed cleanup; any uncertain field or unavailable receipt prevents further live effects until reconciled. Readback remains read-only with its original closed error return above.

The CLI owner additionally exports the pure `formatM602SuccessorFailure(failure:M602SuccessorFailure):string`, with a type-only import from the evidence owner. It returns one compact JSON object plus one LF, selecting exactly `ok:false`, the bounded `error` and the six named `cleanup` properties. It never spreads arbitrary input properties, performs I/O, dispatches an operation or mutates process state. The actual successor execute/qualification failure branches write this function's return to stdout and set exitCode 1. This narrow formatter is the isolated proof boundary for CLI failure receipts; tests cover complete, not-created and uncertain cleanup, all three errors, extra-property exclusion, final newline and input immutability. Primary's actual-diff inspection verifies both branches use the formatter with nonzero exit and no alternate raw-output path. That branch wiring uses proportional structural evidence, not a new live runner, loader, mocking flag or default-resource test. Existing original and readback-only error behavior remains unchanged.

The private shared core owns exclusive entry, authenticated admission, adapter/native accounting, exactly one timing coordinator, dispatch and result publication. It returns an internal frozen snapshot of source hashes, result, diagnostics and timing. The original wrapper retains its original supplemental publication; the successor wrapper performs owned cleanup then publishes the successor observation. Do not create two coordinators or publish an original supplemental report for successor execution. Original paths, schemas, public return unions and defaults remain unchanged. Shared validators may accept a private discriminator containing only the two fixed record versions; validate every original property and every successor extension before returning, never strip unchecked properties to manufacture an original record.

New `tests/helpers/m602-successor-observers.ts` owns actual provisioning, UI/runtime/GPU collectors and resource cleanup. New `tests/helpers/m602-successor-evidence.ts` owns successor manifest, qualification, observation and assessment-projection validation. Operation remains evidence/effect coordination; browser/HTTP/child-process implementation does not move into it. Type-only imports of operation record types are permitted; no runtime circular dependency. Reuse existing provider, package, timing and filesystem modules. No application-source responsibility changes or production imports of evaluation code. Code fingerprint adds both new helpers and the new manifest to all current inputs; no other executable extraction is permitted without reconciliation.

Export successor record types from the evidence owner. `M602SuccessorResult` has precisely the original result fields with version `m602-successor-evidence-v1`; it does not replace the old result type. `M602SuccessorDependencies` is a new type in the operation owner with the existing test fields `root`, `packageEnvironment`, `requestImplementation`, `credentialIO`, `signal`, `filesystem`, plus `successorManifestEnvironment:M602PackageEnvironment` and `observerIO:M602SuccessorObserverIO`; fields may be optional in the TypeScript type to test missing-dependency rejection. It has no original `followup` option. A supplied object, including empty, selects isolated testing permanently. Require root, package environment and successor manifest environment for all operations; execution additionally requires the native double and Groq's credential double. Local execution and qualification additionally require the complete observer I/O set. Missing members fail before entry/resources; no actual factory fallback. Readback never invokes collaborators and requires no request/observer/credential double.

An injected root remains an ordinary `temp/m602-test-<UUID>` child, with test-owned `runs`, `client` and `observer-scratch` children; it cannot reference actual/frozen roots. Test setup creates the empty runs/scratch and three synthetic build files before qualification. Test manifests use the same closed schema/literals and canonical bytes but bind their synthetic M301 digest; the supplied manifest digest authenticates them. The shared reader checks this complete relation, with no skip-validation branch. Default manifest loading admits only the literal default digest/path above.

Export `M602SuccessorObserverIO` with exactly five mandatory trusted collaborators: `startApplication:typeof startLocalService`, `launchBrowser:typeof chromium.launch`, `runtimeRequest:OllamaNativeRequest`, `spawnGpu:typeof import('node:child_process').spawn`, and `connect:typeof import('node:net').connect`. Real defaults remain private; any supplied I/O object must contain all five before use. These seams inject existing primitive shapes, not new providers or arbitrary real roots. The observer owner exports `prepareM602SuccessorObservers(environment?, signal?:AbortSignal)`, where the optional complete environment is `{root:string,applicationRevision:string,io:M602SuccessorObserverIO}` and root must be the isolated UUID root. Absent environment uses the fixed production paths, prepared process revision and private primitives. Execution and qualification forward their caller signal unchanged; omission uses a private initially non-aborted signal. Already-aborted signals refuse all resource acquisition. Register cancellation before the first awaited stage, check before each subsequent stage, and retain cancellation/late-resource ownership throughout preparation and the returned session. Caller abort during an awaited stage closes admission immediately; a late resource is owned and closed without starting the next stage. Success returns `{ok:true,session}`; failure returns `{ok:false,error:'observer-readiness',cleanup:M602SuccessorCleanup}`. Session has `observers:M602Observers`, validated `gpuBefore`, `samples(timing):M602SuccessorSamples` and idempotent `close():Promise<M602SuccessorCleanup>`. Session methods freeze snapshots and never reopen work.

Tests live only in existing `tests/m602-generation.test.ts` (append successor integration using its public synthetic fixtures) and new `tests/m602-successor.test.ts` (observer/evidence/CLI contract and primitive lifecycle doubles). Existing accepted assertions remain unchanged. Code-owned files are the operation, CLI, two new successor helpers and exact manifest. Existing package, timing, historical, filesystem and production modules are reused unchanged. If a necessary cohesive owner falls outside these paths, stop for primary reconciliation instead of placing it in an unsuitable allowed module.

#### Closed records and qualification

All timestamps are canonical UTC ISO; hashes are lowercase SHA-256; counts/memory are finite safe nonnegative integers. Reject unknown fields at every level. Successor entered retains original fields and adds exactly `campaign:'m602-successor-v1'`, `campaignManifestSha256`, `qualificationSha256`; `manifestSha256` still denotes M301. Dispatch/result retain original fields and semantics with the new version. Readback requires current code/configuration, exact successor/M301 identities and all original source hash links.

`M602SuccessorQualification` is exactly:
`{version:'m602-successor-qualification-v1',campaign:'m602-successor-v1',qualifiedAt,campaignManifestSha256,manifestSha256,producerCodeSha256,applicationRevision,nodeVersion,browserVersion,build,ui:true,gpu:true,cleanup}`.
`applicationRevision` is the actual 40-hex HEAD supplied by the prepared command; default execution checks it against qualification. `nodeVersion` is `v24.20.0`, `browserVersion` is `151.0.7922.34`. `build` is the ordered three-entry array of `{path,sha256}`: `index.html`, `assets/index-C6L8S9Ht.css`, `assets/index-C7OtU_Ke.js`. Default bytes must match the accepted build inventory in M602-FUP-ACCEPT-01 at first qualification; each later read compares those ordinary files' current bytes with qualification. Isolated tests use the same paths with their synthetic contents. A different actual build inventory requires primary reconciliation before qualification, never an arbitrary client root.

`M602SuccessorCleanup` is exactly `{ui,runtime,gpu,application,browser,scratch}`, each `complete|'uncertain'|'not-created'`. A resource that was created cannot be called not-created. Qualification has complete UI/GPU/application/browser/scratch and runtime not-created (the in-memory runtime double creates no resource). Groq observation has all not-created. Local successful observation requires every field complete. A Local no-dispatch/early-terminal outcome may honestly record runtime not-created; it cannot qualify.

Qualification invokes the actual built service/browser with the same benign UI procedure and one actual baseline GPU sample, then exercises UI and synthetic runtime/GPU observer callbacks inside one 7000-ms synthetic window through the same coordinator. The already validated actual GPU sample supplies the synthetic GPU value; no second actual GPU command or runtime GET is made. This separates real primitive readiness from synthetic timing qualification. The signal, timer, parsers, state verification and cleanup path are the actual implementation; synthetic native/runtime evidence never masquerades as a model call or actual residency. It creates no case, dispatch, result or assessment. Require successful UI, actual GPU validation, completed synthetic observation slots and confirmed owned cleanup before exclusive qualification publication. Its `qualifiedAt` follows successful cleanup. Identity-preserving readback of qualification may reuse that procedure proof, but each case still provisions and verifies live resources anew.

#### Actual resources, admission and lifecycle

Default preparation requires the already-existing ordinary `data/runs`, the pinned built client, empty `temp/m104-ui`, and empty existing scanner/integration scratch; it does not create a run or alter developer data. Use `startLocalService({runRoot,clientRoot,applicationRevision,port:0})`, the same production options as main.ts. Do not invoke scan/retrieval/generation/review routes. Launch existing full managed Chromium with `channel:'chromium',headless:false,timeout:10000`; no browser acquisition or isolation-disabling argument. Create one fresh context with viewport 1280×800, acceptDownloads false and serviceWorkers block, one page, and no user profile. Admit browser requests only to the returned exact loopback origin using GET/HEAD; block and fail preparation/observation on any other request, popup, download, crash or page error. No external page navigation or form submission. Readiness verifies the built page, enabled Target URL and Local/Groq controls with Local selected.

Each service startup, browser launch, context/page setup and page-ready stage has a 10000-ms bound; individual UI actions have 5000-ms bounds. All preparation paths have a closing latch. Timeout/abort/failure closes admission before callbacks, prevents the next stage and retains ownership of any late-created service/browser/context/page or baseline GPU child. Observe and close late arrivals. Confirm closure within the separate 5000-ms cleanup deadline; pending resource acquisition at expiry is uncertain, not not-created. No readiness correction or later live operation proceeds until primary reconciles that uncertainty. Timeout races never erase ownership.

After verifying predecessors/qualification, provision actual Local app/browser and take one baseline GPU sample before exclusive case entry. Pre-entry preparation failure ends the command with no case/call. Corrected readiness may be prepared again only after confirmed prior cleanup; this cannot replay a consumed entry. After entry, retain unchanged metadata admission, durable dispatch marker and at most one native request. Do not manage, restart or unload the developer's Ollama process.

On native scheduling, each configured observer creates one 1000-ms timer; on expiry it attempts its gate once. Terminal/abort clears the timer. No polling, early retry, late replacement, extended generation window or held-open response is allowed. Delay is a fixed scheduling policy, not an optimum inferred from the previous 13.744-second call. A short call or cold model can fail the observation and stop the campaign.

The observer owner enforces `deadlineMs:5000` as one total elapsed-time deadline per collector, starting synchronously when the accepted `gate.start` invokes that collector's effect. The initial 1000-ms scheduling delay is excluded. The deadline covers all UI steps together, or the entire runtime request/body validation, or GPU spawn/output/validation; individual API or inactivity timeouts do not replace or restart it. Each collector has a private cancellation controller linked to the coordinator and caller signals. At expiry, latch the collector failed, discard its pending value, cancel/destroy its owned work and reject its effect promise; if native lifetime is still active this records a failed slot, while an earlier coordinator close retains its already-frozen aborted/missed status. No expiry changes or extends native lifetime. Subsequent UI steps and value commits are prohibited after this latch. Pass the linked collector signal to supported UI APIs and check it before every step. Late primitive settlement remains owned for cleanup and cannot turn rejection into success. Clear deadline timers/listeners when the effect settles or is cancelled. Baseline GPU uses the same total 5000-ms deadline from immediately before its one spawn invocation; failure prevents entry. Qualification uses these same collector deadline rules inside its separately bounded 7000-ms synthetic window. The separate cleanup deadline and resource-close proof remain mandatory even when the effect promise has already rejected.

UI effect checks abort immediately before every asynchronous step and passes the same signal where supported: focus Target URL, verify it is focused, change Local to Groq, verify Groq checked and Local unchecked. Record only all-true booleans; changing radio necessarily follows focus verification. Do not fill or submit a target. The UI mode is only this idle interface's state and does not change the case's immutable adapter.

Runtime effect performs exactly one GET to `http://127.0.0.1:11434/api/ps`, no redirect/retry/header credential. Require HTTP 200, application/json, complete bounded body at most 65536 bytes, strict UTF-8/JSON, one matching `qwen3.5:4b` record, exact accepted digest, context 32768, positive integer size and nonnegative size_vram no greater than size. Return only digest/context/size fields. Other model content is discarded. Own error listeners and explicitly destroy request/response on timeout/abort; timeout notification alone is insufficient. Confirm actual resource terminal/close, not just parsed response.

GPU sampling directly spawns `C:/Windows/System32/nvidia-smi.exe` with exactly `--query-gpu=memory.used,memory.free` and `--format=csv,noheader,nounits`, `shell:false`, `windowsHide:true`, bounded piped output (4096 bytes per stream), no inherited console or arbitrary arguments. Require exit 0 and one row with two nonnegative safe integers. Discard stderr content and emit no exception text. Abort/timeout requests termination of that exact owned child; require child close/stdio closure, not its killed flag. Baseline uses the same 5000-ms bound and records actual completion before entry; one during-call sample uses the coordinator signal. No numeric capacity threshold or OOM inference from memory totals is introduced.

Collectors check signal state before committing validated values. Coordinator timing freezes before abort notification; collectors freeze on abort and never accept a late mutation. Final samples include values only for completed timing slots; a subsequent resource close can improve cleanup evidence but cannot change timing/sample qualification. Exactly one coordinator owns native lifetime. All response/body/provider/credential validation and native request accounting remain unchanged.

After core settlement, close the owned page/context/browser and service, settle sampling resources, require successful service stop and confirm its loopback port closed; require assigned scratch empty and protected run data unchanged. Use one 5000-ms cleanup deadline for the complete cleanup group, continue owning late settlement and report uncertainty honestly. Never delete unknown scratch residue or use forced termination as proof of clean service/browser shutdown. Any failure exit, including a source-publication error, attempts this cleanup. No linked observation is invented when complete source hashes do not exist.

#### Observation, assessment and continuation

`M602SuccessorObservation` is exactly `{version:'m602-successor-observation-v1',campaign:'m602-successor-v1',caseLabel,campaignManifestSha256,qualificationSha256,producerCodeSha256,enteredSha256,dispatchSha256,resultSha256,diagnostic,timing,samples,cleanup}`. Reuse FUP's diagnostic and timing schemas/coherence/matrix unchanged; dispatch hash/window can be null consistently with result. Publish exclusively as the case's `observation.json` only after owned cleanup. Successful result publication does not excuse failed observation publication.

`M602SuccessorSamples` is null for Groq, otherwise exactly `{gpuBefore,ui,runtime,gpuDuring}`. Baseline is `{observedAt,usedMiB,freeMiB}`, completed before enteredAt. UI is `{textboxFocused:true,radioChanged:true,radioChecked:true}` or null. Runtime is `{digest,contextLength,sizeBytes,sizeVramBytes}` or null. During GPU is `{usedMiB,freeMiB}` or null. Each in-call value is non-null if and only if its corresponding timing slot is completed; validate values and require the full timing interval strictly within the authoritative native window. Failed/missed/aborted/unavailable slots have null values. Runtime/GPU assessment observedAt derives from the corresponding slot's finishedAt; no competing sample clock or late relabeling. Groq timing has unavailable slots and creates no Local resources.

`M602SuccessorAssessment` retains exactly the original assessment fields with successor evidence version and adds `observationSha256`. Bind it to complete source result and observation bytes; assessment time follows result and completed cleanup/publication. Local observation is the exact projection of linked timing/samples, plus the primary evaluator's boolean `oomObserved`; accepted requires false. A primary cannot manually supply different measurements to pass. Preserve the original ten dimension names/order, uncertainty and original scanner/retrieval limitations. No raw generated text enters the assessment.

Advance only when every earlier successor case has an intact current-code/current-manifest/current-qualification result, diagnostic integrity complete, qualified observation with confirmed cleanup, exact projection and accepted primary assessment. Generation failure, invalid/missing/unknown record, sample failure, OOM/unusable UI, drift, disclosure concern or uncertainty stops all subsequent dispatches. CLI execution exits nonzero for nonproposal or unqualified observation; zero means mechanically eligible for primary assessment, never semantic acceptance. Readback exit zero means valid records, including valid failed outcomes, not permission to continue. No original record, readback or assessment can pass a successor gate.

#### Commands, qualification and review gates

Use the maintained README preparation without acquisition. Original commands remain available for their original meanings. The parser adds exactly `execute-successor` and `readback-successor` modes with one case label, and a separate `{ok:true,mode:'qualify-successor-observers'}` success variant without caseLabel. Reject every duplicate, extra, alternate root/campaign or combined mode flag. Imports perform no effects. CLI projects only closed status/count/hash/observation/cleanup fields; never print a retained proposal or raw sample source.

All actual successor commands additionally set `A11Y_APPLICATION_REVISION` to current HEAD and restore its exact previous value in finally. Run through `Invoke-M105Command` with `$m105UiTemp` so browser paths/scratch and compile-cache environment are prepared/restored. Exact command payloads are:

```powershell
# Each command is a separate primary-gated invocation, never a pasted batch.
& $m105Node tests/helpers/m602-run-case.ts --qualify-successor-observers
& $m105Node tests/helpers/m602-run-case.ts --execute-successor --case local-image
& $m105Node tests/helpers/m602-run-case.ts --readback-successor --case local-image
```

For later cases substitute only the next literal in the frozen order after accepted predecessor assessment. Require explicit nonzero propagation. Readback may use the same wrapper without browser scratch because it starts no resource. Historical-only inspection remains unchanged. Before Groq, recheck current public documentation and privately established account limits; do not inspect credentials or issue a capability probe. Apply ordinary sequential spacing where current limits require it without changing controls or adding retries; byte admission does not prove hosted token fit. Account UI observations are not proof that the existing credential belongs to that project or that a request will succeed.

The focused offline command is the prior eight-file FUP command plus `tests/m602-successor.test.ts`, under maintained preparation, Node v24.20.0 and `--test --test-timeout=120000`, followed by independent strict TypeScript. Tests use complete primitive/native/credential doubles and exact isolated UUID scratch; no actual browser/GPU/runtime/provider/credential effects. Test preflight is read-only. Agreed absent first-module seam is `tests/helpers/m602-successor-observers.ts`; its new export may block initial Red only after independent existing runner readiness, with blocked assertions explicitly reported. Green must execute every unchanged assertion.

S1–S6 proof must cover wrong/cross-campaign identity, manifest/code/build/qualification drift, original predecessor rejection, occupancy/partial publication, every missing injected collaborator and no real fallback; true one-call count and stop/assessment gating; startup timeout and late resource arrival, already-aborted preparation and caller abort during every awaited preparation stage, execution/qualification/CLI preservation of cleanup receipts before entry and after partial publication, total collector deadline across multiple UI steps and continuously arriving HTTP bytes, deadline/native-close ordering and late settlement, pre-entry failure with cleanup, terminal before timer, equal timestamps, abort between each UI step, pending child/HTTP/context closure, malformed/oversized samples, late value/close, no Groq observer invocation, exact assessment projection and immutable readback. Reuse existing original/FUP negative tests for unchanged policy, not duplicate every predicate. A valid synthetic qualification must not depend on private frozen inputs or current dist files.

One coherent successor slice uses separately owned test preflight/Red and Green with the budgets in SUCCESSOR-01. Primary opens/closes exact leases and accepts actual diff, tests, strict and cohesion before fresh S3 review. Code corrections never renew live entries. After last executable change, run the maintained full suite with the new test, independent strict/build, exact-original historical inspection and preservation checks; rerun affected generated-asset consumers if build changes. Only after contract final review, accepted Green/S3 and these checks may primary create the literal root and invoke actual no-generation qualification. Then each of six operations remains individually gated; no batch or automatic continuation. Different final critical integration review audits actual outcomes or the stopped handoff, followed by documentation closure. M6-02 remains In progress until its complete Verification passes.

### M602-SUC-PREFLIGHT-01 — Accepted contract and test preflight packet

Different final R3 re-review passes the entire revised artifact after correction cycle 1; SUCF-01–04 are resolved, with no remaining findings. Primary accepts that barrier without changing requirements or the frozen manifest. The final reviewer reproduced the 1175-byte manifest digest, unchanged 208-file tree and three original record pins; earlier complete preservation remains valid. No successor file/root or actual resource exists. The prior research checkpoint preserves the initial REVISE and correction submission as history; this paragraph records the accepted result. The next packet grants read-only preflight only.

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-PREFLIGHT-01
- Lease ID: None for preflight
- Phase: preflight
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m602-suc-test-worker
- Guard contract digest: None for preflight

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-FUP-FINAL-01 baseline (954 regression, strict/build, 77 post-build); successor entry preserved 208-file source/test tree; fresh Node v24.20.0 runner passes 13 generation-contract tests. Preflight pending.
- Accepted test boundary and current test owner: None until accepted; proposed two-file successor boundary owned by this test_worker.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Preflight must determine the missing behavior and existing reusable synthetic fixtures; report any testability/interface conflict before writes. Live observer feasibility and provider outcomes remain downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files: None for this read-only preflight. Prospective test lease only tests/m602-generation.test.ts and tests/m602-successor.test.ts.
- Allowed directory roots: None.
- Forbidden files: Every file for preflight; future tests cannot edit existing accepted assertions.
- Forbidden directory roots: All for preflight. Future Red forbids src, docs, evaluation, corpus, fixtures, .codex, .agents and tests/helpers.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Read-only preflight uses inspection and the already verified runner evidence; create no fixtures/caches and start no tests with write effects. Subsequent Red/Green command is the exact attached maintained-preparation nine-file focused command, with independent strict checking for Green; strict absence errors during first-module Red are reported as such.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: One honest preflight classification; map missing contract behavior to tests and concrete existing doubles. No code/test/manifest/root changes. Existing runner evidence is readiness, not successor coverage.
- Relevant-tree fingerprint: 208-file source/test/config tree SHA256 3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff; HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d. Current plan identity supplied at dispatch.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: None for preflight. Later tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One read-only preflight; later initial Red plus one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: At most two test write corrections, third total turn conditional; no budget renewal by new IDs. No write authorized now.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Inspect once, reuse fresh readiness; Red one decisive focused failure, Green unchanged full focused plus independent strict, primary acceptance and S3.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority, one classification, decisive source references, fixture/reuse mapping, full S1–S6 planned proof coverage, uncertainty, no-write/no-effect confirmation, and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.
```

The exact subsequent focused command referenced by the packet is prepared below; preflight inspects it and reuses runner readiness rather than executing absent tests or creating fixtures:

```powershell
$m602Readme = Get-Content README.md -Raw
$m602Prep = [regex]::Match($m602Readme, '(?s)### Development command preparation.*?```powershell\r?\n(.*?)\r?\n```').Groups[1].Value
if (-not $m602Prep) { throw 'Development preparation block missing' }
. ([scriptblock]::Create($m602Prep))
Invoke-M105Command {
 & $m105Node --test --test-timeout=120000 tests/m602-successor.test.ts tests/m602-followup.test.ts tests/m602-generation.test.ts tests/generation-contract.test.ts tests/generation-stage.test.ts tests/ollama-generation-contract.test.ts tests/ollama-generation.test.ts tests/groq-generation-contract.test.ts tests/groq-generation.test.ts
 if ($LASTEXITCODE -ne 0) { throw 'M6-02 successor focused verification failed' }
 & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
 if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
}
```

**Preflight result and reconciliation:** The test owner returned CONFLICTING after read-only inspection: successor behavior is missing and primitive doubles are feasible, but CLI failure-receipt tests lacked an isolated callable boundary. No file, fixture, cache, credential, process or actual operation was created. Primary accepts this specific conflict and selects the pure formatter specified above in the existing CLI owner, with test-owned behavioral proof and primary-owned branch wiring inspection. This adds no live execution seam or dependency. Final-artifact correction cycle 2 must re-review the complete S1–S6 contract before Red; the single preflight is not repeated and no worker budget is renewed. All other packet bindings remain unchanged. The future packet includes this formatter in the CLI responsibility and keeps the same exact files.

### M602-SUC-RED-01 — Successor behavioral test boundary

Final-artifact correction cycle 2 returns PASS across the complete S1–S6 packet at plan SHA-256 `bf3ffa565beeee1a6b17675383d428ea4573761428544cbc583fcc4e3a74f8f9`, with no findings. Primary accepts the pure-formatter reconciliation and classifies the remaining successor behavior MISSING. The single read-only preflight is complete; both final-artifact correction cycles are consumed. No additional permission is needed within the existing authorized scope. This initial Red changes only the two test-owned files. The following packet is prepared before guard start; the returned digest is supplied in the dispatch and retained at terminal acceptance rather than editing this plan during an active lease.

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-RED-01
- Lease ID: m602-suc-red-01
- Phase: red
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m602-suc-test-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-FUP-FINAL-01 baseline (954 regression, strict/build, 77 post-build); successor entry preserved 208-file source/test tree; fresh Node v24.20.0 runner passes 13 generation-contract tests. Preflight CONFLICTING reconciled by primary through the exact pure CLI failure formatter; all remaining successor behavior is MISSING.
- Accepted test boundary and current test owner: None until accepted; proposed two-file successor boundary owned by this test_worker.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Preflight must determine the missing behavior and existing reusable synthetic fixtures; report any testability/interface conflict before writes. Live observer feasibility and provider outcomes remain downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files:  tests/m602-generation.test.ts and tests/m602-successor.test.ts.
- Allowed directory roots: None.
- Forbidden files: All existing accepted assertions and every code-owned helper/configuration/manifest. Preserve original test content; append successor integration only.
- Forbidden directory roots:  src, docs, evaluation, corpus, fixtures, .codex, .agents and tests/helpers.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Execute the exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01. Expected missing first-module Red; collect full exit/results including unaffected tests. Its stop-on-test-failure intentionally does not run strict after failed Red; independent strict is mandatory in Green. No command or flags added.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Complete coherent behavioral tests for S1–S6 and formatter behavior; expected first-module failure at tests/helpers/m602-successor-observers.ts. Report blocked assertions honestly, no executed-coverage claim. Unaffected original tests remain passing. Existing runner evidence proves readiness only.
- Relevant-tree fingerprint: 208-file source/test/config tree SHA256 3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff; HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d. Current plan identity supplied at dispatch.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Red write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: At most two test write corrections, third total turn conditional; no budget renewal by new IDs. This turn is initial attempt1 only.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent Red boundary then one decisive focused command. Green later executes all unchanged tests plus strict; primary acceptance and S3.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Only missing successor behavior enters Red. CLI formatM602SuccessorFailure is the selected pure proof boundary; no live runner seam. Primary performs structural branch wiring inspection after Green. Preserve complete synthetic isolation and exact contract cancellation/deadline/cleanup failure matrices. Do not change current docs/plan or create the real manifest/root. All new synthetic build/manifest fixtures are test-owned inside UUID scratch and cleaned in finally. Accepted original assertions unchanged. First-module absence is deliberate; no stub creation.
```

### M602-SUC-RED-02 — Bounded synthetic lifecycle correction

Initial Red lease `m602-suc-red-01` closed compliant with digest `5d7c9c54bbf76ba0ff9f955582ebe183e8402aa29c1eb9efe9651a717033ca98` and receipt `78fb69192da746c57519b85d3f4f4b7e132cdc65b7bfb82e9837420a036ee054`; pinned status confirms no post-close drift. Only the two authorized test files changed. All original 71,335 bytes of the generation test match the guard baseline; the apparent raw-hash mismatch was the guard\'s file/executable metadata prefix, not an edit. The nine-file command reports 143 unaffected passing tests and two expected file-load failures for the absent successor module; 24 new behavioral groups did not execute. Strict checking did not run after Red.

Primary rejects initial Red acceptance on independently reproduced fixture defects: the integration driver leaves a 7000-ms qualification timer pending after advancing only 1000 ms, and the reused native fixture orders asynchronous observer completion after native end. A no-I/O Node probe with the maintained preparation reproduced both facts. The bug index treats these as routine task-contained test corrections, so no separate bug record is required. Existing original assertions remain frozen. The ordinary correction below uses attempt 2 of the same test chain, unchanged authority/scope/contract and a fresh lease; no owner approval or budget reset is needed. First-module Red remains the expected command result, not proof of behavioral execution.

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-RED-02
- Lease ID: m602-suc-red-02
- Phase: red
- Attempt: 2
- Correction parent lease ID: m602-suc-red-01
- Worker role: test_worker
- Lease owner: m602-suc-test-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-FUP-FINAL-01 baseline (954 regression, strict/build, 77 post-build); successor entry preserved 208-file source/test tree; fresh Node v24.20.0 runner passes 13 generation-contract tests. Preflight CONFLICTING reconciled by primary through the exact pure CLI failure formatter; all remaining successor behavior is MISSING.
- Accepted test boundary and current test owner: Initial successor Red rejected for independently reproduced fixture timing defects; same test_worker owns correction. Original 71,335-byte prefix of m602-generation.test.ts remains immutable.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Fix synthetic event ordering and bounded clock progression without modifying the accepted production contract. Review response completeness and close events against the actual primitive contracts; live feasibility remains downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files:  tests/m602-generation.test.ts and tests/m602-successor.test.ts.
- Allowed directory roots: None.
- Forbidden files: All existing accepted assertions and every code-owned helper/configuration/manifest. Preserve original test content; append successor integration only.
- Forbidden directory roots:  src, docs, evaluation, corpus, fixtures, .codex, .agents and tests/helpers.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Execute the exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01. Expected missing first-module Red; collect full exit/results including unaffected tests. Its stop-on-test-failure intentionally does not run strict after failed Red; independent strict is mandatory in Green. No command or flags added.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Complete coherent behavioral tests for S1–S6 and formatter behavior; expected first-module failure at tests/helpers/m602-successor-observers.ts. Report blocked assertions honestly, no executed-coverage claim. Unaffected original tests remain passing. Existing runner evidence proves readiness only.
- Relevant-tree fingerprint: 208-file source/test/config tree SHA256 3c6df2b1431e501a6dd178a45adc85ff80d4462df63d56061d1190d9cfb7b2ff; HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d. Current plan identity supplied at dispatch.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Red write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: At most two test write corrections, third total turn conditional; no budget renewal by new IDs. This turn is ordinary correction attempt2 only; attempt1 is terminal compliant but semantically rejected. A third turn remains conditional, never automatic.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent Red boundary then one decisive focused command. Green later executes all unchanged tests plus strict; primary acceptance and S3.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Only missing successor behavior enters Red. CLI formatM602SuccessorFailure is the selected pure proof boundary; no live runner seam. Primary performs structural branch wiring inspection after Green. Preserve complete synthetic isolation and exact contract cancellation/deadline/cleanup failure matrices. Do not change current docs/plan or create the real manifest/root. All new synthetic build/manifest fixtures are test-owned inside UUID scratch and cleaned in finally. Accepted original assertions unchanged. First-module absence is deliberate; no stub creation.


Primary correction disposition: The expected missing-module Red is not a failed implementation attempt or a command-count budget. New decisive evidence independently proves driveSuccessor leaves a 7000-ms timer pending after its sole 1000-ms tick, and advancingLocalNative produces observer-start, native-end, observer-finish because nativeHarness emits its body/end synchronously after callback. Correct only successor-owned fixtures in the allowed files: use a bounded clock driver that flushes asynchronous work and can settle the full qualification/cleanup window, and a test-owned controlled native lifecycle that allows observer effects to finish strictly inside a real simulated dispatch window. Preserve the original helper/assertions byte-for-byte. Audit all successor callers for the same assumption, realistic IncomingMessage.complete and request/response close semantics, and negative tests whose intended failure needs a positive witness. No weaker assertions, production stub, real fallback, or artificially extended actual generation window. Test-owned synthetic timing control is not permission to change runtime policy. Initial handoff hashes are generation fef6f8788a50c58bc51427218a10daf792674e65131156f685372315d7cbf13d and successor 2f617ad658461ed29b710d64ec504ec8f08f71a767330c257cad7d2ddbccece2. Primary verified prefix preservation; guard hashes are metadata-domain-separated. Run the same nine-file focused command once after coherent correction; expected missing-module failure still blocks the new assertions. Report static fixture ordering/protocol proof honestly, not executed behavioral coverage. No additional preflight, authority/research reset, or new scope.
```

### M602-SUC-RED-03 — Conditional final test correction

Red02 closed compliant with digest `fa14781375fdca0b49b72c1eb2467197d532a16aa021c2793ae22a562f6f53e0` and receipt `f9db366035a390509ad561f8517acb24778eb5e35fc5b89ac5adc5a23c0a6227`. Primary verifies the original prefix and corrected clock/native ordering, response completeness/close events, explicit runtime collection and strict UTF-8 fixture. The same focused result is 143 executed unaffected passes plus two expected module-load failures. Both the 24 new behavioral groups and legacy assertions in the importing generation file remain blocked; they did not execute.

Independent primary strict checking supplies material new evidence hidden by the early import failure: wrong existing Groq fixture arguments, unknown generic results, an untyped native double and an inferred dependency object missing its optional filesystem field. A narrow read-only helper audit additionally confirms reuse of a closed browser singleton across qualification/execution, and identifies baseline/entry mock-clock ambiguity against the existing strict timestamp validator. It changes no authority, contract or file. Primary retains these routine test corrections in the owning plan under the bug recording rule rather than creating a separate application-defect record.

Conditional attempt 3 is justified by concrete lifecycle progress in attempt 2, new strict/API evidence and a different bounded correction of collaborator shapes, local types and fresh synthetic resources. The unchanged scope and existing owner authorization cover it; there is no fourth test turn. The current 209-file tree is `af0f05921d862e4c757d2898de6cff1d3814ccf715269d10a8b14d3d60139aee`. All 430 protected files and three original records remain unchanged; no successor root exists. Red remains unaccepted until this final boundary is inspected.

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-RED-03
- Lease ID: m602-suc-red-03
- Phase: red
- Attempt: 3
- Correction parent lease ID: m602-suc-red-02
- Worker role: test_worker
- Lease owner: m602-suc-test-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-FUP-FINAL-01 baseline (954 regression, strict/build, 77 post-build); successor entry preserved 208-file source/test tree; fresh Node v24.20.0 runner passes 13 generation-contract tests. Preflight CONFLICTING reconciled by primary through the exact pure CLI failure formatter; all remaining successor behavior is MISSING.
- Accepted test boundary and current test owner: Initial successor Red rejected for independently reproduced fixture timing defects; same test_worker owns correction. Original 71,335-byte prefix of m602-generation.test.ts remains immutable.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Fix synthetic event ordering and bounded clock progression without modifying the accepted production contract. Review response completeness and close events against the actual primitive contracts; live feasibility remains downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files:  tests/m602-generation.test.ts and tests/m602-successor.test.ts.
- Allowed directory roots: None.
- Forbidden files: All existing accepted assertions and every code-owned helper/configuration/manifest. Preserve original test content; append successor integration only.
- Forbidden directory roots:  src, docs, evaluation, corpus, fixtures, .codex, .agents and tests/helpers.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Run the exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01 once after coherent correction, collecting the expected missing-module Red. Separately run the same independent strict TypeScript command even after that expected failure, and distinguish deliberately missing successor modules/exports from all unrelated test errors. Only the agreed missing callable/type names may remain; no production stub, skip, blanket suppression or weaker assertion.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Complete coherent behavioral tests for S1–S6 and formatter behavior; expected first-module failure at tests/helpers/m602-successor-observers.ts. Report blocked assertions honestly, no executed-coverage claim. Unaffected original tests remain passing. Existing runner evidence proves readiness only.
- Relevant-tree fingerprint: Current 209-file source/test/config tree SHA256 af0f05921d862e4c757d2898de6cff1d3814ccf715269d10a8b14d3d60139aee; HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Red write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: This is final conditional attempt3 under ADR-0024; no fourth turn, sibling lease or reset. Attempt2 materially corrected asynchronous lifecycle and protocol fidelity. New independent strict checking and a bounded read-only helper audit isolate remaining test-side signature/inference errors and singleton factory state. The different corrective action is to bind existing collaborator signatures correctly, fix local test typings, allocate fresh synthetic resources per acquisition and separate baseline/entry mocked time. These exact changes can resolve the supported remaining defects without production or contract changes. Existing owner authorization covers this conditional correction; all other chains remain unchanged.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent Red boundary then one decisive focused command. Green later executes all unchanged tests plus strict; primary acceptance and S3.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Only missing successor behavior enters Red. CLI formatM602SuccessorFailure is the selected pure proof boundary; no live runner seam. Primary performs structural branch wiring inspection after Green. Preserve complete synthetic isolation and exact contract cancellation/deadline/cleanup failure matrices. Do not change current docs/plan or create the real manifest/root. All new synthetic build/manifest fixtures are test-owned inside UUID scratch and cleaned in finally. Accepted original assertions unchanged. First-module absence is deliberate; no stub creation.


Primary final correction disposition: Correct only the successor appendix and new successor test. (1) Existing groqNativeHarness requires an array of reply objects, and virtualCredentialIO requires an options object or defaults; use the actual existing signatures. (2) Give the test-owned operation/dynamic calls and withSuccessorRevision/driveSuccessor results useful explicit types so generic inference does not produce unknown; annotate native fixture with the existing OllamaNativeRequest contract and admit optional filesystem in test-owned dependency typing. (3) successorObserverHarness must return fresh service/browser/context/page/UI state per acquisition, retaining aggregate call recording. (4) Guarantee baseline completion precedes entry under mock Date by a narrow test-owned filesystem wrapper that advances 1ms at exclusive literal case-directory creation; compose, do not discard, this wrapper in code-drift/publication fault overrides. Preserve original first71,335 bytes and all assertions. The advisory explorer found no other definite helper/API conflict. Maintain the corrected bounded asynchronous native/observer window and UTF-8 evidence from attempt2. No runtime policy change, production stub, fixture-owner extraction or real effects. New strict evidence is not another research or preflight cycle. Report new 24 groups and original assertions blocked by the static import honestly; the 143 pass count concerns only executed unaffected files. Stop after final handoff.
```

### M602-SUC-RED-ACCEPT-01 — Corrected first-module Red accepted

Red03 closed compliant with digest `a3a414e31c36c0615a95a1dfa536fd2ca7d3a3df41070b005fae69e2c6355647` and receipt `87aafd2671c961a492c6763d03743322888b15a85e1437398243d9a1b3d0e66b`; only the successor appendix changed in that turn. Primary inspected the actual corrected signatures, fresh factory state, composed entry clock and unchanged original prefix. It independently reproduced 143 executed unaffected passes and the two importing-file failures. Independent strict checking reports only the agreed missing observer/evidence modules and formatter export, with no unrelated fixture error. This is accepted initial Red under the first-module exception, not executed behavioral coverage: all 24 new groups and legacy assertions in the importing generation file remain blocked until Green.

Frozen test boundary: generation test SHA-256 `4c0b1e9d35f4e72c5bd8dc37048e3266e5a37205606f2428980abf482be9b184`; successor test SHA-256 `8b3fedfeb72cc6533780e512452a99d78234bd997196f5fa7fef5d7d1ca63ac2`. The latter corrects a duplicated substring in the worker handoff by hashing the actual file. The 209-file relevant tree is `b085f3e753f97d8c3d0830cf15c7644f64d30aa87515eef9e43c3ccdddce7d9d`. All three test write turns are consumed; the separate Green chain has not started. All 430 protected files remain unchanged and no successor root or real request exists.

### M602-SUC-GREEN-01 — Successor implementation packet

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-GREEN-01
- Lease ID: m602-suc-green-01
- Phase: green
- Attempt: 1
- Correction parent lease ID: None
- Worker role: code_worker
- Lease owner: m602-suc-code-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-FUP-FINAL-01 baseline, final-artifact cycle2 PASS, M602-SUC-RED-ACCEPT-01. Primary reproduces first-module Red (143 executed unaffected passes, two blocked imports) and strict errors only for planned missing modules/formatter. All blocked new and legacy assertions must execute during Green.
- Accepted test boundary and current test owner: M602-SUC-RED-ACCEPT-01, independent m602-suc-test-worker. tests/m602-generation.test.ts SHA256 4c0b1e9d35f4e72c5bd8dc37048e3266e5a37205606f2428980abf482be9b184; tests/m602-successor.test.ts SHA256 8b3fedfeb72cc6533780e512452a99d78234bd997196f5fa7fef5d7d1ca63ac2. Both remain frozen throughout Green; test budget exhausted.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Implement the complete reviewed contract within its five cohesive owners. Report any test defect or binding conflict without editing tests or weakening behavior. Live observer feasibility and provider outcome remain downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files: tests/helpers/m602-operation.ts, tests/helpers/m602-run-case.ts, tests/helpers/m602-successor-observers.ts, tests/helpers/m602-successor-evidence.ts, evaluation/m602-successor-v1.json.
- Allowed directory roots: None.
- Forbidden files: tests/m602-generation.test.ts, tests/m602-successor.test.ts, README.md, package.json, package-lock.json, tsconfig.json. All other paths remain outside the allowlist.
- Forbidden directory roots: src, docs, corpus, fixtures, .codex, .agents and data.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01, followed by independent strict TypeScript. Every formerly blocked assertion must execute and pass unchanged. If accepted Red identity has drifted, reproduce Red before implementation; otherwise reuse it. Never create a stub solely for Red replay.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Behavioral Green for all S1–S6 and CLI formatter assertions, all unaffected tests passing and independent strict TypeScript passing. Record RETAINED, REFACTORED or RECONCILE with actual responsibility and dependency evidence; tests alone do not establish cohesion.
- Relevant-tree fingerprint: Current 209-file source/test/config SHA256 b085f3e753f97d8c3d0830cf15c7644f64d30aa87515eef9e43c3ccdddce7d9d; HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Green write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: At most two Green write corrections, third total turn conditional; no budget renewal by new IDs. This turn is initial Green attempt1 only. Test corrections remain owned by the test worker under their existing chain.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent behavior then focused tests and strict checking. Broaden or repeat only for changed evidence, failure or unresolved concern. Inspect actual structural fit before handoff.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Preserve all original public exports, defaults, record versions and tests. Use the one private shared core and one timing coordinator, with separate observer and successor evidence owners as the contract specifies. Materialize the exact 1175-byte reviewed LF manifest (SHA256 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276). Do not create the real successor root or invoke actual resources, browser, runtime, GPU, credentials or provider. Only complete synthetic doubles in the focused tests are authorized. Never repair a failing fixture, bypass a mandatory field, weaken a validator or insert a default-resource fallback to pass tests. Failure/cleanup control receipts, exclusive publication and current-code identity are acceptance-critical. Primary owns docs, actual-diff inspection, independent strict, S3 and later full-suite/live gates. You are not alone; preserve all existing edits and every protected record. All Git mutations are prohibited.
```

### M602-SUC-GREEN-01-TRIAGE — Initial implementation correction boundary

Initial Green closed compliant with digest `1b82b115b3a36bd7e97faeaa26f1773bdb31cf30fee7525b3d21162060d58b2b` and receipt `faa3aa599eee723d494441f32983297078555a9df07756912cf1d74e0dacb9d3`, affecting only its five allowed files. Primary independently reproduced 216 executed focused tests: 213 pass and three fail, with no skipped or cancelled tests. Strict checking exposes four implementation errors and one test-double typing error. This is not accepted Green.

The implementation correction owns common result version typing, sample inference, Playwright event overloads, the browser cleanup race after pending acquisition settlement, and cancellation/owned-native-resource closure under the existing contract. A bounded independent fixture audit confirms three test-only defects: the partial synthetic observer I/O needs an explicit type-boundary cast; its response-end listener emits close before the production end handler; and its baseline-abort test advances the cleanup deadline before resolved cleanup continuations run. The page-fault cleanup failure remains implementation-owned. All assertions and timeouts remain unchanged. The exhausted three-turn test chain requires an additional owner exception for those three fixture corrections; approval is pending, no test edit or new test lease is authorized. The ordinary Green correction remains available for unaffected implementation work. Existing original and follow-up chains and real-operation allowances remain unchanged.

### M602-SUC-GREEN-02 — Bounded implementation correction packet

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-GREEN-02
- Lease ID: m602-suc-green-02
- Phase: green
- Attempt: 2
- Correction parent lease ID: m602-suc-green-01
- Worker role: code_worker
- Lease owner: m602-suc-code-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-SUC-GREEN-01-TRIAGE below. Initial Green closed compliant, all 216 focused tests execute: 213 pass, three fail. Strict reports four implementation errors and one test-double typing error. Original Red evidence is historical; current unchanged test boundary and primary reproduction govern correction. Three owner-requested test fixture exceptions remain pending; their known failures are explicitly excluded from implementation acceptance and must not be worked around.
- Accepted test boundary and current test owner: M602-SUC-RED-ACCEPT-01, independent m602-suc-test-worker. tests/m602-generation.test.ts SHA256 4c0b1e9d35f4e72c5bd8dc37048e3266e5a37205606f2428980abf482be9b184; tests/m602-successor.test.ts SHA256 8b3fedfeb72cc6533780e512452a99d78234bd997196f5fa7fef5d7d1ca63ac2. Both remain frozen throughout Green; test budget exhausted.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Implement the complete reviewed contract within its five cohesive owners. Report any test defect or binding conflict without editing tests or weakening behavior. Live observer feasibility and provider outcome remain downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files: tests/helpers/m602-operation.ts, tests/helpers/m602-run-case.ts, tests/helpers/m602-successor-observers.ts, tests/helpers/m602-successor-evidence.ts, evaluation/m602-successor-v1.json.
- Allowed directory roots: None.
- Forbidden files: tests/m602-generation.test.ts, tests/m602-successor.test.ts, README.md, package.json, package-lock.json, tsconfig.json. All other paths remain outside the allowlist.
- Forbidden directory roots: src, docs, corpus, fixtures, .codex, .agents and data.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01, followed by independent strict TypeScript. Every formerly blocked assertion must execute and pass unchanged. If accepted Red identity has drifted, reproduce Red before implementation; otherwise reuse it. Never create a stub solely for Red replay.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Behavioral Green for all S1–S6 and CLI formatter assertions, all unaffected tests passing and independent strict TypeScript passing. Record RETAINED, REFACTORED or RECONCILE with actual responsibility and dependency evidence; tests alone do not establish cohesion.
- Relevant-tree fingerprint: Green01 terminal verified repository snapshot b0e54761b822c51077c4f2e43b47db947a05b59a1490131a9f7aa8ae2f60d540, confirmed without post-close drift before primary documentation maintenance; exact new guard baseline captures documentation updates. Frozen tests retain the above hashes. HEAD b8b9141e85ead2027e8eaf4bf59d33b1a3a20462 and lock 38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d remain unchanged.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Green write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: This is the already authorized ordinary Green correction, attempt2 of at most3; conditional third requires material evidence and recorded justification. Test chain is exhausted; pending owner exception does not authorize worker test edits or another test turn. No chain is renewed.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent behavior then focused tests and strict checking. Broaden or repeat only for changed evidence, failure or unresolved concern. Inspect actual structural fit before handoff.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Preserve all original public exports, defaults, record versions and tests. Use the one private shared core and one timing coordinator, with separate observer and successor evidence owners as the contract specifies. Materialize the exact 1175-byte reviewed LF manifest (SHA256 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276). Do not create the real successor root or invoke actual resources, browser, runtime, GPU, credentials or provider. Only complete synthetic doubles in the focused tests are authorized. Never repair a failing fixture, bypass a mandatory field, weaken a validator or insert a default-resource fallback to pass tests. Failure/cleanup control receipts, exclusive publication and current-code identity are acceptance-critical. Primary owns docs, actual-diff inspection, independent strict, S3 and later full-suite/live gates. You are not alone; preserve all existing edits and every protected record. All Git mutations are prohibited.
Primary correction scope: Fix only implementation-owned strict errors: private common result version typing in operation (including downstream successor cast), concrete sample annotation in evidence, and native Playwright event overloads in observers. Correct browser cleanup so a confirmed closed browser/context is marked complete after all owned acquisitions settle, while pending or failed closes remain uncertain; page-fault cleanup test currently exposes this race. Forward linked cancellation to supported Playwright APIs and check cancellation before each subsequent awaited primitive, preserving the single total deadline. Inspect runtime HTTP ownership against the existing native transport pattern: own request/response/socket close, explicitly close owned sockets and keep cancellation effective until owned cleanup settles, without new primitives, retries or deadline extension. Close events, not destroyed flags, prove cleanup. Retain exact public IO types. Do not change any test, assertion, timeout, manifest or contract. Known fixture-only issues (partial observerIO cast, synthetic response-end/close order, and missing microtask flush before cleanup deadline) await owner authorization and must be reported unchanged rather than compensated for in production. Run focused once after coherent corrections and strict separately even if known fixtures still fail, then stop. No actual resources or generation. Responsibility disposition required; no silent additional correction loop.

```

### M602-SUC-GREEN-02-RESULT — Fixture approval boundary

Ordinary Green correction closed compliant with digest `b9af2a3f1a64473eb685c58cd1298ee1294a09493ee5a5b01241c284a7261436` and receipt `5f22f4fab6fd5030c2489c4209a098d63a598df27489ea88f19265faedef54be`. Only operation, successor evidence and successor observer helpers changed. Primary inspected the actual typing, cancellation forwarding, queued-stage admission, acquisition/cleanup settlement and native request/response/socket closure changes. The CLI explicitly projects failure and cleanup fields and uses the formatter in both required failure branches with exit status 1.

The worker's maintained nine-file command executes all 216 tests: 214 pass, two known fixture failures remain, and no test is skipped or cancelled. The page-fault cleanup regression and all seven successor integration groups pass. Primary independently reproduces strict checking with only the known test-double TS2322 at generation test line 1404; all four implementation diagnostics are resolved. Frozen tests retain both M602-SUC-RED-ACCEPT-01 hashes. Implementation hashes are operation `eff7b571b3607c919c9273218856469ab09e9672c8725e6b14f54be1205e3d79`, evidence `d5299f3addf9631a68382ff9003b93ca410d76e7aa70d3066d643c1afecc9ca8`, and observers `4a9652e8a4b902d06deb3ae2e67cadc1a1cc93f997c5f6e68388990b3609378d`.

This is a supported partial correction, not accepted Green or S3 acceptance. The three exact fixture exceptions in M602-SUC-GREEN-01-TRIAGE remain pending. No lease is active. Green attempt2 is consumed; a conditional third is not authorized by this result without its required evidence and justification. No further test-worker turn is available. If the owner authorizes the three primary test corrections, apply only those changes between leases, invalidate previous test evidence, run focused tests and independent strict checking, and re-accept the changed test boundary before any later Green lease. Passing evidence then permits S3 and task-level verification; no new implementation turn is automatically necessary.

Primary reconfirmed all 430 protected hashes and the three original record identities. The successor root remains absent, synthetic scratch is removed, and no actual qualification or generation occurred. Documentation impact is confined to current-state/evidence summaries and the maintained forty-nine-file suite description; requirements, ADRs, frozen packages, roadmap status and later tasks are unchanged. All 13 PowerShell blocks parse and `git diff --check` passes; local navigation is checked at handoff. The original failed campaign remains preserved.

### M602-SUC-TEST-EXCEPTION-01 — Authorized bounded fixture correction

The owner explicitly authorizes the three proposed test-only corrections on 2026-09-21. Both Green leases are terminal compliant and no worker lease is active. The primary may change only the synthetic observer I/O cast in tests/m602-generation.test.ts, defer the response-end fixture's close emissions with queueMicrotask in tests/m602-successor.test.ts, and flush asynchronous cleanup after baseline cancellation before advancing that test's mock deadline. Every assertion and timeout remains unchanged. Prior test evidence is invalidated by these edits; fresh focused and independent strict validation must establish the revised boundary before further Green or review. This exception grants no new test-worker turn, implementation budget, retry or generation allowance.

### M602-SUC-GREEN-ACCEPT-01 — Corrected tests and implementation accepted for S3

The three authorized primary edits are complete with no active lease, no changed assertion or timeout, and the original 71,335-byte generation-test prefix unchanged (SHA-256 c05bc206ba2bb8a5966e2d74217f45ac304abf277d2cdffe2515b2ed536c1d18). The maintained nine-file focused command passes all 216 tests without skips or cancellations, followed by passing independent strict TypeScript. The revised frozen test hashes are generation `feb3720101bd97e73f6cce275d6101aa507e115d412a3c3e82aa0974a00d7705` and successor `d880ad6a23e971d3c23e21899c5620a5bb33aade2793e96df8340210e0bd2143`. Earlier test evidence is superseded. The current 211-file source/test/config fingerprint is `046edc8d827c08fd193bfe5194696905b48d0fcedf07bef64f2c3809a5143e57`, using sorted relative paths, each followed by NUL and its raw SHA-256, joined by LF and hashed as UTF-8; HEAD, lock, environment and command remain as recorded in Green02.

Primary accepts the revised test boundary and behavioral Green without another implementation write turn. Cohesion is RETAINED: operation coordinates the private common execution and evidence dependencies; CLI owns argument modes and closed output projection; successor evidence owns literal records and validation; observer helper owns actual primitive provisioning, sampling and cleanup. The evidence module's operation imports are type-only and introduce no runtime cycle. No further extraction or refactor is needed for the current contract. This is implementation acceptance for review, not S3, live qualification or task completion.

Fresh S3 critical review covers all five implementation paths, both test boundaries and the complete S1–S6 contract, including cancellation/late acquisition, owned close proof, frozen timing, exclusive publication, strict successor/original separation and semantic assessment. It may reuse this fresh deterministic evidence; risk-critical reproduction must use existing isolated doubles, never actual qualification, credentials or generation. The primary concurrently owns the maintained forty-nine-file full suite and subsequent strict/build/preservation checks. One bounded review correction loop remains; the conditional final Green turn is still subject to its evidence gate. Different final integration review remains required after actual execution or a truthful stopped outcome.

### M602-SUC-REGRESSION-01 — Complete current baseline verification

The maintained forty-nine-file sequential suite passes all 978 tests, with zero failures, skips or cancellations, at the M602-SUC-GREEN-ACCEPT-01 identity. The independent strict check is passing. A fresh production Vite build passes after inspection of the exact generated inventory; all three pinned output hashes are unchanged, so the suite's generated-asset consumer evidence remains applicable. Exact-original historical inspection passes with eligibleForContinuation false, all 430 protected files and three original record hashes match, all managed scratch is empty and no synthetic root remains. No successor root, qualification or generation exists.

Initial S3 review is still in progress and has identified a supported cleanup-port socket ownership concern for bounded triage. This complete regression does not override review findings. Any subsequent executable correction invalidates affected evidence and still requires the contracted post-change verification before live effects.

### M602-SUC-S3-01 — Initial critical review and correction disposition

Fresh critical implementation review returns REVISE with three Major findings, no Blocker or Minor, and RETAINED cohesion. S1/S2/S6 have no finding; S3/S4 require the three cleanup corrections, and F2 also affects S5 error privacy. F1: closedPort does not cancel its owned socket at the common cleanup deadline and rejects the connected path before actual close. F2: gpuSample lacks stdout/stderr error ownership, allowing a native pipe error to escape the bounded effect. F3: closeBrowser skips browser.close when context.close rejects. No broader identity, publication, assessment, compatibility or structural change is indicated.

The primary independently reproduces all three intended failing assertions through the reviewer's exact in-memory extraction commands under maintained preparation, Node v24.20.0. F1 observes application uncertain, destroyCalls 0 and one retained close listener after 5000 ms; F2 observes zero error listeners on both GPU pipes and an escaping synthetic stdout error; F3 observes contextCloses 1 and browserCloses 0. Each exits 1 at its intended assertion and disposes its synthetic resources. These read-only review reproductions introduce no tracked test file, actual primitive or written test-boundary change; the reviewer remains their independent evidence owner.

Primary accepts all three findings within the existing observer contract. Green02 made material progress and achieved 216 focused tests, strict and 978-test regression; the new independently reproduced failure-exit evidence justifies the remaining conditional Green03 turn. Its changed bounded action corrects all three ownership gaps together in the existing observer helper. No test or original/follow-up chain is renewed. After the final write turn, primary verifies the unchanged tests and probes, then requests the sole S3 correction review. Complete post-change task verification remains mandatory before any actual qualification.

### M602-SUC-GREEN-03 — Final conditional cleanup correction packet

```text
Milestone Assignment Packet v2

Identity
- Workflow ID: m602-six-fixed-generation-executions
- Roadmap task ID: M6-02
- Work-slice ID: SUC-fixed-campaign-observers
- Assignment ID: M602-SUC-GREEN-03
- Lease ID: m602-suc-green-03
- Phase: green
- Attempt: 3
- Correction parent lease ID: m602-suc-green-02
- Worker role: code_worker
- Lease owner: m602-suc-code-worker
- Guard contract digest: INSERT_AFTER_START

Work-slice capsule
- Owning ExecPlan: docs/plans/m6-02-six-fixed-generation-executions.md, M602-SUCCESSOR-01 and complete M602-SUC-CONTRACT-01.
- Observable acceptance contract: Exact successor manifest/campaign identity with unchanged M301 packages and original exports; exclusive one-entry/dispatch, strict qualification and predecessor/assessment gates; genuine bounded observer primitive lifecycle and receipt propagation, isolated dependency seams, closed durable readback; all S1–S6 obligations in contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Owning plan #authorities-and-prerequisite-evidence resolves Accepted REQ-EVAL-001/002/004–009, evaluation authority #freeze-boundary, #controlled-generation-input-exception and #groq-admission-amendment-to-the-frozen-generation-definition; docs/DEVELOPMENT_ROADMAP.md#m6-02--execute-exactly-six-fixed-generation-cases Verification; ADR-0003/0004/0014 and ADR-0024; BHV-03/04, eligible SPEC-003, eligible/no-mixing/no-fallback SPEC-004 and HS-008/009, HS-015. No requirements amendment.
- Readiness and evaluation-freeze evidence: M6-01 Complete, M2-02/M3-03 capacity gates passed, immutable M301 and 430 protected hashes reproduced by final reviewer, owner-authorized separate successor scope. Exact successor manifest hash 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276 is frozen by contract and materialized only by Green; no live output before gates. Final research correction-cycle-1 PASS accepted by primary; artifact reviewed at 73704840b9a8d802ae834e070a11f29e529e2946e4338f2bfd5f9ef7b0dd5d3d.
- TDD applicability: Applicable — new executable operation/evidence/observer behavior. First-module exception may use absent tests/helpers/m602-successor-observers.ts after verified existing runner readiness; no fake behavior coverage.
- Current-state and preflight evidence IDs: M602-SUC-GREEN-ACCEPT-01 and M602-SUC-REGRESSION-01; all216focused,978full49files,strict/build pass. Initial S3 findings and exact independent reproductions will be reconciled below before this packet is dispatched.
- Accepted test boundary and current test owner: Separate test-worker boundary revised only through the completed three-change primary owner exception. generation SHA256 feb3720101bd97e73f6cce275d6101aa507e115d412a3c3e82aa0974a00d7705; successor SHA256 d880ad6a23e971d3c23e21899c5620a5bb33aade2793e96df8340210e0bd2143. Frozen throughout Green. No more test edits or turns authorized. Reproduction is independently owned review evidence; implementation worker must not author or alter it.
- Relevant boundaries and paths: Existing tests/helpers/m602-operation.ts and m602-run-case.ts; new m602-successor-observers.ts and m602-successor-evidence.ts; new evaluation/m602-successor-v1.json. Test-owned tests/m602-generation.test.ts (append only) and new tests/m602-successor.test.ts.
- Production responsibility placement and fit: None — no application-source responsibility changes. Evaluation operation owns private shared execution and source-record coordination; CLI owns strict argument modes and closed projections; observer owner owns provisioning, collectors and owned cleanup; evidence owner owns successor manifest/records/projection validation. These distinct current responsibilities must remain cohesive.
- Dependency, runtime-call, or interface-edge changes: Evaluation operation -> new evidence/observer owners -> existing package, filesystem, coordinator, startLocalService, Playwright and native primitive seams. No production-to-evaluation import or runtime circular edge. Default actual factories private; any supplied dependency object permanently selects complete isolated doubles.
- Reuse, justified separation, bounded creation, or local-extraction disposition: Reuse adapters/package/timing/filesystem/historical code unchanged. Create exactly two cohesive helper owners and literal manifest. Existing original/ successor operation wrappers share one private core.
- Permitted local structural refactor: Private extraction inside m602-operation.ts and closed two-version validation; original public behavior unchanged. No executable extraction into another file, accepted test edits during Green, or shared-helper modifications.
- Non-goals: All original exhausted chains; old evidence repair/retry; live resources, native runtime/GPU/browser/provider requests or credentials during tests; app source/UI changes; model/prompt/config/schema changes; generalized framework; dependencies; Git writes; requirements/status changes.
- Named uncertainties: Implement the complete reviewed contract within its five cohesive owners. Report any test defect or binding conflict without editing tests or weakening behavior. Live observer feasibility and provider outcome remain downstream.
- Risk tier: S3
- Review and escalation triggers: Single-use entry, immutable evidence, cancellation/concurrency/late resources, partial publication and cleanup. Fresh critical_reviewer after Green, different critical final integration review. Stop for any binding conflict.

Write scope
- Allowed files: tests/helpers/m602-operation.ts, tests/helpers/m602-run-case.ts, tests/helpers/m602-successor-observers.ts, tests/helpers/m602-successor-evidence.ts, evaluation/m602-successor-v1.json.
- Allowed directory roots: None.
- Forbidden files: tests/m602-generation.test.ts, tests/m602-successor.test.ts, README.md, package.json, package-lock.json, tsconfig.json. All other paths remain outside the allowlist.
- Forbidden directory roots: src, docs, corpus, fixtures, .codex, .agents and data.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: Exact maintained-preparation nine-file focused command in M602-SUC-PREFLIGHT-01, followed by independent strict TypeScript. Every formerly blocked assertion must execute and pass unchanged. If accepted Red identity has drifted, reproduce Red before implementation; otherwise reuse it. Never create a stub solely for Red replay.
- Task-level command: Primary-owned maintained README complete suite with tests/m602-successor.test.ts added after Green, independent strict/build, exact-original historical command and 430-file preservation, followed by affected generated-asset consumers if build changes. Not run by preflight.
- Expected decisive result and reusable evidence IDs: Behavioral Green for all S1–S6 and CLI formatter assertions, all unaffected tests passing and independent strict TypeScript passing. Record RETAINED, REFACTORED or RECONCILE with actual responsibility and dependency evidence; tests alone do not establish cohesion.
- Relevant-tree fingerprint: Current211-file source/test/config SHA256 046edc8d827c08fd193bfe5194696905b48d0fcedf07bef64f2c3809a5143e57; algorithm in M602-SUC-GREEN-ACCEPT-01. HEADb8b9141e85ead2027e8eaf4bf59d33b1a3a20462; lock38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d.
- Environment fingerprint or Non-reusable: Windows PowerShell7.6.5, Nodev24.20.0 at C:/nvm4w/nodejs/node.exe, installed lock unchanged; maintained Invoke-M105Command preparation. Mutable external readiness Non-reusable and untested.
- Known external side effects and cleanup: Tests only isolated synthetic temp/m602-test-UUID with finally cleanup, no real fallback; never actual or original roots.

Budget and stopping
- Maximum worker turns: One initial Green write turn, then one ordinary correction and only conditional third with primary recorded ADR-0024 evidence.
- Maximum corrections: Conditional final Green turn, attempt3. Green02 materially resolved all216focused/strict errors;978fulltests/build pass after the completed owner fixture exception. Initial S3 independently reproduces three previously untested lifecycle faults using exact implementation closures and isolated in-memory doubles. New evidence identifies owned cleanup-port socket cancellation/close, GPU pipe-error ownership and independent browser shutdown after context-close rejection. The changed bounded action corrects these three named failure exits together within the same observer owner and acceptance contract, preserving all test assertions and public types. This is likely to close the demonstrated cleanup gaps and uses the existing conditional allowance. No fourth turn, sibling, budget renewal or extra review loop. Existing original/followup chains remain exhausted.
- Maximum repeated identical failure: Stop at second decisive identical failure without material new evidence.
- Maximum no-diff outcomes: Two write handoffs; not a target.
- Validation cadence: Read complete target before editing; implement the coherent behavior then focused tests and strict checking. Broaden or repeat only for changed evidence, failure or unresolved concern. Inspect actual structural fit before handoff.
- Stop and escalate when: UNKNOWN/CONFLICTING, changed binding/path/command/effect, absent prerequisite, real effect required, budget exhausted or outside responsibility. Do not silently fix or broaden.
Handoff
- Report identity/authority/lease digest, exact changed paths, command/results/exit, full S1–S6 test mapping including blocked assertions and fixture correctness, cleanup, residual risks, cohesion and documentation impact. You are not alone in the codebase; preserve others' edits. All Git mutations prohibited.

Primary reconciliation: Preserve all original public exports, defaults, record versions and tests. Use the one private shared core and one timing coordinator, with separate observer and successor evidence owners as the contract specifies. Materialize the exact 1175-byte reviewed LF manifest (SHA256 5ca8c1798647f3b949d016441f30d3c7d1f0055c4a55dabd574836d024513276). Do not create the real successor root or invoke actual resources, browser, runtime, GPU, credentials or provider. Only complete synthetic doubles in the focused tests are authorized. Never repair a failing fixture, bypass a mandatory field, weaken a validator or insert a default-resource fallback to pass tests. Failure/cleanup control receipts, exclusive publication and current-code identity are acceptance-critical. Primary owns docs, actual-diff inspection, independent strict, S3 and later full-suite/live gates. You are not alone; preserve all existing edits and every protected record. All Git mutations are prohibited.
Primary correction scope: Correct only the three primary-accepted initial S3 Major findings in tests/helpers/m602-successor-observers.ts, preserving all other files unless necessary under the identical path projection. F1: the owned TCP port-closure probe must observe the existing common cleanup deadline, destroy its pending socket at expiry, retain close/error ownership, wait for close rather than reject immediately on connect, and never initiate a new probe after cleanup expiry; do not add a second deadline or claim complete on destroy alone. F2: own stdout and stderr error events for the GPU child, fail/cancel the collector without an escaping event exception or raw disclosure, and preserve child/stdio close proof and bounded uncertain cleanup. F3: a rejected context.close must not prevent the ordinary browser.close attempt within the same cleanup group; preserve honest uncertain status for failed/unconfirmed closure and idempotence. No new test files, test edits, weakened assertions, public seam, deadline, manifest, provider call or budget change. Preserve all existing signals, late-resource ownership and no-next-stage gates. Primary accepts the independent review's failing in-memory closure probes as decisive Red for these corrections; the reviewer owns their risk-critical re-execution, with no changed written test boundary. Run the maintained nine-file focused command once after the coherent correction and strict separately; then inspect cohesion and STOP. No implicit corrective loop after handoff. Sole S3 correction review follows primary verification. You are not alone; preserve others' changes and all Git state.

```

### M602-SUC-GREEN-03-RESULT — Final cleanup correction verified for re-review

Final Green closes compliant with digest `74523d1b7c2e4177a02ffc2cee5d1781ff0df4b5a30cadf61373a89c82248f2a` and receipt `041d66c31e232d609a8a57064179a6ca89a451307fdb39f7cc3c5759d288148d`. Only the observer helper changes, to SHA-256 `905dfb2468e3884b2143563be06c648a034f57ad32ff40152297bce188d71ca4`; both accepted test hashes remain unchanged. All 216 focused tests pass without skips or cancellations, and primary independently reproduces passing strict TypeScript.

Primary inspects all three corrections and independently reruns the review probes: F1 now requests destruction once at the common deadline while retaining uncertain cleanup until close; F2 has one error listener on each pipe and no escaping stdout error; F3 attempts both context and browser closure once while preserving uncertainty. All three intended assertions now pass. The extraction-only F1/F3 harness now includes the existing source's `check` binding, newly used by closedPort; no behavior assertion or tracked test changes. Cohesion remains RETAINED in the existing observer owner. The primary accepts corrected Green for the sole S3 correction review. All three implementation write turns are now consumed; no further write correction is authorized by this acceptance.

The earlier 978-test suite is the pre-correction baseline. Repeat the mandated complete suite and final build/preservation checks on this frozen candidate while the same critical reviewer performs its single bounded correction review across F1–F3 and S1–S6. No actual qualification or generation may occur before both gates pass.

### M602-SUC-ACCEPT-01 — Complete S3 and prequalification acceptance

The sole S3 correction review returns PASS across S1–S6 with no remaining findings and RETAINED cohesion. Its ten independent in-memory adversarial checks cover all three corrections, both pipe errors, connected-probe close ordering, late service-stop settlement and immutable receipts. The final observer hash remains `905dfb2468e3884b2143563be06c648a034f57ad32ff40152297bce188d71ca4`. The final 211-file source/test/config identity is `57eba62d2b482adb04858dd06225ade85b3dbe169a21b89d4dd9b0c44aba48a2` under the recorded fingerprint algorithm; accepted test hashes are unchanged.

After the last executable change, all 978 tests in the maintained forty-nine-file suite pass with zero failures, skips or cancellations. Independent strict TypeScript and the fresh production build pass. The build reproduces all three accepted asset hashes, so no generated-consumer evidence is invalidated. All six default packages authenticate and reproduce their frozen wire identities without native requests. Exact-original inspection, all 430 protected files and the three original record pins pass; managed and synthetic scratch is empty. The successor root remains absent at this gate. Ordinary ancestry, ignore status, available managed browser/GPU executable and the parent's seven inherited ACL entries are inspected; ACLs are not changed and no owner-only privacy is claimed.

Primary accepts implementation and prequalification readiness. All three successor implementation turns and the sole S3 correction loop are consumed. The existing owner scope now permits creating only the literal ignored successor root and running the exact actual no-generation observer qualification command. Actual qualification must pass with complete cleanup before any case entry; each later case remains individually gated. No actual case has yet entered or dispatched. Different final integration review and documentation closure remain required after actual outcomes or a truthful stopped handoff.

### M602-SUC-QUALIFY-01 — Actual observer qualification accepted

After M602-SUC-ACCEPT-01, primary created only the literal ignored ordinary `temp/m602-successor-v1` root. Its seven inherited ACL entries match the parent; this is inheritance evidence, not an owner-only privacy claim. No case was precreated. The maintained preparation, exact revision environment restoration and browser scratch wrapper ran the actual qualification command once. It returned exit 0 and qualification SHA-256 `214dc07ae91e2ddccc480807f1816befe14b9f4bc82cac7265d0af2cc7d0e4a6`, qualified at `2026-09-21T16:43:48.337Z`.

Primary inspected the retained closed qualification record: successor/M301 hashes and all three build pins match; producer code is `505f637f95f77e4789f5c38d5f8dcca32635893b1a505c3981c07f708cd3e77a`, revision is `b8b9141e85ead2027e8eaf4bf59d33b1a3a20462`, Node is `v24.20.0`, and Chromium is `151.0.7922.34`. Actual UI and baseline GPU readiness passed. UI, GPU, application, browser and scratch cleanup are complete; runtime is not-created. Managed scratch is empty and revision environment restoration passed. This procedure made no model/runtime/credential request and consumed no generation entry. The first successor Local image case is now eligible for its single actual execution, subject to fresh per-case readiness; later cases remain gated by accepted predecessor evidence.

### M602-SUC-EXEC-01 — Successor Local image prerequisite failure and stop

After accepted actual qualification, primary invoked only `--execute-successor --case local-image`, once, through the maintained browser wrapper with exact revision restoration. Fresh app/browser/GPU readiness passed before exclusive entry at `2026-09-21T16:44:28.459Z`. The command returned a retained failed outcome and exit 1: `missing-prerequisite`, `attempted:false`, request counts version 1, show 0, tags 0, chat 0. It finished at `2026-09-21T16:44:28.578Z`. There is no dispatch marker, wire, native generation window, invocation observation or proposal. The entry allowance is consumed despite zero generation dispatches; no retry is permitted. Local label, Local contrast and all three Groq cases remain not-run, with no directories or requests.

Read-only successor readback passes with `accepted:false`. Retained record identities are:

| Record | SHA-256 |
| --- | --- |
| entered.json | f250bc68196c1d53a155d147f38e6ac95eaf76fc5de4bae94cc058f06cfa88f3 |
| result.json | e28f78108f4a73fc1270c4c84f8c2a0e9b8da3c61479f05720bbd95c92943632 |
| observation.json | 4f2d8603a4c1aa601500c670403d0f5fe7e263396173e14bcdeaf8c93b8d9ffe |

Diagnostic integrity is complete with null rejection code, consistent with no generated response. All three in-call slots are missed-window and their values are null; this is not evidence of in-call observer effectiveness. The baseline GPU sample is retained only in ignored evidence and cannot establish OOM absence or runtime readiness. Observer-owned UI, GPU, application, browser and scratch cleanup is complete, with runtime sampler not-created. Separately, the result has `cleanupFailed:true`: unchanged adapter preparation returned uncertain metadata transport cleanup. Observer cleanup does not clear that uncertainty. No further live effect is allowed before reconciliation, independently of the consumed-entry stop.

Source inspection narrows the failed stage to the first `/api/version` request. The closed `missing-prerequisite` code can represent a refused connection before response or HTTP 404, including an interrupted 404 response; the retained record does not distinguish them. It does not prove that Ollama is stopped, uninstalled, incorrectly configured or defective. No new runtime probe, process management, credential access, generation, repair or raw-response retention was performed to guess the cause. The bug index was consulted; this is currently an observed prerequisite failure without a confirmed repository defect, retained in this task rather than a speculative bug report.

Primary rechecked all 430 protected files and the three original failed-campaign records unchanged. Qualification and successor readback are separate from the original campaign, whose one failed generation dispatch and five stopped cases remain intact. No assessment is manufactured for the missing proposal, and the ten semantic dimensions remain unassessed. M6-02 Verification remains unmet: zero successor generation dispatches, one failed entry, five not-run. A different fresh critical integration reviewer audits this stopped handoff; task completion and archiving remain prohibited.

### M602-SUC-FINAL-01 — Different final review and stopped handoff

A different fresh critical reviewer returns PASS for the truthful stopped handoff only, with no Blocker, Major or Minor finding and RETAINED cohesion. Its S1–S6 and documentation audit independently reproduced the accepted 211-file fingerprint, implementation/test pins, qualification/build bindings and four actual successor record hashes; validated failed readback; rejected eleven in-memory qualification/observation mutations; and rechecked all 430 protected files, preserved original historical evidence, six compliant successor lease receipts, empty scratch and no active lease. Its read-only Node probe and `git diff --check` passed without any live resource or request. The unchanged executable identity supports reuse of the accepted 216 focused tests, 978-test regression, strict TypeScript and build evidence.

Primary accepts this review for evidence integrity and safe stopped handoff, not task completion. The reviewed uncertainty remains binding: metadata transport closure and the exact prerequisite cause are unproved; actual qualification is not an in-call observation; no successor proposal or semantic assessment exists. Original and successor entries remain consumed as recorded, and the five later cases in each campaign remain stopped. No additional live effect, retry, implicit new campaign or implementation write turn is authorized by this acceptance. Further live work requires native cleanup reconciliation and an explicit new scope decision; preparation must preserve all existing records and cannot replenish an entry.

Documentation impact: Updated the ExecPlan's current state, actual evidence, review disposition and outcomes; the roadmap's current checkpoint; README capability/status text; and the material progress narrative/index. Requirements, accepted ADRs, provider controls, frozen input packages and later task selection are unchanged. Thirteen maintained PowerShell blocks parse, local links/anchors resolve and `git diff --check` passes. M6-02 remains In progress and the plan remains unarchived because its Verification is unmet. No commit, push or publication occurred.

### M602-EXEC-ENTRY-01 — Execution activation and current baseline

On 2026-09-20, execution begins from clean HEAD `b8b9141e85ead2027e8eaf4bf59d33b1a3a20462`, with no active lease, M6-02 lease directory or M6-02 case result directory. Fourteen retained original run directories remain present. The original manifest and lock hashes above and all 13 recursive frozen references match. Fresh independent strict TypeScript and the 67 generation-contract tests pass using the unchanged maintained preparation and Node v24.20.0, without runtime/provider calls or credential access.

The exclusive ignored preservation inventory `temp/m602-entry.json`, SHA-256 `822d8a26a7b1207ee7d0a0d83b77d0a9e75cae87fe866e185a185babc4bbc462`, records 430 protected files across 33 exact roots, 126 source files, 70 test/helper files and three existing build files. It expands the preserved M6-01 roots with the earlier capacity evidence and all M6-01 result roots, including the failed G1. Every traversed endpoint and ancestor is ordinary. It contains paths and hashes only and is not case evidence.

Read-only process inspection finds developer-owned Ollama PID 15156 at the retained v0.33.3 executable, and `netstat -ano -p tcp` shows its 127.0.0.1:11434 listener. This does not establish model readiness; adapter metadata belongs to each bound actual operation. `Get-NetTCPConnection` is unavailable in the current sandbox (access denied); no process or runtime was changed. No historical startup or call allowance is replayed.

G uses the existing R3 Decision Review Contract and G1–G6 invariant packet. The initial critical-research assignment covers coupled frozen identity, common validation, attempt accounting, privacy and recovery; the primary owns authority, prerequisites, command binding and authoritative writing. Account-specific non-secret Groq limits/availability remain pending owner information. No public model listing is treated as account readiness. All six operation entries and dispatch ceilings remain unused.

### M602-ENTRY-01 — Planning evidence

On 2026-09-20, clean HEAD `1554cea3c0558a3c3c52e201ad24e4a2ea45b42d` includes completed M6-01. Primary reviewed current authorities, roadmap, capacity/freeze/prior-provider evidence, specification boundaries and maintained commands. One read-only explorer mapped source/test seams and compared frozen schema/instruction identities; no mechanism was selected or implemented.

Fresh independent strict TypeScript and **67 generation-contract tests** pass with zero failures/skips. All 13 M3-01 file references and the original manifest/lock hashes match. Fourteen retained run directories exist, and no active lease is present. Public Groq documentation checks are linked above; no credential, account request, local runtime request, browser or actual model call occurred. The historical 883-test closure is not a freshly executed complete suite in this planning turn.

### M602-PLAN-REVIEW-01 — Planning readiness and documentation

On 2026-09-20, a fresh read-only `critical_reviewer` returned **PASS**, with no Blocker, Major or Minor finding and no missing mandatory planning requirement. It inspected the complete plan, applicable authorities and source/worker boundaries, including exact frozen messages, controlled support, validation, six-operation accounting, privacy, recovery, R3 routing, ownership and YAGNI. It confirmed only the six expected planning/status/navigation documents changed. It made no edits, ran no tests and accessed no credential, runtime or provider.

Primary accepted this as readiness to begin G after owner execution authorization, not acceptance of the unresolved mechanism or permission to start a worker lease or actual case. G must freeze the binding and commands; A must qualify them. Current runtime/account readiness, original outputs and per-call Local usability remain unverified.

Proportional checks pass across the six changed documents: local files/anchors, all seven PowerShell blocks, all sixteen required plan sections, UTF-8 without BOM, final newlines, trailing whitespace, status consistency and `git diff --check`, including direct inspection of the two untracked documents. Twenty-five tasks remain Complete, M6-02 alone is In progress, and M6-03/M6-04 remain Not started. Documentation impact is limited to this plan, its progress record, roadmap planning activation, plan/progress indexes and the README status summary; no requirement, ADR, product contract or scope changed. No implementation or actual-call result is claimed.

## Interfaces and Dependencies

Existing boundaries include `generation-input.ts`, `generation-stage.ts`, `proposal-contract.ts`, `generation-contract.ts`, the two adapter factories, their fit/model/HTTP modules, canonical corpus/support helpers and the frozen manifest. The [M3-01 static verification precedent](completed/m3-01-generation-evaluation-package.md#m301-command-02--resolved-preparation-validation-and-closure-callers) is read-only reference material, not replay authorization.

No new dependency or product API is selected. Accepted M602-G-CONTRACT-01 selects minimal shared candidate/execution extraction with a bounded evaluation caller; implementation qualification is accepted in M602-A-ACCEPT-01. Existing production eligibility, persisted invocation allowlists and aggregate structure stay intact. The caller may produce task-local evaluation evidence; it must not assert a fabricated production run. Every prospective source change declares its exact responsibility, dependency direction, reuse/extraction rationale and test/implementation ownership in the existing assignment packet before coding.

## Revision Note

2026-09-20: Created for owner-selected M6-02 planning after completed M6-01. The plan preserves six immutable cases, names the frozen/runtime identity and validation gap, assigns bounded R3 contract resolution before writes/calls, and excludes retries, model changes and later tasks. Execution remains unauthorized until separately requested.

2026-09-20: Recorded independent critical planning-readiness PASS with no findings and proportional documentation closure. G's unresolved contract remains an explicit future gate; no execution result or allowance was opened.

2026-09-20: Activated owner-authorized execution from the actual clean baseline and recorded fresh offline entry evidence. Research and implementation gates remain mandatory before any fixed operation; no allowance was consumed.

2026-09-20: Authored M602-G-CONTRACT-01 after completed bounded research, analyst DRAFT READY and fresh pre-draft PASS. Exact interfaces, paths, record/command bindings and proof requirements await different final research review before A.

2026-09-20: Accepted revised G, recorded three compliant test leases and rejected final Red on reproduced fixture defects. Marked the task Blocked pending owner disposition of the exhausted test budget; preserved all six unused actual-operation allowances and completed proportional handoff documentation checks.

2026-09-20: Completed the separately authorized offline diagnostic/observation follow-up after bounded R3, guarded test/implementation ownership, corrected browser compatibility, full regression/build and different final integration acceptance. Preserved the original failed operation and five stopped cases; M6-02 remains In progress and no further generation is authorized.

2026-09-21: Recorded owner authorization and the living R3 contract for a separate bounded successor campaign. Preserved the original failure and all prior budgets. Real execution remains gated on the successor contract, qualified actual observers, implementation review and execution-time prerequisites.


2026-09-21: Completed the authorized fixture corrections and final successor implementation review/verification. Actual observer qualification passed; the first successor entry failed prerequisite admission without generation and stopped the sequence. Different final critical review accepts the truthful stopped handoff, with metadata cleanup uncertainty preserved and task Verification still unmet.
