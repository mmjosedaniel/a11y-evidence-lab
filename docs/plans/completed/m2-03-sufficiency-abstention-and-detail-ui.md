# Add deterministic sufficiency, abstention, and Finding detail

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This completed ExecPlan preserves original M2-03 execution, closure, reopening and correction history under `PLANS.md`. Current state and M203-C-POST01 record renewed closure; earlier grants remain consumed.

## Current state

- **Task:** [M2-03](../../DEVELOPMENT_ROADMAP.md#m2-03--apply-support-states-abstention-and-finding-detail-presentation), Complete again on 2026-09-09 after the approved correction.
- **Accepted renewed closure:** [M203-C-POST01-CLOSURE](#m203-c-post01-closure--renewed-task-closure) resolves both production findings and the test-only interception blocker: all 448 tests, strict/build, bounded visual evidence and fresh independent PASS.
- **Allowance / lease:** Both worker writes and the owner-directed narrow primary test correction are consumed. Active lease: None. Owned browser/service closed and scratch empty.
- **Next action:** None in M2-03. M2-04 and later tasks remain Not started. Original closure/reopening and blocked review entries below are historical, superseded by renewed closure.
- **Preserved limits:** Manual screen-reader testing remains excluded; detailed visual 200% remains deferred to M6-03. C-REAL is a prior-client observation with unchanged backend/corpus provenance, not a replay or a match against the current client's 94-file manifest. The unused supported branch of its consumed helper remains restricted. No generation, model/runtime changes, commit or push.

## Progress

- [x] (2026-09-08 22:48Z) Reviewed roadmap-wide status, current architecture and developer boundary, M2-02 closure/capacity qualifications, frozen inputs, relevant application/test surfaces, and task authorities; see M203-PLAN-01.
- [x] (2026-09-08 22:48Z) Independently passed strict TypeScript and 93 pure run, scan-normalization, and retrieval-contract tests on the unchanged application tree.
- [x] (2026-09-08 23:02Z) Passed plan-only validation and fresh critical readiness review with no findings; accepted M203-PLAN-REVIEW-01 and reconciled the five planning documents.
- [x] (2026-09-08 23:43Z) Received execution authorization, reconciled M203-ENTRY-01, and freshly passed C1: Node 24.20.0, strict TypeScript and 93 pure tests (zero failed/skipped/todo).
- [x] (2026-09-09 00:08Z) Froze D1–D4/shared D5 after mandatory DRAFT READY and two distinct research-review PASS results without findings; prepared and syntax-checked C-A. Remaining presentation literals precede C.
- [x] (2026-09-09) Accepted slice A: 33 focused tests, independent strict TypeScript, compliant leases and fresh S3 PASS without findings.
- [x] (2026-09-09) Accepted slice B: 246 focused tests, independent strict TypeScript, compliant Green lease and fresh S3 PASS without findings.
- [x] (2026-09-09) Accepted slice C implementation and helper review after resolving C-R1/C-R2; manual/actual integration proof remains a task gate.
- [x] (2026-09-09) Passed full 448 regression, independent strict/build and the single actual UI-to-retrieval-to-disk proof; owned cleanup verified.
- [x] (2026-09-09) Owner limited manual verification to visual elements, excluded manual screen-reader testing, and deferred full-guidance/detail readability and reflow at 200% to M6-03; observed form evidence does not establish the full visual path.
- [x] (2026-09-09) Accepted different fresh final critical PASS WITH FOLLOW-UPS and the documentation gate; M2-03 is Complete. Archived this plan and verified repaired navigation.

- [x] (2026-09-09) Resolved the two post-closure production findings and directed test-only correction; accepted 448 passing tests, strict/build, actual correction-region visuals and fresh independent PASS, then renewed documentation closure.

## Surprises & Discoveries

- Successful M2-02 retrieval deliberately retains `state: 'active'` and its service owner. `createRetrievalOperation` rejects further activations while that owner exists. Support and terminal decisions must extend this live handoff without introducing automatic resume or silently releasing a supported unfinished workflow.
- `readStoredFinding` permits only unprocessed, active retrieval, and failed retrieval combinations. The repository transition accepts only activation and retrieval completion/failure. Adding an abstention requires a precise new validated transition; a general completed-run rewrite would discard existing guarantees.
- The corpus manifest declares three required roles per profile (`criterion`, `interpretation`, `remediation`) and an empty `unresolvedConflicts` array. An empty production declaration does not excuse omitting conflict-policy tests, nor authorize changing the frozen corpus to create them.
- `ResultDetail` currently renders only `RuleEvidence`; `main.tsx` posts only Analyze. No retrieval HTTP route, citation projection, support policy, or abstention UI exists. The selected evidence layout already preserves card focus and needs no new screen or navigation framework.
- M2-01 source notices explicitly require a presentation review when excerpts are displayed. A citation URL by itself is not the complete accompanying notice presentation.

## Decision Log

- Decision: On 2026-09-09 the owner initially deferred remaining manual screen-reader and detailed zoom proof, then superseded the screen-reader obligation by limiting manual portfolio verification to visual elements. REQ-A11Y-006 and direct guidance mirrors now use automated accessibility/keyboard/focus/announcement checks and manual visual review. Full-guidance/detail readability and reflow at 200% remain deferred to M6-03. Expected accessible behavior is unchanged, and no unobserved visual or spoken result is claimed.

- Decision: Activate only M2-03 planning and keep all application effects pending an execution request. Rationale: the owner requested an ExecPlan; M2-02 completion satisfies the dependency but does not supply that grant. Date/Author: 2026-09-08 / primary.
- Decision: Use three cohesive behavioral slices and the existing tools, repository publisher, retrieval engine, and UI owners. Rationale: these are the current policy, durable-operation, and presentation responsibilities; extra services, setup stages, or per-component TDD cycles are unnecessary. Date/Author: 2026-09-08 / primary.
- Decision: Treat planning inspection as R0 and reserve R3 decision work for the unresolved execution literals. Rationale: accepted support meanings already control; planning neither ranks mechanisms nor selects new serialization, ownership, or failure semantics. One critical plan-readiness review addresses the proposed integrity/recovery boundaries. Date/Author: 2026-09-08 / primary.
- Decision: Execute only M2-03 under the subsequent owner grant; retain all earlier planning and M2-02 evidence as historical. Rationale: the actual entry is clean on the committed-plan branch, frozen inputs match, and no active lease exists. Use one related critical integrity research report while the primary verifies authority and commands. Date/Author: 2026-09-08 / primary.

## Outcomes & Retrospective

Execution entry, the literal barrier and slices A/B/C are accepted. Deterministic decisions, authenticated citations, strict additive outcomes, selected-only persistence, the fixed guidance API and rendered detail pass focused checks and fresh critical reviews. The actual UI/runtime/disk proof and full 448 regression pass. The owner excluded manual screen-reader testing from portfolio verification and deferred full-guidance/detail readability and reflow at 200% to M6-03. Original closure was followed by the owner-approved citation-presentation correction in M203-C-POST01. Renewed verification and independent review pass, and M2-03 is Complete again. The full M2 integration/gold check remains M2-04. Keep detailed results here and concise material outcomes in the [progress record](../../progress/m2-03-sufficiency-abstention-and-detail-ui.md).

## Purpose / Big Picture

After a complete scan, a user can select one Finding, explicitly request guidance, inspect its exact cited passages alongside the preserved scanner evidence, and understand the deterministic outcome. Complete evidence and `supported` guidance indicate eligibility for later generation. Incomplete evidence or completed insufficient/conflicting guidance produces a terminal application-authored abstention with blocking information, no-provider-call confirmation, and manual-investigation guidance. Retrieval execution or integrity failure remains a distinct failure with no support state.

Success is observable through the actual loopback UI and a validated `run.json` readback. No proposal, provider invocation, review decision, or generation action is needed to demonstrate this task.

## Context and Orientation

### Readiness and controlling authorities

Start with the [authority map](../../README.md#authority-and-status-map), [task router](../../README.md#read-by-task), and [requirements semantics](../../PROJECT_REQUIREMENTS.md). M2-02's [final closure](m2-02-embedding-retrieval-capacity-gate.md#m202-closure-01--final-integrated-verification-and-documentation-impact) and the [dated capacity observation](../../LOCAL_MVP_FEASIBILITY.md#m2-02-retrieval-only-observation--2026-09-08) satisfy the dependency. Preserve their limitations and consumed grants. All named task Must rows below are Accepted; directly applicable architecture records are Accepted at their stated MVP/evaluation scopes. Ordinary record, HTTP, copy, and test literals belong inside M2-03.

| Authority owner | Required scope |
| --- | --- |
| [Evidence and review workflow](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#corpus-and-retrieval) | Roadmap IDs `REQ-EVID-004`, `REQ-EVID-007`, `REQ-RETR-004`, `REQ-RETR-005`, `REQ-GEN-001`, `REQ-GEN-009`, `REQ-GEN-010`; support order and meanings. Also preserve `REQ-EVID-003`, `008`, `009`, `011`, `REQ-RETR-001`, `006`, `REQ-CORP-003`–`007`, and `REQ-UX-002`, `004`, `005`, `010`–`014` on affected boundaries. |
| [Information/lifecycle model](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#finding-lifecycle) and [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) | Stable run/Finding identity, one active workflow, immutable native/sibling evidence, terminal abstention/failure, failed-write truth, historical reads without resume, one aggregate and no child identities. |
| [ADR-0022](../../architecture/decisions/ADR-0022-closed-versioned-guidance-corpus.md), [manifest](../../../corpus/wcag22-mvp-v1/manifest.json), [passages](../../../corpus/wcag22-mvp-v1/passages.json), [gold mappings](../../../evaluation/m201-corpus-v1.json) | Exact versioned passages, rule/SC mapping, required roles, normative precedence, curator declarations, attribution and use notices. Gold expectations never become runtime ranking filters. |
| [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md) | `REQ-A11Y-001`–`004`, `010`, and preservation of `009`; bounded checks under `006`, not a support matrix. |
| [Privacy](../../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md) and [reliability](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md) | `REQ-SEC-005`, `012`, `027`; `REQ-QUAL-010`–`012`, `019`, `020`: unprivileged renderer, inert content, service validation, independent strict checking, ownership and last-valid persistence. |
| [ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md), [ADR-0015](../../architecture/decisions/ADR-0015-localhost-browser-mvp-execution.md), [UI contract](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md), [visual guidance](../../ui/VISUAL_FOUNDATIONS.md) | Existing React/loopback composition and OD-026/OD-027 presentation boundary. Preserve scan-only omissions; add only the selected downstream regions. |
| [Evaluation](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#derived-behavioral-scope), [SPEC](../../specs/SPEC.feature), [HARD_SPEC](../../specs/HARD_SPEC.feature) | BHV-02; abstention/failure portions of BHV-03; SPEC-002; `Abstain when evidence or guidance is insufficient` and `Fail retrieval without assigning a support state` in SPEC-003; no-call/abstention boundary of HS-008. Generation/context-fit execution remains M3. |

The [evaluation freeze](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#freeze-boundary) preserves RD-003's six scanner fixtures and M2-01's eight-source/16-passage/three-gold snapshot. New deterministic policy vectors must be frozen before their implementation/evaluation; they do not rewrite either manifest. M3-01 owns generation packages and M2-04 owns the full gold and shared abstention integration evaluation.

### M203-PLAN-01 — current project review

At planning entry, Git is clean at `acb58a4584c2ca67e36eafdf237c9763993725f5`, with 198 tracked files and no active write lease. Ten roadmap tasks are Complete, eighteen Not started, and none active before this selection. M1 supplies actual scan-to-disk/HTTP/React behavior; M2-01 supplies the closed corpus; M2-02 supplies actual local embedding/exact search and internal selected-Finding publication. Later generation, human review, comparison, and final evaluation are absent. The bug index contains no records. Proposed architecture assessments and Deferred features remain outside this work.

M2-02's 398-test/build result and 3846.0786-ms capacity observation are historical evidence. Its post-closure final-newline correction is separately proved and does not renew original hashes. This planning review freshly passed Node 24.20.0, strict TypeScript, and 93 tests from `run-contract`, `scan-normalization`, and `retrieval-contract`, with zero failed/skipped/todo cases using C1. It did not rerun browser suites, build, inference, or capacity. Model availability now is not inferred from historical success.

Current responsibility map: `src/server/retrieval/` owns fixed catalog/query, bounded loopback embedding, lazy collection and ranking. `local-service/retrieval-operation.ts` owns the selected operation; `service.ts` owns shared reservation and stop; `persistence/run-repository/` owns staged publication and transition checks; `domain/run-contract/` owns pure record validation. React's `App.tsx` coordinates the current run, selection and announcement; `ResultsSection`, `FindingsPanel`, `ResultCard`, `ResultDetail`, and `RuleEvidence` separate composition, list, selection and evidence. Tests use Node's runner, Playwright and existing controlled helpers; no new dependency is presently justified.

Preserve checkout fingerprints: manifest `87D8867ED1138BFB38E6C44EED67A79B0A43BDAF2532A056AFAFAED828202DA9`; passages `29EA0033E825A0FA20430AA42E9718B87A87AA59EDADDA0A3876A0A7BE871850`; gold `A7D38CACF98BF263CDD53012350708BE987699DF21B4C0F1BB7F32BC1892E29B`; RD-003 manifest `13C9722BE9EA2E3B0AAF020EA91F429A701180A83814FE7AB21BAF2DDAD57459`. These are checkout-byte identities, not substitutes for M2-02's normalized runtime corpus integrity. Investigate an intentional checkout conversion before deciding whether evidence remains applicable.

## Scope and Non-Goals

Own only deterministic support and evidence sufficiency, resolvable citations, the validated abstention branch, distinct retrieval failure, the required selected-Finding transitions/service API, and accessible detail UI. Selection remains evidence inspection; a separate explicit guidance action initiates the operation. Preserve all Findings and ScannerReviewObservations and their current grouping. Observations are never retrieval targets.

Exclude generation calls/adapters/probes, a Generate control, fake proposals, provider invocation records, review actions, automatic retry/resume, batch analysis, new Finding lifecycle aliases, run reopening, new model/corpus versions, ranking changes, thresholds, rerankers, source acquisition, migrations, persistent vector storage, and generic workflow or UI frameworks. Do not change the M2-02 finite query/input domain, model/runtime configuration, package pins, or frozen fixtures. Any demonstrated need beyond these boundaries returns to its authority before dependent work.

## Decision Review Contract

### M203-ENTRY-01 — execution entry and discovery

The owner authorized execution on 2026-09-08. Current HEAD is `84851520c34e75b9bc1fa8702f8ee0ae1087852a`, symbolic ref `refs/heads/codex/m2-03-sufficiency-abstention-detail-ui`; the index and initial working tree are clean. The latest commit intentionally records the plan. No unrelated dirty paths require disposition. `logs/agent-flow-leases/v2/active.json` is absent. Python is 3.12.10. Runtime turn metadata exposes primary `gpt-6-astra`, effort `high`, matching the owner's selection; configured role pins and runtime role availability match the model policy. No configuration was changed.

Entry fingerprint method: sort repository-relative `git ls-files` paths, form `path:SHA256(checkout bytes)`, join with LF without a final LF, then SHA256 UTF-8. Source/test/package/TypeScript/Vite group: 77 files, `C413F15B69D00F18551E72A2EB7A0993F64A7A676333D7B720B2C68DDCB79873`; AGENTS/PLANS/workflow/skill/requirement/ADR/spec/UI group: 65 files, `DD482ABFF659CC36DC55F0A5A844812D1A98B1702098F59182E05DF6D95C6387`; corpus/evaluation: 4 files, `A21C0BBD5ECE2278F581CCC0F5D9E5095D3F7AF8682F0F5109AFD5E2851870F4`. All four individual frozen-input fingerprints match M203-PLAN-01. Historical test results are not renewed by these checks.

The six `fixtures/rd003` files use the same fingerprint method: `14BE23BDD1E85FDBB0B8CBFD9EE88CD6EB7C00633B54812D15CA9FBA076A3EC9`. The logical stage-entry fingerprint (UTF-8 `git ls-files --stage` lines joined with LF, no final LF) is `9793D457F719180606C8546EA9E9B755C9D2912816A7664FF65174D4E59F8439`. The lease guard captures its fuller index/settings baseline before each future write turn.

Retained generated inventory: `dist/client/index.html` (414 bytes, `6F81570B534AF725BD6CFCCC31DA782CA722B2565ECD94CC63FCF2AE44E5A6D1`), `assets/index-BnKNSoiv.css` (4706 bytes, `C4800A2375D986E09CDB7DE5D0A28354E79521BF89FF90FF24291F64C0BD5517`) and `assets/index-nORRdglA.js` (230208 bytes, `C6D3AF7BA7EC199E68F62EE715C38152A34DF6E140457AFB1A33E61B682A93B1`). Preserve until the C-BUILD disposition. Existing `node_modules`, `m104-browser-runtime`, all prior `logs` evidence/leases, and `temp` directories `astra-workflow-20260908`, `m103-scan`, `m104-setup`, `m104-ui`, `m105-integration`, `m202-capacity-01`, `m202-dependencies-before-setup`, `m202-dependency-restore`, `m202-npm-cache`, `rd002-npm-cache` remain outside cleanup scope. External M2-02 runtime/model/tokenizer stores remain preserved.

One bounded non-ranking discovery inspected native fact types, strict stored-Finding validation, exact catalog/retrieval references, the selected operation, and publication transitions. Current complete retrieval remains active with a service owner; current validation admits no analysis/abstention. Candidate mechanisms frozen for comparison: evidence-first gate with direct terminal evidence abstention versus post-retrieval evidence gate; additive strictly validated nested analysis with existing format 1 historical reads versus format revision; extending current operation/publisher with explicit terminal publication versus adding a separate guidance operation/publisher. Here direct terminal means the existing operation completes the evidence-only branch, preserving truthful `unprocessed -> active -> abstained` activation with a minimal active analysis marker if necessary; it does not mean skipping the lifecycle or inventing retrieval execution. Shared wire candidate is a detached validated run plus canonical citation view, rather than persisted passage copies. Compare only these current-boundary mechanisms under M203-LITERALS-01 criteria and I1–I8; none is selected by discovery. Primary will inspect remaining controlling rows while the single related integrity researcher runs. No worker preflight or write lease is authorized by this entry.

### M203-LITERALS-01 — execution-stage selection barrier

Status: **D1–D4/shared D5 frozen**. Planning inspection is R0; this decision is R3 because new nested result validation and selected-Finding transitions affect integrity, identity, ownership and recovery. The artifact is the following literal contract completed in this plan by the primary. It cannot approve a new architecture, corpus revision or expanded owner scope.

During authorized execution, allow one non-ranking discovery pass only over the current implementation and applicable primary evidence. Freeze the smallest viable mechanism set, common criteria, required outputs and invariant fixtures before comparison. Criteria are authority fit, preservation, failure truth, direct reuse, demonstrable behavior and maintenance cost. Evidence classes are controlling requirements, current source/tests, frozen corpus metadata, existing accepted M2-02 evidence, and current primary source-use evidence where the display review needs it. No broad model, framework, library or architecture survey is warranted.

| Literal | Required decision before dependent preflight |
| --- | --- |
| D1 — evidence and branch order | Exact required fields for each of the three profiles, reference representation, unavailable and not-applicable handling, and deterministic blocking reasons/manual guidance. A missing comparison-only locator or genuinely optional shadow fact must not automatically become missing required evidence. Native `false`, absent alt, or failing ratios may be complete evidence of a violation. Separate human contextual judgment from required captured evidence. Freeze whether the evidence gate precedes retrieval; if retrieval is attempted, execution/integrity error cannot become abstention. |
| D2 — citations and support | Exact validated catalog-to-citation projection, required roles from manifest, rule/SC applicability and order: error without support, conflicting, missing, incomplete, supported. Define the smallest curator-conflict input/validation and normative-precedence rule; the production snapshot has no conflicts. Synthetic conflict vectors stay test-local, cannot enter through user input, and must not bypass production corpus identity validation. Scores never establish support; do not fetch omitted roles or reorder top three. |
| D3 — result and durable transition | Exact nested fields, discriminants, chronology, allowed evidence/passage references, compatibility with valid M2-02 records, support placement, strict extra-key rejection and format-version disposition. Abstention references existing records without copying evidence/passages; it records the applicable sufficiency, blockers, reason, no-provider-call confirmation and manual investigation, with no remediation conclusion or invocation/review object. Freeze supported unfinished state, terminal abstention/failure, and treatment of incomplete evidence when retrieval is absent. |
| D4 — ownership, API and publication | Exact service callable and browser method/path/request/response/error mapping. Reuse `retrieveFinding`, `updateRetrieval` and their current collaborators where responsibilities fit. Define active-owner retention for supported unfinished work, release after durably terminal outcomes, duplicate/stale/wrong-Finding rejection, read/interrupted behavior, operation versus workflow ownership, failed publication, disconnect, shutdown and late-result handling. Preserve valid historical active records without automatic completion or resume. |
| D5 — browser projection and presentation | Exact response admission and detached citation view, current run/selected identity checks, transient busy/failure handling, source-notice presentation, headings/copy and shared announcements. Complete the reuse audit below, ensure safe original-source links, and freeze browser evidence inputs/commands. Raw corpus text is inert; browser code imports no privileged corpus loader, model, filesystem or process implementation. |

D1–D4 and the shared D5 wire shape are decided before A; remaining reversible D5 layout/copy is frozen before C. Exact runtime conformity and browser observations are proved afterward. A worker cannot invent a missing decision while making tests pass. Add an ADR only if findings require a significant mechanism outside Accepted decisions; ordinary task-owned literals do not need one.

Research budget: one `critical_researcher` covering the related citation/record/transition integrity dimension; a second is permitted only if discovery isolates a distinct unresolved ownership/recovery dimension that cannot fit the first report. At most one targeted follow-up per researcher. One `technology_researcher` is permitted solely if the excerpt-display/use-condition question requires fresh external evidence; otherwise use recorded notices directly. Supply [Research Assignment Capsule v1](../../../.codex/README.md#research-assignment-capsule-v1), exact gaps and useful-detail limits; roles remain read-only. One synchronization barrier feeds mandatory `decision_analyst`, then a fresh `critical_research_reviewer` pre-draft checkpoint, primary authoring, and a different fresh final `critical_research_reviewer`. No drafter or optional panel is needed. Apply one supported pre-draft correction and the workflow's two-cycle final decision-artifact ceiling; do not reset budgets through new IDs. `DRAFT READY` and both checkpoints are required; `RETURN FOR RESEARCH` uses remaining allowance and `OWNER DIRECTION` returns to primary triage/owner scope as applicable.

Research checkpoint: `M203-LITERALS-01-R1` returned **RESEARCH COMPLETE** after one report and its sole targeted follow-up; that allowance is consumed. The report compares only the frozen mechanisms against current fact/query/catalog/record/operation/transition/UI source and the named requirements. It recommends the evidence-first gate with truthful active analysis, additive strict format-1 variants, existing operation/publication owners and detached citations. These are inputs to mandatory analysis, not accepted literals. The follow-up corrects its initial contrast recommendation: `messageKey` is optional; `shadowColor` becomes required only for retained `shadowOnBgColor` or `fgOnShadowColor` measurement keys. It recommends original-byte corpus validation in a server-only resolver and automatically applying normative precedence before synthetic conflict classification. Current manifest identity admits zero conflicts, so pure conflict vectors and forged durable-conflict rejection must remain distinct from authentic production outcomes. Source-use evidence reuses the manifest and full recorded README notices; no new acquisition or legal determination occurs. The subsequent analysis and pre-draft dispositions are recorded below; both are decision evidence only.

### Cumulative invariant packet

`M203-LITERALS-01-ANALYSIS` returned **DRAFT READY** after independently auditing the current boundaries and authority mapping. It supports the first candidate in each entry comparison: evidence-first avoids unnecessary execution before a known blocker; additive format 1 avoids an unnecessary migration while the new reader preserves historical reads; extending existing operation/publication avoids duplicate ownership; detached citations avoid durable copies. Each alternative was compared against the same six criteria. Reversal requires an Accepted need for retrieval despite incompleteness, incompatible record semantics, a distinct lifecycle, or independent citation copies respectively. No owner decision or additional research dimension was identified. Analysis is decision evidence only. Failure-envelope admission must distinguish the attempted unsaved update from its included last-durable run; before C, selection during pending guidance must preserve the original Finding's durable outcome without transferring ownership.

`M203-COMMANDS-A-01`: the primary parsed all three recorded PowerShell callers with line-anchored Markdown fences, loaded the maintained preparation alone, and verified installed Node/TypeScript/Vite/browser paths plus empty scan/UI/integration scratch roots. No dependency restoration or generated output occurred. C1 already proves the existing runner. C-A remains pending its test-owned file and reviewed literals.

Each future literal review applies this complete packet after every material revision. Actual evidence is pending; record it with `M203-LITERALS-*` identities. The two research reviewers own decision coverage; slice/final reviewers own implemented proof.

| ID | Trigger / fixture | Required result | Implementation review |
| --- | --- | --- | --- |
| I1 | Every revision and baseline change | Complete D1–D5 at their barriers; accepted versus proposed/observed facts and authorization remain honest; old HEAD/dirty paths are not forced. | All reviewers |
| I2 | Full/partial/zero role sets; unresolved conflict; normatively resolved conflict; tied/high/low scores | Exact accepted support order and mapped canonical references; neither score nor gold IDs substitute for roles. | A critical |
| I3 | Missing/invalid/withheld required facts; false/absent but valid facts; optional/not-applicable fields | Exact evidence policy; truthful abstention with references, blockers, investigation and no remediation/invocation/review. | A/B critical |
| I4 | Unknown, duplicated, wrong-version or altered citation; missing/changed catalog; forged support | Integrity failure without support or abstention; no partial misleading citation set or corpus repair. | A/B critical |
| I5 | Two Findings; duplicate request; wrong run; stale response; one active owner | Only selected nested data changes; original scan/observations/siblings/provider context remain identical; no overlap, fan-out or owner transfer by selection. | B/C critical |
| I6 | Activation/final write failure; stop/deadline/disconnect; historical active data | Last valid aggregate authoritative; no unsaved success, false terminal release, late publication, retry or resume. | B/C critical |
| I7 | Browser requests/content and ordinary Analyze/selection | Same-origin application boundary; no user-supplied vectors/support/endpoint; no model work from selection; inert text and safe canonical citation destinations/notices. | B/C critical |
| I8 | Keyboard selection/action/citation inspection; narrow/zoom/long text; outcome changes | Preserved list/focus, shared truthful announcements, distinct source/state labels, no clipped guidance or horizontal page overflow. | C critical with visual coverage |

### M203-LITERALS-01 — primary-authored contract

The primary selects evidence-first assessment, additive strict format 1, extension of `retrieveFinding` / `updateRetrieval`, and a detached canonical citation view. The preceding analysis records comparison and reversal conditions. `M203-LITERALS-01-PRE` independently passed I1–I8 without findings and confirmed the synchronous publisher, original/LF/CRLF catalog acceptance, altered/BOM/reserialized rejection, three role profiles, sixteen passages and zero conflicts. This is decision evidence, not new implementation verification. The distinct final reviewer must assess this entire artifact and invariant packet before A preflight.

#### D1 — evidence sufficiency

`src/server/domain/finding-sufficiency.ts` exports `assessFindingEvidence(native: NativeFinding): EvidenceAssessment`. It reads the native input through the existing strict native reader; malformed input throws an ordinary bounded validation error rather than becoming incomplete evidence. It returns detached frozen data and never mutates input. Types live in the browser-safe `src/server/domain/finding-analysis-types.ts`.

`EvidenceAssessment` has exactly `state: 'complete' | 'incomplete'`, `availableReferences: EvidencePath[]`, and `blockers: { reference: EvidencePath; reason: 'missing' | 'invalid' | 'withheld' }[]`. References are closed paths relative to this Finding. Required paths are ordered as follows; unavailable required facts produce one blocker with their retained reason. Available facts produce one reference. No duplicates occur.

| Profile | Required paths, in order |
| --- | --- |
| image-alt | `checks`, `evidence.elementKind`, `evidence.altState` |
| label | `checks`, `evidence.elementKind`, `evidence.inputType` only when the available element kind is `input`, then `evidence.nameSources.explicitLabel`, `implicitLabel`, `ariaLabel`, `ariaLabelledby`, `title`, `placeholder`, `presentationalRole` (all seven use the full `evidence.nameSources.` prefix) |
| color-contrast | `checks`, `evidence.foregroundColor`, `evidence.backgroundColor`, `evidence.contrastRatio`, `evidence.expectedContrastRatio`, `evidence.fontSize`, `evidence.fontWeight`, `evidence.measurementSource`; append `evidence.shadowColor` when available `messageKey` is `shadowOnBgColor` or `fgOnShadowColor` |

Contrast optional available diagnostics follow required references: `evidence.messageKey`, then `evidence.shadowColor` if not already present. `measurementSource: 'axe-core'` is an available literal. Unavailable optional facts, `not-applicable` input type and locator are omitted. If label element kind is unavailable, it blocks but does not invent an input-type requirement. False naming facts, absent/empty alt, unresolved naming references and failing measurements are available evidence. Human judgments about image purpose, good alternatives or name correctness are not capture requirements. Any blocker means incomplete; otherwise complete.

#### D2 — support and authenticated citation resolution

`src/server/retrieval/support-policy.ts` exports `classifyGuidanceSupport(finding: unknown, retrieval: unknown, conflicts: unknown = []): SupportResult`, where success is exactly `{ ok: true, value: GuidanceSupport }` and failure `{ ok: false, error: 'result-validation' }`. It validates retrieval through `validateRetrievalResult` before classifying. `GuidanceSupport` has exactly `state: 'conflicting' | 'missing' | 'incomplete' | 'supported'`, `missingRoles: GuidanceRole[]`, `conflicts: [string, string][]`. Roles are `criterion`, `interpretation`, `remediation`, always in that order. Add exact `sourceType` and `guidanceRole` to the existing fixed `PASSAGE_REFERENCES` projection and export `PROFILE_REQUIRED_ROLES` for the three existing rule IDs, matching the frozen manifest. Do not change `RetrievalResult`, its ranking or finite query domain.

Conflict vectors have exactly `{ passageIds: [string, string], resolution: 'unresolved' | 'normative-precedence' }`. Reject noncanonical/unknown IDs, identical IDs, cross-profile pairs, extra keys, duplicate unordered pairs and invalid resolutions. Canonicalize each pair ascending and output pairs lexicographically. A pair with exactly one `recommendation` source resolves by normative precedence even if declared unresolved. `normative-precedence` with zero or two normative sources is invalid. Only unresolved pairs wholly present in returned passages affect support. Errors return no support. Otherwise use conflicting first, missing for zero passages, incomplete for missing roles, supported for all roles. Compute missing roles for every successful state. Scores and gold IDs never establish support.

The frozen production declaration is empty. Synthetic conflict vectors are only pure-policy/display test inputs; persisted support must be recomputed with the authenticated production declaration. A forged durable conflicting state must fail rather than broaden corpus identity.

`src/server/retrieval/citation-resolution.ts` exports synchronous `resolveCitations(finding: unknown, retrieval: unknown, manifestBytes: Uint8Array, passageBytes: Uint8Array): CitationResolutionResult`. Success is exactly `{ ok: true, value: { corpus, passages, notices }, support }`; failure is exactly `{ ok: false, error: 'corpus-integrity' | 'result-validation' }`. Call the existing authenticated `parseCorpusCatalog` on original bytes, then validate retrieval, resolve every returned ID to the canonical passage/source and apply the authenticated conflict declaration. Reuse the parser's normalized LF identity; neither object reserialization nor a caller-provided catalog object is authentication. Resolve all or return failure without partial citations/support. Corpus errors precede result errors.

`src/server/retrieval/corpus-catalog.ts` exports fixed-path async `resolveFindingCitations(finding: unknown, retrieval: unknown): Promise<CitationResolutionResult>`. Reuse a private fixed two-file byte reader with `loadCorpusCatalog`; preserve existing document creation and lazy embedding callers. The new operation rereads these files after retrieval and before publication. The resolver owns canonical projection, support owns role/conflict decisions, and neither domain policy nor browser imports file/crypto/LangChain code.

#### D3 — additive records, construction and validation

`src/server/domain/finding-analysis.ts` exports `buildFindingAnalysis(native: NativeFinding, startedAt: string, finishedAt: string, support: GuidanceSupport | null): FindingAnalysisDecision`. It validates native data, canonical times and the closed support shape, then returns exactly `{ state, analysis }` for supported eligibility or `{ state, analysis, result }` for abstention, detached/frozen. It throws on malformed input, reversed times, incomplete evidence with non-null support, or complete evidence with null support. `FindingAnalysisDecision` is the discriminated union of those two shapes. `analysis` is exactly `{ status: 'completed', startedAt, finishedAt, evidence: EvidenceAssessment }`. Typed construction does not authenticate a retrieval; the durable validator recomputes support against its actual validated retrieval and fixed production metadata.

Preserve all exact historical unprocessed, active retrieval-running/completed and failed retrieval variants. Add only these combinations:

| Branch | Activation | Successful publication | Failure |
| --- | --- | --- | --- |
| Incomplete evidence | `state: 'active'`, `analysis: { status: 'running', startedAt }`; no retrieval | `state: 'abstained'`, completed analysis and abstention result; no retrieval | `state: 'failed'`, `analysis: { status: 'failed', startedAt, finishedAt, error: 'shutdown' | 'result-validation' }`; no retrieval/result/support |
| Complete evidence | Existing active retrieval-running variant | Completed retrieval has its existing fields plus `support`; completed analysis has identical start/finish times. Supported remains active without result; other support states are abstained with result. | Existing failed retrieval variant, without analysis/result/support |

Strict storage readers require exact keys, canonical chronology `parent.finishedAt <= startedAt <= finishedAt`, matching activation times, recomputed evidence/support and exact state correspondence. Historical active records remain readable but never resumed; historical unassessed completion cannot substitute for new assessed service success. Native scan admission still rejects downstream fields. `formatVersion` remains 1; this new reader accepts old records, without claiming old binaries accept new variants.

`AbstentionResult` has exactly `type: 'abstention'`, `findingId` matching its owner, `evidenceReferences` exactly equal to assessment available references, `retrievalReference: 'retrieval' | null`, `reason`, `explanation`, `providerCalled: false`, and `manualInvestigation`. Null retrieval reference occurs only for incomplete evidence with no retrieval. Non-null references only the same Finding's completed retrieval. There is no result identity, copied evidence/passage, remediation, proposal, invocation or review. `providerCalled: false` means no generation-provider call; embedding may have occurred.

| Reason | Exact explanation |
| --- | --- |
| incomplete-evidence | Required captured evidence is incomplete. |
| conflicting-guidance | The retrieved guidance contains an unresolved material conflict. |
| missing-guidance | No applicable guidance was retrieved. |
| incomplete-guidance | The retrieved guidance does not cover every required role. |

Exact incomplete-evidence investigation text by profile:

- image-alt: `Inspect the affected image and verify its purpose and alternative-text state. Capture the unavailable required facts before requesting guidance in a new analysis.`
- label: `Inspect the affected control and verify its element type and naming relationships. Capture the unavailable required facts before requesting guidance in a new analysis.`
- color-contrast: `Inspect the affected text and verify its foreground, background, font properties and contrast measurements. Capture the unavailable required facts before requesting guidance in a new analysis.` Append one space and `The recorded measurement identifies a shadow contribution; inspect and capture that shadow color.` only when shadow color blocks.

All guidance reasons use exactly `Inspect the affected element and the cited sources. Investigate the missing or conflicting guidance before proceeding in a new analysis.` Blocker references/missing roles/conflicts supply the detailed blocking information.

#### D4 — service, HTTP and durable ownership

Retain `LocalService.retrieveFinding(input: unknown, execute?: RetrievalExecutor): Promise<RetrievalOutcome>` and `RunRepository.updateRetrieval`. Extend the success outcome to exactly `{ ok: true, run: CompletedRun, view: FindingGuidanceView }`. Failure retains exactly `{ ok: false, error: RetrievalServiceError, run: CompletedRun | null, persisted: boolean, cleanupFailed: boolean }`, with no view. Keep existing error vocabulary.

Expose `POST /api/finding-guidance`, JSON body exactly `{ runId, findingId }` using existing strict ID readers. No query/fragment, caller-selected endpoint, executor, evidence, vectors, support or corpus is admitted. Content type is the existing exact `application/json` rule. Map success to 200; invalid-request to 400; not-found to 404; busy/workflow-active/not-eligible to 409; stopping/shutdown/missing-prerequisite to 503; every other existing service error to 500. A transport callback rejection returns result-validation with null run, false persisted/cleanupFailed. Existing routes remain compatible; health gains `guidance: true` when the guidance callback exists.

Use the existing request reservation before reading/activation. Assess validated native evidence first. Assign workflow ownership only after durable activation. Complete the evidence-only branch without executing retrieval. For attempted retrieval, validate canonical citations/support and the full detached view before terminal publication. After the final stop/deadline check, invoke the synchronous publisher with no asynchronous gap. Return the prevalidated view only with its successfully saved assessed run.

Expected/current aggregate equality, immutable parent/context/provider/native data, exact sibling/observation preservation and exactly one changed Finding remain required. Permit only unprocessed activation and matching running-owner completion/failure; reject rewrites, direct terminal jumps and supported/historical-active continuation. Release ownership only after a durable terminal abstention/failure and known cleanup. Supported unfinished work retains its owner while the request reservation settles. Independent Analyze after settling remains allowed and does not transfer that owner.

Activation failure never calls the executor. Final publication failure returns the last durable aggregate, reports `persisted: false`, and retains ownership. Deadline forbids late write or owner release. Existing cleanup uncertainty closes admission. A disconnected request before full-body acceptance never activates; after accepted service work it does not cancel the operation, but suppresses writes to the destroyed response. Historical active read/interrupted behavior remains truthful and non-resumable.

#### Shared D5 — wire projection and browser admission

`FindingGuidanceView` is exactly `{ runId, findingId, corpus: CorpusIdentity | null, passages: Citation[], notices: Notice[] }`. `Citation` is exactly `{ passageId, corpusVersion, sourceTitle, sourceType, heading, url, ruleId, successCriterion, guidanceRole, text, score, sourceStatus, copyright, attribution, noticeKind }`. `sourceType` is recommendation/understanding/technique; metadata/text come from authenticated catalog/source records and score from the unchanged retrieval. `noticeKind` is `document` for recommendation, otherwise `software-document`. `Notice` is exactly `{ kind, text }`, with that same kind union. Notice order is first passage use, deduplicated by kind. Evidence-only view has null corpus and both arrays empty. IDs always match the request/durable owner.

`src/server/retrieval/source-notices.ts` owns browser-safe `SOURCE_NOTICES` constants for both full recorded [README notices](../../../README.md#closed-corpus-notices). Their exact text is each corresponding blockquote block with the Markdown `> ` prefix removed (`>` alone becomes an empty line), LF joins and no trailing LF; preserve all remaining characters, including list markers. This is reuse of recorded source-use evidence, not a fresh legal/distribution determination. Every displayed portion has its source status, copyright, attribution and applicable full notice viewable adjacent to it. Ordinary text is inert; links use exact canonical URLs.

Browser admission snapshots strict own-data shapes, validates the returned run and new assessed variant, and checks request/run/Finding IDs, current operation token, immutable parent/native/context/siblings and selected-only permitted progress. The server authenticates canonical text; browser validation checks nonempty text, exact fixed metadata, passage order/IDs/scores, exact notice constants and correspondence, without claiming cryptographic authenticity. Browser imports only pure readers/types/metadata/notices, never fs, crypto, process, model, corpus loader or node:util. A failure envelope has no view; its validated last-durable run may update the displayed aggregate under the same preservation/request checks. `persisted: false` describes the attempted update, not an unsaved included run. Invalid envelopes retain the previously admitted run.

Remaining D5 presentation/copy and selection-during-request semantics remain a before-C gate. Selection cannot transfer ownership or discard an original Finding's durable outcome into apparently unprocessed UI. Authentic conflicting support is impossible in this snapshot: prove pure vectors, durable/API forgery rejection and explicitly isolated synthetic display without weakening admission.

#### Frozen A proof and responsibility boundary

`M203-LITERALS-01-FINAL`: a different fresh `critical_research_reviewer` passed the complete written artifact and I1–I8 without findings on plan SHA-256 `763BA48698A0DD64FE8A65548B3EDEC1BC6BD2984E206BF4FF2EC141D3D2A6F7`. It independently confirmed normalized original-byte parser behavior, canonical metadata, zero conflicts, immutable boundaries, empty index and absent lease. The primary accepts this before-A barrier; final-artifact cycle 1 consumed, no correction cycle used. Later checkpoint bookkeeping does not change the accepted literals. A/B/C and actual browser/runtime/spoken evidence remain unproved.

Freeze vectors before implementation: all three complete profiles; every required unavailable reason; truthful negative facts; textarea/non-applicable and unavailable element kind; optional diagnostics; both shadow measurement keys with required available/unavailable shadow; full/partial/zero returned roles; same-authority unresolved conflict, single-normative precedence and invalid conflict declarations; malformed native/retrieval; altered/BOM/reserialized/unknown/wrong-profile/duplicate corpus references; exact explanation/investigation/reference templates; no input mutation or forbidden fields. Native/retrieval vectors reuse M102/M202 helpers; new vectors do not modify frozen corpus/evaluation/fixtures.

A exact production placement: new `domain/finding-analysis-types.ts` owns shared structural types; new `domain/finding-sufficiency.ts` owns captured-fact assessment; new `domain/finding-analysis.ts` owns deterministic result construction; new `retrieval/support-policy.ts` owns support classification; new `retrieval/citation-resolution.ts` owns authenticated projection; new `retrieval/source-notices.ts` owns recorded notice constants. Extend only `retrieval/corpus-identity.ts` for pure fixed role metadata and `retrieval/corpus-catalog.ts` for shared fixed-byte reading/resolution. All paths are under `src/server/`. Domain → pure types/readers; support → pure retrieval validation/metadata; resolver → authenticated parser/support/notices; catalog → resolver. No transport/UI/persistence edge is introduced in A. Private helper organization within these owners may be refactored only after Green without changing behavior. No run-type integration before B.

A test placement is exactly new `tests/finding-sufficiency.test.ts` plus optional cohesive `tests/helpers/m203-finding-fixture.ts`; existing `tests/retrieval-contract.test.ts` is read-only regression. C-A and independent C-STRICT are prepared above. Missing agreed modules may use only the workflow's verified first-module exception with complete behavior assertions; no placeholder production or tests.

## Plan of Work

### Ownership, budgets and advancement

Use the current [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [guard](../../../.codex/write-lease-guard.md) and [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md). Each slice has a separate `test_worker` and its specified Green worker. Primary completes Packet v2 and accepts read-only preflight, exact test boundary, actual diff/cohesion, terminal receipt and focused evidence before advancing. The path envelopes below are proposed responsibility placements; freeze the exact subset and callables in D1–D5 and project it unchanged into the packet and lease. No directory-wide source permission is implied.

Preflight uses the published classification routes. Covered behavior gets no new test or Green; uncovered behavior gets characterization; missing/regressed behavior gets one coherent Red; partial gaps are isolated. A genuinely absent agreed callable may use only the verified first-module exception, recording which assertions did not execute. Green must run every accepted test unchanged plus independent strict TypeScript. Every write turn has a primary-opened/freshly closed lease; primary documentation occurs between leases. Ordinary test correction belongs to `test_worker`; the bounded primary exception invalidates evidence and requires re-acceptance.

Budgets are one preflight, one Red/characterization, one Green, ordinary attempt-2 correction and conditional attempt-3 correction per unchanged role/phase chain under the current procedure, plus one review correction loop. Record progress/new evidence and the different corrective action before attempt 3. Stop on unsuccessful attempt 3, the same decisive failure twice without new evidence, two no-diff write handoffs, changed binding fields, exhausted authority or unresolved conflict. A worker stop returns to primary triage; continue unaffected authorized work. IDs or replacement agents cannot reset allowances. Post-Green cohesion is `RETAINED`, `REFACTORED` or `RECONCILE`; pre-Green/non-source work uses `None`.

There is no planned setup lease or dependency change. Planning, literal selection, manual browser observations and documentation use **TDD: Not applicable**, with semantic/structural/manual evidence. Any indispensable executable helper is test-worker-owned under its slice, never primary-written as a workaround. Compare a direct use of existing helpers before creating one; do not build another evaluation platform. Independent read-only research may overlap; A, B, C and all write leases remain sequential.

### A — deterministic decision and citation policy

`M203-A-PREFLIGHT-01` is accepted as **MISSING**: current native/retrieval/catalog owners lack the six new policy/type/notice modules, role projection and fixed citation wrapper, and existing tests cover none of their new callables. The test worker reproduced the 77-file ENTRY source fingerprint without writes. Primary independently confirms the actual guard pointer `logs/agent-flow-leases/v2/active.json` is absent (the preflight report's alternate pointer path is not lease evidence). Freeze Red to `tests/finding-sufficiency.test.ts` and cohesive vector helper `tests/helpers/m203-finding-fixture.ts`; existing regression tests remain unchanged. Accept only the expected missing `src/server/domain/finding-sufficiency.ts` / `assessFindingEvidence` import as initial Red after inspecting the complete behavioral matrix. All blocked assertions remain unexecuted until Green. C1 proves the unchanged runner, not new behavior. Fixed-file failure tests must use in-memory I/O fault injection and restore it; frozen corpus files cannot be edited.

Workflow `M203-20260908`, slice `M203-A`, test owner `m203-a-test`: next write is Red attempt 1, lease `M203-A-RED-01`; no correction parent. Primary opens and closes its exact scope through C-LEASE. C-A allows no generated output or model call. The following lease receipt and actual diff, not this planned ID, determine acceptance.

`M203-A-RED-01` accepted: terminal guard status `closed-compliant`, receipt digest `db67ad57aab79b14062f7bc888c112be337522fbdbce9b84d434ced688a269da`, only the two allowed files created, no source/index/HEAD/ignore drift. The primary inspected the complete behavioral boundary and independently reproduced C-A: exact missing agreed module, 12 existing regression tests passed, 21 new registrations blocked before execution, zero skipped/todo. Node syntax checks passed. In-turn test authoring corrections fixed a missing-role expectation, explicit LF/CRLF preparation, and ESM-synchronized in-memory read-fault injection; they changed no contract and consumed no additional write turn. The primary independently demonstrated fault and restoration against the existing catalog loader without file mutation. No policy behavior is yet proved.

Accepted immutable test hashes: `tests/finding-sufficiency.test.ts` = `623D0B7C3983347845DC3BAF97D8C7626F194D1C6FCA487FEA322600CC704964`; helper = `A893298CAA4C879CFDFF2F8246C5FD50843DE5B1D72574D09675AA3AD2138140`. Source/test/config group now includes tracked and untracked nonignored paths with the ENTRY hashing method: 79 files, `D763F6D960EE87DC597F24FB4D89C6D695DFE8B5BFDF7FADCDD813B64D31F731`. Green owner `m203-a-code` may write only the eight production files frozen above; no accepted test change is allowed. C-A must execute all 33 tests successfully, followed by independent C-STRICT; count changes require explanation rather than suppression.

`M203-A-GREEN-01` stopped with **TEST CONTRACT CONFLICT**, not accepted Green. Its fresh `closed-compliant` receipt is `58730283a2261e64a73e01678a9bd7be75a0a4d62207d77d256d70c698cb387d`; exactly the eight permitted production files changed, with no test/index/HEAD/ignore drift. C-A executed all 33 tests: 30 passed and three notice comparisons failed; C-STRICT passed. Primary confirmed the cause against both README notices and the entire fixture family: the software notice requires `FITNESS FOR ANY PARTICULAR PURPOSE`, but the helper contains `FITNESS FOR A PARTICULAR PURPOSE`. The document notice correctly retains `A` and must not change. Production matches the recorded authority; no product-contract change is justified. Prior Red acceptance is invalid immediately. Original test owner corrects only that software-notice word in `tests/helpers/m203-finding-fixture.ts` under Red attempt 2, parent `M203-A-RED-01`, same scope and commands; compare both complete notices against the README blocks and rerun C-A. No fabricated renewed Red is required after already-present implementation. Reaccept fresh test identity before Green resumes; one ordinary correction allowance is used in the test chain.

`M203-A-RED-02` reaccepted the corrected test boundary after fresh `closed-compliant` receipt `e6aa9428725bc200f4d92e90bee0d1a23592eb84d403edee4d0831b3dbc19741`, with only the helper modified. The primary independently compared both entire fixture notices to README blockquotes and matched their exact text. Test-file hash remains `623D0B7C3983347845DC3BAF97D8C7626F194D1C6FCA487FEA322600CC704964`; corrected helper hash is `3B970BB2B3B72C96B1AC25499CE98CD1B28CF31C31894AF7A1B03FD34B2CA3B2`. The test owner ran C-A with all 33 passing, zero skipped/todo; no new failing Red is claimed. Original missing-module provenance remains historical, not reusable corrected-test evidence. Green renewal is attempt 2, parent `M203-A-GREEN-01`, same eight-file scope, commands and contract. No production repair is currently indicated; a no-change successful handoff may renew executed-test and actual-cohesion evidence without speculative refactoring.

Outcome: one validated native Finding plus an actual validated retrieval/catalog package yields the exact support/evidence decision and either later-generation eligibility or an application-authored abstention; invalid execution/integrity input yields an error. It performs no model call or durable write. Authorities: D1–D3, I2–I4, `REQ-EVID-004`, `007`, `REQ-RETR-004`/`005`, `REQ-GEN-001`/`009`/`010`, ADR-0022, SPEC-002/003 and HS-008.

Production envelope: proposed `src/server/retrieval/citation-resolution.ts` owns canonical passage/source resolution and display metadata; `support-policy.ts` owns role/conflict classification; `src/server/domain/finding-sufficiency.ts` owns required evidence/blockers; `finding-analysis.ts` owns the small typed decision/abstention construction. Reuse fixed catalog/reference validators and existing domain fact readers. These are distinct current responsibilities, not a generic policy or schema framework. Keep decision functions pure; service code supplies validated inputs. Domain policy must not import corpus-file I/O, LangChain, transport or React. If a minimal type extraction is necessary for that direction, freeze its exact path before preflight.

Test envelope: new `tests/finding-sufficiency.test.ts` and, only if needed, `tests/helpers/m203-finding-fixture.ts`; exact affected cases in `tests/retrieval-contract.test.ts`. Reuse M102/M202 native and retrieval factories. Freeze an incomplete-role package and conflict vectors as tests, not edited canonical corpus/gold or fake model evidence. Test every support state and precedence, required versus optional unavailable facts, exact references, forbidden fields, deterministic explanations and no mutation. Focused C-A plus C-STRICT prove the contract. `test_worker` then `code_worker`; S3/fresh `critical_reviewer` for identity/reference integrity. Accept `M203-A-*` before B.

`M203-A-GREEN-02` and `M203-A-REVIEW-01` accepted: fresh compliant closure receipt `8b5dcfc18d6114dfc5575c7c1ca91ed4997522698bd3737aaef36da1d06973fd`, no renewal edits or unexpected drift. Primary independently ran C-A (33 passed, zero failed/skipped/todo) and separate C-STRICT (exit 0). Eight production owners are **RETAINED**, group hash `FEE9669AC351D45464D187201EC0E778D02651FDCB202EEE539583B1B1CAF70F`; current 85-file source/test/config group is `9F740128BACC5F1FD408D6B28235EE7A472CEDF15C33BF0545B0105B02D9C954`. The fresh S3 critical reviewer independently reproduced all 33 tests, notice equality, frozen identities and guard no-drift, reused the primary's strict evidence and passed I2–I4/cohesion without findings. One review cycle used, no review correction. This closes A only; B/C obligations remain. Subsequent primary bookkeeping changes the plan baseline without invalidating unchanged pure code evidence.

### B — durable selected outcome and application API

#### Frozen B responsibility and proof boundary

Workflow `M203-20260908`, slice `M203-B`, assignment `M203-B-PREFLIGHT-01`, phase preflight, attempt 1, `test_worker` owner `m203-b-test`; lease/digest/correction parent: None. TDD applies to missing durable/HTTP behavior. A acceptance above, ENTRY, LITERALS-FINAL and the Accepted task authorities supply readiness; no evaluation input changes. Exact C-B and independent C-STRICT below are the only prepared execution callers. Preflight is read-only and reports COVERED, UNCOVERED, MISSING, REGRESSION or PARTIAL with decisive existing coverage/gaps before any test lease.

Freeze eight production paths: `src/server/domain/run-contract/run-types.ts` owns additive Finding variants; `retrieval-validation.ts` remains the historical/new stored-Finding dispatcher; new adjacent `finding-analysis-validation.ts` owns strict recomputed assessment/support/result relationships; `src/server/persistence/run-repository/retrieval-transition.ts` owns selected-only expected-state transitions; `src/server/local-service/retrieval-operation.ts` owns evidence-first activation, citation preparation, publication and workflow ownership; adjacent `contracts.ts` owns the extended outcome/view type; `loopback-api.ts` owns fixed route/body/status/disconnect transport; `src/server/service.ts` wires the existing reservation and callback. No facade, scan/run-validator, publisher or ranking edit is currently needed. Domain validation imports A's pure policies/readers/metadata only; persistence imports validated domain types; operation imports A's fixed file resolver and existing repository; transport invokes the service callback. Private helpers may be reorganized only within these responsibilities after Green. No catch-all helper or second publisher is authorized.

Task-owned integration details: a validated retrieval result is converted to its JSON durable representation before citation projection, so an accepted negative-zero score appears as zero in both saved run and detached view without mutating the executor input or changing ranking. Strict object correspondence ignores own-key enumeration order. Keep historical M202 unassessed fixture factories intact; add separately named assessed fixtures for new behavior. Wire the HTTP guidance callback alongside scan only when the built-client application boundary exists; API-only construction retains no POST actions and reports `guidance: false`. The transport helper reports capability directly from callback presence. These resolve implementation literals within D3/D4 rather than changing architecture.

Exact test write scope, once Red is authorized: `tests/retrieval-service.test.ts`, `tests/run-contract.test.ts`, `tests/run-repository.test.ts`, `tests/local-service.test.ts`, `tests/helpers/m202-retrieval-service-fixture.ts`, and new `tests/finding-guidance-api.test.ts`. No allowed directory roots. A tests/helpers and all production are forbidden to the test writer; all tests are forbidden to Green. Both roles are forbidden from docs, corpus, evaluation, fixtures, `.codex`, `.agents`, AGENTS.md, PLANS.md, README.md, package/lock/config and all Git mutations. Existing assertion edits must be enumerated and justified by the additive contract; retain historical read coverage. C-B's filesystem/socket results are non-reusable absent an isolated pinned run identity; no browser/model call is permitted.

Named risks: I3–I7, custom derived-state integrity, exact-one-Finding durability, owner release/recovery, stop/deadline and disconnect. Risk S3 requires a fresh `critical_reviewer` after unchanged-test Green and independent strict checking. Budgets and stops are exactly Ownership above: one preflight/Red/Green, ordinary attempt 2 and conditional attempt 3 in each unchanged role/phase chain, one review correction; no IDs reset them. Task-level C-FULL remains the prepared closure obligation after C, not authorization for model/browser effects during B. Handoffs report exact identity, diff, commands/results, boundary hashes, terminal lease, cohesion, residual risks and documentation impact. B is not yet accepted.

`M203-B-PREFLIGHT-01` accepted as **PARTIAL** with the missing gap isolated: existing tests cover selected-only retrieval durability, admission, ownership, shutdown/deadline, historical reads and JSON normalization; assessed variants, recomputed relationships, evidence-first outcome/citation integration and fixed HTTP guidance are absent. The test worker reproduced the 85-file identity and preserved all paths. Ordinary behavioral Red through existing `validateRun`, `retrieveFinding` and loopback entry points is available; no missing-module exception is needed. Freeze the six-file test scope above. Adjust only successful service/repository publication expectations and health capabilities for the additive behavior, preserving historical fixture/read assertions. New repository completion must be assessed, and new activation must match recomputed evidence. Authentic conflicting support remains impossible: B rejects forged declarations/records rather than manufacturing a production conflict. Malformed executor/record inputs belong to B; malformed detached response admission belongs to C. The trusted transport callback is not a new general untrusted-outcome authentication boundary. C-B runs all six files before reporting any failure so ordinary Red can execute the entire new behavioral boundary. This changes only diagnostic aggregation, not admitted effects.

`M203-B-RED-01` closed compliantly with receipt `7fed140843ddb5abc19fec8f8f11eb0f56f8b2b5df0530ff9a26424e4e61c695`: exactly the six allowed test paths changed, without source/index/HEAD/ignore drift. The worker's complete C-B run executed every registration without import errors, timeouts, skips or todo cases: A 21/21, API 0/5, service retrieval 8/17, contract 64/66, repository 62/68 and local service 41/69 passed. Primary inspected the actual boundary, including citation-deadline late-write suppression, disconnect lifecycle and otherwise-valid mutation vectors. This is not accepted Red yet: independent C-STRICT found an out-of-scope `expectedCitations` local in the successful retrieval test and an incorrectly typed Promise resolver passed as a socket-write callback. These latent test defects must not be hidden by earlier behavioral failures.

Next test write: `M203-B-RED-02`, phase red, attempt 2, owner `m203-b-test`, correction parent `M203-B-RED-01`, identical six-file scope. Move the accepted-A citation expectation into the consuming successful retrieval test and wrap the socket completion resolver in a correctly typed callback. Preserve all behavioral assertions and production files. Run C-B and C-STRICT on the corrected identity; expected decisive result remains ordinary missing-contract failures with every registration executed, A passing, and no type errors. This consumes the ordinary test correction allowance; Green remains unauthorized until primary acceptance.

`M203-B-RED-02` accepted: fresh compliant receipt `c4c2ae9c77aa4a1c3be7f7804bc9dc9188db521c08d3322a93a494e0087c7f64`; only the two prescribed test corrections changed. Primary inspected their actual placement, matched all six hashes, independently reproduced C-B's missing-contract failures and A pass, and independently passed C-STRICT. No owned test roots remain. The worker corrected an initially misplaced local within this same authorized turn; the final strict result has no diagnostics. Accepted test hashes: retrieval service `E67864DE4AFFCD07F449404ACFFC9AB545400DEA6755F46FDF2CA9DDC1AF22AB`; contract `A3419FA981E126850EE4F00F98075EE8447E1D62B58D59D0ABD3F47BFE3C83EB`; repository `2BF855B4A112BCC5B87216404538A9E637739BDB5E0B75ED6B00DE95BEFA2D8C`; local service `C2030C9DBCB0631525185B56BB31C15982AC7502B6B90A2CF2DD5927B8958985`; M202 helper `BC32F3AA0AE8530D9E118F239F9C1C57561912B6634F494D1C9700DC83B19ABE`; API `CDC5764ACC16847CCEF51F42189C4DF1DDA4277146ACA4DC99EF047A3811C24E`.

Next production write: `M203-B-GREEN-01`, phase green, attempt 1, owner `m203-b-code`, role `code_worker`, profile standard, correction parent None. The exact eight production paths and responsibility/dependency map above control. All tests and A production are forbidden; C-B must execute every accepted assertion and pass, followed by separate C-STRICT and actual cohesion inspection. Existing mutable Red is behavioral provenance, not reusable filesystem state: reproduce C-B before edits. No browser, build, model call or additional scope is authorized.

`M203-B-GREEN-01` and `M203-B-REVIEW-01` accepted: fresh compliant receipt `ced9341dc2ffd89af5257abca4bc8eee04a8c64e6b28e69672d95222576d0a54`; exactly eight production paths changed and all accepted tests stayed unchanged. Primary inspected the complete surface and independently passed all 246 C-B tests (21/5/17/66/68/69), separate C-STRICT and cleanup checks. Eight-file production hash is `9A67F733189EF352C11DB051C9D7480098C467E53E2DB5FDB49C25113144CE12`; the 87-file source/test/config identity is `F81B2CBF19131322C0E0D531872E67A517988FFD83BBC86F250785EE34746AFF`. The different fresh S3 reviewer independently passed the first five C-B files (177 tests), strict TypeScript, integrity/receipt/no-drift checks and I3–I7/cohesion, with no findings. Cohesion is **RETAINED**; no review correction was needed. The worker corrected an initial byte-order expectation mismatch in production serialization and a type narrowing within its first Green turn. The reviewer identified an omitted effect in the capsule summary: the unchanged full C-B entry test creates exactly two owned `data/runs/m102-demo-<UUID>` children. Primary inspected its ownership, ordinary-path, normal-stop/port-closure and exact cleanup assertions and freshly confirmed the original empty directory restored; C-B below now states that established effect explicitly.

Outcome: the existing selected-Finding operation applies A, durably publishes its permitted result, and exposes only the minimum loopback action/citation projection consumed by C. Failure preserves native scan evidence, siblings and the last valid aggregate. Authorities: D3/D4, I3–I7, lifecycle, ADR-0021, `REQ-SEC-027`, `REQ-QUAL-011`/`012`, SPEC-003 failure and HS-008.

Production envelope: `src/server/domain/run-contract/run-types.ts`, `retrieval-validation.ts`, new focused `finding-analysis-validation.ts`, and only required facade/scan/run-validator integration; `src/server/persistence/run-repository/retrieval-transition.ts` and only necessary repository facade/contracts; `src/server/local-service/retrieval-operation.ts`, `contracts.ts`, `loopback-api.ts`, `src/server/service.ts`, and a focused `finding-guidance-view.ts` if citation projection does not fit the existing boundary. Extend the existing publisher and operation, without another writer, queue or generic update API. Preserve native-only scanner admission. Service coordinates pure policy and actual catalog resolution; HTTP handles fixed routing/JSON/status only, never corpus/model selection. Resolve canonical text for display without duplicating it into `run.json`.

Test envelope: `tests/retrieval-service.test.ts`, `run-contract.test.ts`, `run-repository.test.ts`, `local-service.test.ts`, and exact required M202 helper changes; new `tests/finding-guidance-api.test.ts` for its distinct HTTP contract. Each existing assertion change must reflect accepted new behavior, never weakened preservation. A test-only injected executor may supply controlled results; the production HTTP request cannot choose that executor. Test invalid/wrong IDs, zero/insufficient/conflicting guidance, evidence-only abstention, same-owner completion, terminal release, supported ownership, competing scan/retrieval, stale expected state, publication failure, shutdown/deadline, malformed HTTP/projection and absence of new external calls. Historical active records stay valid and non-resumable. C-B and strict checking prove the boundary. Separate test/Green owners; S3/fresh `critical_reviewer`. Accept `M203-B-*` before C.

### C — selected guidance and outcome UI

Outcome: explicit guidance activation uses B, updates only the matching displayed run/Finding after validated durable success, and presents citations and the deterministic outcome in the existing detail. The same risk-routed reviewer covers visual quality; no additional panel. `test_worker` then `frontend_code_worker`, profile **frontend-visual**, S3 for asynchronous identity/admission and the browser-to-service boundary.

Production envelope: `src/client/App.tsx`, `main.tsx`, `components/results/ResultsSection.tsx`, `ResultDetail.tsx`, `ResultCard.tsx`, `resultPresentation.ts`, `styles.css`; proposed `src/client/finding-guidance-admission.ts`, `components/results/FindingGuidance.tsx`, `GuidancePassages.tsx`, and `FindingOutcome.tsx`. Freeze the necessary subset; add a small current-operation hook only if coordination would otherwise mix a distinct responsibility, not merely to shorten App. App retains shared run/selection/reservation/announcement coordination; admission checks wire values and current identities; detail composes native evidence, guidance/citations and application outcome. Display code has no filesystem/model access and does not duplicate authoritative policy.

Frozen test envelope: new `tests/finding-guidance-ui.test.ts`, existing `tests/target-results-ui.test.ts`, `tests/helpers/m104-ui-harness.ts`, `tests/walking-skeleton.test.ts`, and new `tests/helpers/m203-real-guidance-driver.ts`. The walking-skeleton changes are only its three health expectations for the additive guidance capability. The real driver has a distinct current responsibility: the old capacity driver is hard-bound to its retained root and internal invocation, so it cannot supply this UI-to-HTTP proof unchanged. Test changes and harness creation stay with the test worker. Its manual driver has no meaningful Red; syntax, independent strict checking, inspected effect containment and the same C S3 review replace TDD for that helper alone. No real invocation occurs during authoring or C-C.

#### Frozen C assignment and remaining D5 literals

Workflow `M203-20260908`, slice `M203-C`, initial assignment `M203-C-PREFLIGHT-01`, phase preflight, attempt 1, role `test_worker`, owner `m203-c-test`; lease, digest, correction parent and accepted test boundary: None. A/B acceptance, ENTRY, LITERALS-FINAL and M203-UI-01 establish readiness. TDD applies to the existing App's missing guidance behavior; use ordinary behavioral Red through the App, without an eager import of a missing module masking those assertions. Preflight is read-only: inspect the five test and eleven production paths, existing coverage and the exact C callers; C-STRICT is its only executable diagnostic. No generated/browser/model effects are permitted in preflight. Return the workflow classification with the isolated gap. Named remaining external uncertainty is actual spoken announcement/200% browser zoom evidence; it does not block implementation and must not be substituted with DOM, CSS zoom or device-scale evidence.

Freeze exactly eleven production files: `src/client/App.tsx`, `main.tsx`, new `finding-guidance-admission.ts`, `components/results/ResultsSection.tsx`, `ResultDetail.tsx`, `ResultCard.tsx`, `resultPresentation.ts`, new `FindingGuidance.tsx`, `FindingOutcome.tsx`, `GuidancePassages.tsx`, and `styles.css`. The per-responsibility dispositions in M203-UI-01 are accepted **REUSE_AS_IS**, **EXTEND** and **CREATE** as recorded. No hook or additional helper is presently justified. App keeps necessary shared coordination; pure response admission, action/status, outcome and citation list have their named owners. Browser code may import the existing pure domain readers/types, fixed reference metadata and notices; no filesystem, crypto, Node utility, model, corpus loader or service implementation import is allowed. Same-owner private helpers may be reorganized after Green without changing these responsibilities or accepted tests.

`AppProps` adds optional `retrieveFinding(intent)` returning `Promise<unknown>`, with an exact `{runId, findingId}` intent. `main.tsx` supplies only the fixed same-origin JSON POST. The existing test bridge adds a guidance callback and a third `guidance = false` argument to `mount`/`rerender`, preserving every legacy default. The UI harness may allow the existing pure `src/server/retrieval` imports needed by validation, while still blocking all external requests. `FindingOutcome` receives `{finding: Finding, providerContext: ProviderContext}`; an isolated synthetic conflict test may dynamically render that component using A's synthetic policy vector. It must not enter App admission, loosen durable validation, change the corpus or be described as actual retrieved conflict evidence. Dynamic import is confined to that test so ordinary App assertions execute independently.

**Action and ownership:** use an explicit **Get guidance** button. Selection alone does not call the service. Reserve synchronously in App before invoking the captured callback; capture the run, Finding, human label and operation token. Keep selection available during a request. A valid outcome updates the original Finding and its per-Finding citation/error cache even if another item is now selected; announcements identify the original item and never move focus. Keep the action's DOM node mounted with `aria-disabled` and a matching handler guard while busy, after an attempt, for non-unprocessed/historical-active data, known retained ownership or uncertain cleanup, and when the callback is absent. No in-place retry follows failure. Clear per-Finding presentation state only when a different successful Analyze run replaces it; preserve it through a failed Analyze. A supported unfinished owner remains known across independent Analyze runs and prevents misleading new guidance availability. Do not invent an owner-transfer, abandon, restart, resume or Generate action.

**Admission and preservation:** the pure admission owner reads only ordinary own-data descriptors into detached values, validates the complete returned run, rechecks current run/Finding and operation identity after reflection, and compares all immutable parent/provider/context/native/sibling/observation data. Success requires the new assessed selected variant and exact view identity; historical unassessed retrieval is not new guidance success. Validate passage order/references, every fixed metadata field, finite matching score, corpus correspondence and complete exact notices using browser-safe constants. Passage text must be nonempty inert text; authentication of its original bytes remains the server's boundary, not a browser cryptographic claim. Reject malformed, mismatched, extra/accessor or stale data without replacing prior evidence. A validated failure may carry the last durable selected progress while preserving all other data; `persisted: false` describes the failed attempt, not an assertion that its included prior run was unsaved. Keep the bounded failure and cleanup uncertainty visible without fabricating support or abstention.

**Presentation:** retain the native evidence region and all scan-only omissions. Add semantically distinct retrieved-guidance and deterministic outcome regions only for Findings. Show sufficiency, exact available evidence references, required blockers and their unavailability reasons, support/missing roles or conflict references where applicable. Distinguish evidence-only **not retrieved** from a completed retrieval with no applicable guidance. Abstention uses **No proposal generated**, the exact application-authored explanation/manual investigation and **No generation provider was called**; the run's exact mode/provider/model may be inspected here as unused generation configuration. Supported means **Eligible for generation**, not a proposal. Retrieval/integrity failure has no support state. Observations retain evidence-only presentation. Card labels may add concise actual workflow status, never `unprocessed` or provider metadata. One existing atomic shared status announces selection, start and outcome/failure without stealing focus.

Render complete canonical passages, source/heading/version/rule/criterion/role/status, direct citations, copyright/attribution and recorded notices as inert content. Label similarity separately from support/confidence. Deduplicate full notices in first-use order beside the passage list, with accessible native disclosures and a clear association from each passage; do not truncate or replace full notices with URLs. Preserve Evidence Light, wrapping, current hierarchy, focus tokens and the bounded Findings panel. No new visual system, animation, dependency, preview, generation/review/batch controls or future workflow regions.

All C test writes use exactly the five-file test envelope; all eleven production files and all other tests are forbidden. Green uses exactly the eleven production files and forbids all tests and server source. Both forbid docs, corpus, evaluation, fixtures, `.codex`, `.agents`, AGENTS.md, PLANS.md, README.md, package/lock/config and all Git mutations. No allowed directory roots. C-C plus separate C-STRICT and C-BUILD establish Green; C-REAL remains gated after helper/implementation acceptance. S3 I7/I8 identity, asynchronous ownership and accessible rendering require a fresh `critical_reviewer` that also reviews the visual evidence. Budgets, conditional corrections and stops remain Ownership above; each turn receives a fresh primary-managed lease. No allowance is reset by the manual helper or presentation work.

#### M203-UI-01 — bounded reuse and visual contract

Primary accepts the bounded C re-review **PASS**, with no remaining Blocker, Major or Minor findings. C-R1/C-R2 are resolved; the reviewer independently passed 39 UI checks, strict and helper syntax, confirmed current hashes/receipt and viewed the two corrected-state screenshots. Final primary C-FULL then passed **448 tests** on `803176917BAB7FFA272E527DD11A247E2A46F2C1C5E8D2B34EE4AB85525BC46F` (prior declared counts with guidance UI now nine); independent strict exits 0. Scratch and `data/runs` are restored empty. C-REAL prerequisites freshly match the original process/start, loopback listener, executable/runner, manifest and all four blobs, and both finite receipts. `netstat` supplies the read-only listener observation because the higher-level Windows query is access-denied. A malformed read-only shell caller was corrected before any runtime effect; no model request was made by prerequisite checks. Current HEAD and absent proof root are confirmed. The existing one-call procedure is authorized to run now; this is not a renewed capacity evaluation.

`M203-C-REVIEW-GREEN-01` is accepted for the bounded re-review. Primary inspected the two-file diff: the existing presentation owner maps only current processed Finding states, and the existing card renders semantic text without new styling or actions. Cohesion is RETAINED. C-C passes 9+30; independent strict and build pass. Fresh compliant receipt `8db862a422e0624f952646eddbc47d0112ca459887c789f1b51e58175c6a44ea`; source/test/config identity `803176917BAB7FFA272E527DD11A247E2A46F2C1C5E8D2B34EE4AB85525BC46F`. All five accepted test hashes are unchanged. Current build hashes: index `925C502E3BAD922CD8C6EAB109A83F3AF6AF8E3C84E9B02A7B0275D0A77A96E2`, CSS `A53CD0EE68F194FEBE59FAB2C480ACB7873E57C9435AEC39B6DBDF4C84558937`, JS `892C7C429DC0B03732DB67651F6864B76EFE36FBD7EE52E56AF6CADAAEE6B45B`. Primary viewed the current desktop screenshot in `temp/m203-ui-f5b327ab-f526-45b8-b646-a1942be93fd2`; the supported state is visibly retained in its card. The explicit extra test/UI turns are now consumed; same-reviewer C-R1/C-R2 re-review is next. No active lease or actual/manual invocation remains in flight.

`M203-C-REVIEW-RED-01` is accepted: primary inspected all three changed tests/helpers and independently reproduced the four missing state labels in the single aggregate card assertion; the eight existing guidance tests and thirty legacy UI tests pass. Independent strict and nonexecuting helper syntax pass. Fresh compliant receipt `191cafaf8836fee84749176bdd410481c03d63ab118b46b1ae2a97b884737e86`; source/test/config identity `279FAA711C734D073C5D47227F3CE5F482B422DBFE340BB8C8D52F6C5D553A2A`. Accepted revised hashes: guidance UI `1ED0811B81EA8BD2C07A6B21776FBCDE12D1DC839FC45C9E1D6382E3661D55E2`, harness `E44F06F2105EDC71D2BBB8011E7D25C0765BA4F795D2E1EC580E35FC1E300993`, real driver `5A0B859143D0940D646D502922AFC5CAA303D89C33CC0423FE2CCF5BAF40C949`. The existing two other test hashes remain unchanged. Manual outcome preparation now precedes acquisition; all new and existing detail assertions remain active. Normal teardown reports no external/page errors and empty scratch. The single additional test turn is consumed.

Next is the sole owner-granted `M203-C-REVIEW-GREEN-01`, owner `m203-c-code`, role `frontend_code_worker`, phase green attempt 1, no parent, with only the two production paths and card literals above. Primary first moves the current exact three-file GREEN-03 build to absent `temp/m203-build-before-review` using the ordinary containment/inventory/hash checks from the previous build disposition, leaving `dist/client` absent for C-BUILD. Original backups remain preserved. C-C (now nine guidance plus thirty legacy tests), independent strict and build are required; the accepted three-path test boundary cannot change. No further correction turn is included.

Owner-approved C-R1/C-R2 correction packet: same task `M2-03`, workflow `M203-20260908` and slice `M203-C`; the consumed original chains remain historical. The explicit additional grant, not a new ID, supplies one test turn `M203-C-REVIEW-RED-01` and one UI turn `M203-C-REVIEW-GREEN-01`. Each starts at guard attempt 1 with no correction parent because neither continues the guard's exhausted three-attempt chain; no guard change or further turn is authorized. Primary keeps separate persistent owners, opens/closes both leases sequentially, accepts fresh Red before Green, then uses the single review correction loop.

Test ownership is exactly `tests/finding-guidance-ui.test.ts`, `tests/helpers/m104-ui-harness.ts`, and `tests/helpers/m203-real-guidance-driver.ts`; all production and other tests are forbidden. Add behavior assertions showing a processed Finding's supported, abstained, failed and unfinished state remains visible after another item is selected, while ordinary unprocessed cards retain their scan-only names. The affected caller-family audit found existing unscoped eligible/abstention outcome locators in both the guidance tests and real driver; scope these to the selected evidence detail so the new list copy cannot create ambiguous strict locators. Preserve every existing semantic assertion, exact proof invocation and output/effect budget. Resolve C-R2 by preparing the controlled template/outcome before acquiring the manual harness, so fallible corpus/validation work cannot strand it. This non-behavioral helper ordering uses semantic inspection, strict and syntax evidence; no fabricated Red, extra helper, manual call or fault-injection machinery. Run C-C, independent C-STRICT and nonexecuting `--check` for the real driver. Ordinary Red must arise only from absent list states; all existing checks must pass.

UI ownership is exactly `src/client/components/results/ResultCard.tsx` and `src/client/components/results/resultPresentation.ts`; tests and every other production file are forbidden. These existing owners project and render the current Finding status; no new module is needed. Freeze concise state-derived list text: supported completed assessment **Eligible for generation**; abstained **No proposal generated**; failed **Guidance failed**; other active/historical unfinished guidance **Guidance unfinished**. Unprocessed Findings show no workflow text, and observations retain only their existing manual-review tag. Status is visible semantic text inside the existing card, not a new action, provider label, raw lifecycle field or color-only cue. Preserve selection, native evidence, grouping, labels and focus. After accepted Red, run C-C and independent strict; preserve the current inventoried build before C-BUILD because card source changes invalidate it. No code changes beyond these two review findings, new models, actual/manual calls, or retry budget are included. Cohesion before Green is None; final disposition must inspect the actual two-owner fit.

`M203-C-REVIEW-01` returned **REVISE** after independent 38/38 UI tests, strict, identity/lease/input checks and actual source/helper review. Major C-R1: `ResultCard` lacks processed Finding state text, so moving selection hides the previous outcome, contrary to REQ-UX-011's visible sibling states. Minor C-R2: `m203Manual` acquires the harness before fallible outcome construction outside its `try/finally`; a corpus/validation failure can skip cleanup. Primary confirms both against actual code. The C-REAL helper itself has no identified blocking defect, but its overall C acceptance prerequisite remains unsatisfied. No actual/manual call is consumed. An additional explicitly bounded review-correction round is pending owner approval; no lease or automatic budget reset is authorized by this review.

C-FULL freshly passed **447 tests across all 13 files**, zero failures, skips, cancellations or todo cases, on source/test/config `02DDC7C297BA4E152B8328ED8AD6CCED9A593AAD743D72288F42160D3293FDCC`. Counts in declared order are 66/68/69/20/12/28/17/21/5/88/15/8/30. Separate primary C-STRICT exits 0; all three scratch roots and `data/runs` are empty afterward. Existing first and corrected builds remain preserved; current build matches GREEN-03. The full suite does not invalidate the review findings or prove actual zoom/spoken output. Primary reconciled README capability and thirteen-file commands plus the two existing UI guidance owners; preliminary checks pass nine changed Markdown documents and 280 local links. Roadmap remains In progress; no task completion or archive is claimed.

GREEN-03 is accepted for review: fresh compliant receipt `f05475a624b127362064215d8f48e35c90788550a9af2ae7eaa3a0ecadaa0344`, unchanged five accepted tests, 93-file source/test/config identity `02DDC7C297BA4E152B8328ED8AD6CCED9A593AAD743D72288F42160D3293FDCC`. Primary inspected the exact abstained-selection branch and the whole C surface; cohesion is RETAINED. C-C passes 8+30, independent strict and build pass. Current build index hash is `4E1B7D0FF3A41ACFBCD269C0598EADE11B69CAB57CD99406E21B061354E947BA`, CSS `A53CD0EE68F194FEBE59FAB2C480ACB7873E57C9435AEC39B6DBDF4C84558937`, JS `5CF9A447DC4CCC2DA4464FDAFE3648D58D9714755E80C15B80CAF181DDB24C90`. Primary viewed the retained `temp/m203-ui-78ea9778-82ca-4622-8e4c-a7591591a1fc` viewport pair; complete passage/notice visual and spoken/actual-zoom proof remain distinct. Fresh S3 reviewer independently reproduced all 38 UI tests and strict successfully, with no external requests/page errors and normal cleanup; review synthesis is still pending. Only primary plan maintenance follows lease closure; no source/test/config drift is accepted.

GREEN-02 closed compliantly with receipt `b015a0eaf95b0338e94a5880e6f9f5177e06c7d16c6dee54b439f3ebd94804f7`; primary inspected its two-line responsibility changes and matched the 93-file fingerprint `B4423FA5632625B9DBAD46D66371D63B5ECEE3DE128D63B2CE51BD3A7EC28CA8`. C-C passed 8+30 tests; independent strict and first build passed. All accepted tests are unchanged, with normal browser cleanup. Cohesion is RETAINED. Read-only authority review then confirmed REQ-A11Y-010's conditional provider-context/no-call announcement is missing when an abstained Finding is reselected. This differs from the corrected version/cleanup defects and is material new evidence supporting conditional attempt 3; it needs no additional product decision.

`M203-C-GREEN-03` is the final conditional same-contract Green correction: same workflow/task/cycle/role/owner and exact eleven-file scope, parent `M203-C-GREEN-02`, attempt 3. Freeze the existing selection literal extension for an abstained Finding only: `Selected <label>. No proposal generated. Unused generation configuration: <mode>, <provider>, <model>. No generation provider was called.` Use the displayed immutable run context; preserve ordinary scan, observation, supported-selection and state-change announcements, one shared status, and focus. App remains the shared selection/announcement owner; no new module or generalized state machine. The bounded change directly resolves the observed missing branch; all tests remain immutable. Semantic branch inspection and the forthcoming manual re-selection sample supplement C-C, independent strict, and rebuilt output. No further automatic Green turn remains after this assignment; review allowances are unchanged.

Before GREEN-03, primary preserves the three freshly inventoried GREEN-02 build files by moving only ordinary repository-contained `dist/client` to absent `temp/m203-build-green02`, checking exact inventory/hash equality after the move. Hashes: index `633F6CA9D0B6DD17A7D34306C6B3101ED06CB19B4F069B7417D3B5A8A6722F15`, CSS `A53CD0EE68F194FEBE59FAB2C480ACB7873E57C9435AEC39B6DBDF4C84558937`, JS `E9BAAC13BD7FAE25F8ED2E5C699E25D1DA09F8CB34C868BEF138594807813ED7`. The source correction invalidates the prior build identity; C-BUILD writes only the again-absent output directory. Earlier ENTRY backup and all other outputs remain preserved.

Primary reaccepts the two explicitly approved test corrections after fresh C-C: the isolated conflict display now passes and only the missing human-readable WCAG version assertion fails; all 30 existing UI tests pass. Independent C-STRICT exits 0, with normal browser cleanup. The accepted guidance test hash is `AC2A4C6B5172C2D3881AC56AF81C33C6EA0F160DF607F6D54F9BF25CDE8B107F`; other four C test hashes remain RED-03 identities. The 93-file source/test/config identity is `5A90EBE99062C24CD022F632E583454DFD8A54107C80F65DC15E0AD315EC43EF`. No production change compensated for a test defect.

Correction packet `M203-C-GREEN-02`: workflow `M203-20260908`, task `M2-03`, cycle `M203-C`, phase green, attempt 2, parent `M203-C-GREEN-01`, owner `m203-c-code`, role `frontend_code_worker`. All original eleven production paths, forbidden paths, responsibility/reuse map, D5 contract, effects and budgets remain binding; the revised accepted tests are immutable. Correct the version label and propagate an admitted Analyze `cleanupFailed` into the existing sticky guidance owner gate. Use semantic branch inspection plus C-C for that cross-operation condition; do not edit tests or add test machinery. Run C-C, independent C-STRICT and the first C-BUILD, then report actual cohesion and screenshots. No real/manual runtime proof is authorized inside this worker turn. Primary opens and closes the fresh lease; current command evidence is replaced only proportionally by those checks.

The owner explicitly approved one additional narrowly scoped correction to the isolated test's React import. Primary applied ADR-0024's bounded coordinator test exception between leases, only in `tests/finding-guidance-ui.test.ts`, because the existing guard permits at most three worker turns and no worker-chain reset is authorized. No guard or workflow change was made. Normalize the raw dynamic React module namespace to its CommonJS default before reading `createElement`/`createRoot`; preserve every behavioral assertion and all other tests. That grant is consumed. The owner subsequently approved the separate one-line assertion correction replacing the stale normative passage ID with the fixture's actual `understanding111-intent` conflict member. Primary makes only that test-side change between leases and reruns C-C and independent C-STRICT before reacceptance. Neither grant renews the general test budget or authorizes other corrections.

`M203-C-GREEN-01` stopped with 6/8 new UI tests and all 30 existing UI tests passing; strict TypeScript passed. Primary freshly closed its exact lease compliantly (receipt `0901ecc08675ea1e0f0aea52dcd3b09b5e38e8dfdb015a490179973292542680`) and inspected all nine changed production files, with four new cohesive owners. The 93-file source/test/config identity is `28C59A9B73BE23972F62D967892639D30BA03F5DCEF439334997F55D78DDFF22`; all five test hashes are unchanged. Build was not run; `dist/client` remains absent. Both UI harnesses closed normally with no external requests or page errors and empty scratch. The new-region axe and 320px overflow checks passed. Primary inspected retained synthetic screenshots in `temp/m203-ui-73ded428-2012-4860-91d2-5f87c16f2f77`; they show native evidence/reflow and the guidance boundary but do not provide full passage/notice visual coverage.

The isolated conflict test originally threw `createRoot is not a function`: Vite's raw CommonJS namespace differs from a transformed named import. The owner-approved primary import-only correction normalizes the module defaults; the revised test hash is `4448096C2FF8033585911461FAD572F40AAC0A2EF1FF1EDB035EBE4955E79641`. Fresh C-C now renders the isolated component but exposes the stale expected ID `wcag22-sc111`: the fixture actually declares `h37-text-alternative` and `understanding111-intent`. Results remain 6/8 new UI and 30/30 existing UI; independent C-STRICT passes, with normal browser cleanup and empty scratch. A separate owner grant is requested to replace only that stale expected ID; do not add an unrelated citation to production to satisfy it. Prior C test evidence remains invalid pending correction and reacceptance. Remaining production corrections within the existing Green budget: show the expected human-readable WCAG version alongside canonical corpus metadata, and carry Analyze cleanup uncertainty into guidance admission rather than only displaying its Analyze error. No completed Green/cohesion/review claim is made. The guard's lowercased Windows change labels do not indicate a path or Git metadata mutation.

`M203-C-RED-03` is accepted after primary actual-diff inspection, independent C-C reproduction (eight expected missing-action failures; all 30 existing UI tests pass), independent strict/helper syntax (exit 0), matched 89-file identity `2847C22C0047F297900C748B938D5F4197BB997A844C82DDF2E2C8890609F57C`, and fresh compliant receipt `0e46003e9219a6ef5052a3436ec75fc167311f7b56ed664181a0dd0b3455df47`. Both harnesses reported no external requests/page errors and normal cleanup; UI scratch is empty. The five accepted test-file hashes are: guidance UI `FA7A6D5D4CB5576FECF98FE2E44A1540D3D1BB47C3A0DEA18E255D64B51E1153`, existing UI `997F1FCCE1C51CA74637AE74FF84DBE4B05786A26C897DC0CBEF687C5C48914E`, harness `767DF4DBF10446A9DD22AD7F8085774082D598EE479E655E6EBB3ACA96AD4775`, walking skeleton `52E978B19CC54A1ABD1936E50C0137AA240707C66CBB21D3C5975C4FFBB070A7`, real driver `D883722F559C31A957A0A54472BB692B798A203445AC94ACD33B208BB15A040F`. The manual path prepares only its fixed synthetic Analyze result, leaving selection/guidance to the operator; it has not been run. C-REAL remains unused and subject to helper S3 review. All eight deeper UI assertions still require unchanged-test Green.

Primary executed the frozen C-BUILD disposition once: the three matching ENTRY files are preserved at `temp/m203-build-before-01`; `dist/client` is absent. `M203-C-GREEN-01`, owner `m203-c-code`, frontend_code_worker, green attempt 1, no parent, uses exactly the eleven production files and the frozen reuse/D5 contract. All tests/server/docs/config/input paths remain forbidden. C-C, separate C-STRICT and C-BUILD are the only executable verification effects; no C-REAL/manual call during Green. Default-open native notice disclosures retain complete readable text while allowing users to collapse them; source notices remain next to their passages. Primary will inspect actual cohesion, retained screenshots and terminal receipt before fresh S3 review. A successful build may be retained for the single real proof; no second build disposition is allowed.

`M203-C-RED-02` closed compliantly, receipt `68ed7c32f76f920b633381ca6dd1495a15de163ff6e6b1d9d031dc67689f748e`; primary inspected the corrected complete immutable projection, same-priority conflict construction and announcement wait. Strict/helper syntax pass, eight ordinary App failures and 30 existing UI passes recur with clean harness shutdown. Concrete progress is established. A subsequent full helper/caller-family audit found different latent mismatches: API-only walking health incorrectly expects guidance enabled, the manual path combines a rich initial collection with an M102-only outcome, its criterion score differs from the frozen literal, and the real helper accepts either outcome label without comparing displayed citations to the actual returned view. These are test/proof defects, not reasons to change accepted production behavior.

Primary authorizes the final conditional `M203-C-RED-03`, red attempt 3, owner `m203-c-test`, parent `M203-C-RED-02`, unchanged five-file scope and commands/effects. Correct API-only guidance to false (the other two remain true); make manual initial/outcome use the same M102 collection and matching submitted target/provider context with fixed scores 0.8/0.7; compare the real UI's specific outcome, full passage text/direct citation and full notices with fresh canonical resolution before its receipt. Keep the new helper effect-free until its explicit proof command. These are different bounded actions grounded in exact inspected call sites, expected to remove the remaining test-side contract mismatches; no evaluation, production or effect expansion occurs. This uses the remaining conditional correction allowance. An unsuccessful third handoff or further required test correction returns to the owner budget boundary; IDs and roles cannot renew it. Green remains unused.

`M203-C-RED-01` closed compliantly (contract `82dacc46ea8d92f6e4fba84cb617009f31c5a060a7f9bb570099b6f9989f47c1`, receipt `22161afa09ebd7d06c712962d8c43fa59d75943c6e2f5c253bb42c8d14dc7a66`). The guard's active-pointer digest signs a different payload and is not the contract pin; primary pinned status and closure confirmed no drift or violation. Eight new App cases fail on the absent Get guidance action, all 30 existing UI tests pass, strict and helper syntax pass, and both harnesses close with empty scratch and no external requests/page errors. Red assertions are reachable, but the full test boundary is not yet accepted: the real helper must compare the returned immutable projection against its durable seed, and the isolated conflict sample must use the same-priority informative pair allowed by A. The initial helper's consistency-only comparison cannot support a preservation claim. These routine corrections remain in this plan under the bug index's task-contained exception.

`M203-C-RED-02`, same owner/role/phase and exact five-file scope, attempt 2 with correction parent `M203-C-RED-01`, corrects those two issues and removes incidental asynchronous timing from the original-Finding announcement assertion. No production or effect change is authorized. C-C, independent C-STRICT and helper syntax must be rerun before acceptance; the original unaccepted test evidence is superseded. The remaining allowance is one conditional third attempt only with concrete new evidence and primary justification; Green remains unused. Repository readback is mandatory for every actual outcome; the same service returns equal data after terminal abstention or expected `busy` while a supported owner remains. This clarifies the existing repository/service proof without permitting another service or restart.

`M203-C-PREFLIGHT-01` is accepted as PARTIAL: the existing App, selection, native evidence, focus and responsive foundations are present, but guidance activation/admission/detail behavior is absent. Strict TypeScript passed with no diagnostics; the unchanged 87-file identity is `F81B2CBF19131322C0E0D531872E67A517988FFD83BBC86F250785EE34746AFF`. No writes or external effects occurred. Ordinary Red must exercise the existing App; the missing new modules cannot substitute for a behavioral failure. `M203-C-RED-01`, owner `m203-c-test`, phase red, attempt 1, uses the five frozen test files, no correction parent, C-C plus independent C-STRICT/helper syntax checks, and the previously frozen effects/budgets. Primary will record its fresh lease digest before dispatch. Spoken output and actual browser zoom remain separate proof obligations.

The primary's source audit supplies these starting dispositions; accept the completed D5 capsule before C preflight. Governing anchors are the task's `REQ-A11Y-*`, `REQ-UX-011`, SPEC-002/003, the [selected-evidence contract](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#selected-evidence), and the [frontend-quality skill](../../../.agents/skills/frontend-quality/SKILL.md).

| Responsibility | Disposition and placement |
| --- | --- |
| Native evidence and complete results panel | `REUSE_AS_IS`: `RuleEvidence.tsx`, `FindingsPanel.tsx`, `ResultsOverview.tsx`; preserve full grouped collections and panel scrolling. |
| Selected detail composition | `EXTEND`: `ResultDetail.tsx` and its `ResultsSection.tsx` parent pass current Finding/action/view props. Observations retain evidence-only presentation. |
| Operation coordination and announcements | `EXTEND`: App's existing reservation/current-run and shared live-status pattern; stable selection and late-response rejection. `main.tsx` adds only fixed same-origin transport wiring. |
| Retrieved passages/citations and source notices | `CREATE`: `GuidancePassages.tsx`, a cohesive readable list of at most three exact passages with their source metadata and accompanying notices. Native disclosures are permitted for complete notices. No Markdown interpreter or corpus browser. |
| Guidance activation/status and deterministic outcome | `CREATE`: `FindingGuidance.tsx` for action/status composition and `FindingOutcome.tsx` for sufficiency/abstention/failure explanation. No provider or proposal panel until its owning task. |
| Selected-response admission; bounded workflow label | `CREATE`: pure `finding-guidance-admission.ts`; `EXTEND` result presentation/card only as needed to keep sibling workflow states understandable without restoring raw scan metadata. |

State samples: unprocessed Finding, native scanner-review observation, retrieval running, supported/complete evidence, incomplete evidence, each completed insufficient support state, execution/integrity failure, and unsaved/cleanup-uncertain action failure. Use these as a compact semantic test set, not every state × browser × viewport combination. Supported means eligibility only. No Generate/review/retry action appears. Abstention shows its blockers, reason, no-provider-call confirmation and manual investigation with the applicable immutable run context where provider-relevant; ordinary scan-only Results retain their metadata omissions.

Browser samples: existing desktop layout at 1280×800, 320×800 reflow, and actual browser 200% zoom at desktop width. Use a long canonical passage/citation/notice sample to inspect wrapping. Keep all text inspectable without clipping. Selection leaves focus on its card; action completion/announcements do not steal focus; after citation inspection keyboard navigation can return predictably to the selected card without adding the prohibited Back-to-findings control. A single shared announcement pattern states selected item, start, outcome/failure and no-generation-call fact where relevant. Capture build/source identity, browser/version, viewport/zoom, controlled state, actions and result. Screenshots are optional supporting evidence, kept ignored and synthetic only.

Run one automated accessibility check over the new regions and one bounded keyboard/announcement path. Check an actual screen-reader path for the newly required abstention announcement when the available tooling/operator permits it; DOM inspection is not spoken-output proof. Record missing assistive-technology evidence honestly and complete unaffected work; obtain operator evidence if needed to discharge the task's announcement requirement. The complete proposal/review/comparison smoke remains M6-03. Preserve Evidence Light and current tokens; no new themes, fonts, icon kit, animation or dependencies.

## Concrete Steps

All commands run from `C:/projects/a11y-evidence-lab` in PowerShell. Before any Node command, load exactly the first PowerShell block under [maintained developer preparation](../../../README.md#development-command-preparation); it defines values/functions without effects. Do not execute the adjacent `npm ci` block. Current installed dependencies and retained browser/build may be reused after inspection; this task plans no acquisition or dependency restore.

### C0 — read-only execution entry

```powershell
git status --short
if ($LASTEXITCODE -ne 0) { throw 'Git status failed' }
git rev-parse HEAD
if ($LASTEXITCODE -ne 0) { throw 'HEAD inspection failed' }
git diff --cached --name-status
if ($LASTEXITCODE -ne 0) { throw 'Index inspection failed' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'Whitespace check failed' }
```

Primary also records the symbolic ref/logical index, changed-path ownership, guard state, relevant source/test/config/authority fingerprints, all frozen fixture/corpus inputs and generated-output inventory in `M203-ENTRY-01`. Verify required Python only before guard use. Expected: known intentional tree, passed dependency/authorities, no unreconciled active lease. A plan commit is ordinary intentional movement: reconcile it and use the actual execution baseline for every lease/closure check. Never require the historical planning HEAD or an untracked plan.

### C1 — safe planning baseline / C-STRICT

After the maintained preparation, the exact safe check is:

```powershell
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Wrong Node runtime' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/retrieval-contract.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Pure contract tests failed' }
}
```

Expected planning baseline: exit 0, strict check passes, 93 tests pass. The strict-only prefix inside the same wrapper is C-STRICT for future slices. Test counts may change during execution and never become a requirement to suppress new cases. These commands create no build, run, browser or model output; compilation caching is disabled and the prior environment restored by the maintained wrapper.

### Future command package — freeze before use

#### C-PREP and C-A — prepared policy caller

The exact effect-free extraction below selects only the maintained preparation block, never dependency restoration. It is followed by C-A after the new test exists. The entry C1 already verified the runtime, strict checker, runner and existing retrieval-contract tests. C-A may read the fixed corpus and use test-local in-memory vectors; it creates no generated files, browser, service, model call or persistent record. C-STRICT runs independently after Green. Working directory is the repository root. Red's expected nonzero code is inspected, not reported as Green; the explicit throw propagates it. The wrapper restores the prior compile-cache environment. No cleanup is needed for this pure package.

```powershell
$readmeText = Get-Content README.md -Raw
$prepSection = $readmeText.Substring($readmeText.IndexOf('## Development command preparation'))
$prepMatch = [regex]::Match($prepSection, '(?s)```powershell\r?\n(.*?)```')
if (-not $prepMatch.Success) { throw 'Missing maintained preparation' }
. ([scriptblock]::Create($prepMatch.Groups[1].Value))
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/finding-sufficiency.test.ts tests/retrieval-contract.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Policy contract tests failed' }
}
```

C-STRICT uses the same preparation and wrapper with only `& $m105Node node_modules/typescript/bin/tsc --project tsconfig.json` followed by `if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }`. The primary syntax-checks the actual caller before dependent preflight, then pins source/test/environment identity in its packet. C-LEASE uses the guard's exact start projection from that packet and consumes its returned digest for a fresh `closed-compliant` close; no worker controls the lease.

The following slots are deliberately not runnable permission. Primary records the exact complete caller, prerequisites, paths, expected outputs, native exit propagation, environment restoration, owned side effects and cleanup before the relevant preflight/lease. Syntax-check compound PowerShell plus read-only prerequisite checks; never let workers improvise binding commands after review.

#### C-B — prepared durable/API caller

After C-PREP, run the following files sequentially in the maintained wrapper; C-STRICT is an independent command after Green. Existing tests own exclusive `temp/m102-store-*`, `temp/m102-service-*` and `temp/m202-retrieval-*` roots, bounded loopback listeners and their child processes. Preserve their existing containment/topology and normal-stop cleanup checks. The new API test may own only exclusive `temp/m203-guidance-api-*` roots beneath the verified repository temp directory, with the same ordinary-path checks, exact recorded root/process ownership, normal stop and port closure before deleting that root. It may use in-memory callback/executor doubles and project-owned native Findings; no default model call or network target is allowed. Existing local-service entry tests use the retained inventoried client build. No browser, corpus/evaluation writes, dependency acquisition or broad scratch deletion occurs. Mutable filesystem/socket evidence is non-reusable unless a particular isolated test identity is recorded. Before B preflight confirm A acceptance, the existing temp parent/build identities and this caller's syntax.

The existing entry-point reopen/deletion case also owns exactly two fresh `data/runs/m102-demo-<UUID>` children. Its existing current-directory snapshot, exclusive ID tracking, direct-child/ordinary-path/inventory checks, normal entry-child stop, exit and port closure precede deletion of those exact directories. Require the original `data/runs` inventory afterward; it was empty at entry. This explicitly records the effects already inherent in the selected complete local-service caller; it authorizes no other application-data mutation or cleanup.

```powershell
Invoke-M105Command {
  $m203FailedTests = @()
  foreach ($m203Test in @(
    'tests/finding-sufficiency.test.ts',
    'tests/finding-guidance-api.test.ts',
    'tests/retrieval-service.test.ts',
    'tests/run-contract.test.ts',
    'tests/run-repository.test.ts',
    'tests/local-service.test.ts'
  )) {
    & $m105Node --test --test-timeout=120000 $m203Test
    if ($LASTEXITCODE -ne 0) { $m203FailedTests += $m203Test }
  }
  if ($m203FailedTests.Count) { throw ('Durable/API tests failed: ' + ($m203FailedTests -join ', ')) }
}
```

#### C-C, C-BUILD and C-BROWSER — prepared presentation callers

Before C preflight, primary verified the four ordinary empty scratch directories (`m103-scan`, `m104-ui`, `m105-integration`, `m104-setup`), the existing three-file build against ENTRY, and the retained browser. Its 318 files hash to `D13BAF13721B7E65EAE1312EAFB48FDBC51E8559E0C166E1591AE57C8F3BDA3F` using sorted repository-relative `path:SHA256` rows joined with LF; the dependency marker remains `2026-09-01T19:46:51.6545663Z`. Chromium 1234/version 151.0.7922.34, Playwright 1.62.1, axe 4.13.0 and existing Node/TypeScript/React/Vite pins remain the prepared environment. Recheck ordinary topology and scratch emptiness at each caller; no acquisition or runtime repair is admitted. The following four C callers were parsed as complete PowerShell statements before preflight, without execution.

C-C uses the existing M104 harness's generated `m104-test-entry.html`, `m104-test-entry.tsx` and `vite-cache` in empty `temp/m104-ui`, with its preserved browser inventory, loopback-only Vite listener, context/browser close, port-closure proof and exact scratch cleanup. The new UI test may additionally create one exclusive `temp/m203-ui-<unique>` directory per invocation for exactly `desktop.png` and `narrow.png`, each a viewport screenshot at 1280×800 or 320×800. Validate ordinary contained paths and absence before creation; retain these small synthetic review artifacts and report their root/source/test/browser identity. Never overwrite or delete a prior evidence directory. Browser requests remain confined to the owned origin; no scan or model call. Run one added axe check and one bounded keyboard path through the new regions, including focus preservation and predictable return to the selected card. Existing UI regression checks remain unchanged except necessary additive assertions.

```powershell
Invoke-M105Command {
  $m203UiFailures = @()
  foreach ($m203UiTest in @('tests/finding-guidance-ui.test.ts','tests/target-results-ui.test.ts')) {
    Assert-M105EmptyDirectory $m105UiTemp
    & $m105Node --test --test-timeout=120000 $m203UiTest
    if ($LASTEXITCODE -ne 0) { $m203UiFailures += $m203UiTest }
  }
  if ($m203UiFailures.Count) { throw ('UI tests failed: ' + ($m203UiFailures -join ', ')) }
} $m105UiTemp
```

The real/manual helper preparation is checked by `& $m105Node --check tests/helpers/m203-real-guidance-driver.ts` inside the maintained wrapper with an explicit nonzero-exit throw, plus separate C-STRICT. These checks must not import or execute the helper. It only runs its effects when explicitly invoked with the exact C-REAL arguments below. No stub, synthetic model result or conditional skipped behavioral test is permitted.

C-BUILD first preserves the exact ENTRY build between leases. Execute this parsed disposition once after C Red acceptance and before C Green, never during preflight. All subsequent build output belongs only to absent `dist/client`; retain it for C-REAL and task closure. An unsuccessful build retains its outputs for primary inspection, not blind deletion or repeated disposition.

```powershell
$m203OldBuild = Assert-M105OrdinaryPath $m105Build
$m203BuildBackup = Assert-M105OrdinaryPath (Join-Path $m105Repo 'temp/m203-build-before-01') -AllowMissing
if (Test-Path -LiteralPath $m203BuildBackup) { throw 'Build backup already exists; do not repeat disposition' }
$m203ExpectedBuild = [ordered]@{
  'index.html' = '6F81570B534AF725BD6CFCCC31DA782CA722B2565ECD94CC63FCF2AE44E5A6D1'
  'assets/index-BnKNSoiv.css' = 'C4800A2375D986E09CDB7DE5D0A28354E79521BF89FF90FF24291F64C0BD5517'
  'assets/index-nORRdglA.js' = 'C6D3AF7BA7EC199E68F62EE715C38152A34DF6E140457AFB1A33E61B682A93B1'
}
$m203BuildEntries = @(Get-ChildItem -LiteralPath $m203OldBuild -Recurse -Force | ForEach-Object {
  $null = Assert-M105OrdinaryPath $_.FullName
  [IO.Path]::GetRelativePath($m203OldBuild,$_.FullName).Replace('\','/')
} | Sort-Object)
if (($m203BuildEntries -join '|') -cne ((@('assets') + @($m203ExpectedBuild.Keys) | Sort-Object) -join '|')) { throw 'Unexpected retained build inventory' }
foreach ($m203Leaf in $m203ExpectedBuild.Keys) {
  if ((Get-FileHash -LiteralPath (Join-Path $m203OldBuild $m203Leaf) -Algorithm SHA256).Hash -cne $m203ExpectedBuild[$m203Leaf]) { throw 'Retained build mismatch' }
}
Move-Item -LiteralPath $m203OldBuild -Destination $m203BuildBackup -ErrorAction Stop
foreach ($m203Leaf in $m203ExpectedBuild.Keys) {
  if ((Get-FileHash -LiteralPath (Join-Path $m203BuildBackup $m203Leaf) -Algorithm SHA256).Hash -cne $m203ExpectedBuild[$m203Leaf]) { throw 'Build backup mismatch; preserve both paths' }
}
if (Test-Path -LiteralPath $m203OldBuild) { throw 'Original build path remains; preserve both paths' }
```

Green uses:

```powershell
Invoke-M105Command {
  & $m105Node node_modules/vite/bin/vite.js build --configLoader native
  if ($LASTEXITCODE -ne 0) { throw 'Client build failed' }
}
```

C-BROWSER uses C-C's source-backed real Chromium evidence and separately the built-client C-REAL path. Actual 200% browser zoom and a newly spoken abstention announcement remain an operator/tool observation, never a CSS/device-scale or DOM substitute. The existing manual harness may add only `--m203-manual`: actual App, a complete M102 controlled run `m203-manual-01`, and a 1500-ms synthetic guidance collaborator for `finding-0`. Its fixed package contains `wcag22-sc111` at 0.8 and `h37-text-alternative` at 0.7; use A's resolver/policy and construction with the existing fixture timestamps. This yields an incomplete-guidance abstention and both complete recorded notices for the keyboard, zoom and spoken-announcement path. The session uses the same scratch/browser preservation and at most 15 minutes; closing its page or Ctrl+C requests normal cleanup. It performs no runtime/model call. Its exact caller is:

```powershell
Invoke-M105Command {
  & $m105Node tests/helpers/m104-ui-harness.ts --m203-manual
  if ($LASTEXITCODE -ne 0) { throw 'Manual UI proof did not close successfully' }
} $m105UiTemp
```

#### C-REAL — frozen single actual guidance proof

The owner has already authorized this single bounded proof; no further grant is needed when its prerequisites pass. Preparation has observed the existing developer-owned Ollama process (PID 12588, started `2026-09-08T16:27:20.8238758Z`) and loopback listener `127.0.0.1:11434`. Reuse this session without starting, stopping or reconfiguring it. Its retained executable hash is `E4FE6BD835FE146659F5C969DCCAFF2E25A9DE63D90EE204CA5D11B9034B0CA5`; runner hash `D02F2D584BA38C12F3CCC2C66342E7C9F1911372C90349B8E89CEE3A338E73A9`. Manifest and all four referenced blob sizes/hashes match the approved M2-02 identities. The accepted finite sequence/context receipts still match `F4D4FF0C19DD11A20F03B6C385D27D7F0524C213533F9D9E23D3385A7F94FC57` and `B6C46D28B936A419BB5AE24D1633D442E012E568737EBFE19198932DAB8EAFDF`. Recheck these fixed prerequisites before the proof, without tokenizer replay or extra runtime API probes. Absent or changed prerequisites stop the call; do not acquire or launch a replacement.

The prepared driver runs from its tracked helper path, only with `--real-guidance-once` and the actual 40-character HEAD argument. It requires absent ordinary `temp/m203-real-01`, captures source/test/build/corpus identities, and owns only that new root plus its managed browser's normal temporary files in empty UI scratch. Seed `runId: m203-real-01`, `findingId: finding-0` from the existing complete M102 controlled factory through production repository `create`/`finish`. Store the seed as `seed.json`; canonical run data stays under `runs/m203-real-01/run.json`. Serve the actual built App with `startLocalService` and this owned run root. The browser may intercept only `POST /api/runs` to return this already durable controlled seed with matching submitted target/mode, avoiding a public or duplicate scan. All other owned-origin traffic continues to the real service; all external browser requests are rejected. Select the first Finding and activate the actual **Get guidance** control exactly once. Its `POST /api/finding-guidance` must reach B and the default, uninjected production executor.

Invocation budget: one fresh service collection and one selected guidance request, at most 16 canonical document embeddings plus one privacy-safe query embedding. The unchanged executor makes seven metadata requests, or eight when initial model loading requires the additional residency check: at most 25 Ollama requests total and zero generation requests. This source-derived upper bound is not an independently observed HTTP request count. Preserve the executor's existing 300000-ms deadline; use a 330000-ms outer browser/driver ceiling and the service's existing bounded stop. No retry, quality sweep, embedding probe, model download, public scan or capacity benchmark. Failure after the guidance request consumes the call allowance; preserve evidence and finish unaffected work without another model invocation.

Success requires a validated assessed selected outcome, the actual UI's matching support/abstention presentation and citations, complete response/view validation, repository/service readback equal to the returned durable run, and every parent/context/native/sibling/observation field preserved. Accept the actual ranked support outcome without changing ranking or assuming the prior capacity result recurs. Retain only `seed.json`, the owned `runs` tree, `real-evidence.json`, `desktop.png` and `narrow.png` under the proof root. The receipt records source/build/browser/input/outcome/readback and cleanup evidence, without provider payloads, credentials or copied private runtime logs. Close the owned context/browser and application service normally, verify the application port closed, preserve the developer runtime and all outputs, and require scratch restored. No forced termination counts as cleanup success.

```powershell
Invoke-M105Command {
  & $m105Node tests/helpers/m203-real-guidance-driver.ts --real-guidance-once 84851520c34e75b9bc1fa8702f8ee0ae1087852a
  if ($LASTEXITCODE -ne 0) { throw 'Real guidance proof failed; preserve its outputs and do not repeat' }
} $m105UiTemp
```

C-REAL may run only after C implementation/helper inspection, strict/build and fresh C S3 acceptance. The command's HEAD is the reconciled current execution identity, not the historical planning HEAD; recheck it rather than creating a commit. C-FULL is frozen below after successful PowerShell AST parsing and exact discovery equality with all thirteen `tests/*.test.ts` files. It uses the unchanged definitions-only C-PREP first. Existing B owned test directories, two exact entry-test run children, scanner/browser and UI effects and cleanup remain binding. Run sequentially without another browser test or application service; the fresh C reviewer has finished its focused runtime checks before this caller begins.

```powershell
Invoke-M105Command {
  $m203FullFailures = @()
  foreach ($m203Test in @('tests/run-contract.test.ts','tests/run-repository.test.ts','tests/local-service.test.ts','tests/scan-normalization.test.ts','tests/retrieval-contract.test.ts','tests/embedding-retrieval.test.ts','tests/retrieval-service.test.ts','tests/finding-sufficiency.test.ts','tests/finding-guidance-api.test.ts')) {
    & $m105Node --test --test-timeout=120000 $m203Test
    if ($LASTEXITCODE -ne 0) { $m203FullFailures += $m203Test }
  }
  if ($m203FullFailures.Count) { throw ('Core tests failed: ' + ($m203FullFailures -join ', ')) }
}
Invoke-M105Command {
  foreach ($m203Test in @('tests/scan-page.test.ts','tests/walking-skeleton.test.ts')) {
    Assert-M105EmptyDirectory $m105ScanTemp
    Assert-M105EmptyDirectory $m105IntegrationTemp
    & $m105Node --test --test-timeout=120000 $m203Test
    if ($LASTEXITCODE -ne 0) { throw ('Browser integration failed: ' + $m203Test) }
  }
} $m105ScanTemp
Invoke-M105Command {
  $m203UiFailures = @()
  foreach ($m203UiTest in @('tests/finding-guidance-ui.test.ts','tests/target-results-ui.test.ts')) {
    Assert-M105EmptyDirectory $m105UiTemp
    & $m105Node --test --test-timeout=120000 $m203UiTest
    if ($LASTEXITCODE -ne 0) { $m203UiFailures += $m203UiTest }
  }
  if ($m203UiFailures.Count) { throw ('UI tests failed: ' + ($m203UiFailures -join ', ')) }
} $m105UiTemp
foreach ($m203Scratch in @($m105ScanTemp,$m105UiTemp,$m105IntegrationTemp)) { Assert-M105EmptyDirectory $m203Scratch }
```

| Slot | Required exact content and boundary |
| --- | --- |
| C-LEASE | Current guard `start`/`close` callers projected from each exact Packet v2; consume returned digest and structured status; require fresh `closed-compliant`. No worker manages the lease. |
| C-A | `Invoke-M105Command` around pinned Node `--test --test-timeout=120000 tests/finding-sufficiency.test.ts tests/retrieval-contract.test.ts`, followed by independent C-STRICT. Future file must exist after Red; no effects outside admitted test boundaries. |
| C-B | Same runner for `finding-sufficiency`, `finding-guidance-api`, `retrieval-service`, `run-contract`, `run-repository`, `local-service`; freeze exact existing helper/run-root and per-file sequence, then C-STRICT. No live model calls. |
| C-C / C-BUILD | Exact new detail-UI/affected old UI commands with the retained managed browser and existing UI scratch preparation; `vite build --configLoader native` only after handling current `dist/client`. Preserve or explicitly replace the inventoried generated build; never delete an uninspected output. C-STRICT remains independent. |
| C-BROWSER | One controlled application/API/browser path plus the visual/keyboard/announcement samples. Prefer existing M104/M105 helpers and actual service/publication; pin any required test-owned adapter seam and generated root before its test lease. Test doubles are labeled and confined to tests. |
| C-REAL | One bounded actual UI-to-HTTP-to-default-retrieval-to-disk proof using a project-owned controlled Finding and current approved M2-02 runtime/configuration. Reuse its passed capacity gate; check present prerequisites before the call. Freeze invocation count, input, launch/stop, source/build identities, outputs and result criteria. No new model acquisition, capacity benchmark, live public target or generation call. The full three-gold evaluation remains M2-04. |
| C-FULL | The current [complete suite](../../../README.md#build-and-verify-the-walking-skeleton), plus all new task tests, sequentially with their exact scratch requirements, strict TypeScript and client build. Audit the final list against all authoritative test files; run once at task closure unless changed evidence/risk justifies another run. |
| C-CLEAN | Exact owned files/directories/processes from command inventories, normal stop first, absolute containment/topology/ownership checks, then only permitted cleanup. Retain pre-existing M2-02 evidence, developer runtime/model/tokenizer trees, dependency backups, build and shared scratch unless explicitly dispositioned. No broad root deletion. |

## Validation and Acceptance

### M203-FINAL-01 — closure evidence and explicit manual-check deferral

On 2026-09-09 the owner directed completion using collected evidence and initially deferred remaining manual checks. The subsequent explicit direction limits manual portfolio verification to visual elements and removes manual screen-reader testing from scope. The amended [REQ-A11Y-006](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md#accessibility-of-a11y-evidence-lab) controls verification; this supersedes the original manual speech requirement in C-BROWSER and I8, not expected keyboard, focus, announcement or reflow behavior. M6-03 remains Not started and owns deferred full-guidance/detail readability and reflow at actual 200% browser zoom. No spoken abstention/reselection or complete-detail zoom result is claimed. The operator screenshot establishes actual Chromium menu zoom at 200% and readable form content; reported speech establishes form reading only. Retained proof screenshots show the list and top of detail, not a complete visual inspection of passages and notices.

The operator closed the visible test window. Primary independently confirmed loopback port 5173 closed, all four scratch directories and data/runs empty, and no active lease. These observations establish the closing state, not an unobserved terminal exit code or a completed manual smoke path. The complete 448 automated suite, independent strict/build, 39 UI checks including axe/keyboard/narrow rendering, critical slice/helper reviews and the actual abstained UI-to-retrieval-to-disk proof remain accepted on unchanged sources.

The different fresh final critical reviewer independently revalidated the seed/run, exact native/sibling/parent projection, canonical citations and browser admission, legal activation/completion and rejected direct/reverse transitions, ten forged/accessor envelope rejections, and LF/CRLF equivalence. Source fingerprint and all 94 actual-proof entries match. The final verdict is **PASS WITH FOLLOW-UPS**, accepted by primary: no Blocker or Major findings, and production cohesion is RETAINED. The reviewer also matched frozen inputs and all 16 compliant terminal lease receipts. The documentation-only R1 disposition below and owner-authorized visual deferral are the bounded follow-ups; neither blocks the amended M2-03 gate.

Minor M203-FINAL-R1 is dispositioned as a limitation of the consumed proof helper: its unexecuted supported/active branch expects service readback to be busy, but the settled service returns its owned active run with interrupted false, as the accepted retrieval-service test asserts. Only the abstained/equals-disk branch was exercised by the successful actual proof. That proof remains valid; no supported actual-proof or reusable helper claim is made. Do not reuse this helper's supported branch without separately authorized correction and validation. No production or test change, additional model call, or renewed correction allowance is authorized by this disposition.

Documentation impact: Updated capability/developer and lifecycle summaries, REQ-A11Y-006 and its requirements-index/OD/ADR/spec/UI/skill verification mirrors, roadmap/progress status and plan navigation. Sixteen changed Markdown files passed 549 local links and UTF-8/newline/whitespace checks before closure edits; all 16 README/plan PowerShell blocks parsed, and git diff --check passed. Final status and archive-link checks passed after archival: all 16 changed/current Markdown files, 549 local links, UTF-8/final-newline/whitespace, all 16 PowerShell blocks and git diff --check. No stale inbound link to the former active-plan path remains. The 93-file source/test fingerprint still matches, HEAD/index are unchanged, and no active lease or owned runtime remains. No code, test, model/runtime configuration, frozen corpus/evaluation or fixture change accompanied the verification-method amendment.

A slice advances only after primary acceptance of its preflight route, complete test boundary, observed Red/characterization, unchanged-test Green, independent strict check, relevant test/fixture/helper audit, actual cohesion and terminal lease, and its fresh risk-routed review. Record `M203-A-*`, `B-*`, `C-*` with exact commands/cwd/relevant-tree/environment identity and any evidence invalidation. Reuse evidence only under the workflow's complete identity rule; mutable browser/model/filesystem results need an isolated pinned run identity or are non-reusable. A handoff alone does not require a duplicate full suite.

Task acceptance requires I1–I8 and every M2-03 Verification clause. Available evidence and exact citation metadata remain inspectable; errors take precedence over support; only complete evidence plus supported guidance yields eligibility; abstention is terminal, application-authored, referential, explanatory and contains no remediation/invocation/review. Invalid or unsaved data cannot appear durable. Evidence reads/selection do not trigger inference. Source notices accompany displayed portions. Actual UI integration uses the real default retrieval path at least once; deterministic adverse packages supplement it without being presented as real ranking outcomes or a completed M2-04 evaluation.

After A–C, run C-FULL and the bounded browser evidence, inspect the complete diff, then obtain a different fresh final `critical_reviewer` for cross-slice identity, lifecycle, persistence and UI admission, including actual visual evidence. It reuses fresh ordinary evidence and reproduces risk-critical checks proportionally. A planning-readiness PASS does not satisfy any implementation barrier.

At closure, compare changed paths and behavior to the authority map. Update the current developer/API instructions and public capability summary, materially affected lifecycle summary, UI guidance if its maintained responsibility changed, the roadmap and progress record, and indexes. Preserve accepted requirement/ADR semantics and historical M2-02 evidence; no new ADR is presumed. Repair inbound links when archiving this same plan only after M2-03 Verification and the documentation gate pass. Leave M2-04 and later tasks Not started. Run local link/fragment, UTF-8/newline/whitespace, authority/status, command-syntax and `git diff --check` checks. No commit/push is authorized.

## Idempotence and Recovery

### M203-PAUSE-01 — safe resumption

Resumption on 2026-09-09 freshly matched the PowerShell-sorted 93-file fingerprint and all 94 actual-proof source/test/build/corpus entries, unchanged HEAD/ref/index, Node/TypeScript/lock hashes and empty scratch/no active lease. The original thirteen-suite 448 and build results remain relevant to unchanged executed files; no actual retrieval is repeated. The operator uses the same prepared C-MANUAL command from a visible app terminal, avoiding another unobservable background launch. The independent final review proceeds on the integrated code and saved actual proof while operator evidence is pending; closure still requires every gate.

Execution stopped safely at the owner-requested pause. The manual harness ended at its 15-minute ceiling (exit 1), with settled teardown, no external requests or page errors, closed context/browser/Vite, and restored scratch. This is cleanup evidence, not a successful manual accessibility check. Primary independently verified the manual browser PID is absent, port 5173 is not listening, all four scratch directories and data/runs are empty, and no lease is active. The developer Ollama PID/start/path is unchanged. HEAD remains 84851520c34e75b9bc1fa8702f8ee0ae1087852a on the same codex branch; index is empty, task changes remain uncommitted, no publish/push occurred. All evidence and build backups are retained.

No automatic continuation is scheduled. On continuation, first reconcile this current state and complete remaining operator evidence and final integrated review; there is no active agent/session to poll. The real proof already passed and its allowance is consumed. Documentation impact: current plan and progress checkpoint record the accepted actual proof, incomplete manual observations and safe resumption; roadmap remains In progress and the plan is not archived. Final documentation closure is pending with the remaining task gates.

### M203-C-REAL-RESULT-01 — actual guidance and persistence

After the owner-approved two-line helper correction and S3 PASS, primary preserved the original empty setup root and its empty `runs` child at `temp/m203-real-before-seed-fix`; exact ordinary containment and inventory checks passed, with no files deleted. The single approved C-REAL retry exited 0. The built React client, real loopback API, default uninjected retrieval engine and existing approved runtime produced an `abstained` Finding with support `incomplete`, missing the interpretation role. Exactly one actual `POST /api/finding-guidance` occurred; only the seeded Analyze response was intercepted. This is the bounded M2-03 proof, not a capacity or full gold evaluation.

Retained outputs are exactly `temp/m203-real-01/seed.json`, `runs/m203-real-01/run.json`, `real-evidence.json`, `desktop.png` and `narrow.png`. The validated HTTP aggregate equals disk, reopened repository and service readback. Projecting away only the selected Finding's downstream fields equals the original seed, preserving all parent/provider/context/native/sibling/observation data. The response view equals fresh canonical citation resolution; the browser asserted complete passage text, direct URLs, notices, abstention explanation and no-generation statement. Canonical run SHA-256: `239410ADDFB89BF6305E46D6116EAF02156A5B573B88278053DC61CA5EDB35CF`; receipt `E7E242E6DD75D9EA02E32791F11DD93DB739AAD33BD30C0E128272990D264645`; seed `264222E9747AC0418FD2B221F656A21963E743EDDEC3749DF67143CFC5833D22`. Primary independently matched all 94 receipt source/test/build/corpus identity entries.

The receipt confirms normal context/browser/service close, closed owned port and empty UI scratch. Primary observed the same developer Ollama PID/start/path afterward and no runtime control action was taken. Primary also viewed both 1280×800 and 320×800 screenshots. These show list/selection and retained abstention status; they do not substitute for complete-guidance visual inspection, actual zoom or spoken output. The approved manual session supplies that distinct operator observation using the existing synthetic incomplete-guidance collaborator without a model call. The harness is running with `headless: false` and Windows reports its named Chrome for Testing window; the operator initially could not see it, so visibility and manual observations remain unproved.

### M203-C-REAL-SETUP-01 — pre-request fixture failure

Primary accepts the bounded S3 helper re-review **PASS**, no findings. The reviewer independently reproduced the original frozen-property failure and proved the corrected actual declarations, validation, chronology and transition; exact diff/hash/receipt and empty-root preservation checks match. Helper readiness is restored. Fresh runtime prerequisites match the existing process/start, listener, binaries, manifest/blobs and finite receipts. Execute only the owner-approved preservation move and one unchanged C-REAL retry; no further correction or proof allowance remains after those effects.

The additional correction turn is complete and primary accepted the exact two-line diff. Fresh `closed-compliant` receipt `7a8379a0eff9bc375e83e8e0c2cee783c5f96abc2bfc0a5eba60668323f27479` shows only the helper changed, no Git/ignore drift and no active lease. Reversing only the two clone expressions reproduces its prior hash. Current helper hash `D36038EC336AAB89BF7E646D743E2C780058C0E09591EA0F037298C31ECA5AD6`; 93-file source/test/config hash `84D7FCB2B41A4C73DB217AD74929E4E62F3A09AC02E3729C34B9D91B819D0DB3`. Worker syntax/strict and primary pure seed/transition, syntax and separate strict all pass. The complete suite's thirteen test files/dependencies and all production/build bytes remain unchanged, preserving the executed 448-test result. Bounded helper re-review is next; no real retry or preservation move has occurred yet.

The owner subsequently approved the proposed setup fix and one proof retry. This supplies exactly one additional test-worker turn and one bounded helper re-review, plus the preservation move described below; all older grants remain consumed. Assignment/lease `M203-C-SEED-EVIDENCE-01`, workflow `M203-20260908`, slice `M203-C`, owner `m203-c-test`, role `test_worker`, phase evidence, attempt 1, no parent. The new explicit grant supplies authority; the new guard ID does not reset the old chain. Only `tests/helpers/m203-real-guidance-driver.ts` may change, and only its two seed declarations become detached clones. No structural or application-source changes. This corrects a test helper, not production behavior; retain the existing non-TDD helper route and replace its failed setup evidence with a pure seed/transition check, syntax, independent strict and reviewed actual proof.

The prepared caller uses maintained README preparation and `Invoke-M105Command`. Its stdin Node module reads the real driver's two factory declarations, evaluates those expressions with only the existing factories/run ID, applies the same revision/target updates, validates both records, and invokes the existing pure initial-transition check. It also checks the original fixtures remain frozen and unchanged; no repository publisher, service, browser or model executes. Follow with `node --check tests/helpers/m203-real-guidance-driver.ts` and separate `node node_modules/typescript/bin/tsc --noEmit`, checking each exit. The complete primary-supplied caller is syntax-parsed before the lease and is the only added worker validation. The original 448-test files and build remain unchanged, so their executed behavior evidence is reusable subject to inspection and fingerprint reconciliation. The worker must stop after this one turn; further findings return to primary without an automatic correction.

The first frozen C-REAL invocation exited 1 at `tests/helpers/m203-real-guidance-driver.ts:88`: `Cannot assign to read only property 'applicationRevision'`. Both `runningRun` and `completedRun` return validated frozen snapshots; `seed` mutates both directly. The related M104 outcome helper already clones its fixture before changes. This is a test-driver defect; production behavior and frozen fixtures must remain unchanged. The earlier helper syntax/type/review PASS did not exercise seed creation and cannot establish readiness after this failure.

Primary inspection found exactly ordinary `temp/m203-real-01` and its empty `runs` child, with no files, links, seed, canonical run or receipt. The failure precedes repository creation, application service/browser acquisition and all guidance/model requests. All four existing scratch directories and `data/runs` remain empty, the developer Ollama process remains present, and the 93-file source/test/config fingerprint remains `803176917BAB7FFA272E527DD11A247E2A46F2C1C5E8D2B34EE4AB85525BC46F`. The 448-test result remains valid for those executed tests; actual proof is failed, not passed. No cleanup, move or retry has occurred.

Proposed owner grant, not current execution authority: one test-worker turn limited to cloning both factory snapshots before mutation in the real driver, focused non-model validation and independent strict checking, and one bounded helper re-review. Primary would preserve the verified empty failed root at currently absent `temp/m203-real-before-seed-fix`, with absolute containment/topology checks and exact inventory comparison, before one retry of the same C-REAL procedure. Its single actual guidance allowance remains unused, but the frozen absent-root/pre-request failure does not authorize replay by itself. All previous grants stay consumed. No application change, additional model allowance or workflow change is proposed.

Read-only inspection and pure checks may repeat safely when needed. Runtime writes and model calls are not idempotent replay instructions. Resume at the last accepted barrier using Current state and exact current contract; historical commands or another task's consumed grants cannot supply permission.

On worker stop, primary closes/inspects the exact lease and triages scope, environment, contract and remaining allowance. Preserve peer/user changes. Never reset/stash/checkout the tree, rewrite guard state, redefine a live lease, or run two writers. A corrected test invalidates its prior evidence before Green resumes. Plan/status writes occur only between leases.

An invalid catalog/citation yields the reviewed failure; do not rewrite the corpus or fetch live sources. A failed nested write preserves last valid bytes and the completed parent. Historical active data is readable but not resumed; an ordinary new Analyze remains the accepted independent-run path. Shutdown forbids late success/owner release. Uncertain cleanup remains visible and prevents unsafe new work under the existing service contract. New task proof uses exclusive owned run/scratch identities and normal service stop; it never modifies or deletes the retained M2-02 capacity run/evidence or the developer's runtime/model stores.

## Artifacts and Notes

Primary owns this plan, the roadmap activation, plan/progress indexes and the task progress record. Planning makes no source, test, package, fixture, corpus, evaluation or workflow-tool change. Keep only decisive evidence IDs/results and necessary command contracts here; no packet schema copies, transcript archive, generated ledger or separate design/decision report.

### M203-PLAN-REVIEW-01 — accepted planning readiness

On 2026-09-08, a fresh read-only `critical_reviewer` reviewed the complete 254-line plan and exact five-file activation surface against the controlling authorities, current implementation, UI overlay and workflow. Trigger: S3 citation/record integrity, selected-Finding ownership/recovery and browser response identity. Verdict: **PASS for planning readiness**, with no Blocker, Major or Minor findings. The primary accepts this result; no contract correction was needed.

Reviewed plan SHA-256: `E0666B87BDE77B1E8163925874771BE49F877E1949A7DD7645B25A2DF178B1FD`. This identifies the pre-closure review artifact, not a future executable hash constraint; subsequent edits record this accepted checkpoint without changing its binding contract. The reviewer independently confirmed the planning HEAD, empty index, absent active lease, four frozen-input hashes, and three corpus profiles/zero declared conflicts/sixteen passages. It reused the fresh strict/93-test evidence and primary documentation/preservation checks without running application, browser, model, build or tests.

Primary closure checks cover all five planning files: local links/fragments, PowerShell syntax, UTF-8, final newline, trailing whitespace, `git diff --check`, and preservation of every tracked path outside the three intended existing Markdown edits. Documentation impact is limited to this plan, its progress record, roadmap activation and two indexes. Execution literals D1–D5, exact future callers, current model prerequisites, durable adverse paths, rendered accessibility and spoken announcements remain unproved execution work; this PASS does not replace their barriers.

## Interfaces and Dependencies

Reuse `LocalService.retrieveFinding`, the selected operation, `RunRepository.updateRetrieval`, `validateRun`, fixed corpus loader/reference validation, `RetrievalResult`, and App's current run/selection/announcement boundaries. M2-03 adds the narrow policy result, strict nested abstention/support validation, citation view and fixed browser action. D1–D5 settle exact signatures and relationship validation before workers implement them; the plan's proposed filenames do not fix unnecessary private helpers.

Existing pinned Node/npm, TypeScript, React, Vite, Node test runner, Playwright/axe, LangChain and approved Ollama/EmbeddingGemma remain sufficient. Do not add a package or modify the finite embedding contract merely for UI display or policy testing. Application source dependencies remain service → pure policy/repository/retrieval, browser → pure admission/types/application HTTP; no client → privileged runtime or policy → transport edge.

## Revision note

2026-09-09: Recorded the first C-REAL setup failure, invalidated helper readiness, preserved the empty failed root, and reconciled remaining authority without retrying or changing application/test code.

2026-09-08: Created the M2-03 planning contract after reviewing completed M2-02 and current source. Reserved execution-stage literals, three guarded TDD slices, the visual reuse/verification contract, and proportional closure. Recorded independent planning-readiness PASS and primary documentation closure without changing the reviewed contract. Historical Git identities remain provenance; current entry and lease baselines are captured at execution.

2026-09-09: Completed A–C, accepted full-suite/real-service and different final critical review evidence, recorded the consumed helper limitation and owner-authorized visual-only manual method, reconciled current authorities and status, and prepared this same plan for archival after the documentation gate. Earlier pending checkpoints and consumed command grants remain historical.

### M203-C-POST01 — citation presentation correction

The owner explicitly approved this additional bounded round after all original correction grants were consumed. This new allowance does not renew prior chains or the actual-model proof. Independent review and primary source inspection confirm: external citation anchors replace the app document, risking loss of in-memory results; and the zero-passage retrieval branch omits its known corpus version. Both fixes remain in the existing citation presentation owner. No separate bug file is needed for this bounded correction retained in its task plan.

Milestone Assignment Packet v2

Identity:
- Workflow ID: M203-20260908
- Roadmap task ID: M2-03
- Work-slice ID: M203-C-POST01 (explicit owner-approved post-closure round)
- Assignment ID: M203-C-POST01-PREFLIGHT; write packets specialize identity before dispatch.
- Lease ID: None for preflight.
- Phase: preflight; later red/test_worker and green/frontend_code_worker.
- Attempt: 1 under the new explicit allowance; no corrections.
- Correction parent lease ID: None; previous chains remain consumed.
- Worker role: test_worker for preflight and Red; frontend_code_worker for Green.
- Lease owner: m203-post-test / m203-post-ui as appropriate.
- Guard contract digest: None for preflight; primary inserts returned digest for each write.

Work-slice capsule:
- Owning ExecPlan: this plan, current correction section.
- Observable acceptance contract: every external passage citation opens separately, indicates "opens in a new tab" visibly and in its accessible name, uses noopener noreferrer, and leaves the app URL, selected Finding, retrieved detail and later list selection intact. A retrieval view always shows its exact corpus.version, including zero passages; corpus-null evidence-only abstention still says guidance was not retrieved and displays no fabricated version.
- Exact authorities: M2-03 roadmap Verification; SPEC-002/BHV-02 in docs/specs/SPEC.feature; REQ-EVID-004/007, REQ-RETR-005, REQ-A11Y-001–004/010; ADR-0012 semantics and OD-026 no restoration; docs/ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m2-03-selected-finding-guidance. Original I1–I8 remain applicable.
- Readiness and evaluation-freeze evidence: M2-02 remains Complete; same Accepted task authorities and owner-amended manual method. HEAD 84851520c34e75b9bc1fa8702f8ee0ae1087852a; existing dirty implementation preserved; no active lease at entry. Frozen corpus/evaluation and service/persistence/runtime untouched.
- TDD applicability: Applicable; both omissions have meaningful rendered/browser assertions.
- Current-state and preflight evidence IDs: primary source confirmation; test preflight pending.
- Accepted test boundary and current test owner: tests/finding-guidance-ui.test.ts, m203-post-test; revised Red accepted before Green.
- Relevant boundaries and paths: GuidancePassages.tsx, finding-guidance-ui.test.ts, unchanged existing M104 harness and real-proof driver.
- Production responsibility placement and fit: GuidancePassages already owns citation navigation and corpus metadata; extend its rendered links and retrieval summary.
- Dependency, runtime-call, or interface-edge changes: None; existing canonical view to React rendering only.
- Reuse disposition: EXTEND GuidancePassages and existing browser scenarios; REUSE_AS_IS all other production and helper owners; no new abstraction.
- Permitted local structural refactor: None needed.
- Non-goals: source/service/admission changes outside GuidancePassages, storage/restoration, API changes, dependencies, network retrieval, model calls, supported-proof-helper repair, screen-reader/manual test, actual-zoom deferral changes, M2-04, commit or push.
- Named uncertainties: whether existing test assertions select exact link names; preflight must inspect complete affected test/helper and adapt only tests within its single authorized turn.
- Risk tier: S2 browser navigation integration; no changed custom identity, security boundary, persistence or concurrency implementation.
- Review and escalation triggers: fresh independent_reviewer examines actual two-file delta, browser evidence and historical real-proof limits; material new critical risk escalates, cannot expand writes.

Frontend-visual capsule:
- Implementation profile: frontend-visual.
- Frontend-quality skill: .agents/skills/frontend-quality/SKILL.md (already applied).
- Exact UI/design authority anchors: docs/ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m2-03-selected-finding-guidance.
- Reuse-audit evidence ID: M203-C-POST01 primary inspected GuidancePassages/FindingGuidance and all client anchor occurrences.
- Reuse dispositions: EXTEND existing passage presentation and existing test suite; no other production owner changes.
- Required state matrix: populated supported/incomplete guidance, zero-passage missing guidance, corpus-null evidence abstention.
- Required viewport and interaction matrix: retained desktop browser and existing narrow sample; keyboard-activate a citation, fulfill its external document via context route (no actual network), close that popup, verify unchanged app/detail and reselection.
- Browser or visual-evidence target and reproducibility identity: existing Playwright M104 controlled UI harness and valid canonical fixtures, pinned Chromium1234; no claim of manual speech or detailed 200% observation.
- Prohibited visual scope, dependencies, fields, copy, effects, motion: no layout/theme/new controls; only metadata and new-tab indication.

Write scope:
- Allowed files: Red only tests/finding-guidance-ui.test.ts; Green only src/client/components/results/GuidancePassages.tsx.
- Allowed directory roots: None.
- Forbidden files: Green explicitly forbids the accepted test file.
- Forbidden directory roots: Red src/docs; Green tests/docs. All other files outside exact allowed file remain forbidden.

Validation:
- Working directory: C:/projects/a11y-evidence-lab.
- Focused command: use README Development command preparation through its exact heading/fence; Invoke-M105Command { & $m105Node --test --test-timeout=120000 tests/finding-guidance-ui.test.ts; if ($LASTEXITCODE -ne 0) { throw 'Guidance UI suite failed' } } $m105UiTemp.
- Task-level command: primary independent tsc --project tsconfig.json; preserve inventoried dist/client at temp/m203-build-before-post01 then Vite build --configLoader native; complete README thirteen-suite sequence once after Green. No C-REAL replay.
- Expected decisive result and reusable evidence IDs: Red fails exactly the two missing behaviors; Green both regressions and prior UI pass; fresh strict/build/full suite and reviewer accept same final source/test identities.
- Relevant-tree fingerprint: guard baseline plus primary captures both correction files before writing; full 93 entry fingerprint 84D7FCB2B41A4C73DB217AD74929E4E62F3A09AC02E3729C34B9D91B819D0DB3 is historical until independently reconciled.
- Environment fingerprint: retained Node24.20.0, TypeScript7.0.2, Playwright1.62.1, Chromium1234; exact lock unchanged. Browser observations are pinned controlled runs.
- Known external side effects and cleanup: existing loopback Vite/app/test browser and exact README scratch directories; synthetic external citation intercepted before network and popup closed in finally. No model calls; preserve real proof and previous build.

Budget and stopping:
- Maximum worker turns: one read-only preflight, one test write turn, one UI write turn.
- Maximum corrections: zero beyond these two granted writes.
- Maximum repeated identical failure: one decisive unexpected failure stops for primary triage.
- Maximum no-diff outcomes: one, then return.
- Validation cadence: focused Red/Green at boundary; primary strict/build/full at closure; no repetitive model or browser proofs.
- Stop and escalate when: unapproved path/side effect, unexpected prerequisite, wrong failure, exhausted turn, or expanded contract.

Handoff:
- Report identity/authority, actual touched paths, exact commands/results, observed regressions, cleanup, cohesion, residual limits and documentation impact. Workers are not alone in this checkout: preserve peer/user changes and do not revert them. Git writes and metadata mutations are forbidden; workers never manage leases.

Evidence limitation: original 448/full 39 UI/build/C-REAL evidence proves the prior revision only. Renew affected executable evidence after the correction; retain C-REAL as an explicitly historical actual-service observation, linked to unchanged backend and canonical data, without asserting its 94-file manifest matches the corrected client/build.


2026-09-09: Reopened the same plan under the explicit additional correction grant; preserved previous closure, consumed budgets and historical C-REAL evidence, and froze the minimal citation-presentation regression/Green/review scope.

M203-C-POST01-PREFLIGHT accepted: REGRESSION. Test owner inspected complete test/helper/fixtures and proposes extending two existing cases (no new case) with new-tab/session preservation and exact empty-retrieval/corpus-null checks. Static target/version assertions precede popup waiting, and the exact external route and popup clean up in finally. No browser replay or writes occurred in preflight. Entry 93 fingerprint matches. Primary authorizes the sole Red write under the packet.

M203-C-POST01-RED: test lease closed-compliant (contract 859c3e9184a6a9d8a9b336c26be69f00ad5932db41aa50079aa5637411f5045c; receipt 5f6f93940e08d6fbcde8727791ad2c5b546eaed4bf9327bfed1d65f628ee0ee4). Only finding-guidance-ui.test.ts changed, SHA256 A54E6869AF88C75CF4393F86C90D7931C9928880A069271932E006880FEB1890. Worker command failed before Node because it invoked preparation in a child scope. Primary triaged that caller-only error, used the already-validated dot-sourced preparation, and observed exactly two expected failures: target null versus _blank and missing exact corpus version (0 versus 1); the other seven cases passed. Primary inspected and accepted the unchanged revised test boundary. No extra test edit or worker turn occurred. Original Red failure is retained; the primary browser run had no external requests/page errors and normal teardown.

M203-C-POST01-GREEN: sole UI turn changed only GuidancePassages.tsx (SHA256 59450C30F8E098946124AEF9EAE06BA4CE3128281067E1A5B7E91A8A2DEFF4A6). Lease closed-compliant: contract071dd1188a1dd213d77286b1ed4bb8b303bc5229d849862e4791ff46e4c1d0c1, receiptd367c7347f7c05ad9329d89788acec52158639d80ea5922838aacf53b4eef078. Both missing-behavior assertions now pass;8/9 UI cases pass, but popup loading fails because the new test route includes #description while network requests omit fragments. Browser catch-all blocked external attempts; teardown settled and no page errors. No real website or model call was admitted. Accepted test remained byte-identical. Primary independently passed strict, verified/preserved the three-file original build at temp/m203-build-before-post01 and rebuilt successfully. Full suite and final acceptance await a narrowly requested extra test-only correction; neither original allowance is renewed silently.

Primary unaffected verification: all 12 unchanged suites pass 439 tests (66+68+69+20+12+28+17+21+5+88+15+30), no failed/skipped/todo/cancelled. The affected nine-case guidance suite is intentionally pending the requested test-only correction, not rerun against its known invalid interception. Independent strict and rebuilt client pass; old generated build is preserved.

Primary visual-region verification used the unchanged M104 harness with valid supported/missing canonical fixtures and synthetic Analyze/guidance callbacks, Chromium151.0.7922.34 at1280x800 and 320x800. An initial caller omitted Local selection and stopped at form validation before any results; cleanup passed. Corrected caller selected Local, then captured temp/m203-post01-citation-desktop.png, temp/m203-post01-citation-narrow.png and temp/m203-post01-missing.png. Primary inspected all three: new-tab text is readable and wraps at320px without horizontal overflow; exact corpus version remains visible with zero passages. No external requests/page errors, normal settled teardown. These bounded region samples do not claim complete-detail200% or spoken output.

M203-C-POST01-TEST-FIX: The owner responded to the pending correction request with the exact direction to match fragment-free URLs in route/unroute, retain the full citation assertion and rerun the nine-case suite. This authorizes only that narrow primary test correction under the workflow exception, with no active lease, additional worker turn or production edit. Before editing, component59450C30...EFF4A6 and testA54E6869...EB1890 match the reviewed state. The previous affected-test evidence is invalidated; renewed focused/strict checks and independent re-review are required before closure.

M203-C-POST01-VERIFIED: The directed test-only repair creates a URL with an empty hash for matching route/unroute; the original full citation string and popup URL assertion are unchanged. Revised test SHA256 E83024C9740E7D2BBEDF6E9643EAF9A6BFF24FD90AA2E20500F0E5DF3BD3B01E is accepted after all nine guidance UI cases pass, including keyboard popup activation, null opener, unchanged application URL/detail/selection, sibling selection and reselection. Teardown reports external[], pageErrors[] and settled:true. Fresh independent strict TypeScript passes. Together with the unchanged twelve-suite 439 result, the complete thirteen-file suite passes 448 tests with zero failed/skipped/todo/cancelled; no duplicate model proof or unaffected-suite replay occurred.

Final 93-file PowerShell-sorted source/test/package fingerprint:128177CF72A1754FEB4B71155DB330D217942DCD9593B5BEDFF76D5898062AE4. Component SHA256 remains59450C30F8E098946124AEF9EAE06BA4CE3128281067E1A5B7E91A8A2DEFF4A6. Rebuilt index SHA256 CD2E854D2B808EACFCFA6B5B7C2B09A262E6453E3D8B0EE01BC6BBC1744CE59C and assets/index-dsJb7T3J.js SHA256144B1CED7BA4D6C28229D48EB03B96B1932D7275D5891E5BF5F1E6DEFEF4D845; CSS unchanged. All 94 historical real-proof identities reconcile with exactly the two expected source/test changes and the old build verified at its preserved path. That proof remains a prior-client observation, not a newly run or fully matching current-client proof. All other source/test/corpus entries match; HEAD/index and frozen artifacts remain unchanged. The complete correction delta is retained at temp/m203-post01-final-review.diff.

#### M203-C-POST01-CLOSURE — renewed task closure

Fresh S2 independent re-review returns PASS with no Blocker, Major or Minor findings; M203-POST-R1 is resolved and the previous BLOCKED correction verdict is superseded. It inspected the final two-file diff, source/test/build hashes, compliant ownership, explicit primary test exception, nine-case browser evidence,439 unaffected cases, cleanup and the three correction-region screenshots. Primary accepts retained production cohesion, the revised test boundary and complete 448-test evidence. No further application or test work remains in this correction.

Documentation impact: Updated the citation-presentation authority, current capability/UI summaries, canonical roadmap status, progress records and plan navigation. The renewed gate passes sixteen Markdown documents/551 local links, UTF-8/final-newline/whitespace and git diff --check. After archival, repaired navigation and all 16 README/plan PowerShell blocks pass again. No requirements, model/runtime configuration, frozen inputs or later-task scope were changed by this correction.

2026-09-09: Applied the owner-directed fragment-free test interception correction, accepted fresh9/9 browser and strict results alongside439 unchanged tests, resolved the held review blocker, and renewed M2-03 closure while preserving historical evidence limits.

2026-09-09: Documentation-only maintenance adds a direct renewed-closure heading, repairs joined prose/counts, and links current summaries to final acceptance. Historical attempts, result counts, evidence identities and commands remain unchanged; no application task is reopened.
