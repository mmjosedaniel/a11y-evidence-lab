# M6-03 — Verify application accessibility

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with [PLANS.md](../../../PLANS.md).

## Current state

- **Task:** [M6-03](../../DEVELOPMENT_ROADMAP.md#m6-03--verify-the-applications-accessible-core-path), **Complete** after G/A/B/C verification, independent final review and the documentation gate.
- **Dependency and scope:** Current M6-02 closure and owned cleanup pass. Only controlled application accessibility verification is selected; no model, credential, public scan, actual review or M6-02 recovery effect is authorized. M6-04 remains Not started.
- **Latest barrier:** [Final independent review and closure](#m603-final-01--independent-review-and-task-closure) accepts [integrated and rendered evidence](#m603-b-c-verify-01--integrated-and-rendered-evidence): 1061 tests across 54 files, strict/build, all 30 inspected desktop/narrow frames and preservation/cleanup. S2 review accepts the corrected characterization. No production change or active lease.
- **Next action:** None for M6-03. No active lease or remaining task execution allowance; this archive grants no replay. M6-04 remains Not started and requires its own explicit selection.

## Progress

- [x] (2026-09-23 UTC) Confirm dependency-ready current authority and baseline; activate only M6-03 and preserve original readiness history.

- [x] (2026-09-21) Review current roadmap, M6-02 stopped handoff, accessibility authorities, UI boundaries and existing evidence surfaces.
- [x] (2026-09-21) Run independent strict TypeScript without browser/runtime effects; confirm clean entry and no active lease.
- [x] (2026-09-21) Prepare this dependency-gated plan without activating M6-03 or creating a progress record.
- [x] (2026-09-21) Independent planning-preparation PASS with no findings; documentation validation accepted. Execution remains blocked.
- [x] (2026-09-21) Receive conditional execution authorization and recheck current roadmap, M6-02 state, HEAD, dirty paths and active-lease absence. Prerequisite remains unmet; retain Not started and reconcile documentation only.
- [x] (2026-09-23 UTC) G: dependency and authority checks pass; M603-G-PROOF-01 freezes the current proof, commands, identities and controlled effects; task activated.
- [x] (2026-09-23 UTC) A: worker-first preflight and corrected keyboard characterization pass; both leases close compliant and S2 review accepts the corrected evidence.
- [x] (2026-09-23 UTC) B: full-path desktop/narrow visual inspection and mapped automated accessibility, keyboard, focus and announcement evidence pass.
- [x] (2026-09-23 UTC) C: current regression, strict/build, different fresh final independent PASS, preservation/cleanup and documentation closure pass; task Complete and plan archived.

## Surprises & Discoveries

- At planning entry, M6-02 had qualified implementation but incomplete fixed-case evaluation. Its then-current acceptance recorded 978 tests across 49 files, strict/build and a final review for a stopped handoff. That historical review did not close the task; the later native continuation and closure now satisfy the dependency.
- The original M6-02 Local image dispatched once and failed response validation; its required in-call observation was missed. The successor consumed one entry but dispatched no generation after a metadata prerequisite failure. Five cases in each campaign remain not-run. The cause and native cleanup state were unproved at that historical checkpoint; M6-02 later resolved current readiness and cleanup before this task activated. No diagnosis or repair is authorized here.
- The existing synthetic App harness supplies the component collaborators needed for a controlled visual path. Legacy manual launchers are incomplete for M6-03 and include obsolete screen-reader/200% zoom directions or historical revision literals. Reuse the underlying helpers, not their old instructions or evidence claims.
- All 200% zoom testing is Deferred, while accessible zoom behavior remains Accepted. Manual screen-reader speech is not a portfolio gate. Neither a passing axe result nor live-region DOM assertions establish speech or WCAG conformance.

## Decision Log

- **2026-09-23 / Primary — Minimum added proof.** Accept passing characterization for existing behavior, add one continuous keyboard path and its optional viewport capture, and reuse the full suite once for both focused and integrated coverage. No production implementation or invented Red is needed.

- **2026-09-21 / Primary — Preparation without activation.** The user authorized this plan, but the roadmap dependency is unmet. Keep M6-03 Not started, register the prepared plan separately, and create no progress record until actual task activation.
- **2026-09-21 / Primary — Verification first.** Reuse current React owners, UI tests, synthetic collaborators and installed Playwright/axe. No production change or new accessibility infrastructure is presumed.
- **2026-09-21 / Primary — Controlled visual evidence.** Plan one bounded synthetic core path using the actual application UI. State explicitly which service outcomes are controlled; do not infer new scan, retrieval, model, durable-review or comparison-calculation evidence from it.
- **2026-09-21 / Primary — Conditional execution authorization.** The owner authorizes G/A/B/C only after existing readiness conditions pass and explicitly preserves the M6-02 dependency. Record the authorization without activating the task, opening leases or renewing any prior operation/correction allowance.

## Outcomes & Retrospective

M6-03 is Complete. G/A/B/C verification, fresh final independent review and documentation closure pass. The existing application satisfies the bounded checks without production changes; the two test files close the continuous-keyboard and visual-composition evidence gaps. One incorrect test expectation and two proof gaps were corrected in the ordinary test-side correction turn. Historical failures are retained. Zoom testing and screen-reader speech remain outside this verification boundary.

At the conditional execution readiness checkpoint, M6-02 remained incomplete with unresolved native metadata cleanup and no scheduling amendment; only readiness inspection and documentation maintenance occurred. The later M6-02 continuation reconciled current resources without rewriting that historical uncertainty. That prerequisite was subsequently satisfied by M6-02 closure; M6-03 then activated through its own G gate on 2026-09-23 UTC.

## Purpose / Big Picture

Verify the implemented application's essential interaction: target entry and mode choice, complete results navigation, selected evidence and guidance, generation status and proposal, individual review, and comparison. The deliverable is traceable automated evidence plus one manual visual smoke path, with honest limitations. It is not an accessibility certification, support matrix or new product subsystem.

## Context and Orientation

### Controlling authority and current project state

Start with the [authority map](../../README.md#authority-and-status-map), [project requirements](../../PROJECT_REQUIREMENTS.md), selected roadmap entry and [application-accessibility requirements](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md). All selected Must rows, REQ-A11Y-001–004, 006, 009 and 010, are Accepted. Their current verification amendments control older specification/launcher wording.

[OD-022](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-022--portfolio-mvp-yagni-simplification) and [OD-024](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-024--minimum-complete-mvp-behavior-contracts) own the compact scope; [OD-025](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-025--development-authorization-and-roadmap-governance) preserves dependency-ready activation. [SPEC-001–008](../../specs/SPEC.feature) are derived observable examples: intake/results; guidance; proposal/abstention/failure; provider disclosure; review; comparison; preserved work; and source/limitation distinctions. This task verifies their applicable presentation and interaction, not another full backend campaign.

[ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md), the [UI index](../../ui/README.md) and [Analyze/Results presentation](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md) control UI responsibility and state. Preserve canonical running/completed/failed scan states, evidence-only manual-review observations, supported/abstained/failed Finding outcomes, truthful invocation status and individual human decisions. Do not introduce UI-only domain states or retained-run reopening. [REQ-EVAL-004/005](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#evaluation-requirements) preserve provenance and changed-evidence honesty.

The [agent workflow](../../../.codex/README.md), [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [write guard](../../../.codex/write-lease-guard.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md) and [frontend-quality skill](../../../.agents/skills/frontend-quality/SKILL.md) govern implementation if later needed. The skill guides bounded visual verification within the selected task and does not supply development authority.

At entry, 25 tasks through M6-01 are Complete; M6-02 is In progress; M6-03/M6-04 are Not started. M1–M5 implemented the walking skeleton, retrieval, fixed providers, human review and conservative comparison. [M6-01 closure](m6-01-shared-deterministic-evaluation.md#m601-final-01--integrated-review-and-documentation-closure) preserves the informative-image retrieval relevance limitation. [M6-02 current implementation acceptance](m6-02-six-fixed-generation-executions.md#m602-suc-accept-01--complete-s3-and-prequalification-acceptance) and stopped handoff are historical test/effect evidence, not fresh accessibility proof. No prior public, model, review or cleanup grant is renewed.

### Minimum proof and reuse map

| Current responsibility / authority | Reuse surface | Required evidence |
| --- | --- | --- |
| Target/mode/validation, running/completed/failed, complete/zero/coverage failure; A11Y-001/002/004 | AnalyzeForm, AnalyzeSection, App; target-results UI tests | Keyboard reachability and activation, named/associated errors, one shared status pattern, focus preservation and truthful result states |
| Every Finding/manual-review observation, selected item and evidence; A11Y-001/009/010 | FindingsPanel, ResultCard, ResultDetail, ResultsSection | Full collection reachability, named scroll region, visible/programmatic selection, manual-review tags, selection retains focus and announces only the selected item |
| Guidance, abstention, eligible/pending/pre-call/attempted/unknown outcomes; A11Y-002/004 | FindingGuidance, FindingOutcome, FindingGeneration, ProposalDetail; guidance/generation UI tests | Explicit evidence layers, citations and full text, shared announcements, no review for abstention/observations, no fabricated call outcome, stable focus on settlement |
| Proposal-only review and its validation/saved result; A11Y-001–004/006 | ProposalReviewForm, ProposalEditor, ReviewDecision; review UI tests | Operable support/judgment controls, associated validation, original versus edited/human text, saved-decision announcement and predictable focus |
| Intentional rescan, comparison and baseline navigation; A11Y-001–004/006 | IntentionalRescanForm, ComparisonDetail; rescan/comparison UI tests | Keyboard operation, predictable Results focus after replacement/navigation, zero-Finding later result, non-color outcomes and limitations; availability settlement does not steal focus |
| Layout, readability, contrast, overflow and visible focus; A11Y-003/006 | Existing styles and all owners above | Automated applicable axe checks plus actual browser visual observation at desktop 1366×900 and narrow 390×844, 100% zoom; no support-matrix claim |

Initial visual reuse disposition is **REUSE_AS_IS** for each named existing owner, subject to actual preflight. Production responsibility change: **None — no application-source responsibility changes planned**. A confirmed defect needs an exact responsibility/edge/reuse amendment before a writer receives it. Do not assign all changes to App or styles merely because those paths already exist.

Existing automated entry points are `tests/target-results-ui.test.ts`, `tests/finding-guidance-ui.test.ts`, `tests/finding-generation-ui.test.ts`, `tests/finding-review-ui.test.ts`, `tests/intentional-rescan-ui.test.ts` and `tests/comparison-ui.test.ts`. Reuse `tests/helpers/m104-ui-harness.ts` and the review/comparison fixture helpers. The source map is navigation, not a claim of freshly passing or complete coverage. The actual composed visual path and any missing assertions are bound in G/A.

Concrete reuse entry points: `startHarness(manual, entry)` returns the real browser page, source hashes and close boundary; its App bridge injects Analyze/guidance/generation/review/rescan/comparison collaborators and blocks non-origin browser requests. `reviewProfileStages` and `reviewedProfileRun` in `tests/helpers/m402-review-fixture.ts` supply validated proposal/review stages; `comparisonForRuns` and `resolvedZeroComparisonForRuns` in `tests/helpers/m503-comparison-fixture.ts` supply controlled comparison results. Existing comparison/review UI tests show their composition. Do not substitute `runBuiltReviewCase`, which starts a real local service and persists sandbox records, for this synthetic visual route.

Preflight has two concrete evidence gaps to resolve: no existing manual entry covers the full path, and scattered direct focus/click/check calls do not by themselves prove continuous logical Tab order or absence of traps through the core controls. Add only missing characterization/composition, not duplicate scenario suites. The harness also requires prepared scratch, exact environment/browser identity and a recent validation marker; its historical hash map does not cover every later fixture/driver. G must bind those actual inputs and the marker producer/consumer, not assume built-in hashes are complete.

## Scope and Non-Goals

Include only the selected Accepted accessibility checks, a minimal controlled visual path, required test coverage, and bounded correction of a demonstrated task-owned defect after execution authorization. Reconcile instructions/evidence at closure.

Exclude M6-02 recovery, new generation/embedding/runtime/credential requests, public navigation/scans, capacity observations, authentic retained human decisions, production-data mutation/deletion, new evaluation campaigns, UI redesign, dependencies, general runners, screenshot services, accessibility platforms, exhaustive state/browser/assistive-technology combinations, participant studies and release claims. All 200% zoom tests and manual screen-reader testing remain outside this gate. M6-04 stays unselected.

## Plan of Work

### G — Confirm readiness and bind the proof

Do not start executable M6-03 work until M6-02 is Complete with its closure gate and cleanup uncertainty resolved, or the owner explicitly changes the scheduling authority. A generic instruction to execute this plan does not silently amend the dependency. If it remains unmet, finish documentation only and report the blocker. Do not inspect credentials, probe Ollama, reconcile M6-02 transports or retry its cases here.

Once dependency-ready and execution-authorized, read current HEAD/dirty paths, actual prerequisites, relevant source/test/build/config identities, existing leases and owned scratch state. Preserve other work. Record task activation in the roadmap and create its indexed progress record only then. Never require the planning SHA or falsify application revision metadata.

Freeze `M603-G-PROOF-01` here before any affected write or browser command: exact requirement-to-test mapping; controlled fixture/state sequence; current UI/build/browser identity; installed browser/axe settings; exact commands and ports; allowed file mutations; scratch/output ownership; expected effect counters; timeouts; cleanup and evidence capture. Existing helpers and direct browser inspection are the default. Do not introduce a custom observer, integrity framework or new test runner. If a materially new consequential mechanism becomes necessary, stop and apply the workflow's research/Decision Review Contract routing before selecting it.

Bind the synthetic visual path through actual form/actions with controlled collaborators: valid trusted-format HTTPS target and explicit mode → Analyze → complete mixed results and selected Finding → supported guidance → proposal → one controlled review decision → controlled rescan/comparison and baseline navigation. Use known existing fixtures; do not change frozen model/corpus/comparison definitions or execute genuine provider calls. Controlled state may identify a synthetic attempted invocation, but it proves only rendering and interaction. Record which outcomes are injected and which application interactions are real. No saved decision in this harness is an owner decision on an authentic retained proposal.

### A — Verify coverage and correct only demonstrated gaps

Use workflow ID `m603-application-accessibility-verification`. Assign one bounded read-only `test_worker` preflight against the proof map. Existing passing tests are reused under complete evidence identity; missing evidence is not automatically missing behavior. `EXISTING_BUT_UNCOVERED` uses a guarded passing characterization; missing/regressed behavior uses accepted test-worker Red then a separate implementation worker's Green. PARTIAL is narrowed; CONFLICTING/UNKNOWN returns for triage. Never fabricate Red for visual inspection or documentation.

Candidate test-owned paths are the six existing UI tests and their specific fixture/harness helpers; a purpose-named M6-03 test/visual caller is permitted only if existing composition cannot express the current proof. G freezes exact files and symbols, not directory-wide leases. A test-side fixture composition may reuse App collaborators; new substantive production behavior cannot be hidden in a helper. Ordinary production edits remain delegated. Material rendered fixes use `frontend_code_worker`, the frontend-visual packet, accepted reuse dispositions and real-browser evidence; a nonvisual fix uses `code_worker`. Primary owns plans, authority, status and evidence acceptance between leases.

Every worker write turn requires a primary-opened fresh packet/lease, terminal compliant close, actual diff inspection and evidence/cohesion acceptance. One lease per worktree. Accepted tests stay unchanged during Green; workers use Git read-only. Initial turn plus one ordinary correction is the default; a third requires the workflow's recorded progress/new-evidence/changed-action/expected-benefit conditions. One review-correction loop uses remaining budgets. New IDs or workers cannot renew allowances. Consult the [bug index](../../bugs/README.md) before investigating a substantive defect.

Route bounded characterization by its actual risk. Cross-UI integrated behavior is S2 with `independent_reviewer`; use `critical_reviewer` only for a real S3 trigger or the workflow's escalation condition. Preserve research roles as read-only. A handoff or passing lease receipt is not proof of behavior or cohesion.

### B — Observe the compact accessible core path

After G/A acceptance, run the six focused UI suites sequentially using current maintained preparation and isolated UI scratch. Reuse still-valid evidence rather than repeat tests at handoff. Map automated assertions to every selected requirement: keyboard order/activation/no trap, names/relationships, full result navigation, selected state, shared live-region semantics, non-disruptive focus and applicable automated contrast/accessibility rules.

Cover important canonical states through existing focused tests, without a Cartesian matrix: invalid input/missing mode; scan start/completion/failure/valid zero/coverage failure; selection and manual-review observation; guidance loading/support/abstention/failure; eligible/pending/pre-call/attempted/unknown generation; review validation/saved decision; rescan and comparison success/failure/unavailable lineage. Existing negative tests can establish these branches; the manual smoke need not repeat each branch at each width.

Perform one documented visual workflow with the desktop and narrow samples in the map. Inspect layout, complete readable evidence/citations/notices, source distinctions, non-color statuses, visible focus and absence of horizontal page overflow through proposal, review and comparison. Use actual browser-rendered observation; screenshots only where decisive. Programmatic viewport/keyboard operations support inspection but are not manual screen-reader speech evidence. Confirm no production run-store, scanner, embedding or generation effect occurred.

The legacy `--manual`, `--m203-manual` and review launcher directions are not an approved M6-03 script: remove reliance on old zoom/Narrator steps, incomplete paths and historical revision literals in the task-owned caller or instructions before use. Bind evidence to the current browser/build and actual observation, not an old hard-coded log field. A structural axe scan, screenshot or overflow number alone cannot prove the whole visual path.

Record pass/fail/not-run per requirement and decisive state, automated command identity and manual observations with limitations. A failed required check remains failed until its bounded correction and affected re-verification pass. Do not weaken assertions, omit difficult states, claim an inaccessible fixture is a product defect, or mislabel controlled observations as actual backend success.

### C — Integrate, review and close

Run the current maintained authoritative suite sequentially, independent strict TypeScript and current build at closure. At entry the historical suite is 49 files/978 tests; counts are descriptive, not assertions to freeze. Keep actual-case/capture flags absent and no concurrent service/browser work. Preserve the workflow's complete evidence-identity rule; rerun only affected/stale/contradictory evidence before the final complete suite where required.

A fresh final `independent_reviewer`, different from a preceding work-slice reviewer, checks integrated requirement coverage, actual visual evidence, controlled/real distinctions, source ownership, preserved data, cleanup and documentation. Escalate only on named critical triggers. Review is not a second exhaustive browser campaign.

Apply the [documentation closure gate](../../README.md#task-closure-documentation-gate), including affected UI/developer instructions, requirement/status consistency, links and whitespace. Mark Complete and archive only when every required M6-03 check and that gate pass. Deferred zoom/manual speech remain explicitly unverified. Update the task's progress and navigation at accepted material checkpoints; do not activate M6-04.

## Decision Review Contract

No consequential technology or architecture selection is proposed. Planning uses bounded repository evidence mapping and primary synthesis; no analyst, drafter or R3 panel is required merely to reuse established UI checks. G binds task literals and proof coverage in this plan. If discovery requires a new consequential proof, isolation or recovery mechanism, classify its actual risk and add the required contract before comparative research or implementation; do not smuggle it into test setup.

## Concrete Steps

Work from the repository root in PowerShell 7. Read and dot-source only the definition block under [Development command preparation](../../../README.md#development-command-preparation). Do not replay adjacent restore, server, deletion or archived evaluation commands.

### M603-CMD-ENTRY — Read-only planning diagnostic

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') {
  throw 'Reconcile active lease before task maintenance'
}
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) {
    throw 'Pinned Node runtime unavailable'
  }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
}
```

Expected: strict check passes, no emitted files or live resource. This does not establish browser accessibility or clear M6-02.

### M603-CMD-UI — Existing focused command, gated until G

After G binds prerequisites, checks capture flags absent and validates empty ordinary UI scratch, this is the existing focused route; it is not authorized by the planning request:

```powershell
foreach ($m603Test in @(
  'tests/target-results-ui.test.ts',
  'tests/finding-guidance-ui.test.ts',
  'tests/finding-generation-ui.test.ts',
  'tests/finding-review-ui.test.ts',
  'tests/intentional-rescan-ui.test.ts',
  'tests/comparison-ui.test.ts'
)) {
  Assert-M105EmptyDirectory $m105UiTemp
  Invoke-M105Command {
    & $m105Node --test --test-timeout=120000 $m603Test
    if ($LASTEXITCODE -ne 0) { throw 'M6-03 focused UI suite failed' }
  } $m105UiTemp
}
Assert-M105EmptyDirectory $m105UiTemp
```

G must resolve these remaining command bindings before use; no worker invents them after its lease opens:

| Binding | Required resolution |
| --- | --- |
| Readiness/identity | Actual dependency acceptance, current HEAD/dirty paths, source/test/build/browser identity, no active lease/conflicting live work; preserve M6-02 records without replaying its identity-bound inspectors |
| Preparation/build | Exact installed-browser and current build prerequisites, explicit owned ports/scratch/output paths, allowed build mutations and environment restoration; no install/download or broad cleanup |
| Visual caller | Existing helper entry/imports, deterministic state sequence, genuine UI actions, viewport changes, effect counters, local content-safe evidence and exact source/build identity; no obsolete manual launcher contract |
| Gap/fix qualification | Exact files, assertion/expected behavior, worker ownership, focused commands and risk-routed review; no broad path lease |
| Full verification | Current [maintained full-suite and build commands](../../../README.md#build-and-verify-the-walking-skeleton), accepted additions and absent capture/actual-case flags; preserve scanner/UI scratch separation |
| Cleanup/documentation | Stop/await only owned resources; validate literal ordinary disposable paths and contents before deletion; preserve retained runs/evidence; links, syntax, status, UTF-8/newline/trailing whitespace and git diff --check plus untracked-file inspection |

## Validation and Acceptance

Planning acceptance requires this plan and its index link, accurate blocked execution state, traceable Accepted/Deferred boundaries and proportional documentation validation. Roadmap status, requirements and prior evidence remain unchanged.

Execution acceptance requires dependency readiness, reviewed exact proof binding, every selected REQ-A11Y row mapped to passing current automated evidence, and the real-browser desktop/narrow visual path including a proposal-bearing Finding, its controlled decision and comparison. Focus and live-region assertions prove their tested DOM/interaction semantics only. No new actual provider or authentic review evidence is required or authorized here.

For the manual visual observation, TDD is Not applicable: direct rendered observation with exact identity and recorded result is replacement evidence. Automatable behavior and test-side characterization retain the worker-first route; no blanket non-TDD exception applies to a new caller or fix. Completion also requires maintained regression, strict/build, fresh final review, preservation/cleanup and documentation closure. Unmet Must checks block completion; no composite accessibility score masks them.

## Idempotence and Recovery

Resume read-only from this section, current state, the bound proof and actual evidence. Reconcile the current checkout; never restore an old SHA or pretend a prior artifact represents the new build. The M6-02 readback revision restriction is recorded in its [documentation review](m6-02-six-fixed-generation-executions.md#m602-doc-01--documentation-review-and-execution-revision); M6-03 must not override that identity to obtain a pass.

Controlled UI checks may repeat after an accepted fix under their bound isolation and cleanup contract; they cannot replay original generation campaigns or human decisions. Preserve original runs, corpus, frozen manifests and ignored historical evidence. Do not use production data to make a visual fixture convenient. A wrong collaborator or unexpected real request stops the affected lane immediately; record effects rather than assuming cancellation.

Stop only owned application/harness/browser resources and await closure before another operation. Do not kill unknown processes, modify the developer-owned runtime, empty shared scratch blindly or recursively remove temp/data roots. Preserve unfamiliar files and uncertain resource state for primary triage. A guard violation stops writes without reverting others' work. Correct within existing scope/budget; ask for a material owner decision when required.

## Artifacts and Notes

### M603-ENTRY-01 — Current project state and planning evidence

2026-09-21: clean `470e5746265ef32f46291d45fa46a4c989e532da`; no active lease. Primary reviewed authority/status, M6-02 current stopped outcomes, requirements and amendments, specifications, UI ownership, maintained command preparation and bug recording rules. One read-only explorer mapped existing UI/test/harness reuse without execution or edits. Fresh pinned-Node independent strict TypeScript passes. No tests, build, browser, local service, runtime request, credential access or provider call ran in this planning turn. Historical 978-test/build acceptance belongs to M6-02 and was not rerun or claimed as M6-03 verification.

### M603-PLAN-REVIEW-01 — Planning readiness

On 2026-09-21, a fresh read-only `independent_reviewer` returned **PASS for planning preparation; execution remains blocked**, with no findings. It reviewed the complete plan/index, controlling requirements and workflow, M6-02 current state, actual harness interfaces and fixture exports. It confirmed dependency honesty, all selected accessibility rows, compact automated/visual coverage, Deferred exclusions, reuse, worker ownership and command gating. It made no edits or executable/browser/provider checks and did not reproduce the primary's strict TypeScript result.

Primary accepted this as documentation readiness only. Separate deterministic validation passes for both changed documents: all local links/anchors, both PowerShell blocks, all sixteen plan sections, UTF-8 without BOM, final newlines, trailing whitespace and `git diff --check`, with direct untracked-plan inspection. Git contains only this plan and its index change. Roadmap counts remain 25 Complete, one In progress and two Not started.

Documentation impact: created the dependency-gated plan and indexed it under prepared plans awaiting prerequisites. No roadmap status, requirement, ADR, product behavior, retained evidence, developer command or progress record changed. The frontend-quality skill informed reuse-first evidence and desktop/narrow visual boundaries; it did not authorize implementation. Dependency resolution, G bindings, coverage qualification and real visual results remain pending.

### M603-READINESS-01 — Conditional authorization and blocked execution

2026-09-21: the owner authorizes execution through G/A/B/C only after existing readiness gates pass, explicitly without amending the M6-02 dependency or authorizing its recovery. Fresh repository inspection confirms HEAD `470e5746265ef32f46291d45fa46a4c989e532da`, the existing modified plan index and untracked M6-03 plan, and absence of `logs/agent-flow-leases/v2/active.json`. Existing planning changes are preserved.

The roadmap still records M6-02 In progress and M6-03 Not started. M6-02's current state and M602-SUC-FINAL-01 retain unmet task Verification, stopped original/successor campaigns and uncertain native metadata transport closure. Its final PASS is for the stopped handoff only. No explicit scheduling amendment exists in the current task authority or this execution request. Execution readiness therefore fails; planning PASS does not clear it.

No tests, build, browser, service, provider/runtime request, credential access, implementation lease, M6-02 diagnostic or cleanup action ran during this readiness check. Prior automated evidence remains historical; all M6-03 automated and visual execution remains not-run. No progress record or later task was activated. The smallest next owner action is to authorize bounded M6-02 prerequisite/cleanup resolution under that task, or explicitly amend M6-03 scheduling with a disposition of the cleanup uncertainty. This record grants neither route.

Documentation impact: reconcile this plan's conditional authorization, current state and next action only. The prepared-plan index remains accurate; roadmap, requirements, commands, prior evidence and M6-02 records remain unchanged.

## Interfaces and Dependencies

Reuse installed React, Playwright, axe and the current application UI and controlled test collaborators. This task selects no dependency, product API, persisted schema, provider, UI subsystem or backend execution mechanism. Preserve client/service direction; test-only bridge collaborators never enter production imports. Any task-owned fixture/caller must stay small, purpose-named and subordinate to the existing test harness.

## Revision Note

2026-09-21: Created at owner request as documentation preparation while M6-02 remains incomplete. Kept M6-03 Not started, preserved the scheduling and cleanup gates, mapped the compact accessibility proof and existing reuse surfaces, excluded Deferred verification and new actual effects, and left exact execution bindings gated before writes/browser work.

2026-09-21: Recorded independent planning-preparation PASS with no findings and two-document validation. No execution gate, authority or allowance changed.

2026-09-21: Recorded conditional G/A/B/C authorization and the failed readiness check. Preserved the prerequisite gate, Not started status, prior allowances and existing planning changes; no executable work began.

2026-09-21: Reconciled the dependency summary with M6-02's separately authorized continuation and current-resource reconciliation. Historical failure/cleanup evidence and M6-03 Not started status remain unchanged; no M6-03 execution allowance opened.

2026-09-23: Reconciled the current dependency summary after M6-02 Verification and documentation closure. Preserved the original blocked-readiness record and conditional execution authorization; M6-03 remains Not started pending its own G readiness procedure.

### M603-EXECUTION-01 — Dependency-ready activation

2026-09-23 UTC: actual Git HEAD `69e7652e069a7a39f8c45ec75d886a2b88fc2b8b` is clean before task maintenance. The primary directly reads current roadmap, M6-02 archived current state and full M6-03 plan; an independent read-only authority mapper confirms the same gate, all applicable Accepted Must rows and unchanged deferrals. M6-02 reports Complete with owned resources closed and no remaining blocker. No historical operation allowance is renewed. The stale M6 gate summary is reconciled with the completed native result.

The installed Node 24.20.0/npm 11.19.0 pair matches maintained README preparation. All four existing scratch directories (m103-scan, m104-setup, m104-ui, m105-integration) are empty, and no lease is active. Ignored temp/m603-verification-v1/baseline.json freezes 261 source/test/corpus/evaluation/run/build/configuration rows, HEAD and browser marker; SHA-256 `6d824726c92c22dc59dfdf289d440566f7fb4ecc7d7266e3aa9d7bbb2ec7f089`. Existing marker mtime is 2026-09-01T19:46:51.654566Z, within the harness's 30-day bound. Installed Playwright coreBundle.js owns host validation and writes the empty marker only after successful validation; the retained marker is consumed unchanged, not freshly fabricated. Browser launch later verifies151.0.7922.34. No browser, service, model, runtime probe, credential or provider operation occurs during this entry.

Primary owns documentation and source/evidence identity, with actual role definitions preserved. R0 deterministic reuse needs no analyst/drafter/R3 research. Cross-UI interaction characterization is S2. Initial reuse is REUSE_AS_IS for the six UI ownership groups in the map; no application-source responsibility changes are planned. Read-only preflight may identify a bounded test composition, not authorize production edits. One preflight, one characterization or Red, one ordinary same-contract correction and a conditional third follow the existing workflow; fresh review at integration plus a different final reviewer follows the explicit owner request. No duplicated full-suite run is required at handoff.

### M603-A-PREFLIGHT-01 — Read-only coverage packet

Milestone Assignment Packet v2: workflow m603-application-accessibility-verification; roadmap M6-03; slice M603-A; assignment PREFLIGHT-01; lease None; phase preflight; attempt1; correction parent None; role test_worker; owner m603_tests; guard digest None.

Capsule: this ExecPlan owns acceptance. Exact authorities are REQ-A11Y-001–004/006/009/010 in APPLICATION_ACCESSIBILITY.md, SPEC-001–008 presentation portions, OD-022/024/025/027, ADR-0012 and ADR-0024, and roadmap M6-03 Verification. Readiness is M603-EXECUTION-01 and the unchanged existing evaluation definitions. Automatable behavior uses the preflight/characterization-or-TDD route; manual rendered observation is non-TDD. Accepted test boundary None pending preflight; test owner m603_tests. Relevant paths are the six UI suites and m104 harness, m402 review/m503 comparison fixtures, actual src/client owners and shared status pattern. Production placement, dependency/interface changes and permitted structural refactor: None. Reuse actual App/harness/collaborators; no new production framework, observer, backend or model effect. Named uncertainties are full-path fixture composition, continuous Tab/no-trap coverage, exact caller/fixture identity and corresponding commands. S2 integration review; escalate only a demonstrated critical risk.

Write scope: allowed files None; allowed roots None; forbidden files all; forbidden roots all. Validation cwd is repository root; focused command is read-only source/test inspection only (no browser, caches or fixtures); task-level command remains G-gated. Expected result is exactly one workflow classification with precise existing coverage and the smallest uncovered gap, proposed exact files/exports/command. Relevant fingerprint is baseline.json/HEAD above; environment is read-only, no external execution evidence. Side effects and cleanup None. Budget one preflight turn, no write/correction/no-diff turn consumed; return on unknown/conflicting authority or unsafe command. Handoff names exact evidence, classification, bounded gap, responsibility fit, residual limits and documentation impact. The primary is independently resolving G command/resource bindings; no extra reading expands this packet.

2026-09-23 revision: Activated M6-03 after current dependency inspection, recorded baseline and read-only preflight packet, and reconciled roadmap/progress navigation. Exact G proof remains required before browser commands or worker writes.

### M603-G-PROOF-01 — Frozen compact accessibility proof

Primary accepts PREFLIGHT-01 as EXISTING_BUT_UNCOVERED. The six existing suites cover canonical states, negative outcomes, names, relationships, announcements, selection and pointwise focus/axe checks. The only new test contract is continuous keyboard traversal of the composed core path; its same exercised path supplies visual checkpoints. No production change, separate manual runner or new abstraction is selected.

| Requirement | Exact existing evidence owner and added proof |
| --- | --- |
| REQ-A11Y-001 | target-results-ui selection focus; finding-guidance-ui explicit activation; finding-generation-ui mode-bound explicit action; finding-review-ui native controls/keyboard selection; intentional-rescan-ui explicit mode/focus; comparison-ui keyboard baseline/later navigation. New application-accessibility-ui characterizes uninterrupted Tab/Shift+Tab traversal, radio/select key operation and keyboard activation through the full path without direct focus jumps substituting for navigation. |
| REQ-A11Y-002 | All six suites' accessible names/relationships, validation and canonical status assertions; new path checks named controls/regions as the real UI settles. |
| REQ-A11Y-003 | Existing axe/overflow/non-color assertions plus actual primary visual inspection at desktop/narrow checkpoints, including readable evidence/citations, proposal/review separation and visible focus. Zoom200% remains not-run/Deferred. |
| REQ-A11Y-004 | All six suites' single shared status, settlement/error/abstention/review/comparison and focus-preservation assertions; new continuous path checks its expected current announcements. No speech proof. |
| REQ-A11Y-006 | Same new test-owned flow supplies one documented actual-rendered visual smoke; existing tests retain negative states. No exhaustive state/viewport matrix. |
| REQ-A11Y-009/010 | target-results-ui complete collections/manual tags/names/aria-pressed/exact selected announcement and focus; new flow reaches/selects the proposal-bearing Finding from the complete results, retaining visible/programmatic selection. |

Test-side ownership: create only tests/helpers/m603-accessible-core-path.ts (cohesive fixture composition and real UI traversal) and tests/application-accessibility-ui.test.ts (test lifecycle, assertions and optional task-specific evidence publication). Export M603CorePathCheckpoint, m603CorePathViewports, createM603CorePathFixture and exerciseM603CorePath from that helper. Reuse m104 startHarness(false,'app'), m402 reviewProfileStages/reviewedProfileRun and m503 comparison fixtures; the helper depends on existing fixtures/harness types, not the production service or provider adapters. Production responsibility placement, runtime/interface changes and structural refactor None. All production owners remain REUSE_AS_IS. No existing test/helper edit is needed.

The controlled sequence is target URL and explicit Local mode → Analyze → complete results → keyboard selected Finding → Get guidance → supported canonical passages/citations → Generate → proposal → one synthetic approve decision with support and blocking judgment controls → saved decision → explicit later mode and intentional rescan → comparison → baseline preview with original proposal/saved decision → return to later results. UI actions are genuine; callbacks inject validated fixture responses. No authentic owner review, new scan/retrieval/generation, provider request, credential or production-data write is claimed. Assert exact stage counts for analyze/guidance/generation/review/rescan (one each), comparison read as the existing App requires, and zero HTTP fallback; report actual counts. Browser requests are restricted by the existing harness to its own origin, with service workers blocked. Citation links are inspected but never opened externally.

Baseline.json and environment.json in temp/m603-verification-v1 pin current source/test/fixture/build/input/runtime identities before writes. Environment SHA-256 `b070bee332545440fb64b4f77455602389bf511edfb1d2c1259efc8ec91906b1`; browser executable SHA-256 `409805a16d6416087e6b2f778df1cf8f7bbb267d6b99f6b5bb0a618eace234f2`. Node 24.20.0, npm 11.19.0, Playwright 1.62.1, Chromium 151.0.7922.34/revision1234, axe 4.13.0, React 19.2.8, TypeScript 7.0.2 and Vite 8.0.16 are installed. The retained September 1 host-validation marker is read unchanged within 30 days; Playwright coreBundle.js33393–33409 produces it only after host validation, and m104 harness364–420 consumes/authenticates its time and unchanged runtime. No marker rewrite/download/install is authorized. Exact new helper/driver hashes and subsequent evidence bind at accepted lease closure before capture.

Commands run from repository root using only the maintained README preparation definition. Focused command: Invoke-M105Command { & $m105Node --test --test-timeout=120000 tests/application-accessibility-ui.test.ts; if ($LASTEXITCODE -ne 0) { throw 'M603 characterization failed' } } $m105UiTemp. Both independent strict TypeScript (node node_modules/typescript/bin/tsc --project tsconfig.json) and Vite build (node node_modules/vite/bin/vite.js build --configLoader native) use Invoke-M105Command and explicit exit checks. Inspect exact existing dist/client inventory before rebuilding; only that known generated subtree may change. No source/test change is implied by build authorization.

After integration, the maintained full-suite block runs once sequentially with the new test appended in its UI group and all capture/frozen-case flags absent. Its six existing UI commands also satisfy B's focused-suite requirement; do not execute them a second time solely for a phase label. Scanner/integration tests retain documented controlled intercepted-page effects and exclusive scratch; they make no new public/model call. The full suite, strict/build and affected proof are independent of worker claims and accepted from actual logs.

Visual command is the same focused command with only A11Y_M603_CAPTURE_PROOF=1 set process-locally and exactly restored. Default mode creates no visual output. Capture requires absent ordinary temp/m603-verification-v1/visual, creates only its bounded viewport PNGs and content-safe observation metadata exclusively, never overwrites, and retains them for review. One path captures useful results, guidance/citations, proposal/review, comparison and saved-decision baseline checkpoints at 1366x900 and 390x844, 100% zoom, using viewport screenshots/appropriate scroll targets rather than giant full-page captures. Primary inspects actual rendered images; no automated assertion labels them visually passed. Use one capture after accepted characterization; a failed capture stops for triage, with a correction/affected re-observation only when existing task authority and budgets justify it.

Owned resources: existing m104 harness opens one loopback Vite listener on127.0.0.1 port 0 (actual port in log), one pinned managed Chromium/context/page and only m104-test-entry.html/tsx plus vite-cache under temp/m104-ui. It uses ten-second launch/navigation, five-second default action and 120-second Node test bounds, always closes in finally, asserts listener refusal, no external/page errors, unchanged browser tree/marker and empty scratch. The caller checks empty m103-scan/m104-setup/m104-ui/m105-integration before/after; preserve unfamiliar contents instead of clearing them. Baseline canonical runs, frozen evaluation/corpus files and all unchanged source/test owners must still hash-match after proof. No developer-owned browser/runtime termination or model probe is permitted. Actual browser evidence is non-reusable except its exact isolated run identity, state and recorded hashes; documentation-only edits do not invalidate it.

### M603-A-EVIDENCE-01 — Characterization assignment

Milestone Assignment Packet v2: workflow m603-application-accessibility-verification; task M6-03; slice M603-A; assignment EVIDENCE-01; phase evidence; attempt1; correction parent None; role test_worker; owner m603_tests; lease M603-A-evidence-01; guard digest supplied from start in the assignment message. Inherit the complete PREFLIGHT capsule and G contract above. Preflight EXISTING_BUT_UNCOVERED is accepted; characterize existing behavior without fabricated Red or production edits. Accepted test boundary None until handoff; test owner m603_tests. Allowed files exactly tests/helpers/m603-accessible-core-path.ts and tests/application-accessibility-ui.test.ts; allowed roots None; forbidden files None; forbidden roots src,docs,evaluation,corpus,data,.codex,.agents. No Git writes. Relevant fingerprint baseline.json plus the exact initial absence of both new paths; environment.json and maintained preparation control. Focused command is the exact G command with capture flag absent; task-level full suite/strict/build/capture remain primary-owned. Expected result one meaningful passing continuous-path test; any observed production defect or incoherent fixture returns for primary triage, never weakened assertions. S2 work-slice review covers fixture fidelity and evidence before primary capture; a different fresh final independent reviewer owns integrated closure review under the explicit owner request. One initial write plus one ordinary correction, conditional third only under recorded workflow conditions; same failure twice without new evidence or any changed binding stops. Preserve failed logs; no retries until green. Validation cadence one focused execution after coherent implementation, affected checks only for justified corrections. Ignored focused log is temp/m603-verification-v1/characterization-01.log. Handoff reports actual paths/diff, exact command/exit, new file hashes, classification, scope/cohesion, cleanup, limitations and documentation impact. No production source responsibility is added.

### M603-A-CORRECTION-02 — Consolidated characterization correction

Attempt 1 closed compliant with receipt bfd5afe326033a3640d8fc1075a50e66f12e746e3b9ff1f1dc82c47038f4865a. Its single focused run failed because the test expected `needs` where the current canonical announcement says `need`. The preserved log SHA256 is 013b5d33580cef02768e290d7a403d6cd168a2be6a83d1129ecd05edb3077ab3. Browser teardown settled with no external requests or page errors; all four scratch directories are empty. This is an invalid characterization expectation, not production Red. No application defect or separate bug record is warranted.

S2 work-slice review returns REVISE: target-seeking Tab traversal permits arbitrary intervening order/document wrap, and the initial capture omits intake, original proposal, lower passage/limitations content and some active focus. Primary accepts these findings. The one review-correction loop consolidates them with the literal fix under the unchanged full-path proof objective, authority, two-file scope, effects and phase. No product change is selected.

Refined G capture binding: retain one controlled path and the two frozen 100% viewports; add intake and original-proposal checkpoints, targeted native evidence/passage text/source notice/proposal judgment/comparison Before-After/limitations frames and active-control frames where section scrolling excludes focus. Each frame records checkpoint, viewport, actual scroll target and active-control identity. This is a small task-specific selection, not all states or all license paragraphs. Existing tests prove complete canonical text; primary inspects rendered readability, wrapping and distinctions. No repeated service outcome is needed for extra scroll frames. Retain create-only visual output, exact source/browser identity and per-viewport page-overflow checks. No screenshot by itself asserts a visual pass.

Milestone Assignment Packet v2: workflow m603-application-accessibility-verification; task M6-03; slice M603-A; assignment EVIDENCE-02; phase evidence; attempt 2; correction parent M603-A-evidence-01; role test_worker; owner m603_tests; lease M603-A-evidence-02; digest supplied at start. Inherit complete PREFLIGHT/G/EVIDENCE-01 capsule, commands, side-effect boundaries, cleanup, cohesion and stopping rules. Allowed files remain exactly tests/helpers/m603-accessible-core-path.ts and tests/application-accessibility-ui.test.ts; allowed roots None; forbidden roots src,docs,evaluation,corpus,data,.codex,.agents. Accepted test boundary None. Production placement/interface/refactor None. Correct exact announcement and replace target searching with explicit meaningful keyboard order assertions including intervening controls and bounded no-cycle/no-unintended-wrap evidence. Inspect current owners and all later expected strings before the one focused execution. Apply the refined capture binding without adding a framework or production behavior. Default test remains capture-free. Run the same focused command once, log temp/m603-verification-v1/characterization-02.log; any genuine failure stops for primary triage. Earlier failed behavioral evidence is invalid; source baseline/environment and isolated cleanup facts are retained. One ordinary correction is consumed by this turn; conditional attempt 3 requires new evidence and the workflow conditions, never an automatic retry. S2 reviewer rechecks only corrected findings read-only; primary owns subsequent strict/build/full suite, capture, visual inspection and separate fresh final review. No Git writes.

### M603-A-ACCEPT-02 — Corrected keyboard characterization

The primary inspected the actual two-file change and focused log after lease M603-A-evidence-02 closed compliant (receipt c6cd3f04ba48c1fe3590b4dcfed66c25f10d69da46bbebc0fb52113d54408b90). The corrected characterization passes 1/1 in Chromium 151.0.7922.34. It checks each next Tab stop, reverse guidance traversal, source-notice and proposal links, review control order, app-owned saved-decision/Results focus, and both comparison navigation actions without arbitrary target searching or document wrap.

Observed controlled counts are Analyze1, guidance1, generation1, review1, rescan1, comparison reads 3 and HTTP fallback 0. External requests and page errors are empty; teardown settled, the four scratch directories are empty and no visual output was created. Callback outcomes are injected; none of these counts proves an actual scan, model response or authentic retained review. The failed first attempt remains preserved.

Accepted executable identities: tests/application-accessibility-ui.test.ts SHA-256 `6423cccab5e29ec842ec14b67fec54aa1824a9cf3b0fdac4eabd9f20b5973f3d`; tests/helpers/m603-accessible-core-path.ts SHA-256 `170978d45f96d448115f484329d140412e89304d49f62348599de5d367ae787a`; characterization-02.log SHA-256 `a5ce3e92acb92bb9c0ad678d6bab0531e7a6875468e38142836a60eda01853af`. No source owner or existing test changes. S2 corrected-finding readback remains pending before visual capture. Strict/build/full regression is primary-owned and runs once through the maintained README suite, now registering 54 files and excluding the M603 capture flag. The first 261 baseline rows still match unchanged; further preservation is checked after integration/capture.

S2 corrected-finding review returns PASS with no findings after inspecting both complete files, the focused log and compliant receipt. All three findings are resolved. The capture branch and actual visual acceptance remain primary-owned. Accepted identity and exact command-wrapper hashes are retained in temp/m603-verification-v1/accepted-identity.json before capture. No additional worker lease is active.

### M603-B-C-VERIFY-01 — Integrated and rendered evidence

Primary executes the maintained 54-file regression once, after independent strict TypeScript and the inspected client build: **1061 tests pass, zero failures/skips**, all commands exit 0. The six mapped UI suites are included, covering canonical/negative states, axe/contrast, names, relationships, full navigation, announcements and focus. This single integrated run satisfies B's focused-suite lane and C's regression lane without duplicate execution. integration.log SHA-256 `0cedb18ad47da9c6b5fbd4ea312d2c199d1c172a39fab3748da4bd329173b94b`; integration-summary.json retains counts. No application source or existing test changed; generated client bytes also remain identical.

The separately enabled, create-only capture runs the accepted full path once in Chromium 151.0.7922.34 at 1366x900 and 390x844, 100% zoom. Capture passes with the same controlled counts 1/1/1/1/1/3/0, no external requests/page errors and settled teardown. The actual loopback origin is127.0.0.1:5173. Capture log SHA-256 `11f9d2e1033d455d750844833632e18bf313344dd9b6870140cb0977501a82d6`; visual/observations.json SHA-256 `bfd422d957252df56b85f180a18f6fa672e5131bfdaf1c041ee78441623b34be`. Thirty viewport frames cover 15 selected scroll targets, not 30 independent workflow cases. The actual browser/DOM interaction is real; fixture scan dates/browser fields and service outcomes remain synthetic.

Primary directly inspects all 30 original PNGs. Results:

| Observed region | Desktop and narrow result |
| --- | --- |
| Target/mode/Analyze | Readable native form and explicit mode; clear Analyze focus outline. |
| Results and selected native evidence | All synthetic Findings/manual observation are represented; manual tag, grouping, selected border and focus are explicit. Narrow results use their named scroll region; native evidence fields wrap readably. |
| Guidance/citations/notices | Representative complete passage and source-notice text wrap naturally; citation links remain readable and underlined. Existing semantic tests separately verify complete canonical text. |
| Original proposal and review | Original interpretation, supporting evidence/guidance, blocking judgment and post-change reminder remain distinct from human controls. Approve and Save focus are visible; confirmation text and fields reflow. |
| Comparison | Before/After evidence is side by side on desktop and sequential on narrow. Native passing observation, textual outcome, limitations and context-only human work remain explicit. |
| Baseline navigation and saved decision | Both navigation controls have visible focus. Original proposal is preserved above the distinct saved Approve decision and judgment. |

All30 capture checkpoints report no horizontal page overflow. Viewport boundaries and the bounded results scroll region are ordinary vertical scrolling, not missing canonical text. This is a bounded visual smoke, not inspection of every license paragraph or a support matrix. No material accessibility defect was demonstrated and no production fix is selected. All200% testing remains Deferred; manual screen-reader speech is not-run/excluded. No WCAG-conformance, universal-usability or release claim follows.

Preservation: all 261 frozen source/test/corpus/evaluation/canonical-run/build/configuration rows hash-match; the only added executable inputs are the two accepted test files. Browser marker mtime remains 1788292011654566300ns. All four scratch directories are empty; owned harness browser/context/listener teardown settled and no lease remains active. No developer model runtime operation, credential read or authentic review occurred. Exact frame hashes and primary observations are retained in final-readback.json SHA-256 `3aaeb9c444d7f37afe31a42bf1edd04ad976db24db6c19b668768873880605aa`. Local artifacts are ignored and absent from a fresh checkout; tracked records preserve results and identity, not portable evidence availability.

B is accepted. C executable verification is accepted; a different fresh final independent review and documentation/status/archive gate remain pending. Executable evidence remains valid during documentation-only closure because relevant input bytes and command semantics are unchanged.


### M603-FINAL-01 — Independent review and task closure

A different fresh final independent reviewer returns **PASS with no outstanding findings**. It recounts all 1061 passing tests across 54 files, inspects the actual code and successful strict/build/capture records, independently views 12 selected original desktop/narrow screenshots, authenticates all 261 baseline rows and 31 visual files, and confirms empty scratch and no active lease. The primary's broader observation covers all 30 images. No executable rerun is needed for this read-only review.

The candidate documentation gate passes: eight affected documents, 562 local links/anchors, seven README PowerShell blocks parsed without execution, UTF-8/final-newline/trailing-whitespace checks, untracked-file inspection and git diff --check. A transient primary documentation encoding error was corrected before acceptance; original copyright and license wording are unchanged. No product requirement, ADR, schema, dependency, frozen input or application source changed.

Documentation impact: updated the maintained regression command and capture exclusion, current README/UI evidence summaries, roadmap status, task plan/index and progress record/index. Reconciled two stale Local-generation presentation descriptions against the already accepted M6-02 implementation. The roadmap now marks M6-03 Complete; M6-04 remains unselected. Archive movement preserves historical headings and repairs relative/inbound links, followed by proportional final link/format/status checks. This documentation-only movement does not invalidate the accepted executable identity.

All task-owned resources are closed, original runs/evidence remain preserved and changes remain uncommitted. No commit, publish or push was performed. Remaining limitations are controlled service outcomes, sampled Chromium/viewport rendering, Deferred 200% testing, excluded screen-reader speech, and no broader accessibility/support/release qualification.

2026-09-23 revision: Completed the dependency-ready G/A/B/C execution, preserved the initial test failure and accepted correction, recorded automated/visual evidence and fresh final PASS, reconciled affected documentation, and archived after task Verification and the documentation gate passed.
