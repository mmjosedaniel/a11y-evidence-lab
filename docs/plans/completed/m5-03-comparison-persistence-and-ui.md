# Persist and present comparison evidence — M5-03

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Owner:** [M5-03](../../DEVELOPMENT_ROADMAP.md#m5-03--persist-and-present-comparison-evidence), Complete. The owner authorized G, sequential A–C, D verification, required independent reviews, corrections within existing budgets and documentation closure. Controlled scanner/service/disk/browser verification and exact task-owned synthetic baseline deletion are authorized; original retained runs, public scans, model/provider calls, dependency acquisition, later tasks and Git publication remain excluded.
- **Prerequisite:** M5-02 is Complete at [M502-FINAL-01](m5-02-conservative-comparison.md#m502-final-01--integrated-review-and-task-closure). Twenty-three tasks are Complete; M5-04 and M6 remain unselected.
- **Latest evidence:** [M503-FINAL-01](#m503-final-01--integrated-review-and-task-closure) accepts the different final critical review, complete regression, native deletion proof and documentation gate.
- **Active lease:** None. All writers are retired. All three C test and Green writes and its one review correction loop are consumed; D used evidence attempt 1. No budget is reset.
- **Next:** None within M5-03. M5-04 remains Not started and unselected; no later work is authorized by this closure.

## Progress

- [x] (2026-09-19 14:14Z) Reviewed roadmap-wide status, current M5-02 closure, applicable authorities and actual backend/client seams; verified strict TypeScript, 111 focused tests and protected inputs in M503-ENTRY-01.
- [x] (2026-09-19 14:26Z) Independent critical planning PASS without findings; primary accepted the readiness review and five-document reconciliation in M503-PLAN-01.
- [x] (2026-09-19) Execution authorized; fresh clean HEAD, runtime, strict TypeScript and 111 focused tests verified in M503-ENTRY-02.
- [x] (2026-09-19) G accepted: two critical dimensions and bounded follow-ups, mandatory DRAFT READY, fresh pre-draft PASS and different final PASS after one supported artifact correction.
- [x] (2026-09-19) A — Closed durable comparison contract and aggregate update; 216 focused tests, independent strict TypeScript and fresh critical PASS.
- [x] (2026-09-19) B — Service publication, validated readback and client admission; 114 focused passes, independent strict/build and fresh critical PASS without findings.
- [x] (2026-09-20 UTC) C — Accessible comparison presentation and navigation; 29 UI passes, independent strict/build, six visual samples and critical re-review PASS after one bounded correction loop.
- [x] (2026-09-20 UTC) D — Controlled integrated proof, full regression, different final critical review and documentation closure.

## Surprises & Discoveries

- M5-02 computes a bounded `ComparisonResult` in `src/server/local-service/rescan-comparison.ts`, but `service.ts::rescanFinding` deliberately discards it. Existing schemas and integration assertions reject comparison fields. M5-03 must intentionally supersede only those task-boundary exclusions; native candidate archives remain forbidden.
- A resolved later run may have zero Findings. Mounting comparison only inside a selected later `ResultDetail` would hide a valid result. The later-run Results composition needs a comparison owner independent of later selection.
- Existing single-file publication and selected-Finding transitions already protect completed evidence. New comparison data must survive later guidance/generation/review writes, without creating a second storage mechanism or invalidating old records.
- The first planning test invocation omitted `--experimental-test-module-mocks`: `comparison-outcome.test.ts` stopped at `mock.module`, not a product assertion. The corrected prepared command passed all 111 tests. This is command-preparation evidence, not a product regression or a TDD Red.

## Decision Log

- Decision: Plan only M5-03; preserve M5-02 calculation semantics and M5-04's separate checkpoint. Rationale: the missing capability is durable, truthful presentation, not another comparator or evaluation system. Date/author: 2026-09-19 / primary.
- Decision: Reserve G for exact persistence/publication/readback literals before behavioral preflight. Rationale: serialization, identity, failed writes and late completion cross existing integrity boundaries; guessing these inside Green would bypass the workflow. No option is selected here. Date/author: 2026-09-19 / primary.
- Decision: Reuse the current Results hierarchy and rule-evidence components, with a purpose-named comparison detail owner. Rationale: a run-level comparison remains visible for zero later Findings; proposal/review remain separate context. The frontend-quality skill supplies reuse and browser-proof discipline, not new product scope. Date/author: 2026-09-19 / primary.

## Outcomes & Retrospective

G, A–C and D native characterization are accepted. The repository persists a truthful comparison and presents its evidence, limitations and optional human context independently of later Finding selection. A's 216, B's 114 and C's 29 focused tests, independent strict/build, six visual samples and all slice critical reviews pass. The C review caught synchronous announcement ordering and disappearing-preview focus; regression tests and the final bounded correction resolved both. Native integrated deletion/readback proof and the complete 44-file regression pass. Different final critical review and documentation closure pass. No provider or capacity observation was repeated. M5-03 is Complete; M5-04 remains unselected.

## Purpose / Big Picture

After implementation, a user who intentionally rescans any retained baseline Finding can inspect the saved comparison in the later Results: pair comparability, baseline evidence, a uniquely matched later target when available, the conservative outcome, rationale, limitations and non-blocking follow-up. Failure to calculate or save comparison is distinguishable from scan failure and cannot masquerade as a saved result. Already persisted comparison evidence remains inspectable with a broken-lineage limitation after baseline deletion, without recomputation or a hidden baseline copy.

This task implements that storage/API/UI capability and verifies its own boundaries. M5-04 separately demonstrates the complete milestone matrix and bounded public-page behavior; it is not selected or completed here.

## Context and Orientation

### Readiness and authority

The [authority map](../../README.md#authority-and-status-map), [project requirements](../../PROJECT_REQUIREMENTS.md#document-status), and [roadmap authority-location key](../../DEVELOPMENT_ROADMAP.md#authority-location-key) control interpretation. Applicable Must rows are Accepted; [OD-025](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-025--development-authorization-and-roadmap-governance) permits exact-task development only after selection. [OD-026](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-026--defer-user-facing-retained-run-reopening) still defers reload restoration, deep links and retained-run browsing. [OD-027](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md#od-027--simplify-analysis-and-results-presentation) preserves the concise Results hierarchy.

| Controlling source | Exact task obligation |
| --- | --- |
| [Rescan and comparison](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#rescan-and-comparison), REQ-COMP-004–008 | Baseline reference/evidence; conditional after-evidence; conservative result; independent scan-only calculation; durable later aggregate; deletion limitation; no broad claims. REQ-COMP-001–003 continue to control any reused controlled cases. |
| [Evidence and provenance](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#evidence-and-provenance), REQ-EVID-003/008/010/011 | Preserve immutable sources and siblings, minimized evidence, exact locator limitations, one versioned aggregate and no new domain IDs. |
| [Interface](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#evidence-oriented-interface-and-export), REQ-UX-002/004 | Distinct source layers and truthful canonical outcomes, not a second UI workflow. |
| [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md#accessibility-of-a11y-evidence-lab), REQ-A11Y-001–004/010 and verification REQ-A11Y-006 | Keyboard, semantics, non-color distinctions, shared announcements and focus retention; desktop/narrow visual inspection. All 200% zoom tests remain Deferred; no manual screen-reader claim. |
| [Lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#accepted-minimal-information-model), parent execution, comparison and retention sections | Completed scan survives a downstream save failure; no unsaved-success claim; persisted comparison remains readable without its baseline; no reopen/history feature. |
| [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md#decision), [ADR-0011](../../architecture/decisions/ADR-0011-typescript-as-initial-application-language.md#decision), [ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md) | Single-file safe updates, closed runtime validation, strict typing, unprivileged React presentation. |
| [SPEC-006, SPEC-008](../../specs/SPEC.feature), [HS-015](../../specs/HARD_SPEC.feature), [BHV-06](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#derived-behavioral-scope) | Before/after evidence, context-only human work, visible limitations/reminders, no accessibility/conformance/causality claim. SPEC-007 also constrains aggregate preservation. |
| [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), [worker procedure](../../../.codex/execplan-implementation-workflow.md), [guard](../../../.codex/write-lease-guard.md) | Independent test/code ownership, preflight, sequential guarded writes, fresh risk review, honest evidence and closure. |

The [evaluation freeze](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#freeze-boundary) preserves RD-003's six states, M3-01's definition and references, and the [M5-02 companion](../../../evaluation/m502-comparison-v1.json). No frozen content, expectations, directed pair or profile is revised. Arithmetic contrast vectors and reverse binary evaluation remain policy-only, never genuine scans or persisted public binary regression. M2-02 and M3-03 capacity gates are Complete; their observations are configuration-specific and not repeated here.

### Current implementation and responsibility map

`src/server/comparison/comparison-contract.ts` owns the internal result and input readers; `compare-finding.ts`, `scan-pair.ts`, `target-correlation.ts` and `finding-outcome.ts` own deterministic policy. `local-service/rescan-operation.ts::prepareRescan` captures the validated baseline. `rescan-comparison.ts::executeRescanComparison` consumes operation-local candidates, releases them and suppresses comparison success on failed publication/stop. `service.ts` owns composition and reservation. `scan-operation.ts` owns terminal scan publication, not a second downstream lifecycle.

`domain/run-contract/run-types.ts` and `run-validation.ts` own the closed aggregate. `persistence/run-repository.ts` owns staged safe writes and the rename commit point; its `run-transition.ts` and `selected-finding-transition.ts` constrain updates. `local-service/rescan-api.ts` frames rescan responses; `loopback-api.ts` and `service.ts::readRun` serve validated reads. Accepted A adds comparison-types/comparison-validation and the append-only updateComparison transition; accepted B integrates durable publication, immediate lineage and client admission. The original absence described in planning evidence is historical.

The browser uses `rescan-transport.ts`, `rescan-request.ts` and `rescan-admission.ts` to correlate one submitted intent. `App.tsx` owns shared run/selection, immediate baseline preview, request ownership and one live-status region. `components/results/ResultsSection.tsx` composes Results; `ResultDetail.tsx` owns selected evidence; `RuleEvidence.tsx` and `resultPresentation.ts` format retained facts. `IntentionalRescanForm.tsx` owns admission controls and status. `main.tsx` is composition only. Preserve these boundaries rather than placing validation, persistence or a new detail region inside App.

## Scope and Non-Goals

Include one intentional rescan's comparison in its later aggregate, strict read/update validation, truthful service/client transport, missing-baseline inspection, and a bounded comparison region with before/after evidence and accessible feedback. Old runs without comparison stay readable without migration. Preserve baseline and later source collections and every existing downstream Finding value; an unchanged comparison must survive later selected-Finding updates.

Exclude new comparison rules, fuzzy correlation, additional positive archives, page/DOM diff, independent comparison IDs/files, history/version graphs, schema frameworks, generic transactions, databases, polling/monitoring, auto-retry, reload restoration, arbitrary pair selection, charts, scores, batch actions, exports, new dependencies and broad structural refactors. No provider invocation, retrieval, model acquisition, actual public target scan, mutation/deletion of original retained runs, commit or push is part of authorized execution. Do not relabel AI proposals or human decisions as evidence of remediation success.

## Plan of Work

### G — Freeze task-owned literals and command effects

After execution authorization, refresh current HEAD, status, active-lease absence, runtime, protected inputs and the actual affected source/test/configuration identities. Historical HEAD in M503-ENTRY-01 is provenance, never a mandatory future checkout or dirty-path condition. An intentional plan commit does not invalidate execution; unexplained drift still requires reconciliation.

Complete the R3 contract below before A preflight. Freeze the smallest closed persisted representation, cardinality, allowed historical variant, baseline/later relationship checks, permissible copied baseline facts, unique-incomplete representation, non-failing observation, delta/reason/limitation validation and exact mutation rules. Preserve old format readability; choose no migration framework. Resolve the acyclic dependency boundary: `validateRun` must not import a comparator/input reader that calls `validateRun` recursively. Any shared extraction is a leaf value/reader responsibility with demonstrated consumers, not a generic utilities layer.

Freeze publication sequencing, failure categories, commit truth, service reservation/release, postcommit abort/shutdown, and compare-save failure handling. Explicitly distinguish a saved completed scan, a saved comparison, an unsaved comparison and unknown transport outcome. Resolve baseline availability at read/action boundaries, including not-found versus invalid/unreadable, stale captured previews, deletion during execution, and preventing use of a broken-lineage comparison as a new baseline. Do not make baseline availability a new deterministic outcome or recompute saved classification during display.

Freeze exact HTTP/readback and client contracts, unchanged intent correlation, state ownership, error/announcement wording, source-layer context and the accepted C reuse audit. Determine how a completed result with zero later Findings exposes comparison and how legitimate later downstream updates preserve it. Complete the command/effects slots before any dependent lease; no worker invents binding commands or schema decisions.

### Shared ownership, attempts and acceptance

A–C are behavior-bearing, TDD Applicable. Each uses one read-only `test_worker` preflight, then existing covered evidence, a passing characterization, or one coherent Red for the confirmed missing/regressed contract. PARTIAL is narrowed by the primary; UNKNOWN/CONFLICTING stops. Apply ADR-0024's first-module exception only to the exact absent callable after proving the runner: all behavioral tests still must execute unchanged in Green. Never fabricate Red from a missing flag or setup failure.

Primary projects each slice into [Milestone Assignment Packet v2](../../../.codex/execplan-implementation-workflow.md#milestone-assignment-packet-v2), freezes exact filenames within the envelopes below, and opens/closes one exact [lease](../../../.codex/write-lease-guard.md) for every worker write turn. Only one writer is active; test and Green roles remain separate. Primary documentation/authority/guard maintenance occurs between leases. All accepted tests are forbidden to Green; all production paths are forbidden to Red. Workers cannot edit documentation/evaluation authorities or Git metadata.

Each unchanged role/phase chain allows attempt 1, ordinary correction 2 and conditional final correction 3 only with the recorded evidence and predecessor required by the procedure. One review correction loop per slice; two repeated identical failures without new evidence, two no-diff write handoffs, unsuccessful attempt 3, binding-field changes or exhausted allowance stop for triage. New IDs/agents never reset budgets. Retire writers at the slice barrier. Evidence reuse requires exact command, cwd, relevant-tree and environment identity plus guard-backed no drift; mutable external checks are Non-reusable unless isolated state is pinned. Inspect actual diff, terminal receipt, test relevance and `RETAINED`/`REFACTORED`/`RECONCILE` cohesion before acceptance.

### A — Closed durable comparison and aggregate transition

One indivisible outcome: the repository can validate, save and read a permitted comparison on the already completed later run, preserving both source records and every sibling/downstream field, or retain the last valid aggregate on failure. Prerequisite G accepted. Test owner `test_worker`; implementation owner `code_worker`; S3 for custom serialization, integrity and recovery. Authorities: REQ-COMP-004/006–008, REQ-EVID-003/008/010/011, lifecycle and ADR-0021/0011.

Production envelope: extend `src/server/domain/run-contract/run-types.ts`, `run-validation.ts`, `src/server/persistence/run-repository.ts`, its `contracts.ts`, `run-transition.ts`, `selected-finding-transition.ts`; bounded CREATE of purpose-named comparison reader/transition modules under those existing roots. Reuse existing descriptor/value readers and safe-write/expected-current checks. A leaf separation from `src/server/comparison/comparison-contract.ts` is permitted only as frozen by G to avoid a dependency cycle; calculation semantics are protected. Domain readers have no filesystem/browser dependency; repository depends on domain validation, never client code. No transaction service, unsafe cast or loose optional-object escape hatch.

Test envelope: `tests/run-contract.test.ts`, `tests/run-repository.test.ts`, `tests/review-repository.test.ts`, bounded new `tests/comparison-persistence.test.ts` and task-specific helper only if current fixtures cannot express the contract. Cover closed unions, references, invalid numeric/delta/evidence combinations, malformed keys/getters, baseline snapshot minimization, old-record reads, one permitted append and rejection of overwrite, downstream preservation, stale expected record, failed write/rename and truthful commit boundary. G freezes exact focused command. Advance after unchanged Green tests, independent strict check, relevant transition regressions and fresh critical PASS.

### B — Truthful service publication, readback and client admission

One indivisible outcome: the intentional rescan returns a truthful durable comparison or bounded comparison failure through the existing application boundary, and the client admits only its captured run/Finding intent. Prerequisite A accepted. Test owner `test_worker`; implementation owner `code_worker`; S3 for lifetime, identity and late publication. Authorities: A plus REQ-UX-004, lifecycle parent execution and deletion boundary, ADR-0012.

Production envelope: extend `src/server/service.ts`, `local-service/rescan-comparison.ts`, `rescan-operation.ts`, `rescan-api.ts`, `contracts.ts`, `loopback-api.ts`, `scan-operation.ts`/`scan-run-records.ts` only if G identifies a required terminal handoff, and `src/client/rescan-admission.ts`, `rescan-request.ts`, `rescan-transport.ts`, `run-admission.ts`. Bounded CREATE of purpose-named service comparison-publication/lineage and client comparison-admission modules when needed; service coordinates domain result → repository → response; client validates response, never calculates or writes comparison. `main.tsx` may wire a callback, not absorb domain behavior. Exact paths must be frozen in G and the packet.

Test envelope: `tests/rescan-service.test.ts`, `rescan-api.test.ts`, `rescan-comparison.test.ts`, `rescan-admission.test.ts`, `local-service.test.ts`, and bounded new `tests/comparison-service.test.ts` / `comparison-admission.test.ts` only for distinct current contracts. Verify save failure after scan success, response/disk identity, stopped/aborted/late completions, concurrent rejection, selected identity, wrong/foreign/stale/malformed responses, historical no-comparison reads, baseline missing/invalid/unreadable and disabled recomputation/new-comparison use. Preserve no-call behavior and transient candidate disposal. Update old comparison-exclusion assertions only to the accepted nested field, not to allow candidate or raw payload leakage. Advance after focused tests, strict/client build, preserved A evidence and fresh critical PASS.

### C — Accessible comparison in the active Results

One indivisible outcome: the user can inspect the active later run's truthful comparison independent of later Finding selection, with all task-owned evidence/context and predictable navigation. Prerequisite B accepted. Test owner `test_worker`; implementation owner `frontend_code_worker`; `frontend-visual` profile; S2 unless actual identity/concurrency changes trigger S3. Use the same risk reviewer for visual evidence, not an extra panel. Authorities: REQ-COMP-004–008, REQ-UX-002/004, REQ-A11Y-001–004/010/006, SPEC-006/008, HS-015, accepted presentation and [frontend-quality](../../../.agents/skills/frontend-quality/SKILL.md).

Planning reuse audit `M503-UI-01` (primary must refresh/accept in G before preflight): EXTEND `App.tsx` only for shared coordination and existing live announcements; EXTEND `ResultsSection.tsx` to compose a distinct comparison region outside selected `ResultDetail`; REUSE_AS_IS `FindingsPanel.tsx`, existing list semantics and baseline/later navigation where the new contract requires no change; EXTEND `IntentionalRescanForm.tsx` status/availability only as B's contract requires. CREATE `components/results/ComparisonDetail.tsx` for outcome/rationale/limitations and `comparisonPresentation.ts` for a pure view projection. REUSE `RuleEvidence.tsx` and formatting; EXTRACT_LOCAL a narrow evidence-only projection only if positive observations cannot fit without falsely becoming Findings. Freeze its exact path before C. EXTEND `styles.css` only for wrapping, evidence layout and focus using existing tokens. No new hook solely to shorten App, generic component kit, new theme or dependency. `ResultDetail.tsx` may expose existing context through bounded composition, not own comparison lifetime.

Use a named comparison heading, labeled Before/After facts and explicit unavailable-after explanation. Show pair mismatch without target correlation; render a saved unique pass as observation, not a Finding. Keep rule-specific delta distinct from a page score. Existing AI originals, edited proposals and human decisions remain independently labeled context when available; do not copy them into comparison or require them to exist. Follow-up and limitations stay visible and non-blocking. No checked-off manual-check record or remediation-causality wording.

Required state matrix: absent historical comparison; saved resolved including zero later Findings; persistent; contrast improved/regressed; inconclusive unavailable/ambiguous/native-incomplete; not comparable; calculation/save failure with completed scan; transport-unknown; broken lineage; immediate baseline preview and return. Use component/contract tests for the full semantic matrix, not a Cartesian browser matrix. Real-browser inspection covers a saved comparison with Before/After, unavailable-after/broken-lineage limitation, and focus during settlement/navigation at 1366×900 and 390×844. Automate keyboard, labels/headings, live-status semantics, no focus theft, predictable return, inert strings and non-color distinctions. No 200% tests or spoken-output claims.

Test envelope: `tests/intentional-rescan-ui.test.ts`, bounded new `tests/comparison-ui.test.ts`, existing UI helpers only where directly reused. G/C packet freezes exact automation states, ports, build/browser identities, screenshots of synthetic app states only, and cleanup. Advance after tests, strict/build, actual-browser evidence, primary cohesion/reuse acceptance and fresh S2 or triggered S3 review.

### D — Bounded real integration and closure

Use the smallest existing scanner/service/disk/browser harness to prove one directed frozen failing→corrected case through real scan normalization, rescan, saved comparison, validated readback and browser presentation. Prefer extending `tests/rescan-integration.test.ts` with its actual-service lane, plus C's real-browser lane; a mocked HTTP reply alone cannot prove new persistence wiring. G freezes the exact case from the M5-02 companion and the actual source/build/run identities. Distinguish policy-only state fixtures from native observations. No real provider or internet target is necessary.

Verify broken-lineage handling using only exact task-owned synthetic directories/read fixtures; originals are never deleted. The native later comparison remains unchanged and viewable and its limitations become truthful. Combine that D witness with B's valid-Finding positive-control/refusal witness to prove rejection of new comparison from broken lineage; the native zero-Finding case cannot isolate that action gate. M5-04 retains its separate full milestone/deletion/public-page checkpoint; no claim that this bounded proof closes it.

If new executable verification is needed, `test_worker` owns it under an evidence lease after read-only preflight; already implemented behavior receives characterization, not fabricated Red. No production scope is assigned to D: defects return to the owning slice within remaining budgets. TDD Not applicable to read-only/manual inspection and documentation; replacement evidence is hashes, validated disk reads, browser observations and deterministic document checks. One evidence assignment chain uses the shared attempts ceiling. Final review is by a fresh `critical_reviewer` different from slice reviewers, with one supported integrated correction loop subject to remaining implementation budgets.

Run the complete authoritative suite once at closure, independent strict checking and the client build; audit all affected helpers/mocks/skips/focused markers. Reconcile README capability/API/commands, lifecycle's internal-only comparison statement, the accepted presentation extension and UI index, documentation task routing when changed, roadmap, this plan and progress/index owners. Read complete target documents before material edits. No requirement or ADR semantic change is expected. Archive only after Verification and the documentation gate pass; otherwise M5-03 stays In progress with an exact next boundary.

## Decision Review Contract

`M503-G` is decision-oriented R3: custom serialized comparison identity, immutable data, publication/recovery and asynchronous response ownership. The artifact is the primary-authored literal/command section in this plan, not another report or ADR. Primary owns ordinary implementation literals; changed requirements, new durable architecture or expanded external effects require the owner before dependent work. Planning reserved this barrier; M503-G-LITERALS is now accepted after separate pre-draft and final reviews.

One bounded non-ranking discovery may identify the existing implementation alternatives. Then freeze the candidate set and dimensions before comparison: minimally extending the internal result versus a narrow durable projection; staged comparison publication after scan completion versus a bounded coordinated publication arrangement compatible with lifecycle failure truth; extending existing rescan/read responses versus a narrowly necessary active-run comparison-read boundary. These are candidate categories, not accepted designs. Exclude any candidate requiring hidden copies, a history API, new transaction service, candidate archives or changed M5-02 classification. Compare direct reuse/placement, invariant preservation, failure truth, deletion behavior and observable UI support—not speculative flexibility.

Budget: two `critical_researcher` reports, one for durable representation/update integrity and one for operation/readback/lineage/client ownership; each one bounded follow-up only if needed. Primary supplies [Research Assignment Capsule v1](../../../.codex/README.md#research-assignment-capsule-v1) with exact authorities and shared evidence. One mandatory `decision_analyst` synthesis plus one supported correction returns DRAFT READY, RETURN FOR RESEARCH or OWNER DIRECTION. Fresh `critical_research_reviewer` pre-draft checkpoint, at most one supported outline correction; then primary authors literals and a different fresh `critical_research_reviewer` reviews the complete artifact. At most two final-artifact correction cycles, with the entire invariant packet rerun after every R3 correction. No optional drafter or extra research panel is planned. Same decisive gap twice, two unsupported RETURN FOR RESEARCH results, exhausted budget or material scope change stops at the existing reconciliation/owner boundary.

Each invariant starts NOT RUN for M5-03; G records decide-now literals separately from later implementation proof. Both G reviewers own decision coverage, slice reviewers own relevant execution coverage, and the different D reviewer owns integrated coverage.

| ID | Trigger / witness | Required result and proof owner |
| --- | --- | --- |
| I1 | Artifact, task status, all command slots | Complete contract, honest unexecuted states, unchanged authority; G reviews and primary closure. |
| I2 | Closed JSON and foreign/malformed relationship vectors | Exact baseline/later/Finding binding, minimized evidence, no independent identity or hidden copy, old records readable; A critical review. |
| I3 | All pair/match branches, absent locator/ambiguous target | No invented after target; no correlation for mismatch; one real minimized positive only for unique pass; public binary regression rejected; A/B reviews. |
| I4 | Stale update, failed write/rename, success then shutdown | Last valid scan/aggregate preserved, durable response matches commit, no unsaved success or owner overlap; A/B critical reviews. |
| I5 | Later guidance/generation/review update and any baseline downstream state | Comparison and both scan collections unchanged; originals/reviews/siblings preserved; human work cannot gate/calibrate comparison; A/B reviews. |
| I6 | Missing/invalid/unreadable/deleted baseline and stale preview | Saved comparison remains inspectable with truthful limitation; no hidden reconstruction, recomputation or new comparison from broken lineage; B/D critical reviews. |
| I7 | Late/foreign response, unknown outcome, duplicate activation | Captured identity, truthful failure, no unsafe release/retry or overwrite of active results; B/C reviews. |
| I8 | Zero later Findings, before/after and context states, keyboard/navigation | Comparison still visible; distinct sources; shared announcements without focus loss; readable desktop/narrow evidence; C reviewer. |
| I9 | Frozen manifests/fixtures, native versus policy-only tests | Original bytes/profiles preserved, no provider/public call, exact isolated proof and cleanup; D critical review. |
| I10 | Diff/leases, test relevance, documentation and prohibitions | Cohesive modules, unchanged accepted tests during Green, full suite/strict/build, truthful limitations and status; all slice reviewers and D. |

## Concrete Steps

All commands run from `C:/projects/a11y-evidence-lab` in PowerShell 7. Use the complete current [README preparation](../../../README.md#development-command-preparation), dot-sourced in each new session. It defines `$m105Node`, `Invoke-M105Command` and ordinary-path/scratch checks. Do not replay archived acquisition or cleanup commands. No dependency restore, lock mutation or installation is planned.

Read-only entry:

```powershell
git status --short
if ($LASTEXITCODE -ne 0) { throw 'Git status failed' }
git rev-parse HEAD
if ($LASTEXITCODE -ne 0) { throw 'HEAD inspection failed' }
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Reconcile active lease first' }
Get-FileHash -Algorithm SHA256 -LiteralPath 'evaluation/rd003-scan-v1.json','evaluation/m301-generation-v1.json','evaluation/m502-comparison-v1.json'
```

After maintained preparation, current pure baseline command:

```powershell
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Pinned Node required' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/run-contract.test.ts tests/comparison-pair.test.ts tests/comparison-outcome.test.ts tests/rescan-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Focused baseline failed' }
}
```

Expected at planning: strict success, 111 tests pass. Counts may grow during implementation; they are not hard-coded future gates. Preparation disables compilation caching and restores environment. These tests use controlled in-memory/module-mock inputs, not live public scans or model operations.

Future commands become executable only when G records exact values and primary verifies the complete caller, syntax, failure propagation and path/effect containment. Slots cannot be left implicit in a worker packet:

| Slot | Must freeze before dependent lease or verification |
| --- | --- |
| A/B/C focused commands | Exact test list, module-mock/timeout flags, cwd, scratch/cache effects, expected Red/characterization and Green, strict compiler command; new filenames are future until created. |
| Build / full suite | Existing Vite `node_modules/vite/bin/vite.js build --configLoader native` through prepared Node; exact current README test inventories plus new tests exactly once; independent strict; build writes only current `dist/client`. |
| Real integration / browser | Existing pinned Chromium, exact test/caller, controlled companion pair/input hashes, build identity, local service/port ownership, allowed intercepted origin, no unintended outbound traffic, synthetic run IDs/root, browser profile/temp roots and completion conditions. |
| Baseline availability / deletion proof | Exact task-owned isolated baseline/later paths, before/after hashes, validated readback and refusal command, no original-run target, ordinary-path and containment checks immediately before deletion; whether evidence is retained or removed. |
| Preparation / cleanup | Fresh child directory names, ordinary/non-reparse ancestors, Git-ignore/untracked check before write, expected existing/empty inventory, generated dependency location None (no acquisition), generated build/test outputs, owned process/tab handles, shutdown-before-cleanup and exact removal targets. Shared scratch parents, installed runtimes and original data are preserved. |
| Packet / evidence | Exact worker path lease, accepted/protected test hashes, command/environment fingerprints, baseline HEAD plus expected current dirty paths, expected structured results and stop-before-next-effect behavior. Never compare receipts to the historical planning HEAD unconditionally. |

No cleanup command may recursively target a workspace, `data/runs`, shared temp parent or runtime root. A permission denial stops the affected effect; use the permitted approval route, never repeated identical acquisition attempts or another tool to bypass it.

### M503-G-LITERALS — Primary decision artifact

This is the accepted complete task-owned contract. Pre-draft reviewer `/root/m503_g_predraft` returned PASS without findings, independently reproduced the source/test fingerprint and checked 1,500 D7 count/partition predicates with zero contradictions. That accepts the outline only. Different final reviewer /root/m503_g_final returned PASS after correction cycle 1, with every I1–I10 decision disposition passing and no remaining findings. At G acceptance, all A–D runtime witnesses were NOT RUN; the Current state and subsequent artifact entries record their execution without changing this decision contract.

#### Durable representation and append

Select the narrow durable projection, staged append under the existing reservation, and extensions to the existing rescan response and GET. The internal-result alternative duplicates parent facts and equality obligations; coordinated publication cannot remove the independent completed-scan truth; another endpoint supplies no missing capability. No owner-controlled decision remains. These are implementation literals within Accepted authorities, not new architecture.

`CompletedRun.comparison` is one optional closed object on a completed, linked formatVersion-1 run. Absence is the historical variant. Null, array, empty object, running/failed/unlinked placement and unknown keys are invalid. Its base keys are `baseline`, `rationale`, `limitations`, `followUp`. Baseline has exactly `findingId`, `observation`, `requestedUrl`, `scanContext`; observation is the existing minimized rule/checks/evidence/locator shape with `nativeResult: 'violation'`; context is the existing complete allowlisted scan context. Inherit baseline run ID from parent `baselineRunId`; later run ID, requested URL and context from the parent. No independent ID, version, timestamp, failure history, full baseline scan, sibling, downstream state, proposal, invocation or review is copied.

| Branch | Additional exact keys and validation |
| --- | --- |
| Not comparable | `pair: 'not-comparable'`, `mismatches`, `reason: 'pair-mismatch'`. Mismatches exactly equal the canonical nonempty profile comparison. No match, after, outcome or delta. |
| Comparable uncertainty | `pair: 'comparable'`, `match`, `outcome: 'inconclusive'`, `reason` equal to match. No after or delta. |
| Unique violation | Comparable pair, `match: 'unique-violation'`, `after: {kind:'finding',findingId,observation}`, canonical outcome/reason and conditional delta. After equals the sole retained native Finding projection and ID. |
| Unique incomplete | Comparable pair, `match: 'unique-incomplete'`, `after: {kind:'incomplete',observation}`, canonical outcome/reason; observation equals the sole retained ScannerReviewObservation including incompleteReason. No new ID. |
| Unique pass | Comparable pair, `match: 'unique-pass'`, `after: {kind:'native-pass',observation}`, canonical outcome/reason; exactly one closed native-pass observation with the same rule and available exact locator. |

Reuse `compareScanProfiles`, `comparisonProfile`, `classifyEvidencePair`, `comparisonRationale`, `comparisonLimitations` and `comparisonFollowUp`; canonical text and the one-item limitations tuple must match exactly. Delta exists only when returned by the existing classifier and equals its three finite arithmetic values; negative zero is rejected at the boundary. No binary improved/regressed or native-failure-after-pass public branch. Reuse descriptor-safe closed readers; reject getters, malformed keys and contradictory values. No runtime call from the aggregate reader to `compareFinding`, `readComparisonRuns` or `validateComparisonInput`, because they reach `validateRun`. Narrow `comparisonProfile` to the actual `{requestedUrl,scan:{context}}` input without changing its algorithm. Type-only comparison-contract imports are permitted; no fabricated completed baseline or generic helper extraction.

For comparable pairs, B means available baseline locator; E counts every retained same-rule exact-locator Finding and ScannerReviewObservation; U counts retained same-rule unavailable locators; P is validated same-rule coverage passes or zero. Never deduplicate.

| Match | Required retained-evidence admissibility |
| --- | --- |
| baseline-locator-unavailable | !B; other counts unrestricted. |
| ambiguous | B and E+P >= 2; U unrestricted. |
| later-locator-unavailable | B and E <= 1 and (U > 0 or P > 0). |
| no-exact-match | B and E = 0 and U = 0; P unrestricted. |
| unique-violation / unique-incomplete | B and E = 1 and U = 0; exact binding to the corresponding sole native source; P unrestricted. |
| unique-pass | B and E = 0 and U = 0 and P >= 1; exact minimized pass as above. |

This is consistency with a possible discarded-pass arrangement, not proof of what discarded candidates contained. Actual full-candidate validation and existing comparison calculation before candidate disposal are mandatory publication witnesses. No candidate reconstruction or archive is permitted.

`RunRepository.updateComparison(expected: CompletedRun, input: unknown): StoreResult<CompletedRun>` appends exactly once: validated expected deeply equals current; expected/current have no comparison; next has one valid comparison; every other aggregate field is unchanged. Reject duplicate append even if identical, overwrite, removal, stale expectation and finish-path bypass. Reuse the existing staged writer and return its serialized validated durable value. A bounded rename of its shared update helper is permitted. `finish` explicitly rejects comparison; selected-Finding transitions add comparison to immutable top-level keys so guidance/generation/review preserve it.

#### Publication, response and lineage

Hold the existing rescan reservation through completed-scan publication, lifetime checks, baseline/native-context recheck, immediate lineage recheck, append and settlement. Fresh baseline native selected Finding, requested URL and scan context must equal captured facts; unrelated downstream human changes must not gate comparison. A baseline with saved comparison needs an available immediate baseline link before new run creation and again before append. Do not traverse ancestry.

Existing scan failures keep historical behavior. A completed scan is never finished as failed because comparison fails. After completion and before baseline reads/append, stop or deadline precedes signal abort; then calculation failure; then lineage failure. No append starts after deadline, including a late executor return. Append success is terminal commit truth even if stop/abort becomes observable during or after successful rename. No postcommit read downgrades success. Failed append reports shutdown if stopping/deadline, otherwise persistence even with simultaneous abort; preserve its cleanup result. Service releases only when cleanup is certain and deadline has not expired; uncertainty closes admission and retains ownership. Stop keeps admission closed. Lost response means unknown outcome, never automatic retry.

Success is exactly `{ok:true,run}` where run is the repository-returned completed aggregate with comparison. Precreation lineage refusal is exactly `{ok:false,error:'comparison-lineage',run:null,persisted:false,cleanupFailed:false}`. Postscan failure is exactly `{ok:false,error,run,persisted:true,comparisonPersisted:false,cleanupFailed}` with a validated completed run without comparison. `comparisonPersisted` is forbidden on success, precreation and historical failures; old scan error keysets/statuses remain unchanged.

| Postscan error | HTTP | cleanupFailed | Client reservation |
| --- | --- | --- | --- |
| comparison-calculation | 500 | false | Release |
| comparison-lineage | 409 | false | Release |
| comparison-persistence | 500 | Actual update result | Release only if false |
| comparison-aborted | 409 | false | Release |
| comparison-shutdown | 503 | false or failed-update result | Retain |

B must prove the action gate with two isolated equivalent synthetic repository/service states: a valid comparison-bearing completed run with a real selectable retained violation and its immediate baseline. The same otherwise valid intent must reach new-run creation and the controlled scanner when that immediate baseline is available. In the equivalent state after deleting only its exact sandbox-owned immediate baseline (ordinary/non-reparse containment and single run.json inventory checked), require the exact precreation comparison-lineage refusal, unchanged run inventory and zero scanner calls. Capture the exact sandbox/IDs and baseline bytes before this test-owned deletion. Reuse withReviewSandbox; never target original data. The positive control prevents missing-Finding or general admission refusal from masquerading as lineage evidence. This is a synthetic service witness, not a native scan qualification. Integrated D acceptance combines this B witness with the native zero-Finding persistence/read/display/deletion witness; neither substitutes for the other.

Precreation lineage refusal is HTTP409/release; success HTTP200/release. Client settlement for a completed run retains `status:'completed'` and run, adding optional `comparisonFailure:{error,cleanupFailed,released}` for the postscan variants. C consumes the failure visibly; B must already admit the completed evidence truthfully. Never replace that run with the baseline or treat valid zero as scan failure.

Successful existing GET `{ok:true,run,interrupted}` adds `comparisonLineage` only when comparison exists: `{status:'available'}` or `{status:'unavailable',reason}` with reason exactly `not-found`, `invalid-run`, `read-failed`, `stored-run-unavailable` or `baseline-mismatch`. Validate later aggregate independently. Available requires the immediate completed baseline's exact referenced Finding native projection, requested URL and context. Missing/malformed/unreadable/mismatched baseline cannot erase saved comparison. Historical reads omit metadata. Existing busy/stopping responses remain failures.

Initial active comparison inspection and explicit Return to baseline/Return to later refresh availability; no polling. Missing callback or failed refresh means visibly unverified, with preview/human context withheld and new comparison disabled until current successful read. Unavailable clears immediate baseline preview/human context; saved minimized evidence stays visible. Read admission binds captured run identity, native scan and saved comparison; it applies metadata only, never replaces newer downstream aggregate fields. Stale/foreign/malformed replies do nothing. A stop after commit can yield unverified availability, never an unsaved comparison. No read restores interrupted downstream capability.

#### UI contract and accepted reuse audit

`M503-UI-01` is accepted at the decision level: EXTEND App only for shared request/selection/preview/status/focus coordination; EXTEND ResultsSection to compose comparison outside selected ResultDetail; CREATE ComparisonDetail for the region and comparisonPresentation for pure display projection; REUSE_AS_IS FindingsPanel and existing evidence markup; EXTEND RuleEvidence/resultPresentation's evidence input to structural `RuleDetails & {locator: Locator}` to support pass without fake Finding. EXTEND IntentionalRescanForm for availability/status and styles only for wrapping/layout/focus using existing tokens. Main wires the read callback. ResultDetail requires no new ownership. No hook merely to shorten App, theme or generic abstraction. C is S3 because read identity and navigation ownership change; one fresh critical reviewer also inspects its visual evidence.

Use heading `Comparison`, labels `Before` and `After`, canonical outcome/rationale/limitations/follow-up, baseline/later run references and available provenance. An absent after target has an explicit reason. Label pass `Native pass observation`; never display it as a Finding. Label context `AI original proposal`, `Human-edited proposal` and `Human decision` separately when safe current preview context exists. Context is not required, persisted or used to classify. Contrast margin is target evidence, not a page score.

Bounded feedback: saved `Comparison saved.`; calculation `Scan completed. Comparison could not be calculated.`; persistence `Scan completed. Comparison could not be saved.`; lineage `Scan completed. The baseline is unavailable for comparison.`; aborted `Scan completed. Comparison was cancelled before saving.`; shutdown `Scan completed. The service stopped before comparison was saved.` Unknown transport preserves the existing unknown-outcome warning and lock. Available evidence with failed refresh says `Baseline availability could not be verified.` Unavailable says `The saved comparison remains available, but its baseline cannot be inspected.` Disable new comparison with visible reason; no automatic retry. Existing shared live-status region announces settlements/navigation, without new competing live regions or unsolicited focus movement. Return navigation uses existing predictable Results focus.

The full semantic state matrix in C above is binding. Automated browser tests cover heading/labels, keyboard activation, shared live status, no settlement focus theft, return focus, inert evidence strings and non-color outcomes. Manual screenshots cover resolved zero-Finding, unavailable-after and broken-lineage, each at 1366x900 and 390x844, including visible focus. No zoom200 or spoken-output claim.

#### Exact responsibility/path boundaries

All paths below are repository-relative, frozen for packet projection. A packet may narrow them; additions require primary reconciliation before a lease. Test and implementation owners remain separate. No docs, evaluation, package/config, Git metadata or other source paths are worker-writable.

| Slice | Production paths |
| --- | --- |
| A | `src/server/domain/run-contract/comparison-types.ts`, `comparison-validation.ts`, `run-types.ts`, `run-validation.ts`; `src/server/domain/run-contract.ts`; `src/server/persistence/run-repository.ts`; `src/server/persistence/run-repository/contracts.ts`, `comparison-transition.ts`, `run-transition.ts`, `selected-finding-transition.ts`; `src/server/comparison/scan-pair.ts`. |
| B | `src/server/service.ts`; `src/server/local-service/contracts.ts`, `rescan-operation.ts`, `rescan-api.ts`, `rescan-comparison.ts`, `comparison-publication.ts`, `comparison-lineage.ts`; `src/client/rescan-admission.ts`, `rescan-request.ts`, `rescan-transport.ts`, `comparison-admission.ts`, `comparison-request.ts`. No scan-operation or generic Analyze admission change is currently justified. |
| C | `src/client/App.tsx`, `main.tsx`, `styles.css`; `src/client/components/results/ResultsSection.tsx`, `ComparisonDetail.tsx`, `comparisonPresentation.ts`, `RuleEvidence.tsx`, `resultPresentation.ts`, `IntentionalRescanForm.tsx`. Existing paths have been confirmed; new paths are bounded creations. |
| D | No production paths. Defects return to their owning slice and remaining budget. |

| Slice | Test paths |
| --- | --- |
| A | `tests/run-contract.test.ts`, `run-repository.test.ts`, `review-repository.test.ts`, `comparison-persistence.test.ts`, `tests/helpers/m503-comparison-fixture.ts`. The new fixture has actual A/B/C consumers, no generic test framework. |
| B | `tests/rescan-service.test.ts`, `rescan-api.test.ts`, `rescan-comparison.test.ts`, `rescan-admission.test.ts`, `local-service.test.ts`, `comparison-service.test.ts`, `comparison-admission.test.ts`; the shared M503 fixture if a demonstrated new vector requires it. |
| C | `tests/intentional-rescan-ui.test.ts`, `comparison-ui.test.ts`; `tests/helpers/m104-ui-harness.ts` only for the existing synthetic App entry and six-file capture plumbing. Shared M503 fixture may add concrete display vectors. |
| D | `tests/rescan-integration.test.ts`; `tests/helpers/m105-walking-skeleton-harness.ts` only for frozen fixture interception and the owned service/browser proof. |

Dependency direction is domain validation to pure policy, repository to domain, service to policy/repository, client admission to pure validated contracts, UI to client state and pure presentation. ComparisonDetail owns rendering; App composes shared coordination. Preserve these cohesion boundaries through actual diff inspection.

#### Executable command and effects contract

Every invocation uses the complete README preparation in a fresh PowerShell7 session, cwd above, pinned existing Node24.20.0 and installed lock identity from ENTRY02. Tests run sequentially, never with an unrelated service/browser. Exact focused invocations follow; a Red failure must be a confirmed behavioral assertion or the narrow first-module callable exception, not preparation/type/setup noise. Green/characterization require exit0 and zero failed/skipped tests. Strict is separately required after Green. Each command stops before its next effect on nonzero exit.

```powershell
# A, after maintained preparation:
Invoke-M105Command {
  & $m105Node --experimental-test-module-mocks --test --test-concurrency=1 --test-timeout=120000 tests/run-contract.test.ts tests/run-repository.test.ts tests/review-repository.test.ts tests/comparison-persistence.test.ts tests/comparison-pair.test.ts tests/comparison-outcome.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'M503 A focused verification failed' }
}
# B:
Invoke-M105Command {
  & $m105Node --experimental-test-module-mocks --test --test-concurrency=1 --test-timeout=120000 tests/rescan-service.test.ts tests/rescan-api.test.ts tests/rescan-comparison.test.ts tests/rescan-admission.test.ts tests/local-service.test.ts tests/comparison-service.test.ts tests/comparison-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'M503 B focused verification failed' }
}
# Independent strict and build, separate evidence identities:
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'M503 strict TypeScript failed' }
}
Invoke-M105Command {
  & $m105Node node_modules/vite/bin/vite.js build --configLoader native
  if ($LASTEXITCODE -ne 0) { throw 'M503 client build failed' }
}
# C:
foreach ($m503Test in @('tests/intentional-rescan-ui.test.ts','tests/comparison-ui.test.ts')) {
  Assert-M105EmptyDirectory $m105UiTemp
  Invoke-M105Command {
    & $m105Node --test --test-timeout=120000 $m503Test
    if ($LASTEXITCODE -ne 0) { throw 'M503 C UI verification failed' }
  } $m105UiTemp
  Assert-M105EmptyDirectory $m105UiTemp
}
# D:
Assert-M105EmptyDirectory $m105ScanTemp
Assert-M105EmptyDirectory $m105UiTemp
Assert-M105EmptyDirectory $m105IntegrationTemp
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/rescan-integration.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'M503 integrated verification failed' }
} $m105ScanTemp
foreach ($m503Scratch in @($m105ScanTemp,$m105UiTemp,$m105IntegrationTemp)) {
  Assert-M105EmptyDirectory $m503Scratch
}
```

Build may replace inspected generated ordinary files only under existing ignored `dist/client`; inventory before rebuilding and preserve unexpected files. No dependency acquisition, lock changes or generated dependencies. Existing test sandboxes remain their exclusive random task children under `temp/m102-*` and `temp/m401-review-{repository|service}-*`; reuse `withReviewSandbox` for new persistence/service cases. Preserve its ordinary-path checks and stop/port-close before cleanup. Scanner scratch is `temp/m103-scan`; UI scratch `temp/m104-ui`; integration parent `temp/m105-integration`. Browser is existing managed Chromium1234 /151.0.7922.34. UI Vite and actual service bind loopback port0 and retain owned handles; close them in finally before exact child cleanup. Browser requests are limited to the harness's owned loopback origin; scanner requests to `https://m105.test/` are fulfilled from frozen project bytes. No provider/retrieval/public call.

D uses only companion `informative-image-alt` failing to corrected, target `rd3-image`, requested URL `https://m105.test/`, exact locator `:root > :nth-child(2) > :nth-child(1) > :nth-child(3)`. Failing fixture SHA256 `bc54dd14df90931d8a5c75544e97a61f15d070550aa8602e850d1255749c308e`; corrected `e6b9aea2773848540ac247063860690298dc05c886ae29f8627d4e6be93246e1`. Verify these against the companion before execution. Use the actual scanner through existing fixture interception, service, disk readback and browser, not a mocked HTTP success. Policy-only contrast/adverse fixtures remain separately labeled.

The synthetic service store is an exclusive absent ignored child `temp/m105-integration/m503-comparison`; no preexisting contents are reused. Capture production-generated baseline/later UUIDs from validated responses and canonical JSON. Immediately before deletion, record both exact absolute run-directory paths and SHA256 values; require distinct IDs, ordinary/non-reparse ancestors, resolved paths contained in that task child, expected single ordinary single-link run.json inventory, and no aliases. Freeze these concrete IDs/paths in the evidence output before removing only the verified baseline directory. No original `data/runs` target is permitted. Prove baseline bytes unchanged before deletion; later comparison bytes unchanged afterward; GET/UI broken-lineage. This native corrected endpoint has zero Findings and therefore does not independently prove lineage-based action refusal. Teardown stops owned service/browser then removes only remaining verified task children; shared parents remain. An ID/path mismatch stops before deletion.

C's optional content-safe screenshot capture uses only `A11Y_M503_CAPTURE_PROOF=1`, restored exactly in finally; reject an already-set value before capture. Directory `temp/m503-ui-proof` must be absent, ignored and ordinary before creation, or contain only this task's explicitly inventoried prior proof on a rerun. Retain six PNGs `resolved-desktop.png`, `resolved-narrow.png`, `unavailable-desktop.png`, `unavailable-narrow.png`, `broken-lineage-desktop.png`, `broken-lineage-narrow.png` for primary inspection. Capture invocation is the C comparison-ui command above with this one temporary flag; all prior capture flags must be absent. Tests may write those six exact files only when capture is enabled. No screenshots of user/private/browser content. Retain screenshots through closure; no recursive proof-root cleanup is authorized here.

Full regression is the exact README forty-four-file sequential inventory: the original forty files plus `tests/comparison-persistence.test.ts`, `tests/comparison-service.test.ts`, `tests/comparison-admission.test.ts` once in the module-mock/browser-free group and `tests/comparison-ui.test.ts` once in the UI scratch group. The maintained command now includes those additions and rejects all four capture flags. Keep each README timeout/flags/scratch check and failure propagation; no filtered tests, skip/focus markers or removed legacy tests. Repeat independent strict and build at closure only if identity is stale; record reusable exact evidence when unchanged. Recheck protected manifest and referenced fixture hashes, all test/helper diffs, fresh final critical review, links/status/commands and `git diff --check` before completion.

Immediately before every lease the primary freezes exact narrowed write paths, command/environment identity, HEAD and dirty inventory, accepted test hashes and attempt/predecessor in Packet v2. Runtime-dependent IDs are captured at the explicitly specified boundary above, not guessed now. Read-only preflight can identify a mistaken existing filename; it cannot invent a new contract or broaden effects. I1–I10 are all decided by this artifact; their implementation proof ownership remains the earlier invariant table. No runtime PASS is implied by G acceptance.

## Validation and Acceptance

M5-03 passes only when A–C's accepted preflight/Red-or-characterization, separate Green, unchanged test boundary, terminal leases and risk reviews are recorded, plus D's current integration and closure proof. The primary independently accepts actual diffs and evidence, not only worker reports. No implementation check is complete at planning.

Acceptance covers I1–I10 and every roadmap Verification clause. Inspect actual persisted JSON, runtime-validated readback and rendered result; a type assertion, screenshot alone, successful rename alone or internal comparison return alone is insufficient. Non-matching and not-comparable variants cannot fabricate after evidence. Completed scan counts and valid-zero presentation survive comparison failure. Existing original proposal, edited human work and decisions remain context, and limitations/follow-up remain visible even without them. Saved comparison is preserved by later workflow updates and baseline loss does not erase it.

The smallest real proof is the directed controlled scan→service→single-file comparison→validated API→browser lane in D, with separate semantic adverse tests. Automated UI checks cover accessibility, keyboard, focus and live-status semantics; manual inspection covers readable desktop/narrow layout and visible focus. Keep exact source, command, runtime/browser/build, run identities, state and viewport with the evidence. This is neither spoken-output proof nor public-site qualification.

At closure run the complete authoritative suite, independent strict typecheck, production build, test relevance/cohesion audit and [documentation gate](../../README.md#task-closure-documentation-gate), including `git diff --check`. Do not mark Complete or archive until the different final critical review and all task gates pass. The final report states remaining limitations and explicit documentation impact.

## Idempotence and Recovery

Read-only identity/format checks are repeatable. Tests may be repeated only against their prepared isolated state; real browser/filesystem evidence is otherwise Non-reusable. Never retry an unknown save or intentional rescan automatically. Inspect the last validated aggregate through the service boundary, reconcile the operation owner and record what is known without restoring a consumed capability. A failure after completed scan publication cannot rewrite that parent as failed or remove its evidence.

After an unexpected diff, guard failure or worker stop, close the affected lease, preserve all user/peer work and retained evidence, inspect actual changed paths and remaining allowance, and issue a fresh packet only when reconciled. Primary documentation changes wait until no lease is active. A revised test invalidates its accepted Red/characterization; reaccept before Green. A material G change invalidates its decision review and affected implementation evidence. No hard reset, broad deletion, status laundering or new ID to reset an attempt is allowed.

Clean up only named task-owned processes, browser contexts and isolated outputs after retaining decisive content-safe evidence. Resolve every absolute deletion target inside the named workspace child and reject aliases/reparse points. Never remove original data, installed browser/model resources, frozen packages or shared scratch parents. Baseline-deletion proof is an explicit synthetic effect, not permission to delete user evidence. If a new architectural/authority choice or exhausted budget blocks dependent work, finish unaffected authorized work and request the exact missing direction.

## Artifacts and Notes

### M503-FINAL-01 — Integrated review and task closure

On 2026-09-20 UTC, different fresh `critical_reviewer` `/root/m503_final_review` returned **PASS**, with no Blocker, Major or Minor. It reviewed all I1–I10 across the actual integrated diff, controlling requirements/decisions, test relevance, module cohesion, native proof and documentation. Independent prepared strict TypeScript and nineteen in-memory probes pass across all eight comparison branches, publication failure/lifetime precedence, successful commit during shutdown, lineage loss, downstream-state-tolerant readback and forged identity rejection. No filesystem-writing or network probe was added.

The reviewer independently matched the final source/test fingerprint, build and six visual hashes; inspected resolved narrow and broken-lineage desktop presentation; and checked all seventeen compliant M503 receipts with no forbidden/unleased changes, no active lease and no scratch residue. Fresh complete-suite and native proof were reused under their evidence identities. The unknown cause of the earlier service-publication assertion and truncated guidance-summary limitation remain explicitly recorded in M503-REGRESSION-01; neither was relabeled as a fixed defect or hidden by a test change.

Primary accepts the review, complete 44-file regression, independent strict/build, exact native persistence/deletion/readback/browser proof, separate valid-Finding action refusal, frozen-input/original-record preservation and documentation gate. M5-03 is Complete. Its plan is archived with stable evidence headings and repaired navigation; roadmap and progress mirrors agree. Final archive link/status/format checks and `git diff --check` pass in the same reconciliation. M5-04 remains Not started and unselected. No commit, push, publication, public scan, provider call, acquisition or original retained-run mutation occurred.

Documentation impact: Updated current capability/API and the exact 44-file commands in README; comparison lifecycle and UI contract/index; documentation task routing; roadmap status; plan archive/index; and concise progress record/index. Requirements and ADR semantics, calculation rules, frozen evaluation definitions and later-task scope remain unchanged. Controlled evidence does not establish cross-platform or public-page qualification. All 200% zoom testing remains Deferred; no spoken screen-reader observation is claimed.

### M503-REGRESSION-01 — Complete authoritative suite and closure candidate

On 2026-09-20 UTC, primary independently ran maintained README preparation, strict `tsc --project tsconfig.json`, inspected ordinary generated build inventory, and ran `vite build --configLoader native`: exit 0, 75 modules. Build bytes exactly match accepted C-GREEN03. The complete maintained 44-file suite then passed sequentially with all four capture flags absent, no skipped/cancelled tests, and empty scanner/UI/integration scratch afterward. It covers 866 tests; the output transport truncated one ten-test guidance-UI summary, but the sequential fail-fast command completed exit 0 and proceeded through all subsequent suites. No test or expectation changed for the renewed execution.

The first full-suite invocation stopped in the existing invalid-collaborator changed-mode service case: expected failed-record publication was true, observed false. Its result-validation category and failed record were correct. The unchanged eight-case focused reproduction passed, and the renewed complete service suite passed 70/70 before all remaining files passed. Read-only triage found no changed-mode-specific publication branch or M5-03 failed-transition change; transient native filesystem publication is a hypothesis, not a confirmed cause. The initial failure remains recorded for final review. No retry, fault masking or production/test correction was introduced.

The complete-suite D run independently reproduced native persistence and deletion with baseline `run-a6f10066-31e5-457a-98b0-e6180bd8f645` / SHA-256 `8334027e4fd1be0b401a2a3e8c7227add1d4d4e21b6988856af3269107ee0694` and later `run-35918677-5058-4b07-97ad-7e96b1f30fd4` / SHA-256 `bad4ecc774c0e377f9cf8657715141be79f82571fcc18254eca0e02302962b9c`. Exact paths were emitted under the same owned comparison root before deletion. All three integration cases passed; the separate B valid-Finding refusal case also passed in the authoritative suite.

Evidence identity remains source/test fingerprint `626eb7ebd790d5ec5dc3e2be6ab10fd953695594f87f4378d0bd054a1a10d41e` at unchanged HEAD `08d75e9d573a5f71dc284e1b4c5946380b63eb3b`, repository cwd, prepared Node 24.20.0 / PowerShell 7.6.5 / existing Chromium 1234 and unchanged package lock. All 14 original retained path/hash pairs remain unchanged. Three frozen manifests, all 13 generation and eight comparison references pass fresh hash checks. C's six accepted visual samples retain the same production/build identity; D changed only its two test files. No visual recapture, public scan, provider call, acquisition or original-record mutation was needed.

Primary test-relevance and cohesion audit accepts strict aggregate/mutation tests, adverse publication/ownership and admission cases, browser keyboard/announcement/focus and source-layer cases, and actual native/disk/browser characterization as complementary evidence. The zero-Finding native case does not substitute for B's action-gate witness. Production responsibilities remain within the accepted named domain, repository, publication, lineage, admission and comparison-presentation owners. Candidate documentation validation passes 697 local links/anchors, changed/new-file UTF-8/final-newline/trailing-whitespace checks, README PowerShell syntax, exact 44-file suite inventory and `git diff --check`. A first link checker included illustrative template placeholders and a suite checker omitted the unquoted API-test invocation; corrected read-only checks passed without changing repository content for either checker limitation.

Documentation impact: README commands/current capability, documentation navigation, lifecycle implementation status, UI contract and verification, roadmap, living plan and concise progress records are reconciled for implemented M5-03 behavior. Requirements, ADR decisions, frozen evaluation inputs and later-task scope are unchanged. Final reviewer verdict, Complete status and archive navigation are deliberately pending.

### M503-D-EVIDENCE-01 — Native persistence and deletion characterization

The primary accepted the actual two-file diff and compliant terminal lease `M503-D-evidence-01`, digest `75cee307fdf54718735a3e724aa3d3c8c0184a2375d8c1f04a43eae361670de2`, receipt `eefbfa31f3ab9969c63a6c9ceb6ebc4997aedfbaf665d366f9384ddc7f84f564`. Only `tests/rescan-integration.test.ts` and its existing walking-skeleton harness changed. The test owns scenario assertions; the harness owns exact contained ordinary-file inventory, hash checks and cleanup. Cohesion is RETAINED. D evidence attempt 1 is consumed; no production correction occurred.

Prepared `node --test --test-timeout=120000 tests/rescan-integration.test.ts` passes all three cases. The actual frozen failing/corrected image-alt scans produce a baseline Finding and zero later Findings; service success equals validated canonical disk state with comparable/unique-pass/resolved comparison. Baseline bytes remain unchanged until exact authorized deletion; later bytes remain unchanged afterward. GET changes available to unavailable/not-found. Browser baseline preview is established before deletion; Return to later refreshes broken lineage while retaining saved comparison. B's valid-Finding positive/refusal case remains the separate action-gate witness. Original M5-02 executor private-handoff assertions remain strict.

The final focused execution froze generated IDs and bytes before deletion: baseline `run-81e9fc8f-89e7-4ed1-b351-6f5ecb822e27`, SHA-256 `53431abf889d8655c0b7688c93654e590df1e7f1cd131bd3b0f184f2f432bfb6`; later `run-de6a93b8-b9ac-4997-b180-7be666632979`, SHA-256 `8931490eb4a65ea9e73b6902e1faed5ae623dcde757d4e31d6eaf41a7cd9871e`. Their exact absolute directories were the repository's `temp/m105-integration/m503-comparison/<runId>` children, emitted by the test before removal. Browser/service stop and closed port precede exact remaining-child cleanup; unknown IDs, topology or shutdown failure preserve residue. All three scratch roots ended empty; no external request occurred. This observation is pinned to that owned execution, not reusable external-state evidence.

Earlier same-lease characterization runs exposed legacy UI assertions, an ambiguous status locator and preview/deletion ordering. These test-only issues were corrected without weakening the contract. Primary diff review additionally required registered-ID cleanup and preservation after shutdown uncertainty. Final independent strict TypeScript and `git diff --check` pass. Source/test fingerprint is `626eb7ebd790d5ec5dc3e2be6ab10fd953695594f87f4378d0bd054a1a10d41e`; leased test/harness hashes are `9b861ce4556dde01864c20f4b4103d79988d863a3852d1d237c539fdc43c4cdb` and `22a0cb1ca3fdf9155f1945f663e2e29133783c644adf9d5632264b2f6a10f41b`.

### M503-D-PREFLIGHT-01 — Native integrated characterization required

Read-only preflight confirms EXISTING_BUT_UNCOVERED: A–C implement publication, immediate-lineage readback and display, while existing integration tests cover only the older browser rescan and internal M5-02 executor. The primary accepts the exact two-file evidence boundary: rescan-integration and its existing walking-skeleton harness. Supersede only the old service-level blanket comparison exclusion with validation of the approved saved aggregate; retain the direct executor's scan-only private-handoff assertions. Both files are unchanged before D, frozen informative-image-alt bytes match, all three scratch roots are empty and the exact m503-comparison child is absent. No scanner, service or browser ran during preflight. The current built-client identity is C-GREEN03. Evidence1 may add the one native failing-to-corrected/deletion/readback/browser characterization and the necessary contained ordinary-file cleanup checks; production code is outside scope. This is passing characterization, not fabricated Red.

### M503-C-REVIEW-02 — Presentation slice accepted

The same critical reviewer returned PASS with no remaining findings after the complete C re-review. Ten independent in-memory probes pass, including synchronous/reentrant feedback, removed-focus restoration, outside and connected-Finding preservation, and stale metadata; independent strict/diff checks and all protected source/test/image identities match C-GREEN03. Primary accepts C after actual diff/cohesion inspection, six compliant leases, 29 passing UI tests, independent strict/build and renewed visual evidence. All C test/Green attempts and its one review correction loop remain consumed. D may begin; its native proof and final task verification are still required.

### M503-C-GREEN-03 — Availability feedback and focus corrected

Final Green lease closed compliant, receipt `926f73c2073d3e71be02367e83cf6e2e261b19728b120c9bc1c11c8e8891a7d5`. Only App changed: publication receives its completion announcement before invoking the reader, and availability settlement captures the focused Results element so the layout effect restores Results focus only if that element disconnects. Availability changes trigger that existing effect even when synchronous navigation ends with unchanged preview state. Primary inspected these changes and reproduced all four unchanged C-RED03 test hashes and fingerprint `b3c8e0048df0c004b09f66da6566dcde2498428cf2da93a1c30c494560b56925`. All 29 focused UI tests and worker strict pass; primary independent strict and renewed 75-module build pass. RETAINED cohesion remains within App coordination; no layout or dependency change occurred.

After ordinary generated-file/proof inventory, primary renewed the full comparison capture invocation: 14/14 pass, no skipped tests, scratch empty, capture flag restored. The six new screenshots are byte-identical to C-GREEN02's inspected files, so that exact visual inspection remains applicable while browser evidence now binds to the corrected source. Current build hashes: index `8e181769e2ad7d785cab004b41b13d9e8d8fe48ff60469b8ecad0cd465a3e609`, JS `84fb8b223de7a81b40c93a79d9bfc433ca742f621c3ce71a07e22132d1cd143e`, CSS `4962722f0d6585b7c3e2c0b232e6617161829991988b859d12f8f8c10c48cbfa`. Same-reviewer critical re-review is pending; D remains unstarted.

### M503-C-RED-03 — Review regressions accepted

Conditional final test lease closed compliant, receipt `95fc3f9e6cd65ead3ccff6f4302a54f15b690f9b2c71de8094174600ee21b7e9`; only comparison-ui changed. Primary inspected all four new cases and their two local helpers, including both reader-failure modes, reentrant Analyze, both delayed availability failures, synchronous keyboard navigation and an actual-preview outside-focus positive control. Every vector executes before the decisive assertions. Prepared intentional UI passes 15/15; comparison UI has ten passes and four intended R1/R2 failures, no skips or setup errors. Strict passes. Primary reproduced fingerprint `910bccf6149c4d3bd43a7f135e5c60b67e1d9ddd60c0af48992e81b40c68f43c`; revised comparison test hash `5baf5c02062b747aa5af7e8c3336925b1468d166def223be10455cfd67848a75`, other three C-RED02 hashes unchanged. Scratch is empty; existing six proof images are unchanged. The prior test identity is superseded; Green3 must preserve this reaccepted 29-case boundary.

### M503-C-REVIEW-01 — Availability settlement correction required

Fresh critical review returned REVISE with two Major findings and no Blocker/Minor. R1: rescan settlement invokes publish and its synchronous/reentrant availability reader before setting success feedback; this overwrites an unverified notice or a newer Analyze announcement. R2: unavailable/unverified settlement removes focused baseline-preview content without restoring Results focus; synchronous failure during navigation may leave preview unchanged and bypass the existing layout-effect dependencies. Both violate existing I7/I8 and REQ-A11Y-004; R2 also violates REQ-A11Y-001/010. The reviewer inspected all nine production/four test paths, six screenshots and four compliant receipts, independently passed strict and seven admission tests, and reproduced both defects with nine no-write probes of actual bundled App/request code. Candidate fingerprint remains unchanged.

Conditional attempt-3 justification: Red2 resolved all three obsolete assertions and proved 25 UI cases; Green2 resolved all five strict diagnostics and passed those cases, independent strict/build and six visual samples. The fresh review supplies materially new synchronous/reentrant and focused-preview-removal evidence absent from that accepted boundary. The different bounded action is to add behavioral regressions for missing/throwing callbacks, reentrant Analyze, delayed unavailable/unverified preview loss, synchronous navigation failure and outside-focus preservation, then finalize feedback before callback reflection and restore focus only when its element is removed. Existing App coordination owns both fixes; the same four test and nine production paths, authorities, commands, roles and observable contract remain unchanged. This is likely to resolve the concrete reproduced paths without schema or layout changes. The owner's existing conditional third-attempt and one-review-loop allowances authorize Red3 and Green3; no further write turn in either chain is available afterward without owner direction. Current browser/build evidence becomes stale when executable content changes and must be renewed proportionally.

### M503-C-GREEN-02 — Candidate UI and visual evidence

Green2 closed compliant, receipt `a003ed3becb9ab9807d10614f27ca8f7ea45b15014d1672cdd4c19094cea93f0`; only ComparisonDetail and resultPresentation type imports/narrowing changed. Protected C-RED02 tests remain unchanged; primary reproduced fingerprint `afea51614c124d34fa8806030a4a22e5f35647089a88045b0a4d56819601ea93`. Prepared C tests passed 25/25; worker and independent primary strict checks passed. Primary inventoried ordinary generated dist/client files before the successful 75-module Vite build: index `b474d7bb508c904e961e6ace110720422466ff09672385bb2f638b02dd5a98c8`, JS `da0a17448f992653970a56a7f5fe4c87f30bbad80062c48375fd8d66bc62d6d3`, CSS `4962722f0d6585b7c3e2c0b232e6617161829991988b859d12f8f8c10c48cbfa`.

The separate prepared comparison UI capture passed 10/10 with only A11Y_M503_CAPTURE_PROOF temporarily set and restored. The absent ignored ordinary proof root was verified before creation. Primary inspected all six retained synthetic screenshots in temp/m503-ui-proof at 1366x900 and 390x844: resolved zero-Finding, unavailable-after and broken-lineage states. Before/After hierarchy, non-color outcomes, wrapping, visible focus and limitations are readable; automated axe/overflow/keyboard checks passed, scratch emptied, and no external requests or page errors occurred. Screenshot hashes: resolved desktop `a5743f296382fdeb219e945c6fa2a8e9aec370e53811a4c25e6f7b36b45032d0`, narrow `c83866080d018baead93cd9d72d9965b9d40ff2e55925978c802aab34dec4fb9`; unavailable desktop `ea153e0c37da60d22418a49c47e7adb33af7af0e93d1f9be264ebe4b982440ac`, narrow `1930ed1827d03f321d30451affe3761d5af925365cf04e4c60bf55309c75292f`; broken-lineage desktop `0523cb1e87b54df4ed9eb0f4dee34e4bb77b59f2e16c0c4b2b57f389a9885865`, narrow `ffd17da1d6bdfe41863db0cecc77ec47aed43008e3f0ee3f4acad9f12f49f2f3`. No 200% zoom or spoken-output claim.

C remains a candidate. Worker RETAINED cohesion disposition matches primary diff inspection. Review must independently triage the reported ordering concern: a synchronous unavailable-callback settlement can announce unverified inside publish, then rescan success can overwrite that shared announcement despite a correct visible notice. Initial synthetic Analyze totals alone do not establish a defect because production Analyze creates a fresh scan without comparison. No test budget is renewed by this report.

### M503-C-RED-02 — Corrected UI boundary reaccepted

Red2 closed compliant, receipt `11150e674e83c0757caf1716a285755fb8931d016238e507eaf28aa57dc813af`. The primary inspected the three corrected selectors and independently reproduced all protected hashes and fingerprint `d4ae9cba2e04180a8a5fdddd219a63e428a22f1a5fabfa636e49af578b46c7e6`. Exact prepared C commands passed 15 intentional-rescan and 10 comparison UI tests without skips; strict retains only the five named production diagnostics. Helpers remained unchanged. The reaccepted hashes are intentional UI `9725f0fc1dd2efa1e5f236b9b4e2b0926dd9b9f633b0ed2280d58f7eca293f52`, comparison UI `c0c3fab7ae907c1c9bebc8d6143f5f5265f76d3c87ac7865fa676e870db932f1`, harness `2adef53f9d6f524941ada27faa7e76831e745685003b6aaba06d8c56da00ea67` and fixture `0f878f86e92ed092c9c83811f5d7b4ab141b7db0b717a364403b7daa9f500b02`. Scratch is empty, capture absent and diff check passes. Current evidence supersedes the old selectors without fabricating a new behavioral Red.

### M503-C-GREEN-01 — Partial UI and bounded test correction

Green1 stopped with TEST CONTRACT CONFLICT and closed compliant, receipt `e5ee514824f90d5969845c2023232ee455521e210b119190a23ff45ce4806e32`. Exactly nine allowed production paths changed; all four protected tests remained unchanged. Candidate fingerprint `5c634068da8a3ef663282f63f6b92a9de3b627ed4db9ccea73ffdf90d78ba318`. Intentional UI passed 13/15; the two failures count the analyzed URL across all Results, conflicting with required Before/After provenance. Primary independently ran the new comparison suite: 9/10 passed, with the remaining assertion expecting obsolete `automated finding` copy instead of the existing count summary. These three selectors are corrected under Red2, preserving required provenance and truthful summary. Independent strict diagnostics identify missing type exports used by ComparisonDetail/resultPresentation, optional baseline narrowing and the dependent return exhaustiveness diagnostic; these remain Green2 responsibility. No external requests or page errors occurred; scratch cleanup passed. No screenshots were captured. The primary inspected the complete production diff; optional proposal context retains fields directly because the existing ProposalContent requires a guidance view that retained context does not guarantee. Test correction invalidates the prior current test identity and requires reacceptance; historical missing-behavior Red remains recorded.

### M503-C-RED-01 — Comparison UI test boundary accepted

The four-file test lease closed compliant with digest `824ea6076e103516efc4ea9ac28d782902a54ed65159144023d40a6e71c1ff92` and receipt `7abc11018e94a283764cced9cf62153f1400751d3f83e3ad547cfff895772fda`. Primary inspected the actual new tests, complete harness changes, legacy success adjustments and additive fixture helpers. In-turn corrections preserved opaque responses, removed invalid fixtures and baseline-handler mutation, added explicit human-work preservation/context clearing, safe screenshot checks and keyboard activation. These were corrections within Red1, not additional write attempts.

Final focused Red: intentional-rescan UI 13/15 pass, two intended missing Comparison/context and main GET failures; comparison UI 0/10 pass, all ten fail at missing production presentation/readback/announcements. Zero skips/cancellations and no remaining fixture/cleanup failures. Strict TypeScript passes. Primary reproduced all four hashes and source/test fingerprint `11537fcb5acba1e699ec8823a8437c784da4f7a7292005dca0a60650681022b3`; all three scratch roots are empty and screenshots remain uncreated. Because the shared helper changed, primary reran the exact A and B focused commands: 216 and 114 pass, followed by independent strict PASS. No comparison policy or old helper behavior changed. Primary accepts this behavioral Red for separate frontend Green; no first-module exception applies.

Protected C test hashes: intentional-rescan UI `ede36d3a0c5a3b947b2f3f4510ab9881b01851ad947b7af6eb79e0206f820fe4`; comparison UI `f8c13a06e220165c2ed63295d831fd5de982fdefb15380723ed05d6928c1364f`; M104 harness `2adef53f9d6f524941ada27faa7e76831e745685003b6aaba06d8c56da00ea67`; M503 helper `0f878f86e92ed092c9c83811f5d7b4ab141b7db0b717a364403b7daa9f500b02`. The original 14 retained data files were also read-only hashed before C browser verification for later preservation comparison; no data content was copied to tracked files.

### M503-C-PREFLIGHT-01 — Visible comparison missing

Read-only test owner classified C MISSING against unchanged source/test fingerprint `454b7c6343565f64e2cdeee80962c8cffd3c741738d12b071df80e4d45c1f3fc`. App has no comparison-read callback/lifetime, ignores completed comparison failures, and captures unconditional preview; ResultsSection has no comparison region; main and the form do not expose readback/availability. Existing B admission/request modules are reusable. Primary accepts a coherent behavioral Red through the existing App harness, with no first-module exception: narrowly update old success fixtures and explicit available readback; add the complete C state, refresh identity, downstream preservation, navigation/focus/accessibility and six-image capture boundary. Only the four exact C test paths are authorized. No production, authority or command expansion is needed. Current README/lifecycle/router descriptions were reconciled with accepted B between leases; eight changed documents passed 622 local links/anchors before this checkpoint.

### M503-B-REVIEW-01 — Publication and admission slice accepted

Fresh critical reviewer `/root/m503_b_review` returned PASS with no Blocker, Major or Minor. Primary accepts B after inspecting the actual production/test diff, all five compliant terminal receipts, 114 focused passes, independent strict/build and unchanged protected identities. Reviewer independently passed strict TypeScript, 24 pure tests, 61 publication, 24 lineage, seven admission and six response-reentrancy checks; every I1–I10 disposition passed within B's scope. Source/test fingerprint remains `454b7c6343565f64e2cdeee80962c8cffd3c741738d12b071df80e4d45c1f3fc`. Synthetic selectable-Finding action refusal is proved separately from the still-pending D native zero-Finding proof. B workers retire at this barrier; their consumed budgets remain recorded. C/D, complete regression and documentation closure remain required. No requirement, ADR, schema-version or frozen-input change is indicated.

### M503-UI-02 — C reuse audit refreshed

Primary refreshed the accepted M503-UI-01 audit against App's actual rescan settlement, preview navigation, ResultsSection composition, IntentionalRescanForm, RuleEvidence, main and the existing browser harness. The G path/responsibility matrix remains appropriate. Freeze the App callback as optional `readComparisonRun?: ComparisonReadCallback`, wired to `getComparisonRun` in main. Missing callback must remain unverified; tests may supply explicit available readback for existing rescan success/navigation cases. Refresh ownership binds the active run/native-comparison identity and a request token, not the changing downstream aggregate object; readback cannot overwrite human work or restore capability. Retain existing conditional Results focus restoration when replacement removes the active element; availability settlement never moves focus, and explicit return navigation focuses Results. Existing navigation copy `Return to later results` remains compatible with G's Return-to-later action.

The frontend-visual capsule is accepted before C preflight: exact owners and narrow structural evidence input remain as G; the required semantic matrix, two viewports, keyboard/accessibility/focus/inert-string checks and six content-safe screenshots remain unchanged. Two UI guidance documents now describe this Accepted extension with implementation/verification explicitly pending. No C implementation, preflight or test evidence is implied; C remains gated on B acceptance. No public/provider call, dependency, zoom test, new workflow or theme is added.

### M503-B-GREEN-02 — Candidate publication and admission verified

Green2 closed compliant (digest `434168286ca51d04b1e408cde8a21072d1bfc650e9b83beaa204d9190efb6e49`; receipt `275a96d7cac7ac3a0c2b85e6c1574a89ce93f7dace8c1176ab112368b128a9cc`), changing only the three production narrowing sites. The focused seven-file command passes all 114 tests, zero skips; strict passes. Primary independently ran strict and the maintained Vite build, inspected the complete ten-path B diff and four new modules, reproduced all eight protected test hashes and source/test fingerprint `454b7c6343565f64e2cdeee80962c8cffd3c741738d12b071df80e4d45c1f3fc`, and passed `git diff --check`. Inspected ignored generated output was replaced only inside `dist/client`; current assets are `index-uH-93l04.css` and `index-CYizwo2W.js` plus `index.html`. Cohesion RETAINED: service coordinates, while publication, immediate lineage, closed framing, metadata admission and request ownership have focused owners. No calculation policy or accepted test changed. Primary accepts candidate behavioral and structural evidence pending fresh critical review; C/D remain unimplemented. Expected subsequent plan-only guard drift records this handoff after terminal closure and does not change executable evidence identity.

### M503-B-RED-03 — Test typing corrected and boundary reaccepted

Conditional final test lease closed compliant (digest `ea4cb2b1881f92d4cdf04cda11fdabb0316539baf85322e6e5bad2821d5f2212`; receipt `9ede0cfdb90eb0a2df1d251d3fdb08f3b7121ea96b89f6f5e545c439898e9d40`), exactly the three intended test files changed. Primary inspected the length assertion and `HistoricalRescanFailure` helper types; all runtime assertions remain. Focused 114/114 pass, zero skips; strict now reports only the three production sites. This is passing correction evidence against partial Green, not a new Red. Primary reaccepts the eight protected tests at fingerprint `4f5a72070b5eb11a937f8f9aab7d8a1d107669f41379379f0776a63640b0ba8d`. New hashes: rescan-api `d17c974eb2f651da3898ce97cf9eb52a8b060a0d3a6108b1fc4d5f48c6f365bd`, rescan-admission `3192507d8d488a6b126211da2bdcb2e769394faec8d794239b0e573e351ab972`, comparison-admission `f5212ed8ae51721cd77ca162a6dc7897677e091e5af71647e604436be2f4cd21`; other five unchanged. All B test attempts are consumed. Green2 may correct production typing and complete validation/cohesion; it may not modify tests or weaken the closed runtime contract.

### M503-B-GREEN-01 — Behavioral pass and conditional test typing correction

Green1 closed compliant (digest `a148f153dc16e22ad9f80640d7eb57db78731780f11bbe778b6edf429cc9581d`; receipt `1d51f2058bf3826491f425584a02fa7d50b1a658442c200595cb4146ddb724bd`), ten exact production paths changed and all eight test hashes preserved. All 114 focused tests pass, including the previously blocked client-read assertions. Strict fails at three production narrowing sites and three test sites; primary independently reproduced those diagnostics. Source/test fingerprint `1987404efcbc6ad8764ef5966a2cba09190795cbc800db843da608cdb8350ef8`. No Green/cohesion acceptance yet.

Conditional Red3 is justified by material new evidence: the complete behavioral boundary now passes, exposing only test typing defects that missing-module Red could not execute. `comparison-admission.test.ts:221` reuses an array narrowed by an empty-array assertion; the historical failure constructors in `rescan-admission.test.ts:98` and `rescan-api.test.ts:83` now inadvertently admit the new completed-comparison error family. The different bounded action is to preserve all runtime assertions while avoiding `never[]` narrowing and restricting those constructors to their actual historical failed/null error domain. These three local type corrections are likely to remove all test-side diagnostics without weakening the closed response union. The same eight-path Red scope and unchanged contract apply; this is the final test write allowance, not a fresh cycle. After focused validation and strict diagnosis, revised evidence must be reaccepted; remaining production errors belong to Green2. No primary or code-worker test edit is authorized.

### M503-B-RED-02 — Revised boundary accepted

Red2 closed compliant (digest `029ccaa2384ae487149f63b6a093fbb40e944ae3879338c3fc2efe26c2ed2d44`; receipt `fe48c1795c1b6383aef13e4de90ce18881ff52b0e167c774fd9bdc007c7fb9c0`), changing only `comparison-admission.test.ts`. The added representative synchronous cases cover callback lookup invalidating ownership and dispatch returning an already-created rejection after ownership loss. These establish the missing request boundary; response-snapshot reentry remains a required implementation/review inspection alongside existing stale-reply checks. The unchanged command again yields 95 pass, 12 intended behavioral failures and the exact initial missing-module failure, zero skips. Blocked client assertions remain unexecuted. Primary inspected the actual correction and complete B test surface, reproduced all eight hashes and accepts the revised boundary for Green. The comparison-admission hash is `374fcb0e0a08b4dbcdeea104b0c11b6fdaff9dd7344aa197bee88c2bf62ebe22`; shared helper is `6c54f89f0ab79eb887f317f7656ff6b94c57c0757f0904a266f14169ab16d0d9`. Source/test fingerprint `2af5138ca8db5f46186bb55bd4d5e9568ad5eac7478a333377efeec5c0fef25a` uses the maintained PowerShell `Sort-Object` order over `rg --files src tests`, then slash-normalized path/hash LF entries without final newline. Ordinal Python sorting gives a different digest and is not the recorded algorithm; matching individual hashes confirm no source drift. B test attempts1–2 consumed; conditional3 remains only with justification. Green1 receives all tests as forbidden paths.

### M503-B-RED-01 — Publication Red and bounded coverage correction

Test lease `M503-B-red-01` closed compliant, digest `bf3944ca6013f8e14cb70ce7ea4a19480158b47bb83e078d12ef5bfda5077999`, receipt `521cd051c8704038e2e4c72d622e475b774e37a32ec5422a8e4ad6d13f528c82`. Seven authorized paths changed; `rescan-service.test.ts` remains unchanged. Focused result: 108 tests, 95 pass, 12 intended behavioral failures and the exact missing `comparison-admission.ts` module; zero skips. Client read assertions remain NOT EXECUTED until Green. Primary inspected actual test changes, corrected two draft fixture assumptions within Red1, and independently reran all 216 A tests after the additive shared-fixture export; all pass. Source/test fingerprint is `026c4db741f682f40aaf9b1736dd410d11cd583ffb802c4b28a02c9ae8d10576`.

Final inspection finds late stale ownership covered but synchronous invalidation during callback access/response reflection missing from the new read-request tests. The last in-turn coverage note arrived after the worker handoff, so no posthandoff write is authorized. Ordinary Red2 may add only that bounded test within the unchanged eight-path lease scope and rerun the same command. This is the frozen captured-ownership contract, not expanded behavior. Red1's executable provenance remains historical; revised test identity must be reaccepted before Green. B test attempt1 is consumed, ordinary attempt2 is authorized; conditional3 and the Green chain remain unconsumed.

### M503-B-PREFLIGHT-01 — Missing publication and read admission

Read-only test owner `/root/m503_b_tests` classified B MISSING. Positive evidence: `rescan-comparison.ts` returns a transient result that `service.ts` discards; preparation lacks immediate-lineage admission; GET returns only the historical envelope; API/client readers reject the new completed-run failure variants. Existing tests encode those old boundaries. No file changed or effectful suite ran. Primary accepts the classification at A's unchanged source/test fingerprint; current A216/pure117 runner evidence and ENTRY02 establish the prepared toolchain.

Primary freezes the new client callables for the narrow first-module Red: `comparison-admission.ts::admitComparisonRead(raw: unknown, captured: CompletedRun): ComparisonLineage | null`, returning only G's exact available/unavailable metadata after closed HTTP200/read and immutable identity validation; `comparison-request.ts::createComparisonRequest({run: CompletedRun,current:()=>boolean,readCallback:()=>unknown,settle:(result:ComparisonAvailability)=>void}): {start:()=>void,stop:()=>void}`; `getComparisonRun(runId:string,signal:AbortSignal):Promise<unknown>`. `ComparisonReadCallback` accepts that ID/signal and returns `Promise<unknown>`; `ComparisonAvailability` is lineage or `{status:'unverified'}`. Reuse the existing bounded request lifetime and reentrant current checks. Successful admission never returns or applies an aggregate, and no refresh restores downstream capability. These implementation signatures refine G without changing its semantics, responsibility direction or paths. The exact expected initial missing callable is `src/client/comparison-admission.ts::admitComparisonRead`; all blocked behavior assertions must execute unchanged in Green. Server tests use public service/repository boundaries, not new private implementation interfaces.

B's first test lease may write only the seven named test files and shared M503 fixture. Its coherent Red must cover the publication/failure/lineage/client contract and preserve historical scan behavior and candidate disposal. The action-gate witness must pair an available-baseline successful scan with an equivalent exact task-owned deletion/no-create/no-scan refusal; source absence or a zero-Finding fixture is insufficient. Original test/Green budgets remain unconsumed before this lease.

### M503-A-REVIEW-01 — Persistence slice accepted

Fresh critical reviewer `/root/m503_a_review` returned PASS without Blocker, Major or Minor after inspecting the complete eleven-production/five-test surface and applicable I1–I10 obligations. Independent strict and 117 pure tests pass; 2,884 malformed variants invoked zero getters, 16 transition checks pass, and 162 retained-evidence states, 540 actual full-candidate results and 1,134 durable-reader predicates show no contradictions. Primary's full-diff/cohesion inspection and the 216-test candidate evidence remain valid at source/test fingerprint `d847e90c16bc62380bb07e838455ff1c48bac7c528c3917b21914c8699a587c7`. Frozen manifests, lock and five protected test hashes match. All five terminal receipts are compliant; later plan-only guard drift is expected primary maintenance after closure, not executable drift. Primary accepts A and retires its writers at this barrier. B preflight may begin; native provenance, service lifetime, lineage, UI and integrated proof remain unverified. No requirement/ADR or schema-version change is needed; public capability and lifecycle documentation await integrated closure.

### M503-A-GREEN-02 — Candidate implementation verified

Green2 lease closed compliant (digest 455e88c769e323ff791f9b3bb60b3aa0a6f01fea30dcb733037ce2eb4ea3e8c3; receipt 23a972c78c1f7548b24e145899f23db4842dd45173eec833358e0e60c61df213); only the pure native-reader return typing changed. All 216 focused tests pass with zero skips and strict TypeScript passes. Primary independently ran the strict command, reproduced all five RED03 test hashes, inspected the complete A diff and the three new modules, and checked whitespace. Source/test fingerprint d847e90c16bc62380bb07e838455ff1c48bac7c528c3917b21914c8699a587c7. Cohesion RETAINED: closed types/reader, append transition and existing aggregate writer each own their current responsibility; profile algorithm is unchanged and domain validation is browser-compatible. Behavioral Green and structural fit are accepted as review candidate evidence, not slice acceptance. Fresh critical A review is next. Test attempts1–3 consumed; Green1–2 consumed, conditional3 remains only under recorded justification; one slice review correction loop remains. All leases terminal; no B preflight yet.

### M503-A-RED-03 — Revised test boundary reaccepted

Conditional final test lease M503-A-red-03 closed compliant (digest 41aa3ae5ad2b5adafda04ad4f0beaf797f5c2a1b0d7d0c7f9e12544c510fa93d; receipt a95bed08c17ae7ddd031b0db82ca15b3cb56a8066dd377519a5ebb39a2e39220). Only helper linkage and the void callback changed; primary inspected both corrections. All216 focused tests pass, zero skip; strict has only production TS2352 at comparison-validation.ts:13. This is passing correction evidence against partial Green, not a new Red despite the role's terminal label. Original missing-behavior provenance remains RED02; old accepted test hashes are invalidated. New run-contract hash0eb68f9634462fe56267c99d28eb3c628827e8be1426e4bc8d0189e75ad69b2e and helper2d65593f225efed64b9749528eac600498b005f6f666d323a85544199c06ff33; other3 hashes unchanged. Relevant tree1ee5ef8bfc78d37c861470739409ce08f1d82e7b0cb551caaf8f5fa3c50a0617. Primary reaccepts all five protected tests. Test-side three-turn budget is consumed. Green2 may correct production typing only and complete cohesion/verification; no test edit or fabricated Red rerun is authorized.

### M503-A-GREEN-01 — Partial implementation and conditional test correction

Green lease M503-A-green-01 closed compliant, exact eleven production paths only (digest da50c4a3929fd91dbe69e6f5c93a1f5ad975ad79137229d87674a112e4863820; receipt be22bb5ba865802cdd5841d2e03d8c9a1382359b50747d9d8c003ec756965693). All five accepted tests stayed unchanged. Focused result 215/216 passes; the sole failure reveals the helper replacing an existing run-baseline link with baseline-run, correctly refused by append immutability. Strict identifies a test callback returning a record where void is required and a production conditional-type cast. Primary inspected these exact paths. This is material new execution evidence after Red2, whose missing callable previously hid the linkage mismatch. Conditional final Red attempt3 is authorized for the two test defects only: preserve the actual parent baseline ID while deriving the fixture, and make the test callback return void. Same objective/scope/commands; no new behavior or assertions are weakened. The other215 tests already pass, so these two precise corrections are likely to complete the test boundary. Run the unchanged focused command and strict to isolate the remaining production error; report passing revised tests as correction evidence, never a fabricated new Red. Prior accepted Red is invalidated by this test repair and must be reaccepted; test-side budget is exhausted after attempt3. Production cast correction remains Green attempt2, with one conditional Green3 only if later justified. No slice PASS or closure is claimed.

### M503-A-RED-02 — Corrected Red accepted

Lease M503-A-red-02 closed compliant (digest 12c8adb14c9b108252a8032cd40a15bc944f97317e409f8e21b57cf636532c5f; receipt b19d6cf5d4b162a201084ef7860fdcbdf4c6c02228addff365caae5ea45ed603). Primary inspected the five-path diff, valid overwrite/running-finish controls, actual-parent comparator fixtures, contrast and D7 cases, and downstream immutable-field negatives; independently reproduced all five test hashes. Red command: 216 tests,189 pass,27 expected missing-validator/callable failures, zero skip;185 legacy tests pass. New producer and stripped-parent controls execute; append-dependent assertions await Green. Test/source fingerprint26d709a8372a70919cd85b54132608b934a3f343e6d6d616a03359c4da1dbdac. Accepted hashes: run-contract b0028f6384a674f602fea39dba51a09bf0fa6af8715bf9f79936e6d8808904cc; run-repository e2ee94576de5293b497d6e984fbf8e72db2a785f0e52142d31a35ee9e6a37b3a; review-repository9e9daeaa53706cdef6d7c9e754b6d2aee6db879b0d6bcda5dd559904f42b4b34; comparison-persistence312ab98d74d9f2d717be4dde112c4409b7c75c06610c4e3bfb8e02b579a18bac; helper be358fd9487c91d8fc010166f01ac3c960d0c44c8d525cc31b73a39f77a0242c. Primary accepts Red; all tests forbidden to Green. Filesystem evidence is Non-reusable without exact isolated identity, so Green reproduces Red before editing. Domain readers must remain browser-compatible pure code because client admission shares validateRun.

### M503-A-RED-01 — Test-boundary correction required

Lease M503-A-red-01a closed compliant (digest 4fcd27c38fec732fd6b9ea4a95182af40eb76ff51a0ce116fbf25e6124f78ee1; receipt 6050c6bb971028f302a92628f40bb87899931681f5e56faf6cc50d64b28815ae), only its five test paths changed. Frozen command reported 208 tests: 189 pass, 19 expected missing-behavior failures, no skip; all 185 pre-existing tests passed. Primary inspected actual diff and does not yet accept Red: the overwrite vector used noncanonical rationale but expected a transition error; finish bypass started from completed rather than running; the downstream fixture attached unique-violation to a native scan with duplicate exact targets; valid contrast-delta and decisive D7/preservation negatives were missing. Correction attempt 2 retains the exact contract, scope and commands; use valid positive controls and branch-specific negatives before unchanged Green. Prior candidate Red is superseded, not implementation evidence. No production edit occurred.

### M503-A-PREFLIGHT-01 — Durable gap accepted

Read-only test owner `/root/m503_a_tests` returned MISSING: the completed aggregate excludes comparison, the repository has no updateComparison, and its existing generic writer/transitions do not admit the new append. Existing transient comparator coverage does not cover persistence. Primary accepts ordinary Red through existing validateRun/openRunRepository; no missing-module exception or production stub is required. The five-path test boundary is run-contract, run-repository, review-repository, new comparison-persistence and the task-specific fixture composed from existing comparison-fixtures. Source/tests remain at ENTRY02 identity; no preflight writes or implementation attempts occurred. Workflow M503-20260919, slice M503-A; standard S3 with separate test/code owners and critical review. The Red contract is the accepted G durable/append contract only; B/C behavior is excluded. Initial guard start rejected the slash-containing agent owner before creating a lease; active pointer absence was verified. Use guard owner m503_a_tests, mapping to the canonical agent, with a fresh lease ID; no worker attempt was consumed.

### M503-ENTRY-02 — Execution authorization and baseline

2026-09-19: owner explicitly authorized this exact task through G, A–C, D, reviews, existing correction budgets and documentation closure, superseding planning-only authorization without waiving gates. Fresh HEAD is `08d75e9d573a5f71dc284e1b4c5946380b63eb3b`; entry worktree is clean and no active lease exists. Historical planning HEAD remains provenance. Prepared Node v24.20.0, PowerShell 7.6.5 and Python 3.12.10 are available. Maintained README preparation plus the exact four-file baseline command passes independent strict TypeScript and all 111 tests, with zero failures/skips. All three frozen manifest SHA-256 values match M503-ENTRY-01. No acquisition or application source change occurred.

The two authorized R3 dimensions are assigned read-only to `/root/m503_durable_research` and `/root/m503_operation_research`, with configured `critical_researcher` roles, one initial report and at most one follow-up each. They compare the existing frozen candidate categories and criteria under I1–I10; primary owns authority, runtime/command preparation and authoritative writing. No additional panel or implementation preflight is authorized before G acceptance.

Additional read-only checks verify all thirteen generation and eight companion path/hash references. Source/test fingerprint is `ee8b377eeed190b4f5fc197cb05d1871e33f7f1ed3969c21341f9aefbd1c759d`: SHA-256 of UTF-8, no-final-newline, sorted `rg --files src tests` entries rendered as slash-normalized `path:lowercase-file-sha256`, joined by LF. Installed package versions match the selected stack: TypeScript 7.0.2, React 19.2.8, Playwright 1.62.1, axe-core 4.13.0 and Vite 8.0.16. The existing browser directory contains Chromium revision 1234. Scanner, UI and integration scratch inventories are empty. Package lock SHA-256 is `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`. One initial PowerShell package-lock inspection used the object parser, which rejects the lock's empty package key; the corrected `ConvertFrom-Json -AsHashtable` read succeeded without changing any file.

### M503-G-RESEARCH — Evidence and synthesis inputs

Both critical researchers returned RESEARCH COMPLETE, then used their one permitted follow-up at the analyst's request. These are recommendations and source evidence, not an accepted literal artifact or runtime proof. `/root/m503_g_analysis` owns the mandatory synthesis; no optional drafter or extra research is assigned.

Durable evidence D1–D6 covers the comparison requirements, lifecycle and ADR-0021; `comparison-contract.ts`/`compare-finding.ts`; the pure correlation, outcome and profile modules; closed domain readers; the staged repository writer; and explicit transition protection. Existing tests were inspected, not rerun by the researcher. A narrow singleton projection removes duplicated later identity/context while retaining a baseline Finding reference, minimized violation observation and complete allowlisted baseline scan context. The existing internal-result alternative is viable but carries additional equality obligations. A closed reader may reuse pure policy functions without calling run-input readers: their comparison-contract imports are type-only. Narrowing `comparisonProfile` to its actually consumed context is sufficient; a fabricated baseline aggregate or duplicated policy is unnecessary. Append-only `updateComparison(expected,input)` can reuse the existing writer, reject stale/duplicate/overwrite updates and preserve every other field. `finish` must reject comparison and selected-Finding transitions must explicitly preserve it.

D7 independently checked `target-correlation.ts` precedence and `scan-validation.ts` coverage. With B = available baseline locator, E = retained same-rule exact-target count, U = retained same-rule unavailable-locator count and P = validated pass count or zero, the proposed durable consistency table is: baseline-unavailable requires !B; ambiguous requires B and E+P>=2; later-unavailable requires B, E<=1 and (U>0 or P>0); no-exact-match requires B, E=0 and U=0; unique violation/incomplete requires B, E=1, U=0 and the exact corresponding retained source; unique pass requires B, E=0, U=0, P>=1 and one exact minimized pass. Uncertainty stores no after/delta and requires inconclusive with reason=match. Unique rows reuse exact outcome/reason/delta classification. This proves only consistency with a possible discarded-pass arrangement; actual full-candidate admission and calculation remain the publication witness. No pass archive or fabricated candidates are permitted.

Operation evidence OP1–OP9 covers lifecycle truth, service reservation/admission, transient calculation/disposal, rename commit, existing rescan/GET framing, client response ownership, downstream preservation and Results composition, plus existing adverse tests and controlled harnesses. Both candidate dimensions support staged append after scan completion and extending existing rescan/GET boundaries. Coordinated publication would still need independent completed-scan truth; a new read endpoint has no demonstrated capability advantage. Baseline availability stays ephemeral. The operation rereads baseline native facts/context and immediate comparison linkage before append; downstream human fields neither gate nor alter classification. Saved comparison reads remain valid when baseline reads fail. New rescan from a comparison-bearing baseline checks its immediate link before creation without recursive ancestry traversal.

The operation follow-up proposes exact success `{ok:true,run}`; precreation lineage refusal with null run, persisted=false and cleanupFailed=false; and completed-scan comparison failures with persisted=true, comparisonPersisted=false and no saved comparison. Its bounded error table is calculation/500/clean/release, lineage/409/clean/release, persistence/500/actual-cleanup/release-only-if-clean, aborted/409/clean/release, and shutdown/503/clean-or-actual-failed-update-cleanup/retain. Stop/deadline precedes preappend abort; actual append failure precedes simultaneous abort; successful append cannot be downgraded by postrename stop. Existing scan errors retain their contracts. GET alone adds lineage metadata; busy/stopping remain read failures. Initial inspection and explicit baseline/later navigation refresh it without polling or replacing active downstream fields. Unavailable or unverified retention withholds immediate baseline context while saved minimized evidence remains inspectable.

Primary reuse inspection confirms run-level comparison must be outside selected `ResultDetail`. `RuleEvidence` reads rule/evidence/locator only; accepting that structural evidence projection reuses its rendering for a pass without inventing a Finding. Existing `ResultsSection`, App shared coordination, rescan feedback and restrained styles can be extended; new comparison detail/presentation and read-request/admission owners each have a distinct current responsibility. Exact literals, path ownership, command effects and I1–I10 acceptance remain subject to analysis and the two fresh research checkpoints.

Mandatory analyst `/root/m503_g_analysis` independently inspected the seams and returned **DRAFT READY** after both bounded clarifications. It found no remaining owner-controlled choice or decision semantics to defer to Green. It endorses narrow projection, staged append and existing GET, with the D7 admissibility table, exact response/release tuples and metadata-only read admission. Actual scanner/publication/lineage/browser behavior remains unproved. Fresh `/root/m503_g_predraft` receives the complete outline and evidence for the pre-draft checkpoint; the final artifact reviewer must be a different fresh instance. The analyst retains one supported correction; no correction is consumed by incorporating the two original research follow-ups.

### M503-ENTRY-01 — Planning evidence

2026-09-19: initial worktree clean at `b4f76885a4e146bd6c977866847e61dd7d759869`, with no active lease. Roadmap and M502-FINAL-01 agree: 22 Complete, 6 Not started before this planning selection. M5-02's 805-test/40-file strict/build/native integration and final critical PASS are historical closure evidence, not rerun claims. Current prepared Node is v24.20.0; fresh independent strict TypeScript and the corrected four-file baseline command above pass all 111 tests, zero failures/skips. The initial missing-module-mock-flag failure is preserved in Surprises & Discoveries. No source changes were needed.

Current SHA-256: RD-003 manifest `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459`; M3-01 manifest `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; M5-02 companion `8a44166b40f700b5dc35a3290b7a4958533c38fc454302d843499c576f43e0a8`. Read-only recursive path/SHA validation passes all 13 generation references and all 8 companion references; all six fixture files exactly equal RD-003's frozen content. The focused comparison tests also validate the companion and controlled binding.

Primary reviewed roadmap-wide status and routed authorities, frontend owners and the reuse audit. Read-only explorer `/root/m503_seams` mapped existing backend publication, schema, transition, API and test boundaries; no option selection or write was delegated. No dependency, browser, scan, model, original aggregate or Git metadata mutation occurred. Planning artifacts are only this plan, its progress summary, roadmap and the two indexes. Preserve this entry as historical identity when execution refreshes HEAD.

### M503-PLAN-01 — Accepted planning readiness

Fresh `critical_reviewer` `/root/m503_plan_review` reviewed the complete plan, five-path documentation diff and actual schema, repository, service, client-admission and Results seams. Verdict: **PASS**, with no Blocker, Major or Minor. Its independently checked HEAD, active-lease absence, unchanged executable/configuration surfaces and three manifest identities agree with M503-ENTRY-01. The reviewed semantic plan SHA-256 was `3e38ec95528113e1f1ee64d2eb6ba4c4ff2a66c3f9519fb552b46a61669cacbf`; subsequent edits only record this verdict and reconcile pending-review statements, without changing the contract. The downstream-consumer check confirms existing guidance/generation/review client admission preserves the rest of the aggregate while permitting only the selected Finding change.

Primary accepts planning readiness, not implementation or G decisions. Exact serialized/publication/lineage/response literals and runtime/browser correctness remain unproved. Documentation validation passes the five changed/new documents: local links/anchors, all sixteen required sections, two parsed PowerShell blocks, UTF-8 without BOM, final newlines, no trailing whitespace, consistent 22 Complete / 1 In progress / 5 Not started statuses and `git diff --check`. No application or evaluation input changed, and no commit or push occurred.

Documentation impact: Added this ExecPlan and its concise progress record; updated the roadmap, plan index and progress index for planning-only activation. Requirements, ADRs, current implementation summaries and frozen evidence remain unchanged. The frontend-quality skill shaped the reuse audit and bounded accessible browser-proof obligations without introducing a new UI system.

## Interfaces and Dependencies

The implementation retains the pinned Node/TypeScript/React/Playwright/axe/Vite stack and single-file repository. M5-02's internal `ComparisonResult` calculation remains unchanged; the separate validated durable projection, append-only repository update, service outcome and browser admission implement G's accepted contract. No new package, service, provider, public endpoint family or standalone comparison artifact was added. Browser code shares pure types/readers without privileged I/O; the comparison-profile input narrowing preserves an acyclic domain-validation dependency.

### M503-G-FINAL-01 — Final artifact correction

Fresh final researcher /root/m503_g_final returned REVISE with one Major evidence-design finding and no Blocker/Minor. The native corrected endpoint has zero Findings, so it cannot discriminate lineage refusal from missing-Finding admission. Primary corrected B to require a real selectable violation, an available-baseline positive control, and an isolated deleted-baseline exact refusal/no-create/no-scan witness. D retains native zero-Finding persistence/read/display/deletion proof; integrated acceptance explicitly combines both. Product semantics, paths, commands, effects and research conclusions are unchanged. This consumed final-artifact correction cycle 1 of 2. The same reviewer reran every I1–I10 and returned PASS without remaining findings on reviewed artifact SHA256 8e424ae4acde7031320aea20b252bd8a118ee08c151f0a3095782b266b62dd13. Primary accepts G; subsequent living-state edits only record this verdict. All implementation evidence remains NOT RUN.

## Revision Note

2026-09-19: reconciled the candidate A state and consumed test/Green allowances. Green2's terminal receipt is compliant; a later guard status reports only expected primary-owned plan maintenance after closure. The unchanged source/test fingerprint and protected hashes preserve focused evidence identity. No worker wrote after its terminal handoff, and no lease is active.

2026-09-19: recorded owner execution authorization, fresh execution baseline and the two bounded G research assignments. Planning-only history remains preserved; no G option or implementation result is accepted by this update.

2026-09-19: created for owner-requested M5-03 planning after Complete M5-02. Recorded fresh verification, the transient-to-durable gap, gated literals/commands, three sequential worker-owned behavior slices, reuse-first accessible comparison and bounded integration. No implementation or later task selected.

2026-09-19: recorded fresh independent critical planning PASS without findings and reconciled living state, progress and documentation validation. Future execution authorization, G and all implementation gates remain required.
