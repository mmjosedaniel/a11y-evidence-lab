# M6-01 — Shared deterministic evaluation

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain Current state, Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective as work proceeds. Follow [PLANS.md](../../../PLANS.md), the [agent workflow](../../../.codex/README.md), the [implementation workflow](../../../.codex/execplan-implementation-workflow.md), and the [write-lease guard](../../../.codex/write-lease-guard.md).

## Current state

- **Task:** [M6-01](../../DEVELOPMENT_ROADMAP.md#m6-01--run-shared-deterministic-and-workflow-checks), Complete after final critical PASS and documentation closure. Original execution authorization and the narrow owner-authorized startup/replacement G1 are recorded below. M6-02–M6-04 remain unselected.
- **Accepted work:** G identity/contracts, guarded A characterization and critical re-review, exact frozen abstention, three current actual retrievals, all 883 maintained tests, independent strict TypeScript and current build. See [shared matrix](#m601-b-matrix-01--separate-shared-observations), [renewed retrieval](#m601-b-retrieval-02--three-current-actual-observations) and [regression](#m601-c-regression-01--current-maintained-evidence).
- **Preserved failure and limits:** Original G1 failed on unavailable runtime before embeddings and remains intact. The explicitly authorized replacement then completed; G1 still has the known semantic relevance failure despite role completeness. No quality dimension was tuned or erased. All actual/frozen allowances are consumed; no replay.
- **Closure and next boundary:** [Final integrated acceptance](#m601-final-01--integrated-review-and-documentation-closure) passes. No active lease or unresolved finding. Initial and ordinary correction writes and their single review-correction loop are closed. This plan is archived; no later task is selected. Consumed actual-case commands are historical evidence, not replay permission.
- **Identity:** Execution began from clean HEAD `799c0fe388f269c9a36275ef2a39399c16d6e524`; historical planning HEAD `97d1e9e135c242ee49c6f5ea18514ba7c32364f4` is not a future gate. Production/frozen/retained inputs remain unchanged.
- **Scope preserved:** No generation request, credential access, fresh public scan, retained human decision, acquisition, workflow/model configuration change, later task, commit or push. The owner-authorized existing Ollama server remains running for the developer.

## Progress

- [x] (2026-09-20) Review current authorities, roadmap, completed checkpoints, current source/test seams, preserved inputs and verification commands.
- [x] (2026-09-20) Verify strict TypeScript, 162 focused tests, frozen artifact references and clean planning baseline.
- [x] (2026-09-20) Draft the bounded verification plan and activate only M6-01 planning.
- [x] (2026-09-20) Independent planning-readiness PASS and documentation checks accepted after two draft corrections.
- [x] (2026-09-20) Owner authorizes execution through G/A/B/C; current clean checkout reconciled.
- [x] (2026-09-20) G records identity disposition, frequency map, exact commands and critical caller acceptance.
- [x] (2026-09-20) A characterization accepted after one guarded review correction; frozen abstention passes once.
- [x] (2026-09-20) B shared observations accepted after owner-authorized startup/replacement G1; all three actual profiles observed, G1 relevance failure preserved.
- [x] (2026-09-20) C authoritative regression/strict/build, different final critical PASS and documentation closure accepted; task Complete and plan archived.

## Surprises & Discoveries

- Existing tests already execute all six frozen scanner states with managed Chromium, check native buckets and corrected positive observations, and cover review/comparison/persistence failures. A new general evaluation runner would duplicate those responsibilities.
- The frozen no-call package is present, but no existing test explicitly binds its exact path/hash. Synthetic no-call tests alone do not prove that particular vector or its rendered abstention.
- Current role-based retrieval returns supported guidance for all three historical M3-05 profiles, but informative-image G1 has only one gold match and unsuitable decorative-image interpretation/remediation. Role coverage and caller exit zero are not semantic relevance.
- The old retrieval caller writes historical, occupied roots. Its zero-gold-hit failure threshold does not reject G1's semantic problem. It is reference material, not a replayable M6 command.
- M5-04's public target allowances and M4-03's human-review allowances are closed. Historical evidence can be assessed without repeating those external or human actions.

## Decision Log

- **2026-09-20 — Verification-first scope.** Primary selects a documented execution path over existing maintained checks, not a new framework, manifest format or production feature. Add test-side code only for a demonstrated gap.
- **2026-09-20 — Natural frequency.** Execute controlled shared checks once per applicable case, not per generation provider. Reuse admissible historical actual observations only after complete evidence-identity assessment; never describe them as fresh M6 executions.
- **2026-09-20 — Frozen no-call binding.** Plan one explicit frozen-package observation through existing application seams, including rendered terminal abstention and no review entry. Any bridge is test-only, honestly synthetic where needed, and cannot authenticate independently assembled guidance as actual embedding retrieval.
- **2026-09-20 — Preserve adverse evidence.** Report relevance, support, citation integrity, persistence and cleanup separately. Do not tune ranking, change gold or replace a failed observation to obtain a passing summary.

## Outcomes & Retrospective

The exact frozen no-call gap is closed without production changes. Current maintained verification covers 883 tests, and three bounded actual retrievals now bind current source/runtime to unchanged controlled native inputs. The first G1 prerequisite failure remains preserved; owner-authorized startup and one replacement resolved availability without changing any evaluated input. G1 still fails semantic relevance, while all profiles satisfy the frozen direct-support minimum and canonical citation checks. Final independent review and documentation closure pass. M6-01 is Complete and no later task is activated. Review improved the caller before its non-repeatable observation by preserving failed evidence and checking the complete aggregate.

## Purpose / Big Picture

Produce one compact, traceable provider-independent evidence set for the existing scan-to-review/comparison workflow. A reader should distinguish real native scanning, constructed boundary inputs, genuine historical retrieval, frozen no-call evaluation, human-authored decisions and actual public observations. This task does not evaluate Local or Groq generated output: the fixed six generation executions belong to M6-02.

## Context and Orientation

### Authority and current project state

Start with the [authority map and task router](../../README.md#authority-and-status-map), [shared requirement semantics](../../PROJECT_REQUIREMENTS.md), and the selected roadmap entry. Resolve all selected IDs through the roadmap's authority-location key. Controlling scope is:

- [Evaluation and acceptance](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md): REQ-EVAL-001–005 and 007; shared checks, rubric, comparison policy vector, generation-only exception and amendments.
- [Evidence and review requirements](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md): REQ-SCAN-002 and 004, REQ-EVID-002.
- [Quality requirements](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md): REQ-QUAL-020.
- [Specifications](../../specs/README.md): SPEC-001, SPEC-002, SPEC-003 abstention/retrieval-failure scenarios, SPEC-005–008; HS-001, 004, 006, HS-008 no-call boundary, HS-010 and 015. These are derived expectations, not execution evidence.
- Accepted ADR-0018 trusted input, ADR-0019 exact retrieval with its role-selection amendment, ADR-0021 aggregate, ADR-0022 corpus, ADR-0023 local boundary and [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md) ownership. Follow [local feasibility](../../LOCAL_MVP_FEASIBILITY.md) only if actual retrieval renewal becomes necessary.

Twenty-four tasks are Complete at entry. M1–M5 implement the application, including real scanner, retrieval, both adapters, review, rescan and comparison. [M5-04 closure](m5-04-comparison-checkpoint.md#m504-final-01--integrated-review-and-task-closure) records 881 passing tests across 45 files, independent strict/build, controlled comparison, one actual public comparison and final critical PASS. Those are historical observations, not a fresh full-suite run here. M6-02, M6-03 and M6-04 remain Not started.

Actual Local/Groq integration and exact-configuration capacity observations exist; they are not the six fixed generation evaluations. Full application-accessibility verification belongs to M6-03; 200% zoom is Deferred, and semantic announcement tests do not prove speech output. Existing native, retrieval, review and comparison claims remain bounded to their recorded contexts.

### Frozen and protected inputs

Preserve exact bytes, source notices, versioned semantics and private/public distinctions:

| Input | Planning SHA-256 or identity |
| --- | --- |
| `evaluation/rd003-scan-v1.json` | `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459` |
| `evaluation/m301-generation-v1.json` | `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b` |
| `evaluation/m502-comparison-v1.json` | `8a44166b40f700b5dc35a3290b7a4958533c38fc454302d843499c576f43e0a8` |
| `package-lock.json` | `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d` |
| Frozen no-call package | Manifest `shared.noCall`; `temp/m301-generation-freeze-v1/no-call.json`; `1b07d5d764ad99bb028db1aef767b9bb9d0685661b5ad761731d60f93394b6dd` |

The generation manifest's 13 path/hash references and comparison companion's eight references include the corpus/gold, six controlled HTML states and private frozen packages. Hash file bytes case-insensitively against hexadecimal declarations; do not confuse file hashes with normalized corpus identities. Reject missing/mismatched artifacts; no regeneration or refetch to pass a gate.

Preserve the 14 pre-existing `data/runs` directories and all historical `temp/m204-*`, `temp/m301-*`, `temp/m305-*`, M4-03 and M5-04 retained evidence, developer runtime/model assets and credentials. G inventories exact ordinary paths and hashes without copying raw contents into tracked documentation. Existing unknown scratch contents are not disposable.

### Shared evidence and frequency map

This is the minimum matrix, not a new scorecard. B records a result, origin, exact identity, decisive assertion, limitation and disposition for every row.

| Check and authority | Existing evidence / intended execution | Frequency and acceptance |
| --- | --- | --- |
| Native scanner and evidence: SPEC-001/002, HS-001/004/006, REQ-SCAN-002/004, REQ-EVID-002 | `tests/scan-page.test.ts`, `scan-normalization.test.ts`, frozen RD-003 | All six fixed failing/corrected states once in the authoritative suite; exact rule coverage, all violation nodes, incomplete distinction, native minimized facts and corrected same-target positive observations. Synthetic projection tests are separately labeled. |
| Zero, failure and trusted input: SPEC-001/002/008, HS-001/006/015 | Scanner, run-contract, local-service and walking-skeleton suites; M5-04 actual public witness | Malformed/credentialed/non-HTTPS rejection before creation/navigation; complete zero distinct from failure; fresh top-level-only contexts, finite navigation and cleanup. Intercepted HTTPS is not public proof. Historical authorized public observation is dated, not current reachability or hostile-target safety. |
| Actual retrieval, gold and citations: SPEC-003 subset, HS-008 boundary, REQ-EVAL-003/004 | M3-05 G1/G2/G3 observations; retrieval-contract, embedding-retrieval, retrieval-service suites | Assess each of the three profiles once against frozen gold and canonical source identity. Fake embeddings prove policy, not relevance. Historical actual evidence is reusable only under G's identity rule; renewal is a separately bound branch below. |
| Support and frozen abstention: SPEC-003, HS-008, REQ-EVAL-001/007 | finding-sufficiency, generation-stage, retrieval-service and guidance UI suites; exact frozen no-call binding in A | One frozen incomplete-guidance observation, not once per provider. Selected Finding remains visible; missing remediation role/reason/no-call/manual guidance render; no adapter preparation, transport, ProviderInvocation, proposal or review decision. Separately verify missing/conflicting/evidence-incomplete states and retrieval execution/integrity failure without conflating them with abstention. |
| Human review: SPEC-005, HS-010 | review-contract/repository/service, finding-review UI and review-checkpoint suites; M4-03 historical actual decisions | Once per action on synthetic validated proposals: approve, edit-and-accept, reject; support and blocking judgment gates, exact edit/time, preserved original, non-blocking reminder. Historical owner decisions are not replayed or fabricated. |
| Conservative comparison: SPEC-006/007, HS-015 | comparison-pair/outcome/persistence/service and rescan integration/UI suites; frozen M5-02 companion | Three native controlled pairs, uncertainty and compatibility cases; 3.54→4 contrast vector is policy-only improved while still failing; reversed binary regression is non-persisted evaluation-only. No causality or whole-page claim. |
| Durability, failure and cleanup: SPEC-008, REQ-QUAL-020, HS-015 | run-repository, local-service, retrieval/review/comparison persistence/service and integrated tests | Evidence/siblings/original decisions survive downstream failure, restart and permitted updates; exact synthetic deletion and failed-cleanup truth. No pre-existing retained run deletion; no recovery retry masquerading as a new case. |

### Retrieval interpretation retained at entry

[M3-05 actual retrieval](m3-05-generation-checkpoint.md#m305-d-observation-01--three-actual-controlled-retrievals) used the accepted highest-per-required-role selection: G1 gold matches 1/3, G2 3/3 and G3 3/3, all structurally supported. G1's decorative-image interpretation and H67 remediation are unsuitable for the informative-image scenario. Record that relevance failure explicitly even if the existing caller exits zero. At least one direct-support gold passage is the stated REQ-EVAL-003 minimum; do not invent an all-gold threshold or turn that minimum into a blanket semantic pass. The earlier M2-04 global-three results all abstained and cannot stand in for current role-selection behavior.

## Scope and Non-Goals

In scope: assess existing evidence identity, execute the shared controlled checks at natural frequency, close the exact frozen no-call binding gap, record separate outcomes and limitations, and reconcile documentation.

No new production feature, dependency, model, corpus, fixture/gold version, ranking policy, manifest schema, generic evaluation runner, dashboard, telemetry, benchmark, automatic retry or history UI. No generation request, credential access, model acquisition, capacity rerun, fresh public scan or retained human decision is selected. No new rendered design; existing UI tests may verify this task's frozen abstention. If rendered production changes become necessary, replan the slice under the frontend-quality skill before writes. M6-02's six runs, M6-03's broad accessible path and M6-04's portfolio synthesis remain outside scope.

## Plan of Work

### G — Reconcile identity and freeze the minimum proof contract

After execution authorization, primary rereads current state/authorities and records the intentional current HEAD, complete dirty set, source/test bytes, commands, runtime, frozen inputs and protected endpoints. A plan commit or unrelated intentional HEAD movement does not invalidate history automatically; explain the diff and bind a fresh execution baseline consistently. No command compares future HEAD with the historical planning SHA.

For each reusable observation, compare the complete relevant evidence identity: executable source and transitive dependencies, test/caller, exact inputs, configuration, runtime/command and evaluated assertions. Cite the original revision and actual retained files. Unchanged paths alone or passing hashes of output alone are insufficient. A changed client does not erase an unchanged backend observation, but it cannot inherit the old client's visual proof. Missing original identity makes the affected proof pending, not implicitly passed.

Prefer existing commands and archived evidence inspection. Do not run archived preparation/actual-case/cleanup procedures. The primary and read-only test-worker preflight bind the frozen no-call bytes through existing canonical citation, sufficiency, service and UI seams. The frozen package is an evaluation vector: any necessary synthetic retrieval envelope is explicitly test-only, uses canonical passages and does not claim authentic ranking or score provenance. Verify actual application-authored output, not merely the package's expected fields.

Before the first write, add `M601-G-CONTRACT-01` here with exact files/symbols, observable assertions, fixture provenance, UI/disk crossings, commands, expected mutations, protected paths, cleanup, frequency and stop conditions. No production change is presumed. Use a fresh critical reviewer for the no-call integrity and preservation contract; a review may accept the exact packet together with its subsequent proof only if no unreviewed effectful execution precedes acceptance.

**Conditional actual retrieval renewal:** if historical G1/G2/G3 cannot satisfy the complete identity rule, renew only affected profiles. Freeze a current M6-only caller/root contract first; never replay the occupied M2-04/M3-05 caller unchanged. A future authorization to execute this plan includes at most one explicit local embedding retrieval per affected profile, at most three total, after this gate. This is real EmbeddingGemma work, not generation. Bind the fixed model/runtime/corpus/ranking, cold/warm collection state, corpus/query request bounds, distinct exclusive case roots and observed request accounting. No retry, tuning, acquisition or new capacity screen. Existing callers are reuse candidates, not mandatory infrastructure. If a new integrity/recovery mechanism must be selected rather than reused, apply the conditional R3 route below before freezing it. If fresh retrieval is unnecessary, omit this entire caller branch.

No fresh public target is authorized. Reuse the scoped historical public observation only for the assertion it established and after identity assessment. If that assertion cannot be supported, finish unaffected work and request a named target and finite allowance; do not silently use W3C or Ollama again.

### A — Worker-first preflight and bounded characterization

Use workflow ID `m601-shared-deterministic-evaluation`, work-slice ID `A-frozen-abstention`. Primary issues the complete Milestone Assignment Packet v2 to a persistent `test_worker` for read-only preflight. It must identify the missing assertion, existing seam, least change and current focused command. Preflight may run only commands already bound by G; no lease, writes, live inference or private-case execution.

Normal route: **TDD applicable — behavior preflight**, with `EXISTING_BUT_UNCOVERED` expected but not pre-decided. If confirmed, use the workflow's guarded `evidence` phase for passing characterization plus negative tests of the test-side binding; no Red/Green turn is needed for already implemented behavior. `EXISTING_AND_COVERED` instead reuses exact evidence without a new test or Green. Do not fabricate Red or classify this as non-behavioral setup. Candidate envelope is `tests/m601-frozen-abstention.test.ts`, a purpose-named `tests/helpers/m601-frozen-abstention.ts` only if needed for the opt-in private vector, and narrowly selected existing guidance UI fixture/test seams. These are candidates, not a blanket lease. Freeze the smallest exact subset in G; default tests use project-owned synthetic inputs and must not depend on ignored private files. The explicit frozen-case command fails closed if its required package is absent; it cannot skip and report success.

Responsibility direction: test binding reads/authenticates the frozen definition and delegates to existing canonical guidance/sufficiency/service/UI behavior; no application import of evaluation files, no duplicate support algorithm, no wrapper framework. Keep existing browser harness ownership and teardown. A separate helper is justified only by the distinct opt-in file-binding responsibility, not file length.

Primary opens one exact guarded lease per write turn, verifies the digest/packet projection, then delegates. Workers may not edit plans, README, authorities, frozen inputs, credentials, historic evidence or Git metadata. Close the lease terminally; inspect actual diff, tests, assertions, responsibility fit and protected identities before accepting the handoff. Primary documentation changes occur only between leases.

If preflight exposes missing/regressed production behavior, consult the [bug recording rules](../../bugs/README.md), revise the exact packet, and use separate test-worker Red → primary acceptance → code-worker Green/optional same-turn Refactor. Accepted tests stay byte-unchanged during Green. No production path is authorized until that revised packet names it. A significant behavior/architecture change or expanded scope needs the controlling decision/owner, not a workaround test.

Review this integrity-sensitive slice with `critical_reviewer` before using a new opt-in caller or accepting its proof. One ordinary preflight correction; write attempts 1 and 2, with attempt 3 only under the workflow's documented progress/new-learning/different-action/likely-benefit conditions. One review-correction loop uses remaining phase budget, not a new allowance. Two failed attempts without new facts, two no-diff turns or exhaustion stop the worker for primary triage. Do not reset budgets by renaming a slice or caller.

### B — Execute and interpret the shared checks

Primary accepts the exact commands and artifact identity, then executes the frequency map sequentially. The full maintained suite may supply most rows; do not first run it as B and repeat it unchanged as C. Focused A checks are development evidence, not additional fixed evaluation cases.

Perform the explicit frozen no-call observation once. It must link exact package bytes to selected-Finding/application/disk/UI assertions and zero-effect counters. If any layer is not exercised, record that missing proof rather than substituting an adjacent synthetic test. Browser text/semantics can prove this narrow existing state without a new design or screenshot program.

Inspect/reuse the authentic retrieval and public/human observations under G, or run only the specifically activated renewal branch. Record ordered passage IDs and separate gold, relevance, support and citation judgments without publishing private payloads. No generation provider availability check or credential read is needed.

Retain one compact result table here: case ID; actual execution or historical reuse; command/evidence identity; native/synthetic/policy/public origin; observed outcome; separate failed dimensions; persisted/readback/cleanup evidence; limitation and rerun trigger. Aggregate suite counts are development verification, not product success percentages. Preserve failed attempts and unknowns.

A failed quality dimension is an evaluation result, not permission to tune inputs. Record G1 honestly. An unmet applicable Must, failed required check or unverified mandatory case prevents task completion and dependent advancement; distinguish this from a recorded semantic limitation where the controlling contract's explicit minimum still passes. Obtain owner disposition only when authority or scope actually needs to change, never erase the failure.

### C — Integrated verification and documentation closure

After the last accepted test/source change, run the complete authoritative suite (45 files at planning entry, plus any accepted additions), independent strict TypeScript and current client build under the maintained README procedure. Reuse a B run only when its full evidence identity remains current. A reviewer handoff alone does not invalidate it; material changes do.

A different fresh `critical_reviewer` reviews integrated evidence, no-call identity, data preservation/recovery, honest provenance, scope and documentation. Reproduce risk-critical checks where useful; reuse ordinary current evidence. Correct only within remaining budgets.

Primary applies the [documentation closure gate](../../README.md#task-closure-documentation-gate), records limitations and exact retained/disposable artifacts, and updates the roadmap/progress/navigation. Archive this plan only after all M6-01 verification and closure gates pass. Do not activate M6-02 automatically.

## Decision Review Contract

No new product, technology, ranking or persistence decision is proposed. Current planning is bounded repository evidence mapping (one independent read-only code-seam inspection); it is not competitive research. R0 administrative facts remain primary-owned. No research panel or drafting agent is required merely to run existing checks.

If G reveals a consequential proof-mechanism choice, record its question, authorities, options (reuse existing commands first), hard constraints, common criteria, evidence gaps, smallest scope, decide-now/prove-later boundary and stopping rule here before selection. Use objective R0–R3 routing from the agent workflow. A new custom integrity/identity/recovery mechanism triggers R3: bounded critical researcher for each independent critical evidence dimension, mandatory decision analyst, fresh pre-draft critical research reviewer, primary-authored freeze and a different final critical research reviewer. Research/drafting/review remain read-only. Budget one bounded discovery per unresolved dimension and one correction pass; escalate unresolved material uncertainty without inventing evidence. Optional R2 review remains conditional on its listed triggers, not mandatory after every DRAFT READY.

No R3 contract may silently authorize a new application feature, model call budget, target, data deletion or manifest amendment. A routine same-contract caller literal resolved from existing evidence is not automatically a new architecture decision.

## Concrete Steps

All commands run from the repository root in PowerShell 7. First load only the unchanged definition block under [Development command preparation](../../../README.md#development-command-preparation). It defines `Invoke-M105Command`, ordinary-path guards and exact environment restoration without installing or executing anything. Do not execute the adjacent npm-ci block unless a separate locked restoration is actually needed and authorized.

### M601-CMD-ENTRY — Read-only re-entry

```powershell
git status --short
git rev-parse HEAD
git diff --stat
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') {
  throw 'Reconcile active lease before M6-01 maintenance or execution'
}
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) {
    throw 'Pinned Node runtime unavailable'
  }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
}
```

The primary binds the observed intentional HEAD and exact dirty paths into G and each later lease packet. Clean and plan-only-dirty worktrees are both valid after inspection; no test requires this plan to remain untracked. Any changed binding field stops advancement until reconciled consistently.

### M601-CMD-FOCUSED — Planning/pure policy checks

```powershell
Invoke-M105Command {
  & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/retrieval-contract.test.ts tests/finding-sufficiency.test.ts tests/comparison-outcome.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'M6-01 focused contract checks failed' }
}
```

These checks neither launch Chromium nor contact embeddings/providers. They do not replace the closure suite or the frozen observation.

### G command slots — Must be resolved before the affected lease or execution

| Slot | Required binding and side effects |
| --- | --- |
| `M601-CMD-IDENTITY` | Current HEAD/dirty set; source/test/caller and frozen recursive hashes; retained-run and historical proof inventories; ordinary-path checks; runtime/lock identities. Read-only, content-safe output. |
| `M601-CMD-A` | Exact test paths/name filters, Node flags, per-test deadline, required existing build and scanner/UI scratch, expected exit/assertions and restoration. No inference. |
| `M601-CMD-NO-CALL` | Exact opt-in frozen package invocation, binding/negative-validation command, selected case, isolated run root if needed, loopback/browser ownership, fixed invocation/provider-effect counters, persisted/UI assertions, finite work/stop deadlines and cleanup order. No raw package copy to tracked tests. |
| `M601-CMD-RETRIEVAL` (conditional) | Only affected G1/G2/G3, caller source hash, exact new exclusive roots, fixed local runtime/model digest and fit configuration, maximum embedding requests, explicit operation allowance, failure/stop/retention rules. Never an old occupied root or generation call. |
| `M601-CMD-FULL` | Maintained README complete sequential suite, any accepted new suite, independent strict/build, no running app/concurrent test, capture flags absent, exact exit checks and evidence identity. |
| `M601-CMD-CLEANUP` | Owned processes and normal-stop evidence first; exact validated disposable child paths/inventory, exclusions, expected before/after effects and environment restoration. Unknown content or uncertain cleanup means preserve and report, not sweep. |
| `M601-CMD-DOCS` | Affected-authority/status/link/anchor/UTF-8/final-newline/whitespace/PowerShell checks and `git diff --check`; include explicit inspection of untracked new documents. |

Unresolved slots are deliberate gates, not executable placeholders or permission for workers to invent commands. Bind only slots actually needed; mark unused renewal/setup slots Not applicable with reasons. No dependency install, bootstrap, lock regeneration or general cleanup is planned. If prerequisites are absent, document the bounded recovery before any setup lease rather than improvising acquisition.

## Validation and Acceptance

Acceptance requires the complete evidence/frequency matrix with explicit provenance and limits; all task-required assertions verified; exact frozen no-call application/rendered outcome with zero provider effects; adequate authentic retrieval evidence distinguished from synthetic role-policy proof; retained native evidence and human work unchanged; truthful failure/cleanup behavior; current full regression and independent strict/build; different final review; and documentation closure.

Scanner/retrieval/review/comparison success cannot be inferred from a test count, worker report, guard receipt or suite exit alone. Inspect actual assertions and outputs. Do not award a semantic pass to G1, reinterpret non-failing native observations as general accessibility, or count a generation invocation where none occurred. Follow REQ-EVAL-005: material fixture/scanner/corpus/retrieval/adapter/comparison changes require affected new evidence without rewriting history.

## Idempotence and Recovery

Re-entry is read-only first. Never relaunch an actual case because a caller timed out or returned ambiguous output; inspect durable evidence and normal-stop state without treating absence of a response as absence of an effect. Attempt budgets and actual-call allowances are independent and never reset through a new session.

Default suite fixtures are disposable only under their existing exact ownership contracts. Reject occupied opt-in case roots. Stop owned processes before manual cleanup; keep developer Ollama running if it was already running. Remove only explicitly bound ordinary task-created child paths after checking resolved containment, ancestors, contents and relevant link constraints. Never delete `temp`, `data/runs`, browser/model roots, historical evidence or unknown files. Preserve uncertain residue and fail the affected cleanup claim. Retained private evidence stays ignored; documentation records only content-safe identity and conclusions.

A missing frozen private file is a blocker for its case, not permission to reconstruct prompts/guidance from memory. Follow the owning freeze recovery contract and owner authority. Missing runtime, drift, unavailable model or exhausted allowances stop only dependent work; finish safe unaffected checks. No automatic fallback or new model/provider.

## Artifacts and Notes

### M601-G-CONTRACT-01 — Execution binding

Current baseline is `799c0fe388f269c9a36275ef2a39399c16d6e524`; initial dirty set is empty. The primary's first authorized change is this living plan. Historical planning evidence remains historical. M5-04 is Complete, M6-01 is In progress, and the selected evaluation requirements are Accepted. No production responsibility or interface change is selected. Scope is the exact shared frequency matrix above.

Before completing the write/evaluation packet, `test_worker` may perform read-only A preflight: inspect source/tests, the frozen manifest and package structure without copying private payloads into tracked files, and return its independent behavior classification plus the smallest exact test seam. It may use Git inspection, ordinary file reads/searches and content-safe hashing. It may run only `M601-CMD-FOCUSED` using the unchanged README preparation definition block, Node v24.20.0 and PowerShell 7.6.5, repository cwd, compilation caching disabled/restored, no browser/inference/network/provider effects. No writes, lease, opt-in package execution, cleanup, capture flags or live calls are authorized in preflight. Default tests must remain independent of ignored private files.

`M601-CMD-IDENTITY` is read-only Git HEAD/status/diff plus SHA-256 of ordinary source/test/config, frozen references and protected evidence paths; inspect historical callers and their recorded runtime/configuration/assertions. Do not read or hash credentials. Resolve ordinary ancestors before traversal. Preserve all existing run directories, historical evidence and developer assets. Subsequent command slots remain gated until the exact preflight seam and preservation inventory are recorded and critically reviewed.

#### Accepted entry and preflight

Fresh entry strict TypeScript passes using the unchanged README preparation, PowerShell 7.6.5 and Node v24.20.0. No active lease exists. Scanner, UI and integration scratch directories are empty. The current three-file client build exists. All 13 generation and eight comparison recursive path/hash references pass. The read-only ordinary-path inventory is retained as ignored `temp/m601-entry.json`, SHA-256 `d18c324af632220c88cca1fbd97985c30629833701a6184a3e107a54766eb180`: 126 source files, 68 test/helper files, three build files, and 403 protected files across 26 exact roots, including all fourteen original `data/runs` children. The inventory contains paths/hashes only. It is a preservation baseline, not an evaluation result.

Persistent `test_worker` `/root/m601_tests`, assignment `M601-A-PREFLIGHT-01`, independently returns **EXISTING_BUT_UNCOVERED**. Its fresh M601-CMD-FOCUSED execution passes 162 tests, zero failures/skips/cancellations. Existing stage, service and UI tests cover synthetic incomplete guidance but no test binds the exact frozen package through all three boundaries. Primary accepts the characterization route; no Red, production Green or frontend-visual profile is selected.

#### Critical A contract acceptance

Fresh `/root/m601_contract_review` returns **PASS for A lease readiness**, no unresolved Blocker/Major/Minor, against plan SHA-256 `de7f92b96731b7a7bc44ef03731ca8c0432b279b826cf9658b0454c139210d92`. It independently verified all 600 entry endpoints, ordinary ancestry, frozen/lock/runtime identities, fourteen original runs and no active lease, and inspected the actual service/stage/UI seams. Primary corrected the protected-root count and named the maintained harness's real fixed output paths before acceptance. This accepts guarded synthetic characterization only; the new caller and conditional retrieval remain separately gated before opt-in/actual execution.

#### A assignment capsule and command freeze

Workflow `m601-shared-deterministic-evaluation`; task M6-01; work slice `A-frozen-abstention`; owner `/root/m601_tests`; initial assignment `M601-A-EVIDENCE-01`, phase `evidence`, attempt 1, correction parent None. Lease identity and digest are inserted after critical contract acceptance, immediately before dispatch. S3 trigger: frozen-input identity, zero-effect accounting and preservation/cleanup. Critical contract/caller review precedes the opt-in observation; a different final critical reviewer owns integrated review.

Allowed worker files are exactly `tests/m601-frozen-abstention.test.ts` and `tests/helpers/m601-frozen-abstention.ts`; no directory allowlist. Forbidden roots: `src`, `docs`, `evaluation`, `corpus`, `.codex`, `.agents`; forbidden files: `README.md`, `AGENTS.md`, `PLANS.md`, `package.json`, `package-lock.json`. No Git writes or metadata mutation. Only primary maintains this plan and documentation between leases. Production responsibility placement, interface changes and permitted structural refactor: **None — no application-source responsibility changes**. Test file owns assertions and execution; the optional helper owns only exact frozen-package authentication and construction of the test-only envelope. Reuse `completedScanRun`/`buildCheckpointSeed`, canonical citation resolution, existing retrieval fixture helpers, `startLocalService`, repository persistence, generation-stage/adapters and `startHarness`. Test dependencies point inward; no duplicated support algorithm or generic runner.

The default suite uses project-owned synthetic inputs and never reads ignored private packages. An explicit `A11Y_M601_FROZEN_NO_CALL=1` selects the exact frozen vector for one integrated case; invalid nonempty flag values fail closed. Authenticate `evaluation/m301-generation-v1.json` and its `shared.noCall` path/case/frequency/hash and exact package before creating any case root, service or browser. Missing, malformed, altered, wrong-case/path/hash and occupied-root inputs must fail, not skip or fall back. Binding negative tests use synthetic bytes/definitions; they never alter frozen files.

Frozen input is `temp/m301-generation-freeze-v1/no-call.json` with the recorded SHA-256, case `shared-incomplete-guidance`, exact native Finding and passage IDs `wcag22-sc111`, `understanding111-intent`. Resolve their canonical text/metadata from the frozen corpus. Any scores/result envelope are explicitly synthetic policy input, never actual embedding/ranking provenance. Keep all package-native facts unchanged in the selected Finding; unrelated run identity and siblings come from the maintained synthetic fixture. Test-side injection supplies only retrieval/scanner inputs, never terminal abstention output or expected support state.

Observable acceptance: one service-owned retrieval consumes that exact selected Finding and canonical incomplete guidance; the application computes `incomplete`, missing `remediation`, `incomplete-guidance`, its own explanation/manual guidance and terminal `abstained`. The repository's canonical `run.json`, validated service read and rendered UI must agree. Compare complete original scan/sibling content. Assert zero adapter preparation, dispatch/transport and invocation, no proposal or review decision, no Generate/review transition, selected evidence remains visible. Exercise the existing shared generation stage with the same incomplete input when needed to establish its pre-adapter zero-effect boundary separately from service orchestration; identify that as an additional policy assertion, not a second frozen retrieval. Render the actual service-produced outcome through the existing App harness with Analyze/Get guidance activation and named semantic assertions. Harness injection is a test bridge, not proof of real HTTP transport or actual retrieval.

Runtime and frequency: repository cwd and unchanged README preparation for every command; Node v24.20.0, managed Chromium 151.0.7922.34, pinned dependencies/lock, capture flags absent, no concurrent test or application service. Default characterization is development evidence. The explicit private case runs once after caller review; no automatic retry. Existing maintained suite supplies native cases at provider-independent frequency. No model, credential, public navigation, screenshot program or retained human decision in A.

`M601-CMD-A`: `Invoke-M105Command { & $m105Node --test --test-timeout=120000 tests/m601-frozen-abstention.test.ts; if ($LASTEXITCODE -ne 0) { throw 'M601 characterization failed' } } $m105UiTemp`, followed by the independent `tsc --project tsconfig.json` command. `A11Y_M601_FROZEN_NO_CALL` must be absent. The worker may run these after its lease opens; no frozen opt-in execution is delegated.

`M601-CMD-NO-CALL`: primary temporarily sets `A11Y_M601_FROZEN_NO_CALL=1`, runs the identical test command without a test-name filter, checks exit zero and structured result, then restores the exact prior environment value/readback in `finally`. The one integrated case selects synthetic or frozen input once; it must fail closed on missing opt-in inputs, never skip. Exact fixed exclusive root is `temp/m601-frozen-abstention`, with only a `runs` child for the selected synthetic aggregate and an optional content-safe `result.json` retained outside that child. Default characterization uses one exclusive `temp/m601-frozen-abstention-test-*` child; existing harness owns fixed `temp/m104-ui/m104-test-entry.html`, `temp/m104-ui/m104-test-entry.tsx`, `temp/m104-ui/vite-cache` and its Chromium temporary children under `temp/m104-ui`. No original run root is used. Per-test deadline is 120 seconds; browser action/navigation timeouts reuse the finite existing harness. Caller source SHA-256 and exact output inventory bind before this command runs.

The binding helper may expose byte authentication and the fixed opt-in loader; its exact fixed manifest/package pins remain non-injectable in the real loader. Synthetic characterization supplies maintained project-owned native/canonical guidance directly and labels it accordingly; it must not claim a successful fixed-byte authentication of invented synthetic bytes. Negative binding assertions use wrong/missing/malformed synthetic bytes and prove no service/browser/root side effects. The opt-in case establishes successful authentication of the real frozen bytes. No private package or full input payload is copied into tracked tests. Service integration calls `retrieveFinding` once, then `generateFinding` with `generationAdapterHarness` and expects `not-eligible` with all counters zero; this no-call refusal does not consume a generation request. No separate production correction is anticipated.

`M601-CMD-CLEANUP`: normal context/browser/harness/service stop precedes deletion. Reuse existing harness teardown. For a new case root, validate absolute containment under the exact accepted root, ordinary ancestors and descendants, and the known created file inventory before removing only that test-created root (default) or `runs` child (opt-in). Preserve unknown files, uncertain outcomes and residue; report failed cleanup without sweeping. Preserve the opt-in content-safe result if created. No removal of `temp`, `data/runs`, historical roots, build, model/browser assets or entry inventory. Compare all 403 protected endpoints and exact original run inventory afterward.

`M601-CMD-FULL`: execute the README's complete sequential forty-six-file verification fence with capture and frozen-case flags absent (the original forty-five plus the accepted A suite), once after the final test/source change. Fresh independent strict TypeScript and Vite `build --configLoader native` are mandatory. Before build, authenticate the existing ordinary three-file `dist/client` inventory from entry; Vite may replace only this generated output. Do not manually remove it. Reuse this full result across B/C and review while command, relevant source/test/config/build/runtime identities remain current. Save content-safe terminal output to exclusive `temp/m601-*.log` files only; these logs are diagnostics, not a second reporting system.

`M601-CMD-DOCS`: primary inspects all materially changed documentation, local Markdown paths/anchors outside fenced examples, strict UTF-8/no BOM/final newline/trailing whitespace, PowerShell fence parsing, current task/status/navigation consistency, untracked files and `git diff --check`. Recheck frozen hashes/protected inventories and absence of an active lease. Update the existing six documentation owners only as their responsibilities change, then archive/repair links only after verification and different final review pass.

Budgets: one accepted preflight plus its one read-only clarification; one initial characterization write, one ordinary correction and only a justified conditional third write under the unchanged chain; one review-correction loop within remaining phase budget. Two identical decisive failures without new evidence, two no-diff turns, unknown/changed binding, missing prerequisite, forbidden mutation or exhausted budget stops the worker for primary triage. No phase/agent/session rename resets a budget. Unmet required assertion blocks closure. At initial contract freeze, conditional renewal remained gated. The accepted history disposition and caller review below supersede that pending state; the first G1 failure is recorded separately.

### M601-A-CHARACTERIZATION-01 — Test boundary

Read-only preflight classified EXISTING_BUT_UNCOVERED with 162 passing focused checks. The initial `evidence` lease `M601-A-evidence-01`, digest `964df95df7fbe342c5412b9218f710569f9b7dac3f743b7cc2acc39b731cd7d8`, terminally closed compliant; receipt digest `16d4d885675ad521d220fe2a9dfe19b58c9f887c2518384fd9d083e18bfa9e95`. Only the two allowed test files were created. Primary inspected the complete files, cohesion and protected identities. No production fix or fabricated Red/Green was needed.

`tests/m601-frozen-abstention.test.ts` SHA-256 `6554e7695421d7986f4f398cdc94e0bdc4006fea6aded6d74bdc867a5e9d1a6e`; helper SHA-256 `5f12189f9c4b3e6874124da898cc80d9ffcb380fec08e7d426cb64d67225c0a4`. The default command passes two tests and strict TypeScript, with one synthetic guidance call, zero prepare/dispatch/transport, durable readback and rendered terminal abstention. Existing UI harness and service stop/port checks pass; synthetic root removed, UI scratch empty. At this initial characterization checkpoint the exact private case had not run. The package's explanatory prose is authenticated as input metadata; assertions require the current application's own explanation/manual guidance, exact expected reason and missing role, without injecting expected output into production.

Renewal caller review initially returned REVISE: its error path discarded already-retrieved passages and cleanup uncertainty. The corrected command retains available observations and bounded failing stage, preserves typed cleanup failure or unknown, and publishes one terminal record outside evaluation exception handling. Critical re-review precedes execution.

### M601-A-CORRECTION-01 — Bounded review correction

Critical caller review returned REVISE for two Major findings: successful teardown could delete a failed private observation, and preservation checks omitted complete scan/context/coverage/observations plus post-generation-refusal readback. The initial characterization remains passing development evidence, not accepted private-case proof. No opt-in observation has run.

Use the same workflow/task/work-slice, phase `evidence`, test_worker owner and exact allowed/forbidden path projection. Assignment `M601-A-EVIDENCE-02`, lease `M601-A-evidence-02`, attempt 2, correction parent `M601-A-evidence-01`. Change only the test's relevant assertions and cleanup condition; helper changes are unnecessary. Preserve the private run on any assertion or cleanup failure. Compare the complete actual aggregate to the seed after normalizing only permitted selected-Finding workflow fields, and compare disk and service readback again after `generateFinding` refuses with zero counters. Reuse existing helpers and normal teardown; no new framework or production changes. Run M601-CMD-A with opt-in absent and strict TypeScript under unchanged preparation, then stop. Primary closes/inspects and the same critical reviewer re-reviews this single correction loop before any frozen run. Attempt 2 consumes the ordinary correction; attempt 3 remains conditional, not automatic.

The full maintained command exited zero before the attempt-2 lease opened and before the corrected browser test ran. Its unchanged forty-five suites remain reusable only under the complete evidence-identity rule: same individual command/cwd/runtime and unchanged transitive source/test/config/build/input bytes. The changed A suite and strict TypeScript must be rerun after correction; no unrelated native case is repeated solely to reconstruct an aggregate test count.


Attempt 2 terminally closed compliant, receipt `168c1b6166d2d0f069aad4fbb97fcee97e03e22217ec93065ba8068fce79fe29`, lease digest `8558f74c9f2fe55dda3266b42032168b70ecc935217b0ea3462430a64c8f301b`. Primary inspected the sole changed test; helper unchanged. Revised test SHA-256 is `7ab110bec3bc448585ddb87ddcf8fb27a12cd1642a82951c402da84d59005635`. Fresh default characterization passes two tests, strict TypeScript passes, normal cleanup passes. Critical correction re-review returned PASS with both Majors resolved and no unresolved finding. The reviewer verified source hashes, parent/scope, terminal receipt and absent private root before the primary observation below.

### M601-G-HISTORY-01 — Historical identity disposition

Primary inspection and bounded read-only explorer `/root/m601_history` agree: the M3-05 receipts preserve the original three observations, but their core retrieval/caller source bytes cannot be reconciled with current source or reachable Git revisions. Their `6b6644ba65b6ccdfd7d7a86d95f9ce87a95e8c86` revision preceded the dirty implementation observed then; last-change history is insufficient. LF/CRLF and basic newline reconstruction do not reproduce the recorded `exact-retrieval.ts`, `retrieval-ranking.ts`, `retrieval-contract.ts` or caller identities. This is missing original identity, not proof of changed ranking semantics. Corpus, lock/configuration, seed builder, seed/query identities, all three run hashes and receipts remain intact. Preparation SHA-256 is `696ddaf05fb36aa73cd64bb7ee07d008b9fd0e8ad65835200819beed97fd7576`. Preserve G1's relevance failure and all historical meaning; do not reuse these observations as current execution acceptance.

The conditional renewal applies to G1/G2/G3, at most once each and three total. No original caller/root is replayed. Current actual retrieval acceptance will cover the retrieval engine, canonical citations, gold and support on the unchanged controlled inputs. Current service/durability/UI are independently exercised by the complete maintained suite and A; renewal alone is not fresh scanner or rendered end-to-end proof.

The M5-04 public W3C observation remains admissible as dated history. All 208 entry-protected source/config/frozen files match except the two authorized M5-04 test amendments; all three client build artifacts and recorded Chromium executable match. Current public caller SHA-256 `6b54971f17dfc476ac47dff7dcd0bfdedf9c3d58a4f81faa3b884fa8e06a775e` differs only by CRLF from accepted LF hash `77e4012a0530005438bed2693f0887d44c065afbe3af5164aff135f5fe51cb1b`. Both retained runs and result match the archived hashes, including `9a2e4f816f5c4deaad4e1e22f5b97a29f9cfc13528f576aaf27cf0bd85f0886d` for the result. This establishes the September 20, 2026 16:44:37–16:44:40 UTC authorized observation with 35 baseline Findings, selected-Finding rescan and persistent 3.88/4.5 contrast; it proves no current reachability or general support. No fresh public navigation is needed. M4-03 decisions remain dated context only; current review assertions use synthetic maintained tests.

### M601-CMD-RETRIEVAL — Conditional three-profile renewal contract

**Critical caller PASS accepted before execution.** Reviewed JavaScript body SHA-256 `2f548211d61354172bfe7acb816adc6ff20109ef6aa508ba1b4ac7b431b13d8c` (4,721 UTF-8 LF bytes, excluding fences and terminal newline). The shell adds only its stdin line terminator; no body changes are permitted. This is a direct primary-run evaluation command over existing production boundaries, not a new application helper, test framework or recovery mechanism. No new technology or consequential mechanism is selected. Execute one explicit case per command session, sequentially, after A's lease is terminal. Before each invocation verify unchanged HEAD/current source and existing-test hashes against M601-G-CONTRACT-01 (the two accepted added A files are additional, not replacements), frozen references, lock/runtime, all protected endpoints, no lease or service/test and absent exact case root. Use unchanged README preparation, no scratch/browser environment, and set/restore only `A11Y_M601_RETRIEVAL_CASE` to the exact selected `G1`, `G2` or `G3`. Pass the following frozen JavaScript fence unchanged on stdin to `Invoke-M105Command { $m601Caller | & $m105Node --input-type=module -; if ($LASTEXITCODE -ne 0) { throw 'M601 actual retrieval failed; no retry' } }`. Hash UTF-8 caller bytes before use and record the digest here. No automatic loop, retry or alternate target/model. A case is consumed when its one retrieval is entered, including failure.

Each invocation creates exactly one exclusive ordinary root `temp/m601-retrieval-g1`, `temp/m601-retrieval-g2` or `temp/m601-retrieval-g3`; only `input.json` and `result.json` may be created with `wx`. Preserve both on success/failure. Reject occupied roots; never remove or overwrite them. Authenticate original seed bytes through the entry inventory, compare the new seed's complete selected native Finding with that original, and bind current query and frozen gold. No actual scanner, browser, service listener, generation adapter, credential reader or human-decision path is imported. One fresh retrieval factory has a cold in-process collection; runtime model-loaded state is observed from its first existing `ps` request. Existing fixed Ollama transport owns cancellation, response limits and socket cleanup; no runtime start/stop/acquisition is permitted.

Fixed embedding identity remains `src/server/retrieval/embedding-profile.ts`: Ollama 0.33.3; embeddinggemma:latest manifest `85462619ee721b466c5927d109d4cb765861907d5417b9109caebc4e614679f1`, model `0800cbac9c2064dde519420e75e512a83cb360de3ad5df176185dc69652fc515`, BF16, 768 dimensions, context/batch 2048, fixed D/Q prefixes and no truncation. Existing actual-use validators enforce identity/fit. Cold collection has 16 corpus embeddings and one query embedding; at most 17 embed plus eight metadata requests, 25 total per profile, 51 embeddings/75 requests total. Increment/check counts before the underlying call. Existing per-request limits are 60 seconds for embed, ten seconds for metadata, one MiB response; production and caller have a 300-second abort. No claim of hard OS cleanup or capacity qualification. Failure preserves uncertain evidence, consumes the entered case and stops dependent continuation for primary triage.

```javascript
import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { execFileSync } from 'node:child_process';
import { buildCheckpointSeed, realCases } from './tests/helpers/m204-checkpoint-fixture.ts';
import { createExactRetrieval } from './src/server/retrieval/exact-retrieval.ts';
import { requestOllama } from './src/server/retrieval/ollama-http.ts';
import { resolveFindingCitations } from './src/server/retrieval/corpus-catalog.ts';
import { classifyGuidanceSupport } from './src/server/retrieval/support-policy.ts';
import { RetrievalError } from './src/server/retrieval/retrieval-error.ts';
const hash = bytes => crypto.createHash('sha256').update(bytes).digest('hex');
const caseId = process.env.A11Y_M601_RETRIEVAL_CASE;
assert.ok(['G1', 'G2', 'G3'].includes(caseId));
const repo = fs.realpathSync('.');
const ordinary = relative => {
  const full = path.resolve(repo, relative);
  assert.ok(full.startsWith(repo + path.sep));
  for (let current = full; current !== repo; current = path.dirname(current)) {
    const stat = fs.lstatSync(current);
    assert.ok(!stat.isSymbolicLink());
    assert.equal(fs.realpathSync(current).toLowerCase(), current.toLowerCase());
    assert.ok(stat.isDirectory() || (stat.isFile() && stat.nlink === 1));
  }
  return full;
};
const entryBytes = fs.readFileSync(ordinary('temp/m601-entry.json'));
assert.equal(hash(entryBytes), 'd18c324af632220c88cca1fbd97985c30629833701a6184a3e107a54766eb180');
const entry = JSON.parse(entryBytes);
const revision = execFileSync('git', ['rev-parse', 'HEAD'], { encoding: 'utf8' }).trim();
assert.equal(revision, entry.head);
const seedPath = `temp/m305-retrieval-role-proof/${caseId.toLowerCase()}/seed.json`;
const seedBytes = fs.readFileSync(ordinary(seedPath));
assert.equal(hash(seedBytes), entry.protected.find(row => row.path === seedPath).sha256);
const seed = buildCheckpointSeed(caseId, revision);
const native = seed.completed.scan.findings[0];
assert.deepEqual(native, JSON.parse(seedBytes).scan.findings[0]);
const gold = JSON.parse(fs.readFileSync(ordinary('evaluation/m201-corpus-v1.json')))
  .cases.find(row => row.profile === realCases[caseId].profile);
assert.ok(gold);
ordinary('temp');
const root = path.join(repo, 'temp', `m601-retrieval-${caseId.toLowerCase()}`);
assert.equal(fs.existsSync(root), false);
fs.mkdirSync(root);
ordinary(path.relative(repo, root));
const publish = (name, value) => fs.writeFileSync(path.join(root, name), JSON.stringify(value, null, 2) + '\n', { flag: 'wx' });
publish('input.json', { caseId, revision, native, query: seed.query, sourceEntrySha256: hash(entryBytes), collection: 'cold' });
const counts = { embed: 0, metadata: 0, total: 0 };
let initiallyLoaded = null;
const request = async (input, signal) => {
  const group = input.kind === 'embed' ? 'embed' : 'metadata';
  assert.ok(counts[group] < (group === 'embed' ? 17 : 8) && counts.total < 25);
  counts[group]++; counts.total++;
  const response = await requestOllama(input, signal);
  if (input.kind === 'ps' && initiallyLoaded === null && Array.isArray(response?.models))
    initiallyLoaded = response.models.some(model => model.name === 'embeddinggemma:latest');
  return response;
};
let stage = 'retrieval';
let result = null;
let citations = null;
let support = null;
let goldMatches = null;
let failure = null;
let cleanupFailed = null;
try {
  result = await createExactRetrieval({ request })(native, AbortSignal.timeout(300000));
  cleanupFailed = false;
  stage = 'citations';
  citations = await resolveFindingCitations(native, result);
  assert.ok(citations.ok);
  stage = 'support';
  support = classifyGuidanceSupport(native, result);
  assert.ok(support.ok);
  stage = 'request-count';
  assert.equal(counts.embed, 17);
  stage = 'gold';
  goldMatches = result.passages.filter(passage => gold.goldPassageIds.includes(passage.passageId)).map(passage => passage.passageId);
  stage = 'complete';
} catch (error) {
  failure = error instanceof RetrievalError ? error.code : 'evaluation-assertion';
  cleanupFailed = error instanceof RetrievalError ? error.cleanupFailed : cleanupFailed;
  process.exitCode = 1;
}
const terminal = { caseId, ok: failure === null, origin: 'fresh-actual-embedding', stage,
  counts, initiallyLoaded, result, support, goldMatches, citationsValid: citations?.ok ?? null,
  error: failure, cleanupFailed,
  limitation: 'Gold membership and role completeness do not establish semantic relevance. Entered allowance is consumed; no retry.' };
// Publish exactly once outside evaluation failure handling; publication failure remains distinct.
publish('result.json', terminal);
console.log(JSON.stringify(terminal));
```

### M601-B-NO-CALL-01 — Exact frozen abstention accepted

After critical caller PASS, primary executed M601-CMD-NO-CALL exactly once with the corrected pinned source. Both tests pass, exit zero. The integrated case authenticated frozen package `1b07d5d764ad99bb028db1aef767b9bb9d0685661b5ad761731d60f93394b6dd`, used its native Finding and criterion/interpretation passage IDs with explicitly synthetic scores, and observed one controlled retrieval. Actual service behavior persisted `incomplete`, missing `remediation`, reason `incomplete-guidance`, application-authored explanation/manual guidance, and providerCalled false. Adapter preparation, dispatch and transport counters were all zero; no invocation, proposal or review existed.

Complete aggregate normalization preserved all native/context/coverage/observation/sibling fields. Disk and service readback matched before and after the refused generation continuation; durable bytes were unchanged by refusal. The actual React detail rendered the same service-owned result and omitted generation/review controls. Browser requests stayed loopback, no page errors were reported, and harness teardown settled. Service stop and port closure passed; exact task-created `runs` was removed only after success. The empty `temp/m601-frozen-abstention/` marker remains to prevent replay. UI scratch is empty and the opt-in environment is restored absent.

Diagnostic log `temp/m601-frozen-abstention.log` SHA-256 `6e0a93061cc2ddd07f7278dc4ca8ac2e89866558a54f6d0b5318e2ac4e4478bf` binds the controlled provenance and source identities. This is successful frozen application/durability/rendered proof, not actual embedding or HTTP transport proof; the maintained API tests cover the latter synthetic boundary separately. No retained owner run was used.

### M601-B-MATRIX-01 — Separate shared observations

All current test observations below use the commands/identities in M601-C-REGRESSION-01 and its A-only renewal. Counts are development verification, never a product score.

| Case / origin | Evidence and observed outcome | Separate failure, limitation and rerun trigger |
| --- | --- | --- |
| Six frozen scanner states / fresh native controlled | `scan-page.test.ts` executes all six frozen states through actual Chromium/axe/capture; exact selected rules, all-node minimization, native incomplete buckets, same-target corrected passes and native contrast facts pass. | Controlled project-owned content only. Rerun affected cases on fixture/scanner/profile/capture changes. |
| Zero/failure/trusted input / synthetic boundaries and controlled browser | Run-contract, scan-normalization, local-service, scanner and walking-skeleton tests distinguish valid zero, incomplete coverage and failure; reject malformed/credentialed/non-HTTPS input before effects; fresh top-level-only contexts, finite work and cleanup pass. | Intercepted HTTPS is not a new public observation or hostile-target qualification. Rerun on boundary/lifecycle changes. |
| G1/G2/G3 / historical actual retrieval | M601-G-HISTORY-01 preserves dated role-selection observations: G1 `h67-ignored-image`, `wcag22-sc111`, `understanding111-decoration`, one gold match of three and unsuitable decorative interpretation/remediation; G2/G3 three gold matches each. All structurally supported. | G1 semantic relevance fails despite role completeness and canonical citations. Missing original executable identity prevents current acceptance. No ranking/corpus/gold tuning. |
| G1/G2/G3 / fresh actual embedding | M601-B-RETRIEVAL-02 records three completed cold-collection observations, canonical citations and complete roles: G1 one frozen gold match, G2/G3 three each. Each uses 17 embeddings; metadata counts are 8/7/7. | G1 semantic relevance still fails; G2/G3 guidance is relevant to the bounded label/contrast facts. Original metadata-only prerequisite failure is retained separately. Material corpus/query/model/ranking changes require affected new authorized evidence, never a rewritten result. |
| Shared frozen no-call / fresh controlled application + disk + browser | M601-B-NO-CALL-01 verifies exact frozen native/passages, missing remediation, terminal explanation/manual guidance, preserved complete aggregate and zero adapter effects. | Synthetic retrieval envelope and scores; no embedding-relevance claim. Consumed once, not per provider. Material input/application changes require affected new evidence under a separately bound case. |
| Support/failure distinctions / synthetic policy/service | Finding-sufficiency, retrieval-contract/service and generation-stage tests separately exercise evidence-incomplete, missing/incomplete/conflicting guidance, valid supported eligibility and retrieval execution/integrity failure. | Policy-only conflict evidence is not a persisted actual conflict retrieval. Rerun on sufficiency/corpus-validation/state-transition changes. |
| Approve/edit/reject / fresh synthetic validated proposals | Review contract/repository/service, finding-review UI and checkpoint tests verify support/judgment gates, exact edit/time, original preservation, immutable decisions and non-blocking reminder. | No retained human decisions were created; M4-03 decisions remain dated context. Mechanical gates do not prove human judgment. Rerun on review changes. |
| Comparison / fresh native pairs, synthetic uncertainty and policy vectors | Rescan-integration executes the three frozen directed same-target pairs with native pass capture and durable comparison; comparison suites cover persistent/missing/ambiguous/incompatible evidence. Policy-only 3.54 to 4.00 remains failing but improved; reversed binary pair supplies non-persisted regression. | No native provenance for constructed policy values, causality or whole-page claim. Rerun on fixtures, correlation/profile/ordering/publication changes. |
| Public W3C / dated historical actual | M5-04's identity-qualified isolated observation remains 35 baseline Findings and selected persistent 3.88/4.5 contrast with disk/GET/UI/normal cleanup evidence. | No fresh public navigation or current-reachability claim. New observation requires named-target finite authorization. |
| Durability/failure/cleanup / fresh synthetic and controlled integration | Repository/service/retrieval/review/comparison tests preserve evidence and siblings across restart, publication and downstream failures, report uncertain cleanup truth, and limit deletion to exact synthetic roots. A additionally verifies frozen readback and zero-effect refusal. | No universal crash/filesystem/OS-kill guarantee. All 403 protected endpoints and 14 original runs remain unchanged; rerun affected lifecycle/persistence checks on material changes. |

### M601-B-RETRIEVAL-01 — Prerequisite failure, no retry

The critically accepted renewal caller entered G1 once. It failed at `retrieval` with `missing-prerequisite` on its first `/api/version` metadata request: one metadata request, zero embeddings, no retrieved result/citations/support/gold observation, `initiallyLoaded:null`, `cleanupFailed:false`, exit 1. Read-only host inspection found no listener on local port 11434 and no `ollama` process. This is an unavailable developer-managed runtime prerequisite, not a relevance result or evidence of embedding success.

This was the initial stop, later resolved only by M601-G-REENTRY-01 and M601-B-RETRIEVAL-02. The exclusive `temp/m601-retrieval-g1/` retains `input.json` SHA-256 `b9c2d6e3aa4721b82fb69ac0cfe4a2cf027b11df8fef8f90797a094f4fc33eb2` and `result.json` SHA-256 `755464133d1dddb489f0f019cc7474a933925229228eafb62528e9d8e6f47f00`. No retry, acquisition or runtime start/stop occurred. G1 allowance is consumed; G2/G3 have not entered and their roots remain absent. Dependent actual retrieval is stopped while unaffected verification continues. Completing renewal requires the developer-managed runtime to be available and explicit owner authorization for one replacement G1 attempt with a newly reviewed exclusive root; the occupied root and failure must remain intact. The unused G2/G3 allowances do not authorize an extra G1 call.

### M601-C-REGRESSION-01 — Current maintained evidence

The complete maintained forty-six-file command exited zero with 883 tests, no failures, skips, cancellations or todo, under Node v24.20.0 / PowerShell 7.6.5 and Chromium 151.0.7922.34. Diagnostic log `temp/m601-full-suite.log` SHA-256 is `4f3e492b8dd34eaf83fc089f7bc9ebb727ebbfc9aa663c5ecfb4e427b7f7e60d`. Independent strict TypeScript and current Vite build pass. The three build files reproduce entry bytes exactly.

The later A-only correction supersedes that command's two A results with its fresh passing focused result and strict check. All 881 tests in the other forty-five suites retain the same individual commands, cwd, runtime, fixtures, config and transitive source/test/build identities; the new A files are not dependencies of those suites. This yields current evidence for all 883 maintained tests without repeating the native fixed cases. No production or existing test file changed. All 403 protected files, 126 source files, 68 original test/helper files and three build files still match entry. Fourteen original run directories remain intact. The maintained guidance UI suite also intentionally retained its synthetic screenshots in `temp/m203-ui-e10dfc9b-3773-4381-a4d6-a937a48b9637` (four policy images) and `temp/m203-ui-a4c42a16-6173-4c6c-8ac1-ee4f0da45886` (desktop/narrow images), as identified by that suite's log. These are preserved ordinary regression artifacts, not a new M6-03 visual review or disposable-root sweep.


### M601-G-REENTRY-01 — Owner-authorized runtime and replacement G1

After the explicit request for a replacement G1 in `temp/m601-retrieval-g1-02` with the same ceiling, the owner authorized running the needed Ollama runtime and continuing. This grants one startup of the existing retained installation and one replacement G1 attempt; it does not reset any earlier budget or change G2/G3's original one-each allowances. The first failure, its occupied root, frozen no-call observation and every historical artifact remain preserved. No acquisition, upgrade, new model, generation, credential read, public navigation or capacity rerun is authorized. The exact re-entry contract received critical PASS with no unresolved findings; separate startup permission review allowed the explicitly authorized action. Current status returned to In progress; final integrated review follows the renewed observations.

**Startup critical PASS accepted; executed once.** Reuse the existing developer procedure's `serve` only, with executable `C:/Users/developer/Tools/Ollama/v0.33.3/ollama.exe` (36,912,520 bytes; current SHA-256 `e4fe6bd835fe146659f5c969dccaff2e25a9de63d90ee204ca5d11b9034b0ca5`) and existing `C:/Users/developer/Models/Ollama` store. Fresh ordinary ancestor checks pass; executable is absent from PATH, so use its absolute path. No inherited OLLAMA variables were observed. Check hash/path and absent listener again immediately before startup. Launch one hidden developer-owned process, record returned PID/path, and observe at most 20 seconds for its exact listener on 127.0.0.1:11434. No HTTP readiness probe: each retrieval's existing metadata checks own that accounting. Stop dependent work if launch/ownership/readiness fails; no takeover, restart, process kill or cleanup deletion. Leave the server for the developer after this task. Runtime may use its ordinary external logs/cache/key state; never read keys or private logs. Two exclusive repository diagnostic outputs are retained without dumping them.

```powershell
$ErrorActionPreference = 'Stop'
$m601Exe = 'C:/Users/developer/Tools/Ollama/v0.33.3/ollama.exe'
if ((Get-FileHash -LiteralPath $m601Exe -Algorithm SHA256).Hash -ine 'e4fe6bd835fe146659f5c969dccaff2e25a9de63d90ee204ca5d11b9034b0ca5') { throw 'Runtime identity mismatch' }
if (@(Get-NetTCPConnection -LocalPort 11434 -State Listen -ErrorAction SilentlyContinue).Count) { throw 'Runtime port occupied; no takeover' }
if (Get-Process -Name ollama -ErrorAction SilentlyContinue) { throw 'Unexpected existing runtime; reconcile ownership' }
$m601Out = Join-Path (Get-Location) 'temp/m601-ollama-server.out.log'
$m601Err = Join-Path (Get-Location) 'temp/m601-ollama-server.err.log'
if ((Test-Path -LiteralPath $m601Out) -or (Test-Path -LiteralPath $m601Err)) { throw 'Occupied startup output' }
$m601Names = @('OLLAMA_HOST','OLLAMA_MODELS','OLLAMA_NO_CLOUD','OLLAMA_NOPRUNE')
$m601Saved = @{}
foreach ($name in $m601Names) { $m601Saved[$name] = [Environment]::GetEnvironmentVariable($name,'Process') }
try {
  $env:OLLAMA_HOST = '127.0.0.1:11434'
  $env:OLLAMA_MODELS = 'C:/Users/developer/Models/Ollama'
  $env:OLLAMA_NO_CLOUD = '1'
  $env:OLLAMA_NOPRUNE = '1'
  $m601Server = Start-Process -FilePath $m601Exe -ArgumentList 'serve' -WorkingDirectory 'C:/Users/developer/Tools/Ollama/v0.33.3' -WindowStyle Hidden -RedirectStandardOutput $m601Out -RedirectStandardError $m601Err -PassThru
  $m601Deadline = [DateTime]::UtcNow.AddSeconds(20)
  do {
    $m601Server.Refresh()
    if ($m601Server.HasExited) { throw 'Runtime exited; preserve diagnostics and do not retry' }
    $m601Listeners = @(Get-NetTCPConnection -LocalPort 11434 -State Listen -ErrorAction SilentlyContinue)
    if ($m601Listeners.Count) { break }
    Start-Sleep -Milliseconds 200
  } while ([DateTime]::UtcNow -lt $m601Deadline)
  if ($m601Listeners.Count -ne 1 -or $m601Listeners[0].OwningProcess -ne $m601Server.Id -or $m601Listeners[0].LocalAddress -ne '127.0.0.1') { throw 'Runtime listener ownership unavailable; no model call' }
  [pscustomobject]@{ event='m601-runtime-started'; pid=$m601Server.Id; executable=$m601Server.Path; endpoint='127.0.0.1:11434' } | ConvertTo-Json -Compress
} finally {
  foreach ($name in $m601Names) {
    [Environment]::SetEnvironmentVariable($name,$(if ($null -eq $m601Saved[$name]) { [NullString]::Value } else { $m601Saved[$name] }),'Process')
    if ([Environment]::GetEnvironmentVariable($name,'Process') -cne $m601Saved[$name]) { throw 'Runtime environment restore mismatch' }
  }
}
```

**Replacement G1 critical PASS accepted; executed once.** All original caller/request/input/cleanup gates remain unchanged. Derive the original reviewed JavaScript body by replacing its one exact line ``const root = path.join(repo, 'temp', `m601-retrieval-${caseId.toLowerCase()}`);`` with these two lines, and permit no other change:

```javascript
assert.equal(caseId, 'G1');
const root = path.join(repo, 'temp', 'm601-retrieval-g1-02');
```

The resulting body has SHA-256 `9eb529f47209c85d18d1a616f56f19a69cc609cad60469e40b1c6998ff9757d5` (4731 UTF-8 LF bytes excluding any shell terminal newline). Verify that digest and set only `A11Y_M601_RETRIEVAL_CASE=G1` using the original restore wrapper. At most one entered replacement, 17 embeddings/eight metadata/25 total requests, same fixed runtime/model/corpus/cold collection and finite deadlines. The original G1 result must still match its recorded hashes immediately before invocation. G2/G3 use the original reviewed caller and their original absent roots. Each new request count and quality dimension is recorded separately. No extra metadata request is added for startup or evidence reporting.


### M601-B-RETRIEVAL-02 — Three current actual observations

Primary executed the reviewed startup body SHA-256 `638ec47c6a4c15d6233673354fd9621de0551f00cf10234b8cf351683d25e8d9` once after permission review. Returned PID 15156 had the exact retained executable and its sole observed listener was 127.0.0.1:11434. Startup environment restored exactly; no HTTP readiness probe, pull, install, key read or process takeover occurred. Developer-owned runtime remains running. Exclusive `temp/m601-ollama-server.out.log` and `.err.log` are retained diagnostics; their evolving bytes are not frozen evaluation evidence and were not published.

The replacement G1 used its reviewed 4,731-byte body and new exclusive root; G2/G3 each used the original reviewed 4,721-byte body and original unused root. Before each, primary rechecked current HEAD, original source/test/build/protected identities, lock/frozen references, empty managed scratch and absent lease. Each operation exited zero with canonical citations valid, `support.state:supported`, no missing/conflicting role and `cleanupFailed:false`. Collections were cold per factory; the runtime model was initially unloaded for G1 and loaded for G2/G3. No result was treated as evidence of generation, full service/UI execution, capacity qualification or broad retrieval quality.

| Profile | Ordered passages and cosine scores | Gold / semantic relevance | Counts and retained identity |
| --- | --- | --- | --- |
| G1 informative image | `h67-ignored-image` 0.5111375808580503; `wcag22-sc111` 0.48338010884526506; `understanding111-decoration` 0.44174079489543194 | One of three is frozen gold (`wcag22-sc111`); direct criterion support passes the explicit minimum. **Relevance fails**: decorative-image interpretation and empty-alt remediation are unsuitable for this informative image. | 17 embeddings, 8 metadata, 25 total; initiallyLoaded false. `temp/m601-retrieval-g1-02/`: input `b9c2d6e3aa4721b82fb69ac0cfe4a2cf027b11df8fef8f90797a094f4fc33eb2`, result `d60d69ce27a10af13e1f4e46e5dd857611a62fb1850a4ab4ffabf9fb777cc225`. |
| G2 form input label | `understanding412-intent` 0.3886159586729352; `h44-explicit-label` 0.37651744701792195; `wcag22-sc412` 0.2940149620440013 | All three are frozen gold. Primary passage inspection confirms bounded relevance: programmatic name/role and explicit label association address the observed unlabeled input; this is not a claim about every naming mechanism. | 17 embeddings, 7 metadata, 24 total; initiallyLoaded true. `temp/m601-retrieval-g2/`: input `81e5c68dbec4a711d0c2f991c5f32672008c40bee6cd161065f9f28be7eca14c`, result `d72b0512f1b215beb3953e1f4e6e5ff828a49efba1b7ee6359e3756916be895b`. |
| G3 text contrast | `g18-contrast-remediation` 0.5096829257075763; `understanding143-threshold-measurement` 0.5016003654533153; `wcag22-sc143` 0.47792865673287177 | All three are frozen gold. Primary passage inspection confirms bounded relevance to normal-text contrast, unrounded threshold comparison and foreground/background remediation; no whole-page verdict follows. | 17 embeddings, 7 metadata, 24 total; initiallyLoaded true. `temp/m601-retrieval-g3/`: input `d43ba3814c488d27c5cca05c04105a26407a2dba55ef190e2e5d8da43e0ca547`, result `7a01f170a4e2692b3b7bd768adecba9f1ac2dd5e28d08e2b9a4b637d7ee00544`. |

All six files are retained ordinary local JSON; successful outputs are read back and separately interpreted. Across the original failed attempt plus three completions, observed totals are 51 embeddings and 23 metadata requests (74 total); these are operation accounting, not scores. Four retrieval entries occurred only because the owner explicitly added the one replacement G1 allowance after the first metadata-only failure. All original and replacement allowances are consumed. Corpus, gold, query, ranking, model/profile and preserved input bytes were not changed. G1's repeated semantic failure remains a documented limitation, not an unmet claim of universal relevance or permission to tune the evaluation.

### M601-FINAL-01 — Integrated review and documentation closure

Different fresh `critical_reviewer` `/root/m601_final_review` returned **PASS**, no unresolved Blocker, Major or Minor, against complete preclosure plan SHA-256 `9960e431f51b954c901b9c5cf5540c0409b499607855737749f08a315305a4ff`. It independently checked all 600 entry endpoints, exact 403-file/26-root protected inventory and fourteen original runs, all 21 frozen references, both terminal leases, seven fail-closed authentication inputs, actual retained native/query/citation/support/gold results, full-suite summaries, correction evidence, nine PowerShell fences and the complete implementation/documentation diff. It made no additional runtime request or actual-case replay.

Primary accepts the integrated evidence and its separate quality dimensions. G1's semantic relevance failure remains explicit; direct-support/citation minima and the rest of the bounded shared contract are verified. The original failed G1 metadata attempt remains alongside its explicitly authorized replacement. Current evidence covers all 883 maintained tests, strict TypeScript and current build, exact frozen abstention, current actual engine/citation observations and identity-qualified dated public history. No aggregate product score, provider ranking, release qualification or broad accessibility claim is made.

Documentation impact: Updated the root capability/status summary and maintained 46-file command, canonical roadmap, this evidence/coordination record, task progress narrative and mirrored status, and plan navigation. No product requirement, ADR, specification, workflow/model configuration, production file, package, frozen artifact or original run changed. Candidate validation passes for six documentation owners, 439 local links/anchors, nine PowerShell blocks, UTF-8/final-newline/trailing-whitespace and `git diff --check`; post-archive validation passes for all six documentation owners, 442 local links/anchors, nine PowerShell blocks, all sixteen required plan sections and unchanged protected/runtime-input identities. No obsolete active-plan link remains. The roadmap records 25 Complete and three Not started tasks. No active task is implicitly selected after M6-01.

The exact frozen case's disposable run was removed after successful stop/readback; its empty marker, two diagnostic test logs, four retrieval input/result pairs (including the failure), startup diagnostics and existing-suite synthetic screenshots remain ignored and preserved. All managed scratch is empty. Final read-only ownership verification confirms the same developer-owned Ollama PID 15156 and sole loopback listener; the server remains running at the owner's request. No commit, push or publication occurred.

### M601-ENTRY-01 — Planning evidence

On 2026-09-20, the worktree was clean at `97d1e9e135c242ee49c6f5ea18514ba7c32364f4`. Primary reviewed the authority/status route, M5-04 closure, M6 contract, evaluation amendments, relevant specifications/hard invariants, current development commands and protected artifacts. A bounded read-only explorer mapped current test/caller seams; it performed no writes or execution.

Fresh independent strict TypeScript passed. M601-CMD-FOCUSED passed **162 tests**, zero failures/skips. PowerShell 7.6.5 and Node v24.20.0 were observed. Both manifests' 13 and eight path/hash references matched; the frozen and lock hashes above matched; 14 retained run directories were observed. This does not replace execution-time inventories or establish current model/provider availability. No browser/model/public call, dependency mutation or deletion occurred.

### M601-PLAN-REVIEW-01 — Planning readiness and documentation

On 2026-09-20, a fresh independent critical reviewer returned **PASS for planning readiness**, with no unresolved Blocker, Major or Minor. Named triggers were frozen no-call identity/provenance, retained-data preservation and cleanup/recovery. Two initial draft findings were corrected and the complete revised artifact re-reviewed: the manifest property is `shared.noCall`, and behavior preflight uses the conditional `EXISTING_BUT_UNCOVERED` characterization route rather than non-behavioral setup. The reviewed plan SHA-256 was `f7c988a1933c08c476d037ce12bd2e145b69297253a4e37e690a612d01ec0f21`; subsequent changes only record this verdict and planning status, not execution bindings.

The reviewer independently inspected the six-document change, current HEAD, exact dirty set, absence of an active lease, 14 retained run directories, all 13 generation/eight comparison references and recorded hashes, existing proof seams and historical evidence. Both plan PowerShell blocks parse and diff whitespace passes. It ran no tests, browser, model or network operations; the 162 tests remain primary-observed planning evidence and 881 tests remain historical M5-04 evidence.

Primary documentation validation passes for six documents, 429 local links/anchors, eight PowerShell blocks, all sixteen required plan sections, UTF-8/final-newline/trailing-whitespace checks and `git diff --check`. Roadmap counts are 24 Complete, one In progress and three Not started. The exact changed scope is this plan and its progress record, both indexes, the roadmap and the root README status sentence; no source, tests, packages, frozen inputs, requirements or ADRs changed. Execution-time evidence reuse, exact frozen rendered proof and runtime availability remain unverified. This acceptance permits handoff of the plan, not evaluation execution.

## Interfaces and Dependencies

No production interface change is planned. Existing scanner/native normalization, canonical corpus/query/result validation, evidence sufficiency, shared generation no-call branch, local service, aggregate repository and React guidance/review/comparison regions remain their current owners. Test callers depend inward on those boundaries; production must not depend on test helpers or evaluation manifests.

Reuse the pinned toolchain, Playwright-managed Chromium and maintained test harnesses. Actual embeddings, if renewed, use the existing approved local EmbeddingGemma path, never a new service or library. No Groq access or Qwen generation is required. Test-side additions belong to the test worker; primary owns authoritative evidence, instructions, status and summaries. Any production correction needs a separate named code-worker Green lease and unchanged accepted tests.

## Revision Note

2026-09-20: Created for owner-selected M6-01 planning after completed M5-04. The plan inventories current evidence, scopes the missing frozen no-call binding, preserves the informative-image relevance failure, and leaves later generation/accessibility/reporting tasks unselected. Corrected the manifest property and characterization classification during independent review; planning readiness and documentation checks pass. This initial planning entry is historical; the current execution and blocker are recorded above.

2026-09-20: Recorded the owner's subsequent execution authorization and actual clean execution baseline; opened only the read-only G/A preflight route pending the exact reviewed effectful contract.

2026-09-20 execution update: Owner authorization superseded planning-only scope. Guarded characterization and one review correction, complete regression, current build/strict and exact frozen abstention pass. Actual G1 renewal fails before embeddings because the runtime is unavailable; its allowance is consumed and evidence retained. Completion and archival remain blocked; later tasks are unselected.

2026-09-20 re-entry update: The owner authorized existing-runtime startup and one replacement G1. Critical startup/root review passed; all three actual profiles completed within bounds with unchanged inputs and G1 relevance failure preserved. Integrated final review and documentation closure remain pending.

2026-09-20 closure: Different integrated critical review returned PASS. Primary accepted task verification and documentation closure, marked only M6-01 Complete, archived this plan and repaired navigation. Original prerequisite failure and G1 semantic relevance limitation remain preserved; no later task, commit or push is authorized by closure.
