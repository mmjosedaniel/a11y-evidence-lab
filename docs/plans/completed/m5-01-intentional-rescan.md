# Start an intentional later scan

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M5-01](../../DEVELOPMENT_ROADMAP.md#m5-01--start-an-intentional-later-scan) — Complete on 2026-09-16 after G, sequential A–E, independent reviews and documentation closure. Public-page scans, model calls, original retained-run mutations, commits, pushes and later tasks were not performed.
- **Latest evidence:** [Final acceptance](#m501-final-01--integrated-review-and-task-closure): all 776 tests in 37 files, controlled real integration, independent strict/build, eight visual samples and different final critical PASS.
- **Active lease:** None. All sixteen leases closed compliant. Consumed budgets remain historical and are not reset.
- **Next:** None within M5-01. M5-02 remains Not started and unselected.

## Progress

- [x] (2026-09-16 19:16Z) Bind execution to clean HEAD `0cbbf26bdf6facba647f14ece0580fef12bffb28`, no active lease, current prerequisite closures, fresh strict TypeScript and unchanged frozen inputs; M501-ENTRY-02. Dispatch the two bounded read-only G research dimensions; no implementation preflight yet.
- [x] (2026-09-16 15:23Z) Review roadmap-wide status, prerequisite closures, selected requirements, frontend reuse and backend seams. Independent strict TypeScript and 73 run-contract tests pass; frozen inputs unchanged, M501-ENTRY-01.
- [x] (2026-09-16 15:35Z) Accept independent critical planning PASS without findings and five-document validation; M501-PLAN-01. No implementation or target operation occurred.
- [x] Accept G's complete literal/command contract through corrected DRAFT READY, pre-draft PASS and different final artifact PASS; M501-G-ACCEPT-01. No implementation evidence is claimed.
- [x] A: implement and verify independent linked-run creation through the application service/API; M501-A-ACCEPT-01.
- [x] B: implement and verify the scoped transient scanner handoff without comparison; M501-B-ACCEPT-01.
- [x] C: implement and verify request-correlated client rescan admission; M501-C-ACCEPT-01.
- [x] D: implement and verify the accessible intentional-rescan interaction; M501-D-ACCEPT-01.
- [x] E: accept integrated real-browser/service/disk proof, full verification, different fresh final review and documentation closure; M501-FINAL-01.

## Surprises & Discoveries

- The previous capacity blocker has closed. [M3-03 final acceptance](m3-03-qwen-adapter-and-capacity-screen.md#m303-final-02--integrated-review-and-task-closure) records the sole full-stack observation and fresh strict/712-test verification. This is exact-configuration capacity evidence, not general model quality or support.
- `RunContext` has no `baselineRunId`; `readRun` and `tests/run-contract.test.ts` explicitly reject it. Three field-by-field preservation checks also need the new provenance: terminal scan identity, repository parent transition and selected-Finding transition. Merely adding an optional type would not preserve it.
- The scanner already validates/counts four native buckets, but captures DOM facts only for violations/incomplete and discards pass nodes after coverage validation. M5-01 needs a bounded pre-discard handoff, not a persisted positive-results collection.
- Rescan eligibility is independent of a Finding's downstream state. Existing proposal/retrieval eligibility checks must not be reused as rescan gates; the genuine shared operation lock and uncertain-save safety remain applicable.
- [REQ-A11Y-006](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md#200-zoom-verification-deferral--2026-09-15-utc) now defers every 200% zoom test until after MVP. Desktop/narrow, automated keyboard/focus/announcement and visual evidence remain required.

## Decision Log

- Decision: select only M5-01 planning after Complete M4-03. Rationale: the owner's exact request selects this dependency-ready task, not all M5. Date/author: 2026-09-16 / primary.
- Decision: reuse the existing scanner, repository and service reservation; add only task-owned provenance, admission and transient handoff. Rationale: no queue, cache, alternate writer or comparison engine is necessary to start one independent scan. Exact literals remain G decisions. Date/author: 2026-09-16 / primary.
- Decision: use controlled real-browser integration and existing frozen fixtures for this task's default proof. Rationale: M5-01 needs scanner/service/disk fidelity, not another actual public-page or model evaluation. New public targets or retained-run mutations need separate explicit authority. Date/author: 2026-09-16 / primary.

## Outcomes & Retrospective

M5-01 is Complete. The service creates independent baseline-linked runs, the scanner exposes only transient selected-rule candidates, and the client binds explicit rescan intent to truthful outcomes with accessible read-only baseline navigation. All 776 regression tests, controlled real integration, strict/build, bounded visuals and independent slice/final reviews pass. The review-discovered eligibility leak was corrected and independently reproduced as passing. Baseline evidence, frozen inputs and original retained runs remain preserved. Comparison and later roadmap work remain unimplemented and unselected; no provider/public-page, zoom, speech or broad-support claim follows.

## Purpose / Big Picture

From a Finding in the current completed Results view, the operator explicitly chooses a mode for a later scan of the same trusted page. The application creates a distinct run with `baselineRunId`, performs the complete provider-independent three-rule scan, and shows truthful new results or failure while preserving the baseline. The scanner makes selected-rule native non-failing candidates available only within the current operation for the later M5-02 consumer. M5-01 never matches a target, constructs a canonical positive observation or reports a comparison outcome.

## Context and Orientation

The [authority map and task router](../../README.md#authority-and-status-map), [project requirement semantics](../../PROJECT_REQUIREMENTS.md) and [roadmap authority-location key](../../DEVELOPMENT_ROADMAP.md#authority-location-key) control interpretation. At entry all twenty RD/M1–M4 tasks are Complete; eight M5/M6 tasks are Not started. M5-01 planning makes the split twenty Complete, one In progress and seven Not started.

| Controlling source | Selected obligation and task boundary |
| --- | --- |
| [Evidence and review workflow](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md), REQ-SCAN-005, REQ-EVID-008/010/011 | Minimize before persistence; keep all violation/incomplete records; locator is evidence, not identity; one versioned aggregate with optional baseline reference, no raw archives. Existing REQ-SCAN-006/007 coverage and source-preservation behavior remains unchanged. |
| [Rescan and comparison](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#rescan-and-comparison), REQ-COMP-004/006/007/008 | Any retained baseline Finding, no downstream prerequisite; preserve both source scans and original/human work. Pair gate, correlation, positive-observation construction and outcomes are M5-02/M5-03, not this task. |
| [ADR-0009](../../architecture/decisions/ADR-0009-axe-core-as-initial-accessibility-scanner.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) | Existing pinned exact-three-rule scanner and one safe-written aggregate. Native material is transient; baseline linkage is not retry lineage. Scanner remains accepted for evaluation, not release-qualified. |
| [Lifecycle: repeated analysis](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#repeated-analysis-provider-change-and-rescan), [ADR-0018](../../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md) | Independent run/mode, same trusted developer-supplied page, fresh context, no hidden provider activity, no resume. Validated service reads are retained; user-facing run reopening remains Deferred through OD-026. |
| [SPEC-006 and SPEC-007](../../specs/SPEC.feature) | Preserve source evidence, independent identifier and optional baseline linkage; never imply that later comparison examples already work. BHV-06/07 are derived through the evaluation authority. |
| [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md), REQ-A11Y-001/002/004/010 with 003/006/009 as applicable | Keyboard-operable explicit action, meaningful semantic status, selection/focus preservation, readable desktop/narrow controls and bounded visual proof. No 200% test or speech/support matrix. |
| [UI presentation](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md), [ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md) | Keep complete results and source distinctions. Add only rescan controls/status and minimum current-workflow coordination, not a new layout or history browser. |
| [Evaluation freeze](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#freeze-boundary), REQ-EVAL-004/005 | Preserve six fixture revisions and prior results; bind changed capture behavior to new verification. Material profile/input changes require their authoritative version/evidence route, never an edited historical result. |

Applicable Must semantics are Accepted and the explicit deferrals remain outside MVP. Ordinary endpoint, record and interaction literals may be resolved within this task; a significant architecture change or authority conflict requires owner direction before dependent work.

### Implementation entry baseline

`src/server/service.ts::runScan` reserves the operation, prepares/creates the running run, invokes its executor, validates terminal identity and finishes publication. `local-service/input-validation.ts` accepts only ordinary scan input; `loopback-api.ts` has POST `/api/runs`, but no rescan route. The repository already supports independent create/read/finish; do not add a second writer merely for rescan.

`src/server/scan/native-scan-capture.ts`, `normalize-scan.ts` and `scan-page.ts` own native capture, minimization and execution/cleanup. `src/server/scan/normalization/native-rule-evidence.ts::projectNativeNode` is the existing allowlisted projection boundary. Actual-node capture, report URL/time correlation, all-node collection and cleanup/deadline rejection are existing invariants, not optional rescan conveniences.

`src/client/App.tsx` coordinates one current run/selection and shared announcements. `AnalyzeForm.tsx` owns fresh mode input. `ResultsSection.tsx` and `ResultDetail.tsx` own complete results and selected evidence. Existing `run-admission.ts::admit` validates ordinary Analyze envelopes but does not bind a rescan result to its requested baseline. `main.tsx` owns same-origin transport composition. No current rescan control, request or admission exists.

## Scope and Non-Goals

Include independent later-run identity, immutable baseline reference, explicit fresh mode selection, service-owned baseline/Finding validation, source preservation, complete scanning, transient selected-rule candidate access, request-correlated browser admission, accessible rescan controls and real controlled integration.

Exclude all target matching, comparability classification, canonical positive observations, comparison results/persistence/UI, source-page remediation, page-level success claims, general pass/inapplicable storage, raw HTTP candidate payloads, caches/registries/queues, retry/resume, automatic provider switching, model operations/probes/acquisition, new dependencies, evaluation-manifest rewrites, user-facing retained-run loading/history and later roadmap tasks. Missing/withheld/ambiguous locators do not prohibit starting this scan; M5-02 owns their comparison meaning.

## Plan of Work

Use the [agent workflow](../../../.codex/README.md), [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md) and [write guard](../../../.codex/write-lease-guard.md). The primary owns authority, G's authoritative writes, assignment packets, integration, evidence acceptance, status and documentation. Research/review are read-only. Each implementation slice has a separate persistent `test_worker` and implementation owner, sequential preflight/Red/Green/optional Refactor and a fresh review. Primary documentation occurs between terminally closed leases; one active lease per worktree. No ordinary primary production edits.

Per unchanged writer role/phase chain: one preflight, initial write, one ordinary correction and at most one conditional final correction under the workflow's learning/new-evidence requirement; one review correction loop. Two repeated decisive gaps without material new evidence, two no-diff handoffs, exhausted budget or changed binding fields require primary triage. Agents and IDs never reset allowances. Expected missing behavior still requires preflight; existing-covered work reuses evidence, existing-uncovered behavior uses passing characterization, and no fake Red is allowed. The narrow first-module exception applies only when its exact missing export/environment/complete behavioral-test conditions are frozen.

The smallest proof uses current native fixture tests, repository/service tests and the existing built-client browser pattern. No standalone evaluation runner or evidence framework is planned. Freeze only missing tests/helper capability; prefer direct existing callers. No actual public scan, original-run write or model operation is part of default verification. Owned controlled fixtures and disposable roots remain isolated, with exact cleanup.

### G — Freeze rescan literals and command effects

**M501-G; R3; TDD: Not applicable.** Replacement evidence is authority/source inspection, a complete primary-written literal contract and required research checkpoints. No application-source responsibility changes or worker lease.

Resolve the following before implementation preflight; unresolved decision semantics cannot be deferred to a worker as runtime proof:

- Exact rescan input/service method/HTTP route and closed response/errors. Bind baseline run and selected Finding, with explicit new mode; derive page/rule from the validated retained baseline instead of trusting client-supplied copies. Resolve requested-versus-observed-final page choice against the lifecycle and trusted-input authority. The response must identify the newly created run without mutating or returning a substituted baseline as success.
- Optional `baselineRunId` shape, self-reference/invalid-ID rejection, historical run compatibility and immutable propagation through creation, completion, failure and every existing nested update. Cross-record existence/selection validation belongs in the service, not filesystem reads from the pure validator. Do not add a format migration unless a demonstrated compatibility conflict requires it.
- Single-operation reservation before asynchronous baseline admission; rejection of missing/malformed/noncompleted baseline or nonexistent Finding without navigation/new run; preserve baseline on every later failure. Distinguish an actual active/uncertain operation from a historical downstream Finding state, which is not an eligibility gate.
- Minimal transient candidate contract for only the selected baseline rule, sourced from actual native nodes before their references are removed. Resolve which native non-failing categories can supply candidates and what structural/provenance validation is necessary, without implementing semantic matching. Preserve all duplicate candidates and unavailable-locator reasons; do not select a winner, assign Finding IDs or let pass nodes enter violation collections.
- Explicit operation-local ownership/lifetime and future consumer boundary. Candidates become usable only with complete valid scan context and successful required cleanup; error/timeout/abort cannot expose usable comparison inputs. They never cross aggregate, HTTP, client, diagnostics or durable-evidence boundaries. A narrow scoped consumer or operation-local return is a candidate, not an accepted cache API. Until M5-02, production discards them when the operation ends; tests may inspect the handoff. There is no fake comparator/no-op success result or future recovery promise.
- Minimal client intent/admission, deadline and unknown-outcome handling. Capture baseline/Finding/new mode at submission, refuse stale/wrong-run/wrong-mode/wrong-baseline replies, preserve the baseline while pending or failed, and do not auto-retry or claim the service cancelled after a lost reply.
- Exact visible action placement, fresh mode control, pending/failure/completion semantics and focus/return behavior. Preserve only the current workflow's necessary baseline/selection context; no history list, deep link, reload restoration or saved-run chooser follows. Existing shared locks, including review-save uncertainty, must cover rescan without turning proposal status into a gate.
- Exact focused/full/browser commands and resource effects listed below, along with compatibility/freeze checks. Reuse installed runtime, existing fixtures and test mechanisms; resolve source-file paths before the lease.

### A — Create an independent linked run

**M501-A; standard; S3; TDD applicable.** One observable contract: a valid explicit service/API rescan request creates and publishes an independent baseline-linked run through the existing scanner lifecycle, preserving the baseline. Preflight targets `run-contract.test.ts`, `run-repository.test.ts`, `local-service.test.ts` and current API tests. `test_worker` owns the accepted test boundary; `code_worker` owns production Green. Candidate new focused test: `tests/rescan-service.test.ts`; reuse existing fixtures and safe repository sandbox.

Responsibility placement: extend `domain/run-contract/run-types.ts` and `run-validation.ts` for pure optional provenance; extend `local-service/scan-run-records.ts`, `persistence/run-repository/run-transition.ts` and `selected-finding-transition.ts` to preserve it. Reuse repository create/read/finish. A bounded new `local-service/rescan-operation.ts` owns baseline admission and rescan preparation. `service.ts` retains reservation/shutdown composition; if shared scan lifecycle needs extraction, use a purpose-owned `local-service/scan-operation.ts`, without copying it into a second path. `local-service/loopback-api.ts` dispatches to a bounded rescan request handler if substantive validation warrants separation. `main.ts`/production composition wires only the existing executor. Exact paths are frozen by G and the packet; no broad directory lease.

The dependency remains HTTP → service reservation/admission → validated repository/baseline → current scan executor → terminal identity/finish. No domain-to-filesystem, browser-to-repository or provider call edge. Permitted structural refactor is only cohesive extraction of the existing scan lifecycle required by its two immediate entry paths, with existing behavior protected.

Acceptance includes old ordinary runs, new linked running/completed/failed runs, immutable reference across all downstream transitions, mode independence, unprocessed and terminal downstream baseline Findings, missing/invalid baseline and selection, contention/reentry, duplicate creation, publication failure, shutdown and lost-response truth. S3 review accepts actual diff/cohesion, focused checks, independent strict checking and terminal leases before B.

### B — Expose only operation-local selected-rule candidates

**M501-B; standard; S3; TDD applicable.** One observable contract: the complete later scan supplies G's selected-rule transient candidates to the scoped internal boundary and discards them without changing durable collection semantics. `test_worker` owns Red in `tests/scan-normalization.test.ts`, `tests/scan-page.test.ts` and narrowly relevant helpers; separate `code_worker` implements Green.

Extend `src/server/scan/native-scan-capture.ts` to capture only the selected rule's necessary actual-reference non-failing facts, `src/server/scan/normalize-scan.ts`/`normalization/native-rule-evidence.ts` for validated bounded projection and `src/server/scan/scan-page.ts` for operation-local lifetime/cleanup. Create a purpose-named scanner candidate module only if its validation is a distinct current responsibility. Adjust A's executor/rescan wiring only to pass the selected rule and scoped result; keep ordinary scanner output and repository responsibilities unchanged. No generic callback framework, candidate registry, new endpoint or durable candidate field.

Prove ordinary scans do not retain candidates, only the selected rule is exposed on rescans, duplicates are preserved, malformed/partial/late report data cannot be usable, actual node capture does not re-query a replacement element, complete coverage is unchanged and raw material cannot escape any durable/wire/log path. Reuse all six frozen native states for affected capture fidelity; no new fixture revision or comparison policy. A test consumer observes the boundary without matching. Fresh S3 review and independent strict/focused checks gate C.

### C — Bind client requests to later-run responses

**M501-C; standard; S3; TDD applicable.** `test_worker` owns a focused `tests/rescan-admission.test.ts` and required transport witnesses; `code_worker` owns new purpose-named `src/client/rescan-admission.ts`/`rescan-request.ts` as needed. `main.tsx` remains transport composition. Existing `run-admission.ts` validation is reused where compatible; do not duplicate run validation or absorb rescan-specific request binding into unrelated review logic.

The new edge is current UI intent → same-origin rescan endpoint → exact response admission; no filesystem, credentials, raw candidates or provider access. Freeze all exact literals in G. Prove request/body/status correlation, fresh run ID, baseline identity and new mode; reject malformed/stale/mismatched replies, preserve known baseline and distinguish definite refusal from unknown publication. Prove no automatic retry/readback/cancellation claim. Focused tests, strict and fresh S3 integrity review gate D. No rendered UI change occurs in this slice.

### D — Present the intentional-rescan interaction

**M501-D; frontend-visual; S3 for shared ownership/unknown-state integration; TDD applicable.** `test_worker` owns `tests/intentional-rescan-ui.test.ts` and only necessary existing UI harness changes; `frontend_code_worker` owns Green. Primary accepts M501-UI-01 and its exact state/viewport capsule before preflight. The same critical reviewer covers behavior, cohesion and visual evidence; no second visual panel.

**M501-UI-01 reuse audit:** `ResultsSection.tsx`/`ResultDetail.tsx` EXTEND only composition to expose the selected Finding's rescan action, never for a ScannerReviewObservation. CREATE one named `IntentionalRescanForm.tsx` (exact location frozen by G) for new mode input, validation and explicit submit. `AnalyzeForm.tsx`'s native mode pattern is reusable; EXTRACT_LOCAL a shared mode control only if actual two-consumer duplication warrants it. Do not generalize unrelated form behavior. `App.tsx` EXTEND only necessary current-run/selection/reservation coordination using C's request owner; form fields and response-validation policy do not belong there. Existing Findings list, overview, native evidence, guidance, generation, review, styles and shared status region are REUSE_AS_IS except exact composition/status hooks needed by this action.

Required states: selected unprocessed Finding ready; a terminal downstream Finding also ready; no action for manual-review observations; missing new mode; in-flight rescan with baseline preserved; definite refusal/scan failure; unknown outcome with no misleading success/retry; complete nonzero and valid-zero later runs. Check active/uncertain review and competing Analyze/rescan mutations against the single-operation guard. Mode starts unselected for each new intent and cannot silently inherit baseline provider context. Do not prebuild comparison labels, score/delta views or a baseline history UI.

Automate keyboard activation, labels, validation, shared announcements, focus preservation and predictable return through the existing browser/axe harness. Freeze one desktop 1366×900 and narrow 390×844 visual path showing action, pending/error and later result; sample additional states only for a concrete risk. Observe actual visible focus; never claim screen-reader speech. All 200% testing stays Deferred. Preserve current source distinctions and non-certification language. Fresh critical visual/integration review, strict/build and accepted focused tests gate E.

### E — Integrate, verify and close

**M501-E; primary integration; TDD: Not applicable.** No application-source responsibility changes; replacement evidence is integrated execution and artifact/readback inspection. Exercise a controlled baseline scan and an explicitly selected later scan through the built UI, real service, scanner and disposable run store. Reuse existing frozen fixture interception at the test boundary; call it controlled real-scanner integration, not a fresh public-page observation. The baseline can be unprocessed; changing new mode proves no inherited provider state without invoking either provider.

Verify distinct run IDs, correct baseline reference, all three rules, real disk readback, unchanged baseline bytes, no candidate/raw material in persisted/HTTP results and no comparison output. Pair this with current negative/zero/failure tests and B's native capture proof; do not repeat a provider/state matrix. Exact proof commands and cleanup freeze in G, refined to accepted test artifacts before execution without changing their contract. Never replay M3/M4 consumed actual-call commands.

Run the complete maintained suite (entry: 32 files/712 historical tests) plus new accepted tests, independent strict TypeScript and client build. Audit actual diff and test relevance. Obtain a different fresh integrated `critical_reviewer` over I1–I10, current visual proof, complete evidence identity and cleanup. Reconcile README capabilities/commands, lifecycle and UI implementation descriptions when materially affected, roadmap, plan/progress indexes and task progress. Requirements/ADRs change only for an explicit accepted decision, not by implementation convenience. Pass the [documentation closure gate](../../README.md#task-closure-documentation-gate), then mark Complete/archive and repair links. M5-02 remains unselected.

## Decision Review Contract

**M501-G; R3.** Triggers: new persisted lineage identity, source-preserving admission/concurrency/recovery, transient native-content lifetime and wire/publication correlation. Target: primary-written literals and exact command bindings in this plan. Criteria: accepted semantics, minimal compatible change, immutable baseline, complete scanner fidelity, bounded transient data, honest failure, accessible intent and existing ownership. Product behavior and technology choices are fixed by authority; G selects compatible implementation details, not new comparison architecture.

Execution research baseline: clean HEAD `0cbbf26bdf6facba647f14ece0580fef12bffb28`, no active lease, checked on 2026-09-16. Freeze the bounded alternatives for comparison: existing service lifecycle with a dedicated rescan admission method versus a discriminated ordinary-scan admission extension; scoped post-cleanup internal candidate consumer versus an operation-local candidate return. Both must satisfy I1–I10; persistence, caches, queues, public candidate responses and deferred comparison are disqualified. The two critical researchers own lineage/service/client identity and native minimization/lifetime respectively. Their reports are read-only inputs; only the primary writes the contract after analyst DRAFT READY and the fresh pre-draft checkpoint. New architecture or an unresolved owner value choice stops dependent work. Source/authority inspection decides semantics now; tests and browser proof establish implementation later.

One bounded non-ranking discovery pass may identify the minimum reuse/scoped-handoff alternatives. Freeze candidate set and evidence dimensions before comparison. Budget two focused `critical_researcher` reports (lineage/service/client publication identity; native-candidate minimization/lifetime), each with at most one bounded follow-up; one mandatory `decision_analyst` plus one correction; fresh `critical_research_reviewer` pre-draft checkpoint and a different fresh final artifact reviewer. Primary drafts; no drafter or optional extra panel. One pre-draft correction and at most two final-artifact correction cycles within the recorded budget. Parallel read-only dimensions may overlap; synchronize before synthesis.

Research Assignment Capsules project this contract and I1–I10. Synthesis returns `DRAFT READY`, `RETURN FOR RESEARCH` or `OWNER DIRECTION`; DRAFT READY is not final acceptance. Freeze exact schema/API, lifetime and failure semantics now; implementation proves them later. Native test outcomes and runtime visual behavior are downstream proof, not excuses to defer identity/ownership decisions. Repeat the complete invariant packet after material revisions. Stop dependent work for exhausted allowances, repeated decisive gap twice, changed authority or an owner-controlled choice. Routine correction within remaining scope needs no extra permission; no ID resets a budget.

## Concrete Steps

Working directory: `C:/projects/a11y-evidence-lab`. Dot-source the first PowerShell block in the current [README command preparation](../../../README.md#development-command-preparation) in every new command session; it defines the existing runtime/environment wrapper without effects. Do not execute its dependency restore or archived acquisition commands merely to prepare a shell.

### M501-ENTRY-01 — Planning baseline

Clean HEAD `d72797e55883f58544af1c74da9f80d15e1afd75`, no active lease. On 2026-09-16 15:23 UTC, independent strict TypeScript and all 73 tests in `tests/run-contract.test.ts` passed with no failures/skips. This is fresh focused planning evidence, not a new full-suite, browser or rescan result. The latest historical closure records 712 tests across 32 files.

The M3-01 generation manifest remains `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`, all thirteen referenced file hashes pass, and the RD-003 scanner manifest remains `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459`. No input/result changed. Primary reviewed authority/UI boundaries; a bounded read-only explorer identified backend seams without execution or writes.

Repeatable focused planning check after README preparation:

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Resolve the active lease before proceeding' }
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Run contract checks failed' }
}
git diff --check
```

The entry commit is historical evidence, not a future hardcoded HEAD gate. At execution bind current intentional HEAD, dirty paths, relevant source/test/configuration/build hashes, environment/runtime, frozen inputs and exact commands. Committing this plan must not invalidate future leases by requiring an old parent or an uncommitted plan. Revalidate affected evidence on genuine drift.

### M501-G-CONTRACT-01 — Rescan implementation contract

This primary-written contract adopts the corrected DRAFT READY synthesis and pre-draft PASS. It is accepted by M501-G-ACCEPT-01, not implemented behavior. It supersedes the earlier candidate wording where more specific. Research source evidence is L1–L10 and NC1–NC7 in M501-G-RESEARCH-01; the two researchers independently inspected their bounded dimensions. Installed axe 4.13.0 semantics and the pinned primary API documentation support passes-only candidates. Runtime proof remains A–E.

The frozen alternatives were (a) dedicated rescan admission or extending ordinary scan input, and (b) a scoped callback or operation-local return. Both can meet authority and compatibility constraints. Select dedicated admission with one shared scan lifecycle: it isolates the baseline lookup without widening ordinary inputs. Select operation-local return: it avoids callback throw, asynchronous-consumer and reentry semantics with no current consumer. Neither choice authorizes comparison or a new architecture. Use one current later workflow and a read-only baseline preview because restoring an older active workflow would require additional capability-retirement/restoration policy.

#### Domain, service and HTTP

- Add `readonly baselineRunId?: string` to `src/server/domain/run-contract/run-types.ts::RunContext`. Retain format version 1. When present it must satisfy the existing `readId` grammar and differ from `runId`; explicit `undefined`, `null`, extra fields and invalid IDs fail. Historical absence remains valid. Propagate immutably through `scan-run-records.ts::matchTerminalRun`, repository `run-transition.ts` and `selected-finding-transition.ts`; adding, removing or changing linkage during any update fails. Pure validation performs no cross-record reads.
- Closed input is `{runId, baselineRunId, findingId, mode}` with existing ID grammar and mode exactly `local` or `groq`; new ID differs from baseline. The client allocates `run-${crypto.randomUUID()}` once for each valid explicit submission. Exact create-only repository collision returns `create-failed`; never generate a replacement ID, retry or replay. Ordinary Analyze retains server-generated IDs.
- Add `LocalService.rescanFinding(input: unknown, execute?: RescanExecutor): Promise<RescanOutcome>`. `RescanExecutor` is `(run: RunningRun, signal: AbortSignal, selectedRule: NativeRule) => Promise<unknown>` and returns a terminal run to the existing lifecycle. Reserve synchronously before input reflection, baseline read, collaborator call or await; recheck stopping/current operation after reentrant reflection. Reject real busy/admission-closed/uncertain operations, not historical retrieval/generation ownership or Finding state.
- Read and validate the completed baseline via the existing repository under that reservation. Select an actual member of `scan.findings` by `findingId`; a ScannerReviewObservation never qualifies. Missing baseline/Finding is `not-found`; noncompleted baseline is `not-eligible`; invalid stored data retains the existing bounded read errors. Derive rule and `requestedUrl` from the validated baseline, never supplied URL/rule or observed redirect destination. Use `prepareScanRequest` for fresh scan/provider context and current revision/time. Later Findings begin unprocessed; copy no retrieval, analysis, invocation, proposal or review. Any baseline Finding qualifies regardless of state or locator availability.
- Share the existing create/execute/terminal-validation/finish lifecycle in `local-service/scan-operation.ts`. `rescan-operation.ts` owns input/baseline admission and preparation; `service.ts` owns reservation, stop and composition. Preserve ordinary lifecycle behavior, safe writer and deadline suppression. No second repository writer or copied scan lifecycle.
- After `repository.finish` returns a validated completed rescan, before its reservation is released, invoke internal `retrieval.discardSettledOwner()` and `generation.discardSettledOwner()`. The first clears owner and supported snapshot; the second clears owner. Clear all prior settled owners, even from older runs. These synchronous private methods perform no callback, await, I/O or durable mutation and change no reservation/controller/admission/shutdown/deadline/cleanup flag. Refusal, creation, scanning and completed-publication failure preserve them. Ordinary Analyze is unchanged. Durable success retires them even if its reply is lost.
- POST `/api/rescans` is available through the built-client service only; API-only construction retains 405 behavior. Exact URL, `application/json`, fatal UTF-8, closed input, declared/actual body length at most 4096 bytes and a 30000 ms monotonic receipt deadline follow the existing bounded API pattern. Reserve one dispatch before entering service. Disconnect after dispatch proves neither cancellation nor noncommit. Health shape is unchanged.
- Success is HTTP 200 with exactly `{ok:true,run:CompletedRun}` after durable publication. Failure is exactly `{ok:false,error,run:FailedRun|null,persisted:boolean,cleanupFailed:boolean}`. Status mapping: 400 `invalid-request`; 404 `not-found`; 409 `busy`/`not-eligible`; 503 `stopping`/`shutdown`; 500 `invalid-run`/`stored-run-unavailable`/`read-failed`/`create-failed`/`scan-failed`/`result-validation`/`initial-persistence`. Precreation failures have null run and false persisted; they never substitute the baseline. Only creation cleanup failure may report cleanup uncertainty without a run. Postcreation failures retain truthful failed later-run identity, persistence and cleanup. Unexpected postdispatch rejection or malformed output is HTTP 500 with exactly `{ok:false,error:'rescan-outcome-unknown'}`. No raw errors or candidates cross this boundary.

#### Scanner-private candidate handoff

`NativeCandidate` is the discriminated union, per Finding rule, of its existing `ruleId`, `checks`, `evidence`, `locator`, and `nativeResult:'pass'`: exactly five keys. Reuse current fact/check allowlists. Add no identity, state, outcome, prose, HTML, native selector, arbitrary attributes, raw check data or duplicate provenance. Preserve every selected-rule pass in native order, including duplicate locators and all existing unavailable reasons. Deep-freeze detached projected values. Incomplete and inapplicable never produce candidates; synthetic nonzero-inapplicable coverage compatibility remains unchanged. Empty candidates assert only absence of selected-rule pass nodes in this complete operation.

`captureNativeScan(page, selectedRule?)` captures existing violation/incomplete facts plus passes for the selected rule via actual connected same-document element references before their deletion. Validate the rule before browser acquisition. Pass selection separately to reporter registration; do not change the exact three-rule/all-bucket axe options. Never replace missing references by resolving a native target. Missing capture needed for projection is `evidence-capture`; malformed individual facts remain bounded unavailable values.

Keep `normalizeNativeScan(input)` unchanged. Its overload with `selectedRule: NativeRule` returns success `{ok:true,value,candidates}` where value is the ordinary collection; failure stays the existing closed failure branch. Validate the whole report, including unselected buckets, nodes, rule coverage, fatal errors and options before handing off anything. Candidate type/projection belongs in existing `normalization/native-rule-evidence.ts`; no additional scanner module is needed.

Keep `executeScan(run,signal): Promise<TerminalRun>`. Add `executeRescanScan(run,signal,selectedRule)` returning `{run:CompletedRun,candidates:readonly NativeCandidate[]}` or `{run:FailedRun}` through one private executor. Candidates become usable only after paired report URL/time and complete context validation, complete three-rule coverage, no timeout/abort, all work settled, successful page/context/browser closure, empty scanner scratch and validated completed terminal run. Existing 10000 ms work and 4000 ms cleanup budgets remain; failure/late settlement cannot expose a candidate-bearing envelope. Clear operation-held candidate references on failure/exit. Completion here is scanner completion, not durable publication.

The production rescan adapter immediately unwraps `.run` and discards the envelope. Candidates have no domain, repository, service outcome, HTTP, client, diagnostic, provider or retained-closure/map edge. No M5-01 consumer, cache or recovery promise exists. Tests may inspect this private boundary without matching. JavaScript memory erasure is not claimed.

#### Client ownership and visible interaction

`src/client/rescan-admission.ts` exports closed `RescanIntent` with the four input fields and `admitRescan(raw:unknown,before:unknown,intent:RescanIntent):RescanOutcome|null`. Reuse descriptor-safe snapshot/run validation. Bind exact transport status/body, submitted new ID, baseline ID, baseline requested URL and fresh mode/provider/model. Require completed later Findings to be unprocessed. Reject stale, malformed, wrong-baseline, wrong-ID and wrong-mode replies, including unseen other runs for the same baseline/mode.

`rescan-request.ts` owns `createRescanRequest({baseline,intent,current,readCallback,settle})`, returning synchronous `{start,stop}` like the existing review request. Install stop before caller reflection, snapshot captured intent/baseline, then recheck current token/mount/baseline after every reflective boundary. One dispatch and a 30000 ms monotonic deadline; no retry/readback. Presentation is pending, completed with validated run, refused with bounded error/cleanup/released and optional failed-run/persisted evidence, or unknown. Proven local no-call refusal releases. Valid precreation refusals except busy/stopping release only with null run, false persisted and false cleanup. `scan-failed`, `result-validation` and `initial-persistence` may release only with the exactly correlated failed later run, closed cleanup and false cleanupFailed; false persisted remains visibly unsaved. Busy, stopping, shutdown, cleanup uncertainty, malformed/rejected/lost/late response and unknown outcome retain the all-mutation lock. Aborting transport never claims service cancellation.

`rescan-transport.ts::postRescan(intent:RescanIntent,signal:AbortSignal):Promise<unknown>` makes exactly one same-origin JSON POST to `/api/rescans`, returning `{status,body}`. D wires it through `main.tsx` and an App callback; C adds no unused rendered interface. No credentials, arbitrary URL, provider or filesystem access.

Create `components/results/IntentionalRescanForm.tsx` for selected Finding only. Label fresh unselected mode `New scan mode`; action `Start intentional rescan`. Explicit same mode is allowed. Missing mode focuses validation; no inherited selection. Keep baseline evidence/selection/views during pending, refusal and unknown. Shared status announces state; unknown text is exactly `Rescan outcome unknown. The service may have created a later run. Further actions are blocked.` Actual active operations block competing mutations. Existing review uncertainty and new rescan uncertainty block all mutations. Generation uncertainty prevents rescan, guidance and review while preserving the existing explicit independent Analyze behavior; this task does not broaden that existing lock. Historical settled ownerKnown alone is not a rescan gate. Imperative callbacks enforce the applicable locks. See M501-G-COMPAT-01 for this scope-preserving clarification.

Exact admitted success clears old continuation, retainedOwner and ownerKnown unconditionally, including empty generation presentations, and clears prior action/review form/focus ownership. New later results may validly contain zero Findings; ordinary downstream actions are available. Focus the later Results heading only when the outgoing Results region held focus. Retain one immediate baseline record, saved selection and read-only presentation snapshot for scanner, guidance, proposal, invocation and saved human work. The later run remains the active workflow throughout preview; never change its held ref, request tokens, continuations, owners or selection merely to navigate.

`Return to baseline` shows `Baseline evidence — read-only. Return to later results to continue.` Restore saved baseline selection on each entry; subsequent selection changes affect only preview selection. `Return to later results` reveals the same later workflow including intervening validated updates. Both navigation actions focus the displayed Results heading after rendering and use the shared announcement. Neither performs a request, publication, cancellation or capability reconstruction. Preserve actual/unknown locks. Omit baseline guidance/generation/review/rescan mutation controls, block stale callbacks imperatively and never consume active ReviewDecision focus callbacks. Narrow readOnly rendering through Results/Detail/Guidance/Generation preserves historical failure/unsaved/provider evidence while suppressing present-tense reserved-workflow claims. Ordinary rendering defaults remain unchanged. Successful Analyze clears this one pair; the next successful rescan replaces it. No history list, chain, reopening or reload restoration.

#### Exact paths, commands and effects

All paths below are repository-relative. Each packet freezes the listed relevant subset; additional paths require primary reconciliation before a lease. A production scope: `src/server/domain/run-contract/run-types.ts`, `run-validation.ts` in that directory; `src/server/local-service/contracts.ts`, `scan-run-records.ts`, `rescan-operation.ts`, `scan-operation.ts`, `rescan-api.ts`, `loopback-api.ts`, `retrieval-operation.ts`, `generation-operation.ts`; `src/server/service.ts`; `src/server/persistence/run-repository/run-transition.ts` and `selected-finding-transition.ts`. Reuse input validation/scan preparation without changes unless preflight demonstrates a binding need. A tests: `tests/run-contract.test.ts`, `run-repository.test.ts`, `local-service.test.ts`, `rescan-service.test.ts`, `rescan-api.test.ts`. A test-only `tests/helpers/m501-rescan-fixture.ts` is permitted only after primary acceptance of preflight establishes necessary reuse and before the exact lease.

B production: `src/server/scan/native-scan-capture.ts`, `normalize-scan.ts`, `normalization/native-rule-evidence.ts`, `scan-page.ts`, and A's existing rescan adapter in `src/server/service.ts` or `local-service/rescan-operation.ts` as its settled composition requires. Tests: `tests/scan-normalization.test.ts`, `scan-page.test.ts`. No frozen fixture edits. C production: `src/client/rescan-admission.ts`, `rescan-request.ts`, `rescan-transport.ts`; test `tests/rescan-admission.test.ts`. D production: `src/client/App.tsx`, `main.tsx`, `styles.css`, `components/results/ResultsSection.tsx`, `ResultDetail.tsx`, `IntentionalRescanForm.tsx`, `FindingGuidance.tsx`, `FindingGeneration.tsx`. D tests: `tests/intentional-rescan-ui.test.ts`, `tests/rescan-integration.test.ts`, existing `tests/helpers/m104-ui-harness.ts` and bounded `tests/helpers/m501-rescan-browser.ts` only as preflight establishes; reuse the existing walking-skeleton harness. E writes no application source.

In every shell dot-source only the first README preparation block, require it to contain `function Invoke-M105Command`, and preserve its variables/wrapper. No dependency restore or browser acquisition. Working directory is the repository root; Node 24.20.0, Python 3.12.10, package-lock and installed Chromium revision 1234 are entry identities. Every native command checks `$LASTEXITCODE` and throws before dependent work on nonzero. Focused command template inside `Invoke-M105Command` is `& $m105Node --test --test-timeout=120000 <exact-test-file>` followed by exit check. Run files sequentially. A runs its five test paths, with `--experimental-test-module-mocks` before `--test` for `rescan-api.test.ts`. B runs normalization then scan-page; C runs rescan-admission with that mock flag. D runs intentional-rescan-ui with UI scratch after build; E runs rescan-integration with scanner scratch. A/C ordinary tests require no browser. Existing focused dependency tests may be added to packets only for changed behavior or unresolved risk.

Strict is separately `Invoke-M105Command { & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json; if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' } }`. Build uses `& $m105Node node_modules/vite/bin/vite.js build --configLoader native` with exit check inside the same wrapper. Entry inspected the existing ordinary `dist/client` inventory/hashes; D/E may replace only that generated output through Vite after rechecking ordinary ancestry. No backup framework or source generation. At closure run the exact complete sequential README suite inventory and flags plus all accepted new tests, strict and build; historical counts are not fixed expectations. Update maintained commands only at documentation closure after complete target reads.

Browser/scanner commands use only the existing installed browser and exact empty `temp/m103-scan` scanner scratch; UI commands use `temp/m104-ui`. Integration uses `temp/m105-integration/m501-rescan` for exclusive disposable runs and the existing `m105-walking-skeleton-harness.ts` controlled HTTPS interception/real scanner; the helper may switch UI launch to UI scratch using its existing restoration pattern. No public navigation/provider call. Read back both validated records and baseline before/after bytes; no candidates/raw material/comparison fields in JSON, HTTP or client. Existing six frozen fixture tests establish unchanged capture/coverage input behavior.

Optional synthetic application visual proof uses `A11Y_M501_CAPTURE_PROOF=1` only while running `tests/intentional-rescan-ui.test.ts`; preserve/restore the prior environment in finally and require absent capture flags in ordinary regression. Create only missing ordinary `temp/m501-rescan-proof`. Create-only local PNGs are exactly `{desktop,narrow}-{ready,pending,error,complete}.png`, with desktop 1366x900 and narrow 390x844. Record source/build/browser identity, inspect all eight and retain them locally. They are application screenshots, not scanned-page screenshots. Keyboard, labels, visible focus, shared announcements and axe remain behavioral checks. No 200% or speech evidence. Re-capture only after validating/removing exact owned earlier artifacts whose evidence became stale.

Before any preparation/removal, resolve absolute paths and reject unexpected ancestors, reparse points, aliases, nonordinary files and hardlinks where applicable using existing helpers. Close only owned service/listener/browser/context handles before validated exact-child cleanup. Preserve uncertain residue. Never remove scratch parents wholesale, original runs, unrelated evidence or Ollama. Full suite may create its existing exclusive demo roots under its unchanged helpers; no original retained run is mutated. Guard records remain under ignored `logs/agent-flow-leases/v2`; every write turn has a primary-created/verified/closed digest-pinned lease. Primary docs occur between leases, tests are protected in Green, and Git writes remain forbidden.

#### Decided invariants and remaining proof

I1 scope/prerequisite/freeze accepted; I2 optional immutable lineage decided; I3 downstream-independent admission plus genuine reservation decided; I4 fresh independent run and success-only retirement decided; I5 passes-only actual-reference ordered candidates decided; I6 whole-validation/post-cleanup transient gate decided; I7 failure/unknown/retirement boundary decided; I8 exact correlation/release matrix and active-versus-preview separation decided; I9 explicit mode, announcements, focus and read-only return decided; I10 exact owned checks/resources/barriers decided. None of these dispositions claims implementation evidence.

In addition to I1–I10's table, A/C/D/E must prove supported retrieval and known settled unsaved generation can precede successful rescan, later retrieval becomes available and old generation is refused; all refusal/create/scan/publication failures preserve owners/baseline; shutdown/cleanup/late locks remain; durable success with lost reply retires server capability but leaves client unknown/locked; empty generation presentations do not prevent client owner reset; preview navigation performs no service operation, restores no capability and suppresses misleading ownership text. B proves six unchanged native states, selected passes/duplicates/unavailable locators, replacement-reference fidelity, malformed unselected data, abort/timeout/cleanup/late denial and structural/canary nonleakage without dumping native payloads.

The supported pre-draft correction passed I1–I10 with no Blocker/Major/Minor. The different final artifact review passed in M501-G-ACCEPT-01; fresh S3 reviews apply at A/B/C/D and different integrated critical review at E. Lineage follow-up, analyst correction and pre-draft correction are consumed, native follow-up unused. M501-G-COMPAT-01 consumed the first final-artifact correction; at most one remains. No correction allowance or evidence identity is reset by this contract.

| Slot | Required exact binding before the applicable preflight/lease/effect |
| --- | --- |
| A preflight/Red/Green | Current contract/repository/service/API subsets and new rescan service cases; strict command, expected behavioral failure, fixture roots, native exit propagation and allowed/forbidden files. |
| B preflight/Red/Green | Native normalization/capture tests, all six existing fixture states, selected-rule candidate and failure witnesses, installed browser identity, scratch/environment and no raw output capture. |
| C preflight/Red/Green | Exact client request/admission cases, response fixtures, bounded local transport deadline and no-call/retry counters; no actual target operation. |
| D preflight/Red/Green | Exact UI tests/helpers, source/build identity, desktop/narrow states, keyboard/axe/announcement checks, local screenshots and ownership. |
| Integration/full regression | Maintained suite inventory plus new accepted tests, current test flags, sequential browser/service ordering, build preparation/output effects, controlled input/interception identity and validated run readback. No public network/model call. |
| Resource preparation/cleanup | Exclusive task-owned scratch/run/evidence roots, baseline before/after hashes, create-only fixtures, owned ports/processes/contexts, generated build outputs, allowed retention and exact-path cleanup with ancestor/alias checks. Never clean retained user runs, other task evidence or developer-owned Ollama. |
| Guard/documentation | Exact packet projection, protected tests, fresh lease/attempt lineage and terminal calls; command parsing, UTF-8/newline/whitespace, links/status consistency and `git diff --check`. |

Future filenames above are bounded candidates, not commands assumed to exist. G binds current callers; each packet freezes the resulting exact test/source path set before writes. A missing prerequisite or changed command/effect returns to primary reconciliation; no worker invents binding scope after review. Test-only helpers remain with test_worker; primary owns this plan, developer instructions and all authoritative/status documents.

## Validation and Acceptance

The table defines the required invariant coverage. G and A–D acceptance and current E records below supply actual fixture, command, tree/environment identity, expected versus observed outcome and review disposition; historical planning statements do not replace those records.

| ID | Fixture/trigger and expected result | Evidence/review owner |
| --- | --- | --- |
| I1 | Authority and frozen-input check: only M5-01; M4-03 prerequisite Complete; no evaluation rewrite, later outcome or support claim. | Primary/G/E |
| I2 | Ordinary and linked running/completed/failed records: closed shape, valid non-self baseline reference, historic ordinary records readable; all parent/nested updates preserve linkage. | A tests; fresh critical A/E |
| I3 | Missing/noncompleted/malformed baseline, observation/nonexistent Finding, valid Finding in downstream states: service validates selection under reservation; accepted scan does not depend on proposal/review or locator availability. | A service/API; critical A/E |
| I4 | Distinct new run/new explicit mode, both modes under identical scan fixture: correct linkage and unchanged baseline; no inherited downstream state or model call. | A/C/D/E; critical reviewers |
| I5 | Six native fixture states, selected-rule non-failing candidates, duplicate/unavailable facts: actual-reference provenance, complete coverage, no selected target matching or canonical positive observation. | B tests; critical B/E |
| I6 | Malformed/partial/stale report, timeout and cleanup failure: no usable candidate handoff; ordinary scans have no retained candidates; no raw data in aggregate, wire, logs or provider payloads. | B negative/structural proof; critical B/E |
| I7 | Busy/reentry, creation/write/scan/shutdown failures and lost reply: baseline unchanged, last valid later record remains truthful, no automatic retry/resume or invented completion. | A/C and existing risk tests; critical A/C/E |
| I8 | Wrong baseline/run/mode/stale response and selection changes: client rejects substitution, binds submitted intent, preserves baseline and correct mutation lock without false cancellation. | C/D tests; critical C/D/E |
| I9 | Keyboard/mode/error/pending/completed/zero path: semantic explicit rescan, shared announcements, visible focus and predictable return; bounded desktop/narrow proof and no comparison UI. | D browser/visual; critical D/E |
| I10 | Built UI → real controlled scan/service → disposable disk: both runs validate, baseline bytes preserved, no comparison/candidate leakage; full current suite, strict/build, actual diff/cohesion, compliant leases and documentation closure. | Primary E; different integrated critical reviewer |

Test-worker preflight and accepted Red/characterization precede every Green. Record exact failure cause and which assertions ran, protected test identities, final lease, post-Refactor checks where applicable and test relevance. No screenshot, summary or lease receipt replaces behavioral evidence or actual diff inspection.

## Idempotence and Recovery

Read-only checks repeat safely under their evidence identity. A rescan is new explicit work, never an in-place retry; an ordinary Analyze remains unlinked. Failure must not alter the baseline, resurrect interrupted work or make a persisted running/failed record look complete. A lost reply may hide a committed later run: preserve uncertainty, use validated service-owned inspection for diagnosis and never auto-submit another run or promise user-facing recovery.

Keep candidate references within their bounded operation; release them on success, rejection, timeout and shutdown. Do not persist them to bridge M5-01 and M5-02 or after restart. Future comparison uses a future invocation while candidates are live, not a retroactive result derived from absence.

Close only task-owned browser/context/service handles before exact disposable cleanup. Resolve absolute descendants, reject linked/unexpected paths and preserve retained evidence or uncertain artifacts when removal cannot be proven safe. Report retained paths and cleanup failures; never kill unrelated processes, delete originals, use Git reset or replay consumed historical grants.

A worker stop returns to primary triage and terminal lease inspection. Reconcile changed binding fields before another assignment; invalidate affected evidence and reuse only complete matching identities. Exhausted budgets, missing owner choices or expanded architecture/target authority require a concise evidence-backed owner question after unaffected work is complete.

## Artifacts and Notes

### M501-G-ACCEPT-01 — Contract accepted

The primary accepts the different fresh final artifact `critical_research_reviewer` PASS for complete plan SHA-256 `ad37deb785423e01f83752662f603e216a112d07da35bd9cb9833714b1b4b4ae`, before this verdict maintenance. It passed I1–I10 with no Blocker, Major or Minor. It independently reproduced NC3 in memory, checked the exact artifact/HEAD/manifest/lock identities, four documentation-only dirty paths, absent active lease and passing `git diff --check`; it inspected material source/harness seams and reused valid prerequisite/reference evidence. Nothing was invalidated. This closes G and supersedes pending-review wording in the authored contract. No runtime result is claimed.

Primary inspection confirms source/tests remain unchanged. The sorted tracked `src`, `tests`, `package.json`, `package-lock.json`, `tsconfig.json` and `vite.config.ts` inventory is 160 files; SHA-256 of newline-joined `path:lowercase-file-sha256` entries is `80260bc43dbda8884124df44b70a4cd98593579b7384592e400a27ee639de518`. Four changed documents pass strict UTF-8/no-BOM, final newline, local link-path and whitespace checks. Research budgets remain recorded without reset. Documentation impact is the existing plan/status/navigation records only; requirements and ADRs are unchanged.

### M501-A-PREFLIGHT-01 — Missing behavior accepted

The persistent test owner returns **MISSING**: the closed run validator explicitly rejects lineage, three identity comparisons omit it, `LocalService.rescanFinding` and the route are absent, and no settled-owner retirement exists. Existing repository and ordinary-scan behavior is infrastructure, not rescan coverage. Primary accepts four test-owned paths: `tests/run-contract.test.ts`, `tests/run-repository.test.ts`, `tests/rescan-service.test.ts`, `tests/rescan-api.test.ts`. Keep `tests/local-service.test.ts` unchanged as regression; no new fixture helper is necessary. Reuse the existing m102/m202/m302 fixtures and m401 review sandbox. Source/tests remain unchanged at preflight closure.

Primary freshly verified the exact README-wrapped Node runner: all 73 current run-contract tests pass, exit 0, no skips; entry strict remains valid against unchanged source/config. Initial Red may precisely fail on absent `LocalService.rescanFinding(input:unknown,execute?:RescanExecutor):Promise<RescanOutcome>` or `src/server/local-service/rescan-api.ts::receiveRescan` (same request/execute/send signature as receiveReview, with RescanOutcome). Full behavioral tests must be present without stubs, conditional assertions or skipped cases; identify which assertions are blocked. Existing callable lineage tests must fail behaviorally. Green must run every unchanged assertion and independent strict.

Initial Red uses lease `M501-A-red-1`, owner `m501-a-test`, phase red/attempt 1 under workflow `M5-01-20260916`, with the exact four paths above and no directory allowance. All production, docs, corpus, evaluation, frozen fixtures, workflow configuration and unrelated tests are forbidden. Existing contract/repository tests execute with their original safe `temp/m102-store-*` sandboxes; new tests use `temp/m401-review-service-*`, exclusive synthetic files and owned loopback handles, restoring injected functions and closing ports before exact validated cleanup. Unchanged local-service regression may additionally use its existing `temp/m102-service-*`, bounded owned entry processes and exact create-only `data/runs/m102-demo-<UUID>` children, preserving every original run. No browser, model/public request or new test helper is authorized. The coordinating complete v2 write packet projects these decisions and retains all preflight authority, responsibility, budget and stop fields; guard digest is inserted only after start/verify.

### M501-A-RED-01 — Initial Red and bounded typing correction

Lease `M501-A-red-1` closed compliant with receipt `4378f6b61a737bb87dd413da9e41dfff1aa82f40683df21ce23d52c55f0a87c0`: exactly the four authorized test paths, no production/config/HEAD/index/ignore drift. Primary inspected the actual changed assertions and both complete new test files; reported hashes match. Final initial results were run-contract 73/74 (valid linked record rejected), repository 75/77 (linked create rejected), rescan-service 0/7 (exact missing method), rescan-api missing agreed module, and unchanged local-service 69/69. Strict reported only the missing API module. Assertions behind the absent boundaries have not executed. Existing downstream fixtures were validated and round-tripped before the missing call. No owned m401/m102 sandbox residue remains.

Primary identified, and the persistent test owner confirmed read-only, a latent assignability defect masked by the missing module: local test outcomes widened `error` to `string`, and one executor widened selected rule. This would conflict with the accepted closed production contracts after Green. Use ordinary Red correction `M501-A-red-2`, parent `M501-A-red-1`, same owner/role/phase/scope, to import contracted `RescanOutcome`/`RescanExecutor`, derive failure with `Extract`, narrow helper error parameters and remove shadowing aliases. Preserve all behavioral assertions and deliberate malformed-value casts. This corrects tests without weakening production policy. Those exact missing type exports join the accepted first-module boundary; no stub is authorized. Re-run affected service/API Red and strict diagnosis, reaccept final hashes, then Green. Initial Red evidence for unchanged domain/repository/local-service files remains valid; changed new tests require fresh results. One ordinary Red correction is consumed on dispatch; only the conditional third remains, with no budget reset.

### M501-A-RED-ACCEPT-02 — Protected tests

Red correction lease `M501-A-red-2` closed compliant, receipt `80732df3287bf5114b2bfe622ac026a36f75887e6a6865efea75cf43ffd02d1a`, with only the two new test files changed. Primary inspected exact type imports, narrowed helpers, callbacks, behavioral assertions, terminal lease and hashes. Corrected service Red is 0/7 at the exact missing method; API Red is the exact missing module. Strict has only the contracted missing RescanExecutor/RescanOutcome exports and API module. No implicit-any or unrelated test diagnostic remains. Reuse unchanged domain/repository/ordinary-service evidence from Red 1; all later assertions are pending execution in Green. No owned residue remains.

Protected SHA-256: run-contract `1a301bf3fbafad662bc5a833f66ac24b4cf749e00c70c453dbaafe36e60b3204`; run-repository `9f6614234874891009393b2041e2fb30692a910eced0f25364ead79343d6a227`; rescan-service `6d33c161ca290188051887243a84f0f66745bb06f65dc32f85e588fc71df8b8c`; rescan-api `20017be497500c80e48c1b524fc642eae809c321733221eaceaea0df187739b6`. The complete `tests` tree is forbidden in Green. No Red allowance is reset.

Initial Green packet/lease `M501-A-green-1` uses separate `code_worker`, owner `m501-a-code`, attempt 1/no correction parent. Exact thirteen production files are the A G inventory; no input-validation, scan-page, helper, client, dependency or configuration edits. Existing scanner remains the A default executor; selected-rule candidate wiring belongs to B. Run all five A test files with the exact existing wrapper/flags plus independent strict. Changes to shared lifecycle invalidate corresponding ordinary-service evidence and require its regression run. Reuse only complete unchanged identities. Close the lease before primary inspection, documentation and fresh S3 review. A Green retains its initial plus ordinary/conditional correction allowance and one review correction loop.

### M501-A-GREEN-01 — Test-contract conflict and conditional correction

Initial Green lease closed compliant with receipt `5a60c3f4257e6edb94db91b9073991c17f5ef667c1a257296f6e181d41a5dfe3`; exactly thirteen allowed production paths changed, protected tests and Git state unchanged. Domain and repository suites pass (counts not retained in the truncated worker output), ordinary service passes 69/69 and rescan service passes 6/7. API did not run after failure; final strict and structural-fit disposition remain pending. No owned sandbox/demo residue was found. This is incomplete Green, not slice acceptance.

New runtime evidence exposes a test mistake hidden behind the earlier absent callable: throwing an executor produces an authored failed run with `cleanup:'failed'`, preserving the existing admission lock. The test's failure-preservation table nevertheless expects generation to proceed, and its later retained-generation case repeats the same assumption. Primary checked unchanged `createFailedRun` semantics and the extracted lifecycle; required cleanup protection must not be weakened. The bug index was consulted; this routine task-contained test correction remains in this plan.

Conditional Red attempt 3 is justified by concrete progress: corrected type-bound tests now execute against production, six service contracts pass, and the remaining failure distinguishes two previously conflated cleanup states. Different bounded action: use the existing `linkedFailure(run,'closed')` fixture in both cases that need to observe retained capability; explicitly retain the thrown-executor cleanup/admission denial witness. Change only `tests/rescan-service.test.ts`; keep the identical four-file guard scope and all other assertions. This models known clean failure honestly and is likely to resolve the single demonstrated test mismatch without changing G. Parent is terminal `M501-A-red-2`; no new ID resets its allowance. On dispatch, the final conditional Red allowance is consumed. A fourth test write turn is unavailable.

The changed service-test evidence is invalidated until its final hash and focused result are accepted. Run that suite, the now-present API suite and strict diagnosis under the same bound commands/resources. Current production may make corrected tests pass; report that honestly and preserve the original behavioral Red history rather than fabricating another failure. Production repairs, if needed, remain with the separate Green owner and its remaining allowance. No authority or implementation contract changes.

### M501-A-RED-ACCEPT-03 — Final test correction accepted

Lease `M501-A-red-3` closed compliant, receipt `24d32a8b56a9fba4655d7c40c1c5bf8fdc4894c317580953063f1723ab3438f1`, with only the intended service-test change. Primary inspected both known-clean fixtures and explicit thrown-executor uncertainty/denial assertions. Final service-test SHA-256 is `de657ba850742f0e7470379f9a5b7414a7ca43a6f70b919956b1d7f921c83aba`; the other three protected hashes remain as in Red acceptance 2. Rescan service passes 7/7 and first complete API execution passes 6/6. No residue remains. This is corrected passing evidence against partial Green; original Red history remains intact. No further test write attempt is authorized.

Strict exposes one production-only diagnostic in `rescan-api.ts`: the failure return's run needs an explicit validated `FailedRun|null` narrowing. No test diagnostic remains. Ordinary Green correction `M501-A-green-2`, parent terminal `M501-A-green-1`, uses the same thirteen-file scope and separate code owner. Fix that narrowing without a broad cast or wider public outcome; normalize the extracted scan-operation indentation to the existing style and complete its structural-fit checkpoint. No behavior or test change. Run rescan/API and independent strict after this bounded correction; primary then reconciles focused evidence and fresh S3 review. This consumes Green's ordinary correction; conditional attempt 3 remains subject to the existing learning/evidence rule.

### M501-A-GREEN-02 — Corrected Green evidence

Lease `M501-A-green-2` closed compliant, receipt `d50b82afccbec015823d8bfb797d61dcc0f4bf0815b421acd83a1d5592feec07`, with only explicit validated failure narrowing in `rescan-api.ts` and indentation in `scan-operation.ts`. Primary inspected both complete changed regions, the cumulative A production diff, terminal scope and unchanged four protected hashes. Worker strict, rescan service 7/7 and API 6/6 pass, exit 0; primary independently reran strict and `git diff --check`, both exit 0. Domain/repository and ordinary-service 69/69 evidence from Green 1 remains valid: the subsequent edits do not change their dependency behavior. No owned service residue remains. Structural disposition is RETAINED: admission, shared scan publication, transport framing, reservation and private settled-owner retirement remain with their declared owners.

Fresh S3 review is pending; this is Green evidence, not slice acceptance. A test write budget is exhausted; ordinary Green correction is consumed, with only the conditional third available if new material evidence supports it. Review findings must remain visible even if their correction would exceed a budget. No production candidate handling, client or UI behavior is claimed.

Primary final-tree verification supersedes the count-truncated/reused focused results above: run-contract 74/74, run-repository 77/77, local-service 69/69, rescan-service 7/7 and rescan-api 6/6, all exit 0 with no skips. The exact README-wrapped commands ran sequentially, with the module-mock flag only for rescan-api; separate strict also passed. This repetition binds the changed shared lifecycle's exact fingerprint and removes the incomplete count evidence. The sorted tracked plus untracked source/test/config inventory is 165 files, fingerprint `ddb28c47c1a896a4cd9a5d066d4adcc099054d557ff9bac004af013ba08b1bed`, using G's newline-joined path/hash method. Node, lock/config, HEAD and frozen inputs remain unchanged. Final inspection found no m102 store/service/demo or m401 review-service residue. No browser or external provider boundary was exercised.

### M501-A-ACCEPT-01 — Linked-run service accepted

Fresh S3 critical review returns PASS with no Blocker, Major or Minor. It inspected all thirteen production and four test changes, verified protected identities and compliant leases, and independently ran read-only in-memory probes for accessor refusal, shutdown before/after read, requested URL and fresh mode, publication/retirement/settlement ordering, publication-failure preservation, lineage substitution, reentrant shutdown and deadline suppression. All probes exit 0. Primary accepts the actual cohesive diff, terminal receipts, independent strict and final-tree 233/233 focused results recorded above. No evidence was invalidated and no owned residue remains.

A is accepted; B may start. Injected scan executors establish A's service/publication contract, not real scanner/candidate, client, UI or integrated behavior. Those obligations remain B–E. Documentation impact at this checkpoint is the plan and progress narrative; public capabilities/commands remain for E reconciliation. M5-01 remains In progress and M5-02 unselected.

### M501-A-PACKET-01 — Read-only preflight

Milestone Assignment Packet v2

Identity
- Workflow ID: M5-01-20260916
- Roadmap task ID: M5-01
- Work-slice ID: M501-A
- Assignment ID: M501-A-preflight-1
- Lease ID: None for preflight
- Phase: preflight
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m501-a-test
- Guard contract digest: None for preflight

Work-slice capsule
- Owning ExecPlan: this plan; M501-G-CONTRACT-01 and M501-G-ACCEPT-01 control all literals.
- Observable acceptance contract: independent durable baseline-linked run from any validated completed-baseline Finding under one reservation; fresh explicit mode; unchanged baseline; exact closed API and success-only retirement of old settled capability. A uses existing terminal executor; B later supplies candidates without changing this service contract.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Context and Orientation authority table; roadmap M5-01 Verification; REQ-SCAN-005, REQ-EVID-008/010/011, REQ-COMP-004/006/007/008; ADR-0009/0021/0024; SPEC-006/007 and their derived BHV-06/07, HS-001/004/006/015 through the roadmap authority-location key. G and I1–I4/I7/I10 specialize A; other invariants remain protected.
- Readiness and evaluation-freeze evidence: M501-ENTRY-02 and G acceptance; prerequisites Complete; both manifests and all thirteen referenced identities unchanged; no source/test drift or active lease.
- TDD applicability: Applicable — observable service/schema/API behavior.
- Current-state and preflight evidence IDs: M501-ENTRY-02, M501-G-ACCEPT-01; preflight result pending.
- Accepted test boundary and current test owner: None until Red acceptance; persistent m501-a-test owns test side.
- Relevant boundaries and paths: A exact production/test inventory in G; read related existing retrieval/generation/review sandbox fixtures when necessary for ownership witnesses.
- Production responsibility placement and fit: domain run-types/run-validation own optional pure lineage; three named terminal/parent/selected transitions own immutable identity. contracts owns public internal types; rescan-operation owns descriptor-safe intent and retained-baseline admission; scan-operation owns shared existing create/execute/finish lifecycle; service owns reservation/shutdown/composition; rescan-api owns bounded HTTP framing; loopback-api owns routing. retrieval/generation operation owners gain only their internal settled-capability discard. This preserves current responsibilities and avoids duplicate lifecycle/writer behavior.
- Dependency, runtime-call, or interface-edge changes: HTTP → service reservation → rescan admission/repository → existing scanner executor → validated terminal/repository finish → success-only private owner retirement. No domain I/O or provider call.
- Reuse, justified separation, bounded creation, or local-extraction disposition: reuse existing repository, validators, scan preparation, reservation and sandbox/fixtures; create rescan admission/API handlers for current distinct responsibilities; extract shared scan lifecycle because ordinary and rescan now both consume it.
- Permitted local structural refactor: existing service scan lifecycle into scan-operation only; ordinary behavior preserved. None elsewhere.
- Non-goals: B candidates, C client, D UI, comparison/matching, history/reopen, dependencies/config, model/public scans, original runs, Git writes and authority/status edits.
- Named uncertainties: classify exact existing coverage/missing contract; determine whether listed m501 test fixture helper is necessary before any lease. No unresolved behavior semantics may be invented.
- Risk tier: S3
- Review and escalation triggers: immutable data, reservation/reentry, shutdown, safe publication and capability retirement require fresh critical review after accepted Green/strict; path/cohesion/command mismatch returns to primary.

Write scope
- Allowed files: None — read-only preflight.
- Allowed directory roots: None.
- Forbidden files: All writes prohibited, including ignored outputs/caches.
- Forbidden directory roots: Entire repository and external filesystem for writes.

Validation
- Working directory: C:/projects/a11y-evidence-lab
- Focused command: source/test inspection is primary preflight evidence. If needed, after exact README preparation, `Invoke-M105Command { & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts; if ($LASTEXITCODE -ne 0) { throw 'Run contract preflight failed' } }` only after confirming it creates no files. No service/browser fixture execution in read-only preflight.
- Task-level command: G exact A five-test commands and independent strict after writes; closure exact README suite plus accepted additions, build and E proof. These commands are not authorized effects in this read-only turn.
- Expected decisive result and reusable evidence IDs: one workflow classification with exact source/test locations and precise missing assertions; propose smallest cohesive Red set, exact new callable signatures and first-module exception only if required. Do not fabricate Red.
- Relevant-tree fingerprint: G acceptance 160-file fingerprint `80260bc43dbda8884124df44b70a4cd98593579b7384592e400a27ee639de518`; HEAD `0cbbf26bdf6facba647f14ece0580fef12bffb28`; four primary docs dirty, source/tests unchanged.
- Environment fingerprint or Non-reusable: Node 24.20.0/Python 3.12.10/lock hash and wrapper in ENTRY-02; pure inspection stable; later filesystem/service checks Non-reusable until isolated exact execution identity recorded.
- Known external side effects and cleanup: None in preflight. Red/Green later use only existing safe synthetic sandbox patterns and owned loopback handles; exact roots bound before writes.

Budget and stopping
- Maximum worker turns: one preflight; later initial Red plus one ordinary correction and conditional third under workflow learning requirement; persistent instance.
- Maximum corrections: one ordinary plus at most one conditional final within unchanged role/phase chain; review one correction loop. No new agent/ID resets allowance.
- Maximum repeated identical failure: two decisive identical gaps without new material evidence stops for primary triage.
- Maximum no-diff outcomes: two stops for primary triage.
- Validation cadence: preflight classification now; coherent Red then separate protected-test Green; independent strict/focused at slice barrier; no redundant full-suite execution.
- Stop and escalate when: conflicting authority, binding field change, missing environmental prerequisite, budget exhaustion, undeclared effect/path or evidence incapable of supporting classification. Never write without a fresh verified lease.

Handoff
- Report identity and authority, touched paths (None), exact commands and decisive results, one classification, covered/missing behavior, proposed exact test paths/helpers/fixtures/commands and expected Red reason, unexpected state, residual risks and documentation impact. You are not alone in this codebase; preserve primary documentation and other work. Git is read-only; no metadata mutations.

### M501-B-PREFLIGHT-01 — Scanner handoff missing

Primary accepts MISSING: current capture has no selected-pass argument/capture, normalization returns no candidates, the candidate type/rescan executor export are absent and the service still defaults to ordinary executeScan. Exact two-file test boundary is `tests/scan-normalization.test.ts` and `tests/scan-page.test.ts`, using existing report doubles, offline/routed capture and cleanup helpers; no new helper, fixture or service-root resource. Primary fresh baseline is normalization 20/20 and scan-page 88/88, exit 0/no skips, exact README wrapper and empty scanner scratch before/after. Six unchanged frozen states pass the ordinary baseline. No browser/provider acquisition or public request occurred.

Authorize initial Red `M501-B-red-1`, owner `m501-b-test`, same workflow/task/slice, two exact test paths and no directory allowances. All source, docs, frozen inputs/config and other tests remain protected. The narrow first-module boundary is missing `executeRescanScan` and type `NativeCandidate`, plus unsupported second arguments to capture/normalization; complete scanner behavioral assertions must be present without skips/stubs and their nonexecution reported. Normalization remains callable and must fail behaviorally for absent candidates. Candidate typing must preserve G's per-rule discriminated union; no broad rule/check/evidence combination is accepted. No extra public normalization type export is required by tests.

Required witnesses remain G's selected-only passes, five-key detached deep-freeze/order/duplicates/unavailability, whole-report rejection, six-state actual-reference capture including replacement/missing/shadow/overlong/nonunique controls, post-cleanup success and failure/abort/timeout/late denial, ordinary compatibility and scanner-boundary serialization/canary nonleak. Service adapter unwrap is a Green diff/review obligation and actual disk/HTTP nonleak belongs E. Commands are the exact two B tests and strict diagnosis; browser scratch only `temp/m103-scan` under existing owned cleanup. Initial Red budget is consumed on dispatch; ordinary and conditional corrections remain.

### M501-B-RED-01 — Initial Red and witness correction

Initial Red lease closed compliant, receipt `874c6165286bbfda2a7f0a028659d78f5d1d63c2f16acda854ef00fe316cf1e9`, changing only the two tests. Primary inspected both actual diffs and final hashes: normalization `8112ad275eca0b2866b0ab772f687a4f3eb66e82f1115639365a06a616389a2a`, scanner `a20b246c4d601f1be3e5c7e91934f240b288a6f3d7997c502beba510e3f93c79`. Normalization is 20/22 with two missing-candidates shape failures; scanner fails at the exact absent export before any assertion/browser. Primary independent strict diagnosis reports only the contracted missing type/export/arguments/candidate property. Owned scratch is empty. The worker handoff truncated the final digest character; the immutable contract and primary dispatch/closure used the correct full digest ending `e3b`.

Primary inspection finds a bounded witness gap before protecting tests: the new failure checks do not yet exercise cancellation after candidate projection (cleanup-time abort or synchronous-normalization deadline), missing selected-pass capture, malformed unselected node containers, or explicit nonzero-inapplicable exclusion. Ordinary tests cover related shared behavior but cannot prove that already-created candidate references are withheld at the new envelope boundary. Ordinary Red correction `M501-B-red-2`, parent `M501-B-red-1`, same role/owner/two-file scope, adds those precise cases using existing helpers. Preserve every existing assertion; no new resource/type/API is needed. This consumes the ordinary correction, leaving only the conditional third. Re-run both Red diagnoses and strict, then accept final hashes before separate Green.

### M501-B-RED-ACCEPT-02 — Protected scanner tests

Ordinary Red correction closed compliant, receipt `4ed27fd340e9f91a819c734ec14d12d50b75e9a949ac02bbbe0af9775fd0df00`, changing only the two authorized tests. Primary inspected the five bounded new witnesses and protected hashes: normalization `8df0afa3ebc9535cf34c70781b5d96c286690fcae49eea7f503373baf0d33288`; scan-page `7e051ee932cc42456d1619028f0942144ac6179ce0ca220b6c391a7422eea2d8`. Normalization remains 20/22 with two precise absent-candidates failures; scan-page remains the exact missing export, no assertions/browser executed. Strict has only agreed missing APIs/arguments/properties. Scratch remains empty and no input/config/source drift occurred. The original synthetic zero-inapplicable case remains in the ordinary suite; the added selected path now explicitly protects nonzero compatibility too.

Primary accepts the complete Red boundary, including currently blocked assertions. Initial Green `M501-B-green-1` uses separate code owner `m501-b-code`, the four named scanner files plus `src/server/service.ts`, no directory allowance and all tests forbidden. It must execute every assertion unchanged, retain the exact profile/deadlines, immediately unwrap the private envelope in the default service adapter, and report structural fit. Run both B suites, separate strict, and A rescan-service/API regression because default service composition changes. Real disk/HTTP nonleak remains E. B Red has only its conditional final correction left; Green starts its own initial/ordinary/conditional allowance without resetting any earlier chain.

### M501-B-GREEN-01 — Scanner handoff Green

Initial Green lease closed compliant, receipt `b5e24697269a07fbbb5c6b8956f4e603bea094b7de249e869a681ef7167e3127`, with exactly the four scanner owners and default service adapter changed. All six A/B protected test hashes remain unchanged. Primary inspected the actual complete B diff, including the immediate run-only unwrap, and independently passed strict TypeScript. Worker final normalization 22/22, scanner 105/105, A rescan-service 7/7 and API 6/6 all pass, exit 0/no skips; initial strict union narrowing was corrected within the same turn, then strict and affected scanner checks reran successfully. Scanner scratch is empty and no m401 service residue remains. Structural fit is RETAINED with one shared executor and existing rule projection. Fresh B S3 review remains pending; disk/HTTP integration is still E.

The current 165-file relevant-tree fingerprint is `76c4484b150a548d8e724aac5fc6c062017e17ecbc437b379b4e852a081e05a2`, computed with the established tracked-plus-untracked path/hash method. HEAD, environment and frozen inputs are unchanged; no write lease is active.

### M501-B-ACCEPT-01 — Transient scanner handoff accepted

Fresh S3 critical review returns PASS with no findings and confirms RETAINED cohesion across the five production owners. It independently passed sixteen in-memory cases covering all three rules, malformed reports, detachment/freezing, mode independence, cleanup cancellation/failure, timeout, stale context and ordinary output compatibility. It checked the exact 165-file fingerprint, protected test identities, frozen manifests, lockfile, three compliant B receipts, absent lease and whitespace. Current real-browser 105/105, normalization 22/22, service 7/7, API 6/6 and independent strict evidence were reused; nothing was invalidated. Primary accepts B's actual diff, complete executed assertions and cleanup. C may begin. Real integrated disk/HTTP nonleakage remains E, with no public-page, model or cross-platform qualification claim.

### M501-G-COMPAT-01 — Preserve existing Analyze compatibility

Primary inspection before D found that G's blanket sentence about uncertain generation blocking mutations was broader than the accepted existing behavior. The [M4-02 contract](m4-02-accessible-review-ui.md#m402-g-contract-01--transport-and-interaction-contract) at its review-ownership paragraph expressly preserves new independent Analyze after generation uncertainty; `tests/finding-generation-ui.test.ts:447` protects it and README's proposal-review instructions describe the same distinction. App's current reservation and review-owner guard implements that accepted behavior. M5-01 neither selects its replacement nor authorizes changes to that protected generation suite.

The corrected G sentence preserves that existing exception: active operations still block competition, review/rescan uncertainty retains all-mutation locks, and generation uncertainty blocks the new rescan plus existing guidance/review actions while retaining explicit independent Analyze. Historical settled capability alone still does not prohibit rescan. C's rescan-specific release matrix, A/B behavior and every other invariant remain unchanged. This is a scope/compatibility clarification, not a new product decision or evidence of implemented UI. The existing final artifact reviewer receives the first of its at-most-two correction checkpoints; dependent D work awaits its acceptance. All earlier research, test, Green and review budgets remain consumed as recorded.

Correction checkpoint 1 returns PASS for plan SHA-256 `b4f841f43f6c2f619ef12e6d6f6188b386a5e1c1920b762e38df6ab09c17e089`, with no findings across I1–I10. Primary accepts the clarification. The reviewer independently inspected the exact prior contract, protected test, README and App evidence, unchanged HEAD, absent lease and passing whitespace; it reused unaffected research and ran no runtime check. Only the earlier blanket interpretation is superseded. One final-artifact correction remains; no other allowance resets. D must preserve the existing generation-unknown/independent-Analyze test unchanged and explicitly prove that generation uncertainty denies rescan, review/rescan uncertainty denies every mutation, and late responses/preview navigation release no lock.

### M501-B-PACKET-01 — Read-only preflight

Milestone Assignment Packet v2
Identity
- Workflow ID: M5-01-20260916
- Roadmap task ID: M5-01
- Work-slice ID: M501-B
- Assignment ID: M501-B-preflight-1
- Lease ID: None for preflight
- Phase: preflight
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m501-b-test
- Guard contract digest: None for preflight

Work-slice capsule
- Owning ExecPlan: docs/plans/m5-01-intentional-rescan.md, B and M501-G-CONTRACT-01 scanner-private handoff; accepted G and A gate.
- Observable acceptance contract: complete unchanged three-rule scanner exposes only selected-rule native passes as minimized detached deep-frozen operation-local candidates after full report/context/cleanup acceptance; production adapter immediately discards candidates and returns run only. Ordinary executeScan/normalization unchanged.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: Plan authority table + G I1/I4–I7/I10, roadmap M5-01 Verification and authority-location key; REQ-SCAN-005/006/007, REQ-EVID-008/010/011, REQ-COMP-004/006/007/008; ADR-0009/0021/0024; SPEC-006/007; BHV-06/07, HS-001/004/006/015 derived through roadmap. Selected passing candidates only, no comparison.
- Readiness and evaluation-freeze evidence: G-CONTRACT/ACCEPT-01 and A-ACCEPT-01. Both manifests/all13 frozen references remain unchanged; prerequisites Complete; installed browser1234/151.0.7922.34. No A source writes pending.
- TDD applicability: Applicable, observable normalization/capture/lifetime contract.
- Current-state and preflight evidence IDs: G-CONTRACT-01, G-ACCEPT-01, A-GREEN-02/A-ACCEPT-01; B preflight pending.
- Accepted test boundary and current test owner: None until Red acceptance; persistent m501-b-test owns B tests.
- Relevant boundaries and paths: tests/scan-normalization.test.ts,tests/scan-page.test.ts; existing frozen fixtures/helpers inspection; production4 scannerfiles and adapter below.
- Production responsibility placement and fit: src/server/scan/native-scan-capture.ts owns actual-node capture and reference removal; normalization/native-rule-evidence.ts owns candidate type/projection via existing rule fact allowlists; normalize-scan.ts owns full native report validation/selected projection; scan-page.ts owns shared scanner lifecycle and post-cleanup envelope. src/server/service.ts owns default rescan adapter composition and immediately unwraps run. No new scanner module.
- Dependency, runtime-call, or interface-edge changes: validated baseline rule -> default rescan adapter -> executeRescanScan -> existing actual capture/normalization -> post-cleanup private envelope -> immediate run-only unwrap -> existing service publication. Candidates have no domain/disk/wire/client/log/provider edge.
- Reuse, justified separation, bounded creation, or local-extraction disposition: extend4 existing cohesive scanner owners, reuse projection and cleanup machinery, one private shared execute path for ordinary/rescan entrypoints. Reuse current fixture interception/doubles; no new helper anticipated.
- Permitted local structural refactor: scan-page executeScan body into shared private executor solely for ordinary/rescan consumers, preserve10swork/4scleanup behavior. No other structural refactor.
- Non-goals: matching/canonicalpositive/comparison, candidate cache/consumer/callback/API, profile/fixture/dependency/config changes, C–E, public/model calls, original retained runs, Git writes.
- Named uncertainties: classify existing coverage versus missing; smallest coherent Red additions, exact export/type signatures and first-module boundary; confirm no new helper required. Binding semantics already decided; do not invent them.
- Risk tier: S3
- Review and escalation triggers: transient untrusted provenance, full-report validation, cancellation/cleanup/late settlement and nonleakage require fresh critical review. Any authority, path, resource, command or responsibility mismatch returns to primary.

Write scope
- Allowed files: None, read-only preflight.
- Allowed directory roots: None.
- Forbidden files: All writes including ignored outputs/cache/guard metadata.
- Forbidden directory roots: Entire filesystem for writes.

Validation
- Working directory: C:/projects/a11y-evidence-lab
- Focused command: read-only source/test/fixture inspection; no browser/service/test filesystem effects in preflight. Primary may run missing baseline evidence after your report.
- Task-level command: after writes exact README preparation (dot-source first Powershell block containing function Invoke-M105Command) and sequential Invoke-M105Command { & $m105Node --test --test-timeout=120000 tests/scan-normalization.test.ts; if($LASTEXITCODE-ne0){throw 'Normalization failed'} }; then Assert-M105EmptyDirectory $m105ScanTemp and same scan-page.test.ts with wrapper second argument $m105ScanTemp; assert empty afterwards. Separate strict: & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json with exitcheck in wrapper. These effects not authorized in preflight.
- Expected decisive result and reusable evidence IDs: one workflow classification plus precise existing/missing behavior, exact proposed test paths/signatures/resources, meaningful Red reason and first-module exception only if needed. G research is authority/source evidence, not runtime proof.
- Relevant-tree fingerprint: final A165-source/test/config path fingerprint ddb28c47c1a896a4cd9a5d066d4adcc099054d557ff9bac004af013ba08b1bed; HEAD0cbbf26bdf6facba647f14ece0580fef12bffb28. Current changes are A and4primarydocs.
- Environment fingerprint or Non-reusable: Node24.20.0, Python3.12.10, PowerShell7.6.5, lock38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d. Native browser execution Non-reusable until owned isolated run identity recorded. No browser now.
- Known external side effects and cleanup: None in preflight. Later exact existing temp/m103-scan browser scratch only, frozen controlled HTTPS interception, six fixture states unchanged, no concurrentbrowser/service. Preserve uncertain residue, validate ordinary ancestry and exactownedchildren before cleanup, no parent deletion/download/provider call.

Budget and stopping
- Maximum worker turns: one read-only preflight; later initialRed + ordinarycorrection + at most conditionalthird with newevidence/learning.
- Maximum corrections: one ordinary, one conditionalfinal; one reviewcorrectionloop. Persistent role chain; no newagent/ID reset.
- Maximum repeated identical failure: two decisive identicalgaps withoutnewmaterialevidence -> primarytriage.
- Maximum no-diff outcomes: two -> primarytriage.
- Validation cadence: preflight classification now; acceptedRed -> separateGreen withtestsprotected; focused/strict/criticalreview atBbarrier; fullsuiteE only.
- Stop and escalate when: missingauthority/environment, changedbindingfields or requiredpath/effects, exhaustedbudget, unverifiableevidence; no writes withoutprimaryverifiedlease.

Handoff
- Report identity/authority, pathsNone, exact inspections/results, oneclassification, smallest test additions/signatures/expectedRed, existing coverage/reuse, neededhelpersNone or precisejustification, risks/effects/cleanup/documentationimpact. You are not alone; do not revert others. Git inspection only, all mutations prohibited.


### M501-C-PREFLIGHT-01 — Client admission missing

Primary accepts MISSING: all three C production modules and the proposed `tests/rescan-admission.test.ts` are absent. Existing descriptor-safe snapshot, pure run validator, review request and transport patterns provide reuse; no helper or external resource is needed. Exact one-file Red covers response/status/new-run/baseline/requested-URL/mode correlation, fresh unprocessed or zero Findings, closed failure evidence and release matrix, hostile snapshots/reentry/current ownership, single dispatch, stop/late/deadline behavior and four-field same-origin transport. It does not render UI or alter existing Analyze behavior.

Freeze the narrow first-module boundary: static imports in admission, request, transport order; initial loader failure must name absent `src/client/rescan-admission.ts`, with the other two absences and all unexecuted assertions reported. No skip, fallback import or production stub. Primary independently verifies the existing client test runner before Red; B's fresh independent strict remains current for unchanged source. Missing modules cannot excuse implicit-any or widened test helpers.

Task-owned type literals for the request owner: export `RescanCallback` as the captured intent/signal callback; `RescanPresentation` is pending, unknown, or refused with exactly `status`, `error:string`, `cleanup:boolean`, `released:boolean`, `run:FailedRun|null`, `persisted:boolean`. Export `RescanSettlement` as the nonpending presentation union plus `{status:'completed',run:CompletedRun}`. The options' settle callback receives that union; constructor returns synchronous start/stop without caller reflection. A local invalid intent/baseline/selection uses `invalid-request`, and a non-callable callback uses `Rescan is unavailable`; both have null run/false persisted/false cleanup and release because no call occurred. D owns human-facing rendering. These are internal type/presentation literals under G's existing contract, not new service errors or product behavior.

Initial Red `M501-C-red-1`, owner `m501-c-test`, allows only `tests/rescan-admission.test.ts`; all production/docs/config/frozen inputs and other tests are forbidden. Use existing pure completed/failed fixtures, memory-only window/timer/performance/fetch doubles and exact restoration; no listener, browser, network, model or storage. Command is the exact README-wrapped Node test with module-mock flag, then separate strict diagnosis. Initial/ordinary/conditional correction budgets remain the existing C chain.

Primary runner verification: existing `tests/finding-review-admission.test.ts` passes 7/7, exit 0/no skips, under the exact module-mock flag and README wrapper. This is environment/reuse evidence only, not rescan behavior. No source/config changed and no external resources were acquired.

### M501-C-PACKET-01 — Read-only preflight

Milestone Assignment Packet v2
Identity
- Workflow ID: M5-01-20260916
- Roadmap task ID: M5-01
- Work-slice ID: M501-C
- Assignment ID: M501-C-preflight-1
- Lease ID: None for preflight
- Phase: preflight
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m501-c-test
- Guard contract digest: None for preflight

Work-slice capsule
- Owning ExecPlan: docs/plans/m5-01-intentional-rescan.md C, G-CONTRACT-01 Client ownership and visible interaction, B acceptance.
- Observable acceptance contract: exact closed rescan intent and transport/status/body correlation to submitted later ID, baseline ID/requested URL and fresh provider context; completed Findings unprocessed; descriptor-safe request snapshots/current-token/mount/baseline rechecks; one submission and monotonic30sdeadline; truthful release/unknown lock; same-origin POST only.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: plan authority table and G I1/I3/I4/I7/I8/I10; roadmap M5-01 Verification/authority-location key; REQ-COMP-004/006/007/008, EVID-008/010/011, SCAN-005/006/007; ADR0009/0018/0021/0024; SPEC006/007,BHV06/07,HS001/004/006/015.
- Readiness and evaluation-freeze evidence: G,A,B accepted; G-COMPAT-01 accepted preserving existing independent Analyze after generation unknown; prerequisites/frozeninputs unchanged; B acceptance currenttree and strict.
- TDD applicability: Applicable, observable admission/request ownership/transport behavior.
- Current-state and preflight evidence IDs: G-CONTRACT-01,G-ACCEPT-01,A-ACCEPT-01,B-ACCEPT-01; Cpreflight pending.
- Accepted test boundary and current test owner: None until Red acceptance; persistent m501-c-test.
- Relevant boundaries and paths: src/client/rescan-admission.ts,rescan-request.ts,rescan-transport.ts; tests/rescan-admission.test.ts; existing review-request.ts,finding-review-admission.ts,finding-response-snapshot.ts,main.tsx transport pattern and existing admission test doubles read-only.
- Production responsibility placement and fit: rescan-admission owns exact closed input/response correlation using existing pure validator/snapshot; rescan-request owns request lifetime, monotonic deadline, release matrix and bounded presentation; rescan-transport owns single same-origin JSON POST returning status/body. Rendering/main composition remains D.
- Dependency, runtime-call, or interface-edge changes: UI capturedintent (D later) -> requestowner -> callback/transport -> /api/rescans -> admission -> settledpresentation; pure validation only in browser. No serverruntime/filesystem/provider/candidate imports.
- Reuse, justified separation, bounded creation, or local-extraction disposition: create three named owners for distinct current responsibilities; reuse descriptor-safe snapshot and validateRun, review-request lifecycle pattern, existing test timing/mocking. No general transportframework.
- Permitted local structural refactor: None; existing review/generation/ordinaryAnalyze behavior unchanged.
- Non-goals: rendered UI/main wiring, UUID allocation inform (D), comparison/history/recovery/retry/readback, dependencies/config/providers/publicscan/originalruns/Gitwrites.
- Named uncertainties: existing coverage vs missing; smallest onefile test and exact missingexports/types required. G semantics fixed. Existing review start lacks a one-start flag; rescan explicitly requires one dispatch and must not reproduce that omission. validateRun already fixes the provider/model pair per mode; reuse it instead of importing browser/scanner runtime. Transport emits exactly four intent fields and no extra caller fields.
- Risk tier: S3
- Review and escalation triggers: identity/closedserialization/reflection/reentrancy/uncertaintylocks require fresh critical review. Any changedbinding/scope/authority/effect returns primary.

Write scope
- Allowed files: None, read-only preflight.
- Allowed directory roots: None.
- Forbidden files: All writes including ignored/cache.
- Forbidden directory roots: Entire filesystem for writes.

Validation
- Working directory: C:/projects/a11y-evidence-lab
- Focused command: source/test inspection only; no writes/browser/service/network in preflight.
- Task-level command: after writes exact README first powershell preparation and Invoke-M105Command { & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/rescan-admission.test.ts; if($LASTEXITCODE-ne0){throw 'Admission failed'} }; separate strict inwrapper with exitcheck. Fullsuite/build/E later.
- Expected decisive result and reusable evidence IDs: classify exactmissingbehavior with proposed cohesive tests, full closedtypes/signatures and precise missingmodule first-Red ifneeded; no fabricatedRed.
- Relevant-tree fingerprint: B final165-file fingerprint76c4484b150a548d8e724aac5fc6c062017e17ecbc437b379b4e852a081e05a2; HEAD0cbbf26bdf6facba647f14ece0580fef12bffb28.
- Environment fingerprint or Non-reusable: Node24.20.0/Python3.12.10/PowerShell7.6.5/lock38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d; pure controlledtestdoubles stable; no external mutableboundary permitted.
- Known external side effects and cleanup: None preflight; later Node memory-only tests mockfetch/window/performance andrestoreall. NoactualHTTP/listener/browser/storage/model/capture/download.

Budget and stopping
- Maximum worker turns: one preflight; laterinitialRed+ordinarycorrection+conditionalfinal.
- Maximum corrections: oneordinary+conditionalfinal withnewlearning/evidence; one reviewcorrectionloop; no resets.
- Maximum repeated identical failure: two identicaldecisivegaps withoutnewmaterialevidence -> primary.
- Maximum no-diff outcomes: two -> primary.
- Validation cadence: preflight now; Red thenseparateGreen protectedtests, focused/independentstrict/criticalreview; closurefullsuite.
- Stop and escalate when: authority/environment/binding/scope/resourcegap, exhaustedbudget; no writes withoutfreshverifiedlease.

Handoff
- Identity/authority/pathsNone/inspections/oneclassification, exactproposedtestboundary and callable signatures, expectedRed and blockedassertions, unknowns/resourceeffects/cleanup/documentationimpactNone. You are not alone; preserve allwork, Gitreadonly.


### M501-C-RED-01 — Accepted client boundary Red

Primary inspected all thirteen complete behavioral tests and accepts the frozen first-module exception: the focused module-mock command exited 1 at `src/client/rescan-admission.ts`; no assertion executed, and no skip or fallback exists. Independent strict exited 1 with only five TS2307 diagnostics for the three intentionally absent modules. Test-owned narrowing diagnostics were corrected within the initial authoring turn. All behavioral assertions remain unproved until Green.

The sole changed file is `tests/rescan-admission.test.ts`, SHA-256 `445a97e36f18a5d3a5636d4f0df94781685246504d20c5d5d7b1d51a92fc17b5`. Lease `M501-C-red-1` closed compliant, receipt `8a52f99ed4b0fcfbd591354941c831b1fe34878bcd743b2be7ea46333fe35c2c`; no external doubles installed or resources acquired because loading failed first. Whitespace validation passes. The initial Red allowance is consumed; ordinary and conditional corrections remain under the original limits. The existing runner proof and B baseline remain valid; documentation changes do not change the tested source.

### M501-C-GREEN-01 — Client boundary verified

Initial Green creates only `rescan-admission.ts`, `rescan-request.ts` and `rescan-transport.ts` under `src/client/`. Admission owns detached closed selection/response validation; request owns single dispatch, deadline, current checks and release truth; transport emits only the four intent fields. Primary inspected each complete module. Cohesion is RETAINED with no structural refactor or UI wiring.

The exact README-wrapped focused module-mock command passes 13/13 with no skips, and strict TypeScript passes. Primary independently repeated strict successfully and verified the accepted test SHA unchanged. Lease `M501-C-green-1`, contract `238d43f06261e0a1970b1472b179d6171fd66841fb1706bb00969dfe85b86002`, closed compliant with receipt `53813907bc13f546026b9bb55a5f625a5b250d324dfc6222f5714a0f6be12651`; the worker's abbreviated handoff digest is not the authoritative identity. No external resources were acquired; memory doubles restore normally and whitespace checks pass. Fresh critical review remains required before D.

### M501-C-ACCEPT-01 — Client admission accepted

Fresh `critical_reviewer` returns PASS with no Blocker, Major or Minor. It independently passed seventeen in-memory probe groups, including 156 failure/cleanup/persistence tuples and 156 wrong-status negatives, hostile reflection, reentrant stop, thenable rejection, delayed deadlines and transport failure. All doubles restored with zero pending timers. Direct inspection of the 25-module runtime import closure finds no external package, dynamic import, scanner, service or persistence edge; an initial reviewer compiler-API inspection failed before traversal and was replaced by that successful direct inspection.

Primary accepts the actual three-module implementation, unchanged protected tests, fresh 13/13 focused result, independent strict and compliant leases. The 169-file source/test/config fingerprint is `c11691ab60b5a75e89db1d8b0b64c7f3393b401499dca4c3308c748d00e01792`. Frozen paths and whitespace remain clean; no accepted evidence is invalidated. C uses only initial Red and Green, with correction allowances unchanged. Documentation impact is this acceptance and the concise progress checkpoint. D still owns App binding, mutation locks, rendering and built transport; E owns integrated/full-suite closure.

### M501-D-LITERALS-01 — Accepted visual capsule and reuse refinement

Primary accepts M501-UI-01 for the exact G eight-file visual scope. Current source inspection confirms composition in App/Results/Detail, existing guidance/generation presentation owners, and reusable native styles, list, overview and shared status. Extend guidance/generation only with optional `readOnly` defaulting false; preserve historical provenance and unsaved evidence. Keep form validation and rescan status text in the new form owner, C request mechanics in C, and only shared coordination in App. No shared mode-control extraction or new abstraction is needed.

- App callback rescanFinding?: RescanCallback; main supplies postRescan. No default automatic mode.
- Use a native select labelled 'New scan mode', initially empty, options 'Choose a mode', 'Local', 'Groq'. This avoids changing existing Analyze radio names/selectors. Mode remains chosen pending/unknown, resets after released refusal and on a different run/Finding. A new submit always remains explicit.
- Snapshot original baseline selection and read-only guidance/generation presentation maps at valid submission. Pending/failure selection remains live. On each baseline-preview entry restore captured selection; preview selection has separate state. Snapshot contains no request callbacks/tokens/continuations.
- Run-scoped rescan status must remain visible independently of selected Finding or observation, including no selection and baseline preview during another later rescan. Render status through Results composition; form module may export its cohesive small status presentation and shared announcement text. Action form exists only for selected Finding; no scanner observation action.
- Form owns mode/validation; C owns all admission/lifetime/release; App owns only cross-owner reservation, current workflow, captured baseline presentation, and navigation coordination. Results/Detail only compose; guidance/generation narrow optional readOnly defaults false. No new hook/framework/module beyond eight G paths.
- Before preview navigation set an imperative preview ref; stale guidance/generation/review/rescan callbacks cannot mutate while preview is displayed. Ordinary Analyze remains a separate explicit action governed by its existing active/unknown locks; successful Analyze clears the preview pair. Preview does not cancel or invalidate active later requests; validated settlements still update later state. Both navigation buttons focus Results and announce; success focus only if outgoing Results held focus.
- Preserve accepted generation-unknown independent Analyze under its existing lock policy. Generation uncertainty still denies rescan; actual reservation/review/rescan uncertainty blocks all mutations. Historical settled ownerKnown alone does not block rescan.
- For rescan eligibility, generation presentation `unknown` and explicitly reported cleanup uncertainty remain blockers. A settled generation failure with `cleanupFailed:false` may precede rescan even when unsaved or a historical owner remains. Distinguish uncertainty about provider-call provenance from an unsettled operation; no saved-state or downstream-state eligibility gate is added. Preserve ordinary Analyze's existing policy.
- All no-control/readOnly rules and exact unknown/baseline copy are G. No comparison/history/certification claims. Keep existing status, layout, native styles; add only task-specific form/select/status spacing.

The coordinating frontend-visual packet freezes the G ready/pending/error/complete samples at 1366x900 and 390x844, keyboard/validation/visible focus/shared announcements/axe and eight application PNGs only. Source/build/browser identity and create-only screenshot/cleanup rules remain G's exact contract. No 200% or speech claim. Tests additionally cover the full G ownership and preview obligations, unchanged generation-unknown independent Analyze, valid zero results, and built transport. `tests/rescan-integration.test.ts` is authored in D for the existing controlled real-browser/service/scanner/disk harness, then executed at E. Test preflight must establish the minimal permitted harness additions before any lease. D may start only after C acceptance.

### M501-D-PREFLIGHT-01 — Missing interaction and bounded harness extension

Primary accepts MISSING from the persistent test owner: App/main have no rescan callback or request coordination, Results/Detail have no form/status/preview composition, and guidance/generation have no read-only presentation input. The test boundary is exactly `tests/intentional-rescan-ui.test.ts`, `tests/rescan-integration.test.ts` and `tests/helpers/m104-ui-harness.ts`. No additional helper is justified. The existing M105 walking-skeleton harness remains unchanged.

Extend the M104 bridge only with a `rescan` call stage carrying `RescanIntent`, callback identity and signal; `rescan`, `rescanAccessor`, `rescanReads`; a rescan callback proxy trap; and appended optional rescan/accessor arguments to mount/rerender, preserving existing callers. The App callback is `rescanFinding?: RescanCallback`. Avoid reading it during render; reserve and install the stop handle before callback reflection. Tests may describe this callback through the harness's structural type until D implements AppProps.

The complete behavioral suite must fail at the absent accessible `New scan mode` select after valid baseline publication and Finding selection, not at a loader/type error. It covers G's complete state/ownership/preview matrix and gated eight-image proof; the integration test is authored now and executed at E. Primary freshly ran the existing `tests/target-results-ui.test.ts` through the README UI wrapper: 30/30 pass, no skips, Chromium 151.0.7922.34, no external requests or page errors, owned listener closed and scanner/UI scratch empty. C strict remains current. This is harness/baseline proof, not rescan evidence.

Initial Red allows only those three test paths; source/docs/config/frozen inputs remain forbidden. Execute only the new focused UI suite and separate strict diagnosis, with capture flags absent. No build, screenshots, actual scanner integration or provider call in this Red. Use controlled Vite/UI scratch and existing teardown only; unexpected residue stops dependent work. D budgets remain initial Red plus ordinary and conditional correction, separate initial Green plus its original correction limits, and one review loop.

### M501-D-RED-01 — Initial behavioral Red and primary correction

The initial three-file test lease closed compliant with receipt `aa64b0a5288161016adca950ce0b8e7b1832a2717da7b22245b55ad276c81a2d`. The final wrapped browser run reports 15/15 expected failures at the missing accessible mode control, no skips, and strict TypeScript passes. Controlled Chromium is 151.0.7922.34; UI, scanner and integration scratch are empty and no proof images exist. The integration test is authored and typechecked, not executed. Earlier test drafts and their Red executions are superseded.

Primary inspection accepts the missing-production diagnosis but does not yet protect the test boundary. After switching to a scanner observation, the unknown test tries to activate controls that correctly do not exist. The generation-unknown test changes the bridge callback without rerendering its captured collaborator and does not restore its timer double in a finally block. Several waits still require raw error codes instead of the bounded user-facing meaning. Correct these test-owned defects through ordinary Red attempt 2 with identical three-file scope; preserve the complete behavior matrix. No production or resource expansion is authorized. Internal saved-review-focus nonconsumption while previewing remains a source/critical-review witness; both return-navigation actions must focus Results.

Task-local copy refinement: missing mode says `Choose a new scan mode.` A not-eligible response explains that the Finding is not eligible for a rescan; busy explains an operation remains active; stopping/shutdown describe the service stopping/stopped and retained action block. Tests may match these meanings, not internal hyphenated error codes. Exact G unknown and baseline-preview copy is unchanged. Other bounded failure explanations remain implementation literals within G's truthfulness contract.

### M501-D-RED-ACCEPT-02 — Protected interaction tests

Ordinary Red correction changed only the UI test and closed compliant with receipt `931b294c4111715c380827d2a8e880693e3f2ec188e84e3578e451eb31e7b22c`. Primary inspected the actual fixtures, bridge, integration assertions and corrected interaction sequences. The valid mode now precedes generation/review uncertainty, captured collaborators are rebound, timer restoration is unconditional, observation status and present mutation controls are exercised separately, and both navigation directions preserve Results focus. The final wrapped Chromium run has 15 expected missing-control failures, zero cancelled/skipped/todo and no fixture, loader, type, page, external-request or teardown failure. Separate strict and whitespace checks pass. An earlier correction run with a test-owned select timeout is superseded.

Protected SHA-256: UI test `9b5b7d0d55f6ac1de42aba54f4d04cae412533de5f2dc9bff32d88886b03497f`; integration test `4a2515ce976d23e8d2ee542ae37e5b455cf49bed9437983e633e79330c82ec4c`; M104 harness `fc8a96e6e08a30a3c32aafdeaffc319bf5082676a4ac66baa9a5f4e7f8906b63`. Relevant source/test/configuration tree is 171 files, SHA-256 `ae743a16ad3d9654ee9eac2262a984563d61c7780ced815e2e68071273b02bcb`, on the unchanged execution HEAD. All downstream assertions remain unexecuted until Green; no missing-module exception is used. E's real scanner integration and all proof images remain unexecuted.

Initial Green is assigned to the separate frontend implementation owner with exactly G's eight production paths; all tests are immutable. The worker must run the focused UI suite, unchanged generation UI compatibility suite, strict TypeScript and the authorized client build, then disposition cohesion. Primary rechecked ordinary non-reparse build ancestry, contained real paths and single-link files; the three existing generated files retain their entry hashes and may be replaced only by the exact Vite build. UI/scanner/integration scratch remains empty. Primary captures the eight synthetic application images after Green; fresh critical review covers visual evidence and ownership together. No later task or actual provider/public-page operation is authorized.

### M501-D-GREEN-01 — Initial UI implementation and runtime diagnosis

Initial Green touched exactly the eight authorized owners and closed compliant with receipt `d66d3b2ad31efa1b274c14fd72c907fc8e0618b3e49a49bc969a332267169f69`. Strict initially passes; an encoding error in the newly added baseline copy caused the first build failure and was corrected before a successful build. The protected tests remain unchanged. Green and final strict are not accepted; the unchanged generation regression and screenshots were not run.

Primary independently reproduced the entire focused UI suite: 9 pass, 6 fail, zero cancelled/skipped/todo, clean scratch and Chromium 151.0.7922.34. Five failures are ambiguous global text selectors matching shared announcements and visible evidence, including pre-existing generation failure text. The identity test switches the harness root from App to AccessorApp after publishing the baseline, remounting and losing that baseline before submission. These are concrete newly executable test defects, not a reason to suppress required announcements or modify existing generation behavior.

Primary source inspection also identifies a missing implementation witness: the form owns mode locally and is unmounted during baseline preview, losing the chosen mode when the same pending/unknown active intent is displayed again. D-LITERALS-01 already requires preserving that mode. Conditional Red attempt 3 is justified by these newly reachable browser failures and the concrete preview transition; it must scope selectors to the intended Results/detail region, establish accessor mounting before baseline publication, and add the precise pending/unknown mode-preservation assertion without expanding the matrix. Run the complete focused suite to expose all downstream assertions; distinguish genuine production failure from test failure. This consumes D's final Red allowance. A new identity cannot renew it. Ordinary Green correction remains available only after primary re-accepts the corrected test boundary. No source, resource or authority expansion is authorized.

### M501-D-RED-ACCEPT-03 — Runtime-corrected final test boundary

Conditional Red 3 changed only the UI test and closed compliant with receipt `7594a2ea62498293251db81eee620877ed057f2aeeaea5d6a39aa1c8058946b4`. Primary inspected scoped selectors, initial accessor mounting and both new mode assertions. The final full browser run has 13 pass and two genuine production failures: the submitted Groq mode becomes empty after unknown-result observation/Finding navigation, and the submitted Local mode becomes empty after pending baseline-preview navigation. Both return to the same active intent's Finding. The complete remaining matrix is retained; no tests are cancelled, skipped or marked todo. Strict and whitespace pass; external requests and page errors are empty, all scratch is empty and proof images remain absent. An intermediate misplaced test assertion is superseded, not reused.

Protected UI test SHA-256 is now `d15355983b2d05537b46082133b3a0863064410f7fca0a792085f27c6155417f`. Integration and M104 harness hashes remain those in D-RED-ACCEPT-02. Relevant tree: 172 files, `5f279f84aec70c7604dc99f3c249879e524673a2d9241df8390e6e41cf2d186c`. Earlier UI test acceptance and affected executions are superseded. This consumes the final D Red allowance; it does not reset Green or review budgets.

Ordinary Green correction keeps the same eight-file responsibility boundary. Preserve submitted mode only for the matching active run/Finding while its operation remains pending or unknown; another Finding receives no inherited mode, and a released refusal or completed replacement still clears it. The form retains ordinary field/validation ownership; shared coordination retains only the submitted intent needed across navigation. Existing callback, lock, snapshot, focus and evidence contracts remain unchanged. The corrected complete UI suite, unchanged generation compatibility suite, final strict and rebuilt client must pass before acceptance.

### M501-D-GREEN-02 — Submitted intent and frontend verification

Ordinary Green correction preserves the submitted mode in App's shared coordination, keyed by baseline run and Finding; the form retains ownership of fresh input and validation. Pending/unknown navigation now restores the same intent without inheriting its mode into another Finding or run. Primary inspected the actual two-file correction and complete eight-file production surface. Responsibility placement remains RETAINED; no structural Refactor was needed. Accepted tests remain unchanged.

Lease `M501-D-green-2`, contract `bab99e533a13c9dac2a5ebd387e64e57b7eb05bd64342426d76be34d6f6378ad`, closed compliant with receipt `8727d9911012b911eff9c7f53301a342b712b28e7ccc59c6f2f0c6a5dfcae520`. Focused UI tests pass 15/15 and unchanged generation UI tests pass 8/8, with no skipped, cancelled or todo tests. Worker strict checking and client build pass; primary independent strict checking also passes. Relevant source/test/config tree is 172 files, SHA-256 `a74befe42897cbb2e1e130cf5af8007c390b37afff88ef04fc8541effb845f6f`. D Red is exhausted; Green initial and ordinary correction are consumed, with the conditional final correction and one review loop still unused.

### M501-D-VISUAL-01 — Current application visual evidence

Primary ran the accepted UI suite with the explicitly scoped M501 capture flag: 15/15 pass. Chromium 151.0.7922.34 rendered ready, pending, error and complete at 1366x900 and 390x844. All eight create-only PNGs are local ignored artifacts under `temp/m501-rescan-proof/`; the flag was restored absent. Required keyboard, visible focus, announcement, axe and overflow assertions pass. External requests and page errors are empty, owned listeners closed, and UI/scanner/integration scratch is empty. No 200% or screen-reader speech claim is made.

Primary visually inspected all eight images: labels and action remain legible, focus is visible, baseline evidence persists during pending/refusal, error text explains the refusal, and completion exposes the baseline return with Results focus. Narrow content stacks without horizontal overflow or overlapping controls. Existing independently scrolling Findings lists are preserved. No comparison metrics or candidate data appear.

| State | Desktop SHA-256 | Narrow SHA-256 |
|---|---|---|
| Ready | `f55c40113e4e5f6b9f09aecd55a64e910bb20afd6ad6897dc6f1952c3fad2a3f` | `cad039f35456003e97169f3c8587f00e16ad4c9c199e83b45ae7c7a87290bee5` |
| Pending | `749def494244ef32b040488d572d23ecf8765d191f56fc1374a6c00010e79a87` | `3f6aa53d42706ab54ed912271a04a986b600f4140575beab351f8afb691f98df` |
| Error | `28835c7c605c57079a90812d497a097f0423892448be6261ba712e449756fff8` | `88350c0249c3d4fe86050e1482b273b0a4c429596cf6d646b86095eadfa3cde4` |
| Complete | `ac2b9387d16517b9c743f605b312b3cca1497b5a803ea3a4628ca47331a980cc` | `943cc25710a3ec9443e9107fbc3ad8d466a0807ec89bd542d85c7733a18dc61b` |

Built `index.html` SHA-256 is `8450a66fc524276e6072288759fd9a60eed30e93ae5040b0edc6eadaf7c36e70`; `index-BCZj4GE9.js` is `1a629bfcd08092e19b6edb366b23dda76345fe310f163b24448985b1c90a751d`; `index-uH-93l04.css` is `cd4d9b768877c1977f68c54de6a5c75647db0d79b9754631d6f4d0ab9ff97650`. Source identity is D-GREEN-02; protected tests are D-RED-ACCEPT-03. Fresh critical review still gates D acceptance and E.

### M501-D-REVIEW-01 — Preview eligibility isolation correction

Fresh critical review returns REVISE for one Minor, D-R1, with no Blocker or Major. Five independent browser ownership probe groups pass and all eight visual artifacts match. The baseline generation heading count is 1 where the expected count is 0. The same reviewer retains the in-memory expected-zero probe for the sole correction checkpoint.

Independent browser probing reproduced a presentation defect after otherwise passing UUID exception/reentry, callback-accessor unmount, invocation unmount and stale preview-callback checks: an unprocessed baseline with no generation history displayed the generation section after the later run retrieved guidance for the same within-run Finding ID. App passed the active later continuation's eligible Finding ID into the historical view. No mutation control or dispatch escaped. Primary inspected the exact source and accepts this as a violation of I8/I9's isolated baseline snapshot; the same review owns its complete verdict and recheck.

The remaining conditional Green attempt 3 is justified by material new evidence after Green 2's genuine mode-preservation progress. Its different bounded correction must stop active eligibility from entering preview while preserving captured historical generation presentations and the active later continuation. Existing eight-file scope, frontend owner, contract and protected test hashes remain unchanged; no Red attempt or test-budget reset is authorized. The independent failing browser witness supplements the already accepted behavioral boundary; the reviewer will rerun that witness after correction. Existing focused UI/generation tests, strict/build and affected visual identity must be refreshed. This consumes the final Green allowance and the sole review correction loop when dispatched; no further write turn can be inferred.

The bug-index rule keeps this routine in-task correction in the owning plan; no separate bug record is necessary. E remains gated until the full D correction review passes. Earlier source/build-bound D execution and image evidence becomes historical after the edit, not proof of the corrected version.

### M501-D-GREEN-03 — Preview correction and refreshed evidence

Only App's generation eligibility projection changed: `!preview` prevents the later continuation from supplying eligibility to historical rendering. The continuation and captured generation presentations are untouched. Primary inspected the exact one-line correction, protected hashes and terminal lease. Cohesion is RETAINED; no Refactor occurred. Final Green lease `M501-D-green-3`, contract `56e93238545d2d31b8d936cd74f596700d3629570f560397ff0f1a67ae093a37`, closed compliant with receipt `b159c34401c2324e47cb75d87146e701ecd9385fa2e2a53cfbe41a37c7aa3a80`.

Fresh UI 15/15, unchanged generation UI 8/8, worker strict and client build pass, with zero skipped/cancelled/todo. Primary independently passes strict on the corrected source. Relevant tree is 172 files, `d70bff1acb93b40496b8fc0f4b7ff4621a7baa2c3150c9642fcb6c09456d89e7`; fingerprint construction sorts unique paths with PowerShell `Sort-Object -Unique`, hashes raw bytes in lowercase hexadecimal, joins `path:hash` lines with LF and hashes UTF-8. App is `93da29d7463d7a48cb369ffe93b780b221c3b510f868f15402522d5cfef0f9ef`. All three protected test artifacts remain D-RED-ACCEPT-03's exact bytes.

Current build: HTML `332f507ddc4e72d017f9082bc15483a851d1ca8fa02f83a63437ffde588c7b71`, `assets/index-X1U9FJGF.js` `12cf9b99eaa4cfc2a4dfabebc44c0c98ca3cabbe0bde2febe8786695c7925959`, unchanged CSS `cd4d9b768877c1977f68c54de6a5c75647db0d79b9754631d6f4d0ab9ff97650`.

After verifying absolute ordinary ancestry, no reparse points, single-link files and all eight prior hashes, primary removed only the eight stale owned PNGs and retained their directory. Fresh capture on corrected source passes 15/15 with all required axe/keyboard/focus/announcement/overflow checks. All eight new PNG hashes are byte-identical to D-VISUAL-01, so its actual visual inspection applies to these freshly rendered identical images. Source/build provenance is this corrected checkpoint. Capture flag restored absent; all scratch is empty and owned teardown succeeds. The reviewer's exact D-R1 expected-zero witness and complete correction review still gate D acceptance. Red, Green and review-correction write allowances are consumed; no reset is implied.

### M501-D-ACCEPT-01 — Accessible rescan accepted

The same independent critical reviewer returns PASS after the sole correction loop, with no open Blocker, Major or Minor. Its retained browser probe exits 0: D-R1 now observes zero generation headings in the unprocessed baseline preview, while later eligibility remains. UUID failure/reentry, accessor and invocation unmount, and stale preview callbacks also pass. It independently verifies the corrected tree, protected tests, current build, all eight refreshed identical PNGs and compliant Green 3 receipt. Source cohesion remains RETAINED. Primary accepts the actual corrected source, evidence and complete verdict; E may now proceed. All three scratch directories are empty, owned teardown succeeds, and no external request or page error occurred.

### M501-E-PACKET-01 — Integrated verification binding

Primary owns E with TDD not applicable: no application change is planned; replacement evidence is actual built-client/scanner/service/disk execution and full regression. The exact current README preparation and sequential 37-file suite are authoritative: 23 browser-free files, five module-mock files, three scanner/integration files and six UI/checkpoint files. Module-mock files use `--experimental-test-module-mocks`; every file uses `--test --test-timeout=120000`. Browser files use their existing scanner or UI scratch through `Invoke-M105Command`; all three capture flags remain absent. No concurrent service/browser test is allowed.

The accepted `tests/rescan-integration.test.ts` executes once within the complete suite, using the unchanged walking-skeleton harness and exclusive `temp/m105-integration/m501-rescan`. Frozen populated and zero fixtures are intercepted at the controlled target boundary. Expected evidence is a Local baseline and explicitly selected Groq later run, two real managed scanner launches, one POST per run, complete three-rule coverage, validated disk records and exact closed-response equality, unchanged baseline bytes, and no raw/candidate/comparison field or model call. Existing helpers own exact child cleanup and handle teardown; scratch parents and original runs remain untouched.

Source/build/test identities are D-GREEN-03. Node 24.20.0 and Chromium revision 1234/version 151.0.7922.34 remain unchanged. Independent strict and build are fresh for those exact unchanged bytes and are reused at closure unless a new change invalidates them. Every scratch parent must be ordinary and empty before/after execution. The frozen manifests and all thirteen references pass current byte-hash checks; a preliminary case-sensitive hexadecimal comparison was corrected to normalize digest case, with no input changed. Different fresh integrated critical review over I1–I10, documentation reconciliation and final checks still gate Complete/archive.

### M501-E-VERIFY-01 — Complete regression and controlled integration

Primary ran every current README suite file sequentially through its exact command preparation/wrapper, with capture flags absent and no competing browser/service. All 37 files exit 0: **776 tests pass, zero failures, cancelled, skipped or todo**. The source/test/config tree and build remain D-GREEN-03; independent strict and build evidence for those unchanged bytes remain current.

| Group | Files | Passing tests |
|---|---|---|
| Browser-free contracts, repository and services | 23 | 527 |
| Module-mock API/admission | 5 | 41 |
| Scanner and built-client integration | 3 | 121 |
| UI and synthetic review checkpoint | 6 | 87 |

`tests/rescan-integration.test.ts` passes its one controlled real-scanner integration in 3752.0913 ms. The built UI first publishes an unprocessed Local baseline from the populated frozen fixture, then explicitly submits Groq for the later zero fixture. There is exactly one ordinary POST and one rescan POST, two managed scanner launches and two controlled target navigations. Both disk records pass `validateRun`; IDs differ, the later immutable baseline reference and requested page match, all three rule coverage entries exist, and the baseline bytes remain identical. The closed HTTP success equals the exact validated durable later run. Persisted and HTTP structures contain no candidate/raw/comparison/delta/score fields; the rendered later result has no comparison output. No model or external target request occurs.

This uses the real built client, same-origin service, scanner and disk with test-boundary interception of project-owned fixtures; it is not a new public-page or provider observation. Existing A/B/C/D negative, failure, provenance and ownership evidence covers complementary adverse paths. All three scratch parents are empty after every suite and at completion; owned services/listeners/browser contexts close through their existing helpers. No original retained run, frozen evaluation input or dependency changed.

Documentation preparation reconciles README capability/API/37-file commands, UI presentation and navigation, lifecycle implementation limits, and the documentation task route. Nine changed Markdown documents passed 606 local path/anchor and encoding/whitespace checks before the latest evidence additions; all PowerShell blocks parse and the exact 37-file inventory exists. Final post-review checks and status/archive/link reconciliation remain required. No requirement status, ADR, dependency, provider qualification or later task is changed. Different fresh integrated critical review still gates task completion.

### M501-FINAL-01 — Integrated review and task closure

A different fresh integrated `critical_reviewer` returns **PASS** across I1–I10 and the changed-surface quality baseline, with no Blocker, Major or Minor. It inspected the cumulative production/test/documentation changes, confirmed D-R1's correction and RETAINED cohesion, and independently passed twenty in-memory cases through production preparation, shared lifecycle, HTTP framing and client request ownership. Both modes, zero/nonzero success, creation/publication failures, wrong lineage, cleanup uncertainty, shutdown, deadline suppression and lost durable-success replies pass with zero remaining timers. These probes created no files and performed no service/browser/provider operation.

The reviewer independently verified the exact D-GREEN-03 source/test/build identity, all sixteen digest-valid compliant lease contracts/receipts, absent active lease, both frozen manifests and thirteen referenced hashes, eight current PNG hashes and their visuals, six parsed README PowerShell blocks, exact 37-file test inventory, empty scratch and passing whitespace checks. It reused the identity-matched 776-test regression, controlled real integration, independent strict/build and accepted slice evidence without redundant broad execution.

| Invariants | Decisive accepted evidence |
|---|---|
| I1 scope, authority and freeze | G/entry records; current manifest/reference checks; final independent identity audit |
| I2 immutable linkage; I3 reserved downstream-independent admission | A contract/repository/service/API verification and critical review; complete E regression |
| I4 fresh run and mode | A/C/D checks; E built-client Local-to-Groq scan and canonical readback |
| I5 selected native candidates; I6 validation, cleanup and nonleakage | B native positive/negative and actual-reference tests; E closed HTTP/disk proof; final critical boundary probes |
| I7 truthful failures and retirement; I8 correlation and locks | A/C/D critical probes and tests; corrected D-R1 reproduction; final twenty cross-boundary cases |
| I9 accessible intent and preview return | D's 15 UI tests, eight visual states, keyboard/axe/focus/announcements and complete critical correction review |
| I10 integrated closure | E's 37 files/776 passing tests, strict/build, exact owned cleanup, final independent PASS and documentation checks |

Primary accepts the actual final diff, source responsibility placement, protected test boundary, lease results and complete independent verdict. Documentation impact: updated README capabilities/API/commands; lifecycle and UI implementation descriptions; task routing; roadmap status; plan/progress indexes and this task's evidence/progress records. No requirement status, ADR, dependency, toolchain, evaluation input or later-task authorization changed. Before status/archive reconciliation, all nine changed Markdown documents passed 609 local path/anchor checks, strict UTF-8/no BOM/final newline and trailing-whitespace checks; all twelve new source/test files passed the same text checks; `git diff --check` passed. All six maintained PowerShell blocks parse and their suite inventory exactly equals the 37 current test files. Archive dry-run rebasing preserves every relative evidence target. Post-archive checks pass for all ten changed Markdown documents and 677 local paths/anchors, strict UTF-8/newline/whitespace, the final status mirrors (twenty-one Complete, seven Not started, no active task), unchanged source/test/config identity and HEAD, absent capture flags/lease, empty scratch, and unchanged frozen manifests/references. The plan archive rebased twenty-four internal relative links and repaired all referring navigation; historical assignment packets retain their original then-current path as execution history.

Verification and the documentation closure gate pass; M5-01 becomes Complete and this plan is archived with repaired navigation. The roadmap has twenty-one Complete tasks and seven Not started, with no active task; M5-02 is unselected. Source/test/config identity remains D-GREEN-03. Test-owned services/listeners/browser resources are closed and all three scratch parents are empty. The eight ignored application screenshots and built client remain as local evidence/output. Original retained runs and frozen artifacts were not mutated. HEAD and branch remain unchanged; task changes are uncommitted and nothing was pushed.

Limits: controlled fixtures and the recorded Windows/Chromium configuration establish this task's integration, not actual public-page/provider, cross-platform or general support qualification. All 200% zoom tests remain Deferred and spoken screen-reader behavior was not tested. Unknown outcomes intentionally remain locked without automatic recovery. Matching, comparison outcomes, history and retained-run reopening remain later work.

### M501-G-RESEARCH-01 — Read-only evidence synchronization

Both required critical researchers returned **RESEARCH COMPLETE** against unchanged application source at execution HEAD. The lineage report's L1–L7 evidence covers lifecycle/ADR authority, the three immutable transition checks, service reservation/publication, settled retrieval continuation versus actual ownership, existing request/API patterns and UI seams. It recommends a dedicated rescan admission method with one shared scan lifecycle, exact client-allocated new run identity under existing create-only storage, baseline requested-page navigation, and explicit unknown-outcome ownership. These are proposals pending synthesis and both research reviews.

The native report's NC1–NC7 evidence covers the same authority boundary, [pinned axe 4.13.0 result semantics](https://github.com/dequelabs/axe-core/blob/v4.13.0/doc/API.md#results-object), installed-source/in-memory aggregation checks, actual-reference capture, whole-report validation and current cleanup tests. Empty native aggregation produced an inapplicable rule with no element nodes; a true `has-alt` check produced one passed node. No browser was launched. The report recommends selected-rule passes only, detached existing allowlisted facts, and a private post-cleanup execution envelope discarded by the service adapter. Synthetic nonzero inapplicable coverage compatibility remains unchanged; no inapplicable node becomes a candidate.

The primary reconciled command ownership with the existing scanner's enforced `temp/m103-scan` scratch path, existing UI/integration harnesses and sequential README commands. Source, fixtures, corpus, evaluation and original retained runs remain unchanged. Both reports' first passes are consumed; neither follow-up is consumed.

Mandatory `decision_analyst` returned **DRAFT READY** after receiving both complete compact reports and inspecting the current seams. It confirmed I1–I10 decide-now coverage and left runtime results to downstream proof. It resolved the client release matrix and preserved one immediate read-only baseline presentation snapshot without restoring generation continuation or consumed request capabilities. The fresh pre-draft reviewer is active; no pre-draft PASS, authored literal contract or accepted G gate is claimed yet. The analyst's one correction remains unused.

The fresh pre-draft reviewer subsequently returned **REVISE**, with one Major and no Blocker or Minor: I3/I4/I7/I8 did not define retirement of previous settled service capability after successful rescan publication. `retrieval-operation.ts` retains a supported snapshot and blocks unrelated retrieval; `generation-operation.ts` can retain an owner after a known unsaved outcome; `App.tsx` clears its historical owner only when generation presentations exist. Ignoring those flags at admission alone would leave the new run's downstream actions blocked. All other invariant dimensions were satisfied for drafting. The reviewer independently reproduced the native aggregation evidence, checked unchanged source/manifest, absent lease and passing whitespace validation. The same lineage researcher receives its sole bounded follow-up to resolve the transition point, failures, lost reply and return semantics. Drafting and implementation preflight remain stopped until corrected synthesis and the supported pre-draft correction pass.

The lineage follow-up returned **RESEARCH COMPLETE**, adding L8–L10 source evidence for the retained owners and durable publication boundary. It proposes service-private retirement of settled capabilities only after successful durable rescan publication, before reservation release; refusal and failure preserve them, and lost success replies cannot promise their survival. It also proposes retaining the later run as the active workflow while showing one read-only immediate baseline preview, avoiding capability resurrection during return navigation. The primary sent these changes and all I1–I10 interfaces to the analyst's sole bounded correction. No new research identity or implementation allowance was created.

The corrected analyst returned **DRAFT READY** and the same pre-draft reviewer used its supported correction checkpoint to return **PASS** across I1–I10, with no Blocker, Major or Minor. Both correction allowances are consumed. The primary authored M501-G-CONTRACT-01 from that corrected synthesis; a different fresh final artifact reviewer must accept it before implementation preflight.

### M501-ENTRY-02 — Execution entry

The owner authorizes only M5-01 through G and sequential A–E, bounded corrections and documentation closure. Current HEAD is `0cbbf26bdf6facba647f14ece0580fef12bffb28`; the entry worktree was clean and `logs/agent-flow-leases/v2/active.json` absent. The current permission profile allows workspace writes; `.codex`, `.agents` and `.git` are read-only, so no workflow configuration or Git mutation is contemplated. Guard runtime output remains under the existing ignored workspace logs root.

The M4-03 and M3-03 final acceptance records were inspected; both prerequisite tasks remain Complete. Selected requirement rows are Accepted, with Deferred zoom verification and later comparison work kept outside scope. Node `v24.20.0`, Python `3.12.10`, installed Chromium revision `1234`, unchanged package lock SHA-256 `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`, and the maintained README preparation are available. Independent `node_modules/typescript/bin/tsc --project tsconfig.json` passed through `Invoke-M105Command`. Scanner, UI and integration scratch directories were empty.

The scanner manifest remains `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459`; the generation manifest remains `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`. All thirteen generation-manifest referenced hashes pass, and `git diff` shows no fixture, evaluation or corpus changes. The strict check and hash walk were read-only; no public scan, model call, retained-run write or browser launch occurred. This entry supersedes planning-only execution limits without accepting G's unresolved semantics.

Keep planning and future G/A/B/C/D/E acceptance evidence in this one plan, with concise manual progress checkpoints. Do not add another schema, decision ledger, telemetry record or generated report. Full target content, private runs, credentials and ignored historical material do not enter tracked fixtures or documentation.

Planning documentation impact: this plan, roadmap planning activation, plan index, progress index and one concise M5-01 progress record. No source, test, dependency, runtime, requirement or ADR change is made by planning. Future closure updates only materially affected owners.

### M501-PLAN-01 — Accepted planning readiness

On 2026-09-16 15:35 UTC, a fresh independent `critical_reviewer` returned **PASS** for readiness to enter G after execution authorization, with no Blocker, Major or Minor finding. It reviewed the complete plan, all five planning paths, controlling authorities and critical service/transition/scanner/UI seams against HEAD `d72797e55883f58544af1c74da9f80d15e1afd75`. Reviewed plan SHA-256 was `7a525a0361fab5690b110aeb8746e5423a56ae24ec348a08906431b76f2573ce`, before this living-state/verdict maintenance.

The reviewer independently confirmed no active lease, only the five intended paths changed and `git diff --check` passed. Primary accepted the actual documentation diff and verified sixteen required sections, one PowerShell command block, local Markdown paths/anchors, strict UTF-8/no BOM, final newline and trailing whitespace; final record/link maintenance receives the same checks. Fresh strict TypeScript, 73 passing contract tests and unchanged manifest/reference evidence remain in M501-ENTRY-01; the reviewer did not rerun those tests, and no new full-suite or browser proof is claimed.

Exact schema/API, candidate categories/lifetime mechanics, deadlines and interaction literals remain G obligations. M5-01 remains In progress for planning only; twenty tasks remain Complete and seven later tasks Not started. No runtime implementation, target/model call, retained-data mutation, commit or later-task selection occurred. Documentation impact is limited to the five named planning/status/navigation files.

## Interfaces and Dependencies

Retain Node/TypeScript, React, Playwright/axe, the loopback service and single-file repository at current pins. New interfaces are limited to optional immutable baseline provenance, one service/API rescan intent, its client admission/control and a scoped scanner-internal candidate handoff. Exact signatures and result/error literals are accepted in M501-G-CONTRACT-01 through M501-G-ACCEPT-01. Existing public scan collections and provider boundaries remain unchanged; no comparison result type is introduced here.

## Revision Note

2026-09-16: completed G and sequential A–E under guarded separate ownership, preserved bounded correction history, verified 776 tests and controlled real integration, accepted fresh critical slice/final reviews, reconciled documentation and archived after closure. M5-02 remains unselected.

2026-09-16: created for owner-requested M5-01 planning after M4-03 and M3-03 completion. Recorded current verification, missing rescan seams, immutable lineage and transient-only candidate gates, separate worker ownership, reuse-first UI and proportional proof. No execution or later milestone is selected.

2026-09-16: corrected scanner path references and recorded independent critical planning PASS, living-state reconciliation and five-document validation without changing future execution authority.

2026-09-16: recorded owner execution authorization, current clean-HEAD entry and prerequisite/freeze checks, the frozen bounded research alternatives and both read-only evidence reports. G remains pending mandatory synthesis and fresh reviews; no implementation is claimed.
