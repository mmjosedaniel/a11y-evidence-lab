# Integrate and verify human review

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M4-03](../../DEVELOPMENT_ROADMAP.md#m4-03--integrate-and-verify-human-review), Complete after owner-authorized G/A/B/C execution and verified documentation closure. Execution authorization and the three subsequent human proposal decisions are recorded separately below.
- **Latest evidence:** [M403-FINAL-01](#m403-final-01--integrated-review-and-task-closure) accepts all three human-authorized saved decisions, independent critical PASS, preserved originals/siblings, bounded actual desktop/narrow visuals and exact cleanup with retained evidence.
- **Pending:** None for M4-03. M3-03 remains independently Blocked on capacity; M5/M6 remain Not started.
- **Active lease:** None. All three worker leases closed compliant. All three final-POST allowances, the extra A review and supplemental observation are consumed; no resubmission is permitted.
- **Next:** No follow-on task is selected. Preserve this archive and local evidence; all 200% zoom testing remains Deferred until after MVP.

## Progress

- [x] (2026-09-14 20:26Z) Review current state, M4-02 closure, task authorities and review/browser seams; strict TypeScript and 16 focused tests pass. Both retained actual sources validate and match their historical hashes; M403-ENTRY-01.
- [x] (2026-09-14 20:41Z) Accept the planning draft after independent critical PASS with no findings and five-document validation; M403-PLAN-01. No execution, source-case preparation or human decision occurred.
- [x] (2026-09-15 UTC) Receive owner execution authorization and inspect clean intentional HEAD `038da58f467891d50905256b5847ca7f79d58049`; begin G's bounded critical research. No case preparation or POST occurred.
- [x] (2026-09-15 UTC) Accept G's corrected evidence/case/command contract after required research, analyst synthesis and separate fresh pre-draft/final research PASS; M403-G-ACCEPT-01.
- [x] (2026-09-15 UTC) Accept A's bounded test-only caller after five passing characterization tests, strict/build, compliant leases and cumulative critical PASS; M403-A-ACCEPT-01.
- [x] (2026-09-15 UTC) Complete B's three human-authorized decisions, blocked acceptance, abstention-no-review, application-owned readback and bounded accessibility evidence; M403-B-EDIT-OBSERVATION-01.
- [x] (2026-09-15 UTC) Accept C's full verification, different fresh integrated critical PASS, exact cleanup and documentation closure; mark M4-03 Complete and archive this plan, M403-FINAL-01.

## Surprises & Discoveries

- [M4-02 final acceptance](m4-02-accessible-review-ui.md#m402-final-01--integrated-review-and-task-closure) records all 706 tests in 31 files, current strict/build, all three synthetic browser-to-service-to-disk decisions, a committed lost-response case, final desktop/narrow evidence and integrated critical PASS. Recreating those tests with another synthetic fixture would not establish M4-03's real-proposal human-review checkpoint.
- There are two known real pending proposals from M3-05, one per provider, both for `color-contrast`. Each aggregate has 35 Findings and preserves its siblings. Three mutually exclusive final actions cannot be applied sequentially to one canonical pending proposal. This plan therefore gates isolated evidence branches rather than resetting decisions or silently ordering new generation calls.
- M3-05 records semantic limitations in the actual proposals. In particular the Local proposal's blocking-judgment prose contains `false`; this is not a human finding that judgment is complete or unnecessary. Mechanical validation never justifies automatic support confirmation or selecting an acceptance outcome to satisfy a test.
- M4-02 omitted native 200% testing at explicit owner direction [for that task only](m4-02-accessible-review-ui.md#m402-b-visual-02--final-focus-proof-and-owner-directed-zoom-omission). It did not pass zoom or amend global accessibility requirements/M6 scope. Preserve that history; G must bind M4-03's applicable visual evidence without treating the omission as a global waiver.
- During this execution the owner first stated that 200% zoom is unnecessary, then explicitly deferred every 200% zoom test until after MVP. The [REQ-A11Y-006 amendment](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md#200-zoom-verification-deferral--2026-09-15-utc) now controls all MVP tasks, including the former M2-03 carry to M6-03. Desktop/narrow visual and automated accessibility, keyboard, focus and announcement checks remain required; no zoom pass is claimed.

## Decision Log

- Decision: defer every 200% zoom test until after MVP under the owner's subsequent explicit scope amendment. Reconcile the controlling verification requirement, ADR method reference, roadmap and current UI/plan references; preserve historical evidence and accessible behavior requirements. Remove M4-03's headed/native-control procedure; retain desktop/narrow actual-content proof and all other review gates. Date/author: 2026-09-15 / primary.
- Decision: execute only M4-03 through its existing gates and budgets under the owner's attached execution request. The intentional entry HEAD is `038da58f467891d50905256b5847ca7f79d58049`, with no dirty paths before primary plan maintenance. Historical planning HEAD remains evidence, not an equality gate. No semantic confirmation, new upstream call, Git write or later task is authorized. Date/author: 2026-09-15 / primary.
- Decision: select only M4-03 planning. Rationale: the owner explicitly requested this plan and Complete M4-02 satisfies the prerequisite; no actual review is implied. Date/author: 2026-09-14 / primary.
- Decision: plan an evidence-first checkpoint, with no application changes by default. Rationale: transport, validation, persistence and accessible interaction already exist. Extend only a demonstrated missing test-side verification capability; a production defect requires a bounded correction contract. Date/author: 2026-09-14 / primary.
- Decision: preserve retained originals and plan finite isolated branches from authentic proposal bytes, subject to G's provenance/interpretation freeze. Rationale: independent review outcomes must not overwrite one another or manufacture new model executions. Copies are controlled review branches, not new real generation runs. Date/author: 2026-09-14 / primary.
- Decision: separate product-owner semantic decisions from agent verification. Rationale: a research/review agent may inspect evidence or recommend wording but cannot stand in for the required person or infer their confirmation from task execution authorization. Date/author: 2026-09-14 / primary.

## Outcomes & Retrospective

M4-03 is Complete. Three independently isolated human decisions passed the real review endpoint and durable readback, preserving the authentic original and every sibling. The complete owner-adopted edit and final display passed bounded desktop/narrow inspection. Different fresh critical review and exact cleanup passed; all evidence remains retained. The test-only caller adds no application behavior. The screenshot height artifact was corrected under the bounded owner amendment; the earlier evidence failure remains visible. Human decisions should be requested together in plain language once a concrete packet is ready. No capacity, generation-quality, website-remediation or support qualification is implied. All 200% zoom tests remain Deferred until after MVP.

## Purpose / Big Picture

Demonstrate that a person can inspect an authentic validated proposal, resolve or reject unsupported claims, apply a valid blocking judgment, and save one final decision through the current browser/service path. Verify approve, edit-and-accept and reject as independent outcomes, prove invalid acceptance is blocked and abstention has no review action, and validate durable readback without altering original evidence or siblings. This is a review-transition checkpoint, not a new model-quality evaluation or an automatic ground-truth label.

## Context and Orientation

Use the [authority map/task router](../../README.md#read-by-task), [global requirement semantics](../../PROJECT_REQUIREMENTS.md) and the owning roadmap row. Selected authorities:

| Authority | Checkpoint obligation |
| --- | --- |
| [BHV-05 and compact review portion of REQ-EVAL-002](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md) | Individual human decision; review transitions observed separately from generation groundedness, retrieval quality or other dimensions. Shared review checks are provider-independent and add no generation execution. |
| [SPEC-005](../../specs/SPEC.feature), [HS-010](../../specs/HARD_SPEC.feature) | All three final actions, material-support and blocking-judgment acceptance gates, original/edited distinction, non-blocking reminder, action/time and no sibling/page-level decision. |
| [REQ-REV-001, 008, 009](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#human-review-and-manual-checks) | The controlling Accepted semantics behind those examples: one person, one current decision, complete edited proposal only for edit-and-accept, bounded optional note and no per-claim/history machinery. |
| [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md), [lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md) | Validated application-owned single-file evidence; immutable original/context, final-decision integrity and last-valid-file truth. OD-026 still defers user-facing run reopening. |
| [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md), [M4-02 presentation](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m4-02-selected-finding-review), [ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md) | Automated keyboard/focus/announcement checks and manual visual review of the existing source-separated interface; browser presentation is not durable authority. No screen-reader speech or support-matrix claim. |

All task-applicable Must semantics are Accepted; deferred reopening, release and post-MVP behavior remain excluded. Entry has eighteen Complete tasks and M3-03 Blocked. This activation makes M4-03 In progress and leaves eight later tasks Not started. M4-02's 706-test closure is accepted historical evidence, not a fresh full regression in this turn.

### Current implementation and reusable evidence

`src/server/domain/review-contract.ts`, the review operation/repository transition and `src/client/finding-review-admission.ts` already enforce one request-correlated final decision. `/api/finding-review` is the real same-origin endpoint used by `src/client/main.tsx`. The browser uses a 30000-ms local deadline, no automatic recovery read or retry, and retains the mutation block for an unknown save. A primary readback for evidence/recovery is not a new user-facing reopen feature.

`ProposalReviewForm.tsx`, `ProposalEditor.tsx`, `ReviewDecision.tsx`, `ProposalContent.tsx`, the shared status region and existing selection/focus coordination are **REUSE_AS_IS**. So are native evidence, authenticated guidance/citations, final labels and current CSS. No rendered change or new component is planned. The skill applies to evidence inspection; if a visible defect requires correction, first freeze its frontend-visual packet and reuse disposition.

`tests/finding-review-ui.test.ts` and `tests/helpers/m402-review-browser.ts` already exercise built UI → actual review endpoint → actual service → disposable disk. They stage earlier scan/guidance/generation responses to reach the pending proposal; only review reaches the actual mutation path. They currently synthesize proposals and automatically select test confirmations. Reuse their mechanics and exact cleanup approach, **not** their synthetic content or automatic confirmations as human evidence. `m401-review-sandbox.ts`, review fixtures and the existing UI harness provide test-owned boundaries; no general evaluation harness is needed.

### Authentic source candidates

These identities are already recorded in the M3-05 archive. Entry read-only validation confirms each original is still complete, has one pending `color-contrast` proposal, no decision, and 35 Findings. Content was not copied into tracked planning documents.

| Source | Original aggregate and selected Finding | Entry SHA-256 |
| --- | --- | --- |
| Local, [H observation](m3-05-generation-checkpoint.md#m305-h-observation-01--successful-local-diagnostic-observation) | `data/runs/run-b785db0b-5ed0-4baa-b62f-9be6181d5e72/run.json`; `50dcefee-7e74-4253-a373-c5bbf2ab839b` | `df21550331a276095d3d5780eec301d88285af2f91fcc4414a37848a830726aa` |
| Groq, [corrected observation](m3-05-generation-checkpoint.md#m305-f-observation-02--corrected-local-failure-and-groq-proposal) | `data/runs/run-d4d06856-39d7-4196-b607-8e2f2815b302/run.json`; `f0a4f4ff-5beb-4676-9055-0ea763523e70` | `61eb5b6fccc59083850ea147838a1049ab42090e75fc82a607c5874663d5960b` |

G's drafted contract selects the Groq source for all three controlled branches, subject to final G acceptance and A caller verification. Neither source is semantically approved. G selects only the minimum source binding; it does not repeat review once per provider or require three rule profiles. If the person cannot honestly approve an unchanged original, the approve case remains unfulfilled. Do not modify that original, pressure the reviewer, fabricate support or silently substitute an edit under the approve label. Complete unaffected proof and request the needed owner direction without automatically generating another candidate.

## Scope and Non-Goals

Include source/proposal provenance, bounded isolated review cases, the three actual human-authorized final actions, blocked acceptance, no-review abstention, immutable-original/sibling checks, application-owned durable readback and proportional accessibility/visual proof. Preserve the fixed review API/shape, source bytes, invocation versions and evidence hierarchy.

Exclude new product behavior by default, new scans/retrieval/generation, credentials or acquisition, changing any source under `data/runs`, model/corpus/prompt changes, the six generation-evaluation executions, provider ranking, capacity, statistical scoring, support qualification, new IDs/fields in canonical runs, approval history, audit graphs, per-claim records, automatic semantic judgments, run reopening, comparison, source-code remediation, new libraries/frameworks, screenshot services or telemetry. Copies and test-side staged navigation must never be reported as fresh scan/retrieval/generation or three independent model outputs.

## Plan of Work

Follow the [agent workflow](../../../.codex/README.md), [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), [write guard](../../../.codex/write-lease-guard.md) and visual evidence rules in the [frontend-quality skill](../../../.agents/skills/frontend-quality/SKILL.md).

The primary owns authority, evidence interpretation, human-input requests, integration, status, documentation and lease open/terminal close. Research/review roles remain read-only. Any ordinary test/helper or production edit is delegated. One lease per worktree; primary documentation/guard maintenance occurs between leases. Inspect actual diff, complete command/environment/tree identity, cohesion and terminal receipt before handoff acceptance. Only useful independent read-only work may overlap; no parallel writers.

For A, retain one `test_worker` for read-only preflight and any passing characterization. If production behavior is already covered, reuse evidence rather than inventing Red/Green. For an actual production gap, record a bounded correction slice first: `test_worker` owns Red, separate `code_worker` or `frontend_code_worker` owns Green, accepted tests remain unchanged, and the complete packet/leases/review rules apply. No primary production patch or silent expansion follows from discovering a defect.

Writer budgets retain the workflow's one preflight, coherent characterization or Red/Green, at most three writes per unchanged role/phase chain (initial, ordinary correction, conditional final correction with required learning record), and one review correction loop. Stop dependent work on exhausted budget, changed binding fields, two repeated decisive gaps without material new evidence or two no-diff handoffs. IDs and replacement agents do not renew allowances. Primary triages within remaining authority; only actual owner-controlled decisions or expanded/exhausted authority require owner direction.

Actual B allowance, available only after execution authorization and the applicable gates: **three final review POSTs total**, one for each independently isolated action case; at most one final POST per case. Blocked UI attempts must produce zero POSTs. One controlled abstention case is provider-independent. A rejected, failed, lost or uncertain final attempt consumes its case allowance; read-only diagnosis/readback does not renew it. Existing repeatable synthetic regression remains separate and cannot satisfy or reset the human-case allowance.

### G — Freeze the checkpoint evidence and command contract

**M403-G; R3; TDD: Not applicable.** Primary-owned evidence/command decision work, no production writes or implementation lease. Replacement evidence: exact authority/source inspection, complete invariant/case contract and mandatory research checkpoints. Application responsibility change: None.

Freeze the exact proposal source(s), originals' hashes and no-review state, each case's isolated run root, copied bytes and expected selected Finding, temporary-versus-retained evidence destinations, and readback predicates before any actual-case preparation. Default preservation route is three independent byte-identical copies of selected authentic aggregates in separate exclusive test roots, keeping original run/Finding IDs and provenance unchanged. Case labels belong in this plan/local evidence, never canonical run fields. Duplicate IDs across isolated roots are controlled branches, not new application runs or proposal versions. G must verify this interpretation satisfies the real-proposal checkpoint and openly label the staged pre-review pipeline; no source under `data/runs` is writable. If another route would mutate originals, add generation calls or change the authority interpretation, stop for owner direction.

Freeze the smallest way to present copied actual inputs in the current built UI without a product reopen feature. Reuse M4-02's test-side stage replay where compatible: earlier Analyze/guidance/generation requests are intercepted and supplied explicitly identified projections from the authentic source plus its authenticated corpus view; they are not executed. The actual pending aggregate supplied to the UI and service must match the selected source exactly. Only `/api/finding-review` may pass through as a real mutation. Validate every projection and reject unexpected calls. Do not change the original evidence or reference set merely to fit a harness fixture.

Freeze one compact case table before observing outcomes: source/hash, case/root, required action, expected permitted transition, unchanged-original/sibling comparisons, no-call predicates, negative attempts, readback and cleanup. Use existing evidence for exhaustive invalid/malformed/unknown-response cases; add only a missing checkpoint witness. Do not create another generation manifest or turn public observations into evaluation gold.

**Human-input boundary:** execution authorization permits preparation, not semantic approval. Present the selected actual proposal, its cited guidance/evidence and limitations to the person. Before any final POST, the person must either operate the existing controls themselves or explicitly authorize the exact action, judgment/reason, note and complete edit for the identified proposal. Approval/edit require their support confirmation for the resulting proposal; rejection does not. Agent suggestions are proposals only. Any material change to the payload/source invalidates the prior confirmation. One concise owner input may cover all ready case payloads; do not invent per-claim records, reviewer identities or extra approval stages. A synthetic test's `supports-proposal`/checked box is not reusable human evidence.

Freeze exact A/B/C commands and effect slots, existing browser/build/runtime identity, safe ownership/cleanup and bounded visual samples before preflight or effects. The owner's subsequent global direction defers all 200% zoom testing until after MVP, including the former M2-03 carry to M6-03. Bind the current desktop/narrow review visual path and report the deferral accurately. The M4-02 omission remains separate history.

### A — Accept existing coverage and the minimum checkpoint caller

**M403-A; read-only preflight then characterization if needed; S3.** Authorities: BHV-05, SPEC-005, HS-010 and shared-review REQ-EVAL-002, with G's source/ownership contract. Primary issues a complete Milestone Assignment Packet v2. Expected classification is existing behavior with a missing authentic-source verification route; preflight must confirm instead of assuming missing production behavior.

First reuse the existing review-contract, review-admission/API, service/repository and UI suites. If they and a bounded direct procedure meet G, make no code change. If the source-bound caller is missing, `test_worker` may add passing characterization and the smallest test-side helper under a fresh lease. Candidate paths: `tests/review-checkpoint.test.ts`, `tests/helpers/m403-review-checkpoint.ts`; minimal changes to `tests/helpers/m402-review-browser.ts` only if a concrete common mechanism warrants local reuse without altering its existing synthetic tests. Freeze exact paths and tests before the lease. Never copy actual proposals into tracked fixtures.

Test responsibility is a bounded review-checkpoint driver: validate supplied source bindings, prepare exact isolated copies, stage only the agreed earlier responses, accept externally supplied human-authorized input, drive the unchanged real review route, collect validated readback and close only owned resources. Reuse `m401-review-sandbox.ts` and current browser/service primitives; do not create a generic runner, polling system or new UI/endpoint. Synthetic characterization proves the driver cannot dispatch an unapproved payload or mutate originals; it is not human-case evidence. Default application-source responsibility change: **None**. Passing characterization requires no fake Green owner.

Replacement evidence where TDD is not applicable includes structural path/provenance/no-call checks and the existing behavior's passing characterization. If preflight finds a missing/regressed application behavior, consult the [bug index](../../bugs/README.md), freeze a separate bounded standard or frontend-visual correction packet with exact source/test owners and Red target, and pause only affected observations. Do not delegate an open-ended repair or widen the current helper lease. New dependencies, significant architecture or expanded behavior need owner direction.

Advance after actual diff/test relevance/cohesion inspection, accepted focused checks/independent strict TypeScript, compliant terminal leases where writes occurred and fresh `critical_reviewer` acceptance of the exact caller and original-preservation/no-call boundary. The review includes source-bound authorization handling, not an agent's substitute judgment of the proposal.

### B — Observe the real-proposal review checkpoint

**M403-B; primary-operated bounded observation, with a person supplying final decisions; TDD: Not applicable.** No production responsibility change. Replacement evidence is the actual built UI, real review service/disk transition, human-authorized payload and corresponding validated readback. Follow G's accepted command/case freeze; no observation may invent commands or expand targets.

Prepare and validate each exclusive case root and pending copy; inspect the actual original/guidance/evidence with the person before its final input. Exercise the following provider-independent cases, not a provider/profile matrix:

| Case | Required observation |
| --- | --- |
| Approve | Person confirms support for the unchanged original and supporting judgment or reasoned N/A. One final request saves `accepted` with the exact action/judgment/note and server time; original remains unchanged. |
| Edit and accept | Person approves a complete resolving edit with valid references and confirms its support/judgment. Exactly one final request saves `edited-and-accepted`; full reviewer content is distinct from the immutable original. |
| Reject | Person chooses rejection with an honest judgment disposition and optional bounded feedback. One final request saves `rejected`; no accepted plan or acceptance confirmation/edit is fabricated. |
| Blocked acceptance | Before a final decision on a still-pending branch, demonstrate missing support confirmation and unresolved judgment, then N/A without reason. Errors are accessible, no final POST/decision occurs and the pending bytes remain unchanged. These deliberate negative inputs are test probes, not the person's semantic assessment. |
| Abstention has no review | Reuse the existing controlled incomplete-guidance/abstention witness once. Show explanation, available evidence and no provider-call/review action; no decision or actual generation occurs. Preserve its synthetic provenance rather than calling it a real-proposal outcome. |

For each final case compare captured request, real endpoint response, `LocalService.readRun` and validated on-disk aggregate. The selected decision/state must agree, generation/original/retrieval/analysis/parent and all siblings must be unchanged, and `decidedAt` must be server-owned. Preserve complete before/after hashes and minimized evidence locally. Verify source originals remain byte-identical after every case. Do not reset, overwrite or replay a completed case.

Automated keyboard, focus and shared-announcement checks cover the accepted action/selection path and existing adverse cases. Manually inspect one authentic proposal-bearing path through editor/error/final decision at the bounded desktop/narrow samples frozen in G. All 200% zoom testing is Deferred until after MVP at owner direction. Reuse M4-02's current rendering and focus evidence where identities and content coverage suffice, but do not describe synthetic screenshots as showing actual proposals. Record exactly which proof is authentic-content review, controlled negative behavior or reused regression. Do not claim unobserved screen-reader speech or whole-app/browser support.

If the person declines approval, cannot establish support or needs an unresolved manual judgment, preserve that outcome and finish unaffected cases. Do not choose a misleading N/A reason, fabricate acceptance, substitute an edited result for approve or generate another proposal. Ask only for the concrete missing owner decision or changed scope needed to finish the remaining branch.

### C — Integrated review and documentation closure

**M403-C; primary evidence integration; TDD: Not applicable.** No new production module. Run the current full authoritative suite (entry: 31 files) plus any accepted checkpoint characterization, independent strict TypeScript and the client build. Reuse unchanged evidence under its complete identity; broaden or repeat only for changed/stale evidence, failure or unresolved risk. Preserve mandatory independent risk-critical checks.

Obtain a different fresh integrated `critical_reviewer` for the complete I1–I10 packet, actual-case provenance, human-input binding, original preservation, real readback, no-call evidence, visual observations and exact cleanup. It may assess validation/provenance but cannot create a missing human confirmation. Add no independent semantic panel, inter-rater agreement or aggregate score.

Apply the [documentation closure gate](../../README.md#task-closure-documentation-gate): reconcile plan, roadmap/progress and their navigation, current overview/review-checkpoint instructions and materially affected UI verification limits. State separately the observed transitions, remaining semantic limitations and evidence boundaries. Preserve M4-02's omitted zoom and M3-05's historical failures; no retroactive qualification or M6 credit. Requirements/ADRs need no change unless an explicit accepted amendment actually occurs. Run proportional link/command/configuration/encoding/whitespace checks and `git diff --check`. Mark Complete/archive only after every task Verification item and this gate pass; do not start M5 automatically.

## Decision Review Contract

**Identity:** M403-G, R3, future checkpoint procedure freeze. **Triggers:** authenticity/identity of replayed evidence, irreversible independent decisions, exact original preservation and recovery after possible commit. **Artifact:** one accepted case/command subsection in this ExecPlan, not a new evaluation framework, ADR or ledger. **Boundary:** primary may accept compatible technical literals; person supplies semantic decisions, and only the owner can expand actual calls, mutation targets, requirements or budgets.

At most one bounded non-ranking discovery pass may resolve current caller/data/visual-mechanism gaps. Then freeze the minimal candidate procedures and evidence dimensions before comparison: direct reuse versus a small test-side adapter for isolated authentic-input branches. Existing review semantics and source content are fixed, not candidates. Hard gates are original/source integrity, honest staged-versus-actual provenance, real UI-to-service-to-disk review, one authorized decision per branch, meaningful human support/judgment inputs, no new model operations, accessible evidence and bounded cleanup. Prefer no new helper when the existing caller can satisfy those gates.

Budget: one `critical_researcher` report covering the coherent authentic-input/decision-to-disk evidence dimension, one bounded follow-up; one mandatory `decision_analyst` contract/synthesis audit plus one bounded correction; fresh `critical_research_reviewer` pre-draft checkpoint; primary-only authoritative drafting; different fresh `critical_research_reviewer` final review. No drafter or extra quality panel. Allow one pre-draft correction and at most two final-artifact correction cycles within the recorded research budget. If critical coverage cannot fit, reconcile the capsule before adding assignments; no respawn resets limits.

Project this contract and I1–I10 into Research Assignment Capsules with exact source/authority links, evidence identity and remaining budget. Synchronize before synthesis. The analyst returns exactly `DRAFT READY`, `RETURN FOR RESEARCH` or `OWNER DIRECTION`; DRAFT READY accepts neither the final artifact nor any person's proposal decision. Final review checks the complete primary-written contract and explicit pending runtime/human obligations. Repeated decisive gaps twice without material new evidence, exhausted allowance, changed authority or a missing owner-controlled choice stop dependent work. Preparing a useful inspection packet and completing unaffected checks remain authorized; no new permission ritual is needed for routine in-scope preparation.

## Concrete Steps

Working directory: `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`. Read and dot-source the maintained [README preparation](../../../README.md#development-command-preparation) in each command session. Do not replay archived task leases, setup grants, process kills, source mutations or Git constants.

### M403-ENTRY-01 — Planning evidence

At clean HEAD `ae0850df450f89715f73d3089c2778126b665414`, no active `logs/agent-flow-leases/v2/active.json` exists. On 2026-09-14 20:26 UTC, independent strict TypeScript and all **16** tests in review-contract and finding-review-admission passed with no failures/skips. A read-only Node caller used the current `validateRun` on both identified actual sources; each was complete with 35 Findings and exactly one pending contrast proposal/no review. Their hashes match the source table and M3-05 evidence. No provider call, app/browser launch, semantic review or retained-run write occurred. This is focused planning evidence, not a fresh full-suite or checkpoint result.

Additional read-only planning validation confirms the M3-01 manifest still has SHA-256 `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; all thirteen `path`/`sha256` references match their exact bytes. No evaluation input or outcome was changed.

After the README preparation, these exact repeatable checks perform no application operation:

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Resolve existing lease before continuing' }
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/review-contract.test.ts tests/finding-review-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Planning review checks failed' }
}
git diff --check
```

The entry commit is historical evidence, not an executable future equality gate. Before execution accept the then-current intentional HEAD, expected dirty paths, relevant source/test/configuration/build identities, frozen manifest/references, source-original hashes, runtime/environment, working directory and exact commands. A committed plan need not be dirty; reconcile genuine drift and affected evidence instead of requiring an old parent HEAD in receipts/closure. Preserve unrelated changes and all existing records.

### M403-G-CONTRACT-01 — Authentic review procedure

**Status:** Accepted by M403-G-ACCEPT-01 after the different fresh final research review. No case preparation, implementation lease or final POST has occurred. The critical researcher returned RESEARCH COMPLETE, the decision analyst returned DRAFT READY, and the fresh pre-draft research reviewer returned PASS. Its single correction review also returned PASS after removal of native-zoom machinery. The owner's subsequent global post-MVP zoom deferral changes the controlling verification timing, without adding a caller or effect. Research follow-up and analyst correction remain unused; pre-draft correction is consumed; final-artifact review permits at most two correction cycles. Initial final review returned FR-01 (incorrect pending-state restoration literal), FR-02 (root URL alias omitted from the asset-path wording) and FR-03 (stale living status). The artifact incorporates all three supported corrections in cycle 1; complete revised-artifact review passed.

**Selection and evidence:** Existing `m402-review-browser.ts` hardcodes synthetic content/selection and automated judgments; direct reuse cannot prove human review of authentic inputs. G-E1 is that caller/fixture inspection. G-E2 independently validates both original hashes and their 35 Findings, one pending/no-review selected Finding. G-E3 validates authentic scan/guidance projections with `validateRun`, `admitGuidance` and `admitGeneration`, and authenticates retained citations through `resolveCitations`; modified corpus bytes are rejected. G-E4 covers current review input/admission/time gates; G-E5 covers selected-only service/repository publication; G-E6 covers existing owned sandbox teardown; G-E7 covers form confirmation/reset semantics; G-E8 binds the existing static asset allowlist. The analyst and pre-draft reviewer independently confirmed that a reviewed final aggregate cannot be replayed through `admitGeneration`. Final UI evidence must therefore be captured during its original actual submission, before closing that browser.

Select the Groq source in the source table for all three cases. This minimizes source bindings and adds no provider-quality or semantic-suitability claim. Its raw 45148 bytes must be copied exactly. Protect the Local source as well; hash both before and after every actual case. Each original remains read-only and no canonical ID or field is changed during preparation.

| Fixed case | Isolated run root below `temp/m403-review-checkpoint-cases/` | Expected actual review transition |
| --- | --- | --- |
| `approve` | `approve/runs` | `proposal-pending-review` to `accepted`, unchanged original, no edit |
| `edit-and-accept` | `edit-and-accept/runs` | `proposal-pending-review` to `edited-and-accepted`, complete human-authorized edit |
| `reject` | `reject/runs` | `proposal-pending-review` to `rejected`, no accepted remediation plan |

Every root holds `run-d4d06856-39d7-4196-b607-8e2f2815b302/run.json` and selected Finding `f0a4f4ff-5beb-4676-9055-0ea763523e70`. Create both fixed top-level roots exclusively and refuse pre-existing/ambiguous preparation. The evidence root is `temp/m403-review-checkpoint-evidence/`; each fixed case has its own subdirectory. Retain source bytes, inspection packet, exact decision input, attempt marker, request/response/readback and screenshots locally there; no actual proposal bodies enter tracked tests or documents. Ordinary-path checks cover resolved ancestors, reparse points, realpath aliases and multiply linked files. Copies use raw Buffer and exclusive creation, never parse/reserialize as source preparation. Synthetic characterization uses separate disposable roots and cannot consume or renew these real-case allowances.

**Minimal test-side responsibility:** CREATE `tests/helpers/m403-review-checkpoint.ts` for this finite checkpoint's source binding, stage projections, route gate, exact human-decision submission, readback and owned cleanup. CREATE `tests/review-checkpoint.test.ts` for source-independent synthetic characterization of those boundaries. Reuse existing exported validators, citation resolver, service, browser runtime and fixture builders as-is. No production responsibility or interface changes, no generic harness, no new dependencies and no permitted production refactor. Internal named functions keep source preparation, browser routing and decision/readback responsibilities explicit; import is inert. Existing M4-01/M4-02 files stay unchanged.

**Pinned baseline:** intentional HEAD `038da58f467891d50905256b5847ca7f79d58049`; expected dirty paths are primary documentation amendments visible in Git status before A. Relevant source/test/configuration fingerprint `1aa3bf781b7056ba8ce4612a9d080a6935b34e473dc292b863c5f736d8f6cb47` hashes the case-sensitive sorted `git ls-files src tests package.json package-lock.json tsconfig.json vite.config.ts` inventory as normalized relative path, space, lowercase SHA-256 and LF per row. Record a new fingerprint after accepted helper writes. No unrelated drift is allowed. Node is `C:/nvm4w/nodejs/node.exe` v24.20.0, Playwright 1.62.1, retained Chromium 151.0.7922.34 in `m104-browser-runtime/browsers/chromium-1234`. No acquisition is authorized. Build inventory is exactly `index.html`, `assets/index-B8eBTP4N.js`, `assets/index-CiL3UJmL.css`, with SHA-256 respectively `53657fc7841c6539d4146e4499dc700cb5f345736c5a4eae7c71ff56a4914e32`, `0dfd983238ffcfb0c2b0bc868605b293fe905cbb2eb2a8668686f8252235d41e`, `73aadc60be67e8fa9af73a071adcaf3d3fa677a9b6bdf9ac68ae085c0c1f5e8e`. C may regenerate this build with the frozen command; reconcile its actual asset identity before subsequent browser evidence.

The unchanged generation manifest is SHA-256 `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b` and all thirteen path/hash references match. Raw corpus manifest and passages hashes are `87d8867ed1138bfb38e6c44eed67a79b0a43bdaf2532a056afafaed828202da9` and `29ea0033e825a0fa20430aa42e9718b87a87aa59edadda0a3876a0a7be871850`; retained normalized corpus identity remains validated by the resolver, rather than compared with raw-byte hashes. Authenticate and preserve original passage order, scores and support.

**Stage replay and route gate:** Build scan projection by retaining the original aggregate except the selected Finding's `unprocessed` state and native fields, omitting its analysis/retrieval/generation/result; guidance projection retains original selected analysis/retrieval with state `active`, omitting generation/result. Pending projection is the exact parsed original. Validate all aggregates and client admissions. Construct the guidance view only through `resolveCitations` on the retained native Finding/retrieval and the hash-bound corpus. Install routes before navigation; block service workers and downloads. Permit same-origin, query-free GET only for the four `loadClientResponses` URL paths `/`, `/index.html`, `/assets/index-B8eBTP4N.js` and `/assets/index-CiL3UJmL.css` (three files, with the root alias), and explicitly fulfill `/favicon.ico` with 204. Fulfill exactly one each of `/api/runs` with the original `{requestedUrl,mode}`, `/api/finding-guidance` and `/api/finding-generation` with the original `{runId,findingId}`. These three are staged responses and never forwarded. Reject all other requests. The only actual application mutation is the one exact authorized `/api/finding-review` request. Record staged requests, final attempts, actual forwards and rejected requests separately. No scan, retrieval, provider or target-page request occurs.

**Human input and durable attempt boundary:** Use only a primary-recorded decision file, after the person receives the complete actual proposal, relevant evidence/authenticated guidance and limitations and explicitly supplies their decision. No alternate unattended control route is accepted. The exact local `decision-input.json` contains `{caseId,sourceSha256,runId,findingId,review}`. `review` follows the existing authoritative shape, including complete `editedProposal` only for edit-and-accept, exact judgment/reason, optional note and required human support confirmation. The primary records its independently expected SHA-256 in this plan's later evidence binding and supplies it separately on the command line; file contents never grant their own authority. Material changes invalidate confirmation. The helper validates case/source/hash/body, fills the exact authorized native controls and compares the intercepted body deeply with `{runId,findingId,review}`. Create an exclusive durable `attempt.json` before activating final Save/forward. Existing markers or changed case state refuse further submissions across process restarts. Failed or uncertain attempts consume the allowance. Do not restore a pending copy, change case IDs, retry or replace branches.

**Readback:** Capture exact request/response; require `admitReview`, `LocalService.readRun` and freshly parsed/validated disk to agree. Remove only the selected review and restore its exact source state, `proposal-pending-review`; the full aggregate must then deeply equal the source baseline, including all siblings and original analysis/retrieval/generation/result. The stored decision equals authoritative `validateReviewInput` output plus service-owned `decidedAt`; transient support confirmation is not persisted. Time must be no earlier than generation completion and within the captured service-call clock interval. Hash both retained originals again. Final screenshots are captured before the actual case browser closes; readback after restart proves durability only and does not reconstruct a final UI.

**Visual and negative evidence:** `--observe` uses authentic pending content at desktop 1366×900 and narrow 390×844, inspects complete original/citations, editor and associated errors, and performs three blocked-acceptance attempts (missing confirmation, unresolved judgment, blank Not applicable reason) with zero review POSTs. One explicitly synthetic incomplete-guidance abstention witness proves no review action or provider invocation. Existing exhaustive keyboard, focus, announcement, axe and unknown-save suites remain authoritative regression; inspect visible focus and non-color/readability on the authentic path. The actual edit-and-accept command captures complete original, editor and saved decision at the same two viewports. All 200% zoom tests are Deferred until after MVP. No headed/native browser control machinery, speech claims or support matrix is added.

**Commands:** Each shell first dot-sources the first fenced PowerShell block in README whose body begins `$ErrorActionPreference`, rejecting a missing match. README SHA-256 is `3d76dfa7aa8695ac8129ed3c4027eaec895ca5b65e74e5d39ab29b618a40c38c`. Use its `Invoke-M105Command`, environment restoration and ordinary/empty scratch checks. The commands below use Node through `$m105Node`; check `$LASTEXITCODE` and stop on any nonzero result. Browser commands run with `$m105UiTemp` as the wrapper's scratch argument. Existing capture flags are absent. No actual-case command is run before A's critical acceptance.

| Slot | Exact command after preparation | Allowed effect |
| --- | --- | --- |
| PREFLIGHT | `node_modules/typescript/bin/tsc --project tsconfig.json`; `--test --test-timeout=120000 tests/review-contract.test.ts`; `--experimental-test-module-mocks --test --test-timeout=120000 tests/finding-review-admission.test.ts` | Read-only strict and focused checks; no caches or actual case |
| CHARACTERIZE | `--test --test-timeout=120000 tests/review-checkpoint.test.ts` | Synthetic disposable test/service/browser state only, at most 120000 ms per test |
| SOURCE | `tests/helpers/m403-review-checkpoint.ts --inspect` | Read-only source/projection/corpus inspection, no browser or writes |
| PREPARE | `tests/helpers/m403-review-checkpoint.ts --prepare` | Exclusive exact copies and local inspection packet; no browser or POST |
| PRESENT / NEGATIVE / VISUAL | `tests/helpers/m403-review-checkpoint.ts --observe` | Bounded authenticated pending/editor/error screenshots and synthetic abstention, zero review POSTs |
| DECIDE | `tests/helpers/m403-review-checkpoint.ts --case <fixed-case> --decision-file temp/m403-review-checkpoint-evidence/<fixed-case>/decision-input.json --decision-sha256 <primary-recorded-hash>` | One human-authorized real final POST for the matching fixed case, deadline 30000 ms; exact inputs bound before dispatch |
| READBACK | `tests/helpers/m403-review-checkpoint.ts --readback <fixed-case>` | Same isolated root, new local service, validated service/disk read, no browser/POST |
| CLEANUP | `tests/helpers/m403-review-checkpoint.ts --cleanup` | Exact positively owned completed disposable cases only; retain all evidence and pending/uncertain cases |
| REGRESSION | The complete unique README PowerShell block starting `if ($null -ne [Environment]::GetEnvironmentVariable('A11Y_M305_CAPTURE_PROOF','Process')` | All 31 named suites sequentially with prescribed mock flags and scratch; no live app/concurrent browser test |
| STRICT / BUILD | `node_modules/typescript/bin/tsc --project tsconfig.json`; `node_modules/vite/bin/vite.js build --configLoader native` | Independent strict check and maintained client build |

**Cleanup and recovery:** Close owned page/context/browser before stopping the owned service (6500-ms cooperative shutdown bound), then prove its port is closed (3000-ms bound). Recheck exact ordinary ancestors and expected inventory before removing a disposable case. Delete only within the resolved fixed cases root, after retained before/after/readback evidence exists and handles are closed. Retain pending/uncertain roots and all evidence, including attempt markers, indefinitely for this checkpoint. No unrelated process kill, original deletion, broad scratch cleanup or model control. Read-only readback cannot replenish attempts. A cleanup failure preserves the primary result and exact residual path/error.

**A assignment packet preparation:** Standard profile; M403-A; S3 because source integrity and final publication matter. `test_worker` receives one read-only preflight, classifies existing behavior and the bounded missing authentic-source witness, and returns before any writes. Passing characterization of existing behavior is the expected evidence route; no fake Red or production Green. The primary opens the evidence lease only after preflight, inserts the returned digest and exact allowed two files above, forbids `src`, `docs`, `.agents`, `.codex`, `data`, `evaluation`, existing tests/helpers and configuration, and closes terminally before documentation or review. Test-owned ignored disposable effects are limited to the commands above; no real-case effects during the worker turn. Maximum write chain is initial plus one ordinary correction and the workflow's conditional final correction; one review correction loop, no budget reset. The complete assignment packet supplies current fingerprint, guard identity and unchanged authority links from this plan before dispatch. Fresh A `critical_reviewer` accepts actual diff, characterization, caller safety and cohesion. A different fresh C `critical_reviewer` later accepts the integrated results. Human decisions remain pending and no lease stays open while waiting.

### M403-G-ACCEPT-01 — Accepted procedure contract

Primary accepts M403-G-CONTRACT-01 after the different fresh final research reviewer returned PASS on the complete revised artifact/I1–I10, plan SHA-256 `97c4d126f92e5854cf2afb8a7c6399717ebc3f938a5865f44bad5fff100866b6`. Correction cycle 1 resolved FR-01's invalid state literal, FR-02's root URL alias ambiguity and FR-03's stale status. No findings remain. Required research, analyst and both independent research checkpoints are complete; one final-artifact correction cycle remains, with no new work implied. The reviewer independently reconfirmed the source/test/configuration fingerprint, both original hashes, README identity, absent lease and clean whitespace; earlier projection/citation/route reproductions remain valid under unchanged inputs. Primary's 294 local-link checks across the seven amended documents pass. The owner-directed global zoom deferral is reconciled in its existing requirement authority and affected ADR, roadmap and UI/plan references. No actual copies, human decisions or POSTs exist yet.

### M403-A-PACKET-01 — Milestone Assignment Packet v2

```text
Identity
- Workflow ID: M4-03-20260915-01
- Roadmap task ID: M4-03
- Work-slice ID: M403-A
- Assignment ID: M403-A-preflight-01
- Lease ID: None for preflight
- Phase: preflight
- Attempt: 1
- Correction parent lease ID: None
- Worker role: test_worker
- Lease owner: m403_test
- Guard contract digest: None for preflight

Work-slice capsule
- Owning ExecPlan: docs/plans/m4-03-review-checkpoint.md
- Observable acceptance contract: M403-G-CONTRACT-01 and I1-I10; characterize the existing review path with an authentic-source caller only if needed.
- Exact requirement / ADR / roadmap Verification / BHV / SPEC / HS anchors: this plan's Context and Orientation authority table, M4-03 roadmap Verification, BHV-05, SPEC-005, HS-010, REQ-REV-001/008/009, shared REQ-EVAL-002, ADR-0021/0024; owner-amended REQ-A11Y-006.
- Readiness and evaluation-freeze evidence: M403-G-ACCEPT-01, unchanged source/manifest identities in G; M4-02 Complete; M3-03 capacity remains independent.
- TDD applicability: Applicable to classification/characterization of existing executable behavior. Expected passing evidence route; do not fabricate Red or add production Green. Manual actual observation itself is non-TDD.
- Current-state and preflight evidence IDs: M403-ENTRY-01, M403-G-ACCEPT-01; fresh preflight result pending.
- Accepted test boundary and current test owner: None until accepted; m403_test owns any later two-file characterization under a separate lease.
- Relevant boundaries and paths: G's two candidate test files, existing review validators/admission/service/repository, m401/m402 fixtures and browser helpers, retained source/corpus read-only.
- Production responsibility placement and fit: None — no application-source responsibility changes.
- Dependency, runtime-call, or interface-edge changes: None in production; test caller uses existing validators, authenticated citations and local service through the G route gate.
- Reuse, justified separation, bounded creation, or local-extraction disposition: G's explicit REUSE_AS_IS existing modules and two-file CREATE disposition; caller missing authentic-source/human-input binding, not new product behavior.
- Permitted local structural refactor: None in production or existing tests/helpers; internal cohesive functions in the two future test files only.
- Non-goals: Production fixes, new dependencies, upstream calls, actual-case preparation/POSTs, human judgments, zoom tests, new UI, reopening, provider/capacity work, Git writes.
- Named uncertainties: Verify existing coverage and minimum missing source-bound driver/witness; report any binding change before writes.
- Risk tier: S3
- Review and escalation triggers: Fresh critical review after A; stop on production gap, changed binding or exhausted budget.

Write scope
- Allowed files: None during preflight; future proposed tests/review-checkpoint.test.ts and tests/helpers/m403-review-checkpoint.ts require a separate lease.
- Allowed directory roots: None
- Forbidden files: Every file during preflight
- Forbidden directory roots: All roots for writes during preflight; originals under data/runs always read-only.

Validation
- Working directory: C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab
- Focused command: G PREFLIGHT commands, each through unchanged README preparation and Invoke-M105Command, stop on nonzero native exit.
- Task-level command: G CHARACTERIZE/REGRESSION/STRICT/BUILD after their applicable later gates; not authorized by this read-only preflight.
- Expected decisive result and reusable evidence IDs: Coverage classification, exact gap if any, fresh strict and 16 focused tests; no runtime effects.
- Relevant-tree fingerprint: 1aa3bf781b7056ba8ce4612a9d080a6935b34e473dc292b863c5f736d8f6cb47; HEAD 038da58f467891d50905256b5847ca7f79d58049; primary documentation changes are expected.
- Environment fingerprint or Non-reusable: G's pinned Node/README; capture safe controlled environment identity without credentials. Mutable filesystem/browser proof Non-reusable; no browser during preflight.
- Known external side effects and cleanup: None; compilation cache disabled by maintained wrapper. No service, browser, fixture/case creation or provider call.

Budget and stopping
- Maximum worker turns: One read-only preflight; any later evidence write gets a new explicit packet/lease.
- Maximum corrections: Existing unchanged-chain budget of initial write, ordinary correction and conditional final correction; one review correction, not consumed by preflight.
- Maximum repeated identical failure: Two without material new evidence stop.
- Maximum no-diff outcomes: Two stop.
- Validation cadence: Run each focused command once; broaden only on a reported binding/prerequisite problem.
- Stop and escalate when: Coverage conflict, source drift, unknown prerequisite, proposed scope/command/effect change, production defect, or any write would be required.

Handoff
- Report identity and authority, touched paths (expected none), exact commands and decisive results, classification, minimum missing witness, unexpected state, residual risks and documentation impact. You are not alone; preserve others' work and never mutate Git or lease state.
```

### M403-A-PREFLIGHT-01 — Existing behavior, missing caller coverage

Primary accepts `EXISTING_BUT_UNCOVERED`: production review already has action/validation/transition/readback and accessible-interaction coverage, but the existing synthetic caller lacks authentic raw source binding, external human-input hash binding and durable attempt enforcement. Fresh Node v24.20.0 strict checking and 9 review-contract plus 7 review-admission tests pass. The 158-file fingerprint, both original hashes, README hash and intentional HEAD remain unchanged; both actual case/evidence roots are absent. The worker made no writes or runtime effects.

The next full packet inherits M403-A-PACKET-01's authority, responsibility, S3 risk and budgets with these explicit evidence-turn bindings: assignment `M403-A-evidence-01`, phase `evidence`, attempt 1, lease `M403-A-evidence-01`, no correction parent, owner `m403_test`; allow only `tests/review-checkpoint.test.ts` and `tests/helpers/m403-review-checkpoint.ts`, no allowed directory root. All other tracked/untracked paths are outside the write lease; explicitly forbid `src`, `docs`, `.agents`, `.codex`, `data`, `evaluation`, `package.json`, `package-lock.json`, `tsconfig.json` and `vite.config.ts`. The primary supplies the guard's returned digest in the issued packet and terminally closes before further documentation writes. Passing characterization is the accepted existing-behavior evidence route; no production Green or synthetic human proof. Exact validation is G CHARACTERIZE then strict TypeScript through README preparation; synthetic browser tests use the existing UI scratch and exclusively owned disposable fixture roots, never the fixed actual-case/evidence roots. No real-case CLI effect or final human POST is permitted in the worker turn. Synthetic tests must demonstrate source/hash/payload refusal with zero dispatch, one exact accepted synthetic endpoint transition with admission/service/disk/sibling agreement, consumed-attempt refusal and blocked unexpected/upstream routing. One initial write turn is now assigned; later corrections retain the existing chain budget.

### M403-A-HANDOFF-01 — Initial caller evidence under review

The test worker returned CHARACTERIZATION CONFIRMED for the two new test files. Its final two synthetic characterizations and strict check pass; read-only `--inspect` validates both actual originals with 35 Findings and the selected `proposal-pending-review` state. Primary independently ran strict TypeScript successfully and inspected the complete helper/test source. The actual prepare/observe/decision/readback/cleanup modes remain unexecuted and unaccepted pending fresh critical review.

Lease `M403-A-evidence-01`, contract digest `c5639955ca6a8cb27959d8a1e0db52ed91b3411a1434fde46cb3e027fd1292b5`, closed-compliant with receipt `8137444a9e31b88fbc0acb450c90cfa893ebd06ef76db04f3ebe3d24b024d484`: exactly the two allowed creations, no forbidden/unleased change or Git/ignore drift. Helper SHA-256 is `a3df392e758e28dc66198a4364f31a430c71cf954c8a9ce34ae16d38d7530bf3`; test SHA-256 is `69cfa689cbcbedd818d4b04d823e2e66537878dd4b2f9997d123c67ebf9bbe79`. The 160-file relevant-tree fingerprint including both new files is `1a7af2fc7127b092f3bc8767d39a668bf8845b998d5c94b8f9494dbac9a26d7b`. The initial evidence turn is consumed; ordinary and conditional final correction allowances and the one review-correction loop remain. No active lease, task-owned service/browser, synthetic sandbox or actual case/evidence root remains. Primary concerns about complete-edit transcription and recovery binding have been sent to the fresh critical reviewer for cumulative disposition; no caller acceptance is implied by passing tests.

### M403-A-CORRECTION-01 — Complete-edit and recovery corrections

Fresh critical review returned REVISE after independently reproducing the two passing tests. A-01: the helper incorrectly fixes assumption count to the original although the existing editor supplies five slots. A-02: membership-only checkbox updates do not preserve the exact authorized reference order. A-03: restart readback checks the action but does not authenticate the complete saved decision against the human-input/hash/attempt binding. A-04: cleanup does not bind current run content to retained completion/readback and can accept service-only shutdown after uncertain browser teardown. A-05: failure paths can skip both originals' post-operation checks. These are unaccepted task-local helper defects; the bug index's routine-correction rule requires no separate bug record. No production or requirement change is warranted.

Primary assigns the single A review-correction loop through evidence attempt 2: assignment/lease `M403-A-evidence-02`, parent `M403-A-evidence-01`, same workflow, slice, owner, two-file allowlist, forbidden paths, authorities, responsibilities, S3 risk and commands. The issued full packet carries the new guard digest. Correct all five findings together; characterize a complete edit with changed assumption count and reordered references, recovery refusal of a same-action changed decision, and cleanup refusal of altered/uncertain state without deleting it. Existing accepted production tests remain untouched. The prior two-test result remains historical evidence and is superseded for affected helper behavior; refresh characterization and independent strict, then obtain the same critical reviewer's cumulative re-review. Conditional write attempt 3 remains available only under the workflow's learning gate; this assignment does not create a second review-correction loop.

Primary is concurrently running the frozen 31-file regression against the unchanged original 158-file dependency inventory. None of those suites imports either new checkpoint file. The worker may edit its two files while that run proceeds, but must wait for the primary's test-slot release before any service/browser characterization. No concurrent runtime tests or actual-case effects are permitted. The new checkpoint suite is verified separately after correction; unchanged legacy results are reusable only with their exact README command, environment and 158-file identity intact.

### M403-A-HANDOFF-02 — Corrected caller evidence under re-review

Evidence attempt 2 returned CHARACTERIZATION CONFIRMED: five synthetic tests pass, covering exact changed-count edits/reference order, authenticated recovery, altered-disk/uncertain-teardown cleanup refusal and combined operation/source errors. Primary inspected the corrected source and independently passed strict TypeScript and the mandatory Vite build. All three generated assets remain byte-identical to G's pinned build. The same critical reviewer is performing cumulative re-review; A remains unaccepted until that verdict and primary evidence acceptance.

Lease `M403-A-evidence-02`, contract digest `137f307a8fd30e476efd5dddacf63bef417f684d2ef01e8fb98255e6a87ed746`, closed-compliant with receipt `00d89236cafeca2f905171126895e87835d2947bffd92014c9bc0b979e5d3844`: only the two allowed file modifications, with no forbidden/unleased or Git/ignore drift. The helper is SHA-256 `f1ffb9834314a6fe8202d612f35d0dbbf2b1160534761a869bcb18ce27e6532b`; the test file is `cd783815d531026d33513a70f51326ee6e5da976f73c75ccff9bf823decf1bb7`; the 160-file fingerprint is `3dbf5744e9bfc06b62ec17b4d9adb60215dd871b36f544933572729cdb4ae8ed`. Fingerprints use PowerShell `Sort-Object -CaseSensitive` ordering; include the two new untracked files with `git ls-files --cached --others --exclude-standard` for the 160-file inventory. Different ordinal sort order is not evidence of source drift. No active lease, synthetic sandbox or actual case/evidence root remains. The ordinary correction and single A review-correction loop are consumed; only the conditional final write allowance remains under its learning gate, without authorizing another review loop.

### M403-C-REGRESSION-01 — Unaffected authoritative checks and reporting limit

The exact frozen README 31-file regression block completed every maintained native-exit check and all final scratch-empty checks against the unchanged original 158-file fingerprint `1aa3bf781b7056ba8ce4612a9d080a6935b34e473dc292b863c5f736d8f6cb47`. The primary's additional output-summary counter then failed: it expected TAP `# tests` lines, while Node's actual default reporter emits `ℹ tests`. The session therefore exited 1 after the authoritative block, not at a test or cleanup check. Per-file totals were suppressed by that filter and are not claimed as freshly observed. A bounded nine-test review-contract rerun reproduced the reporter format and passed; scratch remained empty. Both original source hashes still match. No application failure or source drift was observed. Preserve this reporting limitation rather than claiming an exit-zero wrapper or rerunning unchanged suites solely to recover omitted totals. The separately corrected five-test checkpoint suite and primary strict/build are recorded above; final actual-case and integrated-review obligations remain pending.

### M403-A-ACCEPT-01 — Caller integrity accepted

Primary accepts A after the same critical reviewer independently reproduced all five corrected tests and returned cumulative PASS with no remaining findings. A-01 through A-05 are resolved: complete assumptions and ordered references, full recovered-decision authentication, current-disk and teardown-gated cleanup, and original integrity checks that preserve operation errors. Primary inspected the actual two-file diff and terminal receipts; production modules are unchanged and responsibilities remain cohesive. The final helper/test and 160-file identities are those in M403-A-HANDOFF-02. Independent strict TypeScript and Vite build pass; the three built assets remain identical to G's freeze. Reviewer checks confirm both original hashes, empty UI scratch, absent actual roots, no active lease and passing whitespace review.

This accepts only the caller and permits the frozen inspect, prepare and observe steps. All three final POST allowances remain unused. Actual human decisions, final visuals, durable branch outcomes and C's different fresh integrated review remain pending. Every 200% zoom test remains Deferred until after MVP under the owner amendment.

### M403-B-OBSERVATION-01 — Prepared branches and pending human input

After A acceptance, the frozen README-prepared `--inspect`, `--prepare` and `--observe` commands each exited 0. The three fixed branches are byte-identical copies of the selected Groq source; both retained originals remain unchanged. Missing support, unresolved judgment and blank N/A reason produce accessible blocking errors and zero final review POSTs. The separate synthetic abstention displays no review action and no provider invocation. Context/browser/service shutdown completed and UI scratch is empty. All three final allowances remain unused; pending branch roots and all evidence are retained.

Primary inspected the eight captures under `temp/m403-review-checkpoint-evidence/observe/`. The desktop and narrow pending/error states preserve source separation, readable wrapping, labeled controls and visible invalid-field focus; desktop editor preserves original and editable content. The 390-pixel-wide full-page editor capture is 19762 pixels tall and repeats page-top content in its lower portion; it cannot establish complete narrow-editor visual proof. Treat this as an evidence limitation pending bounded diagnosis, not an application defect or visual pass. Actual edited and final-decision visuals remain pending.

The local `temp/m403-review-checkpoint-evidence/review-packet.md` presents the complete original, recorded scanner facts, authenticated-guidance record and semantic limits. `proposed-edit-v1.json`, SHA-256 `0879e229cf6253fabe2b98c4bb119437adcd59464300e07319bad8b4970f3693`, is a complete mechanically valid assistant draft awaiting human adoption. It removes unverified color examples and exposes uncertainty/applicability; it is not a human judgment or accepted proposal. These presentation files live outside case subdirectories and are retained evidence. The primary requested exact source-bound action, judgment/reason, note and support confirmation for each acceptance, plus adoption or replacement of the complete edit. No decision-input file or attempt marker has been written and no human approval is inferred.

### M403-C-DOCS-01 — Current command documentation

README now includes the five-test synthetic checkpoint suite as file 32 under the existing UI scratch wrapper; the prior 31 commands, mock flags, timeouts and preparation are unchanged. The current README SHA-256 is `c46c4426a3dc9d7ed1cad7e56f4d1e92fe10267b61c3da5c07237614b607e5bb`. This supersedes the earlier whole-document identity for navigation and the combined inventory only; accepted runtime command effects and existing regression evidence remain unchanged. README also corrects stale review-control/current-task descriptions and links the owner-authorized global zoom deferral. Historical 31-file/706-test results retain their original identity; no fresh total is inferred.

Primary's documentation check passes all 480 local links/anchors across the ten changed documents, UTF-8/no-BOM/final-newline/trailing-whitespace checks, unchanged README preparation and exact prior 31 commands, and the new 32-file inventory. All three pending copy hashes match and no attempt marker exists. `git diff --check` passes. This is interim documentation verification; task closure still requires the remaining human/runtime evidence and different fresh C review.

### M403-B-VISUAL-GAP-01 — Capture limitation and required allowance

The same critical reviewer independently checked raw narrow-editor PNG pixels: all 3378 rows at y=16384 through 19761 exactly duplicate rows 0 through 3377, digest `8a9c32d01b509776b9f927278f46a9acf5c4f0506832ba35814f2be5795bcc6b`. This confirms a captured-image limitation, not merely preview scaling; it does not establish a UI defect. The current helper hardcodes full-page captures, has no region option and refuses existing destinations, so replaying `--observe` is not a valid repair. Tall final-decision captures may have the same limitation and remain unproved.

Proposed bounded correction, not authorized or implemented: retain the same test worker/two-file responsibility; use the remaining conditional evidence write turn to capture the original proposal through the editor/decision in a bounded document region, preserving viewport and application layout, existing captures and all decision/source/route safeguards. Freeze one create-only supplemental observation destination and a zero-final-POST command through the remaining G artifact correction review before effect. Record this new pixel evidence, the full-page-capture lesson, expected complete-content benefit and exact baseline in the final-turn packet. Reuse unchanged production tests; refresh affected characterization/strict and obtain critical review before actual decision commands.

The existing A review-correction loop is consumed. The workflow's explicit rule, "One review correction loop remains the limit," means the conditional third writer turn alone cannot authorize the needed extra review. Primary requested one additional A review loop for this screenshot-only correction. That request is separate from all semantic decisions and grants no replacement case or final POST. Until the owner responds, no lease is open and the affected visual/final-decision work remains pending; unaffected verification and documentation are complete to the boundary recorded above.

### M403-G-CAPTURE-02 — Owner-authorized bounded capture correction

The owner explicitly authorized the proposed bounded capture correction: remaining conditional helper-write turn, one additional A review loop, and one new zero-POST observation. This supersedes only the exhausted A review allowance for that correction; all semantic inputs, three lifetime final POSTs, source protection, G's remaining one final-artifact correction review and C's different fresh review stay unchanged. No semantic approval was supplied.

**Candidate amendment for G's final-artifact correction cycle 2:** keep the installed browser, actual build, desktop 1366×900/narrow 390×844, stage projections, route whitelist and exact case IDs. At editor and final capture points, capture a bounded document rectangle from the original `.proposal-detail` through `.proposal-review` or `.review-decision`, including all intervening edited content and the final controls/reminders. Compute bounds from rendered elements in the current document, retain full viewport width and unchanged layout, and require a positive region no taller than 12000 CSS pixels. Use Playwright's existing `fullPage: true` plus `clip`; no CSS zoom, scale, resized viewport, pixel stitching or new dependency. If pre-submission bounds cannot include the full required content, fail before the final POST and preserve evidence. A post-submission capture failure preserves the consumed allowance and durable evidence for readback; it never permits a retry or claims visual success. Existing pending/error/abstention full-page captures remain unchanged; existing earlier pending captures supply native evidence, guidance and source-notice context. The four existing actual edit-case filenames and exact cleanup inventory remain unchanged. Their region interpretation replaces the earlier full-page assumption only for those editor/final images.

Add one fixed `--observe-capture` mode. It reads the existing untouched approve branch, authenticates the same source/projections, and reuses the same fail-closed zero-review route installation. It opens the original proposal, selects Edit and accept without confirming or saving, and captures `desktop-source-and-editor.png` and `narrow-source-and-editor.png` under the exclusive initially absent `temp/m403-review-checkpoint-evidence/observe-capture/`. Retain one `capture-summary.json` with source/build/browser/viewport/region dimensions, exact staged counts, zero review attempts/forwards, unchanged copy hash and successful owned teardown. Close context/browser/service/port and check both originals even on failure. It must refuse an existing destination; never overwrite the damaged observation or create/reset a branch. No capture flag, provider operation or final decision file is involved.

Exact supplementary command after renewed A acceptance: dot-source the unchanged README preparation, assert UI scratch empty, then `Invoke-M105Command { & $m105Node tests/helpers/m403-review-checkpoint.ts --observe-capture; if ($LASTEXITCODE -ne 0) { throw 'Capture observation failed' } } $m105UiTemp`, followed by the same scratch-empty assertion. This is one authorized actual zero-POST observation. Repeated synthetic characterization remains separate; verify both editor and saved-decision clipping there before any human case attempt. Existing CHARACTERIZE and strict commands, complete evidence identity, terminal lease, inspection and renewed A PASS are required. Primary manually inspects the new actual screenshots before accepting the missing narrow-editor proof. The original corrupt PNG remains retained and not passed.

This is a test-side presentation correction only. No production/source behavior, decision payload, publication/readback/cleanup predicate, attempt marker or runtime call permission may change. The new mode must reuse existing projection, routing, source-integrity and teardown owners rather than copy their substantive logic. The amended method remains pending G's final artifact review; no writer lease or supplementary observation is opened yet.

### M403-A-CAPTURE-PACKET-03 — Conditional final evidence assignment

This supplements every unchanged field of M403-A-PACKET-01 and M403-A-CORRECTION-01; the dispatch packet repeats all fields and inserts the new lease digest after G-CAPTURE-02 acceptance. Workflow `M4-03-20260915-01`, task `M4-03`, slice `M403-A`, assignment/lease `M403-A-evidence-03`, evidence attempt 3, correction parent `M403-A-evidence-02`, role `test_worker`, owner `m403_test`. Exact allowlist remains the two checkpoint files, with no allowed directory roots and the same forbidden paths. No production responsibility or dependency edge changes; reuse existing stage/routing/integrity/teardown functions and keep only capture-region computation and fixed observation coordination in the helper. No structural refactor beyond sharing those existing responsibilities.

Learning gate: prior integrity corrections passed five tests and critical review, but actual 19762-pixel narrow content newly demonstrates raw screenshot row repetition at 16384. Full-page images alone cannot establish complete tall-content evidence. The changed bounded action is G-CAPTURE-02's document-region capture and one fixed zero-POST observation mode. Expected benefit is complete original/editor/final coverage below the reproduced height limit without altering application layout or semantic effects. The owner explicitly granted this final write and one extra A review loop; no fourth write or further correction loop is implied.

Preserve existing test assertions. Extend only relevant synthetic characterization to exercise original-through-editor and saved-decision captures at both fixed widths, bounded dimensions, and refusal of invalid/oversize pre-submission regions without a consumed attempt. Existing request/recovery/cleanup assertions remain accepted. A passing behavior characterization, not fabricated Red/Green, is the replacement evidence because application behavior remains unchanged. Run the README-prepared checkpoint suite and independent strict TypeScript. Use existing synthetic scratch and cleanup; actual `--observe-capture` is primary-owned after renewed A acceptance. The worker must not touch prepared branches, source/evidence roots, human drafts or decisions, or run actual modes. No provider/scan/retrieval/generation calls, zoom tests, Git writes or unrelated edits.

Entry helper/test/160-file identities are M403-A-HANDOFF-02. README preparation and existing 31 commands are unchanged; the current README identity is M403-C-DOCS-01. Strict/build and unchanged legacy suite remain reusable within their full identities; the changed helper invalidates its affected five-test evidence until refreshed. No competing service/browser tests run during the worker's characterization. After the worker stops, primary closes the lease, inspects actual diff and evidence, and obtains the owner-authorized cumulative A re-review. Stop after this final write if unresolved; do not broaden scope or reset the budget.

### M403-G-CAPTURE-ACCEPT-02 — Capture amendment accepted

G final-artifact correction cycle 2 returned PASS with no findings on plan SHA-256 `cbe5ebc0a08303c1e4d41ac399cd9483a770d465905608c04c4153ef40cf642f`. Independent inspection confirmed document-coordinate clipping support, the raw screenshot limitation, unchanged source/copy/build/code identities and no active lease or supplemental destination. Primary accepts M403-G-CAPTURE-02 and the conditional final assignment. G's final-artifact correction budget is now exhausted; the owner's one additional A review loop, final write and single zero-POST observation remain available. Actual semantic decisions remain pending. The following lease dispatch supplies its exact guard digest; no other packet field changes.

### M403-A-CAPTURE-HANDOFF-03 — Final capture correction under review

Final evidence attempt 3 returned CHARACTERIZATION CONFIRMED: six tests pass, including desktop/narrow editor/final PNG dimensions and invalid/oversize pre-attempt refusal, and independent strict TypeScript passes. All earlier request, recovery, cleanup and integrity assertions remain. Helper SHA-256 `5823dfbff064d06d78aff8dd14ad81d31a7d72c5d7917b1decdc12f210d0cb14`; test `bbc64326188ed256ea64bc2dc886452e0fbd8170738378c5495f8de5fbf4561b`; 160-file fingerprint `97466d647596538fb4e79abb1d4d0f31b98e71eba183916cc41fd1a05bd0f1c0`. The previous five-test helper evidence is superseded for affected behavior.

Lease `M403-A-evidence-03`, digest `ddb4f3dea12d1d80bf77f82f9e65ef35c7e80debc5b20ff1ccc9a87361768fcf`, closed-compliant with receipt `9a7ed491060065a66310f5572a2a8117dc17bf07a42150bc0836fa5de00334c9`. Only the two allowed files changed; no forbidden/unleased or Git/ignore drift occurred. No active lease remains. Original and prepared copy hashes match, synthetic scratch is empty, and the supplementary observation destination remains absent. The final writer allowance is consumed. Primary is inspecting the final source and the owner-authorized additional A review is pending; actual supplementary observation and all three semantic decisions remain unexecuted.

### M403-A-CAPTURE-ACCEPT-03 — Corrected capture accepted

The owner-authorized additional A critical review returned PASS with no findings, independently reproducing all six tests and confirming geometry, source/decision integrity, metadata, exclusive observer, cleanup and cohesion. Primary inspected the final helper/test changes, independently passed strict TypeScript and matched the final 160-file fingerprint and all three unchanged build assets. The final source identities and compliant receipt are M403-A-CAPTURE-HANDOFF-03. Primary accepts the corrected helper and authorizes the single frozen `--observe-capture` execution. The extra A review loop and all writer turns are consumed. G's correction allowance is exhausted. No supplementary observation or human decision has yet occurred; all three final POST allowances remain unused.

### M403-B-CAPTURE-OBSERVATION-02 — Complete editor captures

After renewed A acceptance, the single README-prepared `--observe-capture` command exited 0. Primary manually inspected both original-through-editor PNGs: complete proposal text/references and all review fields, five assumption slots, judgment/note, support confirmation and Save are present; text wraps within the viewport, source labels remain distinct and the selected edit action has visible focus. The bounded images contain the lower editor that was missing from the earlier full-page artifact, without repeated page-top content. Native evidence/full guidance/notices remain in the intact earlier pending captures. This accepts bounded pending/editor visual proof only; no user edit or final decision has been accepted or displayed.

`temp/m403-review-checkpoint-evidence/observe-capture/capture-summary.json`, SHA-256 `e41e6e8404fc038a1c4894c2424b2af20f43f6fd41252ff14b897e0ae3e86dab`, binds final build, Chromium 151.0.7922.34, desktop 1366×900 and narrow 390×844. Desktop image is 1366×6114 at document y=8142, SHA-256 `c19a750d6aedbd63085720bb6dea2e8c4bbd0a164dcfce5de9e9e91cf7256102`; narrow image is 390×6722 at y=12983, SHA-256 `61c9a688543edf6f0983618dfc31ea11554d70d745fde6b016231e8c95da9cdd`. Each pre-review stage was replayed once; review attempts/forwards and rejected requests are zero. Copy hashes match before/after, both originals pass the enclosing integrity check, owned context/browser/service/port teardown is confirmed, and UI scratch is empty. The original damaged PNG remains preserved and unpassed.

The supplementary observation allowance is consumed. All three final case allowances remain unused, with no decision-input or attempt file. Pending case roots and local evidence remain intentionally retained for the owner decisions. The human packet's observation note now links this corrected evidence; its complete proposed-edit-v1 bytes/hash and all original content are unchanged. No commit, push, upstream execution or zoom test occurred.

Interim handoff verification passes 481 local links/anchors across ten changed documents, formatting for those documents and both untracked test files, unchanged complete edit-draft hash, all three untouched case inventories, three empty scratch roots, absent active lease and `git diff --check`. Documentation impact is the owner-authorized accessibility verification amendment and corresponding ADR/roadmap/UI/current-plan references, README suite/current-capability reconciliation, and existing plan/progress navigation. No product behavior, dependencies, original evidence or future roadmap authorization changed. M4-03 remains In progress: exact human decisions, their actual endpoint/readback/final visual results and different fresh C integrated acceptance are still required before completion or archiving.

### Effect and command slots

The contract above resolves these slots, subject to final G review and A caller acceptance; they do not permit improvising a command after review.

| Slot | Exact freeze required |
| --- | --- |
| M403-CMD-PREFLIGHT / CHARACTERIZE | Read-only coverage command, exact test/helper paths if necessary, passing characterization/negative witness, strict checker, runtime, flags and maximum durations. No actual source-case final POST. |
| M403-CMD-SOURCE / PREPARE | Exact selected sources and byte hashes, ordinary ancestor/reparse/alias checks, new exclusive isolated roots, create-only byte copies, retained-original protection and validation; no rewrite of IDs/proposals or missing-input regeneration. |
| M403-CMD-PRESENT | Current build/service/browser, exact controlled stage projections, authenticated guidance view, permitted intercepted routes versus the sole real review mutation, source/run identity and request counters. No product reopen route or new actual upstream execution. |
| M403-CMD-DECIDE / READBACK | Person-authorized exact source/action/payload binding, per-case one-POST allowance, endpoint/body and deadline, service-owned read and aggregate comparison, original/sibling/time predicates, minimized local result paths and evidence retention. Human waits are not held-open write leases. |
| M403-CMD-NEGATIVE / VISUAL | Exact blocked/no-POST and controlled abstention inputs; keyboard/announcement/axe checks; one installed browser and bounded desktop 1366×900/narrow 390×844 sample, screenshot/identity destinations and ownership. All 200% zoom testing is Deferred until after MVP at owner direction. |
| M403-CMD-REGRESSION | Maintained 31-file inventory plus accepted new characterization, current mock flags, strict/build, sequential ordering, no competing service/browser test, known output/scratch and exact environment restoration. Existing capture flags are absent unless explicitly frozen for the bounded visual caller. |
| M403-CMD-CLEANUP / GUARD / DOCS | Exact owned services/tabs/handles and shutdown order, temporary versus retained evidence, safe exact-path removals/final absence checks, guard open/terminal callers/protected tests/attempt lineage, documentation links/anchors/command parsing/UTF-8/newline/whitespace and `git diff --check`. |

Missing runtime/browser/source prerequisites stop the dependent effect; no download, model call or fabricated replacement input is authorized. Reuse corrected M4-01/M4-02 cleanup patterns, preserving original failures and exact retained roots when secondary cleanup fails. Keep outcome records local/content-safe; never write credentials, full private transcripts or actual proposal bodies into tracked fixtures or logs.

## Validation and Acceptance

G uses this cumulative invariant packet; every actual-case and visual result is pending at planning. For each row record exact fixture/source, expected result, command/source/build/environment identity, actual evidence or remaining gap and the responsible reviewer under stable checkpoint IDs. Research reviews check contract completeness; A's critical reviewer accepts caller integrity; different C review accepts the integrated evidence.

| ID | Trigger and expected result | Evidence owner |
| --- | --- | --- |
| I1 | Task/dependency/freeze inspection: only M4-03, M4-02 Complete, source/evaluation/model bindings unchanged; no M3-03/M6/provider-quality or support claim. | Primary, G/C reviewers |
| I2 | Source and isolated branch preparation: pending bytes/proposal/provenance match authentic origin; copies are labeled controlled branches, never fresh upstream runs; retained originals stay byte-identical throughout. | G/A checks, A/C reviewers |
| I3 | Exact source/payload semantic input: a person confirms accepted-result support and a supporting or reasoned-N/A judgment; rejection needs no positive assertion. No automated check or generic execution request supplies the person's judgment. | Primary records human input; C reviews traceability |
| I4 | Three separate final branches: exactly one approved action/request per case, correct terminal state/action/time, full edit only for edit-and-accept, no accepted plan after rejection; original result/invocation remain unchanged. | B request/response/readback, C reviewer |
| I5 | Missing support, unresolved judgment and empty N/A reason: accessible blocking errors, zero final POSTs and no decision. Controlled abstention remains explanatory/non-reviewable, with no invocation or review decision. | Existing coverage plus bounded B witnesses, A/C reviewers |
| I6 | Real review endpoint/service and validated readback: browser result equals canonical decision; parent, native evidence, retrieval/analysis and every sibling remain unchanged. Direct fixture mutation cannot stand in for publication. | B service/disk evidence, C reviewer |
| I7 | Keyboard, selection, form-to-decision focus and shared announcements: current behavior remains operable and names the reviewed Finding without changing or stealing focus from siblings. Original/edited/reminder/source labels remain distinct and no certification claim appears. | Current automated coverage and B browser proof, C reviewer |
| I8 | Authentic-content manual visual path: readable full original/edit/decision/citations, non-color status and visible focus at bounded desktop/narrow. All 200% zoom testing is Deferred until after MVP by explicit owner direction under amended REQ-A11Y-006; it is not a pass. Preserve prior results and omissions; no speech/support-matrix claim. | Primary visual observation, C reviewer |
| I9 | Failure/lost response or cleanup uncertainty: last valid record determines truth; no blind retry, reset branch or new grant. Read-only primary recovery may establish state without a new UI feature; exact owned cleanup preserves source and retained evidence. | A adverse characterization/reused M4-02 proof, B/C acceptance |
| I10 | Actual diff/cohesion, accepted characterization or justified no-write route, terminal leases, full current suite, strict/build, finite allowances, exact cleanup and documentation consistency pass. No roadmap completion before all required evidence and human inputs exist. | Primary, C reviewer |

Do not require all three rule profiles or both providers for these shared transitions. Do not rerun generation or create statistically meaningful quality claims. If only synthetic transitions pass, report that useful evidence but leave M4-03 incomplete. A reviewer may legitimately reject a proposal or be unable to confirm support; the resulting limitation is evidence, not a test condition to manipulate away.

## Idempotence and Recovery

Read-only inspection is repeatable. Source originals under `data/runs` are never write targets. An isolated branch has one final decision and cannot be reset/reviewed again; preparing another copy after failure does not reset its consumed attempt allowance. G's three branches are planned independent cases, not retries or a proposal-version graph.

A lost reply can follow a committed decision. Stop submissions, preserve browser uncertainty and read the selected isolated aggregate through the existing service to establish canonical truth; do not infer no-write from the transport error. This is evidence recovery, not a product recovery read, and does not grant a second final POST. If it cannot establish the required observation, mark that case unresolved and request only the necessary new authority after unaffected work is complete.

Validate all absolute paths and ancestors before copy/removal; refuse unexpected contents or links. Close owned browser/context/service/handles before removing only the exact disposable case resources. Retain required before/after evidence separately under the frozen content-safe path; do not delete source runs or existing M3/M4 proof roots, kill unrelated processes/Ollama or use Git reset. Report exact retained paths and uncertainty on cleanup failure.

Any worker stop requires terminal lease inspection and primary triage before another writer or document edit. Corrections use remaining named budgets, invalidate affected evidence and receive fresh acceptance. Human decisions can remain pending without keeping a worker lease active. If support, a significant scope decision or a prerequisite cannot be resolved within existing authority, finish unaffected checks and ask a concise evidence-backed question; never silently waive the requirement.

## Artifacts and Notes

### M403-B-REJECT-INPUT-01 — Human-authorized separate rejection

The owner selected the offered separate-copy rejection demonstration, retaining the supporting judgment and omitting a note. Primary binds only `reject`: action `reject`, blocking judgment `supports-proposal`, no support-confirmation field, no note and no edited content. This leaves the already saved approve branch unchanged and supplies no edit-and-accept decision. The exclusive `temp/m403-review-checkpoint-evidence/reject/decision-input.json` SHA-256 is `c9a9ab9f8c801992f0fd13e448100ef560c15796f4b180c0c9328d0d4b4c4b13`, with the same frozen Groq source/run/Finding binding. Its sole final POST and subsequent readback are authorized; no prior reject attempt exists.

### M403-B-REJECT-OBSERVATION-01 — Separate rejection saved and read back

The frozen reject command and `--readback reject` each exited 0. Exactly one actual review request was forwarded, with unchanged staged earlier responses and no upstream execution. The saved state is `rejected`, action `reject`, judgment `supports-proposal`, no accepted edit or note, and server time `2026-09-15T03:13:34.244Z` inside the captured `03:13:34.169Z`–`03:13:34.294Z` interval. Reviewed run SHA-256 is `5ab6194b117d16ddd7a4320bde69777f9302c5e9acfa0b67b313a42449c64723`.

Primary independently matched the captured request to the exact human-authorized input and reproduced full original-aggregate equality after removing only the decision/restoring pending state. Restarted service/disk readback, source preservation and complete browser/service/port teardown pass. The separate approval remains accepted. Reject's lifetime attempt is consumed, its evidence and completed branch remain retained for C, and only edit-and-accept retains an unused allowance and pending human decision.

### M403-B-APPROVE-INPUT-01 — Human-authorized unchanged approval

After the complete local review packet and a plain-language clarification distinguishing agreement with the actual unchanged proposal from permission to continue, the owner affirmatively approved that proposal as supported by the presented evidence. Primary binds only the fixed `approve` branch: action `approve`, blocking judgment `supports-proposal`, material-support confirmation true, no optional note supplied and no edited content. This does not authorize edit-and-accept or rejection.

The exclusive local file `temp/m403-review-checkpoint-evidence/approve/decision-input.json` has independently recorded SHA-256 `db7f0f0955f4c939b78a443f3bed77397b2eec7f09ca9d401ce202e3dcaa33c3`, selected source SHA-256 `61eb5b6fccc59083850ea147838a1049ab42090e75fc82a607c5874663d5960b`, run `run-d4d06856-39d7-4196-b607-8e2f2815b302` and Finding `f0a4f4ff-5beb-4676-9055-0ea763523e70`. Pre-effect inspection matches final helper/test, both originals and all three built assets; no active lease or prior approve attempt exists. Primary may now execute the single frozen approve command and readback. Other two decisions remain pending and their allowances unused.

### M403-B-APPROVE-OBSERVATION-01 — Approval saved and read back

The frozen approve command and subsequent `--readback approve` each exited 0. Exactly one review request was forwarded and admitted; each earlier stage was fulfilled once without upstream execution. The resulting state is `accepted`, with action `approve`, judgment `supports-proposal`, no note or edit, and server-owned `decidedAt` `2026-09-15T03:10:39.038Z` within the observed call interval `03:10:39.007Z`–`03:10:39.080Z`. Reviewed run SHA-256 is `fd3e218784ccf7c2e0264d280302eecb3416234d3ccc54f78baee18e91c23558`.

Primary independently compared captured request with the authorized decision input and restored the saved Finding to its baseline pending state after removing only the decision: the entire aggregate equals the original, including original proposal, native evidence, invocation, retrieval, parent and all 34 siblings. Existing service readback after restart agrees with admitted response and validated disk. Context/browser/service/port shutdown and readback-service shutdown are confirmed; UI scratch is empty. Both retained originals pass the enclosing source-integrity checks. Approve's attempt is permanently consumed; no retry or replacement branch is allowed. Its completed case and all evidence remain retained for integrated inspection; the other two copies remain pending and untouched.

Primary requested the two remaining decisions in plain language: adoption of the complete presented edit with its support/applicability judgment, and rejection on the separate copy while retaining a supporting judgment. Neither is inferred from the unchanged approval. Actual final edit visuals and C's different fresh review remain pending.

M403-ENTRY-01 and M403-PLAN-01 own planning evidence. Future accepted records are M403-G-ACCEPT-01, M403-A-ACCEPT-01, M403-B-OBSERVATION-01 and M403-FINAL-01, created only when their evidence exists. Keep a compact case table, command/identity bindings, human-authorized payload references, actual results, failure/cleanup facts and review verdicts here. Full proposal/edit evidence stays in the authorized local case records; no copied private conversation, per-claim checklist, new telemetry ledger or separate report document is planned.

Planning changes only this plan, roadmap activation, plan index, progress index and one concise M4-03 progress record. Requirements, ADRs, source, tests, dependencies and current runtime behavior are unchanged. Execution documentation impact follows the actual evidence at C.

### M403-PLAN-01 — Accepted planning readiness

On 2026-09-14 20:41 UTC, the fresh independent `critical_reviewer` returned **PASS** for readiness to enter G after execution authorization, with no Blocker, Major or Minor finding. Reviewed plan SHA-256 was `1619c4a5538622bcbfa320925dd63113366d52a2a50a324797e620db37380027` at entry HEAD `ae0850df450f89715f73d3089c2778126b665414`, before this verdict/living-state maintenance. The review covered the complete plan, five planning paths, selected authorities and actual review/helper seams. No execution contract or semantic decision was accepted.

The reviewer independently reproduced strict TypeScript, all 16 focused tests and both source validations/hashes: 35 Findings each, exactly one pending contrast proposal and no decision. Primary accepted the actual five-file diff and checked all sixteen required sections, the one PowerShell block, local paths/anchors, strict UTF-8/no BOM, final newline, trailing whitespace and `git diff --check`; final living-record/link maintenance is checked with the same procedure. Primary's frozen manifest/thirteen references remain unchanged. No active lease, production edit, original mutation, browser/service launch or provider call occurred.

Source suitability for honest unchanged approval, exact case/caller commands, native-zoom mechanism, full regression and actual human/browser/service results remain explicitly pending. M4-03 stays In progress for planning only; M4-02 stays Complete and M3-03 stays independently Blocked. Documentation impact is the five named planning/status/navigation files; no requirement, ADR or runtime change.

## Interfaces and Dependencies

Reuse existing React review controls, `/api/finding-review`, `LocalService.reviewFinding/readRun`, the strict review/aggregate validators, repository safe writer, pinned browser/axe tests, current build and test sandbox. A minimal test-only source-bound driver is the sole anticipated possible addition; exact necessity/ownership is resolved by A preflight, after G freezes its contract. No new application interface, persistence schema, dependency or model operation is needed by default.

M4-03 proves the human review checkpoint, not generation semantics, capacity or release. M5 and M6 remain separately selected tasks; M3-03 stays Blocked until its own gate is met.

## Revision Note

2026-09-14: created for the owner's M4-03 current-state review and planning request after M4-02 completion. Recorded unchanged real pending sources, source-preserving independent review branches, genuine human-input gates, reuse-first characterization/observation workflow, finite effects and proportional visual proof. Preserved M4-02's task-specific zoom omission and independent capacity/evaluation boundaries. No actual decision, source mutation or new upstream operation occurred.

2026-09-14: recorded independent critical planning PASS and verified five-document closure; updated living status and the concise progress summary without changing future execution authority or the case contract.

### M403-B-EDIT-INPUT-01 — Human-authorized complete edit

The owner approved the previously presented complete edit after the plain-language explanation of its wording and applicability judgment. Primary binds the frozen `proposed-edit-v1.json` (SHA-256 `0879e229cf6253fabe2b98c4bb119437adcd59464300e07319bad8b4970f3693`) to the separate `edit-and-accept` case, action `edit-and-accept`, blocking judgment `supports-proposal`, support confirmation true and no optional note. The exclusive local `temp/m403-review-checkpoint-evidence/edit-and-accept/decision-input.json` has independently recorded SHA-256 `577ebfa2ec0d4eecab2021295a54b84c0700a8b62ca001b46104b215d44ce175`, with the unchanged selected Groq source/run/Finding identity. Both originals, final helper/tests and all built assets match the accepted hashes; no active lease or earlier edit attempt exists. The sole remaining final POST and subsequent readback may now execute. Previously saved approve/reject cases remain final and cannot be resubmitted.

### M403-B-EDIT-OBSERVATION-01 — Complete edit saved and read back

The frozen edit-and-accept command and restarted `--readback edit-and-accept` each exited 0. Exactly one actual review POST saved `edited-and-accepted`, with the exact complete owner-adopted proposal, supporting judgment, no note and service-owned time `2026-09-15T20:33:19.707Z` within the captured `20:33:19.666Z`–`20:33:19.760Z` interval. Reviewed run SHA-256 is `e944236a6ebcd2b8c60fb6115c6d12a5c0693b7d69b58f0aeb5a9c13825adb7d`. Each earlier stage was fulfilled exactly once; no upstream operation occurred. All three lifetime final-POST allowances are now consumed successfully.

Primary independently compared every case request with its bound input, every stored decision with that input plus server time, restarted readback with disk, reviewed hashes and complete baseline restoration after removing the selected review/restoring pending state. All 34 siblings and original context remain equal. The first read-only comparison script used an incorrect `id` lookup; after correcting it to the existing `findingId`, all comparisons passed without any application mutation or new submission. Browser/context/service/port and readback-service teardown are all confirmed.

Primary inspected the four actual edit PNGs: desktop/narrow original-plus-editor (1366×6114 and 390×6722) and original-plus-final-decision (1366×3640 and 390×4672). Original and reviewer-authored content remain visibly separate; final action/time, supporting judgment, cited references, verification reminder and focus indicator are present without the earlier repeated-page artifact. The saved final text includes the adopted uncertainty, assumption and manual applicability judgment. Desktop/narrow proof does not claim 200% zoom, screen-reader speech or support qualification. Completed disposable roots remain retained for C inspection before one exact cleanup; all evidence is retained separately.

### M403-C-CLEANUP-01 — Exact disposable cleanup

After the different fresh integrated critical reviewer independently completed its raw-case audit, primary ran the frozen README-prepared `--cleanup` once; exit 0. The resolved target was exactly the workspace `temp/m403-review-checkpoint-cases` root. All three authenticated completed branches and the empty root are absent. Before/after inventory comparison proves all 48 retained evidence files unchanged. Inventory SHA-256 is `2176041f1aa9ba7506cea9d7d9139e5195056537a673875051d6c648d2a8a595`: PowerShell absolute FullName-sorted rows, path plus space plus uppercase file SHA-256, LF separators/final LF, UTF-8. No original or other task root was removed.

The final 160-file source/test/configuration fingerprint remains `97466d647596538fb4e79abb1d4d0f31b98e71eba183916cc41fd1a05bd0f1c0`. The frozen generation manifest and all 13 referenced raw artifacts remain unchanged. An initial read-only checksum comparison treated uppercase/lowercase hexadecimal text as unequal; normalized comparison passes without file changes. No active lease exists. README preparation and command bodies parse (six PowerShell blocks), and its authoritative suite names 32 unique existing test files. The preclosure ten-document UTF-8/newline/whitespace audit, 480 local links/anchors and `git diff --check` pass; final archive reconciliation is still required.

### M403-FINAL-01 — Integrated review and task closure

On 2026-09-15 UTC, the different fresh C critical reviewer returned **PASS**, with no Blocker, Major or Minor findings. It independently reproduced all six final checkpoint tests and strict TypeScript, audited every actual source/input/request/response/readback/time and full-aggregate preservation predicate, inspected all four edit PNGs and compliant leases, and confirmed exact cleanup and the unchanged 48-file evidence inventory. Primary accepts that verdict against the unchanged final source/test/configuration and build identities.

| Acceptance | Final evidence and disposition |
| --- | --- |
| I1–I2 | Only M4-03 executed. Both source originals, frozen manifest and 13 referenced artifacts are unchanged. Three fixed copies were controlled branches of one authentic proposal, with no new scan, retrieval or provider execution. |
| I3–I4 | Each final action has a separately supplied human decision and pre-effect payload hash. Exactly one actual POST per branch saved accepted, edited-and-accepted or rejected. Complete adopted edit remains separate from the original; all final allowances are consumed. |
| I5–I6 | Missing support, unresolved judgment and empty N/A reason were blocked with zero POSTs. Synthetic abstention remained non-reviewable with no invocation. Every admitted result matches restarted service/disk readback; original context and all 34 siblings are preserved. |
| I7–I8 | Current automated keyboard/focus/announcement/accessibility evidence and bounded actual desktop/narrow original/editor/final captures pass. The damaged earlier full-page capture remains unpassed and superseded by the accepted bounded proof. Every 200% zoom test is Deferred until after MVP; no speech or support-matrix claim. |
| I9–I10 | Recovery/cleanup characterizations pass; all leases closed compliant, exact disposable cases are removed, evidence retained, scratch empty and no owned service/browser remains. Current authoritative regression evidence, independent strict checks, unchanged client build, diff/cohesion audit and documentation gate pass. |

The current authoritative suite is 32 files: unchanged 31-file regression with all maintained native-exit checks completed, plus the six-test final checkpoint characterization independently reproduced by both A and C. C explicitly accepted reuse under complete unchanged identities and the reporting limitation in M403-C-REGRESSION-01. Its additional summary counter failed after native tests because it expected TAP rather than the actual SPEC reporter; no fresh aggregate test count or exit-zero original wrapper is claimed. No production code, dependency or schema changed.

Semantic limits remain: a human decision is not automatic ground truth, model-quality qualification or a verified website remediation. The original proposal's unverified color examples remain inspectable; the adopted edit removes them and records applicability/measurement limits. No source page was changed. M3-03 capacity remains independently Blocked, and M5/M6 are not started.

Documentation impact: Updated README capability and 32-file commands, roadmap/status, plan and progress navigation, this evidence plan and concise progress record, UI evidence limits, and the owner-directed zoom-test timing in REQ-A11Y-006/ADR-0012 and the affected M3-03 cross-reference. Preclosure checks passed UTF-8/no BOM/final newline/trailing whitespace, 480 local links/anchors, all six README PowerShell blocks, 32 unique suite paths and `git diff --check`; new untracked test/helper files were separately checked. Complete status and archive relocation receive a final link/consistency pass below. No commit, push or later task is included.

2026-09-15: completed owner-authorized checkpoint execution, recorded three exact human decisions and durable outcomes, independent final critical PASS, evidence-preserving cleanup and documentation closure. Reconciled the explicit post-MVP zoom-test deferral and preserved all earlier limitations; archived without selecting later work.

**Final archive reconciliation:** The completed archive and all affected navigation/status owners pass the final ten-document audit: 481 local Markdown paths/anchors resolve, UTF-8/no-BOM/final-newline/trailing-whitespace checks pass, and roadmap/progress mirrors show M4-03 Complete. The old active path is absent; its original path remains only in a historical frozen assignment packet. README commands retain six parseable PowerShell blocks and 32 unique existing suites. New test/helper files pass separate untracked-file whitespace checks. All maintained scratch roots are empty, no case root or active lease remains, and HEAD/index are unchanged. Primary accepts final documentation closure with no remaining M4-03 obligation.
