# Integrate Qwen Local generation and verify reference-PC capacity

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M3-03](../../DEVELOPMENT_ROADMAP.md#m3-03--integrate-qwen-local-generation-and-run-its-capacity-screen), Complete after final independent PASS and the documentation closure gate. G, A/B implementation and the sole real C capacity observation pass; no implementation or runtime work remains.
- **Accepted evidence:** [M303-C-OBSERVATION-01](#m303-c-observation-01--successful-full-local-stack-capacity-screen) records the actual scan, supported retrieval, validated Local proposal, simultaneous model residency, usable reviewer interface and successful cleanup. Fresh strict TypeScript and 712 tests across 32 files pass under the unchanged complete evidence identity in M303-RESUME-03. The separate M2-02 gate and frozen M301 package remain intact.
- **Ownership/budget:** No active lease or application process. G research and correction consumption, A/B ownership and all earlier receipts remain historical below. The single C activation is consumed successfully; it grants no repeat scan, retrieval, generation, benchmark or human-review decision. Ollama remains developer-owned and running as requested.
- **Next:** None for M3-03. [M303-FINAL-02](#m303-final-02--integrated-review-and-task-closure) closes the task; this archived plan preserves historical commands and consumed grants without authorizing replay. No later roadmap task is selected.

## Progress

- [x] (2026-09-10 17:02Z) Reviewed roadmap-wide state, M3-02 contracts/closure, local-capacity authorities, current source seams and fixed evaluation limits.
- [x] (2026-09-10 17:02Z) Passed independent strict TypeScript, 99 focused tests, original manifest identity and four source/nine local artifact hash checks.
- [x] (2026-09-10 17:13Z) Accepted fresh critical planning PASS without findings and proportional documentation/preservation checks; M303-PLAN-REVIEW-01 owns the limits.
- [x] (2026-09-10 18:17Z) Received exact-task execution authorization; refreshed clean HEAD and absent active lease. Runtime/frozen-input verification follows in M303-ENTRY-02.
- [x] (2026-09-10 18:22Z) Refreshed strict TypeScript, 99 focused tests and the complete M301 static verifier; retained inputs and all three actual abstentions remain unchanged. Recorded current hardware and missing Qwen prerequisite.
- [x] (2026-09-10 18:37Z) Accepted P metadata plausibility with explicit separate RAM/VRAM/storage allowances and margins, published full identities and refreshed resources; actual capacity remains unproved.
- [x] (2026-09-10 19:21Z) Completed the approved runtime launch and fixed Qwen pull; actual version/full artifact identities and unchanged EmbeddingGemma files pass M303-SETUP-02. No inference occurred.
- [x] (2026-09-10 18:37Z) Completed two critical reports, both targeted follow-ups and mandatory analyst synthesis: OWNER DIRECTION at proof scheduling; no G drafting readiness or implementation permission from the result.
- [x] (2026-09-10 18:43Z) Accepted fresh critical R3 stop/evidence PASS without findings and documentation reconciliation; task remains Blocked, with G and capacity unpassed.
- [x] (2026-09-10) Recorded owner acceptance of both start-dependency amendments, preserving all completion proofs and leaving every later task Not started.
- [x] (2026-09-10) Accepted G literals, actual-artifact accounting evidence, exact commands and both mandatory independent R3 checkpoints; final minor F1 dispositioned.
- [x] (2026-09-10) Accepted A: 34 tests, independent strict and corrected complete critical PASS; native HTTP reproduced the repaired status mappings.
- [x] (2026-09-10) Accepted B: frozen adapter composition, 50 focused tests, independent strict and fresh critical PASS with native reproduction.
- [x] (2026-09-16 00:21Z) Reconciled Complete M4-03 and the implemented reviewer interface; refreshed strict/712-test regression and runtime/resource prerequisites in M303-RESUME-03.
- [x] (2026-09-16 00:46Z) Completed the sole exact-configuration C workflow, validated durable output and preservation, observed both models during generation and the reviewer interface, and verified owned cleanup.
- [x] (2026-09-10) Passed complete 550-test regression across 19 suites, independent strict and client build with verified fixture cleanup.
- [x] (2026-09-10) Passed different fresh integrated critical review and documentation handoff checks; retained Blocked status and the active plan because C remains unpassed.

- [x] (2026-09-16 00:57Z) Accepted final independent critical PASS and documentation closure; marked M3-03 Complete and archived the plan with repaired navigation.

## Surprises & Discoveries

- M3-02's shared fit validator checks a trusted report; it does not count Qwen tokens. The actual adapter must prove the exact request, template/schema overhead and effective configuration before transport. A post-response token count cannot establish pre-call fit.
- Existing embedding fit admits 63 preverified inputs, not arbitrary text. All three actual M2-04 retrieval observations abstained. A different page, a reopened aggregate or M3-01's independently assembled guidance cannot be assumed to supply a live supported production Finding.
- The roadmap's smoke includes a usable single-reviewer interface, while Generate UI and human-review UI are later tasks. Existing browser responsiveness can be observed, but this plan cannot relabel it as unimplemented review behavior or move later UI work into M3-03.
- The original M3-01 manifest hash now matches after M3-02 recovery. There is no current byte-recovery task. Historical M3-02 BUG-0001 is Verified; protect its metadata/serialization regressions rather than reopening its consumed correction budget.
- The first P estimate incorrectly combined discrete GPU and system-memory allocations. The targeted follow-up corrected the two pools and established plausible fit with explicit margins; that correction is not a model capacity result. Published metadata identifies the exact parser/renderer, but does not supply complete actual tokenization proof.
- Current Ollama context documentation lists a 4k default below 24 GiB VRAM. The shared output reservation alone is 4096 tokens. A usable complete request therefore needs an explicitly verified larger effective context; no particular context size or capacity PASS is selected during planning.

## Decision Log

- Decision: Activate only M3-03 planning. Rationale: creation of an ExecPlan is not execution or model-acquisition authorization. Date/Author: 2026-09-10 / primary.
- Decision: Reuse M3-02 through two behavioral slices followed by one manual capacity proof. Rationale: fixed request admission and actual transport integration are distinct current responsibilities; the real-device observation has no meaningful TDD Red. Date/Author: 2026-09-10 / primary.
- Decision: Preserve unresolved eligibility/UI proof boundaries as explicit gates. Rationale: narrowing a required real smoke or extending the six-case exception requires its controlling authority, not a worker workaround. Date/Author: 2026-09-10 / primary.
- Decision: Accept only P metadata plausibility and retain G/A/B/C as unpassed after OWNER DIRECTION. Rationale: actual token accounting cannot be proved from synthetic sizing, and current completion dependencies prevent the required reviewer UI from existing for C. The owner must decide start-checkpoint scheduling; no requirement or later task is changed here. Date/Author: 2026-09-10 / primary.

- Decision accepted by the owner (2026-09-10): Permit the two reviewed implementation-checkpoint starts, with every completion proof retained. The roadmap owns the amended scheduling. No later task, external setup action or renewed research budget is selected by this approval.

## Outcomes & Retrospective

G and separate-owner A/B implementation passed their required reviews and controlled verification. Later M3-05 and M4-03 completion supplied the authentic eligible path and implemented reviewer interface while preserving this task's own capacity obligation. M303-C-OBSERVATION-01 now passes the sole representative full-local-stack screen: one fresh scan and supported retrieval, followed by generation of a validated Qwen proposal in 13,340 ms, with both models resident during generation and responsive review controls. The proposal remains pending human review.

Fresh strict TypeScript and all 712 tests pass with unchanged source, build and frozen-input identities. The metadata estimate and this bounded actual result remain distinct; capacity does not qualify semantic quality, arbitrary inputs, sustained stability or hardware support. Final independent review and the documentation closure gate pass; M303-FINAL-02 records acceptance and archival.

## Purpose / Big Picture

An explicit eligible Local action should use the fixed locally present `qwen3.5:4b` through the approved Ollama loopback boundary, return one candidate to existing shared validation, and durably publish a valid pending proposal or bounded failure. Missing prerequisites and input-fit failures create no invocation; an attempted transport failure retains truthful provenance. Scanner, retrieval, sibling evidence and immutable run mode stay intact.

The task additionally proves the exact local configuration can complete the required representative workload on the reference PC without out-of-memory/storage failure or an unusable interface. Test doubles prove adapter boundaries, not that capacity outcome. Passing capacity does not prove remediation quality, broad support, offline operation or release readiness.

## Context and Orientation

### M303-PLAN-01 — reviewed project state

Entry is clean HEAD `4f3140eaecb92304df07d236f575375c4dab92e9`, 240 tracked files and no staged changes. This is historical evidence, never a future executable HEAD comparison. Fourteen roadmap tasks are Complete: RD-001–RD-003, M1-01–M1-05, M2-01–M2-04 and M3-01–M3-02. This request selects M3-03 for planning; thirteen later tasks remain Not started.

Current capabilities include exact-three-rule scanning, minimized evidence, loopback service/single-file persistence, Analyze/Results, the closed corpus, real local embedding retrieval, deterministic support/abstention, pure proposal validation and internal durable generation continuation. Actual Local/Groq generation adapters, Generate UI, human review and comparison are not implemented. At this historical planning checkpoint, M6-03 retained the owner-deferred full-detail 200% visual check. The later [2026-09-15 UTC verification amendment](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md#200-zoom-verification-deferral--2026-09-15-utc) supersedes that destination and defers every 200% zoom test until after MVP.

[M3-02 final acceptance](m3-02-shared-generation-stage.md#m302-final-01--final-integrated-review-and-documentation-closure) records 500 tests across sixteen suites, strict/build, separate-owner A/B/C, critical reviews and closure. Those full-suite/build results are retained, not rerun during M3-03 planning. Fresh pinned Node v24.20.0 strict checking and `run-contract` 68 + `generation-contract` 8 + `generation-stage` 23 tests pass: 99 total. The manifest hashes to original `63770583A97E1D0517337474DADC706C1047D78E026E22C7A745CE7674ED9E6B`; all thirteen path/hash references match. No provider or browser request was made.

[M2-02](m2-02-embedding-retrieval-capacity-gate.md#m202-closure-01--final-integrated-verification-and-documentation-impact) separately passed the actual EmbeddingGemma capacity gate. Its 3846.0786-ms cold retrieval and qualified driver-cleanup observations are historical retrieval-only evidence. Developer-owned runtime/artifact retention does not establish current liveness, grant process control, or pass Qwen capacity. Use current scope/evidence links; do not replay its large historical setup/recovery procedures.

A bounded read-only source explorer confirmed these seams while primary reviewed authority: `GenerationAdapter`/`PreparedGeneration` in `src/server/generation/generation-contract.ts`; `executeGeneration` in `generation-stage.ts`; fit validation in `generation-fit.ts`; source-owned artifacts and input assembly in `generation-artifacts.ts`/`generation-input.ts`; internal `LocalService.generateFinding(input, adapter)`; and existing selected-generation publication. `src/server/retrieval/ollama-http.ts` shows native HTTP/abort/UTF-8 patterns but its protocol and errors are embedding-specific. Its model validators and finite input table are not generic Qwen components.

### M303-PLAN-REVIEW-01 — accepted planning readiness

A fresh read-only critical_reviewer passed the complete five-document candidate against the selected authorities, shared accounting/transport contract, module responsibilities, leases/budgets and actual capacity-proof limits, with no Blocker, Major or Minor. The reviewed plan's SHA-256, captured by primary before verdict reconciliation, was `46BFD82BF823B75092B038B35AA1844D16E187A3F70F3CE98B1A92E96F22558B`; this receipt and current-state maintenance are later non-contract edits, not a claim that the maintained document retains that hash.

The reviewer independently confirmed HEAD, the documentation-only working state, no active lease pointer, original manifest identity and passing git diff --check; it inspected the actual generation/service and finite embedding boundaries. It reused primary's fresh strict/99-test and thirteen-reference evidence and ran no model/browser operation. Primary accepted all five documents' local links/anchors, PowerShell parse, UTF-8, final-newline and whitespace checks. All 237 tracked files outside the three intended existing-document edits match the 240-file entry fingerprint; the plan and progress record are the only new files. Roadmap status is fourteen Complete, one planning-only In progress and thirteen Not started.

Planning used one review and no correction loop. P, G, developer setup, implementation and actual capacity remain unexecuted. The useful distinction is that a safe plan may gate unresolved real proof without approving a synthetic substitute or narrowing task Verification. No requirement, ADR, application, test, dependency or frozen artifact changed.

### Controlling authority route

Read the [roadmap task](../../DEVELOPMENT_ROADMAP.md#m3-03--integrate-qwen-local-generation-and-run-its-capacity-screen) and [project requirements](../../PROJECT_REQUIREMENTS.md), then each selected identifier in its owner:

- [Generation provider execution](../../requirements/generation-provider-and-model-lifecycle/GENERATION_PROVIDER_EXECUTION.md): REQ-LLM-003/005/007/009/011/021; preserve shared REQ-LLM-001/008/019 and fixed controls.
- [Installation/model lifecycle](../../requirements/generation-provider-and-model-lifecycle/INSTALLATION_AND_MODEL_LIFECYCLE.md): REQ-INST-005/017, with 003/004/006 defining developer-owned setup and absence of application acquisition.
- [Privacy/security](../../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md): REQ-SEC-017, with 004/013–016/021 preserving Local-only inference, minimized input and content-safe retention. [Reliability](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md): REQ-QUAL-006/007/009, plus runtime validation/publication requirements.
- [ADR-0003](../../architecture/decisions/ADR-0003-initial-local-generation-evaluation-preset.md), [ADR-0004](../../architecture/decisions/ADR-0004-reference-pc-capacity-gate-for-local-models.md), [ADR-0005](../../architecture/decisions/ADR-0005-ollama-as-initial-local-model-runtime.md), [ADR-0020](../../architecture/decisions/ADR-0020-manual-developer-managed-local-model-setup.md), [ADR-0023](../../architecture/decisions/ADR-0023-local-mode-data-boundary.md) and [local feasibility](../../LOCAL_MVP_FEASIBILITY.md) own exact candidate, runtime, capacity and data boundaries.
- [SPEC-004](../../specs/SPEC.feature): all three immutable-mode, missing-prerequisite and attempted-failure scenarios; [HS-009](../../specs/HARD_SPEC.feature): corresponding hard boundaries. Preserve the [information lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) and [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md).
- [M302-LITERAL-01](m3-02-shared-generation-stage.md#m302-literal-01--authored-runtime-contract) is the current implementation contract. [M3-01](m3-01-generation-evaluation-package.md) and the [six-execution-only exception](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#controlled-generation-input-exception) own evaluation bytes and their limited interpretation.

Applicable task Must requirements are Accepted or explicitly Deferred; runtime selection remains Accepted for evaluation, not qualification. No new requirement/ADR decision is made by this plan.

### Dated primary-source discovery

On 2026-09-10 the [fixed model page](https://ollama.com/library/qwen3.5%3A4b) lists 3.4 GB, 4.66B, Q4_K_M, Apache 2.0 and digest prefix `2a654d98e6fb`. This is discovery, not a full digest, installed identity or prefilter PASS. Its published inherited sampling settings differ from the explicit shared overrides; G must account for all material effective defaults.

The [chat API](https://docs.ollama.com/api/chat) documents messages, schema format, options, stream/think flags and completion fields. [Structured outputs](https://docs.ollama.com/capabilities/structured-outputs) supports passing schema through format; it does not replace application validation. [Context length](https://docs.ollama.com/context-length) documents VRAM-dependent defaults and observable loaded context/offload. The show-page fetch was unavailable during planning; no missing metadata/API behavior was guessed. Refresh material API/runtime claims from current official documentation or the exact runtime source during G. Published maximum context is not effective allocated context, and none of these pages proves complete pre-call counting.

## Scope and Non-Goals

Implement one fixed Local adapter, its request/accounting/model admission and bounded transport response mapping, minimal internal service composition, concise official setup guidance in the appropriate service/developer boundary, and the required single manual capacity observation.

No Groq implementation/call, 9B or smaller replacement, provider comparison, six fixed quality executions, model/prompt tuning, generalized endpoint/configuration registry, schema platform, telemetry, hardware monitor, downloader, native supervisor, new workflow engine, broad extraction, new UI/HTTP Generate route, human-review implementation or machine-wide egress controls. No dependency is preselected. Adding one requires a demonstrated accepted need and frozen effects, not convenience. Rendered changes require scope reconciliation and the frontend-quality overlay before any affected lease; they are not currently planned.

## Plan of Work

Use direct existing Node tests, current service/repository entry points and one documented developer-operated smoke. Prefer an ordinary bounded command to a new harness; no tracked capacity runner or native process-control tool is justified now. Keep the real output/evaluation observation separate from synthetic contract tests.

### P — metadata exclusion before acquisition

After execution authorization, primary refreshes Git/lease state and prerequisite evidence, then records a fixed-candidate metadata screen under ADR-0004. This is source-based admission of the already selected model, not comparison or model selection. Record current artifact/installed storage estimates, chosen prospective context, estimated model plus context/runtime working set, browser/application/EmbeddingGemma overhead, available RAM/VRAM/storage, runtime/driver support and a documented safety margin with units and uncertainty. Recheck current hardware/storage; the dated 513-GB free-space observation is not current evidence.

Do not declare PASS from file size or nominal VRAM alone. A clearly excessive configuration is excluded before download; unknown decisive estimates remain unresolved. Any consequential mechanism/comparison needed to resolve uncertainty uses G's R3 contract before the dependent effect. No separate routine reviewer layer is added just for recording sourced metadata.

Only after P passes may the developer acquire the exact model through official Ollama setup outside the application and repository. Freeze exact setup commands, runtime/version, destination ownership, license/permission boundary, expected disk/network effects and retained artifacts first. Existing runtime reuse must preserve other developer activity; no reinstall, update, model removal, port takeover or process termination is implied. User action or additional approval is required only when the actual setup/permission/license boundary demands it. The planning request performs none.

Capture full artifact digest, quantization, actual runtime, template/tokenizer/parser identity and effective settings after setup without making a generation request. A provisional prefilter is not actual accounting or a capacity pass. If observed facts invalidate it, stop dependent work rather than increasing context/resources silently.

### M303-P-01 — accepted metadata plausibility screen

Primary accepts P with moderate confidence for prospective context **32768**, one Qwen sequence and the existing reference PC, following M303-G-R1-F1 and mandatory M303-G-S1 synthesis. This is a dated estimate with safety margins, not installed-model admission, effective-context proof, guaranteed residency or capacity PASS. The initial combined-RAM estimate is superseded because discrete VRAM and system RAM are separate pools.

The [official manifest](https://registry.ollama.ai/v2/library/qwen3.5/manifests/4b) is 709 bytes, SHA-256 `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`. Primary independently verified the manifest and [475-byte configuration](https://registry.ollama.ai/v2/library/qwen3.5/blobs/sha256:de9fed2251b37295b763727a59ca35cf5cfe5c7379bc3e2104b2ce3c145aa887) in memory. Config SHA-256 is `de9fed2251b37295b763727a59ca35cf5cfe5c7379bc3e2104b2ce3c145aa887`; it declares GGUF/qwen35, Q4_K_M, renderer/parser `qwen3.5` and minimum runtime 0.17.1. The model layer is 3,389,971,840 bytes, SHA-256 `81fb60c7daa80fc1123380b98970b320ae233409f0f71a72ed7b9b0d62f40490`; license and parameter layers are 11,355 and 65 bytes. The [model listing](https://ollama.com/library/qwen3.5:4b) identifies Apache 2.0; this metadata observation is not personal license acceptance. No model layer was acquired. The manifest has no separate projector layer, which does not prove embedded vision tensors have no allocation.

The [published architecture](https://ollama.com/library/qwen3.5:4b/blobs/81fb60c7daa8) gives eight full-attention layers. At 32768 context, `8 * 32768 * 2 * 4 * 256 * 2` bytes yields 1 GiB FP16 attention cache. Recurrent state and headroom raise the allowance to 1.25 GiB. The [v0.33.3 scheduler](https://github.com/ollama/ollama/blob/v0.33.3/server/sched.go#L469) uses one qwen35 sequence and separates discrete VRAM from host RAM; Windows CUDA defaults to disabled mmap. Its automatic compute/batch estimate informs, but does not guarantee, the compute allowance. The generic predictor underestimates this architecture's cache calculation, so its smaller value is not used.

| Pool | Component allowances in GiB | Total including margin | Fresh available |
| --- | --- | ---: | ---: |
| VRAM | Weights 3.157; context/state 1.25; compute 1; EmbeddingGemma 1; safety margin 1 | 7.407 GiB | 7.614 GiB |
| RAM | Potential staging/offload 4; runtime host 1; embedding host 1; additional app/browser growth 1; safety margin 2 | 9 GiB | 11.325 GiB |
| Storage | Acquisition/partial-file allowance 8; safety margin 4 | 12 GiB | 482.568 GiB |

The fresh pre-recording read returned 11,874,884 KiB free RAM and 518,153,322,496 free storage bytes; GPU fields/driver are unchanged from entry. Current application/browser allocations already reduce free resources; the listed growth allowance is additional. Weight size is an allocation proxy, and scratch, host staging and offload remain estimated. VRAM has only 0.207 GiB beyond the included 1-GiB margin. [Published GPU support](https://docs.ollama.com/gpu) and [native Windows support](https://docs.ollama.com/windows) support preliminary runtime plausibility for the observed RTX 5060/577.05 driver. Retained binary/backend identity and actual compatibility still require setup evidence.

An offline researcher sizing probe used current M302 synthetic fixtures through the production input/request builders, without retaining or printing payloads. Source-compatible rendered byte counts were 13,006/14,326/15,102, with 1,637 schema bytes each; adding the 4096 reserve yielded 18,739/20,059/20,835. These explain the prospective context and are neither tokenizer proof nor eligible C inputs. Neither exact tokenization nor the proposed byte upper bound is accepted. Before acquisition/activation, reconcile materially reduced resources, different artifact/runtime/context/sequence count, absent CUDA or contradictory setup evidence; do not silently enlarge limits or replace the model.

### M303-G-STOP-01 — synthesis and owner-controlled scheduling

M303-G-R1/R1-F1 covered identity/counting/P; M303-G-R2/R2-F1 covered wire/lifetime and real proof scope. Both critical researchers were read-only and each used its one targeted follow-up. M303-G-S1, the mandatory decision_analyst, returned **OWNER DIRECTION**, not DRAFT READY. Primary accepts its reconciliation and P disposition. There is no authored G contract and no worker preflight, test/code write, lease, runtime launch, pull, embedding or generation call.

| Contract area | Evidence and remaining boundary |
| --- | --- |
| L1/L2; I3 | Published identity is verified; installed full tokenizer/backend/rendering/effective configuration is absent. Both exact-tokenizer and verified-upper-bound candidates fail the same actual-artifact proof gate. Every message, notice, schema/template/special-token effect and 4096 reserve must be counted before dispatch. Sample sizing cannot authorize transport. |
| L3/L4; I4/I5 | The [qwen3.5 parser](https://github.com/ollama/ollama/blob/v0.33.3/model/parsers/qwen35.go) with explicit Boolean `think:false` keeps thinking empty; the inspected [Go-rendered ChatHandler](https://github.com/ollama/ollama/blob/v0.33.3/server/routes.go#L2574) then applies schema immediately and cannot take its thinking-triggered second-completion branch. This conditional source proof still needs installed-branch correspondence. Exact serialized bytes, bounded normalized results, truncation/shift controls, deadlines and cleanup remain G literals before A/B tests. |
| L1/L4; I3/I4 | [Local source routing](https://github.com/ollama/ollama/blob/v0.33.3/internal/modelref/modelref.go) is not immutable digest addressing. [Name resolution](https://github.com/ollama/ollama/blob/v0.33.3/types/model/name.go) and named manifest loading leave a concurrent replacement race; response model names are echoes. An explicit developer no-concurrent-runtime/model/configuration-mutation precondition, immediate equality checks and observed-drift rejection may be proposed as a task literal. It is not yet accepted G, does not prove atomicity and introduces no hostile-local-actor control requirement. |
| L5; I1/I2/I6/I7 | Live generation consumes the retrieval operation's exact supported snapshot/owner. All retained M2-04 results remain abstained. The finite admission has 47 query variants, so universal abstention is not proved, but no eligible real path is evidenced. Current UI has Analyze/results/retrieval; required Generate/reviewer UI is downstream. Neither historical state nor M301's six-case exception supplies C. |
| L6; I8 | Read-only P commands are bound and verified. Developer setup action below remains external and unexecuted; exact actual-artifact proof/A/B/C commands remain dependent on missing inputs and decisions. Proposed model, fit, HTTP and adapter responsibilities remain bounded to existing consumers; no generic registry, supervisor, monitor or capacity harness is introduced. |

**Accepted scheduling amendment (2026-09-10).** The owner approved the reviewed proposal. The roadmap now changes only the start conditions for two existing tasks: (1) permit M3-05 to start after accepted M3-03 A/B implementation/review plus its existing Groq prerequisite; (2) permit M4-01 to start after an accepted M3-05 implementation/interface checkpoint while M3-05 real-call verification remains pending. M4-02 retains its M4-01 dependency. Keep M3-03/C incomplete until the one full representative smoke can run with the implemented reviewer interface. Keep every M3-05 real eligible Local and Groq call requirement; C may supply Local evidence only when its complete evidence identity and coverage match. Keep M6-02 behind passed capacity and its existing prerequisites. No task is marked Complete merely because its implementation checkpoint passes. This approval amends scheduling only and selects no later task, extra generation, weaker smoke, synthetic production substitute or changed retrieval policy.

The exact authority is L5 above: "If a required UI or eligible path is unavailable, obtain the controlling owner decision before substituting/narrowing the proof or changing task dependencies." The [M3-03 Verification](../../DEVELOPMENT_ROADMAP.md#m3-03--integrate-qwen-local-generation-and-run-its-capacity-screen) requires the active single-reviewer UI; [REQ-QUAL-006](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md) requires the representative end-to-end workflow. Changing only M3-05's dependency would leave the second cycle through M4-01 unresolved. The accepted owner decision permits these two starts to use accepted implementation checkpoints while every completion proof remains required. It grants neither model acquisition nor renewed research allowance.

**Developer setup action prepared, not executed.** Reuse the retained executable under `C:/Users/developer/Tools/Ollama/v0.33.3/` and existing external `C:/Users/developer/Models/Ollama` store; no reinstall, update, removal or port takeover. The developer-owned action is one `ollama.exe serve`, followed in a separate developer terminal by one `ollama.exe pull qwen3.5:4b`, using the absolute retained executable and process-local `OLLAMA_HOST=127.0.0.1:11434`, `OLLAMA_MODELS` set to that existing store, `OLLAMA_NO_CLOUD=1` and `OLLAMA_NOPRUNE=1`. Do not set a global context override that changes embedding admission. First verify unchanged executable identity, an unused loopback port, ordinary external paths, current P margins and exact published manifest identity. The runtime may create its ordinary developer-owned logs/cache/key state and device-discovery children; the pull contacts the official registry/storage and retains approximately 3.157 GiB plus bounded partial data outside the repository. It accepts no new artifact identity silently. Stop on failure or mismatch; preserve partial data and the prior EmbeddingGemma store, with no independent retry or cleanup deletion. The developer retains process ownership and decides when to stop that server. These are concrete proposed effects, not permission inferred from historical M2 grants; explicit developer execution or the genuinely missing setup grant remains necessary before an agent performs them.

After setup, the non-generative observation commands are GET `/api/version`, GET `/api/tags`, POST `/api/show` for the fixed local model, plus read-only full manifest/layer hashes from the external store; freeze bounded callers before use. They must reconcile actual runtime, complete tokenizer/template/parser/effective settings and installed bytes with P. No empty chat, warmup or load request is permitted. The offline accounting command cannot honestly be frozen yet because its actual tokenizer/algorithm is unresolved. Do not turn that missing mechanism into an owner coding assignment or execute placeholders. No fresh target alone guarantees live support; a later owner-authorized eligibility investigation must have one bounded target/data/action packet and preserve any abstention.

Remaining allowance: one supported analyst correction; no researcher follow-up remains. Required G pre-draft and different final contract reviews, all A/B leases/corrections/slice reviews, integrated implementation review and the single C activation remain unused. Any extra researcher investigation needs explicit re-entry authority rather than a new agent ID. This owner stop is distinct from a failed Qwen evaluation and authorizes no replacement.

### M303-SETUP-01 — concrete remaining setup and investigation request

**Owner grant accepted (2026-09-10); execution pending prechecks.** After the separate scheduling decision, the owner explicitly authorized reuse of the retained executable and store for one developer-owned server launch and one fixed-model pull, with the effects and stop rules above, plus one additional bounded read-only actual-artifact report by the existing accounting critical researcher. That report has no further follow-up allowance; use the already remaining single analyst correction afterward. G's required fresh reviews remain separate. This accepted re-entry does not renew any other budget.

Fresh authorized prechecks confirm the retained executable hash and five existing EmbeddingGemma files unchanged, no Ollama process, no listener on port 11434, no inherited OLLAMA variables, and ordinary runtime/store directories. Available resources are 19,251,416 KiB RAM, 7,810 MiB reported free VRAM and 517,704,445,952 bytes storage; P allowances still fit. The 709-byte published manifest still hashes to the pinned full digest. No generation is included in this setup grant.

**Bound non-generative caller:** Use native .NET HttpClient with proxies and redirects disabled, one 20-second cancellation budget per operation, ResponseHeadersRead, bounded stream reads, strict UTF-8 and JSON parsing, and deterministic disposal. Only fixed `http://127.0.0.1:11434/api/version` GET (16 KiB), `/api/tags` GET (1 MiB), and `/api/show` POST (1 MiB, exact JSON body `{"model":"qwen3.5:4b","verbose":false}` with application/json) are admitted. Require HTTP 200, complete body within the bound and expected JSON types; report bounded metadata only. No auth, custom host, redirect, inference, load or warmup operation is admitted. The server PID/executable and listener owner must agree before these requests. Full external manifest/layer SHA-256 and sizes are read-only. Files/paths and ordinary ancestor checks must pass before writes. These setup observations are developer diagnostics, not application startup behavior.

After renewed prechecks pass, the exact native commands are as follows, each in a separate process-local PowerShell environment. For agent execution, launch the server with a hidden window and retain only its own returned process identity; do not terminate an existing process. The developer retains the launched server afterward. Both commands use the same explicit host/model-store values. No global context override is set.

```powershell
$env:OLLAMA_HOST = '127.0.0.1:11434'
$env:OLLAMA_MODELS = 'C:\Users\developer\Models\Ollama'
$env:OLLAMA_NO_CLOUD = '1'
$env:OLLAMA_NOPRUNE = '1'
$m303Server = Start-Process -FilePath 'C:\Users\developer\Tools\Ollama\v0.33.3\ollama.exe' -ArgumentList 'serve' -WorkingDirectory 'C:\Users\developer\Tools\Ollama\v0.33.3' -WindowStyle Hidden -PassThru
```

```powershell
$env:OLLAMA_HOST = '127.0.0.1:11434'
$env:OLLAMA_MODELS = 'C:\Users\developer\Models\Ollama'
$env:OLLAMA_NO_CLOUD = '1'
$env:OLLAMA_NOPRUNE = '1'
& 'C:\Users\developer\Tools\Ollama\v0.33.3\ollama.exe' pull 'qwen3.5:4b'
if ($LASTEXITCODE -ne 0) { throw 'M303 Qwen acquisition failed; preserve state and stop.' }
```

Before the pull, confirm the newly owned server's bounded version response and matching executable identity; do not adopt an unrelated server. Bind the read-only caller before execution. Recheck P resources, published manifest identity, ordinary destination paths and an unused port before launch. Existing model files are preserved; no custom downloader, installation, update, removal, partial-file recovery, automatic retry or generation is included. Native pull transfer concurrency/retries are Ollama behavior, not extra independent pull invocations. Network/storage effects and the observed Apache 2.0 license listing are disclosed above; the owner retains any personal license decision.

The additional report is limited to the acquired pinned model's complete GGUF tokenizer metadata, retained runtime/backend/rendering correspondence, and whether a sound complete pre-call accounting mechanism can be proved. It may use read-only standard-library inspection and the already identified exact runtime sources; it may not acquire another artifact, install a dependency, create production code, call inference or silently select an unproved bound. Freeze any concrete offline command and its effects before execution. If proof still requires another mechanism or operation, report that precise boundary. This keeps artifact acquisition and proof of accounting distinct.

Primary validation of this owner-accepted scheduling amendment passes: the actual five-document diff preserves every roadmap Verification paragraph and all later-task Not started statuses; 211 local links/anchors, UTF-8/final newlines and `git diff --check` pass. Both proposed PowerShell command blocks parse without execution. Protected tracked source/test/configuration/evaluation paths, staging and absent active lease are unchanged. The prior strict/99-test/static-verifier evidence retains its identity; no unchanged product suite is rerun for this documentation-only amendment. This validates documentation and command syntax, not external setup or G.

### M303-SETUP-02 — accepted acquisition receipt and bounded inspection

The approved native pull exits zero with success. The hidden developer-owned server is PID 42700, started 2026-09-10T19:19:10.7475012Z from the unchanged executable; process identity, command line, sole 127.0.0.1:11434 listener ownership and actual API version 0.33.3 agree. The Qwen manifest was installed at 19:20:54Z. Primary verifies its 709 bytes and full pinned digest, plus every config/model/license/parameter layer size and SHA-256 against M303-P-01. All five pre-existing EmbeddingGemma files are byte-identical; no partial files remain. The runtime remains developer-owned and running. No embedding or generation call occurred.

Bounded tags/show observations agree on qwen35, Q4_K_M and the full Qwen manifest digest. Show is 79,766 bytes, advertises completion/vision/tools/thinking, uses placeholder template `{{ .Prompt }}`, tokenizer model gpt2/pre qwen35, and inherited presence_penalty 1.5, temperature 1, top_k 20, top_p 0.95. Its listed architectural context 262144 is not allocated effective context. Non-verbose tokenizer arrays are absent, so the report must inspect installed GGUF metadata. These observations pass acquisition identity only, not G or C.

**Bound M303-G-R1-ACTUAL-01 read-only commands/effects.** Reuse the unchanged README PowerShell definitions and Invoke-M105Command with pinned Node 24.20.0; inline node:fs/node:crypto only. Open the exact completed 3,389,971,840-byte model blob O_RDONLY, parse only the GGUF header/KV region with a 64-MiB total-read ceiling, at most 4096 KV entries, 1,000,000 array elements and 2-MiB individual strings. Reject truncation, unsupported types, duplicate keys and unsafe numeric overflow. Keep full tokens/types/merges in memory, emit only keys/types/counts, ordered-field hashes, flags, byte coverage/type summaries and fixed special-token rows; no tensor reads or vocabulary dump. This is artifact inspection, not executed tokenizer equivalence.

The same cohesive report may hash and read PE version metadata only for these retained backend files under `C:/Users/developer/Tools/Ollama/v0.33.3/lib/ollama/`: `llama-server.exe`, `libllama.dll`, `libllama-server-impl.dll`, `libllama-common.dll`, `ggml.dll`, `ggml-base.dll`, `cuda_v12/ggml-cuda.dll`, `cuda_v13/ggml-cuda.dll`. Do not execute them. Use existing v0.33.3 Ollama renderer/prompt/llm/config source, including its version-linked llama.cpp tokenizer source and exact submodule/build mapping where needed. Such primary-source reads remain within the approved runtime-correspondence question; no new binary, dependency, build or model acquisition is admitted. Record decisive script/source identities in the report. Unknown actual backend linkage remains a proof gap, not permission to launch a runner. Return the one report to the remaining analyst correction; no additional researcher follow-up is available.

The same correspondence inspection may read the retained `C:/Users/developer/Tools/Ollama/receipts/v0.33.3/ollama-windows-amd64.zip`, whose existing SS1 official-release receipt pins 1,469,175,900 bytes and SHA-256 `52cb36a62e7e501f61514f60212dec7117b6c098811357585e02fffe32d2fcd7`. Primary refreshes its full hash once. The researcher may use Python standard-library zipfile/hashlib to stream only the nine exact members corresponding to `ollama.exe` and the eight already named backend files, rejecting duplicate names and unequal lengths/hashes against the actual files. No extraction, write, executable launch or claim about uninspected members follows. This provides binary-to-retained-release correspondence within the approved report; the [existing official-source identity](m2-02-embedding-retrieval-capacity-gate.md) pins Ollama build commit `b79067b0db7417f20108363bc22adb97f35c966a`.

Within the same approved actual-artifact report, one justified extension reruns the identical bounded GGUF parser and exhaustively validates every merge string: split at its first space after character zero, require nonempty left/right tokens, and check left, right and their concatenation all occur in the actual vocabulary. Emit only total merge count and malformed/missing-left/missing-right/missing-joined counts. This closes the b10760 invalid-merge fallback concern before a one-token-per-byte theorem; a nonzero failure leaves that tighter bound unproved and stops this inspection, with no repeated experiment. The same metadata/read/output bounds apply and tensor data remains unread.

### M303-G-ACTUAL-01 — completed accounting evidence for synthesis

The one owner-authorized actual-artifact report returns RESEARCH COMPLETE and recommends the artifact-linked verified upper bound for synthesis. It does not accept G. Full GGUF metadata inspection covers 52 keys and 11,935,649 bytes, SHA-256 `67acb20727d6c482755f0bc14c5914a48a249a5e9ce4be72e18f2300383f6588`; sorted tokenizer JSON hashes to `bc26cd0f6499d5bd69f5f7d1baab46718c56a8d443328edf1de7e58710a8b2f6`. All 248,320 vocabulary strings are unique/nonempty, all 256 initial GPT2 byte symbols exist as normal tokens, and all 247,587 merges have valid left/right/joined vocabulary entries. Merge JSON hash is `d432cdcd2dcfc5a3a814f591ab0d190dc57d950ead71f947623bac4921497ec9`. Nine named installed runtime/backend members match the retained official archive exactly; their PE version fields are absent, and no loaded-library selection is claimed.

Ollama's [pinned llama.cpp version](https://github.com/ollama/ollama/blob/v0.33.3/LLAMA_CPP_VERSION) is b10760. Its [build](https://github.com/ollama/ollama/blob/v0.33.3/llama/server/CMakeLists.txt#L103) and [Qwen35 compatibility translation](https://github.com/ollama/ollama/blob/v0.33.3/llama/compat/llama-ollama-compat.cpp#L783) retain tokenizer metadata. The [BPE implementation](https://github.com/ggml-org/llama.cpp/blob/b10760/src/llama-vocab.cpp#L538) and [Unicode splitting](https://github.com/ggml-org/llama.cpp/blob/b10760/src/unicode.cpp#L1115) reconstruct valid original text before byte encoding. Initial symbols number one per UTF-8 byte, merges only combine them, and the complete vocabulary/merge checks exclude the expanding fallback. Nonempty special spellings consume one token; stripping cannot add content. The fixed profile adds no automatic BOS/EOS or expanding normalizer.

For the exact system/user text-only request, installed [Qwen3.5 rendering](https://github.com/ollama/ollama/blob/v0.33.3/model/renderers/qwen35.go#L186) with explicit false thinking contributes 99 fixed ASCII framing bytes. Keeping message content untrimmed safely overcounts Go trimming. The schema travels separately as grammar; including its serialized UTF-8 bytes is conservative. Proposed bound is `B = utf8Bytes(system.content) + utf8Bytes(user.content) + 99 + utf8Bytes(serializedSchema)`, requiring `B + 4096 <= 32768`. This counts actual immutable messages, all instructions/notices/passages and actual schema serialization; it is not a synthetic ratio or exact tokenizer. Unsupported domain, malformed text, unknown/changed identity or overflow must fail closed.

Primary materialization of the report's existing controls confirms the pinned [ChatRequest fields](https://github.com/ollama/ollama/blob/v0.33.3/api/types.go#L121): `truncate:false` and `shift:false` are top-level booleans alongside `think:false`, rather than invented sampling options. The [completion wrapper](https://github.com/ollama/ollama/blob/v0.33.3/llm/llama_server.go#L261) uses those controls to prevent silent truncation/shift. Exact wire/profile, configured-versus-loaded context semantics and currency remain for the mandatory synthesis and fresh reviews. Actual loading/offload, supported live retrieval, required UI and capacity remain unproved. No further researcher follow-up is available.

### M303-G-SYNTHESIS-02 — complete synthesis ready for pre-draft review

M303-G-S1-C1 returns DRAFT READY and consumes the sole analyst correction. The artifact-linked upper bound is preferred over adding an unvalidated exact tokenizer. The complete L1-L6/I1-I8 outline is ready for the required fresh pre-draft critical review, not worker preflight or G acceptance. Both research follow-ups, the extra owner-authorized actual-artifact report and analyst correction are consumed; review and A/B/C allowances remain.

The source-backed outline freezes configuration before prepare, pure complete body/count construction before metadata I/O, then bounded attempt-time version/show/tags equality checks. Explicit context 32768 is below training context, maps to one runner sequence without automatic context reduction, and requires no warmup. Top-level truncate/shift false preserve the configured fit contract; loaded allocation/backend/context observations remain C evidence. Wire and response model spelling are both exactly `qwen3.5:4b:local`; parent configuration remains `qwen3.5:4b`. Bare-tag response spelling is rejected. Metadata currency is observed under the developer no-concurrent-runtime/model/configuration-mutation precondition, not atomic digest addressing.

The G artifact outline is six sections: (L1) fixed configuration and actual evidence; (L2) bound, admitted domain and proof vectors; (L3) exact wire/results; (L4) one-attempt lifetime/error/cleanup; (L5) existing internal service integration and preserved C gates; (L6) exact four module callables, three tests/helper and maintained commands/effects. It includes the cumulative I1-I8 map. Proposed native transport is fixed loopback/agent false with 16-KiB headers, 1-MiB body, fatal UTF-8, complete HTTP, assistant/done/stop/exact model admission, absent/empty thinking and no media/tools/remote fields. Shared 120-second action deadline includes metadata operations individually bounded to 10 seconds. HTTP 200 invalid/incomplete maps to incomplete-output, 429 to rate-limit, transport errors to network, other non-200 to provider; no repair, raw diagnostic retention, retry or fallback. Complete cleanup requires settled operation-owned resources; no runner stop/unload is inferred.

The existing LocalService.generateFinding(input, adapter) remains the composition seam. No automatic adapter registry, new Generate route/UI, dependency or acquisition mechanism is added. Primary will materialize exact signatures and command callers after the pre-draft PASS; actual G receives a different fresh complete review before worker preflight. The owner scheduling amendments preserve C's supported-live-input and implemented-reviewer-UI conditions and all real-call completion proofs. No later task is selected.

### M303-STOP-REVIEW-01 — reviewed owner handoff and documentation impact

A fresh critical_research_reviewer returned **PASS for the stop record and owner-handoff readiness only**, with no Blocker, Major or Minor. It reviewed the complete plan (pre-receipt SHA-256 `8CFD42BF2242D34D95D282B8E5F4F872CCFE2ECDBEC977B3C3D2E0F408533885`) and all five actual documentation diffs against L1–L6/I1–I8. The reviewer independently confirmed both dependency edges, retained completion gates, source-conditional parser behavior, live retrieval ownership, the 16+47 admission entries, P arithmetic, clean protected paths, original manifest/configuration identities and absent active lease. It reused the fresh strict/99-test/static-verifier evidence and metadata readback rather than rerunning unchanged tests. This accepts no G contract, scheduling amendment, implementation or actual capacity result. This receipt and current-state reconciliation are later non-contract maintenance, not a claim that the maintained plan retains that hash.

Documentation impact: Updated this plan, the roadmap's execution/status statements, plan navigation, task progress and its status index. Requirements, ADRs, dependency edges, application/source/tests/configuration and frozen evaluation inputs are unchanged. Primary checked all five intended changed paths, UTF-8/final newlines, 208 local links/anchors before this receipt, no staged changes, no active lease and `git diff --check`; the two pre-existing template placeholders in the progress instructions are examples, not broken links. The initial generic link-check assertion at an example was corrected without editing that example or invalidating product evidence. Final checks include the added receipt navigation. Full-suite/build and implementation reviews remain required at actual task closure; none is claimed by this documentation-only stop.

### G — freeze the adapter and proof contract

Before A/B worker preflight, accept the following unresolved literals through the Decision Review Contract. Metadata-only inspection/setup may precede complete G only within P's recorded authority; it cannot start generation. If actual artifacts are necessary for the proof, retain the research barrier until available instead of accepting placeholders as proven.

| Literal | Required decision/evidence before its consumer |
| --- | --- |
| L1 Local identity/configuration | Fixed loopback origin/path/method policy; concrete runtime and full digest, quantization, tokenizer/template/parser/effective-configuration identities; immutable snapshot creation and currency checks without startup probes. Resolve the fact that configuration must exist before shared prepare but availability is checked only at explicit generation. Map missing runtime/model, drift and malformed metadata to existing bounded categories. |
| L2 Complete fit | Exact tokenizer or demonstrably sound upper bound for the complete wire-equivalent request, instructions, canonical notices/passages, schema/template/special-token overhead and 4096 output reserve. Freeze algorithm/version, supported domain, proof vectors, context/output limits and effective-context enforcement. A finite lookup must cover every admitted production request or fail closed; embedding's 63 rows and M301's three inputs are not universal generation proof. Unknown counts, guessed ratios, post-call counts or synthetic test values cannot authorize transport. |
| L3 Wire and result | Exact prepared serialized bytes and mapping of existing Local controls, schema and model routing; response/model/completion/finish-reason admission, error statuses, bounded byte/UTF-8/JSON handling and no repair. Resolve truncation/incomplete output, unwanted tool/thinking fields and safe non-retention. Enumerate permitted metadata requests separately from the single generation request. |
| L4 Lifetime and provenance | All serialization/prerequisite/accounting work before attemptTransport; exactly one actual HTTP start inside its thunk; no redirect/fallback/retry; total shared deadline, bounded I/O cancellation, complete versus uncertain cleanup, drift before dispatch and late-result rejection. Preserve BUG-0001's safe snapshots and selected-only durable truth. |
| L5 Integration/proof scope | Minimal real adapter factory and internal service consumption, no provider I/O at import/startup/selection/Analyze. Establish a live actually supported retrieval route within the current finite embedding boundary and a precise representative-interface interpretation. No manufactured RetrievalResult, edited abstention or use of the six-case exception for capacity. If a required UI or eligible path is unavailable, obtain the controlling owner decision before substituting/narrowing the proof or changing task dependencies. |
| L6 Execution effects | Exact A/B callables/files/tests; P/setup/metadata commands; one C activation, authorized target/data and isolated state, browser/runtime ownership, observations, output retention and cleanup; failure stop and remaining allowances. Freeze native exit propagation and commands before use. |

Preserve M302 runtime instructions/schema/control versions and M301's separate original six-case bytes. A new exact runtime setting may be a task literal, but changing an accepted instruction, model, eligibility policy, parameter or capacity obligation follows its existing authority. Do not repurpose shared validators to conceal an incompatible adapter.

### M303-G-CONTRACT-01 — authored Local adapter contract

**Review state:** Primary authored after M303-G-PREDRAFT-01 PASS WITH FOLLOW-UPS. Its only finding, stale setup wording F1, is reconciled in Outcomes; the pre-draft evidence covers all I1-I8 without a Blocker/Major. The reviewer inspected plan hash `3BAA9307EB12301F3E8F46785E6007C7A0C1A40F4F8A1F631D309F757DDECE4C`; later maintenance and this authored contract intentionally change that hash. Different fresh final review M303-G-FINAL-01 passed with only current-state wording F1, now dispositioned. Primary accepts G; this does not pass A/B or C.

#### L1 — fixed configuration and admission

`ollama-generation-model.ts` owns the deeply frozen `QWEN_CONFIGURATION: GenerationConfiguration`, its private immutable effective-profile snapshot, and `validateOllamaGenerationMetadata(metadata: {version: unknown; show: unknown; tags: unknown}): 'missing-prerequisite' | 'configuration' | null`. No constructor/import performs filesystem, process or provider I/O. There is no caller-selectable runtime, model, endpoint, context or parameter dictionary.

The configuration uses existing Local provider context and M302 versions/LOCAL_PARAMETERS unchanged; adapterId `ollama-generation`, adapterVersion `m303-ollama-chat-v1`, endpoint `ollama-loopback-chat`. Local binding is runtimeVersion `0.33.3`, modelDigest `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`, tokenizerIdentity `sha256:bc26cd0f6499d5bd69f5f7d1baab46718c56a8d443328edf1de7e58710a8b2f6`, templateIdentity `ollama-qwen35-renderer-v0.33.3`, parserIdentity `ollama-qwen35-parser-v0.33.3`, effectiveConfigurationIdentity `m303-qwen35-32768-v1`. Accounting is verified-upper-bound, implementationVersion `m303-qwen35-utf8-bound-v1`, same tokenizer identity, contextTokenLimit 32768 and outputTokenLimit 4096. Component/source identities are the complete SETUP-02/ACTUAL-01 packet, not abbreviated prefixes.

The private profile records Q4_K_M/qwen35, configured context 32768, one sequence, fixed renderer/parser, no automatic BOS/EOS, top-level thinking/stream/truncate/shift false, keep-alive 5m and automatic GPU offload. It records model-inherited presence_penalty 1.5/top_k 20; model temperature 1/top_p 0.95 are explicitly overridden by M302's 0/1. Remaining [v0.33.3 defaults](https://github.com/ollama/ollama/blob/v0.33.3/api/types.go#L1034) are source-bound: num_keep 4, typical_p 1, repeat_last_n 64, repeat_penalty 1, frequency_penalty 0, seed -1; runner num_batch 512, num_gpu -1, num_thread 0, draft_num_predict 4 and use_mmap unset, with other implicit defaults tied to that implementation. Scheduler-selected batch/offload/backend and Windows CUDA mmap policy remain automatic source-bound settings, not claims of observed allocation. Do not add sampling overrides to the shared parameter record.

At explicit eligible preparation only, request version, show, then tags. Version must be exactly 0.33.3. Require exactly one qwen3.5:4b tags entry with matching name/model/full digest, GGUF/qwen35/Q4_K_M; a well-formed list without it is missing-prerequisite, duplicates/malformed/drift are configuration. Show must match GGUF/qwen35/Q4_K_M, empty parent model, completion capability, placeholder template `{{ .Prompt }}`, architectural context 262144, gpt2/qwen35 tokenizer, false add_eos/add_padding and absent-or-false add_bos. Its four unique parameter lines must parse to presence_penalty 1.5, temperature 1, top_k 20, top_p 0.95, with no additional override. Reject remote routing metadata. Validate selected fields as own data with bounded failure on malformed values; unrelated metadata is neither trusted nor retained. The full manifest binds the remaining tokenizer/config data to the verified artifact. Non-verbose null tokenizer arrays are not fresh vocabulary proof.

The developer operating precondition is unchanged retained binary/backend/model/configuration and no concurrent runtime/model/configuration mutation during the action. Check observed metadata immediately before dispatch; observed drift fails closed. This is proportionate developer-managed operation, not atomic digest addressing or protection against a hostile local actor. A changed runtime, artifact, renderer/parser, tokenizer flags, control domain or inherited configuration invalidates this proof. Cold-model fit uses the explicit source-enforced context; C must separately observe actual loaded context/backend/offload. No load/warmup request is permitted.

#### L2 — immutable wire and complete fit

`ollama-generation-fit.ts` owns `prepareOllamaGenerationWire(request: GenerationRequest): {ok:true; body:string; fit:GenerationFit} | {ok:false; error:'configuration'|'input-fit'}`. Return frozen records and fit; the body is an immutable string. Require the request's exact QWEN_CONFIGURATION reference, M302 version/control correspondence, exactly two closed role/content messages in system/user order, well-formed Unicode strings and the fixed M302 schema. Schema identity is the shared GENERATION_SCHEMA object produced by the existing request builder; a different schema is unsupported input-fit. Configuration/control/version substitution is configuration; unsupported message domain, malformed text or arithmetic/serialization failure is input-fit. No truncation, omitted passage, prompt rewrite or fixture lookup is allowed.

Serialize the actual schema and complete body once before metadata I/O. The admitted body has exactly model `qwen3.5:4b:local`, messages, format (the actual schema object), stream false, think false, truncate false, shift false, keep_alive `5m`, and options `{num_ctx:32768,num_predict:4096,temperature:0,top_p:1}`. Shared `responses:1` is a local contract guarantee, not an invented Ollama wire field. No tools, images, prior history, credentials, remote host, debug-render flag or extra options enter the body.

Compute `B = Buffer.byteLength(system.content,'utf8') + Buffer.byteLength(user.content,'utf8') + 99 + Buffer.byteLength(serializedSchema,'utf8')`. The ACTUAL-01 artifact-wide proof, independently reproduced by the analyst and pre-draft reviewer, establishes this upper bound. Original untrimmed content safely overcounts renderer trimming; schema allowance is conservative grammar overhead. Count the exact admitted strings, including all application instructions/notices/passages; transport JSON escapes are decoded before rendering. Require safe nonnegative integers, fixed limits and `B + 4096 <= 32768`; otherwise return input-fit before any metadata or generation request. Fit references the exact configuration.accounting and reports inputTokens B, reservedOutputTokens 4096 and the fixed context/output limits. Never substitute post-response counts or synthetic values for admission.

A vectors cover empty/whitespace content, multilingual/non-BMP and combining characters, literal special-token spellings, escaped control characters, malformed surrogates, full notices/schema, configuration/domain substitution, exact maximum and one byte over. Tests exercise the implementation of this already proven mechanism; they do not replace its artifact/source theorem.

#### L3 — bounded native wire and results

`ollama-generation-http.ts` owns the native HTTP boundary. Export `OllamaNativeRequest = (options: RequestOptions, callback: (response: IncomingMessage) => void) => ClientRequest`; production defaults to node:http.request. Injection is an internal test seam, not a runtime-configurable destination. Every request fixes hostname 127.0.0.1, port 11434, agent false and maxHeaderSize 16384. No proxy, redirect, authentication, custom destination or fallback exists.

`requestOllamaGenerationMetadata(kind:'version'|'show'|'tags', signal:AbortSignal, requestImplementation?:OllamaNativeRequest)` returns a promise of `{ok:true,value:unknown,cleanup:'complete'}` or `{ok:false,error:'missing-prerequisite'|'configuration',cleanup:'complete'|'uncertain'}`. It maps to GET /api/version, POST /api/show with exact `{model:'qwen3.5:4b',verbose:false}`, or GET /api/tags. Empty GET body; JSON content-type and byte-accurate content-length for POST. Each operation has a 10000-ms total cap and the shared abort signal. Connection refusal/unavailable runtime and HTTP 404 map to missing-prerequisite; other bad status, malformed response or identity data maps to configuration. Destruction without settled owned resources is uncertain. These requests contain no Finding data.

`dispatchOllamaGeneration(body:string, signal:AbortSignal, attemptTransport:AttemptTransport, requestImplementation?:OllamaNativeRequest)` returns a promise of existing M302-compatible closed dispatch envelopes: success `{ok:true,candidate:unknown,complete:true,cleanup:'complete'}` or failure `{ok:false,error:'incomplete-output'|'rate-limit'|'network'|'provider'|'timeout'|'shutdown',cleanup:'complete'|'uncertain'}`. It uses POST /api/chat, application/json, exact precomputed UTF-8 content length and the unchanged prepared body. All body/header preparation happens before the capability. The capability's start thunk creates exactly one native request and initiates that prepared body; it contains no admission, serialization or metadata lookup.

All bodies are bounded to 1048576 bytes, with fatal UTF-8, complete HTTP framing (`IncomingMessage.complete`), bounded event handling and one outer JSON parse. HTTP 200 must contain an object with model exactly `qwen3.5:4b:local`, assistant message, string content, done true and done_reason `stop`. Thinking is absent or the empty string. Reject image/tool/remote fields; do not retain optional diagnostics or reasoning. Parse content exactly once into one JSON object, then return it to existing shared proposal validation. No Markdown extraction, repair or second generation. Optional timing/usage fields cannot authorize fit and are not persisted by this adapter.

HTTP-200 oversize/encoding/JSON/model/completion/message/content failures normalize to incomplete-output, preserving M302 response/failed semantics. HTTP 429 is rate-limit; other non-200 is provider, including 404 after entry. Transport failure without an admitted HTTP output is network. Signal cancellation is shutdown; an operation-owned deadline is timeout, with the shared stage retaining final timeout/shutdown precedence. Never read exception text into a result or log raw content.

#### L4 — lifetime, preparation and cleanup

`ollama-generation.ts` owns `createOllamaGenerationAdapter(requestImplementation?:OllamaNativeRequest): GenerationAdapter`. It returns a frozen adapter referencing QWEN_CONFIGURATION. Prepare first performs the pure L2 construction/fit check; unsupported/non-fitting requests therefore make zero requests. It then runs L1's three bounded metadata operations sequentially, with tags last for current digest observation. Any failure returns the closed M302 preparation envelope with truthful cleanup. Successful preparation returns the exact original request/configuration/fit references and inert dispatch closure, cleanup complete, with no outstanding owned I/O. Never reserialize the body after admission.

Dispatch calls the native boundary once with the captured body and stage capability. Shared generation-stage.ts owns its 120000-ms total action deadline, one-use authority, safe metadata capture, selected result validation and late-result precedence. Metadata time is included, not added afterward. Observe abort before native start and during all I/O; clear timers and detach or settle owned listeners/resources. Complete cleanup requires terminal owned request/response/socket state and no possible later operation-owned I/O. A returned promise or destroy() alone is not cleanup proof. Network/cancellation with uncertain server settlement stays uncertain and closes subsequent service admission through M302. Never claim that disconnect stopped or unloaded the Ollama runner. Ignore late callbacks without changing a settled result or starting I/O. Synchronous native-start throws still occur inside attemptTransport and count as attempted failures.

B vectors use the actual adapter through existing stage/service/repository seams: zero I/O at import/construction, no generation on scan/selection/abstention/non-fit/missing/drift; ordered metadata before exactly one native chat start; synchronous/asynchronous errors, malformed or incomplete output, status mappings, timeout/shutdown, duplicate/late events, cleanup uncertainty and durable selected-only truth. Existing BUG-0001 regressions and accepted A tests remain unchanged during Green.

#### L5 — internal integration and preserved capacity boundary

Use the existing `LocalService.generateFinding(input, adapter)` call with the real factory as the minimal composition seam. B tests supply that real adapter with controlled native HTTP, then exercise actual live service/repository ownership and publication. No automatic default adapter, provider registry, service HTTP route or UI is added; existing shared/service production files need no change under this contract. Later explicitly selected Generate integration owns its visible route. The capacity caller will explicitly pass the same factory only after C's real prerequisites pass.

An authentic supported live retrieval owner is mandatory for real generation. Preserve all M2-04 abstentions and the 16-document/47-query finite admission. M301's six-case exception remains exclusive to its six M6-02 evaluations. Owner-approved scheduling allows accepted A/B and M3-05 interface checkpoints to unblock specified later starts, but selects none. C still requires the implemented reviewer interface, exact target/data/action authorization, actual supported retrieval, one smoke, durable readback and observed resource/UI outcome. No C command is activated by this G contract, and M3-03 cannot become Complete without it.

#### L6 — responsibilities, commands, effects and review evidence

A creates only model/fit/http production modules above and `tests/ollama-generation-contract.test.ts`, with `tests/helpers/m303-ollama-fixture.ts` if needed. B creates only the adapter module and `tests/ollama-generation.test.ts` plus `tests/ollama-generation-service.test.ts`; the test owner may extend its existing M303 helper in B before Green. Model admission depends on shared contracts; fit depends on shared artifacts/configuration; HTTP depends on native HTTP and shared types, with no browser imports; adapter depends on those three owners. Reuse existing stage/service/repository behavior unchanged. Private helpers within these owners may be structured for their current responsibility; no new generic utility, dependency or speculative extraction is authorized. Every lease packet repeats this placement/direction and exact allowed/protected paths; tests remain protected during Green.

The complete caller loads only README's first PowerShell definitions block from this repository root using the unchanged lines 80-173, checks Node v24.20.0, and uses Invoke-M105Command with compile cache disabled and restoration verified. Its exact preparation is:

```powershell
$m303Readme = Get-Content -LiteralPath README.md
. ([scriptblock]::Create(($m303Readme[79..172] -join "`n")))
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Pinned Node required' }
}
```

The packet's full caller repeats that preparation and places the single applicable command block below inside Invoke-M105Command; preparation and the chosen check are one PowerShell tool invocation, with no implicit install/build/retry. If README changes, reconcile its exact heading/fence selection before execution. Use these exact focused commands inside that wrapper, checking LASTEXITCODE immediately and throwing before a dependent step on nonzero:

```powershell
# A focused check
& $m105Node --test --test-timeout=120000 tests/ollama-generation-contract.test.ts
if ($LASTEXITCODE -ne 0) { throw 'M303 A focused tests failed' }
# B focused check (A remains protected regression coverage)
& $m105Node --test --test-timeout=120000 tests/ollama-generation-contract.test.ts tests/ollama-generation.test.ts tests/ollama-generation-service.test.ts
if ($LASTEXITCODE -ne 0) { throw 'M303 B focused tests failed' }
# Independent strict check after accepted Green
& $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
if ($LASTEXITCODE -ne 0) { throw 'M303 strict TypeScript failed' }
```

Run only the applicable focused command at its boundary, not both indiscriminately. The primary or independent reviewer owns the independent strict result. The already verified pure runner/environment permits the first-module exception only after test-worker preflight confirms these exact missing exports; initial missing-callable Red never claims executed behavior.

Tests use injected native HTTP or exclusively owned ephemeral loopback fixture ports, never Ollama port 11434 or a model/provider endpoint. B filesystem tests exclusively create ordinary `temp/m303-generation-*` leaves, preserve any pre-existing leaf, and remove only their own verified-contained leaf in finally after service/socket closure. Reuse the M302 fixture ownership pattern; do not reconstruct a historical supported runtime capability. No model or browser action is part of A/B focused commands. Existing controlled browser suites run only in the authoritative full-suite caller at integration, with their maintained scratch/browser ownership.

After B, run affected generation/service regressions, independent strict check and the maintained client build after inspecting ordinary ignored dist/client output; preserve unexpected files and follow its existing bounded replacement procedure. Update README's authoritative suite inventory to include the three new files, then run its full sequential caller, including owned scanner/UI scratch preparation and teardown. No browser download or dependency operation is authorized. Full task closure still requires C and a different fresh integrated critical review; accepted reusable evidence retains the complete command/tree/environment/guard identity. Risk-critical reproduction remains mandatory. A and B each require their own fresh critical slice review and cumulative I1-I8 disposition; no reviewer report substitutes for actual diff, unchanged-test and terminal compliant lease inspection.

| Invariant | Current contract/evidence | Required implementation or capacity proof |
| --- | --- | --- |
| I1 | Selected Accepted authorities, owner scheduling amendments and L1-L6 covered; pre-draft accepted after F1 disposition | Different final G review, A/B reviews, every unchanged completion gate |
| I2 | Original M301 identities and three actual abstentions preserved; M302 artifact/control contract unchanged | Guarded no-drift and full frozen-package verification when identity changes |
| I3 | SETUP-02, ACTUAL-01, S1-C1 and independent pre-draft reproduce complete bound and fixed profile | A/B domain/count/wire/drift vectors; C loaded context/backend/readback |
| I4 | Fixed native loopback boundary, explicit local suffix, no startup/selection/provider probe | A/B zero-call, destination and no-redirect tests; C endpoint observation |
| I5 | Exact attempted-entry, status/output, deadline and cleanup mapping in L3-L4 | B timing/cleanup/late-publication regressions and preserved BUG-0001 tests |
| I6 | Existing live owner/service/selected-only publication reused without contract changes | B real service/repository tests; C authentic supported retrieval and durable readback |
| I7 | P preceded the one authorized pull; full capacity/UI obligations and one C activation retained | C remains unpassed until all real prerequisites and observation succeed |
| I8 | Four cohesive production owners, test-only fixture, exact command/effect boundaries | Actual diff/cohesion, compliant leases, owned test-resource cleanup and final review |

The unchanged per-slice budget is one preflight, one Red, one Green, conditional bounded corrections under the worker procedure and one review correction loop. No research or analyst allowance remains. Final G has at most two supported correction cycles with complete fresh R3 review; material new mechanisms return to authority reconciliation without renewing budgets. Existing C allowance is exactly one activation and remains unused.

### M303-G-FINAL-01 — accepted complete contract

The different fresh critical research reviewer returned PASS WITH FOLLOW-UPS on complete plan SHA-256 `26BF8861E46FA054BA39F0FF54F92400057931F53DDFB467F8F8AC8D6C889089`. No Blocker or Major exists across L1-L6/I1-I8. Minor F1 identified stale current-state pre-draft availability; primary reconciled those bullets while retaining consumed allowances and the cumulative two-cycle final-artifact ceiling. This is documentation-only disposition, with no normative contract correction or technical evidence invalidation.

Independent reproduction confirms the complete GGUF vocabulary/merge proof and 99-byte framing, the pinned Go-rendered branch, content-only parser state, immediate schema and explicit-context scheduler/runner conditions. Protected source/test/evaluation/configuration paths, original identities, empty staging, absent lease, PowerShell parsing and diff checks passed. Full artifact/archive hashing, nine-member installed correspondence and unchanged strict/99-test/static-verifier evidence were explicitly reused. Loaded allocation, live eligible retrieval, reviewer UI and C remain unavailable. Primary accepts G and opens only A/B implementation under its existing gates.

### M303-A-PREFLIGHT-01 — initial implementation assignment boundary

Workflow `M3-03-20260910-implementation`, slice `M3-03-A`, standard profile, TDD Applicable, S3. A owns the exact model/fit/HTTP exports in L1-L3; their dependency direction and bounded creation remain L6. The separate test owner performs one read-only preflight, with no lease or writes. Frozen intended tests are `tests/ollama-generation-contract.test.ts` and optional `tests/helpers/m303-ollama-fixture.ts`; no accepted test boundary exists yet. Constructor/adapter orchestration remains B. No existing application module, shared contract, configuration, frozen evidence, browser/model endpoint or developer runtime mutation is authorized.

The preflight packet references the selected-authority section, M303-ENTRY-02/SETUP-02/ACTUAL-01 and accepted G. Its safe command is the pinned Node version check and read-only source inspection; runner/strict/99-test evidence from unchanged M303-ENTRY-02 is reusable. Any missing-callable classification must cite exact existing contract seams and independently verified runner identity, not search absence alone. The first-module exception may then provide qualified initial Red; complete behavioral assertions must execute unchanged at Green. Primary accepts the result before opening any write lease. Focused and task-level commands/effects remain L6 and the maintained README caller; external environment is non-reusable for capacity.

**Accepted preflight:** M303-A-PREFLIGHT-01 returned MISSING. Exact three A modules and both proposed test paths are absent; existing shared configuration/request/fit/stage and embedding HTTP seams lack Qwen-specific admission, full accounting and response/lifetime behavior. HEAD/protected-tree comparison, pinned Node version and executable hash match ENTRY-02; runner/strict/99-test evidence remains reusable. Primary accepts the first-module exception: the complete static-import behavior suite may initially fail `ERR_MODULE_NOT_FOUND`, with behavioral assertions explicitly unexecuted until unchanged Green. This is not an existence-only test.

**Red assignment:** M303-A-RED-01, lease `m303-a-red-01`, test_worker owner `qwen-a-test`, attempt 1, parent None. The complete separate test owner writes only the two A test/helper files declared in L6, runs the A focused command once after coherent coverage, and returns for terminal closure. No source/configuration/authority/evaluation changes or external provider/browser calls. All G contracts, S3 review, budgets, first-module limits and L6 commands/effects remain binding. The immutable guard digest is supplied in the coordinating packet after lease start; no write is permitted between worker turns.

**Red handoff and bounded correction:** Initial Red returned the exact missing model import (13 behavioral groups unexecuted), and lease `m303-a-red-01` closed-compliant with only the two permitted created files and no Git drift; receipt `5804c535588faa782256b73a9119fab00f52ae82893a51e55438c6b1fa8e3e51`. Primary inspected both files. Before accepting the test boundary, the first Red correction closes specific existing-contract gaps: empty/whitespace and multilingual text, actual complete builder notices/schema, closed message-domain/accessor inputs, metadata parameter/tokenizer drift, content-level thinking/object-only parse, and late/incomplete response plus unsettled terminal-resource cleanup. These are L1-L4 coverage obligations, not new scope. Initial hashes are test `D1EE9ABEDFBE9F7D2C588A00386191A548160CAF92BCD54E0B0E81D28B1359ED` and helper `D7F24123475E58ECFDA5C16B08FD873979205050090567A684035C7A68467ED7`; this correction invalidates their candidate boundary, not prior shared evidence. Assignment M303-A-RED-02/lease `m303-a-red-02`, same owner/phase/scope, attempt 2 with immediate parent `m303-a-red-01`; one conditional test correction remains. Green has not begun.

**Accepted Red boundary:** M303-A-RED-02 supplies 17 complete behavior groups and the purpose-bound in-memory fixture. Exact focused caller again reports only `ERR_MODULE_NOT_FOUND` for the frozen model module; all behavioral assertions remain unexecuted. Primary inspected both complete files and accepted their domain, actual-builder, drift, wire and terminal-resource coverage. Lease `m303-a-red-02` closed-compliant, receipt `888c0116ed4593cf54fd3d4adaab861ec58ec3a6fbd577a9de5eab578942db36`, only two permitted modifications and no Git drift. Accepted hashes: test `C8AD3CD6CDE86491EABD293F1314063A0521A37C8D2D6E527B05E6248B7EF6C2`, helper `BFD0191A18C867907E26EB03B4DA1489292BA185B7B78BFB6346E3DFB787DCC6`. Both are protected unchanged during Green.

**Green assignment:** M303-A-GREEN-01/lease `m303-a-green-01`, separate code_worker owner `qwen-a-code`, attempt 1, parent None. Writes exactly the three model/fit/HTTP source modules in L6; all tests/helpers, existing sources and authorities/configuration remain protected. Implement complete G semantics, run all 17 unchanged tests and strict TypeScript, and hand off actual diff/cohesion and owned-resource cleanup for primary inspection and independent strict/review. No production adapter B or actual provider operation is authorized.

**Green candidate and independent strict:** M303-A-GREEN-01 created only the three declared source owners and executed all 17 accepted unchanged behavior tests successfully. Initial production-only compiler errors were corrected within Green; final focused and strict commands exit 0. Primary closed lease `m303-a-green-01` compliant, receipt `06fdb77b9dd9a71c40c542bba27f63c6b0c1a09629e1d870970700cba2f3663b`, with no forbidden path or Git drift. Primary inspected every source/test/helper, confirmed protected test hashes and independently reran strict TypeScript successfully. Cohesion is RETAINED: model admission, pure body/fit and native HTTP have their named owners; existing application/shared modules remain unchanged. Final source hashes: model `9418E6459EA1E6857B5BE6F5F2FB2ED8E5E2B9B5314847F65B65E4CC0076249F`, fit `DAE8E9A837FFD90A0ADD5DA13D428BEF58F805A3A817DF5E409ADAACB0FBE85B`, HTTP `3AAE71A625C35ACC68F6F6756844ACBEC84AA407D7C731ED08F05199AB0CF7A6`.

Primary interim inspection corrected non-refusal connection uncertainty, known non-200 size/error mapping and premature request-close handling inside the initial Green turn; final changed-boundary checks supersede earlier results. Fresh S3 review M303-A-REVIEW-01 remains required before A acceptance/B preflight. Its independent risk reproduction may use native HTTP redirected only by the test seam to a newly owned ephemeral loopback fixture, never port 11434, with bounded timers and finally-close of its own server/sockets; no file/browser/model/provider operation or source edit is permitted. Existing 17-test/strict evidence is reusable at these exact hashes; the native proof does not establish Ollama conformance or capacity.

**Fresh A review and sole review-correction loop:** M303-A-REVIEW-01 returns REVISE: no Blocker/Minor, one Major A1 in I5. Native fixtures receive non-200 headers, declare Content-Length 1000, write one opening brace and destroy the response. Chat 429 incorrectly becomes network instead of rate-limit; chat 503 becomes network instead of provider; metadata 404 becomes configuration instead of missing-prerequisite. Cleanup is correctly uncertain. Response/socket error and premature-close branches discard the known status; preserve it while retaining explicit timeout/shutdown precedence. Primary confirms those branches in the actual source. This is a routine contained task correction under the bug-record rule, not a separate defect program.

Other I1-I4/I6-I8 and cohesion pass: independent native bytes/one-attempt/success/chunking, statuses/redirect, UTF-8/size/header bounds, reset, abort and refusal probes succeeded. Dedicated probe teardown awaited all owned terminal events and ended with closed servers and zero owned sockets; no Ollama call occurred. The five-file identity remains unchanged. Primary dispositioned plan-only post-Green-closure drift as expected receipt/review maintenance, with no source/test/Git drift or implementation-evidence invalidation.

**Conditional Red correction 3:** The complete first-module boundary now executes 17 passing tests; new decisive native A1 evidence supplies both concrete progress and a bounded new action, satisfying the conditional third-turn rule. Assignment M303-A-RED-03/lease `m303-a-red-03`, owner qwen-a-test, attempt 3, parent `m303-a-red-02`, same two test/helper paths. Add only regressions for preserved admitted error status across response/socket/request failure after headers, with uncertainty and timeout/shutdown precedence; retain all earlier assertions. Focused execution must fail behaviorally at A1, not at an import. No test correction remains after this turn; Green has two possible corrections, and this consumes the sole A review-correction loop. No new IDs renew a budget.

**Accepted behavioral Red for A1:** M303-A-RED-03 adds 15 category/event subtests (metadata 404, chat 429/503 across response error/abort/close, socket error and request error after headers), plus explicit shutdown/deadline precedence. Decisive execution reports 34 tests: 18 pass and 16 fail (all 15 intended A1 subtests and their parent); every prior 17 group and precedence group passes, with zero skip/cancel/todo. A preliminary fixture-only missing socket-close event caused an interrupted run and was corrected inside the turn; it is not Red evidence. Primary inspected the corrected fixture/matrix and terminally closed the lease compliant, receipt `dac4788c77b760d74679dd0598aac216db779fa85fc0c20041940a4bcbb747c2`, only two permitted changes and no Git drift.

Accepted new immutable test hashes: test `3F7A34969FE01F589EBB9C5C1737EA82CC15B5718A29A0B8A9F589618FEE7FBA`, helper `B0BA51E19D52863017B23FDAAE1E3471113E28E17258B044C340C3CC3E633034`. Protect both during M303-A-GREEN-02/lease `m303-a-green-02`, code owner qwen-a-code, attempt 2, parent `m303-a-green-01`, unchanged three-source path scope. The correction preserves known non-200 status across transport teardown with explicit interruption precedence and uncertain cleanup. Run all 34 tests and strict; primary independently checks strict and the same fresh reviewer rechecks complete A plus original native reproductions. No test allowance remains; one conditional Green correction remains, within the already active sole review-correction loop.

**A1 corrected Green candidate:** M303-A-GREEN-02 changes only the HTTP owner, retaining the observed non-200 category across response/socket/request failure and premature close, without replacing explicit interruption reasons. All 34 tests pass unchanged and strict exits 0. Primary terminal closure is compliant, receipt `8a8919af44779c53a3c930cec1047c412bcf5b1a5dacedd389923130d14f4e89`; no other path/Git drift. Primary inspected the exact changed branches, rechecked protected test/helper hashes and independently passed strict. HTTP SHA-256 is `62753EE08E06F3C8E02AB767BD29AB3DF8B8530347CB72634066A78B5600242E`; model/fit remain at the accepted Green01 identities. Cohesion RETAINED, no new dependency or owner. M303-A-REVIEW-02 now rechecks complete corrected A and reproduces original native A1 cases under the prior bounded fixture/cleanup packet. This post-close receipt maintenance is expected plan-only drift; implementation evidence remains pinned separately. A remains unaccepted until that verdict.

### A — Qwen request admission and wire boundary

**Outcome/risk:** A fully admitted fixed profile and selected request produce an immutable prepared payload with proven complete fit, or a bounded pre-call failure. S3: Local egress, request/configuration identity and parser integrity. Preflight inspects the absent Qwen exports and the existing M302 fit and embedding transport patterns.

**Proposed placement:** `src/server/generation/ollama-generation-model.ts` owns fixed metadata/effective-profile admission; `ollama-generation-fit.ts` owns concrete complete accounting and payload correspondence; `ollama-generation-http.ts` owns the fixed native HTTP lifecycle and wire-response reader. Keep serialization next to its real consumer; G may combine tightly related details if separation is wrapper-only. Freeze exact paths/symbols before preflight. Existing `src/server/retrieval/ollama-http.ts` and embedding fit/model logic are reference-only unless a demonstrated shared transport primitive merits an explicitly named behavior-preserving extraction in G. Do not make embedding error types or finite tokenizer tables generic.

**Test ownership:** `tests/ollama-generation-contract.test.ts` and, if needed, `tests/helpers/m303-ollama-fixture.ts`; synthetic metadata/HTTP replies remain test-only. Test positive/negative exact shape and identity, foreign/cloud/redirect destination rejection, missing model/runtime, stale tag/digest/template/configuration, complete count boundaries and 4096 reserve, unchanged prepared bytes, inherited controls and unavailable accounting. Test response size/encoding/JSON/completeness/error interpretation with an isolated local test server or injected transport; no Ollama/model call.

**Direction/acceptance:** Pure profile/accounting functions depend on existing application contracts; server-only HTTP owns external I/O and returns normalized data. No browser import reaches HTTP, filesystem or setup. Separate accepted Red/Green, unchanged tests, focused checks, independent strict check, actual diff/cohesion and fresh critical PASS precede B. A unit count example is not a substitute for G's sound accounting proof.

### M303-A-ACCEPT-01 — accepted request and wire boundary

Complete corrected review M303-A-REVIEW-02 passes with no Blocker/Major/Minor across I1-I8. Its 13 independent native scenarios include all three original A1 truncation failures, now correct with uncertain cleanup; success/chunking/terminal order, oversized status preservation, malformed JSON/UTF-8, reset, shutdown, pre-start cancellation and refusal also pass. Probe exit 0; every owned terminal event was awaited, servers closed and zero owned sockets remained. No model/browser/provider endpoint was contacted. Fresh 34/34 tests and independent strict pass at five-file fingerprint `09006e13a7010f5cc280bdbc8f2df24f6c99308fb429f2519b7fffaa98fd6b42`; prior HTTP probe identity is superseded. Compliant receipts, unchanged accepted tests during Green and actual cohesive diff are accepted. Primary accepts A only. Its sole review-correction loop and all test corrections are consumed; no unresolved A defect remains.

### M303-B-PREFLIGHT-01 — adapter integration assignment boundary

Workflow `M3-03-20260910-implementation`, slice `M3-03-B`, standard profile, TDD Applicable, S3. A is accepted. Fresh test_worker owner qwen-b-test performs one read-only preflight before any lease. B creates only `src/server/generation/ollama-generation.ts` with `createOllamaGenerationAdapter(requestImplementation?:OllamaNativeRequest):GenerationAdapter`, composing accepted A under G L4/L5. Existing `LocalService.generateFinding(input,adapter)`, stage and repository remain unchanged. Test owners are `tests/ollama-generation.test.ts`, `tests/ollama-generation-service.test.ts` and the existing M303 fixture helper; A's test and every existing shared test remain protected.

Preflight confirms exact absent callable, current service/live-owner seams and reusable runner identity without fixture creation or provider calls. L6's three-test B command and later affected/full-suite, strict/build own verification. B fixtures use only owned ephemeral loopback ports and exclusive ordinary `temp/m303-generation-*` leaves, preserve pre-existing state and close owned services/sockets before bounded own-leaf deletion. No actual model/runtime, browser, target, retained evaluation run or app data store operation is authorized. Each future write receives its own exact full packet and lease; no new task or capacity activation is selected.

**Accepted B preflight and Red assignment:** M303-B-PREFLIGHT-01 returned MISSING: the exact adapter factory/module and two B test files are absent, while A exports, shared deadline/capability, live supported retrieval ownership and service/repository publication seams exist unchanged. Current Node/version/hash, HEAD, empty staging and all five A identities match accepted evidence. Primary accepts the first-module exception using the current runner evidence; complete B behavior tests may initially fail on the exact missing adapter import, with those assertions explicitly unexecuted.

Assignment M303-B-RED-01/lease `m303-b-red-01`, owner qwen-b-test, phase Red attempt 1, parent None. Writes only both B test files and the existing M303 fixture helper, preserving every A assertion and existing helper behavior. Full L4/L5/B coverage includes preparation order/no-I/O failures, actual adapter through shared stage/live service, truthful one-attempt provenance, durable preservation, interruption/late callbacks, stale owner and failed publication. Test-only live ownership uses the existing permitted retrieval executor seam, never a historical capability. The exact three-suite B command remains L6; future executable filesystem/port effects and containment/cleanup remain the preceding B preflight boundary. Tests cannot contact the actual runtime or consume C.

**B initial Red inspection and bounded test correction:** B-RED-01 created two files (10 adapter and 6 service groups), left A/helper unchanged, and returned precise missing-adapter import failures: A 34 pass, two B file failures, all B assertions unexecuted. Primary closed `m303-b-red-01` compliant, receipt `ed820f1989002e7ea3f2359b1296bae1462babe7ebd63894ef4dcf32a89dbb10`, with only permitted creations/no Git drift, then inspected both complete files. The candidate is not yet accepted: service fixtures omit ordinary ancestor/root checks before creation/deletion; active-call waits need finite deadlines; one setImmediate is not proof asynchronous corpus preparation reached the tested phase; a Promise.race watchdog is not cleared; stale-owner/late-native labels exceed actual assertions; selected-native/scan preservation needs explicit assertions; a drift fixture lacks its final metadata response. These are contained test-quality/coverage corrections within G L4-L6/B, not new application behavior.

M303-B-RED-02/lease `m303-b-red-02`, same test owner/scope, attempt 2, parent `m303-b-red-01`, corrects those exact gaps before any source lease. Preserve existing assertions and A helper behavior; add only needed explicit stale/late events and complete preservation checks. Initial candidate hashes: adapter test `1AD04E9F3530E9803F0AE4887BCA2A9AB79828D82FE3EA0FA37F4380CEF5F2F8`, service test `C450E72A2989C674B5BB2C58E3D134FA49337908267CEC44A6C648D2124A371B`. No B fixture body has executed or created a leaf. One conditional test correction remains after this turn; Green/review allowances remain unused.

**Accepted B Red02 and Green assignment:** Primary inspected all three changed files and the complete corrected containment/timing/ownership boundary. Fresh terminal close is compliant, receipt `ae74ac661f1faacf55c3ddfd9abb4fa3070373dcdbd1a36aced2a777c2400640`, with no forbidden/unleased or Git drift. Accepted hashes: adapter test `3E26AD86AAD14EAAF51B0222E5DF15FB5641AD57FF0EBB356D317CC216ABB776`, service test `EC783FE8B96C20344427C24A010F530FC969FD49489B988908C1BBD70B0F1BAE`, helper `2E3936665387C09E5EEC8E85E3ABE7E1DE33D6314AE043ADA6E4F2DDAA5AA9F2`; A test unchanged. The decisive exact three-suite Red has A 34 passes and only the two missing-adapter module failures; B behavioral assertions remain unexecuted, not passing evidence. Helper adds only a read-only destruction observation; its changed identity supersedes earlier helper-based evidence, and the fresh A run passes.

M303-B-GREEN-01, lease `m303-b-green-01`, code_worker owner `qwen-b-code`, attempt 1, parent None, creates only `src/server/generation/ollama-generation.ts`. It composes pure fit, sequential bounded metadata, whole-metadata validation and an inert captured-body dispatch closure under L4/L5. Dependency direction is adapter to the three accepted A owners and shared types; no service, stage, A production or accepted test changes. This one cohesive composition owner needs no further layer or structural refactor. Run the exact L6 three-suite command then strict, inspect responsibility fit, and stop for primary terminal closure/independent strict/fresh S3 review. All original B budgets, containment and cleanup rules remain; no actual provider or C activation. A subsequent B test correction requires concrete first-execution evidence and its remaining conditional allowance, never a Green test edit.

### M303-B-GREEN-01 — integrated adapter candidate

Primary inspected the complete 34-line adapter: composition remains adapter to accepted fit/model/HTTP owners and shared types, with service/stage/repository unchanged. Source SHA-256 `B53A10B76C3A3B6149FE95F7DDBE00EC0FE7BCF36821BAE9033BD2B57932882E`. Fresh lease close is compliant, receipt `1e69c5f69cbc7207fe9abfc5f88cdf4eb0def7543d3a430ff546f73009c8d64e`, only the declared adapter created, with no Git drift. Accepted B test/helper identities remain exact. The full focused caller passed 50 tests (34 A and 16 B), and worker strict passed; primary independently reran strict successfully. No M303 fixture leaf remains and diff checks pass. A redundant missing-module Red replay by the Green owner produced no new evidence and is not required for acceptance.

Fresh M303-B-REVIEW-01 uses a new critical_reviewer for S3 transport-entry authority, cancellation and durable integrity, covering the complete B boundary and cumulative I1-I8. Independent risk reproduction may use only bounded newly owned ephemeral loopback native HTTP fixtures and read-only/source-derived probes, never port 11434/model/browser or actual generation. Any filesystem probe must use the accepted exclusive M303 fixture containment/cleanup boundary; no repository writes or guard/Git mutation. Reuse current focused/strict evidence only with full matching identity; native risk reproduction remains independent. B acceptance and C remain pending that review; primary may prepare documentation/full regression between leases without changing the reviewed implementation.

### M303-B-ACCEPT-01 — accepted internal Local integration

Fresh critical review M303-B-REVIEW-01 returned PASS with no Blocker, Major or Minor across I1-I8. Primary accepts B after actual source/test inspection, unchanged accepted-test identities, compliant terminal lease, 50 focused passes and independent strict. Independent native proof passed 13 owned ephemeral HTTP scenarios plus four zero-call cases: immutable capture, metadata ordering, success, missing/malformed metadata, status/redirect/output failures, every metadata-phase abort, chat abort, late completion and synchronous-start provenance. All owned requests/responses/sockets and servers closed; zero remaining connections. No actual provider, service, browser or filesystem operation was used by these probes. The reviewer verified the eight-file fingerprint and B receipt lineage and reused current service/strict evidence; expected primary documentation maintenance is not implementation drift.

The accepted composition module retains all existing stage, live-owner and durable-publication responsibilities. B used no Green correction or review correction loop. README now documents the fixed setup, factory, complete suite and capacity limitation. This accepted A/B checkpoint satisfies only the M3-03 portion of M3-05's owner-approved start condition; Complete M3-04 and explicit task selection remain required.

### M303-C-BLOCK-01 — exact remaining capacity prerequisites

C is not activated. All actual M2-04 observations abstained, and no authentic supported live retrieval within current embedding admission is evidenced. The required single-reviewer interface is not implemented. [M3-03 Verification](../../DEVELOPMENT_ROADMAP.md#m3-03--integrate-qwen-local-generation-and-run-its-capacity-screen) requires one manual representative smoke with the browser, screened EmbeddingGemma path, application, Qwen generation and single-reviewer UI active; [REQ-QUAL-006](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md) retains representative end-to-end capacity. The exact instruction in this plan's L5 is: "If a required UI or eligible path is unavailable, obtain the controlling owner decision before substituting/narrowing the proof or changing task dependencies." No substitute or narrower proof is proposed.

The owner already accepted the two start-dependency amendments. They preserve full completion proof and need no repeat approval. The smallest next scheduling action is a separate selection of dependency-ready M3-04, followed by the accepted M3-05/M4 route when each start condition passes; this task selects none of them. A supported real retrieval must still be established lawfully before C, without changing the frozen observations, using the six-case exception, fabricating a result or silently changing retrieval policy. If that remains unavailable after the interface exists, the missing material evidence returns to the owner under the existing authority. Keep the single C activation unused, M3-03 Blocked, the ExecPlan active and M6-02 Local evaluation gated. No capacity failure, fallback or replacement decision is inferred from an unrun smoke.

### M303-FINAL-REVIEW-01 — integrated review assignment

A different fresh critical_reviewer reviews the entire implemented A/B candidate, seven-document handoff and precise C stop against S3 Local egress, complete fit/identity, attempts/cancellation and durable integrity, using L1-L6/I1-I8. It is read-only and preserves all existing budgets. Current 550-test/strict/build and A/B native receipts may be reused under full matching command/tree/environment/guard identity; independently reproduce a bounded integration-critical native boundary as needed, never the actual runtime or C. Only newly owned ephemeral loopback/native resources with finite watchdogs and verified terminal cleanup are allowed. No model/browser/service/filesystem probe, repository write or Git/guard mutation is needed. Report complete verdict, findings, invariant coverage, cohesion, reproduced/reused/unavailable evidence and documentation impact. Final acceptance here can cover implemented scope and blocker handoff only; it cannot mark the roadmap task Complete.

### B — fixed adapter and internal service integration

**Outcome/risk:** The actual `GenerationAdapter` uses A and the existing shared stage/service to preserve zero-call prerequisite failures, one-call results and durable outcome truth. S3: transport-entry authority, cancellation and persistence.

**Proposed placement:** `src/server/generation/ollama-generation.ts` owns the fixed adapter factory, immutable configuration, prepare and dispatch orchestration. Reuse `executeGeneration` and `LocalService.generateFinding(input, adapter)`; keep service/main changes to demonstrably necessary nonvisual composition, with each exact path frozen in G. Do not add an HTTP route or call providers while constructing the service. Existing generation operation/aggregate validators remain authoritative; any new shared-contract change returns to primary reconciliation, not worker invention.

**Test ownership:** `tests/ollama-generation.test.ts`, `tests/ollama-generation-service.test.ts` and the M303 helper. A's accepted test file and existing M302 tests/helpers are protected during Green. G names any narrowly required existing regression edits before preflight; no evidence/developer-document edits enter worker leases.

Exercise the real adapter against controlled wire fixtures through actual stage/service/repository seams: no calls on startup, scan, non-Local mode, abstention or non-fit; attempt-time metadata before generation; single HTTP start; sync/async transport failure, incomplete/invalid output, successful proposal validation, safe snapshots, shutdown/deadline/late response, duplicate dispatch, stale owner and failed publication. Assert native evidence/siblings/provider context unchanged and separate unpersisted invocation from durable state. Test arbitrary response diagnostics/thinking/tool data never enter records. These are synthetic integration tests, not real Qwen or retrieval evidence.

Advance after A+B and affected generation/service regression checks, strict/build, terminal compliant leases, RETAINED/REFACTORED cohesion and fresh critical PASS. Freeze the real observation packet before C; no live-provider exploratory generation loop.

### C — one manual real Local-stack capacity smoke

**TDD: Not applicable.** Primary/developer owns the manual observation, not an implementation worker. Replacement evidence is the exact configuration note, one actual workflow outcome, validated durable readback, resource/UI observations and independent integrated review.

Before the single activation, all of the following must be resolved:

1. P passes; exact developer-owned runtime/model is present; G's full accounting and binding proof passes; A/B are accepted. Record tag `qwen3.5:4b`, full digest, quantization, effective context, runtime version, material parameters, license, offload configuration or supported automatic/default state, and prefilter result. Unknown decisive configuration blocks the call.
2. Freeze one representative lawful target/input and its real scan/retrieval path, within current query admission, with complete native evidence and actual supported guidance. Preserve exact existing evaluation inputs. No retrieval doubles or controlled-six-case packages may satisfy this gate. If the real path abstains, retain it honestly and do not invoke Qwen or search repeatedly for a passing case.
3. Resolve the roadmap/REQ-QUAL-006 single-reviewer-interface boundary against actual available UI. The existing app may remain visibly active for responsiveness observation, but an internal API call or proposed review screen alone cannot prove an absent required interaction. If authority requires later UI, record the precise owner/dependency decision and keep C/task closure pending; do not implement M3-05/M4 or weaken Verification implicitly.
4. Freeze exact target/network permissions, exclusive task-owned run/test roots and ports, developer/runtime process ownership, evidence destination, finite deadlines, required actions and cleanup effects. Never reuse or mutate retained M2-04 runs. Existing developer-owned Ollama may remain running; only owned temporary app/browser resources may be stopped.
5. Arrange readily observable duration, RAM/VRAM/storage pressure and obvious paging/offload, with browser, real screened EmbeddingGemma retrieval, app and Qwen active as required. Use existing OS/runtime observations and ordinary UI actions, not a monitor, benchmark series or custom supervisor. No claim of simultaneous model residency unless actually observed.

Run the one admitted workflow through the actual Local adapter and service, recording one explicit generation attempt and outcome. Keep native scan, actual retrieval, request/configuration identity, normalized result and durable proposal/failure categories traceable without retaining raw prompts/responses in tracked files. A validated proposal still needs later human semantic review; do not fabricate approval. Invalid output is an honest adapter/validation result, not a successful proposal or permission to tune/retry. Record capacity observations separately from output correctness and require every task Verification obligation for closure.

OOM, storage exhaustion or unusable interface fails the capacity gate. A failed fixed model/runtime evaluation pauses Local evaluation for the recorded replacement decision; no smaller model, repeated screen or Groq fallback is preapproved. An infrastructure interruption or missing prerequisite is not silently relabeled as model failure or success; triage exact evidence and remaining authority before any later action. The one C activation is not renewed by a correction, new run ID or operator restart. No extra actual generation is authorized to obtain a cleaner result. This capacity observation is separate from M6-02's exactly six frozen quality executions.

## Agent workflow and budgets

Follow the [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [write-lease guard](../../../.codex/write-lease-guard.md) and configured role pins. Separate persistent test_worker/code_worker contexts own each behavioral slice. One read-only preflight precedes any lease; record EXISTING_AND_COVERED, EXISTING_BUT_UNCOVERED, MISSING, REGRESSION, PARTIAL or UNKNOWN/CONFLICTING and take its prescribed route. No fabricated Red. The first-module exception requires exact absent callable, verified environment and complete unchanged behavioral assertions at Green.

Every worker write turn receives a fresh Milestone Assignment Packet v2, exact paths/commands/digest and primary-opened, terminally closed lease. Only one lease per worktree; primary documentation/guard work between leases. Tell workers they are not alone and must preserve peer/user changes. Workers use Git read-only. Primary owns decisions, authoritative writes, integration and acceptance; ordinary implementation is delegated.

Per unchanged role/phase chain: initial write plus correction 2 and conditional correction 3 under the existing new-evidence rule, one slice-review correction loop, no fourth attempt or new-ID budget escape. Stop identical decisive failure twice without new evidence, two no-diff outcomes, changed binding fields, violations or exhausted allowance. Primary triages and continues unaffected authorized work; only a real owner-controlled decision, unavailable permission/input, expanded scope or exhausted budget requires owner direction. The narrow direct-primary test correction follows ADR-0024 and invalidates affected evidence.

Each A/B slice receives fresh critical_reviewer review for its named S3 risks; final integrated review uses a different fresh critical_reviewer. Inspect actual diffs, evidence, terminal receipts, tests/helpers and cohesion; administrative compliance is not proof. Optional agents require an independent useful task; role capacity is not a staffing target. No extra routine planning, research or review layer is added.

## Decision Review Contract

**R3 target:** G's remaining Local identity, complete accounting, transport lifetime and representative-proof semantics in this plan. Triggered properties are custom integrity/identity, privacy, cancellation/recovery and consequential proof interpretation. Fixed model/runtime and M301/M302 semantics are constraints, not candidates.

One bounded non-ranking discovery pass may identify existing metadata, exact source references and smallest implementation alternatives. Freeze alternatives, common criteria, hard gates, evidence classes and authority boundary before comparison. Prefer native transport and a small verifiable counting mechanism; compare only materially viable mechanisms for this exact contract, not models/frameworks. Criteria: correctness before dispatch, exact effective identity, loopback-only flow, evidence honesty, accepted workflow fit, minimal maintenance and existing-code reuse.

Use at most two critical_researcher reports: (1) exact Qwen/runtime metadata, complete counting/configuration and prefilter uncertainties; (2) transport/result/lifetime and real eligible capacity-proof scope. Each receives Research Assignment Capsule v1, exact authority/source evidence, a 1000-word useful-detail limit plus necessary proof tables, one targeted follow-up and read-only permission. No actual generation or artifact acquisition by researchers. Parallel independent research is permitted while primary gathers nonduplicated repository facts. Additional required external artifacts/setup follow P's explicit effects before evidence can be consumed; a missing decisive artifact is not hypothetical proof.

The explicit owner re-entry in M303-SETUP-01 added exactly one actual-artifact report to the original research ceiling; that report is complete and its allowance is consumed. Mandatory decision_analyst returns DRAFT READY, RETURN FOR RESEARCH or OWNER DIRECTION after all critical dimensions are covered. Allow one synthesis and one supported correction. Fresh critical_research_reviewer pre-draft review permits one supported outline correction; primary alone authors accepted G after PASS. A different fresh critical_research_reviewer reviews the complete authored contract and cumulative invariant packet before worker preflight. No drafter is needed. At most two final-artifact correction cycles; R3 corrections rerun the complete packet/review. Repeated decisive gaps twice, conflicting authority or exhausted allowance stops dependent work without budget renewal.

| ID | Trigger / expected result | Evidence owner / reviewer |
| --- | --- | --- |
| I1 | All selected authorities and L1–L6 covered; no fake completion, scope/UI/exception expansion | Primary authority map; analyst and both R3 reviewers |
| I2 | Frozen evaluation/runtime inputs distinguished; original actual abstentions and prior capacity limits preserved | G/current hashes; both R3 reviewers |
| I3 | Exact local artifact/effective configuration, complete fit plus 4096 reserve; unknown/drift/overflow prevents transport | G proof, A/B vectors; critical reviewers |
| I4 | Only fixed loopback metadata and one generation request; no payload redirects, cloud inference, setup or startup probes | A/B transport tests, C actual endpoint evidence |
| I5 | Honest attempt entry, bounded output/error/cleanup, no raw secret/reasoning retention or automatic second attempt | B and inherited M302 regressions |
| I6 | Live actual supported Finding and immutable durable native/sibling/context data; no restart capability reconstruction | B service tests and C actual readback |
| I7 | Metadata exclusion precedes acquisition; one real representative smoke meets every capacity/UI obligation or remains unpassed | P/C observation and different final critical reviewer |
| I8 | Small cohesive modules, current consumers, no generic harness/supervisor and safe owned-resource cleanup | Primary diff/cohesion, A/B/final review |

## Concrete Steps

### M303-ENTRY-02 — execution entry and read-only command binding

The 2026-09-10 owner request authorizes this exact task through its prescribed gates, delegated implementation, bounded corrections, verification and documentation closure. It explicitly preserves permission/license/setup boundaries and forbids commits, publishing, pushes, later tasks and additional generation attempts. Entry HEAD is `c0110a8d71f91d03a82d413e983f9baeacb6579f`; `git status --porcelain=v1` is empty and `Test-Path logs/agent-flow-leases/v2/active.json` is False. Historical planning HEAD and receipts remain evidence only. No worker lease is open.

The next P inventory is read-only: `Get-CimInstance Win32_OperatingSystem` selects total/free physical memory; `Get-CimInstance Win32_Processor` selects CPU name; `Get-PSDrive C` selects used/free storage; `Get-Command ollama,nvidia-smi -ErrorAction SilentlyContinue` selects executable paths; `Get-Process` selects only Ollama process ID/path/working set; `nvidia-smi --query-gpu=name,driver_version,memory.total,memory.used,memory.free --format=csv,noheader,nounits` reads GPU state. Inspect only known Ollama model-store manifests and configuration variable names relevant to host/model/context ownership; do not expose unrelated environment or process arguments. These commands install, acquire, start, stop and generate nothing. Native calls require explicit nonzero-exit handling. Developer-owned processes and stores remain unchanged.

For the bounded G comparison, the fixed alternatives are exact artifact-linked tokenization of the complete serialized request, or a demonstrably sound artifact-linked upper bound over the same admitted text-only domain. Both must include template/special/schema overhead and the fixed 4096-token reserve. Native Node HTTP is the transport candidate; existing embedding transport is reference-only. No candidate is accepted in advance. The two prescribed critical researchers own independent accounting/configuration and transport/capacity-scope dimensions; the primary owns current machine inventory and freeze checks and will share their results without duplicating investigation. Existing L1–L6, I1–I8, criteria, barriers and budgets remain unchanged.

**Observed entry evidence.** The Windows CIM queries initially returned access denied in the sandbox. The same read-only query succeeded after automatic approval; this was no installation or runtime permission. CPU is Intel Core Ultra 7 255HX; total visible RAM is 32,961,912 KiB (31.435 GiB), free RAM 11,633,440 KiB (11.095 GiB), and C: free storage 518,186,938,368 bytes (482.599 GiB). NVIDIA reports RTX 5060 Laptop GPU, driver 577.05, total 8151 MiB, used 14 MiB and free 7797 MiB. These are raw reported fields, not an inferred allocation identity; their difference is not assigned to application memory. No Ollama process or PATH command was found, and the five selected Ollama configuration environment names were absent. The retained v0.33.3-directory executable exists (36,912,520 bytes; SHA-256 `E4FE6BD835FE146659F5C969DCCAFF2E25A9DE63D90EE204CA5D11B9034B0CA5`), but its PE ProductVersion is absent and no live version was queried. The retained developer model store contains only the 741-byte EmbeddingGemma manifest; Qwen is absent. Store/executable observation is read-only and does not authorize launch, acquisition or license acceptance. P is not yet passed from these observations.

**Fresh provider-free checks.** From this repository, dot-source the first README PowerShell definitions block unchanged, invoke the strict/three-test sequence below, then dot-source only M301-COMMAND-02's verification block. The actual 2026-09-10 caller executed those maintained blocks successfully: Node v24.20.0, strict PASS, 99/99 focused tests, and staticPacket PASS (3 cases, 6 bindings, 9 files, 96 negative checks, actual states all `abstained`, runtimeGeneration NOT RUN). Manifest SHA-256 is original `63770583A97E1D0517337474DADC706C1047D78E026E22C7A745CE7674ED9E6B`; the verifier checked all source/local identities and retained run/seed correspondence. Relevant tracked source/tests remain identical to entry HEAD. Environment identity includes package.json `01C0B39BD9141588543BD520DBB7A160EAC2D95414EEF54D36F394FE0FB7BA5C`, package-lock.json `38CE94019310A5EFDBA9BB5565ABE6EB13F678FA18FCD9D87B1C5DC21B30748D`, tsconfig.json `3957F80AF41B23DC4CCEFAA6B24823C367E6984980420B596275B8692DF5ABDE`, and Node executable `5C976096E04E5C2C1F091938926234CC9FBEBFE9787DDD149351B3B0ECC707B5`. No full-suite/build, runtime liveness, actual accounting or capacity result is claimed by this entry check.

**Bounded P metadata access.** The accounting follow-up identified the official registry manifest and a 475-byte configuration blob. The permitted next command uses .NET HttpClient for GET `https://registry.ollama.ai/v2/library/qwen3.5/manifests/4b` (8192-byte response ceiling) and GET `https://registry.ollama.ai/v2/library/qwen3.5/blobs/sha256:de9fed2251b37295b763727a59ca35cf5cfe5c7379bc3e2104b2ce3c145aa887` (475-byte ceiling). Use a 20-second timeout, no proxy, no automatic redirects, ResponseHeadersRead and a bounded in-memory body read; print only status, selected identity/location headers, body and SHA-256. Any redirect requires explicit reconciliation before following. Dispose response, stream, client and handler. This reads published metadata only, writes no artifact, accepts no license, launches no runtime and fetches no model layer. The researcher's sandbox socket denial is not retried unchanged: primary requests the ordinary network sandbox override for these exact metadata reads within existing task authority.

The approved read returned the 709-byte manifest with SHA-256 `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd` and a 307 for the configuration blob. The registry-selected destination is HTTPS host `dd20bb891979d25aebc8bec07b2b3bbc.r2.cloudflarestorage.com`, exact path `/ollama/docker/registry/v2/blobs/sha256/de/de9fed2251b37295b763727a59ca35cf5cfe5c7379bc3e2104b2ce3c145aa887/data`. A follow-on metadata command may reread only that configuration redirect and follow it once after exact HTTPS/host/path/port/no-userinfo validation, with the same finite read and 475-byte ceiling, then require the configuration SHA-256 from the manifest. Do not retain the expiring signed query in documentation or print it again. No other redirect is followed. This is the registry's explicit storage delivery for the named tiny metadata object, not model acquisition.

Working directory: `C:/projects/a11y-evidence-lab`. Load only the definitions from [README command preparation](../../../README.md#development-command-preparation) in each PowerShell caller, not adjacent restore/install or historical evaluation procedures.

```powershell
git status --short
if ($LASTEXITCODE -ne 0) { throw 'Git inspection failed' }
$m303EntryHead = git rev-parse HEAD
if ($LASTEXITCODE -ne 0) { throw 'HEAD lookup failed' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'Whitespace check failed' }
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Pinned Node required' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test tests/run-contract.test.ts tests/generation-contract.test.ts tests/generation-stage.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Focused generation contracts failed' }
}
```

Current expected result: strict PASS and 99 tests. This is a provider/browser-free planning check; do not infer full-suite reuse from a test count. Before dependent work verify the original manifest and each referenced input plus applicable retained actual-retrieval identities from their evidence owners.

The following slots are deliberately unresolved future callers. G freezes exact commands, environment/native exit propagation, input/output ownership, mutations and recovery before the corresponding action:

| Slot | Required binding |
| --- | --- |
| P | Current hardware/storage/runtime read-only inventory; primary metadata sources and explicit safety-margin calculation; no pull or inference |
| Developer setup | Exact runtime reuse/install/pull action and external ownership/permission/license/effects; no application invocation, repo/model-store deletion or historical grant reuse |
| Metadata/accounting | Exact selected-artifact observation and offline proof commands; expected transient outputs and retention; no hidden generation or acquisition |
| A/B | Final export/file names and focused Node commands, explicit fixture/loopback-server roots and teardown; current M302 regressions plus independently executable strict/build |
| C | One exact manual driver/call sequence with actual eligible data, real adapter/service/browser/runtime identities, safe records, measurements, owned processes/ports and cleanup; no arbitrary payload passed through UI or CLI |
| Closure | README's nineteen-suite sequence including all M303 suites, strict and client build; browser prerequisites/scratch effects checked first |
| Guard/recovery | Exact packet/lease lineage, role/path contract and guard Python prerequisite; terminal close, failure evidence, authorized disposable children and retained external artifacts |

The A/B modules and tests listed above now exist and passed their recorded commands; C is now recorded as a consumed successful observation in M303-C-OBSERVATION-01; its historical caller is not replay authorization. Do not put a historical HEAD or requirement for an uncommitted plan into a gate. Current source/test/package/input/environment and mutable-state identities control evidence reuse after intentional commits.

### M303-REGRESSION-01 — complete implemented-scope verification

The maintained README inventory now includes all 19 suites. Primary executed its complete sequential browser-free, scanner/integration and UI suite commands through the unchanged development wrapper, with native exit checks before every next step; console output was reduced to each suite's terminal summary. All **550 tests passed**, with zero failed, skipped or cancelled: 68 run-contract, 75 run-repository, 69 local-service, 20 scan-normalization, 12 retrieval-contract, 28 embedding-retrieval, 19 retrieval-service, 21 finding-sufficiency, 5 finding-guidance-api, 8 generation-contract, 23 generation-stage, 10 generation-service, 34 ollama-generation-contract, 10 ollama-generation, 6 ollama-generation-service, 88 scan-page, 15 walking-skeleton, 30 target-results-ui and 9 finding-guidance-ui. All three retained scanner/UI/integration scratch directories were ordinary and empty before and after; M303 fixtures leave no owned leaf. Controlled browser execution does not prove the absent reviewer interface or actual capacity.

Independent strict TypeScript and Vite client build passed before the suite. The existing ignored/untracked `dist/client` tree was verified ordinary, contained and exactly the known index/CSS/JS/assets inventory before its bounded Vite replacement. Build output retains those same three artifact names. No dependency/browser acquisition or developer runtime/model change occurred. B review's independent native ephemeral HTTP probes are disjoint from the sequential application/browser checks.

Relevant eight-file fingerprint `807cd2e12d8fd49d21cfeaf3797bea264ec83469a76ec9f57010c9f36ab53038` is SHA-256 over compact JSON ordered path/lowercase-SHA pairs: model, fit, HTTP, adapter, A contract test, B adapter test, B service test, M303 helper. Current Node remains v24.20.0 with its accepted binary identity; README definitions at lines 80-173, wrapper environment checks/restoration and package/lock/tsconfig/M301 identities remain unchanged. No existing tracked source/test/evaluation/corpus change or staged change exists. The new full suite supersedes narrower repeated checks for this implementation identity; mutable native/browser/filesystem outcomes remain bounded to their isolated run, not a general support guarantee. Final integrated review and C remain required.

### M303-FINAL-ACCEPT-01 — integrated implementation and blocked handoff accepted

The different fresh critical_reviewer returned PASS across L1-L6/I1-I8 for the complete implemented A/B boundary and precise C handoff, reviewing plan SHA-256 `26570d90d34ac703ae7be503152ac9201830497b6cac6d27c7cd50d7d63abddc`. No Blocker, Major or outstanding Minor remains. Two minor current-summary defects were corrected and verified during review: roadmap implementation status and repeated test counts in the navigation index. Primary also reconciled the plan's planning-era artifact, interface and command summaries. These are non-normative documentation dispositions, not a source change, renewed research allowance or capacity waiver.

Independent integrated reproduction passed success, missing model, truncated HTTP 429, oversized HTTP 200 and active-chat abort through the real adapter/shared stage. All 19 fixture requests followed the required order and truthful provenance; every observed owned request/response/socket closed, and all ephemeral servers closed with zero connections. No actual Ollama, model, browser, application service or filesystem mutation occurred. The reviewer verified the eight-file fingerprint, all eight sealed compliant lease contracts/receipts and correction lineage, no active lease, unchanged protected inputs/configuration/README wrapper/Node, empty staging and diff checks. Current 550-test/strict/build and A/B native evidence were reused with matching identities.

Primary accepts the implemented checkpoint and the documentation handoff. Seven existing documents now align the Local adapter, setup/configuration, full verification commands, task status, navigation and capacity limits. All 11 PowerShell fences parse, the 19-suite inventory matches the repository, README preparation lines 80-173 remain unchanged, 504 actual local links/anchors pass with fenced templates excluded, UTF-8/final-newline and `git diff --check` checks pass, and every roadmap Verification line remains unchanged. Final receipt reconciliation receives the same proportional consistency checks; it does not invalidate unchanged implementation evidence.

M3-03 remains **Blocked**, and this ExecPlan is not archived. Actual Qwen execution, loaded context/offload, authentic supported retrieval, required reviewer-interface responsiveness and C remain unverified. Observed metadata is conditional on the developer's runtime/model/configuration stability, and socket cleanup does not prove runner cancellation. M2-02 capacity and frozen M2/M3 evidence remain intact; all thirteen later tasks remain Not started. No actual generation, C activation, evaluation run, commit, publication or push occurred. The existing owner-approved scheduling path needs only separate task selection when the owner chooses to advance it; no repeated setup or scheduling approval is requested here.

### M303-RESUME-01 — capacity readiness after M3-04

On 2026-09-12 UTC, the owner requested continuation of M3-03. Primary inspected clean HEAD `7c4d4bf` and the current roadmap, L5/C contract and application boundaries. [M3-04 is Complete](m3-04-groq-adapter.md#m304-final-01--integrated-review-and-documentation-closure); the accepted M3-03 A/B checkpoint and Complete M3-04 satisfy M3-05's start dependencies. M3-05 and M4 remain Not started. Current FindingGuidance/FindingOutcome components expose guidance and eligibility, while the loopback API has no generation or review route. The implemented reviewer-interface prerequisite therefore remains absent, and no new authentic supported live retrieval is evidenced.

The remaining work cannot be performed inside M3-03's existing source scope: C explicitly says "do not implement M3-05/M4 or weaken Verification implicitly." The next actionable scheduling step is explicit selection of [M3-05](../../DEVELOPMENT_ROADMAP.md#m3-05--present-integrate-and-verify-structured-generation), followed by its accepted interface checkpoint and the separately selected M4 review tasks when eligible. This is not another setup or scheduling-amendment approval. It does not authorize those tasks, resolve authentic retrieval, or waive C. M3-03 remains Blocked; the single capacity activation stays unused. No application, model, provider, secret, retained-run or frozen-input operation occurred. Documentation impact is limited to this current resumption route; prior verification and history remain unchanged.

### M303-RESUME-02 — documentation reconciliation after M3-05

The 2026-09-13 documentation review reconciles this current-state summary with [completed M3-05](m3-05-generation-checkpoint.md#m305-final-06--integrated-review-and-task-closure). That task records authentic supported retrieval and one mechanically validated, saved and displayed original proposal per provider. Its Local observation used diagnostic instrumentation, a different run/Finding input and separate finite authorization; it is neither an identical-input repeatability result nor this task's capacity screen. The original M2-04 abstentions and M303-RESUME-01 remain historical evidence.

The [C contract](#c--one-manual-real-local-stack-capacity-smoke) still requires a representative lawful fresh workflow, the implemented single-reviewer interface, bound configuration/permissions, resource and responsiveness observations, and exact cleanup. Review controls are not implemented, and M3-03 remains Blocked. Preserve its unused single C activation and all existing requirements; no run is reopened, eligibility carried across service sessions, model invoked or later task selected by this documentation update.

### M303-RESUME-03 — capacity preparation after M4-03

The 2026-09-16 UTC resumption (2026-09-15 local) verifies clean HEAD `4526aad827e1834691f2e990ddcfb76e60f49695`, no active lease, Complete M3-05 and Complete M4-01 through M4-03. The current reviewer interface resolves the former UI blocker. The primary-owned manual C contract, one activation, exact model/context, live eligibility, resource observations, durable readback and fresh integrated review remain required; no application implementation or new research mechanism is selected.

The first fixed-loopback metadata GET returned connection refused at `127.0.0.1:11434`, and process inspection found no Ollama server. The installed executable SHA-256 and both Qwen/EmbeddingGemma manifest hashes still match M303-SETUP-02 and the M2-02 binding. Installed assets do not establish runtime liveness. M303-SETUP-01's single launch was consumed, so primary requested a new allowance to start only the same installed server with its recorded host/store/no-cloud/no-prune settings, without acquisition or model changes. That response remains pending; no server launch occurred.

Fresh resource inspection initially found 8,773,232 KiB free RAM, below P's unchanged 9-GiB allowance. After unused applications were closed, a fresh read found 15,627,096 KiB (14.903 GiB), passing that pre-activation allowance without changing its safety margin. The earlier same-resumption GPU sample reported RTX 5060 Laptop / driver 577.05 / 7,797 MiB free VRAM, and storage was 505,411,493,888 free bytes. Pagefile usage was 178 MiB with a 218-MiB peak; these sparse preflight values are not capacity observations. Recheck material resource changes before C; no model has been loaded by this resumption.

Primary loaded the maintained README preparation fence unchanged, checked Node v24.20.0, ran independent strict TypeScript and the exact current 32-file sequential suite selected from its maintained fence. All 712 tests passed, the caller exited 0 and the three assigned scratch roots were empty. No separate developer application service ran concurrently with the test-owned services. A fresh read-only critical reviewer verified that the current 160-file source/test/configuration fingerprint `97466d647596538fb4e79abb1d4d0f31b98e71eba183916cc41fd1a05bd0f1c0`, pinned Node executable and three client assets match M4-03's accepted evidence; the unchanged build is reusable. These checks do not activate C, replace current external observations or establish task completion.

A fresh independent critical reviewer returned PASS for this resumption/documentation handoff across I1-I8, with no findings, on plan SHA-256 `5e1b6ce7223de045fb00452a0fe64919ebeb73aedd0c592697960a9062bc1321`. It independently reproduced strict TypeScript, checked the original frozen manifest and all thirteen referenced artifacts, and verified source/build correspondence. Primary and reviewer checked the three-document diff, 125 local links/anchors and whitespace. This receipt is subsequent non-contract maintenance; the verdict accepts the handoff only, while C and task completion remain Blocked.

The next C preparation will use one fresh representative lawful public input and the existing actual UI/service/retrieval/Local adapter, with pre-generation native/sibling preservation and terminal aggregate comparison. It must bind exact input, finite actions, owned service/browser resources and safe evidence before activation; no retained run or synthetic guidance may supply eligibility. No public-target scan, actual model call, human review decision, runtime launch or C activation occurred here; the regression suite used its existing isolated synthetic fixtures. Documentation impact is current-state and progress reconciliation only; no source, tests, requirements, ADRs, frozen inputs or task status changed.

### M303-C-PROCEDURE-01 — Frozen single capacity workflow

The owner explicitly approved starting the existing Ollama server after M303-RESUME-03 and instructed continuation. Primary performed that one launch after verifying the retained executable, ordinary runtime/store ancestors, no existing Ollama process/listener or inherited Ollama settings, and 17,645,664 KiB free RAM. Hidden developer-owned server PID 40956 started at 2026-09-16T00:35:30.8681144Z with the unchanged M303-SETUP-01 host/store/no-cloud/no-prune settings; the caller restored its prior environment. Existing production metadata readers and validator now pass version 0.33.3 and the exact Qwen/EmbeddingGemma manifest identities. This consumes only the new server-launch allowance, not C; preserve this runtime afterward.

**Scope and input.** Primary owns this TDD-not-applicable manual observation. Bind exactly one new Local scan of `https://www.w3.org/WAI/demos/bad/before/home.html`, W3C's deliberately inaccessible public demonstration, under the existing authorized representative C workflow. The page was read on 2026-09-16 UTC and identifies that purpose; it is the same bounded public target used for the completed M3-05 integration, but none of that task's finite grants or retained runs is reused. Ordinary managed-browser requests for this page and its resources are admitted; no authentication, form submission to the target, crawling, target discovery or alternative page. Select the first native violation in scan order whose existing evidence assessment is complete; allow exactly one Get guidance action, including existing lazy 16-passage corpus and one query embedding. Abstention, execution failure or ambiguous ownership stops the workflow without replacement input or a second retrieval. Exactly one Generate action is permitted only after genuine supported retrieval in this same service. Ambiguous activation consumes the allowance. No human final review decision is authorized or fabricated; inspect the rendered review controls without submitting a decision.

**Fixed configuration.** Qwen remains `qwen3.5:4b`, full manifest `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`, Q4_K_M, Apache 2.0, Ollama 0.33.3, context 32768, reserve/output 4096, temperature 0, top_p 1 and all other material inherited/default parameters, tokenizer/rendering/parser and one-sequence controls in G L1-L4. Explicit local wire suffix, thinking/stream/truncate/shift false and keep_alive 5m remain unchanged. GPU placement is the supported automatic default (`num_gpu=-1`); actual loaded context/backend/VRAM must be observed separately. The M3-05 accepted runtime instruction version is `m302-instructions-v2`, with unchanged schema/output and fixed Local controls; M301 frozen evaluation bytes remain separate. EmbeddingGemma retains its M2-02 BF16 artifact, 2048 context, 768 dimensions, actual corpus/query path and 5m keep-alive. No warmup, empty request, model load/unload, inspector probe, acquisition or parameter change.

**Owned state and caller.** Before starting the application, exclusively create ordinary ignored/untracked `temp/m303-capacity-01`; fail if it already exists. Retain create-only minimized JSON phase records there: baseline identities/run inventory, resource observations and terminal validation/preservation. Enumerate and hash all existing canonical `data/runs/*/run.json` without printing content; every earlier run is protected. The production service may exclusively create one new run directory under its unchanged `data/runs` owner. Use the current README first preparation fence and its complete Run the local service caller unchanged in a primary-owned elevated PowerShell PTY, `A11Y_PORT=0`, current 40-hex HEAD revision, scanner scratch and exact environment restoration. Verify the existing three built assets and empty ordinary scanner/UI/integration scratch before launch. No other application service or concurrent browser test may run. Record the returned loopback URL/port and PTY, then open one task-owned Chrome tab at that exact URL. No `.env` read/hash or Groq action. Existing source/build/frozen identities and fresh strict/712-test evidence from M303-RESUME-03 are reusable only while unchanged.

**Observation and readback.** Use existing UI controls for Analyze, first eligible Finding selection, Get guidance and Generate, with current AX observations and screenshots. Record phase times from actual durable lifecycle fields and manual observation timestamps. Read-only inline Node commands may import existing validateRun/assessFindingEvidence and production pure input/request/fit builders to validate canonical results and record configuration/fit/body hashes without emitting or retaining raw prompts/responses. Before and after retrieval/generation, compare stable sorted-object-key JSON hashes after removing only the selected downstream fields and restoring its prior state; preserve whole-run context/native evidence and every sibling. Validate the final canonical aggregate and inspect all displayed original proposal fields, citations and review controls. A schema-valid proposal remains pending human semantic review; a failure remains a failure.

Take a small finite set of manual resource observations: before the workflow, after actual retrieval, during/just after the single Generate interval and after completion. Use ordinary CIM physical-memory/pagefile reads, Get-PSDrive storage, selected owned process working sets/modules, NVIDIA memory/process fields and bounded no-proxy/no-redirect GET `/api/ps` (10-second deadline, 1-MiB limit). Record actual timestamps, loaded names/digests/context/size/size_vram and selected CUDA/backend module paths; do not infer residency between observations. During generation, exercise an ordinary keyboard focus/scroll action and observe the UI's pending state; after generation inspect the usable reviewer controls. No polling loop, monitor, custom supervisor, timing probes, benchmark series or model request is added. If an observation misses the active interval, report that limit without another generation. The existing scan, retrieval and 120-second generation deadlines remain unchanged; manual observation is bounded to 15 minutes after Analyze, with no extension that renews actions.

**Cleanup and acceptance.** Close only the owned Chrome tab and send literal `stop` to the application PTY; require service-stopped, exit 0, exact environment restoration, owned port refusal, empty assigned scratch and unchanged source/build/frozen/prior-run identities. Preserve the new canonical run, minimized evidence and developer-owned Ollama. Ambiguous cleanup stops further admission and returns to exact identity-based triage, never blanket process termination or deletion. One C activation is recorded when Analyze is activated; no rerun follows failure, restart or new input. Capacity and output correctness are separate observations, every roadmap Verification remains required, and fresh integrated critical review plus documentation closure precede Complete/archive.

### M303-C-OBSERVATION-01 — successful full-local-stack capacity screen

On 2026-09-16 UTC, after explicit owner approval, primary started the retained Ollama v0.33.3 server in a hidden process with loopback host, existing model store, cloud disabled and pruning disabled. PID 40956 began at 00:35:30.8681144Z. The verified runtime identity is recorded below. Production metadata admission passed; free RAM before launch was 17,645,664 KiB, above P's unchanged 9-GiB allowance. No acquisition, warmup, tuning or fallback occurred.

**Exact configuration and accounting.** Runtime SHA-256 is `e4fe6bd835fe146659f5c969dccaff2e25a9de63d90ee204ca5d11b9034b0ca5`. Qwen `qwen3.5:4b` / wire `qwen3.5:4b:local`, Q4_K_M, Apache-2.0, manifest `2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`, ran with configured and observed context 32768, output reserve 4096, temperature 0, top-p 1, one response/sequence, thinking/streaming/truncation/context shifting disabled, keep-alive 5m and automatic GPU setting (`num_gpu=-1`). G's inherited defaults and artifact-linked accounting remain unchanged. EmbeddingGemma BF16 manifest `85462619ee721b466c5927d109d4cb765861907d5417b9109caebc4e614679f1`, layer `0800cbac9c2064dde519420e75e512a83cb360de3ad5df176185dc69652fc515`, used context 2048, 768 dimensions and keep-alive 5m. Runtime prompt/schema/output versions were `m302-instructions-v2` / `m302-schema-v1` / `m301-proposal-v1`. Pure reconstruction of the actual supported request yielded 18,224 wire bytes, conservative input bound 17,502 tokens plus 4096 reserve, and SHA-256 `2495ee1bf795dfe1e0dec6ce5c50f16d57739a8d6a93eb10ad92c48a1a0e50bd`.

**One workflow and durable result.** The unchanged README production caller served port 65131 (PTY 91350); one task-owned Chrome tab analyzed the fixed W3C demonstration target from M303-C-PROCEDURE-01. Analyze activated at 00:41:22.960Z. Native scan ran 00:41:23.117Z–00:41:24.620Z, producing 35 Findings and two manual-review observations, using managed Chromium 151.0.7922.34 / axe 4.13.0 and the unchanged scan contract. The first evidence-complete native Finding, scan-array index 0, was `5cf41880-048c-4e84-b388-b960231c19fd`, color-contrast ordinal 1. One Get guidance produced supported retrieval without missing roles or conflicts at 00:42:24.325Z–00:42:28.439Z (4,114 ms), including normal lazy corpus embedding. One Generate produced a validated proposal at 00:43:37.909Z–00:43:51.249Z (**13,340 ms**). There was no retry or human-review save.

Canonical run `data/runs/run-409e8fba-6ce3-4f45-92ca-a81f491a730c/run.json` is 45,376 bytes, SHA-256 `7b70484b7de263ba841f9d3b6c309910e933a123bb83e301832c9cfd13c90d5e`. Its selected state is `proposal-pending-review`; adapter `ollama-generation` / `m303-ollama-chat-v1` / endpoint `ollama-loopback-chat` records response and passed validation. All eleven proposal fields are present. Normalized proposal SHA-256 is `6a6cc28fe7bf92ff9c22a7654a76317daf14387661e25cd0e40d1031546ec250`. Model confidence and the literal string `false` in blocking manual judgment do not establish a human decision. The suggested font-size/weight remediation still needs semantic review; capacity is not groundedness qualification.

**Finite resource observations.** Four manual snapshots recorded the following; these are observations, not a monitor or latency benchmark. The RTX 5060 Laptop reported driver 577.05 and 8151 MiB total VRAM.

| Phase (UTC) | Free RAM (KiB) | GPU used/free (MiB) | Pagefile current/peak (MiB) |
| --- | ---: | ---: | ---: |
| Before workflow, 00:40:42.361–45.081 | 17,436,852 | 0 / 7810 | 141 / 218 |
| After retrieval, 00:42:50.273–50.545 | 15,049,324 | 810 / 7001 | 141 / 218 |
| During generation, 00:43:49.314–49.549 | 13,791,612 | 5693 / 2118 | 141 / 218 |
| After generation, 00:44:35.029–35.250 | 13,905,000 | 5693 / 2118 | 141 / 218 |

The entire in-flight sample lies within the durable generation interval. `/api/ps` simultaneously reported exact-digest Qwen at context 32768 with size/size_vram 4,197,596,527 bytes, and exact-digest EmbeddingGemma at context 2048 with size/size_vram 681,417,113 bytes. Both retained runner processes exposed the expected CUDA 12 and driver modules. These are reported residency observations, not a measured layer count or exhaustive allocation accounting. Free disk was 505,565,171,712 bytes before and 505,559,687,168 bytes afterward. No OOM, storage failure, paging growth or unusable UI was observed.

**Reviewer interface.** Primary exercised Tab and End immediately after Generate; the pending accessibility tree at 00:43:38.220Z lies inside generation and showed the pending state with appropriately disabled actions. After completion, the interface displayed all eleven fields, resolved citations and human-review controls. An unsaved reviewer note was typed at 00:44:40.409Z and cleared; no decision or judgment disposition was selected and Save decision was never activated. Primary inspected the returned screenshot: the review panel and focused note field were readable and usable without overflow. No local screenshot artifact was created; independent visual review is limited to these recorded primary observations. All 200% zoom tests remain Deferred until after MVP.

**Preservation and cleanup.** Normalized sorted-object-key JSON hashes (array order preserved) reproduce the entire native run at `3f02fc5ffef4083c16f6acc6412526ec6c7734e8bba8c22206776499d0142982` and pre-generation supported run at `cd6748e0f66876a17f83386541ac63a35a057c7f74a069b515de63b013fe58ad`. Native scan/context, retrieval and all 34 siblings remain unchanged. All 13 earlier canonical runs retain their original hashes; exactly one new run exists. At 00:46:02.1877177Z the task tab was closed, the service stopped normally with exit 0, port 65131 refused connections and all three maintained scratch roots were ordinary and empty. The developer-owned Ollama process was preserved as requested. There is no active lease.

**Evidence identity and scope.** HEAD remains `4526aad827e1834691f2e990ddcfb76e60f49695`. The 160-file source/test/configuration fingerprint `97466d647596538fb4e79abb1d4d0f31b98e71eba183916cc41fd1a05bd0f1c0`, exact three client assets, pinned Node executable, original M301 manifest and all thirteen referenced artifacts match M303-RESUME-03. Its fresh strict/712-test result is reusable; no implementation changed. Eleven exclusive, ignored records under `temp/m303-capacity-01/` retain baseline, activation, scan, before-generation, four resource samples, generation, UI and cleanup metadata. Canonical evidence remains local; no raw prompt or proposal is copied into tracked documentation. The single C allowance is consumed successfully. No M6 frozen execution, provider comparison, review decision, broad support claim or subsequent task is authorized by this result.

### M303-FINAL-02 — integrated review and task closure

On 2026-09-16 UTC, a fresh independent critical reviewer returned **PASS** for integrated implementation and completed C across I1–I8, with no remaining Blocker, Major or Minor. Reviewed plan SHA-256 was `84ae88bdac3f5fc29a996f27de249023e573bd67d7720c51258d7863a2e52b5e`; this receipt, status maintenance and relative-link rebasing are subsequent non-contract edits. One minor timing ambiguity was corrected before PASS: 13,340 ms measures generation alone.

The reviewer independently validated the aggregate/proposal, reconstructed native and supported evidence hashes and the exact request, verified all 13 prior runs plus 34 siblings, confirmed the in-flight simultaneous model observation and cleanup, and reproduced strict TypeScript. Complete source/build/frozen identity supports reuse of the fresh 712-test result. Primary's timestamped manual UI observations are accepted with the explicit absence of an independently inspectable screenshot artifact. This closes every M3-03 Verification obligation with the prior G/A/B evidence and the separate actual C record; it does not claim semantic approval, sustained performance, broad support or release qualification.

Primary inspected the seven-document candidate diff and accepted 444 local links/anchors, UTF-8/final newlines, all 11 parsed PowerShell fences, unchanged README command fences, unchanged roadmap Verification statements and `git diff --check`. A separate independent reviewer read the entire M3-04 archive and accepted exactly two navigation repairs; its other bytes and historical claims are preserved. The documentation closure gate passes. Roadmap/progress status is Complete; this plan is archived with its filename and headings retained, internal links rebased and inbound links repaired. Final post-archive checks pass for all 474 local links/anchors across eight affected documents, status consistency, preserved roadmap Verification and README commands, exact two-link M3-04 repair, UTF-8/final newlines and whitespace. These checks verify receipt/navigation maintenance without repeating runtime operations.

Documentation impact: Updated README capacity guidance, the feasibility observation, roadmap status, plan/progress indexes and this task's progress/ExecPlan; repaired only the two inbound links in the M3-04 archive. Requirements, ADRs, task dependencies, source, tests, configuration, secrets, frozen inputs and earlier canonical runs remain unchanged. The new proposal remains pending human review, the application/tab are closed and developer-owned Ollama is retained. No later task, commit, push or publication is selected or performed.

## Validation and Acceptance

Planning is accepted only after a fresh critical readiness review and proportional documentation/preservation checks. It proves a safe route to remaining decisions, not Qwen feasibility or execution readiness.

Execution acceptance requires P, complete G and I1–I8, separately owned A/B preflight/Red/Green/reviews, exact actual Local configuration and wire/counting proof, zero-call missing-prerequisite/fit paths, normalized one-attempt output/failure, preserved service/aggregate behavior and the required real C capacity PASS. Keep prompt/schema production conformance distinct from future frozen evaluation byte conformance. No six-case run, semantic review decision or provider qualification is claimed.

Complete the authoritative suite including new tests, independent strict checking and client build, then different fresh integrated critical review. Broaden or repeat only for changed/stale/contradictory evidence, failed prerequisites or unresolved risk; handoff alone does not invalidate a complete identity. If C cannot satisfy a real prerequisite, accept only supported implementation checkpoints and keep the task In progress or Blocked as warranted. Do not mark Complete from synthetic tests or defer a required proof without the owning decision.

Apply the [documentation closure gate](../../README.md#task-closure-documentation-gate): reconcile materially affected runtime/setup instructions, exact Local configuration and capacity notes, roadmap/progress/plan indexes and relevant current-status statements; update requirements/ADRs only for an explicitly accepted material decision. Run link/anchor, UTF-8/final-newline, configuration/command and formatting checks including `git diff --check`. Archive only after Verification and closure pass; no M3-04/M3-05 activation is implied.

## Idempotence and Recovery

Resume from Current state and the latest accepted contract/evidence; historical M202/M302 failures, helpers, grants and receipts are not reusable authorization. Preserve the current developer runtime/model store and existing run evidence. Exact-byte recovery, if later needed, follows the owning freeze procedure, not a new accepted hash.

Primary closes a stopped lease before triage or documentation. Do not revert peer changes, continue a closed lease, reset budgets or silently weaken binding/accounting. An unavailable genuine prerequisite blocks only its dependent work. Investigate substantive recurring defects under the [bug index](../../bugs/README.md), preserving BUG-0001 history.

Before any removal verify exact disposable paths are ordinary, ignored, untracked and inside their approved root; stop owned services/requests/browser resources first and inspect their terminal outcomes. Never delete an application run store, retained M2/M3 input, model store, runtime, dependency tree or workspace root for cleanup. No broad process kill, port takeover, hidden model retry, commit, publication or push is authorized.

## Artifacts and Notes

Accepted execution adds the four cohesive Qwen production modules and three suites with their helper, and updates README, the documentation index, this plan, roadmap status and existing plan/progress navigation. Safe proof notes remain here; no extra design document, raw transcript, generated ledger, benchmark or diagnostic subsystem is introduced. Detailed private inputs remain in explicitly permitted ignored locations; tracked evidence retains only content-safe identities/outcomes.

## Interfaces and Dependencies

The new adapter implements the existing `GenerationAdapter`/`PreparedGeneration` and normalized result types. It consumes M302's immutable request/configuration, uses its attempt capability and returns a candidate to existing application validation. Internal service/repository lifecycle remains the owner of selected state. The accepted implementation uses native Node HTTP and the artifact-linked conservative accounting bound frozen by G, without a tokenizer dependency or custom parser platform. Ollama/model setup remains developer-owned. M3-04/M3-05 subsequently implemented and verified the fixed Groq path and visible generation; M4-01 through M4-03 subsequently implemented and verified human-review controls. Their completion resolved the interface prerequisite; M303-C-OBSERVATION-01 separately passes C.

## Revision note

2026-09-16 UTC / primary: Recorded the successful sole C observation, final independent PASS and documentation closure; marked Complete, archived the plan and repaired navigation. Preserved historical blockers, consumed grants and semantic/visual evidence limits.

2026-09-16 UTC / primary: Reconciled completed M4 review prerequisites and fresh regression/runtime/resource preparation in M303-RESUME-03. Preserved Blocked status, the unused C activation and the separate pending runtime-launch allowance.

2026-09-13 / primary: Reconciled current M3-03 prerequisites with completed M3-05 and linked its actual integration evidence. Preserved historical observations, Blocked status, the unused C allowance and the required reviewer-interface/full-stack capacity boundary. Documentation-only maintenance; no application or runtime action occurred.

2026-09-10 / primary: Created the M3-03 planning-only ExecPlan after current-state/authority review and bounded source exploration. Preserved fixed Qwen/runtime and prior freezes; separated metadata admission, remaining R3 contracts, two behavioral slices and one real capacity proof. Recorded unresolved actual eligibility and representative UI prerequisites. Accepted independent planning PASS and documentation/preservation checks; no implementation, setup or model operation occurred.

2026-09-10 / primary: Recorded subsequent execution authorization, refreshed prerequisite/freeze/resource evidence, accepted P metadata plausibility and mandatory R3 OWNER DIRECTION, and prepared the concrete owner scheduling question. G, developer setup, implementation and actual capacity remain unpassed; no task dependency or requirement is amended by the proposal.

2026-09-10 / primary: Applied owner acceptance of the two reviewed start-dependency amendments in the roadmap. Preserved every completion gate and later-task status; prepared the remaining external setup and bounded actual-artifact re-entry request. No runtime/model operation or implementation occurred.

2026-09-10 / primary: Recorded explicit owner permission for the exact external setup and one additional bounded actual-artifact report. Renewed read-only prechecks pass; no other research, implementation or capacity budget is renewed.

2026-09-10 / primary: Accepted G and separate-owner A/B implementation with critical reviews, the 550-test full regression, independent strict/build and different integrated PASS. Reconciled seven existing documents and retained the exact C blocker, unused activation, unchanged completion gates and active plan. No later task or actual generation was selected.
