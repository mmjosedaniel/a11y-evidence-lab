# Present the accessible review interaction

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M4-02](../../DEVELOPMENT_ROADMAP.md#m4-02--present-the-accessible-review-interaction), Complete after accepted G/A/B, integrated critical PASS and documentation closure.
- **Latest barrier:** [M402-FINAL-01](#m402-final-01--integrated-review-and-task-closure): all 706 tests across 31 files, current strict/build, synthetic browser-to-disk proof and final-source desktop/narrow evidence pass.
- **Pending:** None within the owner-amended M4-02 verification scope. Native 200% was omitted at explicit owner direction in M402-B-VISUAL-02, not passed.
- **Active lease:** None; all eleven implementation leases closed compliantly.
- **Allowance:** All consumed worker/review budgets remain consumed. Completion grants no additional work, actual scan/retrieval/generation, credentials, acquisition, retained-run mutation or Git writes.
- **Next:** No task is activated automatically. M4-03 actual-proposal review remains Not started; M3-03 remains independently Blocked on capacity.

## Progress

- [x] (2026-09-14 15:47Z) Record execution authorization and current clean baseline; independent strict TypeScript and 92 pure tests pass. M402-EXEC-01.
- [x] (2026-09-14 16:04Z) Complete bounded integrity research, mandatory DRAFT READY synthesis and its one correction, and fresh pre-draft PASS after the supported outline correction; M402-G-RESEARCH-01. Author G for different fresh final review; no implementation lease opened.
- [x] (2026-09-14 15:26Z) Inspect current roadmap, M4-01 closure, selected authorities, frontend skill and relevant service/UI/test seams. Fresh strict TypeScript and 92 pure tests pass; M402-ENTRY-01.
- [x] (2026-09-14 15:37Z) Accept the complete planning draft, independent readiness PASS with no findings and documentation checks; M402-PLAN-01. G and implementation remain unstarted.
- [x] (2026-09-14 16:20Z) Accept G's exact contract, reuse audit and command/path packets after different fresh final PASS; one final-artifact correction consumed. M402-G-ACCEPT-01.
- [x] (2026-09-14 17:05Z) Accept A: same-origin review transport and response admission through separate-owner TDD, strict/affected regression checks and fresh critical PASS. M402-A-ACCEPT-01.
- [x] (2026-09-14 19:44Z) Accept B: accessible review interaction and final-state presentation through frontend-visual TDD, browser evidence and fresh critical/visual PASS; M402-B-ACCEPT-01. Native 200% omitted at owner direction.
- [x] (2026-09-14 19:51Z) Accept integrated verification, different fresh critical PASS and documentation closure; mark M4-02 Complete and archive this plan. M402-FINAL-01.

## Surprises & Discoveries

- [M4-01 final closure](m4-01-review-behavior-and-persistence.md#m401-final-01--integrated-review-and-task-closure) accepts 671 tests in 28 suites, strict/build, integrated re-review and exact cleanup. Its prior Minor test-cleanup finding and owner-authorized correction are preserved there. Production review semantics are implemented, not open design questions for M4-02.
- `ProposalDetail.tsx` labels every proposal as pending, `ResultDetail.tsx` renders it only for `proposal-pending-review`, and `resultPresentation.ts` gives cached generation presentation precedence over the durable state. M4-02 must reconcile all three when displaying a saved decision; adding a form alone would leave contradictory labels or hide the original.
- `ReviewOutcome` success contains a validated durable run; failures contain `persisted: false` and possible cleanup uncertainty. A lost HTTP response is different: the synchronous service may already have committed. UI timeout or abort cannot prove the decision was not saved and cannot authorize automatic resubmission.
- Existing `generation-api.ts` caps its small ID-only request at 1024 bytes. A complete edited proposal needs a separately derived finite request bound; copying that cap would reject valid review content. No shared HTTP framework is required.

## Decision Log

- Decision: execute the existing M4-02 scope through closure under the owner's explicit request, superseding planning-only authorization without waiving G or any implementation/verification gate. Date/author: 2026-09-14 / primary.
- Decision: activate only M4-02 planning. Rationale: explicit owner request and Complete M4-01 satisfy selection and prerequisites; application work awaits execution authorization. Date/author: 2026-09-14 / primary.
- Decision: plan a nonvisual transport/admission slice and a frontend-visual interaction slice, followed by integrated closure. Rationale: each has a coherent independent proof and the appropriate implementation owner. Date/author: 2026-09-14 / primary.
- Decision: reuse M4-01's accepted review contract and writer without redesigning persistence. Rationale: only exposing that contract is needed; its representation, irreversible finality and failure behavior are already implemented. Date/author: 2026-09-14 / primary.
- Decision: use R3 for unresolved client/transport identity and lost-response recovery choices, and S3 for A/B review. Rationale: response correlation, stale/reentrant interaction and irreversible final-decision integrity remain critical even though the writer is reused. Visual review is included in B's existing review, not a separate panel. Date/author: 2026-09-14 / primary.

## Outcomes & Retrospective

G, A and B are accepted. The bounded same-origin endpoint and exact client admission expose M4-01's existing final-decision semantics without changing the writer. The UI supports three review actions, complete editing and immutable final presentation. Strict/build, all 706 tests across 31 files, independent before/after focus reproduction and final-source desktop/narrow visual evidence pass. Native 200% is omitted at owner direction. The different integrated critical review and documentation closure pass; M4-02 is Complete. No human decision on an actual retained proposal is claimed. The stale-form finding shows why asynchronous focus must use the current mounted form at settlement, including selection away and back; passing the ordinary save path alone did not prove that interaction.

## Purpose / Big Picture

Allow the developer to review one valid generated proposal in the existing selected-Finding workspace: approve the original, edit and accept a complete validated proposal, or reject it. Keep evidence, guidance, AI-authored original, reviewer-authored content and saved decision distinguishable. A final action becomes visible as saved only after a trustworthy service response or the explicitly frozen recovery check confirms canonical state. Users retain their place in the result list and can inspect the post-change reminder without being asked to complete it during review.

## Context and Orientation

Start from the [authority map/task router](../../README.md#read-by-task), [global requirement semantics](../../PROJECT_REQUIREMENTS.md) and the owning roadmap row. Selected authorities are all Accepted at their applicable MVP scope:

| Authority | M4-02 obligation |
| --- | --- |
| [REQ-REV-001, 008, 009](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#human-review-and-manual-checks) | Only valid proposals enter individual human review; original preserved; exactly one final action; support and blocking-judgment gates; bounded note; full edited proposal only for edit-and-accept. |
| [REQ-UX-002](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#evidence-oriented-interface-and-export) | Distinguish evidence, guidance, AI interpretation, sufficiency and human work without color alone. REQ-UX-011 controls the existing selected-Finding/no-abstention-review interface. |
| [REQ-A11Y-001–004, 009, 010](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md) | Keyboard operation, semantics, accessible relationships, visible focus, contrast/reflow, one shared announcement pattern, complete results and selection-focus preservation. REQ-A11Y-006 governs the proportional verification method. |
| [SPEC-005, SPEC-008](../../specs/SPEC.feature), [HS-010](../../specs/HARD_SPEC.feature) | Three individual review branches; support/judgment gates; distinguish original/edited content; non-blocking reminder; no whole-page or automatic-remediation claim. |
| [ADR-0012](../../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) | React is presentation/transient interaction over the application-owned service; the validated single-file aggregate owns durable decisions. Preserve each ADR's recorded status and amendments. |
| [Lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md), [existing presentation](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md), [visual foundations](../../ui/VISUAL_FOUNDATIONS.md) | Canonical states and evidence hierarchy; no competing lifecycle, lost evidence, extra panels or decorative redesign. UI guidance remains subordinate to requirements. |

Entry is clean HEAD `5f1de30ee1aad85a4d1ecf99fca9beab6065794d`. Seventeen tasks are Complete; M3-03 is Blocked. This planning activation makes M4-02 In progress and leaves nine later tasks Not started. M4-01's 671-test closure is historical accepted evidence, not a fresh full-suite run in this turn. Actual Local/Groq proposals remain pending human review. M3-03 capacity and M6 evaluation retain independent gates.

M3-05's role-aware retrieval selection, corrected runtime instruction version and historical-read compatibility remain unchanged. The frozen M3-01 manifest and its thirteen referenced inputs remain protected. This task does not run the six fixed generation evaluations. OD-026's run reopening/history UI stays Deferred; a narrowly scoped response-recovery check is not general reload restoration.

### Accepted M4-01 interface to consume

`LocalService.reviewFinding(input: unknown): Promise<ReviewOutcome>` accepts exactly `{ runId, findingId, review }`. `src/server/domain/review-contract.ts` owns these existing literals:

- Actions are `approve`, `edit-and-accept`, `reject`; terminal Finding states are `accepted`, `edited-and-accepted`, `rejected`.
- Approval/edit input requires `supportConfirmed: true`; that transient confirmation is not persisted. Rejection must omit it. The UI cannot automatically set it merely because generation validation passed.
- `blockingJudgment` is `supports-proposal`, `not-applicable` with a reason, `unresolved`, or `contradicts-proposal`. Only the first two permit acceptance. Rejection permits all four. Only N/A carries a reason.
- Optional note is nonblank and at most 1000 JavaScript UTF-16 code units when present; N/A reason is nonblank and at most 500. The validator preserves submitted text rather than trimming it. Empty optional controls must map to omission, not an invalid empty property.
- Only edit-and-accept carries a complete `editedProposal`, validated by the existing closed eleven-field Proposal contract with the original Finding/retrieval context. The original `result` and generation provenance remain unchanged. Server-owned `decidedAt` is not a form field or browser-supplied timestamp.
- Success returns `{ ok: true, run }`. Failure uses the existing bounded `ReviewOutcome` error union with `run`, `persisted: false`, `cleanupFailed`. M4-01 already controls duplicate/stale transition refusal, admission and atomic publication. Do not alter it to make UI tests pass.

### Current seams and reuse audit

This is a planning audit, not the implementation preflight's accepted reuse-audit identity. Revalidate actual cohesion at G/B and name any changed disposition before coding. Source paths below are repository-relative.

| Current responsibility / path | Disposition and bounded change |
| --- | --- |
| M4-01 validator, service operation and repository transition | `REUSE_AS_IS`: canonical review semantics and durable truth. No new decision entity, serializer or writer. |
| `src/server/local-service/loopback-api.ts`, `src/server/service.ts` | `EXTEND`: dispatch one same-origin review endpoint to the existing service method. The endpoint body reader is a distinct new owner, not another substantial branch embedded in the router. |
| `src/client/finding-response-snapshot.ts`, run/proposal validators | `REUSE_AS_IS`: inert detached response inspection and structural validation; reuse without importing filesystem/service implementation into the browser. |
| `src/client/main.tsx`, `src/client/App.tsx` | `EXTEND`: callback composition and necessary shared run/reservation/announcement coordination. Substantive request/admission mechanics have named owners; do not append a monolithic review subsystem to App. |
| `ResultsSection.tsx`, `ResultDetail.tsx` under `src/client/components/results/` | `EXTEND`: pass review controls and compose original, review form and final decision for the selected Finding. No new page or routing framework. |
| `ProposalDetail.tsx` claim/reference presentation | `REUSE_AS_IS` for existing claim/reference semantics; `EXTEND` labels for original versus reviewer-authored full content and final state. `EXTRACT_LOCAL` the structured body only if necessary for these two actual consumers; no generic renderer. |
| `resultPresentation.ts`, `FindingGeneration.tsx`, App selection announcement | `EXTEND`: durable final review state takes precedence over obsolete pending-generation labels while preserving actual provider-call provenance. |
| Review form, complete plain-field editor, saved decision | `CREATE` named task-local owners: `ProposalReviewForm.tsx`, `ProposalEditor.tsx`, `ReviewDecision.tsx`. Form owns current draft/confirmation/errors, editor owns complete proposal fields and references, decision owns immutable human-authored projection. No wrapper-only review container. |
| `styles.css`, existing shared status, results list, scanner evidence and authenticated guidance | `EXTEND` only necessary review control/error layout styles; `REUSE_AS_IS` shared announcement mechanism, list/evidence/citation owners and existing light direction. |
| `tests/helpers/m104-ui-harness.ts`, M305 generation fixtures, M401 review fixtures/sandbox | `EXTEND` only named review callbacks/controlled cases; reuse existing build/browser and exact-root cleanup patterns. New review fixtures must stay visibly synthetic and test-owned. |

## Scope and Non-Goals

Include the minimal same-origin HTTP/client admission needed to invoke the existing internal service, accessible proposal-only controls, complete plain-form edit-and-accept, support and judgment confirmation, optional bounded note, safe validation/operation errors, saved-decision projection, truthful provider/original labels, shared announcements and focus behavior. Preserve all prior evidence and the reminder after each final action.

Exclude actual provider calls, scans, retrieval/model acquisition, credentials, changing retained owner runs, review of actual generated proposals, generation evaluation, capacity work, comparison, source-code remediation, new domain decisions, persistence changes, authentication, reviewer profiles, comments/history, versions, queues, bulk review, automatic retry/regeneration, run reopening, rich-text/JSON code-editor tooling, dependencies, form/component frameworks, design systems, animations, screenshot infrastructure or telemetry. Controlled proposals and disposable runs prove this UI contract; M4-03 remains the real-proposal review checkpoint.

## Plan of Work

Apply the [worker-first workflow](../../../.codex/execplan-implementation-workflow.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), [write guard](../../../.codex/write-lease-guard.md) and the [frontend-quality skill](../../../.agents/skills/frontend-quality/SKILL.md) for B. The primary owns authority, authoritative documentation, acceptance, leases, integration and closure. Research/review roles are read-only. No worker owns the plan, requirements, developer documentation, status or Git mutations.

Each implementation slice receives a complete Milestone Assignment Packet v2 before read-only `test_worker` preflight. Classify existing coverage, characterization and the explicit missing portion accurately. `UNKNOWN`/`CONFLICTING` stops for primary triage; do not manufacture Red. The first-module missing-export exception applies only with verified environment, full behavioral tests and honest capability-absence evidence. Separate Green must execute every accepted test unchanged and pass independent strict TypeScript.

Keep one test owner and the selected implementation owner per slice; Red and Green/optional behavior-preserving Refactor are sequential. Every write turn has a fresh primary-opened and terminally closed lease. Only one lease is active per worktree. Inspect actual diff, complete evidence identity, cohesion, commands and terminal receipt before acceptance. Primary documents between leases; only the existing bounded primary test-correction exception can permit direct test edits with evidence invalidation/reacceptance.

Budget: one preflight, one coherent Red/characterization and Green sequence per slice, at most three writes per unchanged role/phase chain (initial, ordinary correction, conditional final correction with recorded learning), and one review correction loop. No agent/packet/lease ID resets a consumed allowance. Stop dependent work after exhausted budget, changed binding fields, two same decisive gaps without new evidence or two no-diff handoffs. Resolve routine stops within remaining authority and complete unaffected work; ask the owner only at the actual decision/authority boundary. Only useful independent read-only work may run in parallel.

### G — Freeze transport, admission and interaction literals

**M402-G; R3; TDD: Not applicable.** Primary-owned contract work, no production writes or worker lease. Replacement evidence: traced current authority/source, complete cumulative invariants and mandatory research reviews. Production responsibility change: None.

Keep the M4-01 interface fixed. Freeze the exact route/method/content type, finite body limit derived from every valid complete edit including UTF-8/JSON escaping, strict request parsing, error/status envelope and abort/duplicate-dispatch behavior. Freeze how an unexpected transport failure is distinguished from a definite M4-01 refusal; never forge a known-unsaved result after a possible commit.

Freeze client response admission against a captured immutable run, Finding and submitted action/body: valid run shape alone cannot prove the server saved the intended edit or action. Require unchanged original, siblings, parent and invocation; tolerate only the permitted selected final transition and server decision time. Freeze failure envelopes, stale/reentrant/unmount handling, pending ownership, finite local deadline/abort effects and late-response handling. A missing or malformed reply remains unknown unless a specifically authorized, finite current-run read proves canonical state. Choose conservative unknown-state blocking or that minimal read, not a polling/retry/reopen subsystem. Never resend the final decision automatically.

Freeze the UI's complete editor mapping: preserve fixed identity/type/sufficiency context; allow the current complete proposal's editable prose, categorical confidence, assumptions and allowed per-claim evidence/passage references through native controls. Retain all eleven fields in the submitted candidate, validate against existing reference rules, and make unsupported edits resolvable. Define omission of blank optional note, exact existing bounds, N/A reason display, inline validation relationships, and the action-specific request shape. Confirmation must refer to the submitted resulting proposal; changing relevant draft content must invalidate a prior confirmation. Rejection must remain available without a false support assertion or a passing judgment.

Freeze local draft lifetime keyed to run/Finding, selection during submission, pending/final button behavior, and safe handling of a focused form when it is replaced by the decision. Preserve focus on a selected list item; asynchronous completion must not steal focus from a sibling. Reuse the one shared status region to announce the captured Finding's saved/error/unknown outcome with existing provider provenance and without implying sibling changes.

Complete the B frontend-visual capsule: accepted reuse-audit identity, named form/editor/decision owners, source labeling and final-state precedence, bounded state/viewport samples below, exact native-zoom mechanism and browser/cleanup callers. Freeze exact A/B allowlists, command/effect slots, prerequisite identities and protected inputs before preflight. No new product architecture is being selected; an incompatible requirement or broader change requires owner direction.

### A — Same-origin review transport and response admission

**M402-A; TDD; S3; `test_worker` then `code_worker`.** Authorities: REQ-REV-001/008/009, ADR-0012/0021, SPEC-005, HS-010 and G's accepted identity/recovery contract. Preflight target: no review HTTP dispatch or browser response-admission function exists at entry.

Test-owner candidate paths: `tests/finding-review-api.test.ts`, `tests/finding-review-admission.test.ts`, `tests/helpers/m402-review-fixture.ts`; use existing M401 fixtures read-only. Any required existing service test addition must be named before preflight. Production candidate paths:

- `src/server/local-service/review-api.ts` (new): bounded review request/body parsing, one dispatch and honest transport outcome. Reuse the local route pattern without changing generation/retrieval readers or introducing a common HTTP framework.
- `src/server/local-service/loopback-api.ts`, `src/server/service.ts`: minimal callback/route wiring only; the existing review service remains the decision/persistence owner.
- `src/client/finding-review-admission.ts` (new): detached request-correlated response validation, selected-transition checks and bounded failure admission. Depends on browser-safe validators and snapshot/equality; no React, service implementation or filesystem dependency.
- `src/client/main.tsx`: minimal same-origin callback wiring when its consumer exists; if an unused callback would fail strict checking, freeze this wiring in B rather than adding a stub.

No rendered changes in A. Tests prove exact parsing/no dispatch on malformed/oversize/aborted input, single dispatch, service delegation, response/body/action matching, immutable context/siblings, forged/unknown failures, accessor/prototype hazards and no provider execution. Use controlled callback tests and a bounded real loopback test where required; service-owned integration can reuse synthetic M401 pending proposals in exclusive test roots. Commands cover new A suites, affected generation API/admission regression, review-contract/run-contract and independent strict TypeScript. Advance only after accepted Green, terminal leases, actual cohesion inspection and fresh critical PASS or reconciled non-blocking follow-ups.

### B — Accessible review interaction and saved decision

**M402-B; TDD; S3; profile `frontend-visual`; `test_worker` then `frontend_code_worker`.** All M4-02 authorities apply, with the skill's accepted reuse/visual capsule required before preflight. A must be accepted. Preflight isolates missing review controls/final-state presentation without rewriting already-covered generation/list behavior.

Test-owner candidate paths: `tests/finding-review-ui.test.ts`, `tests/helpers/m402-review-fixture.ts`, `tests/helpers/m104-ui-harness.ts`, and necessary focused assertions in `tests/finding-generation-ui.test.ts`. Name any additional existing test/harness entry path at G. Green never edits tests. Production candidate paths are App/main composition; `src/client/review-request.ts` for substantive transient request ownership/deadline/admission mechanics if needed; the three named new form/editor/decision components in the reuse audit; bounded `ProposalDetail.tsx`/local structured-content extraction; `ResultsSection.tsx`, `ResultDetail.tsx`, `FindingGeneration.tsx`, `resultPresentation.ts` and `styles.css`.

Responsibility direction: App owns shared run/selection/reservation and the one announcement setter; the review request owner coordinates the bounded async transport and admission, not domain decisions; the form owns transient fields/confirmation/errors for one run/Finding; the editor owns complete proposal-input controls; the decision owns immutable saved human-authored output. Detail composes them with existing scanner/guidance/original presentation. Rendering and pure mechanics stay outside App when substantively distinct. No generic hook, reducer, form schema engine or wrapper component is justified merely by file size.

Render original content explicitly as AI-authored whether pending or reviewed. Edited content is reviewer-authored and must not be labeled original model output; its confidence and wording cannot be attributed to a new model call. Keep original and accepted edited content inspectable with their references and distinguish which was accepted. Reject displays no accepted plan. Show action, service decision time, judgment/reason and optional note, without actor/history fields. All reviewer/model/target text stays inert.

The form uses native labeled controls and concise support/judgment instructions. Keep the post-change reminder visible for pending and every final branch, without a completion checkbox or acceptance gate. Use final durable state for list/detail/selection announcements instead of stale cached pending-generation text. Do not hide deterministic evidence or guidance while editing. Preserve keyboard order, selected list focus, non-color final labels and submission/error/unknown feedback under G's frozen focus contract.

Focused proof uses the actual rendered application in the existing browser harness, controlled Local/Groq proposal fixtures, automated axe/keyboard/focus/announcement checks and the bounded manual visual samples below. A final decision shown in a fixture is not evidence that a real owner proposal was reviewed. Include a synthetic browser-to-service-to-disk flow through A's real endpoint; freeze seed/build/readback/cleanup in the packet rather than shipping a production test backdoor. Fresh critical review covers B's integrity and visual-quality obligations in one review.

### C — Integrated verification and documentation closure

**M402-C; no new production slice; TDD: Not applicable.** The primary integrates accepted A/B and reuses current evidence identities. Run independent strict TypeScript, the client build and the complete maintained authoritative suite (entry: 28 suites, plus accepted M4-02 suites). Repeat broader tests only for changed/stale evidence, failures or unresolved risk; perform required independent risk-critical reproduction. Audit test fixtures/mocks/skips and actual responsibility placement before handoff.

Obtain a different fresh integrated `critical_reviewer`, including accepted visual evidence and cumulative I1–I10. No separate visual panel, full browser matrix, real provider call or actual-proposal review is required. M4-03 and M3-03 retain their respective real-human-review and capacity obligations.

Apply the [documentation closure gate](../../README.md#task-closure-documentation-gate). Primary updates the implemented UI extension and instructions in the existing UI documentation, current capability/API/developer test inventory, this plan, roadmap/progress and affected navigation. Do not change requirement/ADR semantics merely to reflect implementation. Verify links/anchors, command/configuration consistency, encoding/whitespace and `git diff --check`; close owned resources. Mark Complete/archive only after task Verification and documentation closure pass. M4-03 stays Not started until selected.

## Decision Review Contract

**Identity:** M402-G, R3 transport/client decision-integrity contract. Research, amended pre-draft review, different final artifact review and G acceptance are complete; M402-G-ACCEPT-01 owns the accepted record. **Artifact:** the contract and exact packet/command subsections below; no extra ADR, ledger or research document. **Approval boundary:** primary accepts task-owned literals compatible with existing authorities after required checkpoints; owner decides architecture/scope conflicts.

**Discovery/candidates:** at most one bounded non-ranking pass for exact current transport, browser harness and visual mechanism gaps. Freeze candidates, hard gates and evidence dimensions before comparison. Compare only minimal compatible admission/recovery and local ownership options. Existing M4-01 shape, writer, reference rules and finality are fixed inputs, not candidates. Reject a new backend, generic form framework or workflow/retry engine at the scope gate.

**Criteria:** preserve the exact intended human action and submitted content, original/sibling identity, durable truth despite lost/late replies, bounded transport, no unauthorized call, accessible source distinctions, current-state honesty, cohesive minimal owners and executable controlled proof. Decide literals now; prove runtime/visual behavior in A/B. A confirmation field or valid citation is not proof of semantic truth.

**Budget/topology:** one `critical_researcher` report for the cohesive request-to-visible-decision integrity dimension, covering parsing/serialization, action/body correlation, stale/reentrant ownership, lost-response recovery and failure presentation; one bounded follow-up. Primary supplies routed UI authorities and current reuse evidence. One mandatory `decision_analyst` synthesis/contract audit plus one bounded correction; fresh `critical_research_reviewer` pre-draft checkpoint; primary-only authoritative drafting; different fresh `critical_research_reviewer` final review. No drafter or optional visual research report is planned. Allow one pre-draft correction and the workflow's maximum two final-artifact correction cycles within the existing research budget. If the report cannot cover the critical dimension, stop/reconcile its capsule before adding work.

Use Research Assignment Capsules projecting this contract, current source/evidence identities, exact authorities, required outputs and remaining allowance. Synchronize research before analysis. Synthesis returns exactly `DRAFT READY`, `RETURN FOR RESEARCH` or `OWNER DIRECTION`; DRAFT READY does not bypass either R3 checkpoint or open a lease. Review the exact primary artifact and complete I1–I10 packet, separating evidenced contract facts from pending runtime claims. The primary reconciles findings before accepting G. Repeated decisive gaps twice without material new evidence, exhausted budget, changed scope/authority or unresolved owner choices stop dependent work; new agent IDs never reset limits.

## Concrete Steps

### M402-G-RESEARCH-01 — Synchronized research and amended pre-draft checkpoint

The bounded `critical_researcher` completed E1–E7 for request-to-visible-decision integrity. The mandatory `decision_analyst` returned **DRAFT READY**, then retained that result after its single bounded correction. A fresh `critical_research_reviewer` returned **PASS**, with no outstanding Blocker, Major or Minor, on the complete amended outline and I1–I10. The primary now drafts the sole authoritative artifact; a different fresh final research reviewer must pass before G acceptance. No optional panel or drafter was used.

Research evidence: E1 is the routed M4-02 authority/DRC/I1–I10 packet; E2 is fixed review/proposal validation; E3 is the synchronous service and selected-Finding writer; E4 is detached response inspection and current App ownership. The independently verified Git blob identities are review-contract `f93226005605a55581aa56fa43b023f740474dc3`, proposal-contract `790a5c012ff9bbf5df07198c001c16ca9dc56b3e`, review-operation `bb34b194c48482d656f4d4e2d4bb5c93bcf1112f`, service `55692a2857fb723de357406225d08f342c9fb01f`, and finding-response-snapshot `30a1a5363784a15f1a32a502fb4a29f8cb6e3e21`, at EXEC-01 HEAD. E5 is independent in-memory Node 24.20.0 reproduction of maximum valid escaped content, repeated by the pre-draft reviewer: image/label/contrast compact requests are 67044/67794/67476 bytes; fully Unicode-escaped string tokens are 71014/75199/73381 bytes. Fatal UTF-8 rejection, native duplicate-key last-wins and lone-surrogate escaping also pass.

E6/E7 primary sources, accessed 2026-09-14, are the [Encoding Standard](https://encoding.spec.whatwg.org/#interface-textdecoder), [Fetch abort algorithm](https://fetch.spec.whatwg.org/#abort-fetch), [ECMAScript JSON serialization](https://tc39.es/ecma262/multipage/structured-data.html#sec-json.stringify), and [Node IncomingMessage documentation](https://nodejs.org/docs/latest-v24.x/api/http.html#class-httpincomingmessage). Node documentation returned 24.21.0 while executable evidence uses pinned 24.20.0. Abort supplies no service-commit acknowledgement; normal request close is not alone evidence of an incomplete request. These are contract sources, not M402 runtime proof.

| Compatible candidate | Finality and correlation | Lost-response behavior | Disposition |
| --- | --- | --- | --- |
| Conservative retained review ownership, no GET | Preserves the submitted action and whole-run baseline by preventing another mutation | A committed decision may remain honestly unconfirmed | Selected for the smallest current mechanism |
| One current-operation canonical GET | Can positively reconcile only the exact requested final transition | A pending read cannot permit retry: delayed POST, timeout, GET pending, then original POST commits | Not selected; extra request/admission mechanics do not provide safe negative recovery |

The supported outline correction resolved two gaps. A valid pending selected Finding can coexist with an active sibling; the reviewer reproduced that both a forged final run and its baseline validate and compare after reversal, although the unchanged service refuses `workflow-active` with zero writes. Capture and success admission therefore explicitly require no active Finding. Second, injecting a pending run only through Analyze leaves the guidance view absent. The integration proof now uses three exact synthetic prior-stage responses before the real review request. Neither correction changes M4-01 semantics.

Budget remaining: one researcher follow-up unused; analyst correction consumed; pre-draft supported correction consumed and passed; different fresh final review plus at most two final-artifact correction cycles remain. A/B write and review allowances are untouched. I2–I10 runtime/browser checks are pending, including actual native zoom. This checkpoint accepts drafting readiness only.

### M402-G-CONTRACT-01 — Transport and interaction contract

Status: accepted through M402-G-ACCEPT-01 after both fresh research checkpoints. This section resolves the former G literal slots. Fixed M4-01 validation, service behavior, persistence, finality and provenance remain unchanged.

**Transport (I2/I7).** Add only `POST /api/finding-review`, with no query or fragment and request Content-Type `application/json` (trimmed, case-insensitive as in existing routes). The exact outer JSON object has `runId`, `findingId`, `review`; validate outer keys and existing ID rules before dispatch. Existing service validation owns the nested review object and eligibility. Transport-invalid input has zero service calls; nested invalid review has at most one service validation call and zero writes. Browser requests use compact JSON serialization of detached intent. Native JSON duplicate members use last-wins semantics; no duplicate-member parser is added.

The body limit is **131072 actual bytes**. Every valid compact or fully string-escaped complete edit fits: 9500 UTF-16 proposal prose units plus 1000 note and 500 reason yield at most 66000 escaped bytes; three ID occurrences add at most 1152; thirty evidence references at a conservative 64 units add 11520; nine frozen passage references of at most 38 units add 2052; a 731-byte empty-variable-string structural skeleton multiplied by six adds an intentionally generous 4386. The total bound is 85110. Unlimited redundant whitespace is not promised. Keep the fixed validator/reference bounds protected and test their derivation, including U+0001 and multibyte content.

Validate any Content-Length as a safe decimal integer no larger than the cap and require it to equal received bytes. Count chunks without retaining data beyond the cap; concatenate once and decode with fatal UTF-8 before JSON.parse. A **30000 ms body-read deadline** starts when the review handler takes ownership. Refuse once before dispatch on expiry, oversize, malformed input, abort/error or incomplete close; clear the timer on every settlement/dispatch. Check monotonic expiry again after parsing before dispatch so delayed timer delivery cannot extend the deadline. Reserve dispatch before calling the collaborator; repeated data/end/error/abort events never invoke it twice. A complete request handed to the service can still commit after a disconnect. Do not reinterpret a post-dispatch disconnect as a definite refusal.

| HTTP status | Exact service error or envelope |
| --- | --- |
| 200 | `{ ok: true, run }` |
| 400 | `invalid-request`, `review-validation` |
| 404 | `not-found` |
| 409 | `busy`, `workflow-active`, `not-eligible` |
| 503 | `stopping`, `shutdown` |
| 500 | `invalid-run`, `stored-run-unavailable`, `read-failed`, `review-persistence` |
| 500, unexpected callback throw/rejection or unusable result | `{ ok: false, error: 'review-outcome-unknown' }`; no `persisted` or cleanup assertion |

Known service failures retain exactly `{ ok: false, error, run, persisted: false, cleanupFailed }`. A pre-dispatch transport refusal uses that shape with `invalid-request`, null run and false cleanup. Reuse the router's no-store, nosniff and JSON response headers and its absent-route/method behavior. API-only construction without a review callback exposes no review operation; do not expand health capabilities. The handler exports `receiveReview(request, execute, send)` from `src/server/local-service/review-api.ts`; router/service changes only wire that owner to existing `reviewFinding`.

**Admission (I3/I6/I7).** The B main callback accepts `ReviewIntent` and AbortSignal, POSTs the exact detached `{ runId, findingId, review }`, and returns exactly `{ status: response.status, body: await response.json() }`. A exports `ReviewIntent` (runId/findingId strings, review unknown at the validation boundary) and `admitReview(raw, baseline, intent)` from `src/client/finding-review-admission.ts`. Its result is a detached admitted existing ReviewOutcome or null. Unknown transport envelopes, malformed content and status/body mismatches return null; the request owner presents unknown save.

Capture/admission requires a validated completed run, matching IDs, the selected `proposal-pending-review` Finding and **no active Finding anywhere in the run**. Derive native Finding context by excluding workflow fields and restoring `unprocessed`; use existing `validateReviewInput` with its captured retrieval to derive expected durable ReviewBody. Snapshot all caller-owned input before validation; never invoke accessors or use JSON stringify as a substitute for descriptor-safe inspection.

For success, validate/detach the entire returned run; require the submitted action's exact terminal state; compare the decision excluding only server-owned `decidedAt` to expected ReviewBody, including note presence, whitespace, judgment, complete edited content and reference ordering. Existing run/review validation controls time format and chronology, without a new browser-clock equality rule. Restore the returned selected Finding to pending and remove its review in a detached comparison projection; that entire run must equal the captured baseline. This protects original result, generation/invocation, analysis/retrieval, native evidence, siblings/order and all parent/provider context.

For known failure, require exact envelope keys, recognized error, matching status, literal persisted false, boolean cleanup and null run or a validated run exactly equal to baseline. Only review-persistence can carry cleanup true. A differing run is inadmissible, never published. A review success preserving an active sibling is inadmissible even if its aggregate validates. Tests cover hostile accessors, symbols, prototypes, cycles and reentrant reflection.

**Request ownership (I6/I7).** App owns shared run/selection/reservation and the single announcement setter. `src/client/review-request.ts` owns substantive request capture, invocation, deadline, abort, admission and terminal retention mechanics. Acquire the shared operation token before props.reviewFinding property access, callback availability checks, snapshot reflection or invocation. Do not probe that callback during render. Recheck mounted/token/captured-run ownership after every reflective/callback/admission boundary. The selected list item may change without cancelling or transferring the captured operation.

A separate **30000 ms client deadline** covers dispatch through admission; use monotonic expiry checks as well as a timer. Terminalize before aborting and ignore every later fulfillment/rejection. Unmount invalidates publication before abort. Never infer service cleanup or non-commit from abort. A callback ignoring its signal still stops owning visible pending state at the deadline. No automatic resubmission and no recovery GET exist.

Release for correction only after local validation refusal or a non-callable callback discovered before dispatch, or an admitted exact-baseline invalid-request/review-validation refusal with cleanup false. Every other refusal retains the review mutation lock. A well-formed refused submission is labeled not saved; malformed/lost/late response or callback failure is labeled **Save outcome unknown**. Preserve the last validated evidence. Unknown copy explains that the decision may have been saved and further actions are blocked; it does not claim cleanup failure. Show reported cleanup uncertainty only when a valid service outcome reports it.

Pending/retained **M402 review ownership** blocks Analyze, guidance, generation and every review, while list/citation inspection remains enabled. The additional Analyze guard must derive specifically from review ownership, not globally from existing retainedOwner: preserve the pre-existing generation-unknown/new-independent-Analyze behavior. Review also respects existing same-run retained ownership. A final accepted response releases its review reservation, publishes only the correlated run, and never reapplies a late response. New validated runs clear only applicable transient review presentation when no review lock prevents their creation; no browser storage or reopening mechanism is added.

**Form and editor (I4–I9).** The form is keyed by runId/FindingId. Selection change discards unsaved draft, errors and confirmation; reselecting starts from the original proposal. Pending/unknown operation ownership lives outside the form. No draft survives a new run or unmount. Native action radios are **Approve**, **Edit and accept**, **Reject**, initially unselected; a single **Save decision** button submits the chosen action. The native judgment select initially says **Unresolved** and offers supports-proposal, not-applicable, unresolved and contradicts-proposal with readable labels. Only N/A displays its nonblank reason (maximum 500 raw UTF-16 units). Optional note is maximum 1000, blank/whitespace-only omitted; nonblank note/reason retains exact submitted text.

Support confirmation starts unchecked and explicitly asks the reviewer to confirm the resulting proposal's material claims against cited guidance, recorded scanner evidence or both. Approval/edit require it and an allowed judgment. Action, edited content/references, confidence, assumptions, judgment, N/A reason and note changes reset confirmation. Rejection omits confirmation and editedProposal and remains possible with unresolved/contradictory judgment. The post-change reminder has no completion checkbox or gate.

| Proposal field | Complete plain editor mapping |
| --- | --- |
| type, findingId, evidenceSufficiency | Read-only original context, included unchanged in the complete candidate |
| findingSummary, userImpact, remediation | Textareas, raw maxima 1000/1000/2000; separate native checkbox groups for each claim's available evidence references and captured retrieval passage IDs |
| Evidence/passage references | Options from existing assessFindingEvidence over native context and captured retrieval; preserve initial array order, remove by value, append newly selected values; no arbitrary new reference IDs. Summary needs at least one evidence reference; impact and remediation each need at least one passage. |
| confidence | Native select high/medium/low; reviewer attribution when edited |
| uncertainty, blockingManualJudgment, postChangeVerificationReminder | Textareas, each maximum 1000; complete nonblank values |
| assumptions | Five labeled textarea slots, each maximum 500; blank slots omitted, preserving nonblank slot order and exact text |

Initialize all eleven fields from the immutable original. Use existing validateProposal/validateReviewInput for authoritative validation; do not duplicate the prohibited-claims policy. Native bounds and explicit field/reference errors are related with aria-describedby and aria-invalid. A remaining opaque proposal-validation failure explains that content/reference rules must be corrected, relates the editor error to its controls and focuses the editor's named focusable group; otherwise focus the first invalid control. Validation and operation changes use only App's shared announcement region, not extra live regions/alerts.

Pending freezes inputs with native readOnly/guarded changes and aria-disabled controls while keeping the focused submit control mounted; guard all activation paths synchronously. For saved replacement, capture focus ownership before removal and focus the saved-decision heading only if the same Finding is still selected and focus still belongs to the removed form. List/sibling focus never moves because of completion. Announcements name the captured human-readable Finding and truthful existing provider provenance; they never imply siblings changed.

Original AI content remains inspectable after every final action. Edited full content and its confidence are explicitly reviewer-authored. Display action, server decision time, judgment/reason and optional note; rejection says no remediation plan was accepted. Preserve original/reminder text and the accepted edited reminder where applicable. Durable accepted/edited-and-accepted/rejected labels take precedence over cached pending-generation text in list, detail and selection announcements; retain actual invocation provenance. All model/reviewer/target content is inert text, with existing authenticated citation links and protection.

**Accepted reuse outline.** M402-UI-01 confirms the earlier granular audit from current source. REUSE_AS_IS: M4-01 validator/writer, descriptor snapshots, scanner evidence, citation resolution/presentation, FindingsPanel and shared announcements. EXTEND: App/main composition, ResultsSection/ResultDetail, ProposalDetail source labels, FindingGeneration provenance/final-state copy, resultPresentation precedence and only needed styles. CREATE cohesive owners ProposalReviewForm (draft/confirmation/errors), ProposalEditor (complete inputs), ReviewDecision (immutable decision projection), review-request (async mechanics). EXTRACT_LOCAL ProposalContent from ProposalDetail for the two present consumers, original and reviewer full proposal. `review-form-validation.ts` may own substantive pure candidate/error mapping if needed; it is not a new schema framework. No wrapper-only container, generic hooks/utilities, dependencies, design system or unrelated structural refactor. The only permitted refactor is behavior-preserving separation within these declared responsibilities.

### M402-COMMAND-01 — Exact callers and effect boundaries

Status: accepted through M402-G-ACCEPT-01; effects require their relevant packet and lease. All commands use cwd `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`, PowerShell 7.6.5, Node 24.20.0, Python 3.12.10 and the existing lock. No install, acquisition, provider call, actual target scan, live retrieval, credential inspection or retained owner root is permitted. Existing regression tests may exercise their controlled scanner/provider doubles as already maintained; that is not an actual target or model evaluation.

**M402-CMD-PREPARE.** Load the exact current maintained preparation in each shell; this caller defines functions and reads prerequisites only:

```powershell
$m402Readme = Get-Content -LiteralPath README.md -Raw
$m402Prep = [regex]::Match($m402Readme, '(?s)### Development command preparation.*?```powershell\r?\n(.*?)```').Groups[1].Value
if (-not $m402Prep) { throw 'Maintained preparation is missing' }
. ([scriptblock]::Create($m402Prep))
```

Before browser/build effects, primary checks ordinary resolved absolute paths with Assert-M105OrdinaryPath and empty `temp/m103-scan`, `temp/m104-ui`, `temp/m104-setup`, `temp/m105-integration`; create only a missing named directory, then assert empty. Existing observed directories are empty. The harness checks all its existing scratch/runtime invariants: managed full Chromium 151.0.7922.34 revision 1234; ordinary `m104-browser-runtime/browsers/chromium-1234/DEPENDENCIES_VALIDATED` with nonfuture mtime less than 30 days old (entry: 2026-09-01T19:46:51.6545663Z); unchanged whole runtime tree/marker; absent `node_modules/.vite-temp`. No marker refresh is authorized.

Preflight may run only read-only inspection and the current pure ENTRY-01 command. It must not run a browser/API suite that creates scratch. Node calls use Invoke-M105Command; validate exit codes before any dependent action. A's initial absent callables are exactly receiveReview in review-api.ts and admitReview in finding-review-admission.ts. Full behavioral test files may initially fail at these absent modules/exports under ADR-0024's first-module exception; blocked assertions are not executed behavior evidence.

```powershell
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/review-contract.test.ts tests/finding-generation-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Read-only preflight baseline failed' }
}
```

**M402-CMD-A.** Test ownership: `tests/finding-review-api.test.ts`, `tests/finding-review-admission.test.ts`, `tests/helpers/m402-review-fixture.ts`. Production ownership: `src/server/local-service/review-api.ts`, `src/server/local-service/loopback-api.ts`, `src/server/service.ts`, `src/client/finding-review-admission.ts`. Main callback is B-owned. New fixtures remain pure/inert on import, reusing M401/M302 controlled proposals; API tests reuse withReviewSandbox('service') for exclusive returned `temp/m401-review-service-<suffix>` roots and cleanup. Any provider factory mock throws if called; no adapter execution. Callback-only HTTP tests own only returned port-0 loopback listeners and close them. A Red/Green uses the first loop; accepted A barrier adds the second loop and independent strict. Each invocation is serial:

```powershell
foreach ($m402Test in @('tests/finding-review-api.test.ts','tests/finding-review-admission.test.ts')) {
  Invoke-M105Command {
    & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 $m402Test
    if ($LASTEXITCODE -ne 0) { throw ('Review slice failed: ' + $m402Test) }
  }
}
foreach ($m402Test in @('tests/finding-generation-api.test.ts','tests/finding-generation-admission.test.ts','tests/review-contract.test.ts','tests/run-contract.test.ts')) {
  Invoke-M105Command {
    & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 $m402Test
    if ($LASTEXITCODE -ne 0) { throw ('Affected regression failed: ' + $m402Test) }
  }
}
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Independent strict TypeScript failed' }
}
```

An expected initial Red ends the first command at its decisive failure; the test owner additionally invokes the second new suite directly with the same flags to establish its independent missing-callable result. Green must execute both suites completely unchanged. Do not run broad regressions after a failed focused boundary.

**M402-CMD-B.** Test ownership: `tests/finding-review-ui.test.ts`, `tests/helpers/m402-review-fixture.ts`, `tests/helpers/m402-review-browser.ts`, `tests/helpers/m104-ui-harness.ts`, `tests/finding-generation-ui.test.ts`. B may extend the fixture without changing accepted A semantics; changed relevant fixture identity requires fresh A evidence. Production ownership: `src/client/App.tsx`, `src/client/main.tsx`, `src/client/review-request.ts`, `src/client/review-form-validation.ts`, `src/client/styles.css`, and `src/client/components/results/ProposalReviewForm.tsx`, `ProposalEditor.tsx`, `ReviewDecision.tsx`, `ProposalContent.tsx`, `ProposalDetail.tsx`, `ResultsSection.tsx`, `ResultDetail.tsx`, `FindingGeneration.tsx`, `resultPresentation.ts`. The optional pure validation owner is justified only by actual substantive mapping. No other source, helper, test or configuration path is writable without primary reconciliation before a new packet.

The fixture owns controlled proposal/outcome values. The browser helper owns only the review integration/manual setup and owned service/browser lifecycle, reusing startHarness/withReviewSandbox rather than a new harness framework. m104 harness extends its bridge with review intent/callback/response and reentrant-accessor probes; it preserves existing generation behavior. The UI suite proves I4–I9 plus controlled I6/I7. Use the existing App/main entries, native controls, axe, keyboard/focus and shared-region assertions; a mock final decision is never disk evidence.

```powershell
Assert-M105EmptyDirectory $m105UiTemp
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/finding-review-ui.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Review UI boundary failed' }
} $m105UiTemp
```

At B's accepted barrier, run the exact same scratch-qualified caller serially for `tests/finding-generation-ui.test.ts`, `tests/finding-guidance-ui.test.ts`, `tests/target-results-ui.test.ts`, plus independent strict above. Do not create screenshots during ordinary runs. The B test owner must cover every required state before Green: three actions, missing confirmation, invalid N/A reason, invalid complete edit/reference, pending, definite refusal, unknown save, repeated/reentrant/stale/unmounted completion, selection/focus, Local/Groq provenance and all applicable profiles; preserve existing non-reviewable cases.

**M402-CMD-INTEGRATION.** The review UI suite includes three independent built-client browser-to-service-to-disk cases, each using an exclusive withReviewSandbox('service') returned sandbox and its own valid pending seed. First validate the synthetic aggregate and create only `path.join(sandbox.runs, syntheticRunId, 'run.json')` using exclusive `wx`; `sandbox.runs` is `<returned-root>/runs`. Next call existing startLocalService with `runRoot: sandbox.runs`, `port: 0`, applicationRevision equal to EXEC-01 HEAD and clientRoot `$m105Build`. Require successful startup and immediately push the returned service into `sandbox.services` for teardown, retaining its returned port. Only then call that service's readRun and require exact seed identity before browser interaction. This controlled seed uses the established M401 fixture boundary, not a production injection route.

Load real built UI/assets from that service. Intercept exactly the three prior-stage POSTs `/api/runs`, `/api/finding-guidance`, `/api/finding-generation` with identity-correlated synthetic scan-only, supported-active-with-canonical-citation-view and pending-proposal envelopes. Exercise ordinary Analyze/Get guidance/Generate controls to populate existing client owners. The final browser pending aggregate must equal the independently seeded service aggregate. Then exercise each final review through **unintercepted** `/api/finding-review`; verify its response, service.readRun and literal disk readback, original/sibling preservation and exact submitted action/content. A separate lost-response case may forward the real review request, wait for its commit, then deliberately withhold/abort the browser response to prove unknown/no second POST and durable truth. No other API, external browser request, scan, retrieval or provider execution is permitted. Close browser/context then service, verify the owned port closed, then exact-root cleanup; aggregate original and cleanup failures without hiding either. Use controlled synthetic provenance labels in all evidence output.

**M402-CMD-BUILD.** The existing generated build has exactly `index.html`, `assets/index-7FW09Cml.js`, `assets/index-CuLtGKH2.css`. Before first rebuild, primary verifies the resolved absolute `dist/client` root, ordinary descendants/no aliases/links, and precisely that file/directory inventory. Remove only that verified root with `Remove-Item -LiteralPath $m105Build -Recurse`, then assert absent. Never enumerate paths into another shell. Subsequent rebuilds compare the exact output inventory recorded from the preceding accepted build before removal; unknown contents stop. Vite may emit only dist/client; no dependency cache/install effects are accepted. Before B Green validation and final integration, rebuild after source changes:

```powershell
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node node_modules/vite/bin/vite.js build --configLoader native
  if ($LASTEXITCODE -ne 0) { throw 'Client build failed' }
}
```

A worker may build only when primary has prepared the absent build and its packet grants this ignored output effect. No worker may remove an existing build. Expected B Red may use the existing prior build to demonstrate absent browser review; final integrated proof requires a build of the final source. Primary records complete build paths/hashes and source identity before accepting browser integration.

**M402-CMD-VISUAL.** After B Green, use one bounded manual path and existing browser 151.0.7922.34: desktop 1366x900, narrow 390x844, actual native 200% zoom on the desktop review path. The helper's only manual entry is:

```powershell
Assert-M105EmptyDirectory $m105UiTemp
Invoke-M105Command {
  & $m105Node tests/helpers/m402-review-browser.ts --manual
  if ($LASTEXITCODE -ne 0) { throw 'Review visual session or cleanup failed' }
} $m105UiTemp
```

It uses startHarness(true), title **M4-02 synthetic review**, a synthetic original proposal populated through controlled prior-stage collaborators, and a controlled review callback that derives exact final content from the captured submitted intent using existing validators. It waits at most 15 minutes or until its page closes, then normal finally teardown. The manual callback is presentation evidence only; the separate integration cases own disk truth. Inspect original, edit inputs, invalid submission, corrected complete edit and saved edited decision. Long valid text, references, reason/note, non-color labels and visible focus must remain readable without unintended horizontal page scroll. Automated cases sample approve/reject. Preserve M2-03's separate carried guidance/detail zoom obligation.

Native Computer Use uses the installed skill and documented `@oai/sky` through `mcp__node_repl__js` (import and window listing succeeded at entry). Read current returned windows, choose uniquely the owned managed-browser title, get its returned window handle, observe, then use one native menu/key action and refresh. Establish browser-menu 200% visibly using native zoom controls, inspect the review path, reset to 100%, then close only that owned page. Record window/browser/source/state/viewport and observed zoom; never substitute CSS zoom, viewport or device scaling. No guessed handle, unrelated browser/window action, external navigation or process termination is authorized. Capture failure follows the skill's bounded refresh/reselection recovery; unverified zoom remains pending.

For useful desktop/narrow review captures only, a separate full review-UI run may use temporary `A11Y_M402_CAPTURE_PROOF=1`, restore its exact previous process value in finally, and allocate an absent exclusive returned `temp/m402-review-proof-<suffix>` leaf. Only synthetic PNGs for original/editor/error/final and a small identity record may be written there; no normal-regression capture or screenshot system. The helper prints the exact proof root, state and source/browser/viewport identities. Primary may retain that exact evidence leaf for review; report it and do not sweep by prefix. Native window screenshots are inspected directly through the tool; no screen-reader speech claim.

```powershell
$m402PriorCapture = [Environment]::GetEnvironmentVariable('A11Y_M402_CAPTURE_PROOF','Process')
try {
  [Environment]::SetEnvironmentVariable('A11Y_M402_CAPTURE_PROOF','1','Process')
  Assert-M105EmptyDirectory $m105UiTemp
  Invoke-M105Command {
    & $m105Node --test --test-timeout=120000 tests/finding-review-ui.test.ts
    if ($LASTEXITCODE -ne 0) { throw 'Review visual capture failed' }
  } $m105UiTemp
} finally {
  [Environment]::SetEnvironmentVariable('A11Y_M402_CAPTURE_PROOF',
    $(if ($null -eq $m402PriorCapture) { [NullString]::Value } else { $m402PriorCapture }), 'Process')
  if ([Environment]::GetEnvironmentVariable('A11Y_M402_CAPTURE_PROOF','Process') -cne $m402PriorCapture) {
    throw 'Review capture environment restoration failed'
  }
}
```

**M402-CMD-REGRESSION.** At C run the complete current README inventory exactly in its maintained sequential groups: twenty-two browser-free suites; finding-generation-api and the two A review suites with experimental module mocks; scan-page/walking-skeleton with scanner scratch; and target-results/finding-guidance/finding-generation/finding-review UI with UI scratch: **31 test files**. The maintained README now includes all three new files; do not append them again. Independent strict and final-source build are required. The README's exact maintained commands control these files; no wildcard suite selection, parallel browser suites or running application service. Both A11Y_M305_CAPTURE_PROOF and A11Y_M402_CAPTURE_PROOF must be absent for ordinary regression. Frozen manifest and thirteen referenced inputs are checked again. Risk-critical reproduction remains required even when other accepted evidence is reused.

**M402-CMD-GUARD.** Stable workflow `M4-02-20260914-01`, task `M4-02`, cycle `M402-A` or `M402-B`; assignments and lease IDs include that cycle, role/phase and attempt. For each preflight/write packet populate all v2 fields, exact source/test/environment fingerprints, the above command/effect selection, allowed files, no allowed directory roots, and forbidden docs/.codex/.agents/corpus/evaluation plus all opposite-owner source/tests. Every Green forbids all tests; Red forbids all src. Only primary invokes the existing Python guard. Start uses repeated --allow-file from the exact slice/role list, --forbid-dir-root for protected roots, phase red/green, configured agent-type, attempt 1 (or existing correction lineage), and owner from its packet. Consume returned JSON/digest directly. Verify exact projection before worker dispatch. After worker stops, close exact lease/digest and require exit0, closed-compliant and already_closed false before accepting any handoff. A replay requires the documented pinned status/no-drift route; nonzero freezes advancement for triage. No budget or lease-ID reset.

```powershell
# Primary supplies these variables directly from the complete current packet.
$m402GuardArgs = @('-B','.codex/leases/lease_guard.py','start',
  '--workflow-id','M4-02-20260914-01','--task-id','M4-02',
  '--cycle-id',$m402Cycle,'--lease-id',$m402LeaseId,'--phase',$m402Phase,
  '--attempt',$m402Attempt,'--owner',$m402Owner,'--agent-type',$m402Role)
if ($m402Attempt -gt 1) { $m402GuardArgs += @('--correction-parent-lease-id',$m402ParentLease) }
foreach ($m402File in $m402AllowedFiles) { $m402GuardArgs += @('--allow-file',$m402File) }
foreach ($m402Root in $m402ForbiddenRoots) { $m402GuardArgs += @('--forbid-dir-root',$m402Root) }
$m402Lease = & python @m402GuardArgs | ConvertFrom-Json
if ($LASTEXITCODE -ne 0 -or -not $m402Lease.contract_digest) { throw 'Lease start did not establish authority' }
$m402Digest = $m402Lease.contract_digest
# After the assigned worker has stopped:
$m402Closed = & python -B .codex/leases/lease_guard.py close --lease-id $m402LeaseId --contract-digest $m402Digest | ConvertFrom-Json
if ($LASTEXITCODE -ne 0 -or $m402Closed.status -ne 'closed-compliant' -or $m402Closed.already_closed) { throw 'No fresh compliant closure' }
```

Start and close are separate primary effects, never run together before a worker turn. No active lease permits primary documentation writes. Guarded workers are not alone in the repository; preserve others' changes and perform no Git write or metadata mutation. Before each start confirm no active.json and inspect Git status; at handoff inspect actual diff, output, accepted test identity and cohesion, not only receipt/report.

**M402-CMD-CLEANUP / DOCS.** Existing withReviewSandbox and m104 harness own their exact returned roots, generated entries, service/browser shutdown and port checks; preserve unexpected or uncertain residue with original errors. No owner data/runs or generic prefix deletion. Primary cleanup checks absolute root containment and ordinary topology before removing only an owned returned leaf; if external state changed or ownership is uncertain, preserve and report. Scanner/UI/setup/integration scratch must return empty, browser runtime/marker unchanged and no .vite-temp. Retain the last accepted dist/client build for developer use and any explicitly identified synthetic proof leaf; these are generated artifacts, not new tracked product data.

Primary documentation between leases updates the existing API/developer inventory, UI extension, plan/roadmap/progress and materially changed indexes. Read each target fully before editing, validate local links/anchors and PowerShell fenced syntax, inspect UTF-8/newline/whitespace and exact suite inventory, compare protected inputs/configuration and run `git diff --check` plus `git diff 303b7985e2a5e8b65f6522e4480e396c5953703d --check`. Untracked code/tests require separate inspection. A different fresh integrated critical review covers I1–I10 and final evidence. Only then pass documentation closure, mark Complete and archive with inbound-link repair. No M4-03 activation or commit/push/publication.

### M402-G-ACCEPT-01 — Accepted contract and command freeze

Primary accepts the different fresh final `critical_research_reviewer` PASS on the complete artifact SHA-256 `f27ee10bc58a8a5c26d6e90460b8673dad11792759b6b862e2a81f6c9aa74199`, with no outstanding findings across I1–I10. M402-G-FINAL-01 was resolved in final-artifact correction cycle 1/2 by binding the exclusive seed and service runRoot to sandbox.runs, registering successful startup immediately for teardown, then performing readback. The other cycle remains unused. The researcher follow-up remains unused; analyst and pre-draft corrections remain consumed. No implementation allowance has been consumed.

The final reviewer independently confirmed the five source blobs, nine parseable PowerShell blocks, 31-file closure inventory, exact artifact and no active lease; current strict/92-test and thirteen-input evidence remains identity-consistent. Its additional maximum-ID samples are compact/fully escaped image 67154/71674, label 67904/75859 and contrast 67586/74041 bytes, below the accepted analytical bound and cap. Primary inspected the actual correction and accepts M402-G-CONTRACT-01, M402-UI-01 and M402-COMMAND-01. This record changes no binding literal. I2–I10 runtime/visual evidence remains pending; M4-02 is In progress.

### M402-A-PACKET-01 — Transport and admission work slice

Milestone Assignment Packet v2, read-only preflight; later write packets retain this capsule and declare fresh lease/identity/evidence fields.

**Identity.** Workflow ID M4-02-20260914-01; roadmap task M4-02; work-slice M402-A; assignment M402-A-PREFLIGHT-01; lease None; phase preflight; attempt 1; correction parent None; role test_worker; owner m402-a-test; guard digest None.

**Work-slice capsule.** Owning ExecPlan is this document. Observable acceptance is M402-G-CONTRACT-01's complete transport and admission paragraphs, I2/I3 and A-owned I6/I7, without implementing B. Exact authority anchors are the selected Context and Orientation table, roadmap M4-02 Verification, SPEC-005/SPEC-008 and HS-010; fixed M4-01 review contract and ADR-0021/0024 apply. Readiness/evaluation freeze is M402-EXEC-01 and G acceptance above. TDD Applicable: new callable parsing/dispatch and request-correlated admission are executable production behavior. Current-state evidence M402-EXEC-01; preflight evidence pending; accepted test boundary None until Red acceptance; test owner m402-a-test.

Relevant paths are exactly M402-CMD-A plus read-only fixed validators/snapshots and established fixtures. Production placement: CREATE review-api.ts for bounded HTTP receipt and result framing; EXTEND loopback-api.ts and service.ts only to wire review to its existing method; CREATE finding-review-admission.ts for detached captured-intent correlation. Direction is router to review receiver to existing service method; client admission to pure shared validators/snapshot only, never filesystem/provider implementation. REUSE_AS_IS existing review/proposal/run validation, writer and snapshot/equal; CREATE only these two cohesive boundary owners. Permitted local structural refactor: behavior-preserving private helper separation within these declared owners; no unrelated route or shared domain refactor.

Non-goals: B UI, domain/persistence changes, provider/scan/retrieval execution, retained owner data, dependencies, generalized transport framework, other tasks and Git writes. Named uncertainties: actual absence/coverage classification and source-test seam fit, resolved by read-only preflight; no open G literal. Risk S3 for irreversible decision integrity, serialization and single dispatch; fresh critical_reviewer after accepted A, with one bounded review correction loop. Binding-field conflict stops for primary reconciliation.

**Write scope.** Allowed files None in preflight. Later Red exact paths: tests/finding-review-api.test.ts, tests/finding-review-admission.test.ts, tests/helpers/m402-review-fixture.ts. Allowed directory roots None. Forbidden files: all other repository files. Forbidden directory roots src, docs, .codex, .agents, corpus, evaluation; no preflight writes anywhere, including caches or scratch. Future Green excludes all tests and uses only M402-CMD-A production paths.

**Validation.** Working directory C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab. Focused command: M402-CMD-PREPARE plus the exact pure preflight command; reuse M402-EXEC-01's 92 passing tests while all five evidence identities match rather than repeating them. Task-level command: M402-CMD-A after Red/Green, then complete M402-CMD-REGRESSION at C; not permitted during preflight. Expected result is an honest EXISTING_AND_COVERED / EXISTING_BUT_UNCOVERED / MISSING / REGRESSION / PARTIAL / CONFLICTING / UNKNOWN classification, exact absent-callable evidence and proposed coherent coverage. Relevant-tree fingerprint and environment are M402-EXEC-01's source/test/configuration hashes and pinned runtime, unchanged before this preflight. Known external side effects and cleanup: None in preflight; future API tests are Non-reusable except their isolated returned sandbox/listener identity and use M402-CMD-A exact teardown. No browser or build effect in A.

**Budget and stopping.** Maximum worker turns: one read-only preflight plus initial Red and attempt 2, with attempt 3 conditional under ADR-0024; maximum corrections one ordinary plus one conditional final; repeated identical decisive failure maximum two without material new evidence; no-diff write handoffs maximum two. Validation cadence: decisive new suite per Red, both unchanged suites per Green, affected boundary and strict after success only. First-module Red applies only if preflight proves the named absent callables and current environment; complete tests must exist before Red and all execute unchanged in Green. Stop for binding changes, unexpected effects, missing prerequisite or exhausted allowance. Workers are not alone in this worktree; preserve all other changes and perform no Git writes/metadata mutations or guard operation.

**Handoff.** Report identity/authority, classification, source evidence, proposed test boundary, exact commands or reused evidence, unexpected state, residual risk, and documentation impact. No files may change during this preflight.

### M402-A-PREFLIGHT-01 — Accepted missing behavior and Red packet

Primary accepts test_worker classification **MISSING**: both receiveReview/review-api.ts and admitReview/finding-review-admission.ts are absent, as are all three proposed test paths; exact source/test/config/runtime identities match EXEC-01. Existing M4-01 service and tests remain reuse inputs rather than coverage of the missing transport/admission. All current dirty paths are the four intentional G documentation updates. No preflight writes or external effects occurred. The 92-test/strict baseline remains reusable; no new behavior was exercised.

The complete M402-A-PACKET-01 capsule remains binding. Red identity is assignment M402-A-RED-01, lease M402-A-RED-01, phase red, attempt 1, parent None, role test_worker, owner m402-a-test; digest is the exact guard start return delivered before writing. Allowed files are only tests/finding-review-api.test.ts, tests/finding-review-admission.test.ts and tests/helpers/m402-review-fixture.ts; allowed directories None; forbidden files all other repository files; forbidden roots src, docs, .codex, .agents, corpus, evaluation. Current accepted test boundary None; current test owner unchanged. TDD is first-module Red over the entire frozen A contract, not a production stub. The two complete suites must independently reach their agreed missing-module failures; blocked behavioral assertions are not runtime proof.

Validation uses M402-CMD-PREPARE and M402-CMD-A's first loop, followed by the second new suite directly with identical flags after the expected first failure. Do not run affected regressions, build, browser or production code changes. The fixture stays pure on import; guarded API execution may use only the already frozen exclusive withReviewSandbox('service') roots, returned port-0 loopback listeners, and exact teardown. All provider factories throw if reached. No actual retrieval/scan/provider/credential call is permitted. Baseline fingerprints are unchanged EXEC-01; runtime evidence after writes is tied to the new test hashes and isolated execution. Initial Red consumes attempt 1 only; ordinary attempt 2 and conditional attempt 3 remain as prescribed. Handoff includes complete touched-path hashes, exact command results, unexecuted coverage, actual responsibility fit and cleanup; primary closes and inspects before acceptance.

### M402-A-RED-ACCEPT-01 — Accepted first-module Red and Green packet

Primary inspected all three actual test files, their corrected maximum/ordered-reference witnesses and cleanup boundaries, and accepts complete first-module Red: ten API and seven admission groups are authored, with all behavioral assertions still unexecuted. The prescribed first loop stops with ERR_MODULE_NOT_FOUND for review-api.ts; the second independent caller fails identically for finding-review-admission.ts. No browser, listener, test sandbox or provider operation started. Authoring corrections before the initial handoff attached the real maximum candidate, validated its context, exercised expiry during JSON parsing, made the ordering witness nontrivial, and preserved original/cleanup failures; no extra write turn was consumed.

M402-A-RED-01 closed freshly at exit 0, status closed-compliant, no drift outside its exact three new paths, receipt `598f70d063ade18677c8e472aa723e4e61a06268a6d9a0c252fcca947242805a`. Guard digest is `168613cb9c7bd941715e82e2867bfd13e44439f2f90f4c5cfebae91953972c39`. Primary verified SHA-256 API `7310f6add486aedfa9cd12f4c9846f1c79472844196847413cd930a20aac4adf`, admission `40deb292100babc07d7398ff010876f2034e80c19d1582670f621f3b01cff018`, fixture `e5d74f01bce733f2c6d5ab517798ebbcd60807734219f991a712ca1467b4bf2c`; this is the accepted immutable Green test boundary. Source/test fingerprint is `9b6bedeb41461dc5af480a595fffc49aa556c231291af9cd5440c87c11f354ab`; configuration/runtime remain EXEC-01. No test correction turn is consumed.

The complete M402-A-PACKET-01 capsule applies to Green with these explicit fields: assignment/lease M402-A-GREEN-01, phase green, attempt 1, correction parent None, role code_worker, owner m402-a-code, digest the exact primary guard start return supplied before writing. Current-state/preflight/accepted-test evidence is EXEC-01, A-PREFLIGHT-01 and A-RED-ACCEPT-01 respectively; current test owner m402-a-test. Allowed files exactly src/server/local-service/review-api.ts, src/server/local-service/loopback-api.ts, src/server/service.ts, src/client/finding-review-admission.ts; allowed directory roots None. Forbidden files all other repository files, explicitly every accepted test; forbidden roots tests, docs, .codex, .agents, corpus, evaluation. Production responsibilities, dependency direction, reuse, bounded creation and permitted refactor remain the declared packet; App/main/UI are B-owned. Cohesion disposition before Green: None.

Use M402-CMD-PREPARE then the complete serial M402-CMD-A: both new suites unchanged, four affected existing suites only after success, then independent strict TypeScript. Expected outcome: every test assertion executes and passes, no skip; no server-domain/persistence modification, new dependency, build or browser effect. API tests may use only the frozen returned sandbox/loopback effects and exact cleanup; mutable boundary evidence records its isolated identity. TDD applies, S3 fresh review remains mandatory, and Green alone does not accept A. Initial Green plus ordinary correction 2 and conditional final correction 3 apply without resetting the Red or review budgets. Stop on an accepted-test defect, binding/path/cohesion change, unexpected effect, unavailable prerequisite or exhausted limit; do not repair tests. Handoff reports exact commands/counts/source hashes, immutable tests, actual cohesion disposition, teardown, residual risk and documentation impact. Workers never mutate Git metadata or guard state.

### M402-A-CORRECTION-01 — Test-witness correction within the existing budget

Green attempt 1 implemented only its four declared owners and ran the prescribed boundary: API 10/10 pass; admission 6/7 pass, with the seventh group failing its independent fixture validation before admission. `:root > changed` is not a valid retained structural locator. Primary confirmed this test defect against the fixed validator and also identified the later same-group retrieval-score witness as invalid because changing the first score from 0.9 to 0.7 violates descending order. Neither finding authorizes changing production validation. All source was inspected: request receipt/framing and client correlation remain cohesive separate owners, and router/service changes only wire review.

M402-A-GREEN-01 closed freshly at exit 0, status closed-compliant, no forbidden/unleased or Git drift, receipt `18fd227997e28246864c0c28b33dcd61326dd7bf63036566fe017c4b0069997c`. API mutable evidence belongs to its isolated PID 41820; exact random ports/roots were not emitted, and the suite's closure/readback assertions passed with no review-service roots remaining. No affected regression/strict/build/browser ran. The original three test hashes remain unchanged at this stop. Primary invalidates their accepted boundary when the correction starts and retains the original failure honestly.

Milestone Assignment Packet v2 inherits the complete A capsule with correction identity: assignment/lease M402-A-RED-02, phase red, attempt 2, parent M402-A-RED-01, role test_worker, owner m402-a-test, digest supplied from primary guard start. Allowed/forbidden scopes exactly match Red 1 (three test files only, no allowed directories; source/docs/.codex/.agents/corpus/evaluation forbidden). Objective is only valid, nontrivial mutation witnesses preserving the original rejection contract; use a valid changed structural locator and a changed first score that retains ranking. Confirm the rest of that candidate group remains independently valid before acceptance. No production or G literal changes. Readiness and risk remain unchanged; evidence is A-CORRECTION-01 and the previously accepted preflight.

Run PREPARE and the same two-suite CMD-A first loop against the existing implementation. The corrected assertions may now pass; report this as corrected-test execution, never fabricate another behavioral Red. After both pass, run the already frozen independent strict command to identify test typing issues before reacceptance; this bounded diagnostic adds no side effect or command literal. Failed validation stops for primary triage. Allowed API scratch/listeners and cleanup remain unchanged. This consumes ordinary Red correction 2; conditional final Red 3 remains only under the prescribed material-progress justification. Green 1 is consumed, with ordinary Green 2 and conditional 3 remaining; review correction allowance is unused. Handoff supplies changed hashes, all cases/results, strict result, exact cleanup and remaining risk. Primary closes/reaccepts before a new Green turn.

### M402-A-CORRECTION-02 — Conditional final test correction

Red attempt 2 closed fresh and compliant with receipt `b8e8e4dee47000ac3f638be34302a38c04bdb5c62f321c72cbf72d9eeca9697e`. Only the two invalid admission mutation witnesses changed; the revised admission test hash is `503579d1553ffecf8c4ae28f79c55ec769e88a3e012e856f7dea59e3a61eeead`. All 10 API and 7 admission tests now execute and pass. Independent strict TypeScript then reports exactly TS2683 at `tests/finding-review-api.test.ts:179`: the timeout callback's `this` lacks its `http.ClientRequest` type. No production path changed. The test process retained no loopback listener or review-service scratch root.

This is material new evidence: the corrected behavioral witnesses pass, and strict checking isolates one erased callback annotation in a health-check helper. Inspection of the affected test/helper family finds this exact untyped timeout callback only here. The different bounded action is adding the explicit `this: http.ClientRequest` annotation; it changes no behavior or assertion and is likely to resolve the sole remaining strict diagnostic. Conditional final Red attempt 3 is authorized under the existing correction policy, with the unchanged M402-A contract, objective, authority, dependencies, expected outcome, test_worker/red/standard route and exact three-file test scope. Parent is terminal `M402-A-RED-02`; no attempt 4 or budget reset is authorized.

The complete M402-A-PACKET-01 and A-CORRECTION-01 bindings remain applicable. Assignment M402-A-RED-03 changes only the typing described above, preserves the two accepted mutation corrections and all production files, and runs PREPARE, the same two focused suites, then independent strict TypeScript. Report corrected-test execution honestly; no new behavioral Red is claimed. Every changed test evidence identity is invalidated and must be accepted afresh before Green resumes. Existing API test-owned listener/scratch and cleanup rules remain fixed. Failure stops for primary triage; no further write turn in this Red chain remains. Green 2 and conditional Green 3 remain, and the review correction loop is unused. Primary opens and terminally closes the fresh lease before evidence acceptance or documentation maintenance.

### M402-A-TEST-ACCEPT-02 — Revised test boundary and Green continuation

Primary inspected the bounded annotation and all revised witnesses and accepts the corrected test boundary. Red 3 closed freshly and compliantly with receipt `60573f199488ec364376ff05c96dd1f1b96e0fa338ce9a5a2d057e9c8d3da917`; only the API test changed. Final API/admission/fixture SHA-256 values are `3b738dfce2dbf6c01f1e002c416009274108daa5b2fafd29431361ce549da236`, `503579d1553ffecf8c4ae28f79c55ec769e88a3e012e856f7dea59e3a61eeead`, and `e5d74f01bce733f2c6d5ab517798ebbcd60807734219f991a712ca1467b4bf2c`. Source/test fingerprint is `6939edee48a5b8bf33bd976e06a02766e18b8a44595cc092e494a74955ac95c5`; runtime/configuration remain EXEC-01. Exact focused commands pass all 17 tests without skips; independent strict checking exits 0. Isolated API invocation PID 25776 leaves no owned listener or review-service root. These are corrected-test passing results; the missing-module Red remains historical evidence only. Red's final conditional attempt is consumed; no further Red write turn is authorized.

Complete M402-A-PACKET-01 and the Green fields of A-RED-ACCEPT-01 apply with assignment/lease M402-A-GREEN-02, green attempt 2, parent terminal M402-A-GREEN-01, code_worker owner m402-a-code. The exact four source paths and forbidden roots remain unchanged. This revised test boundary is immutable during Green. Baseline is the current fingerprint and unchanged four source hashes from Green 1; current/preflight/test evidence is EXEC-01, A-PREFLIGHT-01, A-TEST-ACCEPT-02. No production change is known necessary. Reuse fresh pure admission/strict results when complete identity matches; repeat the API boundary under a newly isolated invocation, then run all four frozen affected regressions. Changed production invalidates affected evidence and requires both focused suites and strict anew. No artificial edit is required to produce a diff. Inspect actual structural fit and return RETAINED, REFACTORED or RECONCILE with path/symbol evidence. All existing command, cleanup, source-only write and stopping rules remain binding. Green 2 consumes the ordinary correction; conditional Green 3 and the single review correction loop remain only under their existing conditions.

### M402-A-GREEN-ACCEPT-01 — Behavioral and structural acceptance for review

Green 2 closed freshly and compliantly with no changed path, receipt `df3c3c36b04f94a6395563a225c24a0079f482b33b00628094f91211e848f9b9`. Primary inspected the complete four-path production change and revised test boundary; the source/test fingerprint remains `6939edee48a5b8bf33bd976e06a02766e18b8a44595cc092e494a74955ac95c5`. The four source hashes from Green 1 and three accepted test hashes remain unchanged. This is the first no-diff write handoff; no artificial source change was made.

Fresh serial CMD-A execution passes review API 10, generation API 5, generation admission 10, review contract 9 and run contract 73: 107 tests without failures/skips. Corrected review admission 7 and independent strict exit 0 are reused under the complete matching command/cwd/tree/environment/guard identity. API invocation PID 12616 passes owned listener/port closure and sandbox removal; random port/root values were not emitted and no review-service root remains. No build, browser, provider or retained-owner-data effect occurred. `git diff --check` passes.

Primary separately accepts behavioral Green and RETAINED cohesion: receiveReview/frameOutcome owns bounded HTTP receipt/framing; router and service only wire the existing review operation; admitReview owns pure detached captured-intent correlation through existing validators/snapshot. No material responsibility mixing or refactor is present. A is not yet complete: fresh critical_reviewer must inspect I2/I3 and applicable I6/I7, actual code and test relevance, protected semantics and integrity-critical evidence. One review correction loop remains; Red attempts are exhausted, Green conditional attempt 3 remains only with the required justification. B waits for primary acceptance of that review.

### M402-B-PACKET-01 — Accessible interaction work-slice packet

Milestone Assignment Packet v2. Primary accepts this frontend-visual capsule/reuse audit and authorizes read-only preflight after M402-A-ACCEPT-01. Preparation creates no B execution evidence.

**Identity.** Workflow M4-02-20260914-01; roadmap task M4-02; work-slice M402-B; assignment M402-B-PREFLIGHT-01; phase preflight; attempt 1; lease None; correction parent None; role test_worker; owner m402-b-test; guard digest None.

**Work-slice capsule.** Owning ExecPlan is this document. Observable acceptance is the complete frozen M402-G-CONTRACT-01 request-ownership, form/editor and final-presentation contract, I4–I9 and B's I6/I7/I10 integration obligations. Exact requirement/ADR/SPEC/HS and roadmap Verification anchors are in Context and Orientation and roadmap M4-02; REQ-REV-001/008/009, REQ-UX-002/011, REQ-A11Y-001–004/006/009/010, SPEC-005/008, HS-010 and ADR-0012/0021/0024 apply. Readiness/evaluation freeze: EXEC-01, G-ACCEPT-01 and required A-ACCEPT-01. TDD Applicable: currently absent observable review interaction requires browser behavior tests before production. Current-state evidence is A-ACCEPT-01 and the accepted reuse outline; preflight evidence None; accepted test boundary None; test owner m402-b-test.

Relevant boundaries and exact paths are M402-CMD-B, existing domain validators, M104/M305 browser fixtures and M401 sandbox. Responsibility placement/reuse is M402-UI-01: App owns shared run/selection/reservation/announcements; main owns the actual same-origin callback; review-request owns capture/invocation/deadline/abort/admission/retention; ProposalReviewForm owns transient draft/confirmation/errors; ProposalEditor owns complete plain fields/references; ReviewDecision owns immutable saved human content. ResultsSection/ResultDetail compose those regions; ProposalDetail distinguishes the original; EXTRACT_LOCAL ProposalContent serves the actual original/reviewer consumers. FindingGeneration and resultPresentation give final durable state precedence while retaining actual invocation. Styles extend existing native controls and restrained palette. Optional review-form-validation owns substantive pure candidate/error mapping only when needed. REUSE_AS_IS existing validators, snapshots, evidence, FindingsPanel, citations and shared status. Dependency direction is browser callback to same-origin route, request owner to A's pure admission/validators, App through result composition to form/editor/decision; no browser dependency on filesystem/provider implementation. Permitted structural refactor is behavior-preserving separation within these declared owners; no generic layer or unrelated refactor.

Non-goals are the task's Scope and Non-Goals, especially actual scans/retrieval/provider calls, credentials, acquisition, owner-run mutation, history/reopening, automatic retry, bulk review, new dependencies and M4-03/M3-03 execution. Named uncertainties: actual coverage classification, fixture/callback seam sufficiency and current responsibility fit; no unresolved G literal. Risk S3: irreversible finality, reentrancy, request ownership and uncertain save. Fresh critical_reviewer includes visual review after primary accepts B evidence; one review correction loop. Binding conflicts stop for primary reconciliation.

**Frontend-visual capsule.** Profile frontend-visual; skill [.agents/skills/frontend-quality/SKILL.md](../../../.agents/skills/frontend-quality/SKILL.md). Exact UI anchors: [selected evidence](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#selected-evidence), [M3-05 extension](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m3-05-selected-finding-generation), [responsive behavior](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#responsive-behavior), [accessibility behavior](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#accessibility-behavior), [visual foundations](../../ui/VISUAL_FOUNDATIONS.md), and routed Accepted requirements above. Reuse-audit evidence M402-UI-01/G-ACCEPT-01 is primary-accepted; exact dispositions are the responsibility paragraph and G's accepted outline.

Required states: pending proposal; unselected action; all three actions; missing/reset confirmation; unresolved/contradictory judgment and invalid N/A reason; invalid complete edited fields/references and opaque validator refusal; pending operation; releasable exact-baseline refusal; retained definite refusal; unknown save; three saved decisions with distinct immutable original/reviewer content. Cover repeated activation, callback access/reflection, unmount/late/deadline, sibling selection/focus and discarded drafts. Preserve non-reviewable abstention, generation failure/invalid output, ScannerReviewObservation, parent running/failed, complete-zero and scan-only behavior. Exercise Local/Groq provenance and all three supported profiles without a cross-product support matrix. Preserve generation-unknown/new-independent-Analyze while review ownership blocks Analyze and all mutations.

Viewport/interaction matrix is the exact bounded Visual and Interaction Samples: automated axe, native keyboard, related errors, shared announcement semantics, selected-list focus and conditional saved-heading focus; desktop 1366×900, narrow 390×844, and primary-owned genuine native 200% desktop zoom. Browser target is existing M104 App/main harness and three independent final-source built-client→service→disk cases under M402-CMD-INTEGRATION. Source/configuration/runtime identities start from A acceptance, browser 151.0.7922.34/revision 1234 and controlled synthetic fixture IDs. Ordinary tests make no screenshots; optional explicit capture/manual effects are frozen in CMD-VISUAL. No screen-reader speech, general support/capacity or actual human-review claim. Prohibit new theme, animation, design system, rich text, dependencies, fields, queues, history, retry or automatic-remediation copy.

**Write scope.** Preflight allowed files None; allowed directory roots None; forbidden files all repository files; forbidden directory roots src, tests, docs, .codex, .agents, corpus, evaluation. No writes/caches/scratch or application launch in preflight. Future Red exact five test paths and Green exact fourteen production paths are M402-CMD-B; no allowed directory roots. Future Red forbids source/docs/authorities; Green forbids all tests/docs/authorities. Current A tests/semantics remain protected; a relevant fixture change requires fresh affected A evidence.

**Validation.** Cwd C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab. Focused caller PREPARE plus CMD-B; not executed in read-only preflight because it owns mutable browser/scratch effects. Use source/fixture inspection and fresh A evidence to classify honestly. Expected result is one prescribed coverage classification with exact symbols/gaps and coherent test proposal, never absence-search alone. Task-level caller is CMD-B affected UI suites and strict/build at B, complete CMD-REGRESSION at C. Relevant-tree fingerprint is A's accepted current fingerprint before B writes; configuration/Node/PowerShell remain EXEC-01. Browser/service evidence is Non-reusable except its pinned isolated state and returned ownership identity. Preflight external effects/cleanup None. Future browser/helper effects are only frozen CMD-B/INTEGRATION/VISUAL with serial owned teardown, exact scratch and unchanged retained browser runtime. Existing build is permitted for expected Red only; primary must prepare absent output before any worker final-source build.

**Budget/stopping.** One preflight; one coherent Red and one Green with one ordinary correction each and conditional final attempt 3 under the existing material-progress rule. No budget reset, at most two identical decisive failures without material new evidence, at most two no-diff write handoffs, and one review correction loop. Red authors the full bounded browser contract before Green; expected absent review controls are behavioral Red, not a need for production stubs. No source change during Red; no accepted test change during Green. Focused pass precedes broader checks. Stop for changed bindings, unavailable prerequisites, wrong Red, unexpected effect, test defect, responsibility mismatch or exhausted allowance. Every future write turn requires a fresh primary-opened/closed lease; workers preserve peer changes and perform no Git/guard metadata mutation.

**Handoff.** Return assignment/authority and classification, exact source/fixture evidence, proposed coherent coverage, commands/evidence reused, unexpected state, residual risk and documentation impact. Writes remain prohibited in preflight. Later Green additionally supplies exact test/build/browser identities, cleanup and RETAINED/REFACTORED/RECONCILE cohesion evidence.

### M402-B-PREFLIGHT-01 — Missing interaction and initial Red packet

Primary accepts MISSING from read-only preflight: App/main has no review callback or operation owner, ResultDetail hides terminal reviewed proposals, ProposalDetail always says pending, cached generation state overrides final status, and the M104 bridge has no review callback/accessor seam. Existing generation tests prove only the earlier stage. No preflight effects or writes occurred. One invalid Windows glob search was replaced by exact reads and supplied no evidence. Source/test/configuration and A evidence remain unchanged; the expected concurrent README edit documents accepted A only.

Primary corrects one overstatement in the handoff: current M402 pending/success aggregate helpers support modes/actions but their underlying M302 aggregate is image-alt-only. M401/M302 provide canonical primitives for all three profiles. The already authorized M402 fixture extension must compose validated scan/guidance/pending aggregates for label/contrast, preserve existing A helper semantics and verify each prior-stage admission before relying on browser Red. This is the declared fixture responsibility, not new scope or a new preflight. Three independent final-action integration cases may cover image-alt/Local approve, label/Groq edit-and-accept and contrast reject; the proposed fourth forwarded/withheld response case is within G's optional synthetic lost-response boundary. No actual prior-stage execution is permitted.

Complete B-PACKET-01 applies to initial Red: assignment/lease M402-B-RED-01, phase red, attempt 1, parent None, test_worker owner m402-b-test, digest supplied by the primary guard start. Allowed files exactly tests/finding-review-ui.test.ts, tests/helpers/m402-review-fixture.ts, tests/helpers/m402-review-browser.ts, tests/helpers/m104-ui-harness.ts and tests/finding-generation-ui.test.ts; allowed directories None; all other files forbidden, with src/docs/.codex/.agents/corpus/evaluation forbidden roots. Baseline fingerprint remains `6939edee48a5b8bf33bd976e06a02766e18b8a44595cc092e494a74955ac95c5`, configuration/runtime EXEC-01, expected dirty documentation includes README. No accepted B test boundary yet; A source/tests remain protected except the declared additive fixture extension, which invalidates relevant A evidence until rerun.

Author the complete bounded I4–I9/I10 test contract before handing off, using only declared owners, exact frozen callbacks/HTTP and native controls. Preserve existing generation cases, adding its review-unknown versus generation-unknown Analyze distinction in the allowed existing suite as needed. No import of absent new production owners or implementation fallback may replace observable browser Red. The helper's manual path and optional capture branch are authored but not executed during Red; preserve normal no-screenshot behavior.

Validation sequence: PREPARE; independent strict TypeScript as the already frozen test-authoring diagnostic; then CMD-B with UI scratch to establish ordinary missing-control/presentation Red. Strict failure stops rather than being called Red. Every case must be authored without skip/conditional absence bypass; expected browser failures may prevent later assertions from executing and must be reported honestly. Existing prior build may establish missing built-client controls; no build removal/build execution, manual session or capture effect is authorized during Red. Browser/service/scratch effects are only CMD-B/INTEGRATION with exclusive synthetic roots, registered services and exact error-preserving cleanup. No broad regressions after failed focused Red. Initial attempt 1, ordinary correction 2 and conditional final 3 apply; stop for test/runtime/fixture/binding defects or unexpected effects. Handoff gives complete case coverage, actual failures versus blocked assertions, strict result, changed hashes, browser/source/build identities and exact cleanup. Primary inspects and terminally closes before accepting Red or issuing Green.

### M402-B-CORRECTION-01 — Complete and correct the browser test boundary

B Red 1 is **RED INVALID**. Its freshly compliant terminal receipt is `d7ca36cb7a54a3b772921e3bd1ddded2991d46c77ff2acbc39322e1a321b42eb`; four allowed test/helper paths changed, no source or forbidden path changed, and no active lease remains. Source/test fingerprint is `3cd47bece7487cbe8f73bb89ddd1c17785cea08482169fb5a4a1036fcc3044cd`. The worker ran initial strict exit 0, then three authoring strict failures with five, one and two diagnostics respectively, corrected them inside the turn, then strict exit 0 and CMD-B exit 1. Continuing edits after the first strict failure did not follow PREFLIGHT-01's explicit stop rule. This deviation is recorded, not retrospectively authorized or treated as valid Red; the next lease must stop on its first failed diagnostic. It does not erase or reset the consumed attempt.

CMD-B reports 15 tests: two passing, thirteen failing, no skips. Four built-client cases reached expected missing review radios after their valid synthetic prior stages. Nine App-harness cases instead failed at an out-of-context `reviewAccessor` reference in `openReview`'s serialized callback; their UI assertions did not execute. The duplicate scan-only fixture does not establish the distinct negative branches, and the disabled optional-capture gate is not screenshot evidence. Primary inspected the actual tests/helpers and identified incomplete contract witnesses below. Existing build and protected source remain unchanged; UI scratch is empty, no review-service root remains, and `.vite-temp` is absent. Changes to the shared review fixture invalidate relevant A fixture evidence until a matching rerun.

**Complete correction packet.** M402-B-PACKET-01 and M402-B-PREFLIGHT-01 remain the complete v2 capsule, frontend overlay, authority, dependency, objective, expected outcome, command/effect and exact path contract. Assignment/lease `M402-B-RED-02`, phase red, attempt 2, parent terminal `M402-B-RED-01`, test_worker owner `m402-b-test`; guard digest is inserted from the fresh start response in the assignment before writing. Accepted test boundary remains None. Current evidence is this record and B-PREFLIGHT-01; baseline is the fingerprint above, unchanged EXEC-01 configuration/runtime and the four hashes in the Red 1 terminal handoff. Allowed files remain exactly the five B test paths; allowed directory roots and forbidden individual files are None; source, docs, .codex, .agents, corpus and evaluation roots remain forbidden. No application responsibility changes; cohesion disposition None. All non-goals, S3 review, maximum repeated/no-diff failures and cleanup obligations remain fixed.

The coherent correction is a faithful, complete browser test boundary for the unchanged G interaction contract. Transfer every serialized callback argument explicitly and inspect the same assumption across both browser callers. Complete independent submitted-intent assertions; all complete-editor fields/reference groups, raw-text/omission mapping and confirmation-reset categories; actual authoritative opaque validation plus inline relationships/focus; safe local/exact-baseline retry versus retained/null-baseline/cleanup uncertainty; synchronous mutation guards, accessor/admission reflection, monotonic deadline, pending unmount and late completion; selected/sibling focus and stale durable labels; meaningful keyboard, visible-focus, long-content/reflow and axe samples; and distinct existing non-reviewable branches. Preserve generation-unknown allowing a new independent Analyze while review uncertainty blocks mutations. Avoid a Cartesian test matrix or production implementation assumptions beyond frozen accessible names and observable behavior.

Correct the built-client caller so normal review POSTs use the real browser request/response without response substitution; only the explicit lost-response case may fetch then withhold. Assert exact prior-stage requests, one final POST, response/service/literal-disk agreement, original/sibling preservation and no unexpected API/external request. Keep bounded existing timeouts and error-preserving teardown. Correct the optional capture branch to its exclusive suffixed owned leaf and real original/editor/error/final states with small source/browser/state identity. Correct the manual owner to the exact synthetic title, desktop viewport, existing-validator-derived final callback, ready state, bounded lifecycle and aggregated cleanup errors. Manual/capture execution remains unauthorized during Red; only their implementation is in this existing test scope.

Perform all authoring and static callback/fixture inspection before the frozen validation sequence: PREPARE, independent strict TypeScript, then CMD-B. The first failed strict diagnostic or wrong runtime Red stops writes and returns control to primary; do not run-edit-run through it. Expected absent review controls/presentation are ordinary Red and may block later assertions, which must remain authored and honestly reported. No build, broad regression, source edit, acquisition or manual session. This consumes ordinary correction 2; conditional attempt 3 requires the recorded material-progress gate, never confidence alone. Handoff includes exact command order/results, changed hashes, contract coverage versus blocked assertions, cleanup and any deviation. Primary must close, inspect and accept before Green.

### M402-B-CORRECTION-02 — Conditional final test correction

Red 2 closed freshly and compliantly, receipt `6e1b4d31ac04a1903bd1b3c3788cd00eb25c72ce5c76559e47a1c898e88040c5`, with exactly three changed paths: UI suite, browser helper and M104 harness. The worker's four-path wording included the unchanged shared fixture; primary checked the actual receipt/diff. Source/test fingerprint is `0de3ee877a9db4b416c8227dea5ed36b56647ceacd4020db2fcb3ce21c65f02f`. Strict exit 1 reports nine test diagnostics, and the worker correctly stopped without further edits or CMD-B. No browser, build or service ran. The empty browser marker is a zero-byte age marker, not text to trim; its original mtime remains unchanged. A nonterminating read-only `.Trim()` diagnostic supplied no evidence and caused no mutation.

Material progress since Red 1 is the explicit callback transfer, preserved accessor component identity, normal real POST response observation, independent exact-intent assertions, real pending-unmount/admission-reflection cases, local release and retained uncertainty cases, distinct negative fixtures, and long-content/axe samples. Strict now identifies concrete return-type and union-narrowing errors rather than the former runtime closure defect. Primary inspection additionally isolates test-oracle issues before Green: long-content success must preserve the same long original baseline, multiline text assertions must not assume one line, and optional visual evidence must capture its actual region/viewport/source. No production behavior changed and no browser result is claimed.

Conditional final attempt 3 is authorized under the unchanged B test contract and existing budget. The different bounded action resolves the nine exact diagnostics using void-returning async adapters, explicitly narrowed/local edited candidates, pending-Finding narrowing and a stable nonoptional page reference; it also finishes the already required faithful fixture/assertion/capture mappings. This is likely to resolve the isolated strict failures and expose ordinary absent-control Red without a new framework or scope. It is not another hypothesis about the original ReferenceError. No attempt 4 or renewed Red chain is authorized.

**Complete packet.** Inherit every v2 capsule, frontend overlay, authority, dependency, command/effect, path, risk, expected outcome, non-goal and stopping field from B-PACKET-01/PREFLIGHT-01/CORRECTION-01. Assignment/lease M402-B-RED-03; red attempt 3; parent terminal M402-B-RED-02; test_worker owner m402-b-test; digest supplied from primary guard start. Current/preflight evidence is this record and B-PREFLIGHT-01; accepted B tests None; baseline is the fingerprint above with unchanged EXEC-01 runtime/configuration and Red 2's recorded five test hashes. Exact same five allowed files, no allowed directory roots, same forbidden roots. Cohesion None: no application responsibility change. Maximum remaining Red write turns one, further corrections zero; existing repeated-failure/no-diff limits and one future B review correction remain unchanged.

Before validation, complete these concrete assertions within the same contract: use the exact long baseline when projecting a successful edited run and assert its actual original/edited reminders; use multiline-safe independent metadata assertions; exercise required empty-reference errors, all five assumption bounds, raw N/A reason, action reset and exact-baseline versus null-baseline validation refusal. Add the bounded generation-unknown/new-independent-Analyze regression in its already allowed existing suite. Verify keyboard form submission and visible focus in the bounded visual case. Restore per-case callback/clock/pending state so setup cannot inherit a prior case's traps. For integration, use the combobox role to distinguish human judgment from the editor field, handle the response promise immediately, assert prior-stage counts/no unexpected API call and equality of admitted response, service and disk. Complete actual source identity, region scrolling, desktop/narrow samples, exact proof-root output and ordinary existing-parent validation in optional capture; do not execute it during Red. The shared successful fixture must validate submitted intent with native context and preserve its caller's exact baseline. Existing parent-state regressions remain reusable and need no copied matrix.

Sequence remains PREPARE, independent strict, then CMD-B only if strict passes. Stop writes immediately on the first failed strict or wrong runtime/fixture failure; report ordinary missing-control/presentation Red honestly and never skip blocked assertions. No broad regression, build, source, manual/capture, acquisition or owner-data effect. Handoff supplies exact command history, actual versus blocked coverage, final hashes and cleanup. Primary closes and inspects before accepting or escalating an exhausted allowance.

### M402-B-STOP-01 — Invalid fixture and exhausted test-correction budget

Red 3 closed freshly and compliantly with receipt `7098ab77392b3021fb27b5dac205580909cae1a2b29ad84924fdaadd472e2980`; exactly the five authorized test paths changed, no source/authority/Git drift occurred, and no active lease remains. Source/test fingerprint is `a4adac3296b40f946558d5f7b0b8efe2da11688939c368f25b1139a987a1d8af`. Final UI/fixture/browser/harness/generation-UI hashes respectively are `eba58dc15f2eb2aa7ce2ba27a495d492544ddeb0942a525f8a19853d730358fe`, `2256609f1063f2e02839b92105e37a18f5a75c2a9f78eb2c203918a46b28e094`, `3a48abc0950aab240f30229085ba3827c25f7d240e7f2102a3586b366ee084f4`, `7c56c9bf1ee52c91e0b29085a74222ef9e3d31277965770a8a974173906406bb`, and `e0e9cd02c7fa8d279928b5a3ac402b02a896cb534d1c0020d30386a6fa88a498`.

PREPARE and independent strict exit 0. CMD-B exits 1 after 88.15 seconds: eighteen tests, one pass, seventeen failures, no skips/cancellations. Twelve App cases and four independent built-client cases reach expected missing review controls after validated synthetic prior stages. Their post-control assertions remain unexecuted, including final POST/commit/readback. The optional capture flag is absent and produces no evidence. The negative-state test instead fails during construction of `failedGenerationRun('response-validation', ...)`, before its branch assertions. Therefore Red is invalid, not accepted on the strength of the sixteen expected failures.

Primary confirms the cause from `readGenerationFinding`: response-validation requires a response invocation with failed validation; the fixture omits invocation entirely. The exact proposed correction is to import the existing `generationInvocation` helper in `tests/finding-review-ui.test.ts` and add `invocation: generationInvocation('local', 'response', 'failed')` to that one failed-generation fixture. Preserve the response-validation scenario and every assertion; changing it to a different failure would weaken the intended witness. No production change is indicated. The bug-index rule keeps this routine task-contained fixture correction in this plan rather than creating a new bug record.

All Red attempts are consumed. The [correction procedure](../../../.codex/execplan-implementation-workflow.md#corrections-and-exceptions) states that an unsuccessful attempt 3 stops automatic continuation and cannot be reset with a new agent or ID. Primary therefore requests an owner decision for only this bounded direct primary test correction under the documented exception, followed by fresh strict/CMD-B and test-boundary reacceptance before the untouched delegated Green budget. This is a proposed exception, not permission already exercised or a fourth guarded attempt.

Unaffected work before owner handoff is refreshing the two accepted A suites because the shared fixture identity changed, and checking the proposed fixture entirely in memory. The latter uses PREPARE and this exact pure diagnostic; it writes no file, starts no service/browser and performs no provider operation:

```powershell
$m402FixtureDiagnostic = @'
import assert from 'node:assert/strict';
import { failedGenerationRun, generationInvocation } from './tests/helpers/m302-generation-fixture.ts';
import { validateRun } from './src/server/domain/run-contract.ts';
const original = failedGenerationRun('response-validation', { runId: 'review-generation-failed' });
const corrected = failedGenerationRun('response-validation', { runId: 'review-generation-failed', invocation: generationInvocation('local', 'response', 'failed') });
assert.equal(validateRun(original).ok, false);
assert.equal(validateRun(corrected).ok, true);
console.log('Original fixture rejected; proposed invocation-complete fixture accepted.');
'@
Invoke-M105Command {
  & $m105Node --input-type=module -e $m402FixtureDiagnostic
  if ($LASTEXITCODE -ne 0) { throw 'Fixture diagnostic failed' }
}
```

Browser identity is Chromium 151.0.7922.34 with emitted source hashes, no page errors/external requests and successful teardown. All four named scratch directories are empty; no review-service root or `.vite-temp` remains. The original three-file build is unchanged. No manual/capture/final-source build/full-suite/UI review occurred. The future frontend worker completed read-only responsibility preparation with no structural conflict or writes; that is not Green or a review verdict. M4-03/M3-03 and all protected evaluation inputs remain independent.

Primary executes the frozen in-memory diagnostic at exit 0: the original fixture is rejected and the invocation-complete candidate is accepted. The proposed change has not been applied. Fresh serial API 10/10 (isolated PID 43480) and admission 7/7 pass with the current extended fixture, restoring A's affected evidence identity; owned API listener/disk teardown passes. Independent strict exit 0 remains current because no source/test changed after Red 3. The owner decision concerns only the narrow primary fixture correction and its fresh strict/CMD-B acceptance; it does not renew a worker attempt chain or authorize any other task.

Handoff documentation reconciles the current blocked status in the roadmap, plan/index, progress/index and project overview while preserving historical checkpoints and G/A acceptance. Six-document checks pass 370 local links/anchors, UTF-8/no BOM, final newlines and trailing whitespace; sixteen README/plan PowerShell fences parse. Seven untracked source/test files pass separate formatting inspection, `git diff --check` passes, and the frozen manifest plus thirteen references remain byte-identical. Initial audit callers incorrectly included template links and embedded fence literals; excluding fenced template prose and anchoring actual code fences resolves those audit errors without changing repository content. Source/test fingerprint remains the Red 3 value; HEAD remains EXEC-01. This is verified handoff documentation and cleanup, not B/C acceptance, a current full-suite pass or task closure. No plan archive, commit, push or publication occurs.

### M402-B-EXCEPTION-01 — Owner-authorized fixture correction

The owner explicitly authorizes the proposed narrow primary test-correction exception and continuation. Before editing, primary confirms no active lease and preserves the STOP-01 source/test baseline and unrelated work. Only `tests/finding-review-ui.test.ts` changes: import existing generationInvocation and attach its Local response/failed-validation record to the one response-validation fixture. No assertion, scenario, helper, source or dependency changes. This is the documented direct primary exception, not a new worker attempt or reset of exhausted Red allowances. Prior B test evidence is invalidated; PREPARE, independent strict and the complete CMD-B must run freshly before primary reacceptance and delegated Green. Existing G/A acceptance, command/effect restrictions and untouched B Green/review budgets remain fixed. Roadmap/current-state mirrors return to In progress when this approved correction is accepted.

### M402-B-TEST-ACCEPT-01 — Corrected browser test boundary accepted

The owner-approved EXCEPTION-01 changes only the failed-generation fixture's required invocation and its existing-helper import. Fresh PREPARE and independent strict TypeScript exit 0. Complete CMD-B exits 1 in 88.50 seconds: eighteen tests, two passing, sixteen expected missing-control failures, no skips/cancellations. The distinct negative states now pass. All twelve App and four built-client cases reach the absent review controls; later assertions and actual final POST/readback remain blocked until Green. The disabled capture branch produces no visual evidence. Primary inspects every failure and accepts ordinary behavioral Red, with the complete unchanged assertions frozen for Green.

Current source/test fingerprint is `b0d041999d0c24956823c47020d0be414335eeec8ccdb9cbd20e550da59f6f71`; UI test SHA-256 is `ac5ec3179830062248b760cb39f441d402fc7108bbfa94f0435ed9cb06879a88`. Other four B test hashes remain STOP-01, as do configuration/runtime and accepted A production. Fresh A 17/17 from STOP-01 remains applicable because the primary correction touches no A dependency. Chromium 151.0.7922.34 has no page errors or external requests; teardown settles, four scratch roots are empty, no review-service root or .vite-temp remains. No active lease exists. The primary verifies the exact original three-file/one-directory ordinary build inventory and removes only the resolved dist/client root under CMD-BUILD; output is absent for the first Green build. No other generated or retained data is removed.

### M402-B-GREEN-01 — Frontend implementation assignment

Complete Milestone Assignment Packet v2 is M402-B-PACKET-01 with these phase-specific values; all governing authority, observable contract, responsibility placement, dependency direction, accepted frontend capsule, non-goals, risk and stopping fields remain fixed. Workflow M4-02-20260914-01; task M4-02; slice M402-B; assignment and lease M402-B-GREEN-01; phase green; attempt 1; correction parent None; role frontend_code_worker; owner m402-b-code. The primary inserts the guard's returned digest before dispatch. Readiness is EXEC-01/G-ACCEPT-01/A-ACCEPT-01; preflight M402-B-PREFLIGHT-01; accepted test boundary is all five CMD-B test paths plus the protected A tests, owned by m402-b-test and accepted in TEST-ACCEPT-01. TDD Applicable. Current relevant-tree and environment identities are TEST-ACCEPT-01. Named uncertainty is actual implementation fit and runtime result; no open literal or test-authoring permission remains.

Allowed files are exactly the fourteen CMD-B production paths; allowed directory roots None; forbidden individual files None; forbidden roots tests, docs, .codex, .agents, corpus, evaluation. Preserve every peer change; no Git or guard mutation. The implementation worker's prior read-only responsibility outline fits the accepted named owners and is accepted as preparation, not implementation evidence. Reach the full frozen interaction contract without changing tests or authoritative validators.

Validation: cwd and PREPARE/CMD-BUILD/CMD-B are M402-COMMAND-01. Primary has prepared absent dist/client; the worker may create the first final-source build and report its exact inventory/hashes. It may not remove an existing build. Run independent strict and build, then complete CMD-B; only after focused pass run the three affected UI suites serially with identical UI-scratch qualification. Production corrections within this Green turn follow the normal workflow; a needed rebuild after changed source returns control for primary exact-output preparation. No manual/capture effects in this assignment. Full regression and primary native zoom remain later gates. Expected decisive result is all unchanged behavior assertions executing and passing, strict/build and affected UI pass, plus RETAINED/REFACTORED/RECONCILE actual cohesion and exact cleanup. Browser/service results are Non-reusable outside their isolated returned identities.

Budget: initial Green 1; ordinary Green 2 and conditional Green 3 remain under the original rules, with one review correction loop. Exhausted Red allowance remains exhausted. Stop on a test defect, changed binding/placement, unexpected effects, required build replacement, exhausted allowance or repeated/no-diff limits. Handoff records identity, exact touched files, commands/results and hashes, actual cohesion, cleanup, residual risk and documentation impact. Primary terminally closes and inspects actual diff/evidence before accepting or correcting.

### M402-B-CODE-CORRECTION-01 — Refusal presentation and announcement

Green 1 closes freshly and compliantly with receipt `15642cd01d024c93e9c94782042937c60763f6d40be51bcaedc226ff797247ef`: exactly fourteen authorized source paths, no tests or forbidden changes. Strict/build exit 0; CMD-B reports 17/18 passing with no skips. All four actual built-client/service/disk cases pass, including committed lost response. The refusal case stops at its broad not-saved locator because both the visible paragraph and shared status contain that phrase; its later branches remain unexecuted. Source/test fingerprint is `ad968a22aaba8b9ef03f2d58ddeddd92f8f2c96524287679dd140f7e8cf5b874`. No full Green, affected regression or native visual acceptance is claimed.

Primary inspects the actual source diff, preserved tests and G's announcement contract. The worker reports TEST CONTRACT CONFLICT; primary reconciles this as an ordinary presentation correction: G requires visible not-saved labeling and a truthful contextual announcement, but does not require identical wording in both. Preserve the visible label and announce Review refused with the readable reason, captured Finding and actual provenance. Share the substantive refusal explanation without sharing its presentation prefix. The same mapping currently invites correction/resubmission for a validation refusal even when its null baseline retains the lock; make that invitation conditional on actual release. This resolves a concrete contradictory instruction while preserving truthful refusal, cleanup and blocking content. No test, semantic gate, scope or binding contract changes; no new test-authoring allowance.

The primary verifies Green 1's ordinary three-file/one-directory build with hashes index.html `01de6644f7dcfc18eb957f4922c388d014c75e5c82c2605619c69c7a174fc0a8`, CSS `73aadc60be67e8fa9af73a071adcaf3d3fa677a9b6bdf9ac68ae085c0c1f5e8e`, JS `c18447bb43b9ea90c25978042c7ee205a49060d87c4721681cbdee951516de42`, and removes only that resolved build. All four scratch roots are empty. Build output is absent for Green 2.

Complete v2 correction packet retains B-PACKET-01/GREEN-01 authority, objective, readiness, frontend capsule, responsibility placement, dependency directions, accepted tests, all fourteen source paths, forbidden roots, commands/effects, expected complete Green and S3 review. Identity changes only to assignment/lease M402-B-GREEN-02, green attempt 2, parent terminal M402-B-GREEN-01, same role/owner; fresh digest is supplied before dispatch. Current evidence is this record and unchanged TEST-ACCEPT-01 tests, fingerprint above and EXEC-01 configuration/runtime. Implement only the bounded refusal wording/mapping correction, then fresh PREPARE, strict/build, complete CMD-B and three affected UI suites if focused pass. Preserve every assertion. Initial Green is consumed; this is ordinary correction 2, with conditional 3 only under recorded material progress and one review loop. All stopping, cleanup, no-Git and terminal-handoff obligations remain fixed.

### M402-B-GREEN-ACCEPT-01 — Behavioral and structural Green accepted

Green 2 closes freshly and compliantly with receipt `81e4d89b636cc139485a3d309d358862e4cf6f9d987457f4912059f18360ffcc`: only App, review-request and ProposalReviewForm change in this correction; all five B test hashes and A implementation remain unchanged. Fresh strict/build and CMD-B 18/18 pass. Serial affected generation 8/8, guidance 10/10 and target-results 30/30 pass, with no skips or cancellations. Primary inspects the actual fourteen-file production surface, three-file correction and terminal receipt, confirms the protected test hashes and build inventory, and accepts behavioral Green and RETAINED responsibility placement. Async mechanics, pure validation mapping, draft/editor, original/reviewer content and saved decision each have their declared cohesive owner; App retains shared coordination only.

Source/test fingerprint is `df8d77b56ea2ef4923eba5fa5a84ae7959c60238441385dde86a27323040e48f`. Final-source build contains index.html `06872bf113fd28c894220ec854b038f26d6af19373a2ad5dfd214e850cabd989`, assets/index-CiL3UJmL.css `73aadc60be67e8fa9af73a071adcaf3d3fa677a9b6bdf9ac68ae085c0c1f5e8e` and assets/index-Di0NE5rC.js `7b699ecb47efe8060bbee08d14158ed402f427aac15b792cd4cc2905a5f83dcc`. Configuration and runtime remain EXEC-01. Tests use Chromium 151.0.7922.34; all three final actions and committed lost-response service/disk cases pass. Four scratch roots are empty; no review-service roots or .vite-temp remain; harness teardown settles without page errors or external requests.

Test relevance: retain the three distinct layers (transport/admission, App behavior and independent built-client/service/disk integration), synthetic profile fixture, controlled browser bridge and narrow generation-regression assertions. No only/skip/todo markers or screenshot golden assertions exist. The optional explicit-capture branch returns without evidence during ordinary regression. Prior-stage doubles are bounded to three existing routes; final review requests reach the actual service and exact disk. All original/sibling and captured-action assertions remain necessary. B is not yet accepted: primary visual/native 200% proof and fresh S3 critical/visual review remain required, followed by C's different integrated reviewer and complete authoritative suite.

### M402-B-VISUAL-01 — Partial capture and native-window prerequisite

Primary's explicit CMD-VISUAL capture run uses the unchanged Green 2 source/build/test identity and browser. The seventeen ordinary behavior/integration tests pass, including all four final service/disk cases. The optional capture branch alone fails at UI test line 619: its broad finding-summary/required text locator matches the shared status, whole original-proposal container and exact inline error. No test edit or source workaround is made. The exact retained synthetic root is temp/m402-review-proof-GUOae1, containing only original-desktop.png, original-narrow.png, editor-desktop.png and editor-narrow.png. No error/final capture or identity.json exists, and the capture command is not a pass. Environment restoration and owned teardown succeed.

Primary and fresh B reviewer inspect all four images. Their shown portions have readable source labels/prose, wrapped references, native controls and visible focus, but each image is only a viewport sample; they cannot establish the complete error/final/native-zoom path. The optional helper defect remains for review disposition rather than being hidden by the ordinary disabled-capture pass.

The frozen manual helper starts at 19:06:53 UTC with run m402-manual-review, controlled Local callback, unchanged source hashes, Chromium 151.0.7922.34 and 1366x900 viewport. Two native Computer Use window inventories do not expose its title, while read-only process inspection identifies managed Chromium PID 17728 and the expected title. No native input, guessed handle, unrelated-window action or zoom claim occurs. Leave the helper to its existing fifteen-minute timeout and normal finally cleanup; only afterward reconcile the interactive-desktop launch route. This is a native-window prerequisite failure, not evidence of visual compliance.

### M402-B-REVIEW-PROBE-01 — Saved focus after returning during submission

The fresh S3 reviewer identifies a specific unexercised I6/I8 combination: submit, select a sibling, return to the same still-pending Finding, focus its replacement form, then resolve the saved response. The submitting form's captured ref may be unmounted. This is a source-supported concern pending runtime proof, not an accepted defect or authorization to change tests. After the primary releases the sole browser runtime, the reviewer may run one bounded read-only reproduction with the existing startHarness and reviewProfileStages/reviewedProfileRun. Use PREPARE and empty UI scratch, feed the reviewed JavaScript through a single-quoted PowerShell here-string to Node --input-type=module -, and invoke through Invoke-M105Command with $m105UiTemp. No repository source/test writes, build, screenshot, actual service/disk seed or other provider/network effect.

The exact interaction uses synthetic image-alt/local run review-focus-return-probe, normal Analyze/Get guidance/Generate, Approve with supports-proposal and confirmation, and window.m104.hold('focus-return'). After Save is aria-disabled, select the second list button, reselect Image alternative issue 1, focus the mounted Save button and assert it owns document.activeElement. Resolve only focus-return with the exact independently derived approve final aggregate. Require one review callback, Saved review decision heading present and document.activeElement equal to that heading. Report the observed active element, browser, origin, viewport and run identity. Finally close the owned harness and aggregate any original/cleanup errors. Missing prerequisites or a changed source boundary stop the probe; the existing test-worker budget remains exhausted. The reviewed caller is a risk-critical verification of the unchanged contract, not new test authoring or a scope expansion.

### M402-B-EXCEPTION-02 — Owner-approved capture locator correction

The owner explicitly approves one additional primary test-only correction after the optional capture failure: in tests/finding-review-ui.test.ts, replace only the broad finding-summary/required error locator with getByText('Finding summary is required.', { exact: true }). This targets the existing inline error and changes no assertion, fixture, product behavior or validation message. Before editing primary confirms no active write lease; the synthetic manual helper may finish its existing read-only presentation session using unchanged application source. The old capture command remains failed history. This explicit exception renews no worker attempt or review allowance.

Affected B test evidence is invalidated by the edit. After the existing manual helper's exact cleanup, run fresh independent strict TypeScript and the complete explicit capture command with exact environment restoration, inspect all emitted states and record the new test fingerprint before Green resumes. G/A and unchanged application-source evidence remain intact. The separate focus concern still requires independent runtime reproduction and ordinary primary review triage.

Primary reaccepts the corrected test boundary after the manual helper ends normally at its timeout with exit 0 and settled cleanup. Fresh strict TypeScript exits 0; complete explicit capture passes 18/18 in 17.55 seconds with no skips, page errors or external requests. Environment restoration and owned teardown pass. UI test SHA-256 is `4d672766e4a6be994fa317fc5c1b4144945bd6bdcaf62955797ed539fc3c0e0d`; source/test fingerprint is `744c51761642d6b3594458c70e77a6c8f612f22d5a75f7ea7d73df7345c8c2c3`. Application source and Green 2 build remain unchanged.

The exact retained synthetic capture root is temp/m402-review-proof-Lwv9sC: eight original/editor/error/final desktop/narrow PNGs and identity.json, Chromium 151.0.7922.34, run review-capture, 1366x900 and 390x844 viewports. Primary inspects all eight images and identity: shown content and references wrap, native controls remain readable, and the invalid summary has clear visible focus and adjacent error text at both widths. These viewport samples do not show every region simultaneously and do not prove native zoom. B remains pending the focus reproduction and complete manual visual path. The next manual launch will use the same authorized helper on the interactive desktop through explicit sandbox escalation; no default invisible launch is repeated.

### M402-B-CODE-CORRECTION-02 — Current form focus at settlement

The fresh critical reviewer confirms Major I6/I8 through REVIEW-PROBE-01: one callback, replacement Save button focused before response, saved heading present afterward, but active element BODY rather than the heading. The probe exits 1 on that expected focus assertion; Node 24.20.0, Chromium 151.0.7922.34, isolated origin http://127.0.0.1:5173, 1280x800, synthetic run review-focus-return-probe. Harness teardown settles without page errors/external requests; all four scratch roots are empty, no .vite-temp or active lease exists. Source/test fingerprint remains EXCEPTION-02. App's settlement calls the submitting form's closure after the keyed form has unmounted during sibling selection. This task-contained correction needs no separate bug record.

Conditional Green 3 is justified before its lease: Green 2 delivered all 18 focused and 48 affected UI passes, and the approved capture correction now supplies eight inspectable images. The independent probe adds decisive evidence of a distinct remaining lifecycle gap. The different bounded correction is to keep one current selected-form attachment in App, identified by Finding and actual element; form lifecycle attaches/detaches that element, with detachment clearing only its own attachment. Correlated saved settlement checks that current attachment's Finding and active-element containment immediately before publishing the final run. Remove the obsolete submission-time focus closure. Existing request correlation protects the captured run; preserve no-steal behavior for list/sibling focus. This directly repairs the reproduced stale-ref cause without changing the review or focus contract.

Complete Milestone Assignment Packet v2 is M402-B-PACKET-01 and GREEN-01 with this correction overlay: workflow M4-02-20260914-01, task M4-02, cycle M402-B, lease/assignment M402-B-GREEN-03, phase green, attempt 3, parent M402-B-GREEN-02, persistent owner m402-b-code, role frontend_code_worker. The same fourteen allowed production files and forbidden roots remain exact; no directory roots or test/authority writes. The guard digest is supplied with the dispatched packet. Readiness is accepted G/A, TEST-ACCEPT-01 as revised/reaccepted by EXCEPTION-02, and the unchanged frontend capsule. TDD remains applicable; accepted tests stay unchanged. The existing risk-critical read-only probe supplies the specific before/after witness and is repeated by the independent reviewer after Green; it is not another test-authoring turn.

Responsibility and dependency direction remain form lifecycle through ReviewControls to App's necessary shared focus coordination, then the existing ReviewDecision focus consumer. Extend only App.tsx and ProposalReviewForm.tsx unless another already allowed path is required for that exact interface. No registry, new hook, abstraction or unrelated refactor. Validation is unchanged PREPARE, independent strict, first build after primary exact-output preparation, complete CMD-B, then three serial affected UI suites; record actual cohesion and cleanup. No manual/capture effects inside the worker lease. Primary will inspect diff and terminal receipt, then obtain fresh probe and manual/capture evidence.

This consumes the conditional final Green turn and the one B review correction loop. No Red allowance is renewed. An unsuccessful attempt 3, changed binding, repeated/no-diff limit or missing prerequisite returns to primary and the existing owner boundary where necessary. No further automatic implementation correction is authorized by this record.

Green 3 closes freshly and compliantly with digest `bbe1cf4f192c87f152eac56c94e21ce439d7280602eca52665bb57b1e8853f88` and receipt `60b2909fc18542803f7eb4511a6bc3096879df4cb7ecbf74792241b7cbe58770`. Exactly App.tsx and ProposalReviewForm.tsx change; tests, authorities and Git identity remain intact. Primary inspects the actual attachment/lifecycle/settlement changes and accepts RETAINED cohesion. Fresh strict/build and focused 18/18 plus generation 8/8, guidance 10/10 and target 30/30 pass, no skips. All four scratch roots are empty, no review-service root or .vite-temp remains, and browser teardown settles without page errors/external requests.

Current source/test fingerprint is `491560d6ab9e9da2404a1f7e02138b20d42a3c69a4f679b76cb9b90067103a43`. App SHA-256 is `0593c995ded66edbe2355488a25935918c89ca0388ad05a3c9ced572917908e8`; form SHA-256 is `9bc3adc405d6bb2052d4ab4c4dbb8c65763e37bd0ea63eecedf0f77920a6fca6`. The three-file build is index.html `53657fc7841c6539d4146e4499dc700cb5f345736c5a4eae7c71ff56a4914e32`, assets/index-B8eBTP4N.js `0dfd983238ffcfb0c2b0bc868605b293fe905cbb2eb2a8668686f8252235d41e`, and assets/index-CiL3UJmL.css `73aadc60be67e8fa9af73a071adcaf3d3fa677a9b6bdf9ac68ae085c0c1f5e8e`. Prior screenshots remain historical source evidence; final-source native/capture and independent focus re-review are pending. The frozen manifest and thirteen references pass another exact-byte check.

### M402-B-VISUAL-02 — Final focus proof and owner-directed zoom omission

The independent reviewer repeats the identical REVIEW-PROBE-01 once on Green 3: exit 0, exactly one callback, saved heading focused with active element H4 and text Saved review decision. Browser, fixture, origin and viewport match the failing probe. Teardown settles, no page errors/external requests or scratch residue occurs, and source/test fingerprint remains Green 3. Review confirms element-matched cleanup, current Finding ownership and unchanged captured-run/token admission. The Major focus finding is resolved.

The same manual helper launches on the interactive desktop with current source, Chromium 151.0.7922.34 and synthetic m402-manual-review. Computer Use inventory exposes its exact title but resolution and capture repeatedly reject the returned window with the contradictory ownership error that it no longer belongs to Chrome while its current owner is Chrome. No native input or native-zoom image is obtained. Browser-connector inventory does not expose the managed test browser either.

The owner explicitly directs that the 200% zoom test is unnecessary. This supersedes the native-200 verification condition in this task's I9, CMD-VISUAL and bounded samples only; record the check as omitted at owner direction, never passed. Desktop/narrow visual inspection, reflow, keyboard/focus, automated accessibility, complete regression and documentation closure remain required. No global requirement, later M6-03 verification scope or support claim changes. There is no new implementation/test-worker allowance.

Primary reconciles cleanup of the now-unneeded manual preview without Windows UI automation: read-only process identity ties managed Chromium PID 24676, created 2026-09-14T19:32:56.552157Z, to Node PID 35480 running exactly tests/helpers/m402-review-browser.ts --manual and disposable profile temp/m104-ui/playwright_chromiumdev_profile-n0SE9b. Before terminating only that browser process, recheck its exact executable, creation time, parent and profile plus the parent helper command. Do not target Node, a process name, a guessed window handle, any unrelated browser or user profile. Let the helper observe disconnect and run its existing finally cleanup; then verify its terminal result and all owned scratch before further browser work. This bounded process cleanup grants no source/test writes, native UI input or broad process-tree termination. If identity differs or cleanup fails, stop and preserve exact state.

The verified browser-only termination succeeds. The manual helper exits 1 because its context.close observes the already closed browser; this is an explicit cleanup failure, not a manual visual pass. Its finally still closes the browser/server and Playwright removes the temporary browser profile. The harness preserves exactly its generated m104-test-entry.html, m104-test-entry.tsx and vite-cache under temp/m104-ui because an error occurred. Primary inspects the complete fifteen-endpoint ordinary inventory (two generated entries, vite-cache/deps and eleven dependency-cache files), with no links or unexpected content. No page error or external request occurred.

For this named cleanup only, confirm browser/helper processes are absent and port 5173 is free, then validate ordinary containment and the exact inspected inventory before removing those three generated endpoints. Preserve both existing screenshot roots and all other temporary/user data. Recheck four empty scratch roots, absent .vite-temp and unchanged validation marker before final-source capture. This resolves a disposable preview cleanup prerequisite, not an implementation correction or a budget renewal.

Primary verifies the stopped browser/helper and all children using the exact disposable profile are absent; a bounded bind/close proves port 5173 free. Exact inspected scratch cleanup succeeds, four scratch roots are empty, .vite-temp is absent and the zero-byte validation marker keeps its recorded timestamp. The failed manual session remains non-reusable evidence. No unrelated process, file, profile or screenshot root is changed.

Fresh independent strict TypeScript and complete explicit final-source capture pass 18/18 in 18.48 seconds. The final retained synthetic root is temp/m402-review-proof-QXfJfu, with eight PNGs and identity.json SHA-256 `93e52f173a760913a8a2ba97194ffb9d32c2a5e26f4d3f044b0b5db6eadbceb9`. Its source hashes match Green 3; browser/run/viewports remain the controlled capture identity. Primary compares every PNG hash: all eight are byte-identical to the already primary/reviewer-inspected Lwv9sC samples, so those exact visual observations apply to the fresh source identity. Final-source capture teardown settles with no page errors/external requests and exact environment restoration. Nine-document checks pass 613 local links/anchors, encoding/whitespace and sixteen PowerShell fences; git diff --check passes. Final B review and C are still pending at this checkpoint.

### M402-B-ACCEPT-01 — Accessible review interaction accepted

Primary accepts the fresh B critical review PASS with no remaining Blocker, Major or Minor finding. The reviewer checks the complete fourteen-path frontend surface, five test/helper boundaries, applicable I1-I10, actual cohesion and evidence identity. The current-form focus Major is resolved by its identical independent before/after reproduction; the capture locator Minor is resolved by the owner-approved test correction and fresh final-source capture. RETAINED cohesion is accepted: existing coordination, request, pure validation, form, complete editor and durable decision owners remain distinct.

Current source/test fingerprint is `491560d6ab9e9da2404a1f7e02138b20d42a3c69a4f679b76cb9b90067103a43`. Strict/build, eighteen review tests, forty-eight affected UI tests, synthetic actual browser/service/disk final actions and committed lost-response proof pass. The reviewer independently verifies final image identity and byte equality against its eight personally inspected samples. Native 200% is omitted only at explicit owner direction; bounded viewport evidence establishes no broader support or spoken screen-reader claim. All implementation leases are closed compliantly, exhausted budgets remain exhausted and no source/test finding remains open.

C's complete maintained regression is running serially with no competing browser/service; a different fresh critical reviewer begins independent cross-slice and closure review while those checks run. Final verdict and task status remain pending complete results and documentation reconciliation. This checkpoint does not activate M4-03, change M3-03 capacity status or review any retained owner proposal.

### M402-C-VERIFICATION-01 — Integrated closure candidate

Primary runs the exact maintained thirty-one-file README inventory through PREPARE, its sequential groups and prescribed scratch/environment. Both capture flags are absent. All 706 tests pass, with zero failures, skips or cancellations; summed test durations are 95.10 seconds. The caller filters displayed output to the native test summaries without changing any suite, assertion, flag or exit handling. This includes all new transport/admission/browser suites and four actual review-to-disk branches, including the committed lost response. Independent strict TypeScript from final-source capture and the Green 3 client build remain current under identical source, configuration, command and environment identity; no source changed after those checks, so a duplicate build adds no evidence.

Fresh post-suite source/test fingerprint remains `491560d6ab9e9da2404a1f7e02138b20d42a3c69a4f679b76cb9b90067103a43`. All four scratch roots are empty, no review-service sandbox or .vite-temp remains and no write lease is active. HEAD, four configuration hashes, frozen generation manifest/thirteen references, build files and browser marker retain recorded identities. The maintained inventory contains exactly 31 unique existing test files; new/untracked source/test formatting passes separately from git diff --check.

I1 is supported by unchanged task/authority/frozen scope; I2/I3 by accepted A plus complete regression; I4/I5 by complete native-control and final-decision assertions; I6-I8 by hostile/late/selection/unknown cases, axe/keyboard/announcement checks and the independent before/after focus probe; I9 by inspected final-source desktop/narrow samples with the explicit native-200 omission; I10 by actual browser/service/disk cases, accepted cohesion/leases, full suite and current strict/build. The different fresh C critical reviewer checks cross-slice integration, remaining risk and documentation closure. No new implementation is delegated or authorized.

Documentation impact is nine existing owners: project overview/API/review/developer commands; documentation navigation; UI index/presentation extension; roadmap; plan/index; and progress/index. Requirements and ADRs need no semantic change. Before final review, reconcile current B acceptance and C verification without prematurely marking Complete. After review PASS and candidate documentation checks, update task status, archive this plan and repair all affected relative links, then repeat proportional link/format/consistency/diff checks. M4-03 stays Not started and M3-03 stays Blocked; no Git write, commit, push or publication is included.

### M402-FINAL-01 — Integrated review and task closure

Primary accepts the different fresh C critical reviewer PASS with no remaining Blocker, Major or Minor. The reviewed candidate plan SHA-256 is `3c9b5c246523b842e508e0c2663812eec8c24ad4f891c1a737692d2a41621a4f`. Review covers cumulative I1-I10, cross-slice HTTP/service/admission/UI behavior, failure and recovery, original/sibling/provenance preservation, actual responsibility placement and test relevance. The reviewer independently verifies 31 capture-source hashes, thirteen frozen references, eight screenshot identities, three build hashes, all eleven compliant terminal receipts, no active lease, complete suite inventory and diff whitespace. It personally inspects four final-source visual samples and reuses matching A/B risk-critical reproduction and current strict/build/full-suite evidence.

All 706 tests across 31 files pass, with no failures/skips/cancellations. Final source/test fingerprint remains `491560d6ab9e9da2404a1f7e02138b20d42a3c69a4f679b76cb9b90067103a43`; actual synthetic review-to-disk checks cover all three decisions and committed response loss. The independent stale-form focus reproduction passes after the bounded correction. Final desktop/narrow captures and their identity remain at temp/m402-review-proof-QXfJfu. Native 200% is omitted at owner direction, never claimed as passed. No actual retained proposal, provider quality, capacity, support matrix or screen-reader speech is established.

Candidate documentation passes nine-document encoding/whitespace, 615 local links/anchors, sixteen PowerShell fences, complete unique suite inventory, untracked source/test formatting and git diff --check. Primary accepts the documentation gate and reconciles roadmap/progress Complete status, public capabilities, UI verification limits and plan navigation together. The plan is archived only after this acceptance; post-archive checks verify repaired relative/incoming links and unchanged source/test identity. All four scratch roots are empty, no review-service root or .vite-temp remains, and no worker lease is active. The two earlier capture roots are retained as exact synthetic diagnostic evidence, not additional current passes.

Documentation impact: Updated the project overview/API/review/developer instructions, documentation navigation, UI index/presentation, roadmap, plan/index and progress/index. Requirements and ADRs retain their semantics. Only M4-02 closes; M4-03 remains Not started and M3-03 remains Blocked. No commit, push or publication occurs.

Post-archive verification passes: nine documents, 615 local links/anchors, sixteen PowerShell fences, UTF-8/newline/whitespace, status/index consistency and git diff --check. No incoming link retains the former active-plan path. Final build hashes and source/test fingerprint are unchanged, four scratch roots remain empty and no active lease exists. The archive and its Complete mirrors are accepted together; no required M4-02 work remains.

### M402-A-ACCEPT-01 — Transport and admission accepted

Primary accepts the fresh critical_reviewer PASS with no Blocker, Major or Minor finding. The reviewer inspected all four production files, all three tests/helper, fixed domain/service boundaries and correction/lease records; it independently reran API 10/10 and admission 7/7 with no skips, checked all ten contract/receipt digests and 150 source/test/config baseline entries, and reused matching strict/97-test affected regression evidence. Isolated API PID 13680 passed actual loopback-to-disk readback and listener cleanup; no review-service root remains. HEAD and source/test fingerprint remain EXEC-01 and `6939edee48a5b8bf33bd976e06a02766e18b8a44595cc092e494a74955ac95c5`; no active lease or invalidated runtime evidence exists. The initial reviewer guard-hash comparison used raw rather than prefixed encoding and was corrected from the guard's actual _hash_path contract; it was not repository drift.

I2/I3 and A-owned I6/I7/I10 pass at this scope. Primary accepts RETAINED cohesion and the test relevance assessment. No review correction was needed. A's three Red and two Green attempts are consumed; no budget is reopened. B may now enter its accepted packet's read-only preflight. B ownership, rendered behavior and all browser/native-zoom evidence plus C integrated/full-suite/docs gates remain pending. M4-02 remains In progress; no requirement, ADR, M3-03 or M4-03 status changes.

### M402-EXEC-01 — Execution entry

The intentional execution HEAD is `303b7985e2a5e8b65f6522e4480e396c5953703d`; Git status was clean and no active lease existed. The committed planning checkpoint is retained. Initial expected dirty path is this plan only. M4-01 is Complete; the applicable M4-02 requirements are Accepted and its only prerequisite is satisfied. No capacity, actual-proposal or generation-evaluation work is selected.

Windows PowerShell 7.6.5, Python 3.12.10 and pinned Node 24.20.0 are available. The maintained README preparation was extracted by its exact heading and dot-sourced. Independent strict TypeScript and the three ENTRY-01 suites pass: 92 tests, zero failures/skips. This is current focused evidence, not a full-suite closure. The source/test fingerprint is SHA-256 of UTF-8 LF-joined `rg --files src tests` path/hash rows, case-insensitively sorted as PowerShell Sort-Object, retaining returned Windows backslashes, lowercase per-file hashes and no terminal newline: `a3712ab111db335b90bf5526c5fb18d6551aab8062e9d1f6f8a1849c8d074579`. Package, lock, strict config and Vite config hashes respectively are `01c0b39bd9141588543bd520dbb7a160eac2d95414eef54d36f394fe0fb7ba5c`, `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`, `3957f80af41b23dc4ccefaa6b24823c367e6984980420b596275b8692df5abde`, and `8d75b9863c86a8eca2267c74d8875be46061c288f5eaef6bea93c427d3dacd07`.

The M3-01 manifest retains `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b` and its thirteen input hashes pass. A generic PowerShell traversal was interrupted after failing to terminate; the finite Python dictionary/list traversal replaced it and checks the exact expected count. Neither inspected model output or changed input files. Existing scanner/UI/integration scratch directories are empty; `dist/client` already exists and requires inspected exact-output removal before rebuilding. Browser revision 1234 is installed. Native Computer Use import and window inventory work; actual owned-preview capture/200% zoom remains unproved.

All commands run from `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab` after reading and dot-sourcing the current [README preparation](../../../README.md#development-command-preparation). Do not replay historical M4-01 leases, cleanup roots or Git comparisons. Planning needs no install, application launch or browser capture.

### M402-ENTRY-01 — Planning evidence

On 2026-09-14 15:26 UTC, clean HEAD `5f1de30ee1aad85a4d1ecf99fca9beab6065794d` had no active `logs/agent-flow-leases/v2/active.json`. Node 24.20.0, independent strict TypeScript and all **92** tests across run-contract, review-contract and finding-generation-admission passed, with no failure/skip. This is focused planning verification, not a rerun of M4-01's full 671-test closure or evidence of a review UI. The M3-01 manifest SHA-256 remains `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; all thirteen `path`/`sha256` references also pass fresh exact-byte hash checks.

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Resolve existing lease before continuing' }
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/review-contract.test.ts tests/finding-generation-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Planning checks failed' }
}
git diff --check
```

The entry HEAD is historical evidence, not a future executable equality gate. Execution records the then-current intentional HEAD, expected dirty paths, relevant source/test/configuration hashes, frozen/protected inputs, command/cwd and runtime/environment. Reuse evidence only under the workflow's complete identity/no-drift rule. A committed plan need not be dirty; reconcile changed bindings instead of mechanically replacing an old hash or requiring the old parent in lease/closure comparisons.

### Future command and effect slots

The historical planning slots below are now resolved by M402-COMMAND-01, which supersedes this slot table as the executable contract after final review and G acceptance. They are not permission to improvise commands during a worker turn.

| Slot | Exact freeze required |
| --- | --- |
| M402-CMD-A | Preflight/Red/Green callers, new API/admission test paths and existing regressions, strict command, expected initial capability absence versus behavioral failure, flags, runtime and timeout; controlled loopback ownership where used. |
| M402-CMD-B | Browser harness callback/entry wiring, focused UI suite, pinned build/browser identity, synthetic proposal/guidance provenance, deterministic fixtures, environment, viewport/deadline and accessibility/keyboard/focus checks; no actual scan, retrieval or generation. |
| M402-CMD-INTEGRATION | Exact synthetic seed through existing valid aggregate boundary, exclusive run root, owned service/listener, built UI/driver, one finite set of actions/readbacks, no production backdoor and no retained owner root. Seeded controlled input is labeled synthetic at every evidence boundary. |
| M402-CMD-VISUAL | Reuse existing harness/app build for the bounded samples below; exact browser/tab/process owner, viewport and genuine browser zoom mechanism, permitted screenshot paths and source identity. A screenshot/DOM assertion alone cannot prove native zoom or contrast. |
| M402-CMD-REGRESSION | Current complete README inventory plus every new accepted suite, flags/order/maximum durations, strict/build callers and known build/browser/scanner scratch effects. No parallel browser suite or running application service. |
| M402-CMD-PREPARE / CLEANUP | Exact absolute scratch and generated paths, safe empty/absent preparation, ancestor/reparse/alias checks, exclusive creation, permitted files/build effects, owned service/driver shutdown before cleanup, evidence preservation, exact removals and final state checks. Unexpected contents stop cleanup. |
| M402-CMD-GUARD / DOCS | Current guard and terminal-receipt callers, packet digest/path sets/protected tests/authority files/attempt lineage; primary-only metadata maintenance between leases; documentation links/anchors, UTF-8/newline/whitespace, command parsing, suite inventory, frozen-input checks and `git diff --check`. |

Use the maintained preparation for scoped environment restoration and pinned Node/browser assets; do not install packages or models to fix a missing prerequisite without an explicit bounded preparation decision and authority. Test helpers must preserve original failures and exact-root context if secondary cleanup fails, incorporating M4-01's corrected cleanup contract. Add no new harness framework, shell wrapper system or telemetry to administer this task.

## Validation and Acceptance

The cumulative packet is supported by accepted A/B evidence and M402-C-VERIFICATION-01; the different C reviewer checks the full set for closure. The native-200 part of I9 is omitted at explicit owner direction under M402-B-VISUAL-02. Evidence records retain exact input/state, command/build/environment identity, result and owner.

| ID | Trigger and expected result | Proof owner |
| --- | --- | --- |
| I1 | Current task and protected-input check: only M4-02, unchanged M4-01 semantics/evaluation/generation bindings; no actual owner proposal/capacity/quality claim. | Primary, G/C reviewers |
| I2 | Malformed, unknown-key, invalid UTF-8, oversized, aborted and duplicate transport events: bounded refusal, no unintended dispatch; valid complete maximum-sized edited input fits the frozen finite limit. Review calls only the existing service method. | A tests, A reviewer |
| I3 | Wrong run/Finding/provider, sibling/original mutation, substituted action/edit/note/judgment, malformed decision time or forged success: client admission refuses without publishing false success. Accept only the captured request's valid selected final transition. | A tests, A reviewer |
| I4 | Absent/false support confirmation, unresolved/contradictory acceptance judgment, N/A without reason, blank/oversized fields or invalid references: accessible errors, no false acceptance. Rejection needs no success assertion; editing relevant content requires renewed support confirmation. | B tests, B reviewer |
| I5 | Each final action: original remains inspectable and distinct; only edit-and-accept supplies the complete accepted reviewer proposal; reject accepts no plan. Decision action/time/judgment/note and post-change reminder are visible, not color-only; no reminder-completion gate. | B tests and visual evidence, B reviewer |
| I6 | Pending, repeated activation, sibling selection, changed run, callback reflection/reentrancy, unmount and late response: only the captured operation may publish, no duplicate final request, draft/confirmation never leaks to another Finding; selected list focus and sibling state remain intact. | A/B tests, B reviewer |
| I7 | Definite refusal versus response lost after possible commit, local timeout or malformed response: truthful distinct error/unknown state, no optimistic save or automatic retry. Exact G recovery/retained-admission behavior applies; cleanup is never inferred from browser abort. | A/B tests, A/B reviewers |
| I8 | Native controls, all three actions and validation states: automated axe, keyboard, accessible names/relationships, logical order, shared announcements and focus preservation pass. List/selection remains complete; saved review supersedes cached pending labels without inventing a new provider invocation. | B browser tests, B reviewer |
| I9 | Bounded desktop/narrow/native-zoom samples: complete original/edit/decision text, citations, reason/note, readable controls, non-color source labels and visible focus; no clipped required content or unintended horizontal page scroll. No manual screen-reader speech claim. | Primary/worker browser evidence, B/C reviewers |
| I10 | Synthetic actual browser-to-service-to-disk proof, relevant fixture/mock audit, compliant leases, actual diff/cohesion, current full suite, strict/build, exact cleanup and documentation closure pass. M4-03 real-proposal review and M3-03 capacity remain independent. | Primary, C reviewer |

### Bounded visual and interaction samples

Use one installed supported browser, not a browser/assistive-technology matrix. Planning targets are desktop 1366×900 and narrow 390×844 CSS-pixel viewports, plus actual 200% browser zoom on the desktop review path; G binds the runnable mechanism and exact sample identity before execution. These are task evidence samples, not a support claim. Do not equate resized viewport, device scale factor or CSS zoom with native browser zoom.

Automated cases cover pending proposal, missing confirmation, invalid N/A reason, invalid complete edit/reference, submission pending, definite refusal, unknown save and each final decision; use both Local/Groq provenance and all applicable review profiles without crossing every visual/state combination. Negative cases preserve abstention, failed/invalid generation and ScannerReviewObservation as non-reviewable. Existing parent `running`/`failed`, complete-zero and scan-only states remain unchanged regression cases, not new UI designs.

Manual visual review follows one controlled selected-proposal path through original inspection, edit form, inline error and saved edited decision; sample approve/reject labels in automated checks. At narrow width and genuine 200% zoom inspect long valid content, references, note/reason, visible focus and reflow. Reuse screenshots only when they materially support the review. Record source/build/browser/viewport/zoom/fixture/interaction identity and observed result. M402-B-VISUAL-02 and M402-B-ACCEPT-01 record the final-source desktop/narrow visual result and owner-directed omission of native 200%; the original native-zoom procedure above is superseded only for this task's verification. M2-03's separately deferred full-guidance/detail zoom item remains with M6-03.

## Idempotence and Recovery

Planning checks are repeatable and read-only apart from this documentation change. Before future effects, inspect Git/lease state, expected path identities and remaining allowance. A worker stop returns to primary triage; do not reopen a closed lease, hide a failure, reset a budget or overwrite unrelated changes.

The server permits only one final review. Local timeout, connection loss or abort does not imply the server did not commit. Follow G's frozen unknown-outcome behavior; preserve prior validated evidence and never resubmit automatically. A canonical validated read, if included in G, is limited to the captured run/operation and does not introduce run history or a recovery subsystem. Invalid input can be corrected only where service outcome/admission establishes that doing so cannot duplicate an unknown final decision.

Clean only exact, verified, task-owned test paths after owned listeners/browser contexts/drivers close; keep evidence needed to diagnose a failure before removing disposable artifacts. Report uncertain cleanup and the exact retained root. Never delete owner runs, stop unrelated applications/Ollama, reset Git, broaden recursive targets, or install prerequisites as routine recovery. If native zoom or another required browser mechanism is unavailable, report the missing evidence and preserve task status; do not fabricate a pass or silently defer an Accepted check.

## Artifacts and Notes

M402-ENTRY-01 owns planning evidence. Future acceptance records M402-G-ACCEPT-01, M402-A-ACCEPT-01, M402-B-ACCEPT-01 and M402-FINAL-01 are created only when their evidence exists. Keep commands, relevant-tree/environment identities, protected-input checks, accepted tests, terminal leases and decisive review/visual outcomes in this living plan; no new ledger or raw reviewer transcripts. Private retained-run contents, credentials and ignored operational material are not copied into tracked documentation.

Planning documentation impact is five paths: this plan, roadmap activation, plan index, progress index and one M4-02 progress record. No implementation, requirement or ADR change is needed to create it. Execution reconciles the actual affected UI/API/developer documentation at C.

### M402-PLAN-01 — Accepted planning readiness

On 2026-09-14 15:37 UTC, the primary accepted the fresh read-only `critical_reviewer` verdict **PASS** for planning readiness, with no Blocker, Major or Minor findings. The review covered the full five-path activation, selected authorities, existing M4-01 literals, future R3/S3 boundaries, I1–I10, frontend-quality reuse/ownership, native browser/focus proof, command gating, unknown-save integrity and scope/closure honesty. The reviewer directly inspected current transport/service/validator/UI seams, confirmed HEAD/dirty paths and no active lease, and ran `git diff --check`; it reused the primary's fresh strict/92-test evidence rather than repeating it. No browser, provider, retained-run inspection or write occurred in review.

The reviewed plan SHA-256 was `28e9bb1002246e33f88f4f2d5853702d2560f7656783d83fe01a8a660d8fcacc`, independently checked by the primary before this living-record update. Subsequent changes record the verdict, frozen-reference check and current-state/progress reconciliation only; no binding contract or command changed. Primary checks cover five documents, local links/anchors, one parseable PowerShell block, sixteen required plan sections, strict UTF-8/no BOM, final newline/trailing whitespace, frozen references and `git diff --check`. Only the five planned documentation paths are changed and no active lease exists.

This accepts readiness to enter G after execution authorization, not G literals, worker packets, UI implementation or browser behavior. No planning correction loop was consumed. M4-02 remains In progress; M3-03 remains Blocked and M4-03 Not started.

## Interfaces and Dependencies

Use current React/TypeScript, native controls, existing CSS, pinned build/browser/axe tests, the application-owned same-origin service and M4-01's accepted review validator/outcome. New task-local interfaces are a bounded review HTTP handler, browser request-correlated admission, minimal callback/request coordinator, form/editor and immutable decision view. Exact transport/error/deadline and UI literals are frozen in M402-G-CONTRACT-01 and accepted in M402-G-ACCEPT-01.

Browser code has no direct filesystem, provider, process or credential authority. Service owns durable decisions. The UI owns only transient drafts and faithful presentation. M4-03 consumes the completed review interaction; this task neither selects it nor makes a real-human-review or capacity claim.

## Revision Note

2026-09-14: created for the owner's M4-02 current-state review and planning request after M4-01 completion. The frontend-quality skill shaped the granular reuse audit, named visual owners and bounded native-browser evidence. Recorded two delegated TDD slices, a future R3 transport/admission gate, current fixed review semantics and strict separation from actual-proposal review, capacity and evaluation. No implementation or browser review action occurred.

2026-09-14 15:37 UTC: recorded independent planning readiness PASS with no findings, fresh frozen-reference checks and documentation closure; reconciled current state and progress without changing contract, commands or execution authority.

2026-09-14: completed M4-02 after G/A/B acceptance, the verified current-form focus correction, 706 passing tests, final-source desktop/narrow evidence, different integrated critical PASS and documentation closure. Native 200% was omitted at explicit owner direction; no broader task or requirement was changed.
