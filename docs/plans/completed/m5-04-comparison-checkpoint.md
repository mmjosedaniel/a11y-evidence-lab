# M5-04 — Comparison checkpoint

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with [PLANS.md](../../../PLANS.md).

## Current state

- **Task:** [M5-04 — Integrate and verify comparison](../../DEVELOPMENT_ROADMAP.md#m5-04--integrate-and-verify-comparison), Complete after Verification, different final critical PASS and documentation closure.
- **Accepted closure:** [M504-FINAL-01](#m504-final-01--integrated-review-and-task-closure). Twenty-four roadmap tasks are Complete; the four M6 tasks remain Not started.
- **Accepted evidence:** G/A, controlled and public B, 881-test regression, final caller checks and independent strict/build pass. The W3C baseline/rescan saved a persistent contrast comparison; the separate Ollama zero-Finding observation is preserved.
- **Findings and allowances:** No open finding. All worker grants and G correction cycles are consumed. W3C used exactly 2/2 activations; Ollama used one activation and its lane is closed. Both isolated public roots remain preserved. No retry or further worker write is authorized. Active lease: None.
- **Next boundary:** M6-01 is dependency-ready but not selected or started. This completed plan is historical evidence; its commands and consumed grants are not permission to replay.
- **Archive navigation:** Start with [final closure and limitations](#m504-final-01--integrated-review-and-task-closure), then the [evidence mapping](#m504-verify-02--final-evidence-mapping) and [actual public comparison](#m504-public-02--actual-selected-finding-comparison-accepted). The [commands](#concrete-steps), [acceptance matrix](#validation-and-acceptance), [recovery](#idempotence-and-recovery) and [decision contract](#decision-review-contract) preserve the completed execution procedure. Historical Git IDs and consumed grants do not authorize a new assignment or replay.

## Progress

- [x] (2026-09-20 UTC) Reviewed project status, prerequisite closure, applicable authorities, current comparison seams and existing proof; strict TypeScript and 117 focused tests pass. See M504-ENTRY-01.
- [x] (2026-09-20 UTC) Created a verification-first plan with bounded characterization, separate controlled/policy/public lanes, guarded ownership and explicit public authorization gate.
- [x] (2026-09-20 UTC) Independent critical planning-readiness PASS, no findings; five-file documentation validation and scope checks pass. See M504-PLAN-REVIEW-01.
- [x] (2026-09-20 UTC) G: execution authorization, fresh endpoint, exact public grant and proof/command contract accepted after research, synthesis and both fresh research reviews.
- [x] (2026-09-20 UTC) Execution authorized; fresh clean endpoint, ordinary retained-data/scratch inventory and frozen references checked in M504-EXEC-01. Exact public target selected from the owner's reply; no activation yet.
- [x] (2026-09-20 UTC) A: preflight, characterization, owner-approved narrow correction, primary inspection and critical caller PASS complete; all findings closed.
- [x] (2026-09-20 UTC) B: controlled integration, accessibility observations and preservation/cleanup pass. The separately authorized W3C sequence completed one Local baseline and one selected-Finding Local rescan with a persistent comparison and clean shutdown. Ollama zero-Finding evidence remains preserved separately; I8 passes.
- [x] (2026-09-20 UTC) C: authoritative 45-file suite passes all 881 tests; final literal-bound strict/thirteen caller tests and independent strict/build pass. Different final critical review returns PASS with no findings; Verification and documentation closure pass. See M504-FINAL-01.

## Surprises & Discoveries

- M5-03 already proves actual image-alt scanner/service/disk/browser publication and exact synthetic baseline deletion. Its separate valid-Finding service case proves refusal after lineage loss. Rebuilding either mechanism is unnecessary. Its actual-state observation is pinned to that execution, not a current live-state guarantee.
- At task entry, the three frozen native transitions stopped at `executeRescanComparison` in `tests/rescan-integration.test.ts`; only the separate image-alt case crossed service publication and browser display. M5-04 added native label/contrast service/HTTP/disk/GET/UI coverage, as recorded in the [final evidence mapping](#m504-verify-02--final-evidence-mapping). The synthetic adverse-case matrix remains a separate evidence class.
- The accepted `3.54 → 4.00` contrast vector is arithmetic-only: it must never become a canonical comparison record, third fixture or extra browser scan. The UI's separate `3 → 4/3/2` synthetic examples are not that frozen vector.
- Earlier public scan observations do not authorize a new target or prove a current selectable Finding. A live zero-Finding result is valid scanning but cannot satisfy this selected-Finding comparison checkpoint.

## Decision Log

- **Decision:** Start with existing-behavior characterization, not a planned production rewrite. **Rationale:** M5-01–03 already implement the capability; this checkpoint closes demonstrable integration gaps. **Date/author:** 2026-09-20 / primary.
- **Decision:** Keep controlled native scans, synthetic adverse integration, policy vectors and actual public observation explicitly separate. **Rationale:** REQ-COMP-001–008 and evaluation authority prohibit promoting constructed inputs to scanner evidence. **Date/author:** 2026-09-20 / primary.
- **Decision:** Freeze proof callers, side effects and cleanup before leases or primary-run evaluation. **Rationale:** Public scan provenance and exact-directory deletion require identity and recovery correctness; the guard alone cannot prove those properties. No new product architecture is proposed. **Date/author:** 2026-09-20 / primary.

## Outcomes & Retrospective

Controlled native/service/browser proof, adverse integration, policy/deletion checks and bounded visual observations pass without production changes. Both the valid-unavailable-image correction and final W3C caller binding passed critical review. The first public page had zero Findings; the separately authorized W3C sequence produced a saved persistent contrast comparison from an actual baseline Finding, preserving both public roots. The authoritative 45-file suite passes 881 tests, and affected final caller checks plus independent strict/build pass. Existing presentation is reused. Different final critical review and documentation closure pass. M5-04 is Complete, with no production change or new product scope. The remaining M6 tasks are unstarted. The extra caller corrections showed why a canonically valid unavailable-evidence case must be tested before public activation; preserved failures and explicit grants remain part of the evidence.

## Purpose / Big Picture

Demonstrate that a developer can select any retained baseline Finding, intentionally scan the same page again, and inspect a conservative saved comparison independently of retrieval, generation or review. A reviewer must be able to distinguish native evidence, deterministic policy tests, uncertainty, durable readback and the limitations of one changing public page. Passing this checkpoint makes M6-01 dependency-ready; it neither executes M6 nor establishes accessibility, remediation causality or release qualification.

## Context and Orientation

The [roadmap](../../DEVELOPMENT_ROADMAP.md) owns status. [Requirements](../../PROJECT_REQUIREMENTS.md) own semantics; the [documentation router](../../README.md#read-by-task) locates controlling modules. Read these exact task authorities before execution:

- [Rescan and comparison](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#rescan-and-comparison): REQ-COMP-001–008; also REQ-EVID-003/008/010/011 and REQ-UX-002/004 for immutable minimized records and source distinctions.
- [Privacy and security](../../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md): REQ-SEC-006 exact run-directory deletion; [lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#comparison-lifecycle) and its retention/deletion boundary.
- [Evaluation authority](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md): BHV-06, freeze boundary and [comparison-only contrast vector](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#comparison-only-contrast-policy-vector). [SPEC-006/007/008](../../specs/SPEC.feature) and [HS-015](../../specs/HARD_SPEC.feature) are derived observable views, not executable tests.
- [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md): REQ-A11Y-001–004/006/010. All 200% zoom testing remains Deferred; manual verification is visual only, not spoken-screen-reader proof.
- [ADR-0018](../../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), and [OD-025/026/027](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md): trusted operator target, one aggregate, separate test/implementation ownership, exact task selection, no general reopening, existing presentation.
- [Agent workflow](../../../.codex/README.md), [worker procedure](../../../.codex/execplan-implementation-workflow.md), [write guard](../../../.codex/write-lease-guard.md), [frontend-quality](../../../.agents/skills/frontend-quality/SKILL.md), and the current [UI contract](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md).

Prerequisites are satisfied for planning: M5-03 and earlier tasks are Complete; applicable Must requirements and decisions are Accepted or explicitly Deferred. No significant product choice is reopened. Runtime evaluation requires fresh frozen-input checks. `evaluation/rd003-scan-v1.json` and its six fixture states define native inputs; `evaluation/m502-comparison-v1.json` binds three directed failing→corrected pairs; `evaluation/m301-generation-v1.json`, its thirteen references and the corpus remain protected, not executed here.

Current ownership is cohesive: `service.ts::rescanFinding` coordinates admission → `executeRescanComparison` → `publishComparison`; comparison modules own profile/correlation/outcome, publication owns durable truth, repository owns JSON writes, lineage inspection owns immediate-parent availability. `POST /api/rescans` and `GET /api/runs/:id` are existing HTTP boundaries. `App.tsx` owns coordination, `ResultsSection.tsx` composition, `ComparisonDetail.tsx` and `comparisonPresentation.ts` presentation. The executor alone returns a private comparison handoff and deliberately does not publish it. Test-only controlled fixture correlation uses stable manifest keys; public runtime correlation uses exact rule and unique locator.

## Scope and Non-Goals

Include only M5-04 evidence preparation, missing integrated characterization, bounded native/policy/public comparison verification, exact synthetic deletion proof, accessible comparison checks, necessary in-contract defect corrections through replanning, and documentation closure. No production edits are presumed necessary.

Exclude new rules, manifests or fixture revisions; policy-vector persistence; arbitrary pair browsing; history/reload restoration; fuzzy matching; hidden backups/cascades; a generic evaluation platform; new dependencies, services, abstractions or storage; provider/model calls, probes or downloads; original retained-run mutation; public-page modification; M6 executions; certification, conformance, scanner-fidelity claims for constructed vectors, or remediation-causality claims. Do not inspect credentials or copy ignored private evidence into tracked files. No Git writes, commit or push is authorized by this request.

## Plan of Work

### G — Execution entry and proof binding

After the owner requests execution, primary records `M504-EXEC-01`: actual HEAD, dirty-path disposition, no active lease, Node/PowerShell/browser/lock identities, source/test fingerprint, build identity, frozen references, all original retained-run path/hash pairs, and scratch inventory. Preserve unrelated work. Never require the plan to remain uncommitted or compare a future lease to the historical planning HEAD. A commit requires endpoint reconciliation, not blindly changing a constant.

Primary compares existing commands plus a bounded direct procedure against a minimal adaptation of the existing test harness. Use the [decision contract](#decision-review-contract) for unresolved proof identity/recovery mechanics; do not research established outcome rules or select new product technology. Freeze one small `M504-G-CONTRACT` here containing acceptance-row-to-caller mapping and every unresolved command slot below. Procedure review can approve controlled work while the public URL remains explicitly gated; no unresolved field may affect the next worker packet or command.

Public grant must name one trusted, non-sensitive, non-authenticated HTTPS target the owner is permitted to analyze and authorize at most two actual scanner activations: one fresh baseline, then one intentional later scan from one real baseline Finding. Record exact immutable mode choices and no retrieval/generation. No historic grant carries forward. Target may be named in the execution request; otherwise ask only for this missing choice while completing unaffected controlled work. Zero Findings, scan failure or external drift is evidence, not permission to choose another target or retry. No public outcome is forced in advance.

G exits only with primary acceptance of exact procedure, commands, effects, budgets, evidence classes and required reviews. Its decisions define how to prove existing behavior; native/public results remain unproved until B.

### A — Small test-only integration characterization and caller preparation

Work-slice ID `M504-A`, S3 for provenance, publication and deletion/recovery. Test owner: one persistent `test_worker`; implementation owner: None unless primary accepts a concrete defect and revised packet. Before preflight issue [Milestone Assignment Packet v2](../../../.codex/execplan-implementation-workflow.md#milestone-assignment-packet-v2), projecting G and the acceptance matrix. Preflight reads current integration/caller coverage and returns the workflow's exact classification. `EXISTING_AND_COVERED` reuses still-valid evidence without edits; `EXISTING_BUT_UNCOVERED` uses a passing `evidence` lease, not fabricated Red.

Expected test envelope: `tests/rescan-integration.test.ts` and `tests/helpers/m105-walking-skeleton-harness.ts` only. The test owns scenarios/assertions; the harness owns real service/browser/scanner setup, controlled interception, owned inventory and teardown. Reuse `tests/helpers/comparison-fixtures.ts` and `tests/helpers/m503-comparison-fixture.ts` read-only initially. Freeze narrower paths per turn. Do not widen the fixture scanner's `https://m105.test` origin policy to smuggle in a public observation. A separate opt-in caller is permitted only after G establishes a demonstrated need, exact path, responsibility and characterization; no unguarded primary script implementation.

Observable contract: extend only the missing crossings in I1–I7 below. Prefer the existing native pair loop plus service publication/readback for label and contrast; retain its executor-only private-handoff assertions separately. For persistent, missing/ambiguous and mismatched-profile branches, drive real service/repository/HTTP/browser composition with explicitly synthetic controlled scanner inputs; do not substitute an already calculated comparison response for the crossing under test. Existing pure outcome and UI matrices remain complementary. Reuse, rather than duplicate, exact deletion and valid-Finding action-refusal tests. Add one bounded actual-service keyboard/announcement/focus witness if still uncovered.

No application-source responsibility or runtime interface changes; no structural refactor is planned. All `src/`, manifests, fixtures, corpus, package/lock/config, original `data/runs`, documentation and Git metadata are forbidden worker writes. Primary owns plan/evidence/status/developer documentation between leases. Generated test outputs are separately bounded in G and the guard projection, never a blanket permission over `temp/` or `data/`.

Every write turn needs a fresh primary-opened lease and terminal close; only one active writer. Inspect actual diff, test relevance, responsibilities, accepted evidence and terminal receipt. Focused commands are G's exact selected integration tests plus independent strict TypeScript; a fresh `critical_reviewer` reviews the complete caller and adverse effects before B's public activation. A different final reviewer is reserved for C. Reviewer roles remain read-only.

`MISSING` or `REGRESSION` stops this characterization assignment. Primary consults the [bug index](../../bugs/README.md), reproduces the in-contract defect, updates this plan and the responsibility/command envelope, then delegates genuine Red to the test owner and Green to a separate `code_worker` (or `frontend_code_worker` for a material rendered change). Accepted tests stay unchanged during Green. The primary does not implement the fix. New product behavior or architecture needs owner direction; relabeling a missing capability as evidence is forbidden.

Budget: one read-only preflight; each applicable role/phase has attempt 1, ordinary correction 2, and conditional 3 only with all recorded progress, learning, materially different action, likely benefit and unchanged-authority checks. One review-driven correction loop uses the same remaining role/phase allowance, not a fresh counter. Stop on two identical failures without new evidence, two no-diff outcomes, an exhausted budget or changed binding fields. Preserve the same work-slice identity through correction. Workers retire at acceptance.

### B — Accept controlled evidence and one bounded public observation

Primary runs only the reviewed G/A callers. Controlled proof and actual public proof run sequentially; no leases or another verification process overlap their run store or browser. Automated local test launches are not public scan grants. Observe the actual native three pairs, integrated synthetic adverse branches, policy-only arithmetic, exact owned deletion/readback and separate positive/refusal control; map each result to I1–I7.

For the public lane, use the actual production scanner without response interception, fixture bytes, injected outcomes or provider work. Start the accepted built client and local service against the isolated G-owned run root. Through existing UI/HTTP boundaries obtain one new baseline, choose one retained Finding without changing it, and activate its one intentional later scan. Verify exact response-to-canonical-readback equality, distinct run IDs, selected baseline identity, available before/after evidence, pair/match rationale, later-only Finding preservation, zero provider activity and unchanged baseline bytes. Observe the outcome supported by the data; a conservative inconclusive/not-comparable result is not itself a defect. Failure before a saved comparison does not pass I8.

UI disposition is `REUSE_AS_IS`: existing App/Results/comparison owners, no CSS or composition rewrite. Retain complete browser evidence identity. Automated keyboard, focus and shared-status checks cover selected Finding → rescan → result → immediate baseline/return; manual visual inspection covers comparison evidence, limitations and visible focus at existing comparison-test desktop 1366×900 and narrow 390×844. Reuse M5-03's exact-source visual samples where the complete identity remains valid; capture only missing/current states. No exhaustive matrix, 200% test or speech claim. Material rendered correction first returns to the frontend-quality reuse audit and separate-owner workflow.

Public activation count remains consumed after failures. No automatic retry, new target, target modification, model fallback or silent acceptance waiver. Preserve bounded failure evidence and finish unaffected work; request only the missing new authority if I8 cannot finish. Synthetic deletion is separate from public observation: delete no original or public baseline merely to reuse a cleanup command.

### C — Integrated review and documentation closure

Accept B only after direct evidence inspection, current protected-input/original-record verification and exact teardown. Run the maintained complete authoritative suite once, independent strict TypeScript and build; expand/repeat only for changes, failed prerequisites, stale evidence or unresolved risk. Current suite inventory is 44 files/866 tests before A, not a future hard-coded expected count. Record actual complete counts and any truncated/unavailable evidence honestly.

A different fresh `critical_reviewer` audits I1–I9, the actual cumulative diff, cohesion, primary acceptance, terminal leases, caller identity, negative/deletion paths, public provenance, accessible proof and documentation. Use the same bounded correction rules; review PASS is not owner approval for extra effects. Complete M5-04 only after every task Verification item and the [documentation closure gate](../../README.md#task-closure-documentation-gate) pass. Reconcile affected current capability/instructions if changed, roadmap, plan/progress indexes and concise progress; archive this plan and repair links only then. Leave M6 Not started.

## Decision Review Contract

`M504-G` concerns only a small proof/command procedure, not new comparison semantics. R3 is triggered by unresolved proof identity, irreversible exact-directory deletion and recovery in the actual public/isolated-store caller. Local factual inventory is R0. One non-ranking discovery pass may identify existing direct commands and minimal existing-harness adaptation; freeze those viable options and any hard disqualification before comparative research. A generalized runner is out of scope, not a candidate to develop.

One `critical_researcher` covers the single coupled dimension of caller authenticity, owned run-store isolation, finite external effects and teardown. Supply Research Assignment Capsule v1 with this plan, the relevant existing helper/callers and I1–I9. Limit to one report (about 1,200 useful words) and one targeted follow-up; do not duplicate known source collection. Add another dimension only by recorded scope/budget reconciliation, not automatic staffing. The mandatory `decision_analyst` gets one synthesis plus one bounded correction and returns `DRAFT READY`, `RETURN FOR RESEARCH` or `OWNER DIRECTION`. No drafter is needed. Fresh `critical_research_reviewer` pre-draft review permits one supported outline correction; primary alone writes `M504-G-CONTRACT`; a different fresh research reviewer then checks the complete authored artifact. Maximum two final-artifact correction cycles; repeated decisive gaps/exhaustion stop under the workflow, without resetting budgets.

Common criteria/hard gates: smallest reusable implementation, authentic production crossings, no invented native evidence, exact frozen pair identity, no original-data mutation, bounded public target/call allowance, complete cleanup/failure ownership, testability, no new dependency/product surface. Evidence classes are repository facts, controlled native execution, constructed boundary tests, policy-only vectors and live external observation. They cannot substitute for each other. Decide now: callers, literal commands/paths, fail-fast predicates, allowed mutations, per-command evidence and recovery, and test envelope. Prove later: actual outcome, durable identity, keyboard/visual observation and teardown. Target trust/permission and any extra public activation are owner-controlled; research cannot decide them. Significant new architecture returns to the ADR/owner boundary.

Artifact-local outputs: one compact mapping of I1–I9 to exact caller/input/assertion; all command slots below; source/environment identity; mutation/cleanup allowlist; grant disposition; alternatives and rejection reasons; residual limits. The cumulative invariant packet is the acceptance table below: triggers and expected results are fixed, actual result is Pending until evidence is accepted, G research reviewers own procedural coverage, A critical review owns callers, and C's different critical reviewer owns integrated evidence. Every G material revision or R3 correction reruns the whole packet. Planning-readiness review does not replace either G research checkpoint or implementation review.

## Concrete Steps

Working directory for every command: `C:/projects/a11y-evidence-lab`, PowerShell 7. Read and dot-source the first PowerShell block under [current development preparation](../../../README.md#development-command-preparation), unchanged. Use its `Invoke-M105Command`; do not replay historical task commands or restore/install dependencies by default.

These read-only planning checks already exist and may be repeated after entry reconciliation:

```powershell
git status --short
git rev-parse HEAD
$m504Readme = Get-Content -LiteralPath README.md -Raw
$m504Section = $m504Readme.Substring($m504Readme.IndexOf('### Development command preparation'))
$m504Prep = [regex]::Match($m504Section, '(?s)```powershell\r?\n(.*?)```').Groups[1].Value
if (-not $m504Prep) { throw 'Development preparation not found' }
. ([scriptblock]::Create($m504Prep))
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/run-contract.test.ts tests/comparison-pair.test.ts tests/comparison-outcome.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Focused comparison checks failed' }
}
git diff --check
```

Expected: independently successful strict check and all focused tests passing (117 at planning), no skipped/cancelled cases, no repository/runtime data change. Hash verification compares SHA-256 case-insensitively; do not compare upper-case manifest text with lower-case output using case-sensitive equality.

**Future command slots — NOT executable authorization.** G must replace each applicable slot with an exact invocation or ordered UI action plus validation/failure/cleanup predicates before the associated packet is issued. Primary-run evaluation has the same command-preparation obligation as workers.

| Slot | Values and effects to freeze |
| --- | --- |
| ENTRY | Actual intentional HEAD/dirty set; relevant source/test/config and environment fingerprints; frozen manifests/references; original retained-run inventory/hashes; no active lease; ordinary non-linked scratch and generated-file inventory. |
| GUARD | Exact current guard start/finish commands, owner, workflow/slice/assignment IDs, phase, allowlisted files/generated roots, baseline/digest, timeout and terminal-receipt checks. No blanket Git metadata permission. |
| BUILD / FOCUSED / FULL | Exact current README strict/build and explicit suite commands, proper mocking flag, scanner/UI scratch, browser profile, all optional capture flags absent unless separately named. Build overwrites only inventoried generated `dist/client` files; fixture tests create/delete only their registered synthetic children. No package metadata mutation or install. |
| CONTROLLED | Exact integration invocation and selected tests; native three-pair input hashes and service/readback/UI assertions; synthetic adverse collaborators; original executor-only assertions; exact owned children and per-case teardown. |
| POLICY / UI | Existing policy-test invocation; bounded UI test/capture invocation, fixture identities, viewports, source/build/browser hashes and content-safe ignored evidence destination. No public-target screenshots or raw payload in tracked artifacts. |
| PUBLIC | Owner-granted URL and two-scan limit; exact caller path/hash, entry point, startup/stop, loopback port, isolated ignored run root, modes, real baseline Finding-selection predicate, one rescan action, actual disk/readback/UI checks, no interceptor/provider/dependency acquisition. Until filled: blocked. |
| DELETION / CLEANUP | Exact generated IDs, absolute child directories, ordinary-path/no-reparse checks, expected files/hashes, live synthetic baseline-deletion timing and assertions, final shutdown/closed-port ordering, remaining-child cleanup and unknown-residue stop. No guessed ID, wildcard deletion or broad-root removal. |
| DOCS | Changed-file/link/anchor/UTF-8/final-newline/trailing-whitespace/PowerShell syntax checks, status consistency, exact diff inventory and `git diff --check`. |

A future helper or modified test command must be labeled future until A creates, tests and reviews it. Its invocation, permission boundaries and negative tests are frozen before its lease; final bytes are pinned before B. Do not invent binding commands inside a worker turn. If preparation requires substantial custom tooling, stop and reconsider the direct procedure under PLANS.md rather than grow a framework.

## Validation and Acceptance

Every row starts Pending for M5-04 execution; M504-ENTRY-01 supplies only planning regression/freeze evidence. Record exact commands, input/source/environment identities, actual results, reviewer and primary disposition with each row at acceptance.

| ID / trigger | Required evidence and result |
| --- | --- |
| I1 — declared native pairs | All three unchanged manifest directions pass stable-key/profile binding and native unique same-rule/locator pass with complete coverage. Actual service publication, validated disk readback and saved presentation cross the production boundaries; no vector or callback-only result is called a native scan. |
| I2 — uncertainty/profile adverse cases | Integrated synthetic persistent, missing/ambiguous match, and materially mismatched profile retain exact rationale; absence without positive observation is inconclusive; mismatch skips correlation; no fabricated after-target/evidence. Existing finer-grained pure/service/UI tests complement this small integrated set. |
| I3 — ordered policy | Exact accepted 3.54→4.00 contrast margins improve while still failing; reversed/equal arithmetic behaves correctly. Binary positive→failure reversal stays policy-only, with no public persisted comparison or invented baseline Finding. No third native fixture. |
| I4 — independence/preservation | Scan-only baseline works with zero retrieval/provider operations; synthetic optional proposal/current-review/manual context cannot gate or change the outcome. Both scan collections, baseline bytes, existing proposal/review and sibling data remain unchanged. Later-only Findings remain visible without new/regressed labels. |
| I5 — deletion/lineage | Exact owned synthetic baseline deletion leaves saved later bytes unchanged and comparison viewable with broken-lineage limitation. Separate selectable-Finding positive control proves subsequent action refusal without new run/scan; zero-Finding UI disabling alone is insufficient. No recomputation, cascade or hidden baseline copy. |
| I6 — publication/identity | Service success equals validated canonical durable state; failed publication is not durable success. Baseline/later/Finding identities, current downstream state and immediate-lineage metadata remain correctly bound through HTTP/client admission. Reuse affected M5-03 adverse-path tests with valid identity. |
| I7 — accessible honest UI | Existing semantic before/after, rationale, source distinctions, limitations and reminders; automated actual-service keyboard/focus/shared-status path; bounded desktop/narrow visual inspection. No accessibility/conformance/certification/causality inference, no speech or zoom proof claim. |
| I8 — actual public lane | Exact authorized target, one authentic completed baseline with selected Finding and one later actual scan; saved comparison and UI/readback agree, baseline preserved, zero provider calls, finite consumed allowance and owned teardown. Record real outcome/limits, not a forced expected improvement. No live observation yet. |
| I9 — complete artifact/closure | G commands/authority/grants align; all leases terminal, actual diff/cohesion inspected, protected inputs/original records preserved, no unknown cleanup loss; complete suite, independent strict/build, different final critical PASS and documentation gate. M5-04 status truthful; M6 unstarted. |

Evidence reuse requires the exact command, cwd, relevant-tree fingerprint, environment fingerprint and guard-backed no-drift state specified in the worker procedure. Mutable browser/filesystem/public observations are Non-reusable unless an isolated run identity/state is pinned; historical public observations never prove current behavior. Handoffs alone do not invalidate valid evidence, and passing receipts alone never prove correctness.

Planning/documentation and G research use structural, semantic and independent-readiness evidence, not TDD. Existing behavior uses accepted coverage or passing characterization. Any genuine production correction records behavioral Red, separate-owner Green, unchanged accepted tests, independent strict and full affected review; it never silently converts this verification plan into feature work.

## Idempotence and Recovery

Read-only inventory, static validators and pure tests may repeat with matching identity. A public call is not idempotent: record consumption at invocation, retain failure facts and never replay it as setup or cleanup. Controlled test invocations use fresh registered synthetic children; unexpected pre-existing state stops the caller rather than deleting it. Preserve all fourteen original retained run directories observed at planning and re-inventory at execution; never hard-code that count as permission to remove extras.

Resolve all destructive targets to exact ordinary children of G's isolated application-owned root; inspect ancestors and contents, reject links/reparse points/unknown files or IDs. The sole live deletion is the deliberately registered synthetic baseline while the test needs to observe lineage loss. Final cleanup first closes owned UI/scanner/service resources and verifies port shutdown, then removes only the registered remaining children with known contents; remove an empty task-only parent only when explicitly listed. Never remove `data/runs`, `temp`, the repository, corpus or a user browser/profile. Public artifacts are preserved by default unless G's owner-accepted retention/cleanup disposition explicitly includes their exact generated directories. Report material removals and recoverability.

On crash, timeout, shutdown uncertainty or unknown residue, preserve it; inspect owned process/path identities read-only. Do not kill unrelated processes, overwrite evidence, force-delete locks, reset Git or count uncertain execution as success. Primary reconciles failed commands and remaining grants, closes any lease terminally and renews only within existing scope/budget. Changed source, authority, command, environment or mutable-state assumptions invalidate the corresponding evidence and review. New authority or exhausted limits pauses dependent work, not unaffected checks.

## Artifacts and Notes

The entries below preserve the state at each execution checkpoint, including superseded blockers, pending gates and consumed allowances. [M504-FINAL-01](#m504-final-01--integrated-review-and-task-closure) owns the final accepted disposition; earlier entries are not current authorization or unresolved work.

### M504-EXEC-01 — Execution entry

2026-09-20: owner-authorized execution of this exact task begins at clean HEAD `434d7de121472a076a02b6eca6027059480c6995`; historical planning HEAD is not the current baseline. No active `logs/agent-flow-leases/v2/active.json`. Permissions expose workspace-write with one guarded writer at a time. The configured primary default is Astra/high and callable role metadata exposes the prescribed custom roles/pins; actual primary model/effort is not exposed by runtime metadata, so configuration is not claimed as runtime verification. Pins remain unchanged.

Read-only inventory is retained in ignored `temp/m504-entry.json`, SHA-256 `c371dafc6f120dbff2974e7f9e9668af2f0bae2920cf96f85d48afa3089ef89d`: 208 tracked source/test/configuration/frozen-input file hashes, all fourteen original run path/hash pairs, generated build and browser identity. No original run content was printed or changed. Node `24.20.0`, PowerShell `7.6.5`; Chromium executable `151.0.7922.34`, SHA-256 `409805a16d6416087e6b2f778df1cf8f7bbb267d6b99f6b5bb0a618eace234f2`. Scanner, UI and integration scratch are ordinary empty directories. Existing `dist/client` contains only inventoried `index.html`, `assets/index-C6L8S9Ht.css`, and `assets/index-CqcnJYy1.js`. The entry evidence file is primary-maintained local verification evidence, not a new runner or product artifact.

RD-003, M3-01 and M5-02 manifest hashes match M504-ENTRY-01; thirteen generation and eight comparison recursive path/hash references pass case-insensitively, including all six fixture bytes through the comparison companion. M5-03 remains Complete; selected Must requirements/decisions are Accepted or explicitly Deferred. I1–I9 execution remains Pending.

Fresh execution of the unchanged Concrete Steps entry command passed independent strict TypeScript and 117 focused tests, zero failed/skipped/cancelled, exit 0; `git diff --check` passed. Sorted `path + space + sha256` rows, slash-normalized, joined with LF and no terminal LF, give SHA-256 fingerprints: source/test/configuration (196) `87a1cfb3b1fae23a447d093864512e511fd0674332161cdb52549091b0bc5448`; tracked frozen inputs (12) `20afd7919f18aee2c80520a9590602a1801d94bd223c6c762f8804a10329063e`; three-file build `eae1e020a1047c0b280a66ddcf5a7270b481ce40349ba4669527a89eef0737d9`; fourteen originals `ff4fecd61885bf0b43df3f6e89b8ff20a74c1703c1e6e0d653010e0b6c17a311`. Python is `3.12.10`; the prospective public root does not exist. Existing six M5-03 screenshots are preserved; no reuse claim is made without their complete evidence identity.

The owner answered the explicit two-activation request with `https://docs.ollama.com/capabilities/embeddings`. This task selects only that exact trusted non-sensitive non-authenticated HTTPS URL, one fresh baseline and one intentional later scan from an actual baseline Finding, Local mode for both. Two activations maximum; failures consume allowance. Zero Findings, failure or drift authorizes no retry or alternative. Public artifacts will be preserved. The owner's broader suggestion to find another URL is not used. Public execution still requires G/A acceptance.

G discovery froze two procedure candidates before comparative research: existing commands plus bounded direct developer-managed procedure, or minimal existing-harness adaptation with one opt-in public caller only if needed. A generalized runner is out of scope. `/root/m504_g_research` is the read-only critical researcher for caller authenticity, isolation, finite effects and teardown; one report plus one targeted follow-up. Primary concurrently owns authority and execution inventory. Existing CLI `main.ts` fixes `data/runs`, while the service factory accepts an isolated root; research must account for that actual caller boundary.

### M504-G-CONTRACT — Proof and command contract

Historical public binding: the owner-authorized [M504-PUBLIC-02-CONTRACT](#m504-public-02-contract--owner-authorized-w3c-sequence) superseded the original Ollama target, isolated root and finite activation grant after its review/caller gates passed. Other I1–I9, command, timeout, ownership, cleanup and preservation rules governed both executions. Both public lanes are now closed; their separate results remain preserved in [M504-PUBLIC-01](#m504-public-01--valid-zero-finding-baseline-comparison-unavailable) and [M504-PUBLIC-02](#m504-public-02--actual-selected-finding-comparison-accepted).

2026-09-20, primary-authored after `/root/m504_g_research` returned RESEARCH COMPLETE, `/root/m504_g_analysis` returned DRAFT READY, and fresh `/root/m504_g_predraft` returned PASS with no findings. All three are read-only. Research source facts G-E1–G-E10 establish the current CLI/service boundary, native coverage gap, strict versus legacy cleanup, valid-Finding refusal and cooperative timeout limits; they do not establish runtime success. Research used one report, synthesis one turn and pre-draft zero corrections. A different authored-artifact review remains required.

**Selection and limits.** Select minimal existing-harness adaptation plus one fixed opt-in public caller. Existing commands alone fail isolated-storage and missing service/UI-crossing gates: `src/server/main.ts` fixes `data/runs`, while `startLocalService` accepts an isolated root. A custom inline launcher would itself be new caller code. A generic runner, new CLI option, dependency or production interface is unnecessary. Reconsider only if an existing reviewed isolated caller closes these gaps, or implementation needs a new production boundary. Confidence is high in this source-based choice; actual public availability, Finding selection, outcomes and teardown remain unknown. Target and finite allowance are the owner's M504-EXEC-01 grant.

**Cumulative invariant packet.** All execution results remain Pending. The original I1–I9 requirements above continue to control; this maps each to concrete evidence without substituting evidence classes.

| ID | Caller, input and decisive assertion |
| --- | --- |
| I1 | `tests/rescan-integration.test.ts`: retain the image-alt service/browser/deletion case and separate three-pair executor/private-handoff loop; add label and contrast through actual service, HTTP response, validated canonical disk/readback and built UI. Use only six unchanged frozen buffers, exact directed pair/profile/stable key and native unique positive pass. |
| I2 | Same integration test and existing controlled harness: construct scanner-boundary persistent, missing, ambiguous and profile-mismatch cases; production service computes/publishes the comparison. Never inject a calculated comparison. Assert exact match/outcome/reason, absent fabricated after-evidence, and mismatch correlation skip. |
| I3 | Existing `tests/comparison-outcome.test.ts`: exact 3.54→4.00, reverse/equal arithmetic and binary reversal stay policy-only. No canonical persistence or extra native fixture. |
| I4 | Integrated cases preserve baseline bytes, both scan collections and later-only Findings; synthetic proposal/review/manual context and siblings remain unchanged. Existing comparison service/persistence tests complement this with independent downstream-context negatives. Assert zero retrieval/generation application requests, no new/regressed labels. |
| I5 | Existing native image deletion uses exact registered ID/path/hash and proves later bytes and saved UI survive. Existing `tests/comparison-service.test.ts` valid-Finding positive/refusal case independently proves missing-lineage refusal with zero executor/new run; zero-Finding disabling is not the witness. |
| I6 | Integrated success response equals validated disk aggregate and service GET, including baseline/later/Finding IDs. Existing service/persistence/admission suites retain publication-failure, stale/foreign identity, current-state and concurrency negatives. |
| I7 | One actual-service keyboard selection→rescan→shared status/result→baseline→return path in integration. Existing comparison UI matrices complement it. Capture local application comparison and focus at 1366×900 and 390×844; primary visually inspects both. Existing presentation REUSE_AS_IS; no zoom or spoken-output claim. |
| I8 | Fixed public caller below: actual default scanner, exact owner URL, fresh Local baseline, first actual Finding, one Local rescan, canonical equality and unchanged baseline, actual outcome and teardown. Failure/zero Findings does not pass this row. |
| I9 | Primary accepts actual diff, cohesion, terminal receipts, frozen/original preservation and complete evidence identity; complete suite plus independent strict/build; different fresh final critical reviewer; documentation gate. |

**Ownership and generated effects.** M504-A's four-file maximum is `tests/rescan-integration.test.ts` (scenarios/assertions), `tests/helpers/m105-walking-skeleton-harness.ts` (controlled setup/strict owned teardown), new `tests/helpers/m504-public-comparison.ts` (fixed opt-in actual sequence), and new `tests/m504-public-comparison.test.ts` (negative caller characterization). Existing comparison helpers are read-only. Method-level test mocks preserve the existing integration command without adding module-mock requirements; the new negative suite may use Node module mocks. No production responsibility/interface change or structural refactor is permitted. Primary alone maintains documentation between leases.

Controlled additions create only fresh named children `m504-label`, `m504-contrast`, `m504-persistent`, `m504-missing`, `m504-ambiguous`, `m504-profile` of ordinary empty `temp/m105-integration`. Existing integration cases retain their current exact children and cleanup semantics; legacy cleanup is regression evidence, not strict-cleanup proof. New M504 callers must use strict registered-ID cleanup. Actual generated run IDs cannot be known before scanning: register only validated service-returned/disk-matched IDs, enumerate their absolute direct-child directories, verify `run.json` is the sole ordinary single-link file, and record bytes/SHA-256. Revalidate exact inventory and hashes immediately before each removal; no unknown child becomes owned by enumeration alone. The existing sole live deletion is the synthetic image baseline; no other live deletion is allowed. After owned UI context/browser closure, successful service stop and closed-port check, remove each remaining registered exact child, then its explicitly named empty test-only parent. Shutdown failure, aliasing, changed hash or unknown residue stops deletion. Validation/removal is not atomic against hostile concurrent writers; exclusive trusted operation is required.

Ordinary managed scanner/UI scratch remains `temp/m103-scan` / `temp/m104-ui`; callers may create browser-managed ephemeral children only through the existing managed browser and inspect emptiness afterward. Do not force-remove uncertain residue. Attempt 1's two partial screenshots under `temp/m504-ui-proof` are preserved unchanged. The bounded correction captures only fresh `temp/m504-ui-proof-02/comparison-desktop.png` and `comparison-narrow.png`, using full-page capture at the same two viewport widths with the comparison and visible focus present. Reject pre-existing proof output rather than overwrite it. Capture uses `A11Y_M504_CAPTURE_PROOF=1`; normal regression requires it absent. No raw target payload or public-target screenshot enters tracked content.

**PUBLIC (future until A acceptance).** Only `tests/helpers/m504-public-comparison.ts --public` may execute the public lane. Reject missing/extra arguments before effects; do not expose target/mode/root options. Fix URL `https://docs.ollama.com/capabilities/embeddings`, Local/Local, current entry HEAD and existing built client. Exclusively create absent ordinary unaliased `temp/m504-public-comparison`, with `runs` and `evidence`; reject any pre-existing root and preserve the entire root on success or failure. This store is outside integration scratch and no public cleanup command is authorized.

Start `startLocalService({runRoot, applicationRevision, clientRoot, port: 0, stopTimeoutMs: 5000})`; record its returned loopback URL. Use actual built UI, default production scanners, no route/response interception, fixture substitution, injected outcome, model or provider action. Count consumption immediately before each activation, then one Analyze. Require completed validated baseline with HTTP/disk equality; select `baseline.scan.findings[0]`, its rule group and within-rule index, and verify selected Finding ID in the rescan request and saved comparison. Zero Findings/failure/timeout stops the lane. Execute exactly one intentional rescan. Compare HTTP success, GET and validated disk equality, distinct IDs, baseline/Finding binding, modes/coverage, preserved baseline bytes and later-only scan state. Record actual outcome without predicting improvement. One failed activation still consumes its allowance; no retry, alternate target or third action.

UI/HTTP waits are 15000 ms, browser launch 10000 ms, context/browser closure observations 5000 ms each, service stop 5000 ms and closed-port observation 5000 ms. The caller's enclosing startup observation is 30000 ms and enclosing shutdown observation 25000 ms, accommodating the ordered individual limits without claiming cancellation. Existing production scanner work/cleanup timings stay unchanged. Timeout is uncertainty, not cancellation or process termination. Close owned UI context then browser, stop service, verify closed port and inspect scratch, collecting uncertainty without deleting public state or continuing scans. Bounded ignored evidence may contain IDs, hashes, outcome, method/path counts and shutdown facts; no raw payload logging. Negative tests use mocks and disposable synthetic roots to prove pre-effect argument/root rejection, baseline failure/zero stopping later work, later failure preventing a third action, and timeout/close failure/unknown or changed paths preserving state. Negative-test roots must be exclusive ordinary named `temp/m504-public-negative-*` children created by that test, never the real public root; only their exact validated synthetic files may be removed after their mocked operation is settled.

**ENTRY and command binding.** Use M504-EXEC-01 inventory and its ignored JSON; before B, rehash original and frozen files, source/tests/config, build/browser/lock and all four caller files, recording resulting identities here. Expected source changes are only the accepted four-file envelope; documentation changes are primary-owned. Any unexpected drift stops affected evidence. Public outcomes are Non-reusable; controlled browser state is reusable only with the isolated run/evidence identity pinned. Fresh terminal guard receipts support path ownership, not behavioral correctness.

All commands use the Concrete Steps cwd and unchanged README preparation; strict/build/focused processes run sequentially. Existing dist files were inventoried in ENTRY, so the following build may replace only those generated client outputs and their Vite-produced replacement names under `dist/client`. Verify no unknown file before rebuilding; do not acquire dependencies.

```powershell
# BUILD / independent strict (C; earlier only if stale or absent build).
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed.' }
  & $m105Node node_modules/vite/bin/vite.js build --configLoader native
  if ($LASTEXITCODE -ne 0) { throw 'Client build failed.' }
}
# FOCUSED / CONTROLLED (A and primary B evidence; new suite is future).
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed.' }
  & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/m504-public-comparison.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Public caller negative characterization failed.' }
}
Invoke-M105Command {
  & $m105Node --test --test-timeout=120000 tests/rescan-integration.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Comparison integration failed.' }
} $m105ScanTemp
# POLICY and complementary identity/deletion/publication evidence.
Invoke-M105Command {
  & $m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-outcome.test.ts tests/comparison-service.test.ts tests/comparison-persistence.test.ts tests/comparison-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Comparison policy or boundary checks failed.' }
}
# PUBLIC: only primary after G/A PASS and caller-byte binding, network-enabled.
Invoke-M105Command {
  & $m105Node tests/helpers/m504-public-comparison.ts --public
  if ($LASTEXITCODE -ne 0) { throw 'Public comparison checkpoint did not complete.' }
} $m105ScanTemp
```

For corrected UI capture, use this exact wrapper after unchanged README preparation. `Remove-Item Env:` removes the process variable instead of restoring an empty string. This is a controlled-fixture pass, not extra public scans. It writes only the two named fresh replacement screenshots. Primary may use the A controlled output if complete identity and state are pinned; do not repeat merely because ownership changed. Ordinary full regression has all five capture flags absent.

```powershell
if (Test-Path Env:A11Y_M504_CAPTURE_PROOF) { throw 'Capture flag must start absent.' }
try {
  $env:A11Y_M504_CAPTURE_PROOF = '1'
  Invoke-M105Command {
    & $m105Node --test --test-timeout=120000 tests/rescan-integration.test.ts
    if ($LASTEXITCODE -ne 0) { throw 'Comparison integration capture failed.' }
  } $m105ScanTemp
} finally {
  Remove-Item Env:A11Y_M504_CAPTURE_PROOF -ErrorAction SilentlyContinue
}
if (Test-Path Env:A11Y_M504_CAPTURE_PROOF) { throw 'Capture flag cleanup failed.' }
```

FULL is the exact existing README PowerShell block beginning `if ($null -ne [Environment]::GetEnvironmentVariable('A11Y_M305_CAPTURE_PROOF','Process') -or`, currently UTF-8 block SHA-256 `9545fe02707982c6ddff38c2cc839627169e3081a57c0f203c3042b8143e1088`, with two primary-owned changes after A acceptance: add absence check for `A11Y_M504_CAPTURE_PROOF`, and append `tests/m504-public-comparison.test.ts` once to the module-mocked comparison list. All existing 44 explicit files and per-list scratch/flags/order remain unchanged. Parse and bind the resulting 45-file block before C. This prescribed edit is part of the command contract, not worker discretion. Run the complete block once at C; conditional repeat requires concrete failure/drift.

**GUARD.** Workflow `M5-04-20260920-01`, task `M5-04`, cycle `M504-A`, initial lease `M504-A-evidence-01`, owner `m504_test_worker`, role `test_worker`, phase `evidence`, attempt 1, no correction parent. Preflight uses no lease. After preflight acceptance, primary executes:

```powershell
python -B .codex/leases/lease_guard.py start --workflow-id M5-04-20260920-01 --task-id M5-04 --cycle-id M504-A --lease-id M504-A-evidence-01 --phase evidence --attempt 1 --owner m504_test_worker --agent-type test_worker --allow-file tests/rescan-integration.test.ts --allow-file tests/helpers/m105-walking-skeleton-harness.ts --allow-file tests/helpers/m504-public-comparison.ts --allow-file tests/m504-public-comparison.test.ts --forbid-dir-root src --forbid-dir-root docs --forbid-dir-root fixtures --forbid-dir-root evaluation --forbid-dir-root corpus --forbid-dir-root data
```

Require exit 0, `status: started`; insert its exact returned digest and endpoint in packet v2 before dispatch. Generated ignored effects are only those above and outside guard proof. No Git writes or metadata mutations. After worker stops, primary runs `python -B .codex/leases/lease_guard.py close --lease-id M504-A-evidence-01 --contract-digest <returned-digest>`; the placeholder is runtime binding, not a value to invent. Require exit 0, fresh `closed-compliant`, `already_closed: false`, then inspect actual diff/evidence. Use same pinned `verify`/`status` commands only as documented. Corrections use terminal parent and attempt 2 (conditional 3) under existing budget; primary records exact updated identity before start, never resets the counter. Tool wait polling is at most 60 seconds; command/test deadlines above do not imply kill permission.

**DOCS and acceptance.** Primary checks every changed document in full as required, local links/anchors, UTF-8, terminal newline, trailing whitespace, changed PowerShell block syntax, roadmap/status consistency and exact changed-path inventory; run `git diff --check` and inspect new untracked test files separately. G authored PASS permits A preflight only. A requires terminally closed caller work, meaningful negative/controlled evidence, primary diff/cohesion acceptance and fresh `critical_reviewer` PASS before PUBLIC. C uses a different fresh critical reviewer, full I1–I9 audit and documentation gate. Public failure preserves I8 Pending and does not prevent unaffected C checks. No M6 execution or task Complete/archive before every required row passes.

### M504-G-ACCEPT-01 — Procedure accepted

2026-09-20: different fresh `/root/m504_g_authored` returned authored-artifact PASS, no Blocker/Major/Minor, for candidate plan SHA-256 `f9ac6ac0199c67bee834228a7d8d0534d0fa1bdf118bec6f7e1d7a9b4069c491`. It audited the entire I1–I9 packet and independently reproduced all 208 entry file hashes, fourteen original run hashes/inventory, three build files, browser/manifest/lock identities, no lease/public root, FULL block hash and 44 unique files, PowerShell syntax and diff checks. No drift or evidence invalidation. Primary accepts G. Subsequent administrative status recording changes no accepted procedure. Runtime results and caller implementation remain Pending; public allowance 0/2. No research correction used.

Primary documentation checks pass changed-document UTF-8/newline/whitespace/local links/anchors and PowerShell syntax. The initial fence validator matched an embedded quoted delimiter; anchoring closing fences corrected that validator without document changes. A requires its own preflight, lease and S3 caller review; G PASS is not execution evidence.

### M504-A-PREFLIGHT-01 — Existing behavior lacks integrated proof

2026-09-20: persistent `test_worker` `/root/m504_test_worker` returned EXISTING_BUT_UNCOVERED, no lease/writes/runtime effects. It verified unchanged HEAD and four expected primary documentation edits, actual service/default-scanner/publication seams, existing native image and executor-only pairs, synthetic/policy versus integrated gaps, existing UI accessibility versus actual-service gap, and absence of the two new caller/negative files. The four-file G envelope suffices without production changes. Primary accepts passing characterization, not Red; no Green owner is needed absent a demonstrated defect.

Packet v2 binds workflow `M5-04-20260920-01`, cycle `M504-A`, test owner `m504_test_worker`, S3, G's four-file responsibilities, commands, effects and finite correction budget. Initial evidence assignment/lease is `M504-A-evidence-01`; exact guard digest is supplied in the assignment after successful start. Optional two-image capture uses G's fresh proof root during the controlled pass; primary will independently inspect images and reuse only fully bound isolated evidence. Public execution remains primary-only after caller PASS. No authority or semantic binding changed during preflight.

### M504-A-EVIDENCE-01 — Closed attempt, correction required

2026-09-20: lease `M504-A-evidence-01`, digest `28abc322bed62bde6055bf3a52752429725d6c59ed69457e4a91f7ae5fc814f0`, closed freshly with exit 0 and `closed-compliant`; receipt `d891d56748b4693e942cc7a7b5d38ba75638e4f0ffd14aaf57f6bc86815f8534`. Only the four allowed test files changed; no forbidden/unleased path, Git endpoint/index/ref or ignore drift. Primary inspected the actual diff and new caller/test files. New responsibilities remain test-only; no production defect was demonstrated.

Worker evidence: after a corrected TypeScript narrowing error, strict plus five caller tests passed. Integration initially failed one duplicated text locator, then five tests passed after narrowing its UI scope. Native label/contrast reached service/HTTP/disk/GET/UI; constructed persistent/missing/ambiguous/profile cases passed production composition. The subsequent capture test body also passed five tests, but its wrapper exited 1 because the flag restoration produced an empty value. Explicit removal then passed and all three scratch roots were empty. Final caller timing/HEAD/count changes and screenshot-only `fullPage` changes occurred afterward: those source versions lack current strict/negative/integration evidence. Primary does not accept stale results as the final boundary.

Primary visually inspected both preserved first captures: desktop contains visible focus but no comparison; narrow contains only part of the comparison and no focused control. SHA-256: `temp/m504-ui-proof/comparison-desktop.png` `c02fac22b594d9dc7917628edf59ab2a4fee35f1223b7ff4b789c85d07882427`; narrow `319a40952663e9ca9b6f456b8cefb002420b2d01b9d46f59d0a4d239a286da4d`. They remain partial evidence, not accepted I7 proof. Primary's isolated process flag set/`Remove-Item Env:`/absence check passes without a test/browser launch; it independently confirms empty integration/scanner/UI scratch. No public root or activation exists.

Terminal caller SHA-256: integration `68704c8c5aa5f7a048775f4d06574058db0cc9774f170ee26207e3840b15db96`; harness `cbd37df897947e74106e0b52d04b2b876972c3c5594573d169cc28f6b8dddcaf`; public caller `169d27954d56f03a33b2c78cf22b29e6af36b815776418ad247b783b4e021997`; negative suite `4445257aeb29f532f5d366978fd5478f11fdf371578c09eb2eb672f2f529b9cf`.

Correction disposition: preserve first captures, use two fresh full-page captures under `temp/m504-ui-proof-02`, and use the explicit flag-removal wrapper. Freeze enclosing caller startup/shutdown observations at 30000/25000 ms without changing individual limits or scanner behavior. This bounded command/output refinement receives the same authored research review's whole-packet recheck (correction cycle 1). Fresh S3 caller review runs before attempt 2 so its source findings can join this single correction. No new path owner, product interface, public effect, or budget is selected. Attempt 2 requires current strict/negative/controlled capture proof; public remains gated.

Correction-contract review: `/root/m504_g_authored` returned PASS, no findings, after whole I1–I9 audit of plan SHA-256 `44f19dc22f88d9c9e1a59957cbdb9f516a144f18952a2373c220c7b9355d1c5b`. It independently reproduced four caller and two screenshot hashes, absent new/public roots and active lease, empty scratch, compliant receipt, unchanged production/frozen/original/build/browser inputs, four PowerShell syntax checks and FULL hash. Primary accepts this refinement; one authored-artifact correction remains. Primary also ran independent strict TypeScript on the terminal four-file candidate with unchanged README preparation: exit 0. This does not replace missing current negative/integration/visual proof.

### M504-A-REVIEW-01 — Caller correction contract

2026-09-20: fresh `critical_reviewer` `/root/m504_a_review` returned REVISE, no Blocker, five Major and one Minor. It independently inspected all four files, matched their terminal hashes and receipt, and reproduced three failures through unchanged function bodies with in-memory stubs, without filesystem writes or actual service/browser/network effects. Primary accepts these findings against the new test caller, not as product defects:

1. Late service/browser/context acquisitions can escape an already-started shutdown. Preserve ownership through settlement, clean acquired late resources, prevent further startup/scan progression, and retain truthful uncertainty. Witness each delayed acquisition separately through the actual sequence.
2. Failed Analyze/rescan clicks leave response waits unobserved; context closure can produce unhandled rejection. Observe both promises from creation, and test both failed-click/response-rejection paths with no retry or skipped cleanup.
3. Nonempty scanner/UI scratch still permits `ok: true`. Require verified empty scratch for success; test each residue and inspection-failure case separately and preserve it.
4. The public label group is incorrectly named `Form controls`; the existing UI says `Form labels`. Exercise actual selection for all three groups, within-rule index and submitted Finding ID.
5. Heading/navigation checks alone do not establish public UI agreement. Bind displayed run/Finding identities, outcome/reason/rationale and available before/after evidence, plus applicable later Findings, to canonical data; wrong displayed identity/outcome must fail.
6. Add extra/wrong-argument and unsafe/aliased-root pre-effect negatives. Separate unknown/changed-state preservation from simultaneous shutdown failure.

Ordinary attempt 2 stays within the same four files, roles, test responsibilities, commands, individual/enclosing deadlines and side effects. Small test-only seams or standard module mocks may exercise the actual sequence; do not replace it with a wholly fake sequence for its own ownership/selector/UI assertions. No product hook, extra module or generic runner is authorized. Preserve all existing assertions and first images; use the approved fresh replacement capture path. Current strict, negative, controlled capture and subsequent reviewer recheck are required. One review-driven correction loop uses this same attempt; conditional 3 is not pre-authorized. The bug index confirms routine corrections contained in this task do not need a separate bug record. I4's existing downstream-context/sibling tests will be mapped explicitly at evidence acceptance/full regression.

### M504-A-EVIDENCE-02 — Corrected characterization and visual proof

2026-09-20: lease `M504-A-evidence-02`, digest `e3c05280c7619b88b8660a0033a4f69130b8c9f69063532e39ad79514e656f6e`, closed freshly with exit 0 and `closed-compliant`; receipt `22066f4482fd910f3987f8d3d640da97ffee7e8963dc9f55e1b4588bae1ecd45`. Three allowed paths changed in this turn; the existing harness remained unchanged. No forbidden/unleased path or Git metadata drift. Primary inspected the complete candidate and both full-page images. No public activation occurred.

Current strict TypeScript and eleven negative tests pass, including late-acquisition ownership after shutdown returns, paired click/response rejection, all rule selections, exact arguments/root rejection and separate scratch/state preservation failures. Five controlled integration tests pass with the shared canonical UI assertion. The capture wrapper exits 0, its flag is absent, scanner/UI/integration scratch is empty, and public/negative roots are absent. The negative suite's last assertion-only edit was followed by its own current strict/test run; integration inputs did not change afterward. Caller re-review still determines acceptance; a worker's claim about native evidence fields is not independently sufficient.

Final file SHA-256: integration `4cedc3bd3c1697f1a2de515173131040e001204dce3e6c08a262886eca187eb0`; harness `cbd37df897947e74106e0b52d04b2b876972c3c5594573d169cc28f6b8dddcaf`; public caller `3960886ec189056e2f1902057ce4b0efd677aeb0a04feb7f17184f50cfb4e57b`; negative suite `1fa9e7ae47b4b400e4a6d0aebb95a343dc68fae5e9863d9f93675817ae8df0f6`.

Primary visual PASS for `temp/m504-ui-proof-02/comparison-desktop.png` (1366-pixel viewport, 150530 bytes, SHA-256 `fc20d3ded11e79c5725c286afd1d23daccdb1a63062673e068e978fe3296733e`) and `comparison-narrow.png` (390-pixel viewport, 159700 bytes, SHA-256 `b5e62c40529439700acc6400a92d2febddb5101e41bfaeee929e21022db19c6d`): complete comparison, Before/After provenance and label evidence, readable single-column narrow layout and visible Return-to-baseline focus. The fixed label transition shows explicit-label No → Yes and a resolved unique native pass. No clipping or overlapping content was observed. These are visual observations at 1366×900 and 390×844 viewport settings with full-page capture, not speech or 200% zoom evidence. First partial images remain unchanged.

### M504-A-CORRECTION-03 — Conditional final correction justification

Attempt 2 made concrete progress: findings 1–4 and 6 now pass independent caller review, current strict/eleven-negative/five-integration proof and complete visual samples exist. The remaining Major is narrower than the former heading-only check: provenance, locator, kind, outcome and rationale are checked, but the reviewer executed the actual assertion with a valid improved-contrast observation and established zero reads of native `evidence`. Its broad repeated-text searches also do not associate values with field labels. This is material new evidence explaining why the previous assertion expansion was insufficient.

Authorize conditional evidence attempt 3, same workflow/task/slice/role, four-path envelope and acceptance contract, parent `M504-A-evidence-02`. Different bounded action: inspect the existing RuleEvidence/ComparisonDetail field contract, assert canonical values against each rendered field label within Before/After, including available native fields and uncertainty, and applicable incomplete reason/contrast delta. Add direct wrong-value/missing-field witnesses using the actual assertion; a locator-only omission is insufficient. Avoid importing a renderer that would supply both expected and actual values. Keep the helper test-only and small; no product/interface/module addition or generic framework. This should close the gap because the oracle will now depend on the canonical native values and their displayed field association, and the real service/browser integration exercises that same assertion.

The one review correction loop continues; counters do not reset. Final write turn only, no further correction after an unsuccessful attempt 3, two identical decisive failures without new evidence or two no-diff handoffs. The same owner authorization and `.codex/execplan-implementation-workflow.md#corrections-and-exceptions` permit this conditional turn without another owner approval. Run unchanged README preparation, strict/negative and normal controlled integration with all capture flags absent. Preserve both screenshot generations; no new capture or output path is authorized. Existing visual observations remain tied to their exact unchanged application/build/native fixture/viewport state; new assertions require fresh integration runtime proof. No G command, effect, path or expected outcome changes. Public remains gated at 0/2.

### M504-A-EVIDENCE-03 — Final native-field characterization

2026-09-20: conditional lease `M504-A-evidence-03`, digest `9ddeccd5bff87b0b00b3c72977ddbd2cedc06c9ac37d2c7617ca93c1029df243`, freshly closed-compliant with exit 0; receipt `a77f49e1e1c5f1d8a29953a3563cdcb354d47027efe21354a62321a3ea5426d8`. Only the public helper and its negative suite changed. Their final SHA-256 values are `ea26dd6e97d1e61a43071d1a093541534c9fcae997bd900ccf9a16235c857a33` and `1aa8c3a91a59c99ff51a444d4e8d06aa59d4bd201d82e03db284bc4ea07dfc0f`; integration and harness retain M504-A-EVIDENCE-02 hashes. No forbidden path, Git metadata or public effect changed.

Primary inspected the actual assertion and negative cases. A test-side oracle independently formats native facts, associates expected text with the exact field-label parent, reads checks, verifies displayed incomplete reason and applicable contrast delta, and retains identity/outcome/locator assertions. Direct negatives reject wrong associated Input type, missing Explicit label, wrong unavailable evidence and wrong contrast Change. A valid incomplete and improved-contrast presentation pass. This does not claim every hidden native field is displayed; checks are source validation, while rendered fields are compared to the UI.

After correcting an optional-delta TypeScript narrowing error, the unchanged README preparation plus FOCUSED strict/negative command passed: twelve tests, zero failed/skipped/cancelled, exit 0. Normal CONTROLLED integration then passed all five tests with capture flags absent, using the strengthened assertion against the actual unchanged built UI. Label baseline `run-a6ffd3a4-e33d-4f75-abaf-a063d18fffbb` hash `f88de435c471e02da7683a3ea6272d335c24bce810d8a7fbd39e15a58a1e375d` and later `run-b31aadb1-aa50-45c0-bcd0-de85742cd913` hash `74cfe0af879a82e3d7ab8bdb2a0a66648e6a843a028421b350827782c0e03984` identify this controlled execution. Those registered synthetic records were removed after owned shutdown under G; both screenshot generations remain unchanged. All three scratch roots are empty, public/negative roots are absent, and no active lease remains. Terminal critical caller review and full regression remain required; no worker allowance remains.

### M504-A-REVIEW-03 — Exhausted correction boundary

2026-09-20: terminal critical caller review returned REVISE, one Major and no Blocker. All original findings 1–6 are resolved, but the new oracle asserts image `elementKind` is available and equals `img`. The canonical validator admits `{ unavailable: "missing" }`; the existing UI correctly labels this rule's element as Image without requiring that available fact. Independent no-write reproduction confirmed `validateRun` succeeds and the caller assertion rejects before any UI disagreement. This is a test-caller defect, not a demonstrated application defect.

Attempt 3 is consumed. The primary requested an explicit exception for one additional guarded worker correction and reviewer recheck: remove that extra admission condition, add a valid-unavailable-image witness and rerun affected checks, preserving every other scope/public limit. No exception is assumed from elapsed time. No new assignment ID or agent may reset the exhausted chain. Public allowance remains 0/2, A/B remain unaccepted, and unaffected current-command regression plus independent strict/build may proceed. The README's prescribed 45-file command edit remains gated on A acceptance; the unchanged 44-file command and current twelve-test caller evidence are distinct until then.

### M504-VERIFY-01 — Unaffected verification and incomplete evidence mapping

2026-09-20: after terminal attempt 3 and without another lease, primary executed the unchanged maintained README FULL command, exact block SHA-256 `9545fe02707982c6ddff38c2cc839627169e3081a57c0f203c3042b8143e1088`, after unchanged preparation. All 44 explicit files completed, every invocation checked exit status, the enclosing command exited 0 and final scratch checks passed. Output filtering omitted one per-file count from retained tool output, so this record reports the verified file coverage rather than inventing an aggregate test count. The separate current twelve-test caller run is M504-A-EVIDENCE-03; it does not cover the reviewer's newly reproduced unavailable-image case. No public or provider action ran.

Primary independently ran G's strict TypeScript and Vite build command, exit 0, after confirming the exact three-file ordinary build inventory. The regenerated three files are byte-identical to entry. All fourteen original run hashes and inventory, browser, protected source/configuration/frozen files remain unchanged except the two existing test paths explicitly allowed by A. Current source/test/configuration fingerprint (198 rows, including two new test files, same sorted path/hash algorithm as entry) is `74f0a8ee9aef525a4ef31d21e5cf48123fafd2ed114818ee7f4afa6f1db54050`. All thirteen generation and eight comparison recursive references were independently checked. Scanner/UI/integration scratch is empty, all capture flags absent, both screenshot generations preserved, and no public root or active lease exists.

| Row | Current primary disposition and evidence boundary |
| --- | --- |
| I1 | Controlled proof passes: existing native image service/disk/browser case, new native label/contrast service/HTTP/disk/GET/UI cases, and retained separate three-pair executor/private-handoff loop. Six frozen buffers and profiles are unchanged. |
| I2 | Constructed scanner-input persistent, missing, ambiguous and profile-mismatch cases pass through actual comparison calculation/publication/UI. Missing/ambiguous evidence is inconclusive; profile mismatch skips correlation. These are explicitly synthetic, not live-page outcomes. |
| I3 | Full regression includes exact policy-only contrast 3.54→4.00, reverse/equal and binary reversal in `tests/comparison-outcome.test.ts`; none is persisted as a new native/public evaluation. |
| I4 | Integrated scan-only flows prove zero retrieval/generation requests, complete baseline bytes preserved and later-only Findings visible without new/regressed labels. Existing populated-baseline integration preserves sibling Findings and manual observations. `rescan-service.test.ts` admits downstream Finding states; `comparison-pair.test.ts` retains reviewed state; `comparison-service.test.ts` permits downstream changes while protecting immutable native source; `comparison-persistence.test.ts` asserts the entire aggregate excluding its appended comparison is unchanged. `comparison-ui.test.ts` distinguishes original AI/current human context and prevents stale metadata replacing newer review state. These optional-context witnesses are controlled tests and static separation, not new real-provider observations. |
| I5 | Existing native image integration deletes only its registered synthetic baseline, preserves later bytes and displays broken lineage. The separate valid-Finding service positive/refusal case proves a missing immediate link causes zero executor calls/new run; deletion-during-scan retains completed scan without comparison. No original/public record is deleted. |
| I6 | Integrated HTTP success equals validated canonical disk and GET. Full service/persistence/admission suites retain publication, stale/foreign/current-state, reservation and shutdown negatives. |
| I7 | Actual-service keyboard selection/rescan/status/baseline-return passes; primary accepted full desktop/narrow comparison/focus captures in M504-A-EVIDENCE-02. Broader UI matrix passes. No spoken-output or 200% zoom evidence is claimed. |
| I8 | Not executed. A caller review remains REVISE; exact URL grant remains 0/2. Controlled success cannot substitute for this row. |
| I9 | Partial: compliant three-lease chain, scoped actual diff, unchanged protected data, current regression/strict/build and cohesive test-only placement verified. A/B acceptance, 45-file command promotion, different final task PASS and completion/archive remain pending. |

Documentation impact at this boundary: update this active plan, roadmap, plan index, progress record and progress index to the explicit Blocked state. Root README's command remains unchanged because its prescribed promotion requires A acceptance; product capability, requirements, ADRs, UI authority, frozen artifacts and instructions do not change. The task remains active and unarchived. No M6 task, commit or push is authorized or performed.

### M504-HANDOFF-01 — Independent incomplete-handoff review

2026-09-20: different fresh `critical_reviewer` `/root/m504_final_review` returned REVISE for task closure, with one Major, no Blocker and no Minor; the explicit blocked handoff is acceptable. It independently reproduced the valid-unavailable-image rejection, found no additional material cohesion/scope defect, and confirmed the required owner boundary without granting another correction, lease or public activation.

Independent checks matched all four caller hashes, the compliant three-lease chain, 208 entry files with only the two expected existing-test changes, fourteen original records, build/browser identities, thirteen generation and eight comparison references, both screenshot generations, and the unchanged 44-file command. Scratch is empty, public root and lease absent, and `git diff --check` passes. Runtime suites were inspected as primary/worker evidence, not repeated by the reviewer. Public availability/outcome/teardown remain unproved; cooperative timeouts and the trusted-filesystem boundary remain limitations.

Primary handoff documentation checks pass across exactly five changed Markdown files and four test/helper files: UTF-8 without BOM, final newlines, no extra EOF blank lines or trailing whitespace, all 308 local links/anchors, four plan PowerShell blocks, unchanged maintained FULL hash, exact change inventory and roadmap counts of 23 Complete / 1 Blocked / 4 Not started. The final review confirms the status mirrors and README command gate are consistent. This is documentation closure for an incomplete handoff, not task Verification success: M5-04 remains Blocked and active, with no archive, M6 work, commit or push. The pending owner exception request remains unanswered and cannot be inferred as approval.

### M504-OWNER-EXCEPTION-01 — Explicit additional correction allowance

2026-09-20: the owner approved the pending request for one additional narrowly scoped worker correction and reviewer recheck. This explicitly adds one write turn to the exhausted M504-A budget; it does not erase or reset attempts 1–3, approve an unsuccessful caller, increase the two-scan grant or change any other scope. The exact correction removes the invalid available-`img` prerequisite in the test-side UI oracle and adds a valid-unavailable-image witness. Apply that same semantic fix to both Before and applicable After observation checks through their shared helper. No production, authority, dependency, fixture, public target or runtime command change is authorized.

The unchanged guard accepts only a three-attempt chain. Represent this separately approved allowance as workflow `M5-04-20260920-01`, task M5-04, semantic slice M504-A, guard cycle `M504-A-owner-exception-01`, lease `M504-A-owner-evidence-01`, phase evidence, guard attempt 1 with no machine parent. Its semantic predecessor is terminal `M504-A-evidence-03`, receipt `a77f49e1e1c5f1d8a29953a3563cdcb354d47027efe21354a62321a3ea5426d8`. The new identifier records an explicit owner-granted budget exception, not a fresh ordinary allowance: no attempt 2/3 or further write turn is authorized. Do not modify guard code or prior receipts.

Only `tests/helpers/m504-public-comparison.ts` and `tests/m504-public-comparison.test.ts` may change. Reuse the persistent test worker and existing oracle/fixtures. Run G's unchanged FOCUSED strict/negative and normal CONTROLLED integration commands, without capture flags or new screenshots. Primary then closes the lease, inspects the actual diff, requests the approved critical caller recheck and, if accepted, promotes the exact prescribed README 45-file command before B/C. A different final reviewer may continue the same integrated task review after remaining evidence exists; this grants no extra implementation correction. Original protected identities, HEAD, empty scratch and absent public root/lease were rechecked before this assignment. Existing evidence is preserved; changed caller evidence must be refreshed.

### M504-A-OWNER-EVIDENCE-01 — Narrow correction verified

2026-09-20: lease `M504-A-owner-evidence-01`, digest `74a9405e0110ace05509453c8a57835faae2a2017591ab2e1e22f7fe6af40c14`, freshly closed-compliant, exit 0; receipt `73def7386ff5a2411fc1263be0f869b0e5d8b87846e313e274639ae4ca46927a`. Only its two authorized files changed, with no Git metadata or unleased drift. Helper SHA-256 `e1a355b4ff31f7073e7c37fd92ae6442540c7409b8abf7b4bd6e6c09a8b6deb4`; negative suite `7795520182e2bc942cc79ce72075e36456cf82bb014c76cce142bca99886cdad`. Integration/harness hashes remain unchanged. Primary reconstructed the previous helper bytes by reinserting the single removed assertion and matched the prior hash exactly.

The new production-projected comparison witness gives both retained image Findings unavailable element-kind facts, validates the canonical aggregate and passes the shared UI oracle. Wrong Before Alternative text and missing After Alternative text still fail. An initial fixture mutation omitted the matching retained later Finding and was correctly rejected by aggregate validation; rebuilding from baseline/later through the existing production projection corrected the fixture. No application defect occurred.

Unchanged G FOCUSED strict plus thirteen caller tests and normal CONTROLLED five-test integration pass, exit 0, zero failures/skips/cancellations. All capture flags are absent, three scratch roots empty, public/negative roots absent and both screenshot generations unchanged. No public activation occurred. Primary accepts the narrow semantic diff and current focused evidence, subject to the approved critical caller recheck; the extra write allowance is consumed.

### M504-A-ACCEPT-01 — Caller accepted for bounded public execution

2026-09-20: approved critical caller recheck returned PASS with no Blocker, Major or Minor. Independent byte reconstruction, production-validated unavailable-image Before/After positives and wrong-field negatives close the remaining finding without changing activation/resource/cleanup semantics. It matched all four file hashes and compliant terminal receipt, empty scratch and absent public root/lease. Primary accepts A and its controlled evidence. This does not pre-accept public results or grant further corrections.

Pre-B identity checks reproduce all protected source/configuration/frozen entries, fourteen originals, build/browser and thirteen/eight recursive frozen references. Current 198-row source/test/configuration SHA-256 is `88ccdfc943cb5f5a943d7617a232a58e11b1f5c59761ca4c415ecfdac908c935`. The public executable helper is pinned to `e1a355b4ff31f7073e7c37fd92ae6442540c7409b8abf7b4bd6e6c09a8b6deb4`, current entry HEAD and unchanged three-file build. No test/service process or lease overlaps B. Only the exact owner URL, Local baseline and Local selected-Finding rescan are authorized, at most two activations, with failures consuming allowance and no retry/alternate target. Use the network-enabled command initially, as frozen in G, without weakening browser isolation.

After A acceptance primary applied the exact prescribed README promotion: fifth capture-flag absence check, new caller negative suite appended once, and the forty-five-file description. All existing 44 files retain their order/flags/scratch. Its parsed command hash is bound at C before execution; this is the existing G command contract, not a new runner.

### M504-PUBLIC-01 — Valid zero-Finding baseline; comparison unavailable

2026-09-20: primary executed the exact reviewed `tests/helpers/m504-public-comparison.ts --public` command once with the network-enabled permission boundary, unchanged README preparation, pinned caller/HEAD/build and empty scratch. The actual production scanner completed a Local baseline of the exact owner URL `https://docs.ollama.com/capabilities/embeddings`. It returned zero Findings and twenty separate scanner review observations. The caller correctly stopped without an intentional rescan, reported `baseline-has-no-findings`, and the checkpoint wrapper exited 1. This is a valid completed zero-Finding scan, not a failed scan or an accessibility verdict; review observations are not selectable Findings and cannot satisfy I8.

Exactly one scanner activation and one `POST /api/runs` occurred; no `POST /api/rescans` or retrieval/generation/review request occurred. UI context/browser closure, service stop, closed port `65048` and both scratch-emptiness checks all report true. The response, validated canonical disk and GET matched before the zero-Finding stop. Baseline ID `run-b7fbdbe8-bc03-4306-b4f4-6cdffe15da89`, canonical SHA-256 `d883b7d34403a226e9927c1df7145b6153a1551ceb8f401a8baf97d27a51a9e1`; ignored `temp/m504-public-comparison/evidence/result.json` SHA-256 `1c96250dc8dcf116557f3dd49b1d15019c87ea23bf58480f9a279fd4ef8ad393`. The entire public root is preserved, containing only that result file and the run's single `run.json`. No comparison is stored. No retry, alternative or remaining activation is inferred from this outcome.

Primary proposed, but has not executed or authorized, one bounded fixed-caller amendment and two additional activations on `https://www.w3.org/WAI/demos/bad/before/home.html`, with a fresh isolated root and preserved Ollama evidence. The [W3C overview](https://www.w3.org/WAI/demos/bad/) identifies the page as its deliberately inaccessible learning/evaluation demonstration. A read-only source check confirmed that exact page; it was not an application scanner activation or a guarantee of runtime Findings. Owner approval remains required before changed G binding, worker lease or new public effects.

C command binding: the prescribed promoted README FULL block parses and names 45 unique files; SHA-256 `457a0dec831a04e55b0138ac56029db545bd8f7463e10ffadb184009997025e3`. Primary started it after the public caller fully shut down, with all capture flags absent and no active lease. Its results are recorded only after completion.

### M504-PUBLIC-02-CONTRACT — Owner-authorized W3C sequence

2026-09-20: the owner explicitly approved one bounded caller update/review and two additional production-scanner activations on `https://www.w3.org/WAI/demos/bad/before/home.html`: one fresh Local baseline and one intentional Local rescan from its first actual Finding. The entire Ollama root and its result/run hashes in M504-PUBLIC-01 remain protected. That prior lane is closed, not replayable; its unused rescan allowance does not carry over. W3C allowance starts at 0/2, failures count, and no subsequent retry or alternative is authorized.

This literal-only amendment retains the accepted G procedure and cumulative I1–I9 packet. Fix the new caller root to absent ordinary unaliased `temp/m504-public-comparison-02`, still outside integration scratch, with exact `runs` and `evidence` children. Preserve the entire new root on success or failure. Do not delete, overwrite or reuse `temp/m504-public-comparison`. Before execution verify both historical Ollama file hashes, absent new root, current HEAD/build/browser/source identities, empty managed scratch, absent capture flags and no running test/service or active lease. The unchanged caller command remains `tests/helpers/m504-public-comparison.ts --public`, network-enabled initially after review. No target/root arguments or additional caller abstraction are introduced.

The only worker write is replacing the existing `publicTarget` and `fixedRoot` literals in `tests/helpers/m504-public-comparison.ts`; all other helper bytes and all tests stay unchanged. Existing negative fixtures read the exported exact target and therefore exercise the revised target without a second implementation. Use the existing test owner, same semantic M504-A slice, workflow `M5-04-20260920-01`, guard cycle `M504-A-public02-owner-01`, lease `M504-A-public02-evidence-01`, phase evidence, guard attempt 1 with no machine parent. This is a separate explicitly granted single turn, semantically following terminal owner-exception lease `M504-A-owner-evidence-01`; it creates no ordinary correction allowance. No second write turn is authorized.

Before opening that lease, the original authored-contract critical research reviewer must use its final correction cycle to audit this amendment against the entire I1–I9 contract and preserved evidence. After the literal change, run unchanged FOCUSED strict/thirteen-negative checks; the current full regression's controlled integration remains valid if byte reconstruction proves only these two non-controlled-input literals differ and all shared oracle/lifecycle/controlled fixture inputs are identical. No screenshot rerun is needed for unchanged application/build/visual state. Primary closes the lease, independently reconstructs the old helper bytes and obtains the owner-approved critical caller recheck before public activation. Rebind the helper hash and current source fingerprint. The final C review must distinguish the prior zero-Finding lane, the actual W3C observation, the unchanged controlled evidence and the affected refreshed caller tests.

Both W3C scans target this same before-page; no corrected page, fixture response, injected outcome or synthetic Finding is used. The W3C demonstration provenance does not guarantee the application will find a selectable Finding or any particular comparison outcome. Actual default scanner, HTTP/disk/GET/UI checks, first-Finding identity, zero provider work, bounded startup/shutdown, method/path counts and preserved run bytes remain exactly as G. Its developer purpose is supported by the [W3C overview](https://www.w3.org/WAI/demos/bad/) and [exact page](https://www.w3.org/WAI/demos/bad/before/home.html), read without an application scanner activation. No new production capability, evaluation input, dependency or M6 work is selected.

Authored-contract correction cycle 2: `/root/m504_g_authored` returned PASS, no remaining findings, after the complete I1–I9 audit of plan SHA-256 `7d49a2e2d60197d62f3af4250f8f52348bbd62bf3506dc0b4531ffb33f5d8b4f`. It independently matched the accepted helper, both Ollama artifacts, protected originals/build/browser/entry files and 45-file command, verified absent new root/lease and empty scratch, and inspected the controlled oracle's independence from the two literals. Stale current-summary phrases were reconciled during that review without changing the operative amendment. Primary accepts the revised binding; the final G correction review is consumed. Literal implementation, focused refresh and critical caller acceptance remain required before activation.

### M504-REGRESSION-01 — Complete promoted regression

2026-09-20: primary executed the exact promoted FULL block bound in M504-PUBLIC-01, after the Ollama service/browser had fully closed. PowerShell accounting verified all 45 file invocations and 881 tests passed, zero failed/cancelled/skipped; command exit 0 and final scratch checks passed. The unchanged README preparation supplied fail-fast command handling and exact environment restoration. All five capture flags were absent. This supersedes the incomplete output-count accounting of the earlier 44-file run and includes the accepted thirteen-test caller suite.

The execution identity is helper `e1a355b4ff31f7073e7c37fd92ae6442540c7409b8abf7b4bd6e6c09a8b6deb4`, negative suite `7795520182e2bc942cc79ce72075e36456cf82bb014c76cce142bca99886cdad`, source fingerprint `88ccdfc943cb5f5a943d7617a232a58e11b1f5c59761ca4c415ecfdac908c935`, unchanged integration/harness/build/browser/frozen identities and exact FULL hash `457a0dec831a04e55b0138ac56029db545bd8f7463e10ffadb184009997025e3`. A subsequent authorized literal-only public binding change refreshes its affected strict/caller evidence under M504-PUBLIC-02-CONTRACT; unaffected controlled source and commands are not repeated merely because the public target changes. No public activation occurred during regression, and the isolated Ollama root remains preserved.

### M504-PUBLIC-02-CALLER — Exact binding and refreshed evidence

2026-09-20: lease `M504-A-public02-evidence-01`, digest `d941aee35891bd490365269c8bbb5408d60da2d848db95840ca363ce3480f0eb`, freshly closed-compliant, exit 0; receipt `c1bda645e532a67248d49e46aa77ccdc79934bffa0fc2e28af2b8d211f2d1b71`. Only the allowed helper changed. Primary independently reversed the two authorized URL/root literal substitutions in memory and exactly reproduced prior helper SHA-256 `e1a355b4ff31f7073e7c37fd92ae6442540c7409b8abf7b4bd6e6c09a8b6deb4`. Current helper SHA-256 is `77e4012a0530005438bed2693f0887d44c065afbe3af5164aff135f5fe51cb1b`; negative suite remains `7795520182e2bc942cc79ce72075e36456cf82bb014c76cce142bca99886cdad`. Current 198-row source/test/configuration fingerprint is `c2f6d942a30b3bd3f85a17ba112678ce106316f6ce24d915e2eb359818f7457a`.

Refreshed strict TypeScript and all thirteen unchanged caller tests pass, zero failures/skips/cancellations, exit 0. The exact reconstruction satisfies G's conditional reuse rule for the 881-test full regression and controlled/visual evidence: no shared oracle, lifecycle, application, build, test, fixture or command byte changed. The old Ollama artifacts and inventory remain unchanged; new W3C root absent, three scratch roots empty, capture flags absent, no public activation. This consumes the single caller-update turn. Critical caller reviewer `/root/m504_a_review` returned PASS with no Blocker, Major or Minor, independently confirming the exact two-literal reconstruction, unchanged test hashes, both preserved Ollama artifacts, absent new root/lease, empty scratch, compliant terminal receipt and clean diff formatting. Primary accepts the revised binding for the authorized two-activation W3C sequence; actual availability, outcome and cleanup remain to be observed.

Primary also ran the independent strict TypeScript and Vite build command against this final source identity after confirming the exact ordinary three-file build inventory. Both pass, exit 0; regenerated client bytes exactly match entry. No source change is planned after this check. No dependency acquisition, model/provider call or public scan occurs in these verification commands.

### M504-PUBLIC-02 — Actual selected-Finding comparison accepted

2026-09-20: after critical caller PASS, primary executed the exact reviewed caller once using the network-enabled permission boundary and unchanged README preparation. Fresh checks matched HEAD, caller, original records, build/browser, both prior Ollama artifacts and exact inventory; the new root and active lease were absent and all capture flags absent with three empty scratch directories. The actual default scanner completed both Local scans of the same exact W3C before-page. Exactly two activations were consumed, with one `POST /api/runs` and one `POST /api/rescans`; no retrieval, generation or review request occurred. No retries, synthetic response, corrected-page substitution or forced outcome occurred.

The baseline contains 35 actual Findings. The caller selected first Finding `0dd8e414-7a05-4619-828e-3000bfc91e4c` from baseline `run-6307a0b8-00f2-49d2-91bc-8ef0f4ab4008`, then intentionally rescanned to `run-66806024-1189-4db5-84fa-d9e1596f48cb`. Both records have completed status, exact requested/final target, Local mode and entry application revision. Production validation, successful HTTP response, canonical disk and GET agree. The baseline bytes remain unchanged. The actual comparison is comparable, uniquely correlated `color-contrast`, outcome `persistent`, reason `contrast-margin-equal`: both native observations report ratio 3.88 against 4.5, with zero margin change. The built UI presents canonical Before/After facts, IDs, rationale and outcome, and baseline/return navigation passes. This is one observed target at this time, not general support, conformance or causal evidence.

The caller exits 0 with `ok: true`, stage complete. Context closure, browser closure, service stop, closed port `59479`, scanner scratch and UI scratch checks all pass. The preserved ignored root `temp/m504-public-comparison-02` contains exactly three files: baseline `run.json` SHA-256 `3f680fd8a0e12b32e1c0f629675eecb40fb4162ac44b57994f5bc0550a162aa2`, later `run.json` SHA-256 `8da3e3414361cfeb4501eeebcede9bcb9b5a8b9e15ab707e86a25896e87aa87e`, and `evidence/result.json` SHA-256 `9a2e4f816f5c4deaad4e1e22f5b97a29f9cfc13528f576aaf27cf0bd85f0886d`. No public artifact is deleted or copied into tracked content. Primary accepts I8 and B. Both public grants are closed; no more activation is authorized.

### M504-VERIFY-02 — Final evidence mapping

The I1–I7 mapping in M504-VERIFY-01 remains accepted under M504-PUBLIC-02-CALLER's exact relevance proof. Native frozen transitions, synthetic adverse cases, policy-only arithmetic, downstream-state preservation and deletion/refusal witnesses remain distinct. The saved W3C comparison in M504-PUBLIC-02 now supplies I8; the Ollama zero-Finding scan is preserved separately and is not comparison evidence. I9's code/evidence portion passes: actual four-file test-only diff and cohesion inspected, all leases terminally compliant, final caller PASS, full 881-test regression, affected thirteen caller tests, independent strict/build, frozen/original preservation and accepted bounded visual checks. Different final review and documentation acceptance remain the only closure gates.

Documentation impact is limited to the existing README command/status summary, roadmap, owning plan, plan index and concise progress record/index. Requirements, ADRs, application interfaces, production code, dependencies and frozen evaluation inputs are unchanged. Final closure must reconcile status and navigation, preserve the earlier failures and finite grants, and validate links, configuration, formatting and diff before archive. M6 is not selected.

### M504-DOC-CANDIDATE-01 — Closure candidate checks

2026-09-20: primary independently rechecked all 208 entry source/configuration/frozen files with only the two expected existing-test changes, fourteen original records and their exact inventory, three build files, browser identity, thirteen generation and eight comparison references, final 198-row source fingerprint and all five files across the two isolated public roots. All match. Three managed scratch roots are empty, no active lease exists, both observed public service ports are closed, and both accepted desktop/narrow screenshots retain their hashes. The exact maintained FULL command byte hash still matches M504-REGRESSION-01. A documentation-only newline normalization was detected and reversed for that block before acceptance; executable text never changed.

Candidate documentation checks pass: UTF-8 without BOM, final newlines, no trailing whitespace or extra EOF blank lines, 368 local links/anchors across the five currently changed Markdown files, all four plan PowerShell blocks, exact test-only implementation scope, and `git diff --check`. The progress index has no content change yet and joins final reconciliation at closure. Roadmap counts remain 23 Complete / 1 In progress / 4 Not started. No requirement, architecture or application command contract changed. These checks establish the candidate documentation gate; final verdict/status and archive navigation require proportional final rechecks.

### M504-FINAL-01 — Integrated review and task closure

2026-09-20: different `critical_reviewer` `/root/m504_final_review` returned PASS after the complete I1–I9 audit, with no Blocker, Major or Minor. The earlier unavailable-image finding is closed. Its read-only independent checks reproduced both the narrow oracle correction and final literal amendment, validated all three public aggregates, and confirmed actual first-Finding linkage and retained later observation. Independent profile and outcome checks reproduce the W3C persistent contrast result. Protected entry files, fourteen originals, build/browser, thirteen/eight frozen references, terminal receipts, empty scratch, accepted screenshot hashes and exact 45-file command bytes all match. The reviewer accepted the relevance of the primary's 881-test regression, refreshed caller checks and independent strict/build; it did not repeat runtime suites or activate a scanner.

Primary accepts G, A, B, C and all I1–I9 Verification items. The complete roadmap contract has native three-rule transition evidence, separate synthetic adverse and policy-only vectors, immutable baseline/downstream state, durable publication/readback, exact synthetic deletion and valid-Finding refusal, bounded keyboard/status/focus and visual checks, and the actual public selected-Finding comparison. Scope and cohesion remain test-only: integration owns controlled crossings, the existing harness owns its narrow scanner seam, the fixed caller owns its finite public lifecycle/canonical UI assertions, and its negative suite owns adverse caller behavior. All five worker write turns ended with compliant receipts; no ordinary production edit occurred.

Documentation impact: Updated the existing README command and capability summary, roadmap, owning plan and index, and progress record/index. Candidate configuration, links/anchors, formatting, protected-state checks and `git diff --check` passed in M504-DOC-CANDIDATE-01. Primary accepted completion and mirrored status together after the six-document gate passed, then archived this plan and repaired navigation. Post-archive checks pass for all 445 local links/anchors across six documents, UTF-8/formatting, four PowerShell blocks, exact maintained command bytes, protected original/build/browser identities, unchanged HEAD and `git diff --check`. The validator initially treated a fenced example placeholder in the progress index as a live link; excluding fenced examples corrected that checker error without changing repository content. No requirement, ADR, dependency, frozen input or product behavior changed. Twenty-four roadmap tasks are Complete; four M6 tasks remain Not started. No commit or push occurred.

Residual limits: the two public observations apply only to those targets and execution states. Transient native pass candidates are intentionally not retained, so full historical correlation cannot be reconstructed from canonical records alone; the actual execution asserted it and no replacements were fabricated. Controlled and policy-only evidence remains explicitly separate. The trusted-operator/filesystem and cooperative-timeout boundaries remain; no hostile-URL isolation, general support, accessibility/conformance, causal remediation or release qualification is claimed. All 200% zoom testing remains Deferred and no screen-reader speech was observed. Both public roots, original records and screenshots are preserved. No further scans or worker correction is authorized. M6-01 is dependency-ready and awaits explicit selection.

### M504-ENTRY-01 — Planning evidence

2026-09-20: clean initial HEAD `284a1005a6282763e832f85c282febc0d0423f46` (`M5-03: Comparison persistence and UI (#26)`), no active lease. Node `24.20.0`, PowerShell `7.6.5`; package-lock SHA-256 `38ce94019310a5efdba9bb5565abe6eb13f678fa18fcd9d87b1c5dc21b30748d`. Maintained preparation plus the exact strict/focused command above passes 117 tests, zero failures/skips/cancellations. No service/browser/model was launched.

Frozen SHA-256: RD-003 `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459`; M3-01 `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; M5-02 `8a44166b40f700b5dc35a3290b7a4958533c38fc454302d843499c576f43e0a8`. Thirteen M3-01 and eight M5-02 recursive path/hash references pass. An initial ad-hoc checker incorrectly compared differently cased hex strings; corrected case-insensitive comparison passes without any input edit. This was a checker error, not corpus drift.

Primary authority/status review and a bounded read-only code explorer established the coverage map in this plan. [M5-03 final closure](m5-03-comparison-persistence-and-ui.md#m503-final-01--integrated-review-and-task-closure) records its accepted 44-file regression, independent strict/build, native deletion proof, six visual samples and different final critical PASS. Its preceding regression entry preserves an initially failing service-publication assertion, successful unchanged reproduction and unknown cause; do not erase that limitation or pre-authorize retries. Its [native characterization](m5-03-comparison-persistence-and-ui.md#m503-d-evidence-01--native-persistence-and-deletion-characterization) and current actual code, not a summary alone, locate reusable proof. No original retained record was read for content or changed during planning.

Planning documentation scope is this plan, its concise progress record, roadmap and their two indexes only. Requirements, ADRs, runtime instructions and application behavior remain unchanged.

### M504-PLAN-REVIEW-01 — Planning readiness and documentation

2026-09-20: fresh `critical_reviewer` `/root/m504_plan_review` returned PASS for planning readiness, with no Blocker, Major or Minor. It independently inspected the complete plan and activation documents, applicable authorities, current production boundaries and tests. It confirmed the native integration gaps, preserved policy-only vector, valid-Finding refusal witness, guarded ownership, finite effects and future command gates. Reviewed candidate SHA-256 was `314d8e957a6b81be1dfc2baf827b1bd9d6ce04abaa91a0eb09d5613beb7ea5d3` at unchanged planning HEAD; this subsequent status/evidence reconciliation changes no reviewed execution semantics.

The reviewer independently verified the manifest/lock identities and eight comparison references, PowerShell syntax, five-file encoding/formatting and `git diff --check`. Primary validation additionally passed all local links/anchors, sixteen required plan sections, exact five-document change inventory and roadmap counts of 23 Complete / 1 In progress / 4 Not started. The 117-test/strict result remains primary-run evidence; the reviewer did not repeat runtime checks. G procedure acceptance, public target/Finding availability and every actual execution outcome remain pending. No implementation, runtime scan, deletion or model call occurred.

Documentation impact: only this plan, its progress record, roadmap and two indexes changed. The frontend-quality skill constrained reuse and future bounded browser evidence; it introduced no UI or product-scope change. The task remains In progress for planning, with no commit or push.

## Interfaces and Dependencies

Retain the pinned Node/TypeScript/React/Playwright/axe toolchain and existing browser installation. No package/configuration change, acquisition, model or new interface is selected. Existing rescan/read APIs, one validated `run.json`, comparison publication/lineage modules and presentation owners are the boundaries to verify. Any test-only caller must use them without runtime test hooks or a parallel product state model. The only planned new persistent artifact is this task's documentation and any demonstrated minimal test coverage; content-safe ignored verification artifacts are not product records or a new evidence subsystem.

## Revision Note

2026-09-20: Created after M5-03 closure for the owner's planning request. Bounded the remaining integrated/public evidence, separated policy-only vectors from native/runtime records, required fresh execution identity instead of frozen historical HEAD comparisons, and retained guarded worker ownership, exact-command review and YAGNI.

2026-09-20: Reconciled planning-readiness PASS and documentation evidence after independent review. Corrected draft viewport literals to the existing comparison-test samples before review; no execution contract changed after the verdict.

2026-09-20: Closed G → A → B → C after actual public comparison, full regression, different critical PASS and documentation acceptance; preserved the failed caller history, explicit finite grants and separate zero-Finding observation. Archived after roadmap completion and repaired navigation.
