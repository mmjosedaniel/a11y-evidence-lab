# Verify the closed-corpus retrieval checkpoint

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M2-04](../../DEVELOPMENT_ROADMAP.md#m2-04--integrate-and-verify-retrieval-and-abstention), Complete after accepted bounded checkpoint observations, different fresh integrated critical review and documentation closure. The original execution scope and exclusions remain unchanged.
- **Entry evidence:** [M204-PLAN-01](#m204-plan-01--current-project-state). M2-03's renewed closure and M2-02's capacity gate satisfy the dependency. [Planning readiness](#m204-plan-review-01--accepted-planning-readiness) passed before execution; the final checkpoint result is recorded in [M204-FINAL-01](#m204-final-01--integrated-review-and-task-closure).
- **Remaining gates:** None. Integrated review and documentation closure pass. [M204-B-ACCEPT-01](#m204-b-accept-01--bounded-checkpoint-observations) records all actual/controlled observations and visual limits.
- **Allowance / lease:** All six authorized evidence-worker turns are consumed and terminally compliant; renewed readiness review and the single S repeat passed. Actual guidance activations: 3 of 3. Active lease: None. No further guidance activation is authorized.
- **Next action:** No further M2-04 execution. M3-01 requires separate selection. Retain runtime as the approved developer session and preserve all failed/successful evidence.

## Progress

- [x] (2026-09-09 15:31Z) Reviewed roadmap-wide state, current requirements/workflow, M2-02 capacity and M2-03 renewed closure, gold mappings and relevant implementation/test boundaries; recorded M204-PLAN-01.
- [x] (2026-09-09 15:31Z) Fresh Node 24.20.0, independent strict TypeScript and 119 focused tests passed on the unchanged implementation.
- [x] (2026-09-09 15:40Z) Accepted fresh critical planning-readiness PASS without findings and reconciled the five planning documents; see M204-PLAN-REVIEW-01.
- [x] (2026-09-09) Received bounded execution authorization and captured M204-ENTRY-01; strict TypeScript and 119 entry tests pass.
- [x] Accepted M204-A-PREFLIGHT-01 as EXISTING_BUT_UNCOVERED and froze the preparation contract and C-PREP.
- [x] Completed A's bounded corrections: strict TypeScript, 18 service tests and all seed/cleanup preparation checks passed on attempt 3; prior failures remain recorded.
- [x] Completed fresh A critical review: REVISE, with three Major and one Minor helper finding; B readiness failed.
- [x] Completed unaffected C-FULL: all 449 tests across the thirteen authoritative suites pass, with no failures/skips/todos/cancellations. Preserved artifacts and verified empty maintained scratch.
- [x] Completed the sole owner-extended helper correction and refreshed syntax, strict TypeScript, 18 service tests and preparation; terminal lease is compliant. Browser readiness was still unaccepted at that handoff.
- [x] Completed A re-review: REVISE with two Major browser findings; the correction/re-review allowance is exhausted and all real activations remain unused.
- [x] Completed the explicitly renewed correction: syntax, strict TypeScript, 18 service tests and actual Chromium preparation pass; fifth lease closed compliant. Renewed A review subsequently passed.
- [x] Accept A: necessary test-only preparation and renewed critical review PASS, without production changes.
- [x] Execute and accept controlled Z/F/I through UI, service, publication and validated readback with clean teardown. Preserve failed S evidence; its nonempty-citation comparison requires correction.
- [x] Accept B: G1/G2/G3 gold/citation/no-call observations, controlled S/A/Z/F/I, existing C boundary and bounded visual samples pass; prior failed S remains retained.
- [x] Complete regression, independent integrated review, owned cleanup and documentation closure; accepted disposition: proceed to separately authorized M3-01, with no later task started.

## Surprises & Discoveries

- M2-03's actual proof produced `incomplete` guidance missing **interpretation**, not the shared evaluation package missing **remediation**. Its real result remains useful historical integration evidence, not completion of this checkpoint's shared package ([M203-C-REAL-RESULT-01](m2-03-sufficiency-abstention-and-detail-ui.md#m203-c-real-result-01--actual-guidance-and-persistence)).
- The [old real driver](../../../tests/helpers/m203-real-guidance-driver.ts) is tied to one image seed and a consumed root. Its unused supported branch incorrectly expects a busy read; the settled owner is readable with `interrupted: false`. The [recorded restriction](m2-03-sufficiency-abstention-and-detail-ui.md#m203-final-01--closure-evidence-and-explicit-manual-check-deferral) forbids reusing that branch without correction. A new task-specific test driver must use the actual service contract, not copy this assertion.
- Generic [M2-02 factories](../../../tests/helpers/m202-retrieval-fixture.ts) are not gold inputs: the label factory uses a text input and the contrast factory uses different color/ratio facts. [M2-01 gold](../../../evaluation/m201-corpus-v1.json) fixes email and the retained #888888/3.54 contrast facts. M102 factories return frozen snapshots; any seed preparation clones before changing fields.
- The authenticated production snapshot declares no conflicts. [Corpus validation](../../../src/server/retrieval/corpus-validation.ts) rejects a changed declaration, and [stored analysis validation](../../../src/server/domain/run-contract/finding-analysis-validation.ts) recomputes support under that snapshot. A synthetic conflict can prove the pure policy and isolated outcome view, but cannot honestly be published as a real canonical run.
- Supported outcomes retain the single service workflow owner, including across a new run. Three independent real cases therefore use sequential service lifetimes; the checkpoint must not add owner reset, resume or automatic retry.

## Decision Log

- Decision: Select only M2-04 planning. Rationale: a plan request is not execution permission. Date/Author: 2026-09-09 / primary.
- Decision: Reuse existing application behavior and tests, with one bounded test-side preparation slice followed by evaluation. Rationale: M2-01 through M2-03 implemented the relevant contracts; another production subsystem or unconditional Red/Green cycle would add no proof. Date/Author: 2026-09-09 / primary.
- Decision: Separate real ranking observations from controlled policy/display cases, and preserve the conflict snapshot boundary. Rationale: fixed gold is an expectation, not an input to ranking; test doubles cannot establish real model outcomes or bypass authenticated persistence. Date/Author: 2026-09-09 / primary.

## Outcomes & Retrospective

The bounded checkpoint observations pass: three actual gold-profile retrievals, authenticated citations and durable preservation; supported/incomplete/missing/failure controlled outcomes; and the honest pure conflict boundary. All three actual sets lack a required guidance role and correctly abstain without generation. The final 449-test regression, independently refreshed strict TypeScript, unchanged verified build and bounded manual visual samples pass. Accepted disposition is **proceed to separately authorized M3-01** after different fresh integrated critical review and documentation closure passed. M2-04 is Complete. The failed original S and all superseded preparation evidence remain preserved. The corpus, gold, application and model configuration remain unchanged; no later task is started. The approved runtime remains running as the developer session. Full-detail actual 200% verification remains deferred to M6-03; no screen-reader result is claimed.

## Purpose / Big Picture

Demonstrate that one selected Finding retrieves exact closed-corpus guidance through the actual local embedding, service, UI and single-file publication path. Evaluate relevance, citation validity, support, abstention, failure and preservation separately. Show supported eligibility and the required deterministic adverse branches without invoking any generation provider. Finish with an honest M2 checkpoint disposition, not a model ranking, accessibility certification or release claim.

## Context and Orientation

### Authorities and readiness

Start with the [authority map and task router](../../README.md#authority-and-status-map), [requirements semantics](../../PROJECT_REQUIREMENTS.md), and exact roadmap task. All directly applicable Must rows are Accepted; no Proposed architecture or Deferred product work is selected.

| Owner | Controlling boundary |
| --- | --- |
| [Evaluation requirements](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#evaluation-requirements) | REQ-EVAL-002–005: separate observations, frozen directly supporting gold, exact applicable provenance, new evidence for affected changes. REQ-EVAL-001/007/008 and the shared-check table preserve provider-independent frequency and pre-observation freeze; no generation execution belongs here. |
| [Evaluation freeze and profiles](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#fixed-mvp-manifest), [gold](../../../evaluation/m201-corpus-v1.json), [scan manifest](../../../evaluation/rd003-scan-v1.json) | Three controlled failing profiles and predeclared acceptable passages; retain six scanner fixtures and expected native outcomes. Shared `incomplete` package must lack remediation guidance while resolving applicable text. |
| [Evidence and retrieval requirements](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#corpus-and-retrieval), [lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#finding-lifecycle) | REQ-EVID-004/007/011, REQ-RETR-001/002/004–006, REQ-GEN-001/009/010; selected-only input, deterministic support/abstention versus failure, immutable scan/siblings, no invocation or review. |
| [ADR-0019](../../architecture/decisions/ADR-0019-in-process-exact-vector-search.md), [ADR-0022](../../architecture/decisions/ADR-0022-closed-versioned-guidance-corpus.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) | Unchanged broad rule/SC filter, exact cosine top three and passage-ID tie-break, authenticated closed corpus, one validated aggregate. Required-role completeness is evaluated after ranking. |
| [SPEC](../../specs/SPEC.feature), [HARD_SPEC](../../specs/HARD_SPEC.feature) | BHV-02 and abstention/retrieval-failure portions of BHV-03; SPEC-002, SPEC-003's `Abstain when evidence or guidance is insufficient` and `Fail retrieval without assigning a support state`; HS-006 and no-call/abstention boundary of HS-008. |
| [Application accessibility](../../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md), [selected guidance presentation](../../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m2-03-selected-finding-guidance) | REQ-A11Y-001–004/006/009/010 and accepted citation navigation, corpus-version, focus and announcement behavior. Manual checks are visual only; full-detail actual 200% proof remains explicitly deferred to M6-03. |
| [Privacy](../../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md), [reliability](../../requirements/quality-security-and-operations/RELIABILITY_REPRODUCIBILITY_AND_OPERATIONS.md) | Content-safe local evidence, renderer isolation, independent strict checking, bounded service operation and last-valid durable truth; preserve the trusted-input limitation. |

M2-03 is Complete after [renewed closure](m2-03-sufficiency-abstention-and-detail-ui.md#m203-c-post01-closure--renewed-task-closure). M2-02's [accepted capacity evidence](m2-02-embedding-retrieval-capacity-gate.md#m202-closure-01--final-integrated-verification-and-documentation-impact) and [configuration observation](../../LOCAL_MVP_FEASIBILITY.md#m2-02-retrieval-only-observation--2026-09-08) satisfy the upstream gate, not current runtime availability. Do not repeat the capacity benchmark or reopen its acquisition/tokenizer decisions. If actual configuration differs materially, reconcile affected evidence before evaluating it.

### M204-PLAN-01 — current project state

Planning entry is clean HEAD `1adceff9126bfdb146ee5663a839dc6bced80e4e`, 216 tracked files, empty index, and no active lease. Eleven roadmap tasks are Complete; seventeen are Not started before this selection. M1 implements the exact-three-rule walking skeleton; M2-01 owns eight sources, sixteen passages and three gold mappings; M2-02 supplies default local embedding/ranking and its passed capacity screen; M2-03 supplies authenticated citations, sufficiency, abstention, service/API and UI. Generation, human proposal review and comparison remain unimplemented. The bug index has no records.

The new planning baseline freshly passes Node 24.20.0, strict TypeScript and 119 tests from `run-contract`, `scan-normalization`, `retrieval-contract`, and `finding-sufficiency` using C-BASE below. No browser, build, live model or full suite was run for planning. M2-03's 448-test/build results and prior-client actual abstention proof are historical, not fresh M2-04 outcomes. No current runtime availability is inferred.

Preserved checkout SHA-256 values: corpus manifest `87D8867ED1138BFB38E6C44EED67A79B0A43BDAF2532A056AFAFAED828202DA9`; passages `29EA0033E825A0FA20430AA42E9718B87A87AA59EDADDA0A3876A0A7BE871850`; gold `A7D38CACF98BF263CDD53012350708BE987699DF21B4C0F1BB7F32BC1892E29B`; scan manifest `13C9722BE9EA2E3B0AAF020EA91F429A701180A83814FE7AB21BAF2DDAD57459`. These are checkout identities, separate from normalized runtime authentication. Capture all referenced fixture hashes again at execution entry.

The bounded read-only reuse audit identified [existing policy tests](../../../tests/finding-sufficiency.test.ts), [service tests](../../../tests/retrieval-service.test.ts), [API tests](../../../tests/finding-guidance-api.test.ts), [UI tests](../../../tests/finding-guidance-ui.test.ts), and the old driver's reusable responsibilities. It found no production fix justified by inspection. This is not a passing execution preflight.

## Scope and Non-Goals

Own only the M2 retrieval integration checkpoint, exact controlled case preparation, necessary test-side coverage and evidence. Production source, packages, lockfile, runtime/model configuration, frozen corpus/gold/scanner inputs, workflow tools and past evidence are unchanged by the planned route. A demonstrated current-contract production defect may receive a newly bounded Red/Green correction in this same plan after primary triage; no speculative repair lease exists now.

Exclude generation or provider probes, new models, downloads, live public scans, query tuning, role preselection, reranking, source expansion, rewritten gold, repeated benchmarking, new state/serialization contracts, workflow reset/resume, runtime testing endpoints, dashboards, a general evaluation runner, and M3/M6 execution. A new significant decision must follow its authority before dependent changes; the checkpoint itself cannot approve it.

## Plan of Work

### Ownership, risk and budgets

Follow the [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), [write-lease guard](../../../.codex/write-lease-guard.md), and current role TOMLs. Primary owns authoritative documents and acceptance between leases; research/review roles are read-only. Every worker write turn has a fresh Packet v2 and terminally closed lease; only one writer is active in this worktree.

The present route applies already Accepted semantics, not comparative research: no mandatory R3 research panel or new Decision Review Contract is warranted. Primary handles repository facts and ordinary execution literals. If preparation reveals a consequential unresolved decision, classify it under the [R0–R3 policy](../../../.codex/README.md#research-work), record its Decision Review Contract here before comparison, and invoke only the triggered roles. Do not confuse a critical implementation/evidence review with authority to skip required decision work.

A uses one test-worker preflight and one coherent evidence assignment when needed, with the existing ordinary attempt-2 and conditional attempt-3 correction rules, plus one review correction loop. No automatic Green is scheduled. Existing covered behavior needs no write; uncovered behavior gets passing characterization. A genuine missing/regressed production behavior requires a separately frozen narrow Red/Green contract, exact source responsibility map and applicable review before it can advance. More than three TDD cycles requires rescoping. IDs, agents or pre-request failures cannot renew budgets; a worker stop returns to primary triage.

S3 review is justified for the proof's citation/record identity, real-versus-controlled evidence, preservation and teardown boundaries. Use one fresh critical reviewer for A's final helper/case package and a different fresh critical reviewer for integrated closure. The same reviewers cover necessary visual evidence; no extra UI panel. Planning receives one fresh critical readiness review with one bounded correction re-review. Independent read-only inspection may overlap primary documentation; all runtime cases and write leases are sequential.

### A — reconcile and prepare the smallest proof

On execution authorization, record M204-ENTRY-01 from the actual HEAD, symbolic ref, index, dirty-path ownership, active lease, relevant source/test/authority/installed-tool identities, frozen inputs and generated-output inventory. Historical HEAD values and consumed M2-03 roots are not entry conditions. Inventory existing build, scratch, data and retained evidence before any command that could replace them.

Freeze M204-CASES-01 in this plan before observing new real outputs: exact three seed objects or deterministic construction plus hashes; matching gold-profile subsets and target keys; source/query/model configuration; controlled adverse values below; expected assertions; exact caller/effect/cleanup packages. Gold arrays are acceptable alternatives: at least one returned passage must belong to the profile's frozen gold set, not every listed passage (some sets exceed k=3). Preserve the complete returned order/scores, gold hit/miss, resolved metadata, role coverage, sufficiency and actual state as distinct observations. No gold membership or guidance role enters the production query/ranking path.

Use test-worker preflight over the existing test/helper family. Prefer the existing thirteen suites and a single purpose-named `tests/helpers/m204-retrieval-checkpoint.ts` only for the currently missing three-profile runtime driver. If a separate case factory is necessary, `tests/helpers/m204-checkpoint-fixture.ts` owns seed/case construction, not runtime policy. No generic framework or change to the consumed M203 driver is planned. Permit only the exact needed subset of these paths and `tests/finding-sufficiency.test.ts`, `tests/retrieval-service.test.ts`, `tests/finding-guidance-api.test.ts`, `tests/finding-guidance-ui.test.ts`, and their existing M203 fixture/harness collaborators in the final packet. New helper imports must not execute model/browser/service work.

Responsibility placement: **None — no application-source responsibility changes.** The helper coordinates existing service/browser/repository boundaries and local evidence; existing validators, query projection, canonical resolver, policy and UI remain the production owners. A uses the applicable preflight/evidence route to characterize existing production behavior, not a non-TDD setup assignment. Test-owned passing characterization, seed validation, exact transition/readback checks, syntax, independent strict checking and controlled failure/cleanup probes establish the test package; no fabricated Red or Green is needed for covered behavior. Manual evaluation and primary documentation alone are **TDD: Not applicable**, with observation and semantic checks. Do not write an executable helper directly as primary or use non-TDD to smuggle production changes.

Validate preparation without live embeddings: clone frozen factories; match email/contrast gold facts and all required evidence; preserve native checks, siblings, observations and chronology; run the real validators/initial publication in exclusive test storage. Prove supported settled service readback succeeds with `interrupted: false`; after restart an active run is historical/interrupted, never resumed. Exercise launch failure, malformed output, deadline, early assertion failure and orderly teardown using bounded existing test seams, not a second cleanup platform. Accept the actual caller, helper diff and fresh S3 review before C-REAL. Missing paths or binding choices remain a preparation gate, not worker discretion.

### B — execute the fixed checkpoint

| Case | Input and proof lane | Required observation |
| --- | --- | --- |
| G1/G2/G3 | Exact `informative-image-alt`, `form-input-label`, `text-contrast` failing-profile seeds matching M2-01; three separate sequential actual service lifetimes | One explicit UI guidance action per case; real HTTP, uninjected default EmbeddingGemma retrieval, authenticated citation view, selected-only publication and validated readback. At least one frozen acceptable gold passage; exact returned top-three support assessed honestly, including legitimate abstention. |
| S | Complete image Finding and the canonical criterion/interpretation/remediation triple from `imagePassages` in the M203 fixture, with existing controlled scores | Controlled service/disk plus UI eligibility; active unfinished owner retained, no result/proposal/invocation/review. This is a policy/integration control, not real ranking evidence. |
| A | Same complete image Finding; only `wcag22-sc111` at score 0.9 | Shared frozen `incomplete` package: interpretation and remediation absent, applicable text resolves; terminal explanatory abstention, missing remediation named, no generation call or review. Exercise real service/publication with the existing test-only executor and browser view. |
| Z | Same valid Finding; empty ranked passages | `missing`, visible exact corpus version despite zero passages, terminal abstention, unchanged scan/siblings. |
| C | Existing pure policy vector: the canonical image triple plus unresolved pair `h37-text-alternative` / `understanding111-intent` | `conflicting`, exact references/explanation/no-call policy and isolated FindingOutcome display; normative WCAG precedence also passes. Separately prove a forged conflict cannot enter the fixed canonical aggregate. Do not claim real corpus conflict or persisted-conflict integration. |
| F | Existing service executor throws bounded `embedding-failed` | Durable selected Finding failure; no support, analysis-result abstention, ProviderInvocation or review; scan/siblings retained; visible failure. |
| I | Scoped in-memory corpus-read substitution with altered passage bytes, restored afterward; canonical files unchanged | Actual authentication fails before a partial citation view can publish; selected failure is durably bounded with no support or abstention. Add only the missing service-level altered-byte case if current read-fault plus policy coverage does not prove this joined path. |

Cases S/A/Z/F/I use explicitly test-only injected collaborators and isolated storage, never ordinary user data or a production test API. If an exact browser-to-service lane is absent, the test worker adds the smallest passing integration characterization; do not intercept a guidance success and call it real service proof. Case C deliberately remains policy/display plus authenticated-rejection evidence because the accepted snapshot is conflict-free. If required acceptance cannot be satisfied by these honest layers, stop for the controlling corpus/requirement decision rather than bypass validation or silently weaken the checkpoint.

G1–G3 use one fresh root per case under the future absent `temp/m204-retrieval-checkpoint` root. A seed is a project-owned synthetic completed scan built through the repository's normal initial publication boundary, not a claim of a freshly observed browser scan. Reuse recorded scanner fidelity and its regression tests; retain each seed's exact construction/provenance. Only the Analyze response may be intercepted to show that already persisted seed. Guidance, default retrieval, canonical resolution, writes and validated readback remain actual. Capture returned versus disk versus repository/service equality and the native/parent/sibling projection before/after.

Request execution authorization for a maximum of three explicit actual guidance activations, one per gold profile; preparation must freeze the corresponding model-request/input budget and deadlines from the existing default engine before use. Each fresh process may build its sixteen passage vectors and query vector; those cold starts are necessary isolation, not three capacity screens. No repeated sample, automatic retry, fallback or tuning. A semantic gold miss remains failed case evidence; complete unaffected authorized cases unless a shared integrity/runtime/safety fault invalidates their prerequisites. New model requests after the allowance require owner direction.

### Existing presentation verification

The [frontend-quality skill](../../../.agents/skills/frontend-quality/SKILL.md) applies only to the visible-state proof. `REUSE_AS_IS`: App's current selection/announcement coordination, `FindingGuidance` action/status, `GuidancePassages` citations/notices, `FindingOutcome` explanations, and the grouped list/native-evidence owners. No layout, CSS, action, component or dependency addition is planned; no frontend-visual Green lease is opened merely to run a browser.

Reuse current UI suites for keyboard/focus/announcement semantics, in-flight selection, disabled actions, citation popup isolation and empty corpus-version labeling. Inspect actual G1–G3 citations in the current built UI and one shared abstention detail at 1280×800; use one 320×800 sample for long text/notices. Check complete text and original links, clear source/ranking/support distinctions, visible focus and no clipping. Freeze exact automated/manual observations and build/browser identity before running them. No screenshot archive, exhaustive state-by-viewport matrix or spoken-output claim. Full-detail actual 200% stays at M6-03 under the explicit owner deferral. A reproduced rendered defect triggers primary reconciliation and the skill's bounded visual contract before any UI source edit.

## Concrete Steps

All commands use PowerShell 7 from `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`. Load only the first definitions block under [README development preparation](../../../README.md#development-command-preparation) in each shell; do not run the adjacent install block. The maintained `Invoke-M105Command` disables compilation caching and restores its environment; browser commands also use its approved scratch. Existing dependencies/browser are prerequisites, not a download grant.

### C-BASE — safe entry check

```powershell
git status --short
if ($LASTEXITCODE -ne 0) { throw 'Git status failed' }
git rev-parse HEAD
if ($LASTEXITCODE -ne 0) { throw 'HEAD read failed' }
git diff --cached --name-status
if ($LASTEXITCODE -ne 0) { throw 'Index read failed' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'Whitespace check failed' }
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Wrong Node runtime' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/retrieval-contract.test.ts tests/finding-sufficiency.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Pure checkpoint tests failed' }
}
```

Planning result: exit 0, strict check passes, 119 tests pass with zero failed/skipped/todo. No build, service/browser, model or canonical run is created. Counts may change with justified tests; never suppress cases to retain this count.

### Future caller package — unresolved until prepared

These slots are not runnable permission. Primary records complete exact callers, cwd, installed prerequisites, native exit handling, required structured results, input/output paths, permitted metadata/effects, deadline and cleanup before the relevant preflight/lease or primary evaluation. Syntax-parse compound PowerShell and test decisive failure branches, not only successful subcommands. Reuse the [command-preparation procedure](../../../.codex/execplan-implementation-workflow.md#command-preparation).

| Slot | Binding content to freeze |
| --- | --- |
| C-LEASE | Exact guard start/close from each Packet v2, returned digest and required `closed-compliant`; no worker guard/Git writes. |
| C-PREP | Exact seed/factory/helper validation and targeted characterization callers; expected output/temporary storage, no actual model calls. New helper must exist after its evidence turn; do not execute the consumed M203 command. |
| C-CASES | Exact selected existing policy/service/API/UI cases plus necessary additions; per-suite scratch and cleanup; serialized filesystem mocks restored in finally and isolated from real runs. |
| C-BUILD | Independent pinned strict compiler and `vite build --configLoader native`; either prove current build/source correspondence or explicitly preserve/replace the inventoried `dist/client` output. No blind overwrite or restore/install. |
| C-REAL | Task driver entry/options, current intentional HEAD/source/build identities, three exact case IDs/roots/seeds, approved runtime/model provenance and finite-input admission, actual request count/timeout, retained result fields, sequential launch/stop and failure dispositions. Missing runtime is a prerequisite failure, not acquisition permission. |
| C-VISUAL | Exact current build/browser, accepted controlled/real state, desktop plus one narrow sample, automated semantic checks and bounded manual visual checklist; no extra model request or external citation fetch needed. |
| C-FULL | Complete [thirteen-file maintained suite](../../../README.md#build-and-verify-the-walking-skeleton) plus any added task test; existing per-file order/scratch, independent strict check and build. Audit the actual authoritative suite list at closure. |
| C-CLEAN | Exact task-owned processes/ports/directories/files, normal stop and settled teardown first, absolute containment/ordinary-path/ownership checks, permitted deletion versus retained evidence and build backups. Never delete a broad root, corpus, developer model/runtime or old evidence. |

## Validation and Acceptance

The primary accepts evidence, not role assertions or receipts alone. Every case records applicable fixture/target identity, exact source/test/build revision, corpus/query/filter/ordered passages/scores/support, exact runtime/model configuration, request count, canonical run/readback identity, and separate completion/failure observations. Label seeds and supplied-result tests as controlled. Generation configuration is not a ProviderInvocation. No raw pages, prompts, model payloads, credentials, external traces, vectors or private files enter tracked evidence.

Acceptance requires all three real gold cases, at least one acceptable gold hit for each, canonical citation inspection, deterministic S/A/Z/C/F/I evidence at their stated honest boundaries, no generation activity, immutable completed scan/siblings, valid aggregate readback, and required UI checks. A real result may legitimately abstain when its ranked roles are incomplete; a controlled supported case does not establish actual supported ranking. The conflict-free snapshot limitation stays explicit rather than being reported as end-to-end conflict persistence.

Run focused validation at A's boundary and the complete authoritative suite/strict/build once at task closure; broaden or repeat only for changed/stale/contradictory evidence or unresolved risk. Confirm no test skipping, unconditional pass, weakened expectation, gold-driven ranking or production test seam. A fresh integrated critical reviewer examines the complete evidence map, actual diff, mutable proof identities, integrity/preservation and cleanup, reusing valid ordinary results and reproducing risk-critical checks proportionally. Planning PASS replaces none of these gates.

The checkpoint disposition is **proceed**, **correct the current milestone**, or **record a significant decision before proceeding**, per the roadmap. A gold miss, fabricated state, unsaved success, failed required check or unresolved critical finding prevents Complete. Record failed observations before any authorized correction. Changes to frozen inputs/configuration need their existing decision/versioning route and new affected evidence; do not rewrite old results or silently alter criteria.

At successful closure reconcile public capability/developer instructions only where materially affected, roadmap, this plan, progress and indexes under the [documentation gate](../../README.md#task-closure-documentation-gate). Requirements, ADRs, evaluation gold and past task plans remain unchanged unless a separately justified authoritative amendment is needed. Mark M2-04 Complete and archive this same plan only after verification and closure pass; M3-01 remains Not started until separately selected. Validate links/fragments, UTF-8, final newline, whitespace, command syntax, status consistency and `git diff --check`. No commit/push is authorized.

## Idempotence and Recovery

Read-only checks and pure tests can repeat when justified. Live guidance calls, seeded publication and browser/service startup have effects and consume their explicit budgets; a pre-request failure does not automatically authorize replay. Record whether a request actually began and preserve partial outputs. New attempts use reconciled exact ownership and allowance, never the consumed M203 roots or grants.

Close/inspect every worker lease before primary maintenance; preserve unexpected user/peer changes without reset, stash or checkout. An amended test invalidates affected prior evidence before reuse. Stop shared runtime work on integrity, ownership, deadline or uncertain cleanup faults, complete unaffected read-only work, and return genuine authority/budget gaps to the owner.

Normal service stop and awaited browser teardown precede cleanup. Preserve the last valid canonical run, failed evidence, existing developer runtime/model/tokenizer assets, prior capacity/proof directories and inventoried backups. Never clear supported ownership by editing stored state, resume a historical active Finding, or corrupt the canonical corpus to manufacture an adverse case. Assert exact owned listeners/resources have closed; a successful assertion before failed teardown is not an exit-zero proof.

## Artifacts and Notes

### M204-CASES-01 — frozen preparation contract

Primary accepts M204-A-PREFLIGHT-01 as **EXISTING_BUT_UNCOVERED**. Existing production behavior covers the checkpoint; the missing proof is the three-profile driver, exact controlled-case preservation and joined altered-byte service case. No production gap, Green or research decision is selected. Only `tests/helpers/m204-retrieval-checkpoint.ts`, `tests/helpers/m204-checkpoint-fixture.ts` and `tests/retrieval-service.test.ts` enter A's evidence lease. The fixture owns deterministic input/case construction; the driver owns bounded service/browser coordination and receipts; the existing service suite owns joined integrity and preservation characterization. No production responsibility or dependency changes, generic runner, cleanup platform or structural refactor is authorized.

G1/G2/G3 use run IDs `m204-g1`, `m204-g2`, `m204-g3`, selected `finding-0`, Local mode, actual HEAD `250abacdc228fbce8a8c366c0e4638ba17132b31` and synthetic target `https://m204.test/`. Construction clones M102 `runningRun`/`completedRun` before modification. Keep their fixed chronology and nonselected image Finding/native incomplete observation; align requested/final URL and revision; replace only the selected Finding with the matching M202 rule factory, overlay every exact `expectedMinimizedEvidence` field from M201 gold, use the corresponding public structural locator recorded in M204-A-EVIDENCE-02 (keeping the RD003 selector as evaluation metadata), and reconcile coverage counts to the constructed collection. Preserve the native check groups supplied by the rule factories. This is explicitly synthetic completed-scan evidence, not a new scanner observation. Gold membership is evaluation-only: at least one returned acceptable alternative per profile, with no gold or required-role input to ranking. Freeze constructed canonical seed hashes and finite query admission before any actual request.

Controlled S/A/Z/F/I use the same complete image seed with run IDs `m204-s`, `m204-a`, `m204-z`, `m204-f`, `m204-i`. S fixes `[wcag22-sc111:0.9, understanding111-intent:0.8, h37-text-alternative:0.7]`; A fixes only `[wcag22-sc111:0.9]`, missing interpretation and remediation; Z fixes `[]`; F throws `RetrievalError('embedding-failed')`; I substitutes altered passage bytes in memory only during the controlled operation and restores the original read binding in `finally`. Every case requires unchanged native/parent/sibling data and validated disk/repository/service equality; supported settled ownership is readable with `interrupted: false`, while a later restarted read is historical/interrupted. C reuses existing pure conflict/normative policy, isolated display and canonical-rejection tests without forged persistence.

Exact future driver CLI is `--prepare <HEAD>`, `--real G1|G2|G3 <HEAD>` or `--controlled S|A|Z|F|I <HEAD>` (one literal case per call). Unknown/missing/extra arguments fail before effects. Imports perform no service/browser/model effects. Preparation uses only test collaborators and exclusive `prep-*` children; validates all seeds through normal repository create/finish/read and real validators, finite input admission, and launch-failure/malformed-output/deadline/early-assertion/ordered-teardown branches. It may launch the retained managed browser for controlled proof, with no live embedding. Real commands use the production service's uninjected default engine. Controlled browser integration may compose the existing loopback API and client-asset owners with a callback into the real service's test-only executor seam; it must not manufacture a guidance success response. Only Analyze may return the already persisted synthetic seed.

All outputs live under exclusive `temp/m204-retrieval-checkpoint`: per-case `g1/g2/g3/s/a/z/f/i` children (absent on entry), containing `seed.json`, `runs/<runId>/run.json`, `evidence.json` and optional bounded visual samples; `prep-*` children are exact disposable test ownership. Each case starts a fresh app-service lifetime, browser/context and disposable vector collection, and must close normally before the next case. Existing `temp/m104-ui` is the browser scratch and must begin/end empty. Existing evidence/runtime/build backups remain untouched. Retain failed and successful case receipts and canonical runs. Delete only settled, exact owned disposable preparation children after containment and ordinary-path checks; no broad cleanup.

Model bound per G case: 16 canonical document embeds plus one selected query; seven metadata requests, or eight if initial residency is absent; at most 25 requests per case and 75 total, including at most 51 embeds. Counts are source-derived unless separately observed; no injected requester is permitted in the real lane. Loopback `127.0.0.1:11434`; Ollama 0.33.3 and exact `EMBEDDING_IDENTITY` manifest/model, BF16/768/context 2048/batch 2048, `truncate:false`, keep-alive 5m, existing prefixes and finite-input admission remain unchanged. Retrieval deadline 300000 ms; metadata 10000 ms, individual embedding 60000 ms; browser launch 10000 ms, outer case deadline 330000 ms and existing service stop bound. No automatic retry, generation, ranking adjustment, model acquisition or capacity rerun.

Preparation caller C-PREP runs the maintained definitions-only README block, then `Invoke-M105Command` around pinned Node `--check` for each new helper, independent `node_modules/typescript/bin/tsc --project tsconfig.json`, `--test --test-timeout=120000 tests/retrieval-service.test.ts`, and driver `--prepare 250abacdc228fbce8a8c366c0e4638ba17132b31`. Every native nonzero exit throws before the next step. Driver preparation uses the UI scratch wrapper. No real case may run before primary helper/diff/evidence acceptance and the fresh critical A review. Existing policy/API/UI cases run sequentially through their maintained commands at focused/closure boundaries; the complete thirteen-file suite remains C-FULL.

### M204-A-EVIDENCE-01 — first preparation boundary

The test owner created the two bounded helpers and added the controlled service preservation/integrity characterization. Both helper syntax checks passed; independent strict TypeScript stopped with TS1107 at the two callback `continue` statements. The service suite and preparation command did not run. No browser, actual embedding, or guidance activation occurred. Guard closure is compliant, limited to the three assigned files, with no index, HEAD or unleased drift; pinned post-close status confirms the receipt.

Primary inspection also found unfinished requirements in the same driver contract: independent seed/gold matching and receipt identities, real completion and gold observation, canonical citation comparison, all-case settled readback, exact controlled outcomes, durable failure receipts, immediate resource registration, request bounds and meaningful shared-path failure probes. Attempt 2 corrects these omissions and the syntax failure within the same objective and three-file scope. It does not renew the budget or authorize production work. A's test boundary remains unaccepted; no real caller may run before validation and fresh critical review.

### M204-A-EVIDENCE-02 — conditional final correction

Attempt 2 passed both syntax checks and strict TypeScript. The service suite passed its 17 existing tests but the new controlled case failed at seed validation; C-PREP stopped before preparation. Its lease closed compliant with only driver/service-test edits and unchanged Git identity. No runtime allowance was consumed. Primary traced the rejection to `readLocator`: the accepted public contract permits structural `:root > :nth-child(...)` locators, whereas the task-authored literal incorrectly copied the RD003 evaluation selector. This is a test-preparation error, not a production defect or a change to gold.

Conditional attempt 3 is justified by the newly passing compiler boundary, implemented receipt/admission/preservation checks, and this exact new diagnostic. Its different bounded action corrects the seed locator to the structural path derived from each frozen HTML fixture, retaining `selector`/`targetKey` separately as evaluation metadata. The paths are G1 and all image controls `:root > :nth-child(2) > :nth-child(1) > :nth-child(3)`, G2 `:root > :nth-child(2) > :nth-child(1) > :nth-child(2) > :nth-child(2)`, and G3 `:root > :nth-child(2) > :nth-child(1) > :nth-child(2)`. A primary read-only reconstruction passes all eight production validators, query builders and finite-query admission with these literals. This corrects the earlier locator instruction in M204-CASES-01 while preserving its accepted observable contract, authority, scope and expected outcomes. Attempt 3 retains the same three-file evidence lease projection and C-PREP; no fourth attempt or budget reset is authorized.

Before that lease, C-BUILD preserves the inventoried three files byte-for-byte under the absent task-owned `build-before` directory, requiring exact entry hashes and ordinary contained paths. The maintained definitions-only preparation then runs pinned Node strict TypeScript and `node_modules/vite/bin/vite.js build --configLoader native`, throwing on native failure and retaining the new output inventory. Replacing the inspected client output is expressly limited to this build; no install or dependency operation occurs. Preparation subsequently freezes the built-client hashes.

C-BUILD passed independent strict TypeScript and Vite 8.0.16 production build. All three rebuilt file hashes exactly match the entry inventory and preserved copies. This establishes current source/build correspondence; it does not validate the test fixture or execute the UI.

### M204-A-ACCEPT-01 — prepared evidence boundary

Primary inspected the three-file diff and terminal lease receipts. Attempt 3 changed only the fixture's structural locator literals. Both helper syntax checks, independent strict TypeScript, all 18 service tests (zero failed/skipped/todo), and C-PREP passed. The new characterization exercises S/A/Z/F/I preservation, all-case settled readback, supported restart interruption and joined altered-byte integrity failure. No production edit or Green occurred. The evidence chain is now exhausted through its successful third turn; it is not renewable through another ID.

The retained preparation receipt is `temp/m204-retrieval-checkpoint/preparation.json`, SHA-256 `40F4E05DC62F4A3F50E778B69CC8344AA6BBA20118DAE36D43BAC8F641E18D7E`. It freezes all eight seed/query hashes, source/test/build/corpus/package identities, all sixteen finite document inputs, and passing shared-path launch, malformed-output, deadline, early-assertion plus cleanup-failure, and orderly-teardown probes. Primary readback confirms those results and removed preparation children. It does not establish live browser/runtime results. The three seed hashes are G1 `A536952C8726150DBE68C53AC222D1737F7B63FAFD1BCFBD493960423349B207`, G2 `127B1D8EDA0BC90D4450FFFE7BA30F9074F908186E1F454FD94CBEC10F705BEE`, G3 `9CE8950A861BDCFB7C1AE47C48600D62B54B7A354892D7854AD34F5BD46970ED`.

Accepted file hashes: fixture `C6011F1378D346C1072427DAD6BDC49DE182446DBFAC42574FFCC44A7DAA58AC`; driver `A505358BD9907B8642FCF019BCD391A71DE70F428BE4288D8EDA880415CA430E`; service suite `DC94027334AC3864CEE0C98F0589CC8BC1CE572FBD2A8190A075D87CC55E8932`. The fresh critical A review remains required before B.

### M204-A-REVIEW-01 — readiness failure and owner boundary

The fresh read-only critical reviewer returned **REVISE / B readiness FAIL**, with three Major and one Minor finding and no Blocker-class finding. Primary inspected the cited code and accepts all four findings. The reviewer checked all 96 preparation-recorded hashes, compliant lease history, exact frozen seeds and relevant production owners. Independent pure diagnostics reproduced the wrong selection, valid shared-URL citation combinations and ineffective negative probe without model, browser or service effects.

| Finding | Confirmed defect | Bounded correction awaiting authority |
| --- | --- | --- |
| Major: selected identity | `browserAction` clicks the first Findings button. The UI groups image results first, so G2/G3 would activate preserved sibling `finding-1` instead of intended `finding-0`. | Resolve the intended Finding's displayed identity from the frozen seed, select it explicitly and assert the exact guidance request identity before dispatch. Add a no-model check covering G2/G3 grouping. |
| Major: citation links | A whole-detail count of one link per URL rejects distinct canonical passages sharing the H44 or Understanding 1.4.3 URL. | Assert the link inside each passage's identified rendered item and check valid repeated-URL combinations without changing canonical text or ranking. |
| Major: visible Z/F/I outcomes | Z has no passages/notices, and F/I bypass success-view checks; server-object assertions can pass without observing the required browser state. | Await/assert Z's visible corpus version and abstention and F/I's visible failure/no-support state before teardown, retaining the existing case boundaries. |
| Minor: malformed-output probe | `assert.throws` followed by an unconditional synthetic throw makes both accepting and rejecting validators produce a passing failure receipt. | Let the shared validator rejection drive the probe; unexpected acceptance must fail preparation. Verify both paths through the existing bounded seam. |

These findings invalidate **B readiness** and the malformed-output probe's claimed negative evidence. Valid seed publication, finite-input checks, service characterization and ordinary regression results remain observations at their actual boundaries. No production, requirement, ADR, corpus, gold, model or configuration change is justified. Routine corrections remain contained in this plan; no separate bug report is needed for these fully diagnosed task-local helper issues.

All three turns in `M204-A`'s evidence chain are consumed. The [correction rule](../../../.codex/execplan-implementation-workflow.md#corrections-and-exceptions) states, "No skipped predecessor, sibling correction, or attempt 4 is allowed." The review-follow-up allowance does not create another write turn. Primary will not bypass this limit with a direct test edit, replacement agent or silently reset chain. **Required owner decision:** authorize one additional guarded test-worker turn to correct these four findings in the existing driver, with only directly necessary test characterization in the same three-file scope, followed by one preparation refresh and the remaining A re-review. If authorized, record that explicit budget extension and its predecessor chain before opening a newly linked guard chain; do not alter old leases or receipts.

The refresh must preserve the current preparation receipt under a named, hash-checked task-owned historical path before producing a new receipt; it cannot overwrite or silently replay it. Re-run affected syntax, strict, service and preparation checks, including decisive regressions for the review findings. Keep the real allowance at one activation per G profile, three total. Only a passing re-review permits the already authorized B cases; a different fresh critical reviewer is still required after integration. No additional model invocation is requested.

### M204-RESUME-01 — owner-directed bounded continuation

After the primary reported the four concrete review findings and the consumed correction allowance, the owner instructed the primary to report progress and continue. This is accepted as authorization for **one additional guarded test-worker correction turn** limited to those four findings, preparation refresh and the remaining A re-review. The previously recorded stop is preserved as history. This grants no additional real retrieval activation, production change or later-task selection.

The guard supports at most three turns in one chain. Use the explicitly extended review-correction chain `M204-A-REVIEW`, workflow `M2-04-20260909-01`, linked to the original terminal `M204-A-EVIDENCE-03` and M204-A-REVIEW-01. This is the fourth authorized evidence-worker turn overall, not a reset of the original budget. Its sole attempt 1 may edit only `tests/helpers/m204-retrieval-checkpoint.ts`; all other source/test/authority files remain forbidden. The fixture and service suite require no change for the four findings. No automatic attempt 2 is authorized in this extension.

Before that lease, preserve the original hash-verified `preparation.json` as `preparation-before-review-01.json` within the same ordinary task root, with absent destination and exact post-move hash verification. C-PREP then uses the same syntax/strict/service/prepare caller and task roots; the fresh receipt must include decisive no-model checks for target selection, per-passage link expectations and rejecting-versus-accepting malformed-output behavior. The driver adds actual Z/F/I visible-state waits before normal teardown. The earlier preparation receipt and full-suite logs remain historical evidence; no actual case root may be reused or created before re-review passes.

### M204-RESUME-EVIDENCE-01 — extended correction handoff

The sole extended write turn changed only the driver. Lease `M204-A-REVIEW-EVIDENCE-01`, contract digest `ffe7a2d47aca2ef31e2de3e3de1ac7fb1fb87a1f1a5ee5f54e6be615384444de`, closed compliant with receipt `a45be5a920215faf98ce0275416381328f2be530cffa8ff783ff6339b0dee432`; pinned status confirmed no post-close, HEAD or index drift before primary documentation maintenance. No further correction turn is granted by M204-RESUME-01.

Refreshed C-PREP passed syntax, independent strict TypeScript, all 18 service tests and preparation. Driver SHA-256 is `28A8B8BC83586105382A224BE9C6B80F39F78D41C5AB6AC7ECD486F522166C7F`. Fresh preparation SHA-256 is `3E919D3C5550BF0B8897FBC42EF37D7CBDC0310140653D82AE199321E7AF9DCF`; the preserved predecessor at `temp/m204-retrieval-checkpoint/preparation-before-review-01.json` retains SHA-256 `40F4E05DC62F4A3F50E778B69CC8344AA6BBA20118DAE36D43BAC8F641E18D7E`. Primary compared every recorded source/test/build/corpus fingerprint and all eight seed/query identities: no mismatch and no seed/query change. Case roots and runtime-start root are absent; maintained scratch and `data/runs` are empty.

The correction scopes citations by passage identity, adds Z/F/I browser assertions and makes the malformed-output probe depend on the real validator rejection. It also selects the intended presented Finding by label, but its `getByRole('heading')` descendant filter contradicts `ResultCard.tsx`, which renders the label as `strong`. Thus passing pure presentation checks do not prove the browser selector can match. Primary sent the complete corrected surface and this finding to the remaining A re-review; no runtime case may be used to bypass that readiness gate.

### M204-A-REREVIEW-01 — remaining browser defects

The same independent critical reviewer completed the sole A follow-up on the exact extended driver/preparation identities above: **REVISE**, two Major findings, no Blocker or Minor. It independently matched all 96 recorded artifact hashes and the retained predecessor receipt. The shared-URL identity scoping, explicit Z/F/I visible-state assertions and malformed-output probe are corrected at their inspected boundaries. The probe also passed an independent in-memory negative check: an incorrectly accepting validator makes preparation fail.

Two exact browser defects prevent B readiness:

1. The new Finding-card locator requires a descendant heading, while the actual `ResultCard` label is a `strong` element. Its immediate uniqueness check finds zero cards for every profile before guidance dispatch. Match the existing card's actual text/accessibility structure and verify G1/G2/G3 selection against rendered cards, including the preceding image sibling for G2/G3.
2. The passage-ID-scoped citation check calls `count()` immediately after HTTP completion, before waiting for React to render the passage. Playwright `count()` does not wait; delayed rendering can fail a valid case after activation. Wait for the identified passage item before uniqueness and link assertions. Inspect the changed selector family for the same ordering assumption and validate delayed rendering without a model request.

The remaining correction is test-driver-only; no product or frozen-input change is justified. Both findings are diagnosed in this task-local record. All authorized evidence-worker turns and the sole review follow-up are consumed. The workflow states that exhausted authorization stops automatic continuation and that changing IDs or replacing an agent cannot reset the allowance. **Required owner decision:** authorize one additional guarded turn on `tests/helpers/m204-retrieval-checkpoint.ts` and one renewed A review for these two findings, including bounded no-model browser characterization of the actual selectors and render waits. Preserve the current receipt as `preparation-before-review-02.json` with exact hash verification before a fresh preparation run. The original three actual activations and different fresh integrated-review requirement remain unchanged. This record grants no extension itself.

Handoff documentation reconciles this plan, roadmap, both indexes and the task progress record to Blocked. Validation passes UTF-8/final-newline checks, 192 local links/fragments across five documents, all three frozen PowerShell block parses and `git diff --check`. HEAD/branch remain the entry values, index is empty, no lease is active, runtime processes are absent and the case/runtime roots remain uncreated. No commit, publish, push or later task occurred. The task remains unarchived; these handoff checks do not replace pending checkpoint acceptance and integrated review.

### M204-RESUME-02 — authorized browser correction and renewed review

The owner explicitly approved the concrete request for one further guarded driver-correction turn and renewed review, including browser verification before real activation. This extends the consumed allowance by exactly one write turn and one A review. Reuse workflow `M2-04-20260909-01`, slice `M204-A-REVIEW`, evidence attempt 2, lease `M204-A-REVIEW-EVIDENCE-02`, parent `M204-A-REVIEW-EVIDENCE-01`, owner `m204_test`. This is the fifth authorized evidence turn overall; no automatic attempt 3 is granted. All other scope and actual-call limits remain unchanged.

Packet v2: the observable contract, Accepted authorities, S3 risk, preflight classification and responsibility map remain M204-CASES-01 and A. Only `tests/helpers/m204-retrieval-checkpoint.ts` may change; no directory allow-list. Forbid `src`, `docs`, `corpus`, `evaluation`, `fixtures`, the checkpoint fixture and retrieval-service test. No production responsibility or interface changes. Reuse existing UI/build, browser, canonical resolution and coordinated cleanup; permit only bounded local sharing of the actual card-selection and passage-wait operations with their no-model characterization. No generic harness, dependency or new executable file. Accepted fixture/service behavior remains unchanged; prior driver preparation does not establish the corrected browser boundary.

Correct both M204-A-REREVIEW-01 findings and inspect the same locator/count family for matching assumptions. Preparation must exercise G1/G2/G3 against actual rendered production cards and selected detail, including the preceding image sibling, and exercise delayed citation presentation with two passage identities sharing a canonical URL. Use at most one retained managed Chromium lifetime, three profile pages plus one bounded delayed-presentation probe, within 120 seconds; no public target, local model, generation or real case root. Reuse the current client build and normal component structure, not copied invented card markup. Controlled synthetic responses are permissible only in this explicitly labeled preparation lane and must never enter the real lane. Record selection, delayed wait and normal cleanup in fresh preparation evidence. All browser/API resources must register immediately and close through the existing coordinator; UI scratch must return empty.

Before lease start, primary preserves the hash-checked preparation receipt as `preparation-before-review-02.json`, with absent destination and ordinary contained paths. C-PREP remains the exact README definitions-only preparation, both helper syntax checks, independent strict TypeScript, all 18 service tests and driver `--prepare 250abacdc228fbce8a8c366c0e4638ba17132b31`, once, with native nonzero stopping later commands. Its preparation now includes the bounded browser checks above; use sandbox escalation for that caller. No runtime or actual G case is authorized to the worker. Maximum write turns: one; corrections: zero; repeated decisive failure: stop on first; no-diff outcome: stop; validation: once after the coherent correction. Any changed binding field or unexpected failure returns to primary triage. Worker reports exact changes, command results, hashes, browser observations, cleanup and residual risks, and performs no Git mutation or guard operation. The primary accepts the diff and terminal compliant lease before renewed A review and dependent B cases.

### M204-RESUME-EVIDENCE-02 — corrected browser preparation

The renewed driver-only correction closed compliant under `M204-A-REVIEW-EVIDENCE-02`, digest `2750a8c0192392314ff5136e9e625200a2d6f0005f3746f30a80f04e65d275b6`, receipt `f0d9a6e5b609d125f51c517a9f9163b9d4547593c913e1bf5c090821cf084f77`. No other path, HEAD or index drift occurred. Driver SHA-256 is `06DBF9543D9AE626A9AC4C2A76E4E3C9B51539B9A6B1E96F8A4C8C53E82496D6`; fresh preparation SHA-256 is `F86CF442FA1BA26FCA98C9A2B22650948F330D6EEC7F6D51BC277D158BB50229`. Both earlier receipts retain their recorded identities.

C-PREP ran once and passed both syntax checks, independent strict TypeScript, all 18 service tests and Chromium preparation. The primary inspected the corrected shared operations and matched all 96 source/test/build/corpus hashes and eight unchanged seed/query pairs. `analyzeAndSelectCheckpoint` matches the actual card text, waits and verifies its exact `strong` label and selected detail. `assertRenderedPassages` waits for each exact passage identity before uniqueness and canonical-link checks. These operations are shared by preparation and B, not replaced by copied markup.

One Chromium 151.0.7922.34/API lifetime rendered the existing built app. G1 selected the image Finding; G2/G3 selected the label/contrast Finding after the preceding image group and scanner-review observation. The synthetic G2 probe started the shared passage wait with zero rendered items, held the response until that absence was confirmed, then released the validated canonical envelope and observed both H44 passage IDs sharing one URL. Exactly one synthetic guidance callback and zero model requests occurred. Context, browser, API, listener and scratch checks passed; `serviceStopped: false` is not applicable because this preparation lane starts no application service. No B case root was created. The renewed critical A review examines this exact package before B readiness can be accepted.

### M204-A-ACCEPT-02 — renewed readiness and final regression

The renewed critical reviewer passed A preparation and B readiness with no Blocker, Major or Minor findings on the exact M204-RESUME-EVIDENCE-02 driver and receipt. It independently matched all 96 artifact hashes, eight seed/query pairs, historical receipt identities and compliant terminal lease. It inspected the complete driver, actual production card structure, shared browser operations and isolated delayed-render evidence. The two remaining findings are resolved; earlier fixes remain intact. Primary accepts the diff's test-only responsibility placement and this readiness barrier. This is not an actual retrieval result or final integrated review.

C-FULL completed on the corrected tree with all 449 tests passing across thirteen suites and no failure, skip, todo or cancellation. Logs are retained in `temp/m204-retrieval-checkpoint/verification-final`; the explicitly retained final synthetic compact screenshots are in `temp/m203-ui-15e3fb38-b0fd-4c89-91c0-3c161db71103`. All original inventoried children remain; application, frozen inputs and build bytes are unchanged. Maintained scratch is empty. The different fresh integrated reviewer and B observations remain required.

### M204-B-S-01 — controlled observations and citation comparison defect

After accepted A readiness and final regression, the primary invoked S once using its frozen caller. The UI submitted the exact selected Finding, service publication produced supported active ownership, and durable JSON/repository/service equality checks passed. The driver then failed `assert.deepEqual(response.admitted.view, expectedCanonicalView)`: browser admission intentionally retains null-prototype passage records, while canonical resolution returns ordinary objects. `node:assert/strict` includes prototypes in this comparison. No passage value discrepancy was observed. S exited 1 after complete normal context/browser/API/service/port/scratch teardown, with no timeout or model request. Its receipt and canonical run remain under `temp/m204-retrieval-checkpoint/s`; this is failed checkpoint evidence, not a supported-case PASS.

A read-only reconstruction from the retained S seed/run passed normal canonical resolution and browser admission, reproduced the strict comparison failure, and passed strict equality after converting the validated browser view to its JSON value representation. It confirmed null versus Object prototypes and supported active state with all required roles. This is an exact test-driver assertion defect; changing application admission or weakening canonical text/metadata validation is not justified. The affected comparison is shared by every nonempty-citation case, so A/G1/G2/G3 were not started. No runtime startup or actual guidance activation was spent diagnosing it.

The unaffected Z/F/I callers each ran once and passed. Each observed one Analyze seed response and one controlled guidance request, exact selected identity, normal durable publication/readback, preserved parent/native/sibling data and all six cleanup checks. Z visibly showed missing support, explanatory no-call abstention, manual investigation and corpus version. F/I visibly showed their exact failure without a success/support/abstention outcome. I changed bytes only at the serialized in-memory read seam; the actual corpus file still has SHA-256 `29EA0033E825A0FA20430AA42E9718B87A87AA59EDADDA0A3876A0A7BE871850`. C retains the passed pure policy/display and canonical-rejection boundary in C-FULL, not a forged persisted conflict.

| Case | Observation | Receipt SHA-256 | Canonical file SHA-256 |
| --- | --- | --- | --- |
| S | Failed helper comparison after supported publication; clean teardown | `A8D2729C044A47D8553DDD2DBDE2DF651940C2D8776918D4C5CFAF8C486A1561` | `11894136E6C2FB61FF5E6BB8BE7B945F1C8556A148B0B32FD0187962A9DFF2B8` |
| Z | PASS: missing guidance, terminal abstention, no generation call | `B8B5734C8FDF5887DA608F8A7DBCE61FF5AF513460FCCBC178308AB9BE4F278E` | `F256F772B9D6AC7E0B47A113D2FDA4E11CB015BC681F65A5DAB3051913603F87` |
| F | PASS: embedding-failed, no support state or abstention | `5AA150A7C8B139F4A3334FEF76666D8833CD8B0F4D849407FE1F1016B31BF1EC` | `2E1CD7204AA899E1C9550F1653FC356DC428F1AEC5AD207FA91320FCA8312A8D` |
| I | PASS: corpus-integrity failure, no support state or abstention | `99CFBD728494AD22FD4CAC0D337C48D85F6C095ED32F4B858B6C9E4BB9882786` | `9794AB5A2A4275B23CEB2AF5FC44F2F81D2509291DE9388C1768B66B59C94A9A` |

The evidence identities include the unchanged M204-RESUME-EVIDENCE-02 driver, source/test/build/corpus/configuration and Chromium 151.0.7922.34. No extra browser or service remains; maintained scratch is empty. The normal supported service owner was never reset, and no historical active Finding was resumed.

**Required owner decision:** one further guarded driver-only correction, one renewed readiness review, and one controlled S repeat. The bounded correction compares validated canonical JSON values without treating an internal prototype as wire identity and adds a decisive no-model regression using the same comparison. Preserve `s` as the exact contained `s-failed-prototype-01` directory after verifying its complete inventory and hashes, and preserve current preparation as `preparation-before-review-03.json`, before producing any fresh S/preparation output. These names are proposed resumption literals, not current execution authorization. The failed result remains failed and visible in history. Existing Z/F/I/C evidence needs no automatic repetition; all three actual activations remain available, and the different fresh integrated reviewer is still required. No new helper framework, dependency, ranking, gold, corpus, source or model change is indicated. The task stays active and unarchived.

Handoff checks pass: five documents are valid UTF-8 with final newlines, 192 local links/fragments resolve, all three frozen PowerShell callers parse, and `git diff --check` is clean. Documentation impact: updated this plan, roadmap, plan index, task progress record and progress index; no public capability, command, requirement, ADR or completed-task amendment is needed for the partial result. HEAD/branch are unchanged, index and maintained scratch are empty, no lease is active and no model runtime is running. No commit, publication, push or later-task execution occurred.

### M204-RESUME-03 — authorized canonical-value correction

The owner explicitly approved one guarded citation-comparison correction, renewed readiness review and one controlled supported-case repeat, preserving the failed evidence. Use workflow `M2-04-20260909-01`, slice `M204-A-REVIEW`, evidence attempt 3, lease `M204-A-REVIEW-EVIDENCE-03`, parent `M204-A-REVIEW-EVIDENCE-02`, owner `m204_test`. This is the sixth authorized evidence turn overall, not a reset. One write turn, no further correction, one renewed readiness review and one S repeat are granted. The three original real activations remain unchanged.

Attempt-3 justification: the preceding turn fixed and exercised actual card selection and delayed citation rendering; independent review and all 449 tests pass. The first S execution then supplied materially new evidence: exact canonical values match, but strict object equality includes internal prototypes. Read-only reconstruction reproduces that mismatch and proves normalized validated JSON equality. A different bounded action compares canonical wire values, with decisive matching and altered-value regression; no production or scope change is necessary.

Packet v2: retain M204-CASES-01 observable acceptance, Accepted authorities, frozen inputs, EXISTING_BUT_UNCOVERED preflight and S3 identity/preservation risk. Only `tests/helpers/m204-retrieval-checkpoint.ts` may change; no directory allow-list. Forbid `src`, `docs`, `corpus`, `evaluation`, `fixtures`, the checkpoint fixture and retrieval-service test. Production placement/interface changes: None. Driver owns proof comparison and bounded coordination; reuse actual admission/resolution and existing browser helpers. Permit only local sharing of the canonical JSON-value comparison with preparation. Do not loosen production validators, change data/roles/ranking, add dependencies/frameworks/files, or modify established seed/service tests. Inspect all related comparisons and callers before coding. Add a no-model preparation regression using the same comparison: admitted null-prototype citations must match canonical values; altered citation text, URL or order must still fail. Existing delayed browser proof stays bounded to one browser/API lifetime and its current pages; no additional model or B case belongs to the worker.

Before writes, primary verifies the complete ordinary S tree, its recorded receipt/run hashes and absent contained destination, then preserves it as `s-failed-prototype-01`. Preserve hash-checked `preparation.json` as `preparation-before-review-03.json`, with no replacement. C-PREP is unchanged: maintained README definitions-only preparation, both helper syntax checks, independent strict TypeScript, 18 service tests and driver `--prepare 250abacdc228fbce8a8c366c0e4638ba17132b31`, once after the coherent correction, using the UI scratch wrapper and sandbox escalation. Native failure stops later commands. Relevant source/test/build/corpus/config and retained-browser fingerprints remain the prior preparation identity except the explicitly revised driver. No-diff or decisive validation failure stops the turn; no automatic retry. Worker reports exact changes, command/results, fresh identities, cleanup and residual risks; no Git mutation, guard or documentation writes. Primary closes the lease and accepts the diff/evidence before renewed review and S/A/real effects.

After a passing renewed review, primary runs the unchanged S caller once against a fresh absent `s` root. The prior failed case stays failed and retained. A and G1/G2/G3 retain their original unspent callers. Z/F/I/C and the final 449-test suite remain valid at unchanged tested boundaries; the current correction cannot affect their empty/failure or production paths, and the new nonempty comparison receives fresh focused/browser/S/A/real evidence. Reuse the unchanged source build and independently refreshed strict check; do not repeat unrelated suites merely because the unused comparison helper changed. Final integrated critical review must assess that evidence mapping.

### M204-RESUME-EVIDENCE-03 — canonical comparison correction

Lease `M204-A-REVIEW-EVIDENCE-03` closed compliant, contract digest `0c840840d43b5c615b15cf73412e764f9aeffdb4932817dcc25acc43c7afcf91`, receipt `bac8ea74b0666f259635c52257ba786d4f25174a252e843e9f7b70d79adc8a11`; only the driver changed, with no HEAD/index/unleased drift. Driver SHA-256 is `FBBEB9145C20AAFC76DFCDBEAE468499FF4E87D7FFB21ABDB0D2801FDEA15383`, preparation SHA-256 `79E9D69F75CA10842CE02B43342F6FCED8E206E81F2AE28A55713322686202DC`. The failed S archive retains its exact three files and the earlier preparation retains SHA-256 `F86CF442FA1BA26FCA98C9A2B22650948F330D6EEC7F6D51BC277D158BB50229`.

C-PREP passed once: both syntax checks, independent strict TypeScript, 18 service tests and existing browser preparation. The primary inspected the new shared comparison and confirmed all 96 artifact hashes and eight unchanged seed/query pairs. The comparison normalizes ordinary/null record prototypes and key order while retaining finite primitive values, array order and every enumerable data field. It is used after production admission and independent canonical resolution, not as a new application parser. Preparation accepts the real null-prototype admitted view and rejects changed text, URL and passage order. The prior card-selection and delayed-passage browser probes still pass; actual resources closed and scratch is empty (`serviceStopped: false` remains not applicable in the API-only preparation lane). No S repeat, A case, runtime startup or real activation occurred in this correction turn.

Renewed critical review **PASS** is accepted, with no Blocker, Major or Minor findings. The reviewer independently exercised the exact comparator against preserved S and empty Z values; changed text, URL, order, score and passage removal all failed. It matched all 96 artifacts, seed/query identities, three historical preparation receipts and the three failed-S files. It confirmed the unchanged validated-input boundary, retained browser fixes and evidence reuse for Z/F/I/C and the 449-test suite. Readiness now permits the single authorized S repeat and unspent A/G1/G2/G3 callers; it does not replace the different fresh integrated reviewer.

### M204-B-ACCEPT-01 — bounded checkpoint observations

After the accepted canonical-comparison re-review, the single authorized S repeat and A each passed with exit 0. S showed supported eligibility while preserving its service owner; A showed incomplete guidance missing interpretation and remediation, terminal explanatory abstention and no generation call. The failed S remains unchanged at `s-failed-prototype-01`. Z/F/I retain the already accepted results in M204-B-S-01; the independent reviewer explicitly confirmed their unaffected evidence boundary. C remains the passed pure policy/display and canonical-rejection proof, never persisted conflict evidence.

The approved runtime started once after the effect-free path-check correction: PID 33516, start `2026-09-09T17:46:33.4679441Z`, owned listener `127.0.0.1:11434`, unchanged Ollama 0.33.3 and fixed EmbeddingGemma identity/configuration. G1, G2 and G3 then each ran once, sequentially in fresh service lifetimes and exclusive roots, with exit 0. Each used one real UI guidance activation, unmodified HTTP dispatch, uninjected default retrieval, independently authenticated citations and validated durable readback. Only the Analyze response supplied its explicitly synthetic completed-scan seed. No fresh scanner observation is claimed.

| Profile | Ranked passages and exact scores | Frozen gold observation | Role coverage and workflow |
| --- | --- | --- | --- |
| G1 image | `h67-ignored-image` 0.5111375808580503; `h37-text-alternative` 0.4914878432385412; `wcag22-sc111` 0.48338010884526506 | PASS: H37 and SC 1.1.1 are acceptable alternatives | Criterion/remediation present; interpretation missing; incomplete, abstained |
| G2 email label | `understanding412-intent` 0.3886159586729352; `h44-label-applicability` 0.3766893138624733; `h44-explicit-label` 0.37651744701792195 | PASS: all three are acceptable alternatives | Interpretation/remediation present; criterion missing; incomplete, abstained |
| G3 contrast | `g18-contrast-remediation` 0.5096829257075763; `understanding143-threshold-measurement` 0.5016003654533153; `understanding143-intent` 0.497414152808573 | PASS: all three are acceptable alternatives | Interpretation/remediation present; criterion missing; incomplete, abstained |

Relevance is separate from role completeness: G1's first-ranked H67 discusses intentionally ignored images and is not one of that profile's direct-support gold targets; H37 and the criterion supply its two gold hits. G2/G3 return directly relevant accepted targets but lack a required criterion role. None of the three real sets is supported, and none led to generation. These bounded observations satisfy the frozen at-least-one-alternative gold contract; they do not establish general retrieval quality or justify ranking/corpus changes. Scores, ordering, full canonical metadata/text/notices and exact seed/query identity remain in each receipt.

| Case | Receipt SHA-256 | Canonical file SHA-256 |
| --- | --- | --- |
| S repeat | `EA12C768DA2A6437CB86B781BC3A5EA51D9A5BF8C4B5A2518E115CA5BB5B9C6C` | `742ADCE0388CFCD5A01A6765DBB839DA076320C09725373ED66F508317428022` |
| A | `7643956F5A8413685A05C93B341287A240C1987115869FEC88485E814A45AD3F` | `413464FB5AF3FFAC0C5D79E2B4998714ED31E938BC35CB37893F3C047E3E0BAF` |
| G1 | `04ABB5352BB96923A9477513BD3C221CE675B3D29BAE6038A0C2EF37CA00783F` | `9292D3C1EC28466FB5FEB6CAEBBC23F39C570D6EFC4C2D3C2767351BF57F4C8E` |
| G2 | `ECA7511B3DDC515D52C49EFB577E32377099D2E88934C5108520C10674204171` | `0471BFE4BF569CF3BF16E746AE4A0F394B2F0280A256D9777F8C8C489971077D` |
| G3 | `C7F410E5058127E119FE42EE3DD582796F3326285437D5B974A6AE79310E88ED` | `AB4950B5A2B79260189249A38B524C35FDC83256F803D27EFAFFBA781F5F4147` |

Primary independently compared each successful receipt with its retained canonical file and confirmed one exact selected guidance request, no failure/timeout, and unchanged returned/disk/repository/service values. All started application resources closed normally; real-case `apiClosed: false` is not applicable because no extra controlled API server exists. The application service, browser/context, owned listeners and scratch checks pass in every case. The three real activations are exhausted. Runtime logs corroborate 51 `/api/embed` requests plus 22 metadata requests (3 version, 6 tags, 6 show, 7 ps), 73 total within the predeclared maximum 75; no generation route appears. This is aggregate runtime-log corroboration, not injected per-case transport instrumentation. The approved runtime remains a developer session; model residency is not claimed to be unloaded.

Manual visual inspection covered the saved full-page G1/G2/G3 desktop and A desktop/narrow samples on the unchanged build, Chromium 151.0.7922.34, desktop viewport 1280x800 and A narrow viewport 320x800. The selected detail shows native evidence separately from passage text, canonical links, source status/notices, ranking, role support and no-call abstention. Long text/notices wrap without horizontal clipping or overlapping content; selected-state and guidance-button focus indicators are visible. The bounded Findings pane remains independently scrollable. Exact text/link equality and no horizontal overflow are also checked by the driver. Existing UI suites supply keyboard/focus/announcement, disabled/in-flight actions and citation-navigation checks. No manual screen-reader or actual full-detail 200% proof is claimed; the latter remains M6-03's explicit deferral.

Primary accepts B under the frozen contract. Proposed final disposition is **proceed to separately authorized M3-01**, conditional on different fresh integrated review and documentation closure. No production, corpus, gold, runtime configuration, provider or later-task change was needed.

### M204-FINAL-01 — integrated review and task closure

A different fresh read-only `critical_reviewer` completed S3 integrated review on 2026-09-09 with **PASS**, no Blocker, Major or Minor findings. The primary accepts the review and the underlying retained evidence. Its checklist covered Accepted authority and freeze conditions, cohesive test-only responsibilities, real versus controlled provenance, citation and aggregate integrity, ownership/recovery, guarded corrections and preserved failures, evidence reuse, cleanup and bounded visual claims.

The reviewer independently reconstructed all eight successful cases from frozen seeds/queries and retained canonical runs, recomputed citations/support/analysis, matched all 98 preparation fingerprints (96 artifact entries plus two package hashes), and rejected 24 citation mutations. It accepted the historical-driver difference for Z/F/I and reuse of the unchanged thirteen-suite boundary, inspected all 449 passing test results, and freshly passed independent strict TypeScript. It independently inspected all three real desktop samples and A desktop/narrow samples. No live retrieval or browser operation was repeated. Reviewed implementation identity remains driver `FBBEB9145C20AAFC76DFCDBEAE468499FF4E87D7FFB21ABDB0D2801FDEA15383`, fixture `C6011F1378D346C1072427DAD6BDC49DE182446DBFAC42574FFCC44A7DAA58AC` and service test `DC94027334AC3864CEE0C98F0589CC8BC1CE572FBD2A8190A075D87CC55E8932`; preparation is `79E9D69F75CA10842CE02B43342F6FCED8E206E81F2AE28A55713322686202DC`.

The six worker leases are terminally compliant, with no active lease. HEAD and branch remain the entry values and the index is empty. Native scan/sibling evidence, all original generated-root children, frozen corpus/evaluation/fixtures, application source, dependencies and client-build bytes are preserved. Started application/browser resources closed; maintained scratch and `data/runs` are empty. The approved runtime remains the retained developer session. Its logs corroborate 51 embeddings plus 22 metadata requests, all HTTP 200 and within the frozen bound; no generation route appears. Failed S and all superseded preparation receipts remain preserved.

Documentation impact: Updated the public README, roadmap, this ExecPlan, plan index, task progress record and progress index to reconcile accepted observations, final status, limitations and archive navigation. Requirements, ADRs, specifications, configuration and frozen inputs need no amendment. Primary closure checks pass six-document UTF-8/final-newline validation, all 256 local links/fragments, syntax for the three retained PowerShell blocks, status/scope consistency, preservation and `git diff --check`. Task Verification and the documentation gate pass. Post-archive navigation and new-artifact whitespace checks also pass; the roadmap has twelve Complete and sixteen Not started tasks.

M2-04 is **Complete**. Disposition: **proceed to separately authorized M3-01**. All three actual sets correctly abstained, so supported eligibility remains controlled evidence. G1's first-ranked H67 relevance limitation and the pure-only conflict boundary remain explicit; no general retrieval-quality, generation or release qualification is claimed. Cleanup deadlines remain cooperative. Screen-reader testing is excluded and actual full-detail 200% proof remains deferred to M6-03. No commit, push or later-task execution occurred.

### M204-VERIFY-01 — unaffected regression and preserved handoff

C-FULL passed all thirteen maintained suites sequentially: 449 tests, zero failures, cancellations, skips or todos. Exact per-file outputs are retained under `temp/m204-retrieval-checkpoint/verification`. This includes the deterministic S/A/Z/F/I service characterization, pure conflict/normative and canonical-rejection checks, isolated conflict display, automated accessibility, keyboard/focus/announcement, scanner and production-entry coverage. It establishes those controlled contracts, not actual G1/G2/G3 results or the unexecuted controlled service-to-browser cases.

The UI suite explicitly retained fresh synthetic compact samples under `temp/m203-ui-d7c132b3-78a2-4063-a4b6-be0ef744621b`; its logged evidence event identifies the root and viewports. Primary viewed both 1280x800 and 320x800 images: visible regions have legible grouping and no overlap or horizontal clipping. The narrow image contains the results/list, not the selected detail. Neither image substitutes for the planned A detail or actual-case samples. Screen-reader testing remains excluded, and full-detail actual 200% proof stays deferred to M6-03.

All ten frozen corpus/evaluation/fixture files and every original generated-root child are preserved. The only changes to entry-tracked non-document content are the intended service characterization; application source, package files, completed plans and configuration are unchanged. Rebuilt client bytes match the preserved entry build. Maintained scanner/UI/integration scratch and `data/runs` are empty; no M204 case root or runtime-start root exists, no Ollama/runner process is present, and no lease is active. The index is empty; HEAD and branch remain the entry values. Retain the new suite screenshots, build backup, preparation and verification evidence. No commit, publish, push or M3 work occurred.

Documentation impact: update this plan, roadmap, active-plan index, task progress record and progress index to the reviewed Blocked state. Public capability/developer commands, requirements, ADRs, corpus and completed-task records need no amendment. The plan stays active and unarchived. Documentation checks at this handoff do not satisfy the still-missing checkpoint Verification or integrated-review gates.

### M204-CALLERS-01 — bounded evaluation callers

Cwd remains the repository root. Each case uses a new PowerShell session with the maintained definitions-only README preparation. After fresh A review, execute controlled S, A, Z, F and I one at a time; C is covered by the unchanged policy, canonical rejection and isolated UI suite. Then start the already installed runtime below and execute real G1, G2 and G3 one at a time. Each invocation must settle, retain its receipt and close its app resources before the primary accepts or triages it and starts the next case. A semantic gold miss does not permit retry; unaffected cases may continue only after shared prerequisites and cleanup are confirmed.

Exact case caller is the following, with the eight predeclared literal pairs `--controlled S`, `--controlled A`, `--controlled Z`, `--controlled F`, `--controlled I`, `--real G1`, `--real G2`, `--real G3`; the illustrated first pair is not a loop or retry:

```powershell
Assert-M105EmptyDirectory $m105UiTemp
Invoke-M105Command {
  & $m105Node tests/helpers/m204-retrieval-checkpoint.ts --controlled S 250abacdc228fbce8a8c366c0e4638ba17132b31
  if ($LASTEXITCODE -ne 0) { throw 'M204 case failed; preserve evidence and return to primary triage' }
} $m105UiTemp
Assert-M105EmptyDirectory $m105UiTemp
```

Use the sandbox escalation route for local browser/model-boundary execution rather than consuming a case on a known network sandbox restriction. This changes the execution environment, not browser isolation or target scope. The driver owns one Analyze seed response, one UI guidance activation, readback, full-page real/A samples and A at 320 px, and contained cleanup. Its outer 330000-ms cancellation and service/browser deadlines are cooperative, not an OS-kill guarantee. Receipts distinguish source-derived request limits from observed UI request counts. Initial-seed publication failure is before browser/model work; retain the created case root and caller failure for triage even if no completed driver receipt exists.

C-FULL uses the exact thirteen-file sequential README suite and scratch mapping, with per-file native-exit checking and exclusive logs under `temp/m204-retrieval-checkpoint/verification`. After M204-RESUME-EVIDENCE-02, repeat that same caller once with only the output root changed to the absent `temp/m204-retrieval-checkpoint/verification-final`, preserving the earlier logs. This final corrected-tree regression may overlap read-only A review; all browser/service execution remains serialized and no B case begins until it settles. Required build evidence is C-BUILD above; unchanged source and identical current build bytes preserve that result. Strict TypeScript was independently repeated after the final helper change. Reuse requires unchanged relevant fingerprints. C-CLEAN retains case runs/receipts, preparation, build backup, verification logs and developer assets; normal app stop, browser/context closure, owned-port closure and empty UI/scanner/integration scratch are required. No broad deletion or existing-evidence cleanup is permitted.

The existing runtime is installed but stopped. Binary, manifest, four blob identities and retained finite-input receipts were checked without model calls. The startup caller below reuses the same child-only settings as M2-02's ordinary developer session. No install, pull, configuration-file change, probe embedding or capacity rerun is authorized. The exact PID/start/listener is retained; startup failure retains its partial output and forbids replay pending triage. The resulting runtime stays as the developer session after app cases, with no claim of unloading model residency. This is an ordinary startup, not the historical acquisition/debug launcher.

Primary caller triage: its first invocation rejected `Wrong cwd` before any effect because the literal used forward slashes while PowerShell returned backslashes for the same directory. Inspection confirmed the actual repository cwd, absent runtime-start root and absent runtime processes. Normalize both operands with `GetFullPath` below, preserving the exact same-directory requirement. This is a primary-owned command-literal correction between leases, not another worker edit or runtime-start attempt; no process, directory or model request occurred. The one approved ordinary startup remains unspent.

```powershell
$ErrorActionPreference='Stop'
$repo='C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab'
$runtime='C:/Users/mmjos/Tools/Ollama/v0.33.3'
$exe=Join-Path $runtime 'ollama.exe'
$models='C:/Users/mmjos/Models/Ollama'
$owned=Join-Path $repo 'temp/m204-retrieval-checkpoint/runtime-start'
if([IO.Path]::GetFullPath((Get-Location).Path) -ine [IO.Path]::GetFullPath($repo)){throw 'Wrong cwd'}
if(Test-Path -LiteralPath $owned){throw 'Runtime-start root already exists'}
if(@(Get-Process -Name ollama -ErrorAction SilentlyContinue).Count){throw 'Existing runtime requires reconciliation'}
if(@(Get-NetTCPConnection -State Listen -LocalPort 11434 -ErrorAction SilentlyContinue).Count){throw 'Port already occupied'}
if(@(Get-ChildItem Env: | Where-Object {$_.Name -like 'OLLAMA*'}).Count){throw 'Unexpected Ollama environment override'}
if((Get-FileHash -LiteralPath $exe).Hash -cne 'E4FE6BD835FE146659F5C969DCCAFF2E25A9DE63D90EE204CA5D11B9034B0CA5'){throw 'Runtime identity mismatch'}
$manifest=Join-Path $models 'manifests/registry.ollama.ai/library/embeddinggemma/latest'
if((Get-FileHash -LiteralPath $manifest).Hash -cne '85462619EE721B466C5927D109D4CB765861907D5417B9109CAEBC4E614679F1'){throw 'Model identity mismatch'}
foreach($candidate in @($repo,(Join-Path $repo 'temp'),(Join-Path $repo 'temp/m204-retrieval-checkpoint'),$runtime,$models)){
$item=Get-Item -LiteralPath $candidate -Force
if(-not $item.PSIsContainer -or ($item.Attributes -band [IO.FileAttributes]::ReparsePoint)){throw 'Nonordinary prerequisite'}
}
New-Item -ItemType Directory -Path $owned | Out-Null
$runtimeTemp=Join-Path $owned 'temp'
New-Item -ItemType Directory -Path $runtimeTemp | Out-Null
$childEnvironment=@{OLLAMA_HOST='127.0.0.1:11434';OLLAMA_MODELS=$models;OLLAMA_NO_CLOUD='1';OLLAMA_NOPRUNE='1';OLLAMA_DEBUG_LOG_REQUESTS='0';TEMP=$runtimeTemp;TMP=$runtimeTemp;HTTP_PROXY=$null;HTTPS_PROXY=$null;ALL_PROXY=$null;NO_PROXY=$null}
$process=$null
try {
$process=Start-Process -FilePath $exe -ArgumentList 'serve' -WorkingDirectory $runtime -WindowStyle Hidden -PassThru -Environment $childEnvironment -RedirectStandardOutput (Join-Path $owned 'stdout.log') -RedirectStandardError (Join-Path $owned 'stderr.log')
$started=$process.StartTime.ToUniversalTime().ToString('o')
$deadline=[DateTime]::UtcNow.AddSeconds(10)
$listener=@()
do{
$process.Refresh()
if($process.HasExited){throw 'Runtime exited before ready; preserve outputs and do not retry'}
$listener=@(Get-NetTCPConnection -State Listen -LocalAddress 127.0.0.1 -LocalPort 11434 -ErrorAction SilentlyContinue)
if($listener.Count){break}
Start-Sleep -Milliseconds 200
}while([DateTime]::UtcNow -lt $deadline)
if($listener.Count -ne 1 -or $listener[0].OwningProcess -ne $process.Id){throw 'Runtime listener ownership unavailable; preserve output and do not retry'}
$receipt=[ordered]@{id='M204-RUNTIME-01';pid=$process.Id;startUtc=$started;executable=$exe;listener='127.0.0.1:11434';modelManifestSha256=(Get-FileHash $manifest).Hash;configuration='unchanged approved M202 developer configuration';metadataRequests=0;embeddingRequests=0;generationRequests=0;disposition='retained developer session'}
$receipt | ConvertTo-Json | Set-Content -LiteralPath (Join-Path $owned 'start.json') -Encoding utf8NoBOM
$receipt | ConvertTo-Json
} catch {
$failure=[ordered]@{id='M204-RUNTIME-01';status='failed';error=$_.Exception.Message;pid=if($process){$process.Id}else{$null};outputRoot=$owned;retryAuthorized=$false;cleanup='preserved for ownership reconciliation'}
$failure | ConvertTo-Json | Set-Content -LiteralPath (Join-Path $owned 'failure.json') -Encoding utf8NoBOM
throw
}
```

### M204-ENTRY-01 — authorized execution entry

The actual entry is clean HEAD `250abacdc228fbce8a8c366c0e4638ba17132b31`, branch `codex/m2-04-retrieval-checkpoint`, 218 tracked paths, empty index and no active lease. The intentional planning commit is the current baseline. The ignored `logs/m204-entry-01.json` receipt (SHA-256 `BDF3F9ADA44D3BC22C695B89479F06881420521C731FC791B8BEBDBF71AE41A3`) records all tracked checkout hashes, installed Node/compiler/lock identities, generated-root inventory and all three existing client-build hashes. `temp/m204-retrieval-checkpoint` was absent. Existing M202/M203 evidence, browser runtime, dependency backups, build backups, scratch roots and developer runtime assets are preserved. No pre-existing dirty path requires ownership reconciliation.

M2-03's renewed closure and M2-02's passed capacity gate were read at their linked authority anchors. Applicable Accepted requirements and the fixed three gold profiles remain unchanged. C-BASE passes independent strict TypeScript and all 119 tests, with no failures, skips or todos, using Node 24.20.0 and the maintained preparation definitions. This is entry verification, not a new capacity or gold result.

Runtime-exposed turn metadata confirms the primary session uses `gpt-6-astra` with `high` effort. The discovered custom roles retain `test_worker` Sol/medium and `critical_reviewer` Astra/max; no configuration was edited. Official [subagent configuration guidance](https://learn.chatgpt.com/docs/agent-configuration/subagents) was consulted; the local turn metadata, rather than prompt text or defaults, establishes this session setting.

Workflow `M2-04-20260909-01`, slice `M204-A`, read-only assignment `M204-A-PREFLIGHT-01` uses the persistent `test_worker` owner `m204_test` with no lease or write scope. Its Packet v2 freezes the plan A/B case boundaries, S3 integrity/ownership/teardown risk, existing helper/test families, inspection-only caller and no model/browser effects. It must return coverage classification, minimum missing proof paths and exact preparation callers. Primary owns entry/authority/inventory work in parallel. No Green or research panel is presumed.

Planning changes only this plan, its progress record, roadmap activation and two indexes. During execution retain compact decisive case/evidence identities here and ignored synthetic run/receipt evidence under the reviewed task root; no new report platform, ledger or separate decision document.

### M204-PLAN-REVIEW-01 — accepted planning readiness

On 2026-09-09, a fresh read-only `critical_reviewer` reviewed the complete 198-line plan, exact five-file surface, controlling authorities and relevant source/test/history. Trigger: S3 citation/evidence identity, selected-only persistence, supported ownership and teardown/recovery. Verdict: **PASS for planning readiness only**, with no Blocker, Major or Minor findings. Primary accepts the result. During review, primary clarified A's applicable preflight/evidence route and reconciled roadmap/table consistency; the reviewer covered the final saved contract.

Reviewed plan SHA-256: `89E36AF76AC692BB34E1CF1A32E2960AAD52567B76028901229A1CA16392A8B0`. This identifies the pre-closure artifact, not a future executable baseline. Subsequent edits record acceptance without changing that contract. The reviewer independently confirmed current HEAD, empty index, exact five-file scope and absent active lease. It reused the fresh strict/119-test evidence and primary deterministic documentation/preservation checks; no model, browser, tests or writes were executed by the reviewer.

Primary planning validation passes five-file local-link/fragment, PowerShell syntax, UTF-8/final-newline/whitespace and `git diff --check` checks. The 216-path tracked baseline audit finds only the three intended existing Markdown changes; source, tests, packages, archived plans and frozen inputs remain unchanged. Roadmap status is eleven Complete, one planning In progress, sixteen Not started. Runtime availability, exact callers and seed identities, actual gold observations, browser proof and owned cleanup remain future gated work. No requirement, ADR, corpus or completed-task amendment is needed for this planning handoff.

## Interfaces and Dependencies

Reuse `LocalService.retrieveFinding`, `POST /api/finding-guidance`, `RunRepository` initial/update/read publication, `validateRun`, `createFindingQuery`, the default embedding/ranking engine, `resolveFindingCitations`, `classifyGuidanceSupport`, `buildFindingAnalysis`, browser admission and current guidance components. The task establishes an evaluation result and bounded test callers, not a new application API, serialization format or package. Existing pinned Node/npm, TypeScript, React/Vite, Playwright/axe, LangChain and the passed Ollama/EmbeddingGemma configuration remain the baseline.

## Revision note

2026-09-09: Created the M2-04 checkpoint plan from completed M2-03 and current source/test/authority evidence. Separated real gold retrieval from controlled adverse proof, preserved the conflict-free corpus and manual-verification limits, and planned test-only preparation plus bounded evaluation rather than another implementation subsystem. Recorded independent readiness PASS and primary planning-document closure without changing the reviewed execution contract.

2026-09-09 execution update: Recorded authorized entry, three guarded preparation turns, the corrected structural locator literals, passing strict/build and 449-test regression, fresh critical readiness failure, preserved evidence and the explicit correction-budget owner boundary. M2-04 is Blocked; no actual guidance activation or runtime startup occurred.

2026-09-09 closure update: Accepted the owner-authorized corrections, renewed readiness review, preserved failed S and successful repeat, all three actual retrieval cases and controlled checkpoint, bounded visual samples, 449-test regression, fresh independent strict TypeScript, different integrated critical review and documentation closure. Marked M2-04 Complete and archived this plan with repaired links. Earlier blocked states remain historical; the approved runtime is retained and M3-01 is Not started.
