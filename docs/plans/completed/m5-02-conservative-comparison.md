# Compare one baseline Finding conservatively

> Privacy note: Personal directory prefixes in this archived plan have been replaced with `C:/projects/a11y-evidence-lab` and `C:/Users/developer` (or their backslash equivalents). These are illustrative aliases, including in recorded commands and errors. Artifact names, hashes and outcomes are unchanged; the aliases must not be used to authenticate original path-bound evidence or replay historical operations. For current setup, use the [local startup guide](../../DEVELOPMENT.md).

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task and authority:** [M5-02](../../DEVELOPMENT_ROADMAP.md#m5-02--apply-comparability-correlation-and-outcome-rules), authorized on 2026-09-17 for G, sequential A–D, bounded corrections, verification and documentation closure. No M5-03, actual public scan, model call, acquisition, original-run mutation, commit or push is authorized.
- **Latest evidence:** [M502-FINAL-01](#m502-final-01--integrated-review-and-task-closure): 805 tests, strict/build, real controlled integration, corrected checkout preservation, different integrated critical re-review PASS and documentation closure.
- **Gates and allowances:** G and A–D are accepted. A used initial Red/Green; B used all three Red turns, two Green turns and its sole review loop; C used all three Red turns and initial Green; D used one setup turn and its sole integrated correction loop. All chains are retired; no active work or remaining correction is selected.
- **Active lease:** None.
- **Next:** None for M5-02. M5-03 and later tasks remain unselected. Reopen only through new owner authorization; preserve the historical evidence and limitations below.

## Progress

- [x] (2026-09-17 15:12Z) Review roadmap-wide status, M5-01 closure, comparison authorities and current service/scanner seams; strict TypeScript and 96 focused tests pass, with frozen inputs unchanged. See M502-ENTRY-01.
- [x] (2026-09-17 15:25Z) Accept independent critical planning PASS with no findings and five-document validation; M502-PLAN-01. No implementation or target/model operation occurred.
- [x] (2026-09-17) Receive execution authorization; inspect clean current HEAD `4309a8f05d56032241c5a9418467f5505a455680` and confirm no active lease. Historical HEAD is not an execution gate.
- [x] (2026-09-17) Accept G's complete literal, comparison-input and command contract; different final critical research PASS with no findings.
- [x] (2026-09-17) A: accept closed comparison admission and pair profiles after 107 tests, independent strict and fresh critical PASS. Composition ordering is B's proof obligation.
- [x] (2026-09-17) B: accept exact correlation and bounded evidence outcomes after corrected guarded TDD, 24 tests, independent strict and critical re-review PASS.
- [x] (2026-09-17) C: accept real rescan handoff consumption, truthful publication and six-state scanner integration after focused checks, independent strict and fresh critical PASS.
- [x] (2026-09-17) D: accept all 805 tests, strict/build, preserved inputs and cleanup, corrected checkout preservation, different final critical PASS and documentation closure; task Complete and plan archived.

## Surprises & Discoveries

The following entry observations describe the pre-implementation baseline; accepted slices and current behavior are recorded below.

- [M5-01 closure](m5-01-intentional-rescan.md#m501-final-01--integrated-review-and-task-closure) records 776 passing tests across 37 files, real controlled scanner/service/disk integration, fresh critical PASS and cleanup. The current clean checkout is committed at `3053b049e43ca22d161f954d81ff4505ca4711d7`; its archive's uncommitted-at-closure statement remains historical.
- `service.ts::rescanFinding` currently unwraps `(await executeRescanScan(...)).run`, discarding candidates. `prepareRescan` returns only the running run and selected rule, not the validated baseline selection/context needed by comparison.
- Native candidates contain selected-rule **passes only**. Normalization preserves duplicate locators in violations, incomplete observations and passes. Uniqueness must be established across the relevant later collections, not by counting passes alone.
- The RD-003 manifest freezes six states and their native expectations, not a directed comparison-pair table. Its limitations deliberately leave the comparison-only vector with its requirement authority. G must freeze a minimal comparison definition without silently rewriting the evaluated scan or generation manifests.
- The common synthetic repository fixture contains duplicate image locators and a different scan profile from production defaults. It is useful adverse evidence, not automatically a unique comparable success case.

## Decision Log

- Decision: select M5-02 planning after Complete M5-01. Rationale: the exact owner request selects one dependency-ready task, not all M5. Date/author: 2026-09-17 / primary.
- Decision: keep this task at service/pure-calculation and transient integration boundaries. Rationale: [M5-03](../../DEVELOPMENT_ROADMAP.md#m5-03--persist-and-present-comparison-evidence) explicitly owns durable comparison fields and visible presentation. Date/author: 2026-09-17 / primary.
- Decision: use existing normalized evidence, native fixtures and focused test mechanisms. Rationale: the accepted exact-match policy needs no dependency, registry, fuzzy matcher or new evaluation platform. Exact signatures and failure handling remain G decisions. Date/author: 2026-09-17 / primary.

## Outcomes & Retrospective

M5-02 is Complete. Conservative comparison validates complete source scans, gates exact profile compatibility before unique target correlation, and classifies only sufficient native evidence. The real rescan service consumes candidates during the operation and preserves truthful scan publication; current HTTP, disk and client contracts remain unchanged. All 805 tests, strict TypeScript, client build, three actual controlled pairs over six frozen states, and required critical reviews pass. The final review found and resolved a checkout-byte preservation gap through one exact LF attribute; passing current-tree tests alone was insufficient proof of frozen-input reproducibility. All owned scratch is empty, original inputs and retained runs are preserved, and Git changes are uncommitted. M5-03 owns comparison persistence and UI and remains unselected.

## Purpose / Big Picture

The service will calculate a conservative comparison for the selected baseline Finding during its intentional later scan. It first establishes whether the complete source scans are comparable, then correlates at most one exact target, then returns bounded rationale and evidence. A missing later violation is never itself proof of resolution. A reviewer can observe the result through the internal comparison boundary and controlled tests; public display and persistence wait for M5-03. The M5 milestone's full user-visible outcome is not claimed by this task alone.

## Context and Orientation

The [authority map](../../README.md#authority-and-status-map), [requirements semantics](../../PROJECT_REQUIREMENTS.md), and [roadmap authority-location key](../../DEVELOPMENT_ROADMAP.md#authority-location-key) control interpretation. Twenty-one RD/M1–M4/M5-01 tasks are Complete; activating this plan leaves one In progress and six Not started. Both capacity tasks are closed; their exact-configuration observations are not general support or model-quality evidence. The six fixed generation executions remain M6 work, not permission to repeat previous calls.

| Authority | M5-02 obligation |
| --- | --- |
| [Evidence and provenance](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#evidence-and-provenance), REQ-EVID-008/010; [scanning](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#target-authorization-and-scanning), REQ-SCAN-005 | Reuse minimized allowlisted evidence; preserve unavailable facts and source collections; locator is correlation evidence, not identity. REQ-EVID-002 supplies controlled-fixture provenance. |
| [Rescan and comparison](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#rescan-and-comparison), REQ-COMP-001–008 | Directed manifest-bound controlled pairs, public pair gate, exact unique correlation, native positive evidence, ordered contrast outcomes, downstream independence and prohibited-claim limits. Persistence/deletion presentation portions of 004/006 are M5-03/M5-04, not waived. |
| [Comparison lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#comparison-lifecycle), [repeated analysis](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md#repeated-analysis-provider-change-and-rescan) | Two complete scans, separate pair and Finding dispositions, independent linked later run; proposal/review/manual judgment never determine the outcome. |
| [Evaluation freeze](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#freeze-boundary), REQ-EVAL-004/005; [contrast vector](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#comparison-only-contrast-policy-vector); [BHV-06](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#derived-behavioral-scope) and [SPEC-006](../../specs/SPEC.feature) | Preserve frozen observations; distinguish native scan cases from arithmetic and reversed binary policy-only evidence. Rerun affected cases against changed comparison code without relabeling history. |
| [ADR-0009](../../architecture/decisions/ADR-0009-axe-core-as-initial-accessibility-scanner.md), [ADR-0018](../../architecture/decisions/ADR-0018-trusted-operator-url-boundary.md), [ADR-0021](../../architecture/decisions/ADR-0021-single-file-run-aggregate.md) | Existing scanner, trusted developer target and one aggregate; no raw archive, new target-security system or alternate writer. |
| [M5-01 accepted contract and evidence](m5-01-intentional-rescan.md) | Successful cleanup precedes candidate exposure; baseline linkage, reservations, terminal publication and response identity remain protected. |

All applicable MVP Must requirements and decisions are Accepted or explicitly Deferred. Ordinary task-owned literals may be resolved in G; a significant new architectural mechanism or changed requirement needs owner direction. The [comparison assessment](../../architecture/candidates/RESCAN_EVIDENCE_COMPARISON_ASSESSMENT.md) is Proposed input, not authority for adopting every suggested field or rule.

Entry boundaries (historical): `src/server/local-service/rescan-operation.ts::prepareRescan` validates the retained baseline/Finding under service reservation; `src/server/scan/scan-page.ts::executeRescanScan` returns a frozen successful `{ run, candidates }` envelope after cleanup; `normalization/native-rule-evidence.ts::NativeCandidate` contains detached minimized pass facts with no Finding ID. `local-service/scan-operation.ts::startScanOperation` validates/publishes the terminal run. `domain/run-contract` readers validate durable records, but do not validate the transient candidate array or establish cross-run comparability. There is no comparison owner yet.

## Scope and Non-Goals

Include validated internal inputs/result, pair comparability, exact selected-target correlation, at most one constructed minimized positive observation, conservative outcome/rationale/limitations, a bounded controlled-evaluation binding and consumption of M5-01's live candidates. Keep a public-runtime baseline Finding contract separate from evaluation-only positive-baseline vectors.

Exclude comparison fields in `run.json`, repository comparison updates, HTTP comparison payloads/routes, client/UI changes, broken-lineage presentation, arbitrary retained-pair selection, history, fuzzy/AI matching, fingerprints, DOM diff, page-level scores, materiality/tolerance engines, `new` classification, remediation causality, independent comparison IDs, general pass archives, callbacks/registries for hypothetical consumers, new dependencies and M5-03/M5-04/M6 execution. No actual public scan, model call, acquisition or original-run mutation is selected. This nonvisual task does not use the frontend-quality overlay; a material rendered change requires replanning into its owning task.

## Plan of Work

Follow the [agent workflow](../../../.codex/README.md), [worker-first procedure](../../../.codex/execplan-implementation-workflow.md), [ADR-0024](../../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md) and [write guard](../../../.codex/write-lease-guard.md). Primary owns authority, G's research-derived writes, exact assignment packets, evidence acceptance, integration, status and closure. Research/review remain read-only. No ordinary production edits by the primary.

For each A–C slice use a separate persistent `test_worker` and `code_worker`, read-only preflight and sequential guarded Red/Green/optional Refactor. One active lease per worktree; all accepted test-owned paths are forbidden during Green. Primary plan/status maintenance occurs between terminally closed leases. Inspect actual diff, cohesion, command evidence and terminal receipt, not just reports. Each S3 slice needs fresh `critical_reviewer` acceptance before advancing. Record RETAINED, REFACTORED or RECONCILE after Green; rerun focused checks after actual refactor. No visual reviewer or frontend worker is needed.

Budget each unchanged writer role/phase chain at initial attempt plus one ordinary correction and one conditional final correction only with documented progress/new evidence under the procedure. One read-only preflight and one coherent Red/characterization boundary per slice; one review correction loop. Repeated decisive failure twice without new evidence, two no-diff handoffs, changed binding fields or exhausted allowances return to primary triage. Agents and IDs never renew budgets. Covered behavior reuses valid evidence; uncovered existing behavior receives passing characterization; missing/regressed behavior receives honest Red. The first-module exception requires its exact environment/missing-callable/complete-test conditions; no production stub or fake Red.

The smallest proof combines pure comparison tests with the existing scanner/rescan integration mechanism. Add only missing bounded tests and static input bindings; no separate runner or reporting framework. Read-only G dimensions may overlap; all source/test writes and browser/service verification remain sequential.

### G — Freeze comparison and handoff literals

**M502-G; R3; TDD: Not applicable.** None — no application-source responsibility changes. Replacement evidence: complete authority/source mapping, frozen literal and input contract, required research reviews and exact command/effect preparation. Resolve these before A preflight:

1. **Admitted sources and ordering.** Exact internal signatures, closed input/result/error shapes, selected baseline/Finding and later-run linkage, candidate envelope ownership, selected-rule/count/coverage consistency and reflection-safe validation. Invalid or noncompleted sources fail with no comparison, not `not comparable` or `inconclusive`. Required individual unavailable facts remain valid evidence and follow conservative classification. Do not weaken `validateRun` to admit policy vectors or unsupported profiles; profile mismatch witnesses must themselves be valid inputs or explicitly isolated policy-boundary tests.
2. **Pair key.** Map normalized requested and observed final page identity, exact rule profile, viewport, locale, browser/scanner, evidence policy, coverage prerequisites and applicable contrast measurement profile to existing fields and exact equality rules. Resolve readiness/timeout/other context relevance from authority; do not compare entire runs or include mode, timestamps, run/Finding IDs, application revision, proposal/review state or evidence values merely because they differ. Complete bucket counts may change between scans; completeness is not equality of violation/pass counts. Mismatched valid profiles produce a bounded pair-only rationale without invoking correlation. Unknown required provenance cannot become silent compatibility.
3. **Correlation and evidence.** Exact public rule/locator equality across later Findings, native incomplete observations and selected-rule passes; no order-based winner or deduplication. Freeze disposition for unavailable baseline locator, no match, duplicate/cross-bucket conflict and unique incomplete evidence. Do not claim a target was relocated when the data establish only no exact match. A unique incomplete match remains inconclusive, never a positive observation. Unmatched/ambiguous cases have no fabricated current target/after-evidence. Freeze the minimum rule-specific facts needed for a valid native pass observation, its context provenance and one-object maximum; no ID, generic pass archive or raw native payload.
4. **Outcome and result contract.** Pair mismatch has no per-Finding outcome or match section. Comparable binary failure-to-failure is persistent; binary runtime improved/regressed are impossible. Native pass plus unique match and complete coverage may resolve; absence or ratio arithmetic alone cannot. Contrast ordering uses retained measured minus expected ratio only for exact matched failures with identical measurement semantics and sufficient finite evidence. Freeze required measurements/checks, expected-threshold/font-profile relationships, numeric representation/equality and missing/conflicting evidence handling without inventing thresholds, rounding tolerances, color recomputation or an accessibility score. Result carries baseline references/evidence, only uniquely matched later evidence, bounded rationale, limitations and non-blocking follow-up wording; context never affects calculation.
5. **Controlled definition.** Freeze the exact three directed failing-to-corrected pairs using RD-003 scenario/rule/stable key/revision/state roles and input identities; reject undeclared directions or profile mismatches before controlled correlation. Stable keys stay in test/evaluation metadata, not product identities or locator substitutes. Define reused-failure persistence, missing/duplicate and drift witnesses explicitly. Reversed binary positive-to-violation and the accepted 3.54/4.00, expected-4.50 contrast vector are policy-only inputs, not scans, persisted runs or baseline Finding records. Share production classification logic; add no duplicate evaluation comparator. Select the smallest versioned task-owned companion definition necessary for REQ-COMP-001, with exact location and provenance before tests. Preserve both existing manifests, six fixture bytes and original observations; never retrofit directions into their historical bytes. Any material baseline change follows the evaluation authority and needs owner direction where its boundary is exceeded.
6. **Service/lifetime/failure boundary.** Freeze the minimal internal handoff carrying validated baseline selection and successful later envelope into comparison before candidates are discarded. Return a bounded internal calculation result for M5-03 to consume later; keep current HTTP/run outputs unchanged and do not add a debug endpoint, retained service cache or test-only production observer. Define when this internal result is authoritative relative to terminal run validation, publication, shutdown and failed writes. A failed/late/cancelled/cleanup-uncertain operation exposes no successful comparison. A comparison validation/implementation exception must not invent scanner failure or cleanup uncertainty; bind exact existing-compatible outward failure behavior and truthful preserved scan state before coding. If that requires changing an Accepted lifecycle or M5-03 scope, stop for owner direction rather than hiding it as a literal.
7. **Commands/effects.** Freeze precise A/B/C focused commands, current full suite/build/strict commands, controlled browser inputs, finite deadlines, scratch/evidence paths, allowed generated outputs and cleanup before their applicable effect. Future filenames below are candidates until exact packets bind them; workers cannot invent scope after review.

### A — Validate inputs and gate the scan pair

**M502-A; S3; TDD applicable.** Observable contract: admitted complete sources produce either comparable context for downstream correlation or a pair-only mismatch; malformed/noncompleted sources fail. Preflight targets existing `tests/run-contract.test.ts`, `tests/scan-normalization.test.ts` and a candidate `tests/comparison-pair.test.ts`.

Bounded creation under `src/server/comparison/`: purpose-owned input/result types and validation plus `scan-pair.ts` (exact filenames frozen by G). Reuse domain readers and validated scan/fact types. If access to an existing private evidence reader is necessary, permit only its cohesive export/local extraction in `src/server/domain/run-contract/`, with unchanged behavior and regression coverage; no new general validation layer. Dependency: comparison → existing pure domain readers/types; never domain → scanner/service/filesystem. Pair gate owns context equality, not correlation or I/O. Test-owned helpers belong under `tests/helpers/` only as immediately needed.

Prove every material dimension, valid count differences, provider/downstream independence, unavailable-versus-malformed facts, closed input validation and zero correlation calls on mismatch. Protect source/test baselines and forbid client, repository mutation, dependencies and frozen artifacts. Accept strict/focused results, cohesive diff and fresh critical PASS before B.

### B — Correlate and classify one selected target

**M502-B; S3; TDD applicable.** Observable contract: A's comparable pair yields one conservative evidence result without mutating either source. `test_worker` owns candidate `tests/comparison-outcome.test.ts`, the declared controlled input bindings and necessary pure helpers; `code_worker` owns exact correlation, native-positive projection, rule-specific outcome and bounded rationale modules in `src/server/comparison/`. Primary owns research-derived companion definition/documentation, between leases; workers consume it, not choose its meaning.

Separate current responsibilities: `target-correlation.ts` counts exact matches over the complete relevant later collections; `finding-outcome.ts` classifies native binary/contrast evidence; `compare-finding.ts` composes A → correlation → outcome/result. Keep a small positive-observation projection with its evidence owner rather than a generic registry. Exact module names may be reconciled in G for actual cohesion; no per-field wrappers or one giant comparison utility. The same classification functions serve policy-only tests; their positive-baseline adapters remain test-only and cannot mint public runtime Finding IDs.

Tests cover unique pass resolution, unique failing binary persistence, higher/equal/lower failing contrast margins, missing required evidence, incomparable profiles before correlation, missing/withheld/changed locators, duplicate pass/failure/incomplete conflicts, unique incomplete, target removal, unrelated later Findings and source immutability. Use frozen state provenance for native evidence and clearly labeled policy vectors for arithmetic/reversed binary regression. No inference of native buckets from ratios. Keep rationale deterministic, bounded, content-safe and free of certification/causality claims. Fresh S3 review, focused cases and independent strict gate C.

### C — Consume the real transient rescan boundary

**M502-C; S3; TDD applicable.** Observable contract: the production rescan path invokes the accepted comparison policy for its selected baseline and complete later scan while candidates are live, without leaking the candidate collection or persisting/publishing comparison data in M5-02.

Extend `local-service/rescan-operation.ts::prepareRescan` only for the bounded internal validated baseline selection. Create a cohesive `local-service/rescan-comparison.ts` if needed to own scanner-envelope admission and comparison invocation; `service.ts` remains reservation/composition. `local-service/scan-operation.ts` retains terminal validation/publication and settled-capability retirement. Permit only the exact internal executor/type adaptations G requires; do not put classification in those coordination modules or modify scanner capture/profile merely to fit comparisons. Direction: reserved service → admitted baseline → real scanner → pure comparison → existing terminal publication. No provider, client or second writer edge.

Preflight targets `tests/rescan-service.test.ts`, `tests/scan-page.test.ts`, `tests/rescan-integration.test.ts` and candidate `tests/rescan-comparison.test.ts`. Reuse current harnesses for controlled real Playwright/axe evidence from the six frozen states; assert actual production comparison results at the internal boundary without a production instrumentation framework. Preserve existing recursive HTTP/disk no-candidate/no-comparison witnesses. Test failed/invalid envelopes, selected-rule/coverage mismatch, comparison exceptions, reentrant stop, deadlines, cleanup uncertainty and failed terminal publication; no stale success escapes, source bytes stay identical and successful durable rescan remains usable as before. A test double alone cannot prove real scanner-to-comparison integration. Obtain fresh critical review before D.

### D — Verify and close the task

**M502-D; primary integration; TDD: Not applicable.** None — no application-source responsibility changes. Replacement evidence is complete accepted implementation and controlled integration proof plus task-wide regression, invariant review and documentation reconciliation.

Run the current complete authoritative suite including new accepted tests, independent strict TypeScript and client build. Reuse identity-matched C integration evidence; repeat only on drift or missing/contradictory/risk-critical coverage. Inspect test relevance, skipped/focused markers, all source changes and owned resource cleanup. Obtain a different fresh integrated `critical_reviewer` over I1–I10 and the changed-surface quality baseline. Reconcile README capabilities/commands, lifecycle's now-implemented calculation description, task routing where useful, roadmap, plan/progress indexes and the task summary. Do not announce public comparison UI or persistence. Pass the [documentation closure gate](../../README.md#task-closure-documentation-gate), then Complete/archive with repaired links. M5-03 remains unselected.

## Decision Review Contract

**M502-G, R3:** identity/correlation, evidence integrity, transient lifetime and failure/publication semantics trigger this route. Target is the primary-written literal and controlled-input contract in this plan plus only its necessary static comparison definition. Criteria: exact Accepted behavior, source preservation, honest uncertainty, minimal current-consumer interfaces, no wider persistence/UI scope and traceable native versus policy-only proof.

One bounded non-ranking discovery pass may identify existing reuse and minimal handoff alternatives. Freeze candidate set, critical dimensions and I1–I10 before comparative research. Budget two `critical_researcher` reports: pair/correlation/outcome and controlled-input identity; service envelope/lifetime/publication compatibility. Each has at most one bounded follow-up. Mandatory `decision_analyst` receives both reports and has one correction. Require DRAFT READY and a fresh `critical_research_reviewer` pre-draft checkpoint before primary drafting, then a different fresh final artifact reviewer. No drafter or extra panel is planned. Permit one pre-draft correction and at most two final-artifact correction cycles; repeat the complete invariant packet after material revisions.

Capsules link exact authorities, current evidence identity, question, hard gates, forbidden effects, report scope and remaining allowance. DRAFT READY is not approval; RETURN FOR RESEARCH consumes only remaining evidence budget, and OWNER DIRECTION stops at the owner boundary. Decide semantics now; later tests prove them, not invent them. Significant authority/scope change, two repeated decisive gaps without new evidence or exhausted budgets stop dependent work. Review cannot accept changed product requirements or reset a budget. This future literal route is not consumed by current planning fact gathering/readiness review.

## Concrete Steps

### M502-ENTRY-02 — Execution authorization and research boundary

The owner authorizes this task through its prescribed gates and closure. Current initial HEAD is `4309a8f05d56032241c5a9418467f5505a455680`, with a clean working tree and no `logs/agent-flow-leases/v2/active.json`. Runtime permissions permit repository writes and read-only Git metadata; no Git write is selected. M5-01 remains Complete. Frozen-input verification is refreshed before accepting G; historical verification is not a new runtime observation.

Fresh entry verification passes: Node `v24.20.0`, Python `3.14.6`, independent `tsc --project tsconfig.json`, and 96 tests from `tests/run-contract.test.ts` and `tests/scan-normalization.test.ts` under the maintained README preparation and `--test --test-timeout=120000`. Both manifest SHA-256 values equal M502-ENTRY-01, all thirteen M3-01 file references verify, and all six RD-003 fixture files exactly equal their frozen UTF-8 inline content. `git diff --check` passes. These are current prerequisite observations, not the full task suite or comparison behavior evidence.

The G candidate set is bounded to existing exact-match pure domain composition and either direct service composition or a purpose-owned internal rescan-comparison executor using the existing publication boundary. Candidate evaluation must not add callbacks, caches, registries, durable fields or alter Accepted lifecycle semantics. Two independent R3 dimensions are fixed: pair/correlation/outcome and controlled-input identity (`M502-G-R1`); service envelope/lifetime/publication compatibility (`M502-G-R2`). Both use I1–I10, the seven G decide-now items and the existing Decision Review Contract; each has one report and at most one follow-up. Research is read-only and synchronizes before the mandatory analyst. No implementation preflight is authorized before G acceptance.

### M502-G-RESEARCH-01 — Synchronized research evidence

Both configured `critical_researcher` dimensions return **RESEARCH COMPLETE**. R1 traced Accepted comparison/evaluation authority, reflection-safe run readers, native selected-rule normalization, frozen inputs and pinned axe 4.13.0 contrast implementation. Candidate count must equal selected-rule pass coverage; exact-match uniqueness spans violations, incomplete observations and passes. The pinned scanner truncates retained ratios while native outcome uses its underlying measurement, so a retained pass ratio equal to the expected threshold is possible. Research proposes explicit positive-evidence minima and font/threshold consistency, to be reconciled by analysis and review before freezing.

R2 traced preparation, reservation, scanner cleanup, terminal admission, publication and closed HTTP framing. Four memory-only probes through the existing publisher confirmed that an uncaught comparison exception would manufacture a scanner failure with uncertain cleanup, and that shutdown reentry during a successful commit must suppress comparison without falsifying the committed scan. Research recommends a purpose-owned executor returning separate internal scan and calculation results, using the existing publisher and projecting only its scan outcome to the current service/API. No browser, service listener, model, file mutation or public navigation occurred in either research assignment.

The primary independently inspected the relevant readers, native contrast implementation and service publication seams. Both reports share the current entry identity; only plan maintenance has changed. Each researcher retains one bounded follow-up. `decision_analyst` now reconciles G1–7 and I1–I10; these reports accept no literal contract, implementation or source change.

`decision_analyst` subsequently returned **DRAFT READY** over G1–7 and I1–I10, with no remaining owner-controlled choice. The synthesis preserves unique but insufficient native-pass after-evidence as inconclusive, prioritizes known duplicate matches before unavailable later locators, and separates independent terminal admission from complete candidate-envelope admission. A fresh `critical_research_reviewer` is checking this complete synthesis before authoritative drafting. No implementation preflight or companion write has occurred. Roadmap execution wording now mirrors the owner's grant.

During pre-draft review, the reviewer identified that the controlled helper's stable-key-to-captured-evidence binding and closed rejection shape were not exact enough. The primary invoked R1's one bounded follow-up for that specific gap. Drafting remains stopped pending evidence, analyst reconciliation and complete checkpoint acceptance. The reviewer independently reproduced the four publisher cases and verified original manifest identities and no active lease; no additional decisive gap was reported at that interim checkpoint.

R1's follow-up verified the exact same-position locators for all six frozen states from `native-scan-capture.ts` and fixture trees. The analyst's sole correction returned **DRAFT READY**, defining shared completed-source/candidate readers, one shared profile comparator, and a closed test-only controlled binding that rejects invalid metadata, invalid scans, undeclared directions and profile drift before target selection. The complete invariant packet returns to the same fresh pre-draft reviewer; no allowance is reset.

The reviewer also reproduced a descriptor-reinspection hazard: a Proxy can supply different individually valid runs during initial terminal admission and later envelope enumeration. Primary reconciliation requires comparison and publication to use the same detached terminal snapshot; envelope checks never substitute a reread `run`. This is a decide-now identity constraint under I2/I8, not a current implementation defect or a new runtime mechanism.

Working directory: `C:/projects/a11y-evidence-lab`. Use the current [README command preparation](../../../README.md#development-command-preparation) in each PowerShell 7 session. Dot-source its first preparation block; do not run restore/acquisition or archived task commands simply to prepare a shell.

### M502-ENTRY-01 — Planning baseline

On 2026-09-17, initial HEAD `3053b049e43ca22d161f954d81ff4505ca4711d7` is clean and no active lease exists. Primary reviewed current roadmap/task closures, requirements/evaluation, source seams and status/navigation; a bounded read-only explorer inspected scanner/service ownership without effects. At 15:12 UTC, Node `24.20.0`, independent strict TypeScript and 96 tests across the two pure files below pass, with zero failures/skips. The 776-test/37-file result is historical M5-01 closure evidence, not a fresh full-suite run in this turn.

RD-003 SHA-256 remains `13c9722be9ea2e3b0aaf020ea91f429a701180a83814fe7ab21baf2ddad57459`; all six fixture files equal their frozen inline content. M3-01 SHA-256 remains `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; all thirteen referenced file hashes pass. No comparison input has yet been authored or evaluated. The entry commit is historical, not a hardcoded future HEAD/dirty-path gate; bind then-current intentional HEAD and tree/environment identity before execution and leases.

After README preparation, the repeatable focused check is:

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Resolve the active lease before continuing' }
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Focused comparison-prerequisite checks failed' }
}
git diff --check
```

### Future command and effect bindings

| Slot | Exact binding required before applicable preflight, lease or effect |
| --- | --- |
| G definition | Companion location/format/version, directed pairs, vector interpretation, original manifest/file hashes, validation command and permitted static metadata writes; no overwrite of frozen files or native observations. |
| A/B | Exact existing/new test files, commands and expected Red or characterization, pure input fixtures, strict command, accepted test protection and no filesystem/network/provider effects. |
| C | Exact real-scanner/service tests and internal-result assertion seam, six-state input identity, installed browser/runtime, controlled request interception, finite test deadlines and failure witnesses. No actual public navigation. |
| Full closure | Current README suite inventory plus new tests, browser/service execution order, strict/build commands and exact generated build paths. Historical test counts are not an expected-count gate. |
| Resources | Exclusive task-owned scratch/run/evidence roots, create-only fixtures, baseline hashes, owned process/port/context lifecycle, environment restoration, retention and exact cleanup. Check absolute descendants and links/aliases before deletion; no original run, shared scratch root, other task evidence or developer-owned runtime cleanup. |
| Guard/docs | Exact packet path lists/digest and terminal lease commands; current source/test/configuration/runtime/frozen-input fingerprints; UTF-8/newline/whitespace, command parsing, affected links/status and `git diff --check`. |

Future filenames are not existing commands. G and each packet must resolve actual callers and exit propagation before dispatch. A command/effect change returns to primary reconciliation; it cannot bypass a reviewed gate. Primary owns developer instructions and authoritative evidence/status documents between leases, not implementation workers.

## Validation and Acceptance

Future implementation evidence is pending. Record fixture, exact command, relevant tree/environment identity, expected/actual result and reviewer disposition for each invariant. Differentiate an invalid scan from a valid noncomparable pair, a comparable uncertain match and a classified Finding outcome.

| ID | Required witness and expected result | Owning review |
| --- | --- | --- |
| I1 | Only M5-02 selected; Accepted requirements preserved; original manifests/fixtures/results unchanged; new directed definition is explicitly versioned and reviewed, with native and policy-only evidence distinct. | Primary/G/D |
| I2 | Invalid, noncompleted, wrong-lineage or malformed envelope fails with no comparison; individual unavailable facts remain preserved. Complete valid profile mismatch is pair-only not comparable with zero correlation. | A/C critical |
| I3 | Every named page/profile dimension and coverage prerequisite checked; count changes, mode and downstream changes alone do not reject a pair or alter outcomes. | A critical |
| I4 | Public exact rule/locator match counts all relevant later buckets; duplicate/conflicting/changed/unavailable matches cannot force a winner. Controlled scan pairs require declared direction and stable key. | A/B critical |
| I5 | Resolved requires one unique native pass observation, complete coverage and required minimized evidence; absence/removal/ratio-only/incomplete cannot resolve. At most one positive object, no new identity or candidate collection leak. | B/C critical |
| I6 | Binary runtime failure stays persistent; no public binary improved/regressed. Failing contrast higher/equal/lower margins classify only with compatible sufficient evidence; unavailable/conflicting facts stay inconclusive. Policy-only binary reversal and contrast vector never become runtime records. | B critical |
| I7 | Baseline/later source data and nonselected Findings remain unchanged. Rationale/evidence presence respects pair and match disposition; later-only Findings have no new/regressed label; no conformance/causality claim or provider dependency. | B/C/D critical |
| I8 | Real successful scanner envelope is consumed before discard; no successful comparison after abort/cleanup uncertainty/validation or publication failure. Exceptions preserve truthful scan and cleanup state; no cache, delayed candidate reuse or HTTP/disk comparison data. | C critical |
| I9 | Controlled six-state native evidence and declared policy cases pass through shared production comparison logic; tests observe the actual internal result, not a duplicated oracle or fake callback. Original manifests/history remain separate. | B/C/D critical |
| I10 | All accepted tests unchanged during Green; compliant terminal leases, actual cohesion/relevance audit, full suite/strict/build, owned cleanup, different final critical PASS and documentation closure. M5-03 remains unselected. | Primary/D critical |

Record preflight classification, honest Red failure and assertions executed, protected test identity, Green and post-refactor results, terminal lease identity and acceptance. Reuse evidence only when command, working directory, relevant-tree/environment fingerprints and no-drift state all match. A lease receipt or summary proves neither semantic correctness nor real integration.

## Idempotence and Recovery

Read-only planning checks and pure deterministic comparison tests repeat safely under their evidence identity. Real scanner/service tests use exclusive disposable roots and finite owned resources; do not replay historical public/model grants. Preserve both source records and all original observations; no comparison can repair or overwrite them. Candidates are not recoverable after an operation; future comparison requires a new authorized rescan, never a disk cache or inference from absence.

A worker stop returns to primary inspection and terminal lease closure. Reconcile changed binding fields and invalidate affected evidence before another bounded assignment. Unexpected paths or guard failures freeze the affected write branch without reverting user changes. Cleanup uncertainty remains explicit; close only owned handles, validate exact descendant paths and preserve uncertain artifacts for diagnosis. Never reset Git, delete shared roots or stop developer-owned processes. Missing authority, significant architecture change or exhausted budgets require owner direction after unaffected work is complete.

## Artifacts and Notes

Keep G/A/B/C/D decisions and accepted evidence in this plan, with concise checkpoints in the [progress record](../../progress/m5-02-conservative-comparison.md). No new ledger, telemetry, report generator or duplicate policy document. Retain only content-safe bounded evidence; private/ignored runs, raw page/native/provider data and historical transcripts do not enter tracked files.

Planning documentation impact: this plan, roadmap planning activation, plan index, progress index and one task summary. No source, test, dependency, runtime, requirement or ADR change. Future controlled-definition creation is gated by G, not performed by this planning request.

## Interfaces and Dependencies

Retain pinned Node/TypeScript, existing runtime validators, Playwright/axe and the single-file service. New interfaces are limited to pure validated comparison input/result and its scoped rescan invocation; exact fields and error behavior await G. No durable comparison schema, HTTP/client contract, provider call, configurable policy or new library is established here.

### M502-PLAN-01 — Accepted planning readiness

On 2026-09-17 a fresh independent `critical_reviewer` returned **PASS — planning readiness only**, with no Blocker, Major or Minor, over I1–I10 and the common responsibility/cohesion baseline. It reviewed the complete plan and four accompanying planning changes against HEAD `3053b049e43ca22d161f954d81ff4505ca4711d7`. The reviewed plan SHA-256 was `3d4e6ba82a6bcd1a985eb73f36ad4f1edb3e9a2d27a0a5c249030ad1506ff1eb`, before this verdict/living-state maintenance.

The reviewer independently inspected controlling authority and critical source seams, confirmed exactly five planning paths dirty, no active lease, no source/test/configuration/frozen-input changes and passing `git diff --check`. It reused primary's fresh strict/96-test and frozen-input checks in M502-ENTRY-01; neither review nor planning reran the historical 776-test suite or browser/service integration. Primary documentation validation passed five documents, sixteen required sections, one parsed PowerShell block, 282 local links/anchors, strict UTF-8/no BOM/final newline and trailing-whitespace checks. Final verdict/link maintenance receives the same validation.

Primary accepts the actual planning diff and complete verdict. Remaining exact input/result, measurement, directed-definition and publication/failure mechanics are G obligations, not accepted implementation decisions. M5-02 remains In progress for planning only: twenty-one tasks Complete, one In progress, six Not started. Documentation impact is limited to the five named planning/status/navigation documents. No implementation, target/model call, retained-data mutation, commit or later-task selection occurred.

## Frozen contract and review evidence

### M502-G-CONTRACT-01 — Authored comparison contract

**Status: Accepted after corrected DRAFT READY, M502-G-PRE PASS and different fresh M502-G-ACCEPT-01 PASS.** This section supersedes the unresolved literal placeholders above. It implements no behavior and changes no requirement or ADR. R1's follow-up, analyst correction and supported pre-draft correction were consumed; no final-artifact correction was needed. Implementation allowances are tracked in their slice packets.

#### G1 — Closed admission and responsibility placement

Create `src/server/comparison/comparison-contract.ts` for comparison types and input admission, `scan-pair.ts` for profile projection/equality, `target-correlation.ts` for exact match disposition, `finding-outcome.ts` for evidence sufficiency/classification and its fixed rationale mapping, and `compare-finding.ts` for pure composition. Dependency direction is comparison to existing pure domain types/readers; never domain to scanner, service or filesystem. Export existing `readLocator` and `readRuleDetails` from `src/server/domain/run-contract/finding-validation.ts` unchanged for current candidate-reader reuse. No generic validation layer or fabricated Finding record is permitted.

`compareFinding(input: unknown): ComparisonCalculation` accepts exactly `{ baselineRun, baselineFindingId, laterRun, candidates }`. `ComparisonCalculation` is `{ ok: true, value: ComparisonResult } | { ok: false, error: 'invalid-comparison-input' }`. `validateComparisonInput(unknown)` has that same failure branch and success `{ ok: true, value: ValidatedComparisonInput }`; its value is exactly `{ baselineRun, baselineFinding, laterRun, candidates }`, with the actual selected validated Finding. Admission uses descriptor-safe closed readers, detaches and freezes returned containers, and catches inspection/validation failures without raw errors. An unexpected error after admission is not disguised as invalid input; the service boundary contains it separately.

Export `readComparisonRuns(input: unknown)` from the contract owner. Its exact input is `{ baselineRun, laterRun }`; output has those two completed runs, or it throws the existing bounded invalid-contract exception. Reuse `validateRun`, require both completed, distinct run IDs and `laterRun.baselineRunId === baselineRun.runId`. Public admission validates the selected ID and selects it from the admitted baseline, without consulting downstream eligibility.

Export `readComparisonCandidates(input: unknown, selectedRule: Rule, coverage: RuleCoverage): readonly NativePassObservation[]`. It throws on invalid input. Each candidate has exactly `ruleId`, `nativeResult: 'pass'`, `locator`, `checks`, `evidence`; reuse the domain locator and rule-detail readers. All entries must have the selected rule and the dense array length must equal `coverage.passes ?? 0`. Preserve duplicate locators, order and valid unavailable facts. A missing candidate collection is not an empty collection. Original run admission continues to validate violation/incomplete counts, complete three-rule coverage, source chronology and pinned profiles; no reader is weakened for policy vectors. Unsupported scanner/evidence/profile values remain invalid sources, not fabricated pair mismatch witnesses.

`NativePassObservation` and `ComparisonObservation` are rule-discriminated minimized types. An observation contains only `ruleId`, `nativeResult`, `locator`, `checks`, `evidence`, plus `incompleteReason` for incomplete observations. It contains no Finding state, workflow, new identity or raw native material. `CompletedRun` is the existing completed PageAnalysisRun shape. No new domain record is added.

#### G2 — Pair profiles and result envelope

`scan-pair.ts` exports `comparisonProfile(run: CompletedRun, selectedRule: Rule): ComparisonProfile` and `compareScanProfiles(baseline: ComparisonProfile, later: ComparisonProfile): readonly PairMismatch[]`. The profile has exactly `requestedUrl`, `finalUrl`, `rules`, `viewport`, `locale`, `browserVersion`, `scannerVersion`, `evidencePolicyVersion`, `scope`, `readiness`, `freshContext`, `importedState`, `interaction`, `crawling`, `iframes`, `contrastProfile`. The last field is the admitted contrast profile for selected contrast and `null` for binary rules. This is a pure projection and equality policy for two current consumers, not configurable runtime policy.

Use exact canonical requested and observed-final URL strings independently, exact ordered rule tuple, viewport width/height and all other enumerated values. Mismatch codes/order are `requested-url`, `final-url`, `rule-profile`, `viewport`, `locale`, `browser-version`, `scanner-version`, `evidence-policy`, `document-scope`, `readiness`, `scan-context`, `contrast-profile`; `scan-context` covers the five boolean context flags. Return each differing dimension once, in that order. Complete valid sources and candidate coverage are admitted before this gate. Differing completed bucket counts, timeout limits, timestamps, IDs, application revision, provider/model, downstream state and evidence values do not change compatibility. Readiness does, because it defines the observed document point. Unknown required provenance never becomes silent compatibility.

Every result has `baseline: { runId, findingId, observation }`, `laterRunId`, `context: { baseline: { requestedUrl, scanContext }, later: { requestedUrl, scanContext } }`, `reason`, `rationale`, `limitations` and `followUp`. Context uses detached complete source scan contexts, including observation provenance; only the named profile dimensions influence compatibility. `limitations` is a frozen one-string array; `followUp` is one string.

A mismatch adds `pair: 'not-comparable'`, nonempty ordered `mismatches`, and `reason: 'pair-mismatch'`; it has no `match`, `outcome`, `after` or `delta`, and invokes no target correlation. A comparable result adds `pair: 'comparable'`, `match` and `outcome`. Only a unique match carries `after`: `{ kind: 'finding', findingId, observation }`, `{ kind: 'incomplete', observation }`, or `{ kind: 'native-pass', observation }`. No candidate collection escapes. Only sufficient matched contrast failures carry `delta: { baselineMargin, laterMargin, change }`. Binary runtime results cannot carry improved/regressed outcomes or a delta.

#### G3 — Exact correlation

`target-correlation.ts::correlateTarget(input: ValidatedComparisonInput)` returns a discriminated `TargetMatch` with `match` and, only for a unique match, the bounded `after` projection above. It never classifies or performs I/O. After pair acceptance apply this exact precedence:

1. Unavailable baseline locator: `baseline-locator-unavailable`.
2. Count exact selected-rule and exact available-locator matches across all later Findings, incomplete observations and candidates. More than one: `ambiguous`.
3. Otherwise any same-rule later item with an unavailable locator: `later-locator-unavailable`.
4. Otherwise zero matches: `no-exact-match`.
5. Otherwise: `unique-violation`, `unique-incomplete` or `unique-pass` according to the single native bucket.

No deduplication, order winner, fuzzy matching or target-movement inference is permitted. Unavailable, ambiguous and no-match results have no after-evidence and are inconclusive. A unique incomplete observation retains its actual evidence/reason and remains inconclusive. A unique pass with insufficient facts retains one `native-pass` after-object but remains inconclusive; the object records native evidence, not sufficient proof of resolution. Later-only Findings remain untouched and receive no new/regressed label.

#### G4 — Evidence outcomes and fixed wording

`finding-outcome.ts::classifyEvidencePair(baseline: ComparisonObservation, later: ComparisonObservation)` returns `{ outcome, reason }` plus the optional contrast-only `delta`. The same classifier serves admitted runtime matches and separately typed policy-only vectors. Public composition always supplies a baseline violation; a positive baseline enters only the explicitly declared binary policy case, with sufficient positive baseline facts and a same-rule later violation. Unsupported rule/bucket combinations return inconclusive/conflicting-evidence. Unique native incomplete is inconclusive/native-incomplete. Native binary violation-to-violation is persistent/binary-still-failing even when individual facts are unavailable.

Resolution requires native pass, unique correlation, complete later coverage and sufficient later evidence. Available check IDs record provenance and never prove individual check success. Image requires available `elementKind: img`, available `altState` and a valid nonempty recognized `any` check. Label requires available element kind, valid available input type (or explicit textarea not-applicable), all minimized name-source facts available and a recognized nonempty `any` check. Preserve allowed `none` checks without interpreting their individual outcomes.

Contrast requires exactly `any: ['color-contrast']`, empty `all`/`none`, available foreground/background, finite retained ratio, expected ratio, font size/weight, and measurement source axe-core. `shadowColor` may be available or explicitly missing; invalid/withheld shadow is insufficient. `messageKey` must be explicitly missing; other message states cannot establish sufficient measured evidence. Missing required facts/checks yield `insufficient-evidence`; available contradictory measurements/checks yield `conflicting-evidence`. When both are present, insufficient required evidence takes precedence. A native-pass resolution needs no baseline contrast measurements.

For retained font consistency parse the existing px component, require the displayed pt string to equal `(px * 72 / 96).toFixed(1)`, and use the pinned axe classification `Math.ceil(px * 72) / 96`, bold 14pt/normal 18pt boundaries and expected 3 for large text or 4.5 otherwise. This validates retained provenance, not colors or native outcomes. For two sufficient violations, expected threshold classes must match; two individually valid differing classes are inconclusive/measurement-profile-mismatch. Sizes, weights and colors may change within the same class. Native pass retained ratio may equal expected because axe truncates its retained ratio; pass below expected or violation above expected is conflicting. Equality is valid for either native bucket; arithmetic never infers a bucket.

For two sufficient comparable contrast violations calculate finite JavaScript numbers `baselineMargin = baselineRatio - baselineExpected`, `laterMargin = laterRatio - laterExpected`, `change = laterMargin - baselineMargin`. Exact `>`, `===`, `<` classify improved/contrast-margin-increased, persistent/contrast-margin-equal, regressed/contrast-margin-decreased. No rounding, tolerance, color recomputation or score. A sufficient native pass is resolved/native-pass. Policy-only sufficient binary pass-to-violation is regressed/native-failure-after-pass and creates no runtime baseline Finding ID.

The reason vocabulary is closed to the correlation uncertainty codes, `pair-mismatch`, `native-incomplete`, `insufficient-evidence`, `conflicting-evidence`, `measurement-profile-mismatch`, `native-pass`, `binary-still-failing`, the three contrast-margin codes and policy-only `native-failure-after-pass`. Use a fixed application-authored sentence per reason: pair/profile differs; baseline locator unavailable; later locator unavailable; no exact match; multiple possible matches; native incomplete; required evidence unavailable; evidence conflicts; measurement profiles differ; unique native pass; still-failing binary; increased/equal/decreased failing contrast margin; or policy-only failure after pass, respectively. No input value, URL, locator or native prose enters rationale. The literal limitation is "This comparison describes automated evidence for one target; it does not establish accessibility, conformance, or remediation causality." The non-blocking follow-up is "Review the target and relevant manual checks before drawing a broader conclusion."

#### G5 — Frozen controlled definition and identity binding

The sole new static definition is [evaluation/m502-comparison-v1.json](../../../evaluation/m502-comparison-v1.json), version `m502-comparison-v1`. Its closed sections are `version`, `scope`, `sources`, `profile`, `endpoints`, `pairs`, `policyCases`, `limitations`. It records both original manifest hashes, all six endpoint path/hash/scenario/rule/key/revision/role/kind/expected-locator tuples, one exact production profile and exactly three directed failing-to-corrected pairs. It changes no historical bytes or observation. Stable keys are evaluation metadata, never public identities.

Both states for `rd3-image` bind `:root > :nth-child(2) > :nth-child(1) > :nth-child(3)`; both `rd3-email` states bind `:root > :nth-child(2) > :nth-child(1) > :nth-child(2) > :nth-child(2)`; both `rd3-text` states bind `:root > :nth-child(2) > :nth-child(1) > :nth-child(2)`. These are static derivations from frozen element trees and the production `parent.children` locator algorithm, not new browser observations.

`tests/helpers/comparison-fixtures.ts` owns only current pure fixtures, fixed-file integrity and `compareControlledScanPair(input: unknown)`. Input is exactly `{ definitionVersion, baseline, later, candidates }`, each endpoint exactly `{ scenario, ruleId, targetKey, revision, stateRole, run }`. Caller-supplied locator/path/hash metadata is prohibited. Output is exactly one of:

- `{ kind: 'invalid-input', reason: 'invalid-metadata' | 'source-integrity' | 'invalid-scan-input' }`;
- `{ kind: 'not-comparable', reason: 'undeclared-direction' }`;
- `{ kind: 'not-comparable', reason: 'profile-mismatch', mismatches }`;
- `{ kind: 'inconclusive', reason: 'missing-target' | 'ambiguous-target', side: 'baseline' }`;
- `{ kind: 'comparison', result: ComparisonCalculation }`.

Order: descriptor-safe metadata/version/individual RD-003 tuple validation; original manifest and six exact fixture integrity checks using only enumerated paths; shared `readComparisonRuns` and candidate validation with the declared baseline rule before selecting any Finding; exact declared direction; shared profile comparison between sources and against the companion declaration; baseline stable-key-to-fixed-locator binding; then production comparison with the sole actual selected ID. Return the canonical ordered union for profile differences. The declaration is a profile, never a fake completed run; binary profile projection sets contrastProfile to null. Baseline zero/multiple exact declared-target Findings produce the named helper inconclusive result. Production owns all later correlation and outcomes; no separate later helper branch, duplicate comparator or production fixture adapter exists.

The controlled browser harness fulfills the same verified exact fixture byte buffer used for identity checking. Within that existing operation, assert `#targetKey` and `expectedLocator` each select exactly one identical element of the declared kind. Aborting other requests and real scanner/executor output establish the observation; helper metadata alone does not establish native fidelity. Use the three original scenario/rule/key families and exact revisions only.

Reused failure persistence, reversed binary positive-to-violation, contrast 3.54 to 4.00 with expected 4.50 (improved), reverse (regressed), equal margins (persistent), and missing/duplicate/withheld/drift variants are explicitly policy-only/adverse vectors. They use shared production correlation/classification and never become declared native scan directions, positive baseline Finding records or persisted runtime comparisons. No third fixture, new scanner result archive or generation evaluation is introduced.

#### G6 — Operation lifetime and truthful publication

Extend `local-service/rescan-operation.ts` with exported `PreparedRescan = { ok: true, run: RunningRun, rule: NativeRule, baselineRun: CompletedRun, baselineFindingId: string }`, retaining the already validated detached baseline. Create `local-service/rescan-comparison.ts` for envelope admission and execution; `service.ts` remains reservation/composition. Only export the existing `ScanOperationDependencies` type from `scan-operation.ts`; its publisher behavior is unchanged.

`executeRescanComparison(dependencies: ScanOperationDependencies, prepared: PreparedRescan, signal: AbortSignal, execute: RescanExecutor = executeRescanScan, onCompleted?: () => void)` returns `Promise<{ outcome: ScanOutcome, comparison: ComparisonCalculation | { ok: false, error: 'comparison-failed' | 'scan-unavailable' | 'shutdown' } }>`. Wrap the existing `startScanOperation` settlement callback; the completion callback remains solely the existing successful-publication capability retirement. The service calls this same executor and projects only `outcome` into its unchanged reservation/API return. Tests inspect the actual internal result; no callback observer, cache or debug route is added.

Extract the envelope's own enumerable data `run` descriptor once and admit it through `matchTerminalRun` against the operation's running record. Comparison and publication must use that same detached admitted snapshot. Later envelope validation checks ordinary container/own-key shape and the candidates data descriptor without substituting or rereading `run`. Completed envelope keys are exactly `run,candidates`; failed keys exactly `run`. Accessors are never invoked. A valid independent terminal survives malformed candidate data, extra keys or candidate accessors; those produce invalid-comparison-input but do not invent scanner failure or uncertain cleanup. An inaccessible/invalid terminal cannot establish scan truth and follows the existing failed scan path.

Compute synchronously while candidates are operation-local and live; contain unexpected comparator exceptions as comparison-failed, then still return the independently admitted scan to its existing publisher. Recheck stop after reflective boundaries. Release candidate/envelope references after calculation/rejection; retain only bounded provisional comparison until publication. A successful comparison is exposed only after completed publication with no aborted signal, stopping state or expired deadline. Precedence is stop/abort/deadline to shutdown; otherwise unsuccessful scan/create/validation/publication to scan-unavailable; otherwise the bounded calculation result. Successful commit followed by shutdown reentry preserves the completed scan outcome but suppresses comparison; never rewrite it as failed. Suppressed results are released and no late candidate reuse is possible. These are ownership rules, not a memory-erasure claim.

The existing injectable `RescanExecutor` parameters and unknown return boundary stay, but results explicitly migrate to scanner envelopes. C's test owner migrates successful raw test terminals to `{ run, candidates: [] }` only where pass coverage is null, and failed terminals to `{ run }`; intentional invalid cases remain invalid. Missing candidates and raw-terminal return are rejected, never silently synthesized. Current HTTP, repository and client contracts remain unchanged; comparison errors are internal and M5-03 owns future exposure.

#### G7 — Commands, effects, ownership and proof

Working directory is the repository root. Each command session dot-sources the first PowerShell block under the exact README `Development command preparation` heading, with its environment checks/restoration unchanged. All Node commands run through `Invoke-M105Command` and check `$LASTEXITCODE` immediately, throwing before any dependent command. Node `24.20.0`, Python `3.14.6`, pinned dependencies and retained Chromium 1234 are present; no acquisition is authorized. Initial source/test/config identity is `c6144a9d0f83a13859f2c898ea4b29f44745ca1cb65984e7052d84bf1ba55d21` over 171 sorted source/test/config paths, encoded as relative path, NUL, lowercase content SHA-256 and LF. Recompute relevant identities for each packet/lease.

Focused commands, after preparation:

| Slice | Exact Node arguments; each separate row is sequential |
|---|---|
| A | `--test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/comparison-pair.test.ts` |
| B | `--experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts` |
| C internal | `--experimental-test-module-mocks --test --test-timeout=120000 tests/rescan-comparison.test.ts` |
| C service | `--test --test-timeout=120000 tests/rescan-service.test.ts` |
| C API | `--experimental-test-module-mocks --test --test-timeout=120000 tests/rescan-api.test.ts` |
| C scanner | `--test --test-timeout=120000 tests/scan-page.test.ts` with `$m105ScanTemp` |
| C integration | `--test --test-timeout=120000 tests/rescan-integration.test.ts` with `$m105ScanTemp` |
| Strict | `node_modules/typescript/bin/tsc --project tsconfig.json` |
| Build | `node_modules/vite/bin/vite.js build --configLoader native` |

Full closure runs the complete current README suite inventory sequentially, plus the three new test files, preserving module-mock flags and scanner/UI scratch groups. Current inventory is 37 files; counts are observations, not hardcoded pass gates. Build may replace only inspected ordinary `dist/client` generated output. The initial three existing build files were inspected; no other output path is authorized.

A source ownership is the contract/profile modules plus unchanged reader exports; tests own `tests/comparison-pair.test.ts` and the minimal fixture helper. B source ownership is correlation/outcome/composition; tests own `tests/comparison-outcome.test.ts` and helper extensions within the frozen definition. C source ownership is preparation/executor/service plus type-only publisher export; tests own `tests/rescan-comparison.test.ts`, existing `tests/rescan-service.test.ts`, `tests/rescan-integration.test.ts` and necessary bounded helper extensions. Exact per-turn paths, hashes and forbidden accepted tests are bound in Milestone Assignment Packet v2 before each lease. The primary alone owns this plan, companion and authority/status documents between leases. No client, dependency, runtime configuration, original manifest, fixture or retained-run write is permitted.

Pure A/B tests have no network/provider/browser or mutable filesystem effect; read-only fixture/hash reads are allowed. C uses existing controlled browser harnesses and six frozen states at `https://m105.test/`, intercepts and fulfills only that exact synthetic origin with the verified bytes, and aborts every other request. Production scan/cleanup budgets stay 10000/4000 ms; test deadline is 120000 ms. Assert Chromium `151.0.7922.34` and the declared profile. No screenshot or raw output archive is authorized.

All three existing scratch parents `temp/m103-scan`, `temp/m104-ui`, `temp/m105-integration` are ordinary and empty at entry; `temp/m105-integration/m502-comparison` is absent and create-only. The new real comparison witness owns only that child plus browser resources created inside its scanner scratch interval. Existing regression suites retain their existing exact create-only child paths and cleanup contracts. Preserve all shared scratch parents. Close owned browser/page/context/service/listeners and verify port closure where used; restore environment exactly. Before any removal resolve absolute descendants, reject reparse/alias paths, and remove only known task/test-owned children. Preserve uncertain residue for diagnosis; never stop developer-owned processes or remove original runs, browser/runtime/dependency installations. Node compilation caching stays disabled through the maintained wrapper.

Pure tests prove A/B contracts; C must prove real six-state scanner-to-comparison output at the actual production executor, not just mocks. Closure maps I1–I10 to accepted tests, original-source preservation, no HTTP/disk candidate/comparison leakage, failure truth and owned cleanup. Fresh critical reviewers accept each S3 barrier and a different fresh critical reviewer accepts integrated closure. All authoritative suite/strict/build and documentation checks, including affected links and `git diff --check`, must pass before Complete/archive. M5-03 remains unselected.

### M502-G-PRE-01 — Pre-draft acceptance

The fresh `critical_research_reviewer` returned **PASS**, with no remaining Blocker, Major or Minor, over corrected synthesis and the single-snapshot reconciliation. It independently reproduced the publisher failure/shutdown cases and the differing-descriptor snapshot hazard, verified original manifests/six bytes/thirteen references, inspected source responsibilities and accepted the complete I1–I10 readiness packet. These are contract/research observations only. A different fresh final artifact review remains mandatory before A preflight.

### M502-G-ACCEPT-01 — Final contract acceptance

Different fresh `critical_research_reviewer` `/root/g_final_review` returned **PASS** across I1–I10 with no Blocker, Major or unresolved Minor. Reviewed plan SHA-256 `5599e1e825c8fdef3bd9d5b71a45813a0fcba69985c0d2b8f3aa3dc75eaf416f` and companion `8a44166b40f700b5dc35a3290b7a4958533c38fc454302d843499c576f43e0a8`; subsequent living-state maintenance does not alter G semantics. It independently verified two manifests, six endpoint bytes/tuples/locators, thirteen generation references, publisher failure/shutdown probes and the differing-descriptor hazard, and inspected pinned axe arithmetic. Primary inspected the authored diff, verified 182 local links/anchors, UTF-8/formatting, source preservation and `git diff --check`. Source/test/configuration identity remains M502-ENTRY-02. No implementation, native observation or task completion is claimed. G permits A preflight; documentation now reflects execution rather than planning-only status.

### M502-A-PACKET-01 — Input and profile preflight

Milestone Assignment Packet v2

**Identity:** Workflow ID `M5-02-20260917`; roadmap task `M5-02`; work-slice `M502-A`; assignment `M502-A-preflight-01`; lease ID None; phase preflight; attempt 1; correction parent None; worker role `test_worker`; lease owner `/root/a_test`; guard digest None.

**Work-slice capsule:** Owning ExecPlan is this document. Observable contract is A plus G1/G2: strict detached input admission and exact ordered profile mismatches, with unavailable facts preserved. Authority is the exact requirement/ADR/BHV/SPEC anchors in Context and Orientation, particularly REQ-COMP-001/002/007, REQ-EVID-008/010, ADR-0009/0021/0024 and BHV-06/SPEC-006. Readiness/freeze evidence is M502-ENTRY-02 and M502-G-ACCEPT-01. TDD Applicable: new executable admission/profile behavior. Current-state evidence M502-ENTRY-02; preflight evidence None pending report. Accepted test boundary/owner None until Red acceptance.

Relevant paths: new `src/server/comparison/comparison-contract.ts` owns closed types, observation projections and admission; new `src/server/comparison/scan-pair.ts` owns pure profile projection/equality; existing `src/server/domain/run-contract/finding-validation.ts` owns unchanged evidence readers, with only `readLocator`/`readRuleDetails` exported. These are distinct present responsibilities. Dependency direction: comparison to existing pure domain types/readers only. Reuse domain validation; bounded creation of two cohesive comparison modules; no general validation layer. Permitted local structural refactor: private helpers within those new owners only, preserving frozen interfaces; existing reader behavior unchanged. Tests: new `tests/comparison-pair.test.ts` and `tests/helpers/comparison-fixtures.ts`; helper at A contains only immediately needed pure fixtures and must not import B's absent comparator. Non-goals: correlation/classification, service, browser, fixture/companion edits, dependencies and later tasks. Named uncertainties: classification of existing coverage and first-module Red eligibility. Risk S3: identity/evidence integrity and reflection-safe admission; fresh critical review required before B. Changed authority, scope, missing evidence or budget exhaustion returns to primary.

**Write scope:** Allowed files None (read-only preflight); allowed roots None; forbidden files All writes; forbidden roots entire repository and external filesystem. Future Red is limited to the two named test files; future Green to the three named source files. No worker Git mutation.

**Validation:** Working directory `C:/projects/a11y-evidence-lab`. Use exact README preparation and G7 wrapper. Preflight may inspect source/tests and reuse matching 96-test/strict evidence; no fixture, cache or resource creation. Focused future command: `$m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/comparison-pair.test.ts`; immediate exit check. Task-level command: strict `$m105Node node_modules/typescript/bin/tsc --project tsconfig.json`, then G7 complete suite at D. Expected result: supported preflight classification, exact missing callables and complete assertion design; evidence ID M502-A-PREFLIGHT-01. Relevant-tree fingerprint `c6144a9d0f83a13859f2c898ea4b29f44745ca1cb65984e7052d84bf1ba55d21`. Environment Node 24.20.0/Python 3.14.6, unchanged lock/config, cache disabled. Side effects/cleanup None; pure later tests permit read-only fixture access only.

**Budget and stopping:** Maximum worker turns one preflight, then at most three write turns per unchanged role/phase chain; maximum corrections one ordinary plus conditional final with recorded progress/new evidence. Maximum identical failure two without new evidence; maximum no-diff outcomes two. Validate each coherent write boundary, independently strict after Green; no redundant full suite. Stop on binding-field change, unknown/conflicting classification, path/cohesion expansion, unexpected effects or exhausted allowance. First-module missing-callable Red requires exact agreed imports, complete executable assertion bodies and independently valid environment; no stubs or fabricated assertion failure.

**Handoff:** Return identity/authority, classification, source and coverage evidence, exact commands/results or matching reused identity, missing callables, proposed complete Red boundary, unexpected state, risks and documentation impact. Do not write during preflight.

### M502-A-PREFLIGHT-01 — Missing input and profile behavior

Persistent test owner `/root/a_test` returned **MISSING** after tracing existing run/scan readers and native candidate projection plus exact path/export absence. The three contract-reader and two profile callables do not exist. Fresh runner/96-test/strict evidence remains identity-matched; primary independently recomputed the 171-path fingerprint. No writes/effects occurred. First-module Red is accepted: complete tests will import the exact G1/G2 modules; initial module-resolution failure blocks behavioral assertions and must be described honestly. No B callable belongs in A.

### M502-A-PACKET-02 — Complete Red assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-A`; assignment `M502-A-red-01`; lease `M502-A-red-01`; phase red; attempt 1; correction parent None; role `test_worker`; guard owner `a_test` (agent `/root/a_test`); guard digest supplied from the primary's start result before dispatch. The initial guard invocation rejected the slash-containing owner before creating a lease; the valid guard identifier changes no assignment or budget.

Owning ExecPlan this document; observable acceptance A and frozen G1/G2, with the complete assertion boundary in M502-A-PREFLIGHT-01's accepted handoff. Exact authorities, readiness/freeze, production placement, dependency direction, reuse, permitted refactor and non-goals are unchanged from M502-A-PACKET-01. TDD Applicable; current/preflight evidence M502-ENTRY-02/M502-A-PREFLIGHT-01. Accepted tests None until this handoff; test owner `/root/a_test`. Relevant boundaries: the three future A source paths and two test paths in PACKET-01. Named uncertainty resolved MISSING; remaining risk is truthful missing-module Red with complete assertions. S3; fresh critical barrier after Green, binding changes return to primary.

Allowed files exactly `tests/comparison-pair.test.ts`, `tests/helpers/comparison-fixtures.ts`; allowed directory roots None. Forbidden files all other paths; forbidden roots `src`, `docs`, `evaluation`, `fixtures`, `.codex`, `.agents`. No Git mutation. Helper contains pure current fixtures only; no B import or duplicate implementation.

Working directory repository root. Exact README preparation/G7 wrapper, then `$m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/comparison-pair.test.ts`, capture native exit immediately and require the specific absent production module. Task-level strict/complete commands remain G7; strict is not expected to pass before absent modules exist. Decisive result M502-A-RED-01: 96 existing tests remain passing, first-module import fails, complete new assertions unexecuted. Relevant-tree baseline `c6144a9d0f83a13859f2c898ea4b29f44745ca1cb65984e7052d84bf1ba55d21`; post-write fingerprint and exact test hashes required. Environment Node 24.20.0/Python 3.14.6, unchanged lock/config, cache disabled. Effects limited to allowed test files; no other mutable filesystem/network/browser/service effects; cleanup None.

Maximum worker turns this initial write plus one ordinary and one conditional final correction in the same Red chain. Maximum corrections two under the conditional rule; identical decisive failure twice without new evidence and two no-diff handoffs stop. Validate complete coherent Red once; do not fabricate stubs, skips or conditional tests. Stop on any changed binding/scope, wrong failure or unknown condition. Handoff exact identity/digest, touched paths, command/native result, test assertion inventory and unexecuted status, fingerprint, risks/documentation impact. Primary owns guard verification/closure and acceptance.

### M502-A-RED-01 — Accepted first-module Red

Lease `M502-A-red-01` verified and freshly **closed-compliant**, exit 0, receipt `5f57758d5d5b7dd17d15c5ee459acb363b3a0a7cc111dbbeab9e7a3046fd8fc5`; only the two allowed test files were created. Primary inspected their complete assertion boundary, unchanged production and matching hashes. Prepared focused command exited 1 at exact `ERR_MODULE_NOT_FOUND` for `comparison-contract.ts`; 96 existing tests passed and all 11 complete new behavioral test bodies were blocked before evaluation. Both test files passed Node syntax checks, with no skip/focus/todo markers. This is missing-callable Red, not an executed behavioral failure.

Accepted test hashes: `tests/comparison-pair.test.ts` = `bdfb94234d371216285fc65caac5ee2ad7b549d78a37ed2d03915bbe373290b8`; `tests/helpers/comparison-fixtures.ts` = `04b10c8d5128af7ebb006d4155cf5d830a14c724cd551083906f39b43e4e3ff5`. Post-Red identity over 173 paths = `c20400bd751ad110f8e12997965ddf78ccd11d2fdb6cc15eb03f032754d4359c`. Fingerprints use Python ordinal path sorting, not PowerShell culture sorting. No correction was consumed; all new behavioral bodies and strict typing remain Green obligations.

### M502-A-PACKET-03 — Complete Green assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-A`; assignment/lease `M502-A-green-01`; phase green; attempt 1; correction parent None; worker role `code_worker`; guard owner `a_code` (agent `/root/a_code`); digest supplied directly from verified guard start before dispatch.

Owning ExecPlan this document. Observable acceptance G1/G2 and A; strict readers and exact profile comparison must execute every accepted assertion unchanged. Exact authority anchors are Context and Orientation's REQ-COMP-001/002/007, REQ-EVID-008/010, BHV-06, SPEC-006 and ADR-0009/0021/0024. Readiness/freeze M502-ENTRY-02/M502-G-ACCEPT-01. TDD Applicable; current/preflight evidence M502-A-PREFLIGHT-01; accepted Red M502-A-RED-01. Accepted test boundary is its two hashed files; current test owner `/root/a_test`.

Relevant boundaries and responsibility placement: `comparison-contract.ts` owns closed comparison types/admission and minimized observation shapes, `scan-pair.ts` owns pure profile projection and equality, existing `finding-validation.ts` receives only exports of unchanged `readLocator`/`readRuleDetails`. Dependency direction comparison to existing pure domain types/readers; no scanner/service/I/O import. Reuse existing domain validators, create only two purpose-owned modules for current behavior. Permitted local refactor only private details within those new modules; no existing reader behavior change. Non-goals B correlation/classification/composition, C/service, client, filesystem effects, dependencies, frozen inputs and other tasks. Named uncertainties None; test contradictions must return to primary. Risk S3 for evidence identity/reflection admission; fresh critical reviewer after Green and independent strict. Changed binding/cohesion or exhausted budget stops advancement.

Allowed files exactly `src/server/comparison/comparison-contract.ts`, `src/server/comparison/scan-pair.ts`, `src/server/domain/run-contract/finding-validation.ts`; allowed roots None. Forbidden files all other paths; forbidden roots `tests`, `docs`, `evaluation`, `fixtures`, `.codex`, `.agents`. Tests remain byte-identical. No Git mutation.

Working directory repository root. Exact README preparation/G7 wrapper: focused `$m105Node --test --test-timeout=120000 tests/run-contract.test.ts tests/scan-normalization.test.ts tests/comparison-pair.test.ts`; immediate nonzero exit throws. Then strict `$m105Node node_modules/typescript/bin/tsc --project tsconfig.json`, immediate exit check. Task-level complete suite/build remain D/G7. Decisive result M502-A-GREEN-01: all existing and new assertions execute/pass, strict passes, exact unchanged test hashes, cohesive diff. Relevant-tree baseline `c20400bd751ad110f8e12997965ddf78ccd11d2fdb6cc15eb03f032754d4359c`; recompute post-write ordinal fingerprint. Environment Node 24.20.0/Python 3.14.6, unchanged lock/config/cache-disabled wrapper. Side effects only three allowed source files; no network/browser/service/model/extra filesystem output. Cleanup None.

Maximum turns initial Green plus one ordinary correction and one conditional final correction in the same chain; maximum corrections two subject to documented final-attempt gate. Maximum identical decisive failure two without material new evidence; no-diff handoffs two. Validate coherent Green once and after any actual structural refactor; reuse matching accepted Red without rerunning. Stop for test conflict, changed scope/dependency/cohesion/budget or unexpected effect. Handoff identity/digest, changes, exact command results, all test evidence and post-write identity, RETAINED/REFACTORED/RECONCILE disposition, remaining risks/documentation impact. No lease authority survives the turn.

### M502-A-GREEN-01 — Input and profile implementation evidence

Code owner `/root/a_code` returned **GREEN CONFIRMED**, initial attempt, with all 107 focused tests passing (11 new assertions now executed), strict exit 0 and unchanged accepted test hashes. Lease `M502-A-green-01` independently verified and freshly **closed-compliant**, exit 0, receipt `b02afa84b145a2e0a74178cacf7bb5171f06be351f1fbda6a8d7513d229ab8ca`; exactly two declared source creations and the reader-export modification occurred. Primary inspected both complete new modules and the existing two-line export diff, independently passed strict with the prepared caller, and checked protected test hashes. Post-write ordinal identity over 175 paths: `8796a0d0505bf41bcbc2fc9dca6977f309e5365886e228b2816eba3223ea211a`.

Cohesion **RETAINED**: contract admission reuses pure domain readers; profile projection/equality has its own owner; no I/O edge or existing validation behavior change. B's frozen result/TargetMatch/outcome types will be added to the same contract owner with their first current consumers, explicitly included in B's packet. This sequencing introduces no new policy. No A correction consumed. Fresh S3 review remains required before B preflight.

### M502-A-ACCEPT-01 — Input and profile barrier accepted

Fresh `critical_reviewer` `/root/a_review` returned **PASS**, no Blocker/Major/Minor. It independently ran the exact 107-test focused command and 426 memory-only probes covering accessors/proxies, descriptor snapshots, malformed coverage, valid profile mismatch, deep freezing and source preservation. Exact source/test identity and test/companion/original-manifest hashes matched; `git diff --check` passed. Independent primary strict evidence was reused. Primary accepted actual cohesive source diff and unchanged test boundary. No correction was needed; A workers retire at this barrier.

Both immutable terminal receipts are compliant with no active lease or Git-state drift. Later guard `status` reports closed-drifted because the Red snapshot precedes authorized Green and both snapshots precede primary plan maintenance; that expected post-close state does not invalidate the verified write intervals or current matching source/test evidence. No browser/service operation or B/C behavior is claimed. B may now preflight.

### M502-B-PACKET-01 — Correlation and outcome preflight

Milestone Assignment Packet v2

**Identity:** Workflow `M5-02-20260917`; roadmap task `M5-02`; work-slice `M502-B`; assignment `M502-B-preflight-01`; lease None; phase preflight; attempt 1; correction parent None; role `test_worker`; owner `/root/b_test`; guard digest None.

**Work-slice capsule:** Owning ExecPlan this document. Observable acceptance is B and frozen G1–G5: after valid pair admission, exact all-bucket correlation, minimized evidence projection, conservative native outcomes, fixed rationale/limitations and the declared controlled binding. Exact requirement/ADR/Verification/BHV/SPEC anchors are Context and Orientation, particularly REQ-COMP-001–008 within M5-02 scope, REQ-EVID-002/008/010, REQ-SCAN-005, BHV-06/SPEC-006 and the comparison-only contrast policy vector. Readiness/freeze M502-G-ACCEPT-01; prerequisite M502-A-ACCEPT-01. TDD Applicable: new executable policy. Current/preflight evidence M502-A-ACCEPT-01 / None pending classification. Accepted B tests None; A pair test stays protected; B test owner `/root/b_test`.

Relevant boundaries: new `target-correlation.ts`, `finding-outcome.ts`, `compare-finding.ts` under `src/server/comparison/`; existing `comparison-contract.ts` for frozen result/TargetMatch/outcome type additions with their first consumers. Correlation owns exact cross-bucket counting and minimized matched projection; classifier owns sufficiency/arithmetic and fixed reason wording; composer owns admission-to-profile-to-match-to-outcome order/result assembly. The contract owner remains closed types/admission; no behavior refactor there. Dependency direction composer to pure comparison owners, each to pure existing domain types/readers only. Reuse accepted A and domain facts; bounded creation of three distinct current responsibilities, no generic helper framework. Permitted structural refactor private helpers within those new owners; no A behavior change. Test paths `tests/comparison-outcome.test.ts` and `tests/helpers/comparison-fixtures.ts`, the latter extending current pure fixtures with the exact G5 fixed-file integrity/controlled binding; it invokes production readers/profiles/comparator, never duplicates comparison policy. Non-goals service/runtime integration, persistence/API/UI, fixture/manifest changes, dependencies and other tasks. Named uncertainties: exact existing coverage and new callable absence; helper import sequencing may block A's test module during honest first-module Red. Risk S3: target/evidence integrity and false positive resolution. Fresh critical review required before C; binding/scope/cohesion/budget changes return to primary.

**Write scope:** Allowed files None; allowed roots None; forbidden files all writes; forbidden roots repository/external filesystem. Future Red exactly two named B test paths; future Green exactly four named B source paths. No Git mutation or agents.

**Validation:** Working directory repository root. Exact README preparation/G7 wrapper. Focused command `$m105Node --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts`, immediate exit check. Task-level strict command `$m105Node node_modules/typescript/bin/tsc --project tsconfig.json`; full G7 suite/build at D. Expected preflight result supported classification, exact missing callables and complete behavior assertion boundary, evidence M502-B-PREFLIGHT-01. Relevant-tree identity 175 ordinal paths `8796a0d0505bf41bcbc2fc9dca6977f309e5365886e228b2816eba3223ea211a`; environment Node 24.20.0/Python 3.14.6, unchanged lock/config/cache disabled. Reuse fresh A evidence; preflight has no mutable effects. B pure tests may read only fixed companion/manifests/six fixture bytes; no modification, network/browser/provider/service or retained-run effects. Cleanup None.

**Budget and stopping:** One read-only preflight; maximum write turns three per unchanged role/phase chain, one initial plus ordinary correction and conditional final correction requiring recorded learning/progress. Maximum corrections two subject to that gate; identical decisive failures twice without new evidence or two no-diff handoffs stop. One supported review correction loop. Validate at coherent slice boundaries; independent strict after Green; no fabricated Red or production stubs. Stop for conflicting/unknown classification, changed binding/command/effects, missing owner decision or exhausted allowance. First-module Red requires exact agreed absent imports and complete assertion bodies, with blocked A/B bodies honestly distinguished from failures.

**Handoff:** Identity/authority, classification, actual repository/coverage evidence, exact commands/results/reuse identity, proposed test boundary and any required command reconciliation, unexpected state, residual risks/documentation impact. No write authority exists during preflight.

### M502-B-PREFLIGHT-01 — Missing policy and safe integrity witness

Persistent B test owner returned **MISSING** after actual A-reader/profile inspection, absent three B modules/exports and existing coverage inspection. The new `correlateTarget`, `classifyEvidencePair`, `compareFinding` and G5 test-only controlled binder do not exist. Frozen G1–G5 semantics remain decidable. Primary accepts complete proposed assertions across all native buckets, unavailable/duplicate precedence, per-rule sufficiency, exact contrast boundaries/arithmetic, result projection/content safety, downstream independence and fixed controlled identity/admission/profile ordering.

Primary reconciles the B command to add existing Node `--experimental-test-module-mocks`, solely to exercise the controlled binder's source-integrity failure with read-only substituted bytes. No frozen file, production injection seam, dependency or browser/network effect is added. This is a bounded verification-command correction, not new comparison semantics. Prepared primary prerequisite with that flag and the existing pair test passes all 11 tests, exit 0, cache disabled. The worker's extra direct-node preflight run omitted the required wrapper and is excluded from authoritative runner evidence; it performed only the pure pair suite. Primary confirmed no configured process compilation-cache destination or source/test drift, and the prepared primary run supersedes it. Subsequent assignments explicitly retain the wrapper requirement; no writer attempt or correction budget was consumed by this read-only handoff clarification. G7 reflects the exact command; D must retain the flag for the outcome test. First-module Red is eligible at the three agreed B production modules; helper imports may also block A bodies, which must be reported as unexecuted rather than failed assertions.

### M502-B-PACKET-02 — Complete Red assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-B`; assignment/lease `M502-B-red-01`; phase red; attempt 1; correction parent None; role `test_worker`; guard owner `b_test` (agent `/root/b_test`); digest supplied from verified guard start before dispatch.

Owning ExecPlan this document; observable acceptance B/G1–G5 and the complete M502-B-PREFLIGHT-01 assertion boundary. Exact authority anchors, readiness/freeze, production placement, dependency direction, bounded creation/reuse, permitted structural refactor and non-goals remain PACKET-01. TDD Applicable; current/preflight evidence M502-A-ACCEPT-01/M502-B-PREFLIGHT-01. Accepted B boundary None until handoff; owner `/root/b_test`. A pair test remains protected. Relevant test paths new `tests/comparison-outcome.test.ts` and existing `tests/helpers/comparison-fixtures.ts`. The helper owns only pure fixtures and fixed integrity/controlled binding and delegates policy to production; preserve A helper behavior. Named uncertainty MISSING resolved; first-module import must be the precise agreed B callable. Risk S3, fresh critical review after Green, stop for changed semantics/path/effects or unresolved test contract.

Allowed files exactly the two named B test paths; allowed roots None. Forbidden files `tests/comparison-pair.test.ts`; forbidden roots `src`, `docs`, `evaluation`, `fixtures`, `.codex`, `.agents`; all unlisted writes forbidden. No Git mutation. Source integrity mocks are memory-only and scoped/restored; original bytes cannot be changed.

Working directory repository root. Exact README preparation and cache-disabled G7 wrapper are mandatory in every command session. Focused command `$m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts`; capture native exit immediately, require the precise missing-production-module failure. Task-level strict/G7 complete suite stay unchanged except the outcome test's module-mock flag. Expected evidence M502-B-RED-01: complete behavioral assertions authored, initial B module resolution fails, any blocked A/B bodies explicitly unexecuted; no syntax/runner/mock failure is accepted as Red. Relevant-tree baseline 175-path `8796a0d0505bf41bcbc2fc9dca6977f309e5365886e228b2816eba3223ea211a`; record post-write ordinal fingerprint and both B test hashes, plus unchanged A pair hash. Environment Node 24.20.0/Python 3.14.6, unchanged lock/config, cache disabled. Effects only test writes and enumerated read-only companion/manifests/fixture reads with memory-only mocks; no browser/service/provider/network/cache/extra output. Cleanup restore mocks/environment; no filesystem cleanup.

Maximum worker turns this initial Red plus one ordinary and one conditional final correction in the same role/phase chain; maximum corrections two under recorded progress/new-evidence gate. Maximum identical decisive failures two without new evidence; maximum no-diff handoffs two. Validate coherent Red and syntax; full strict awaits absent production. Stop on changed bindings, wrong failure, test contradiction, unapproved effect or exhausted budget. No stub, skip/focus, conditional assertion or duplicated comparison implementation. Handoff identity/digest, touched paths, exact prepared command/exit and blocked bodies, test hashes/tree identity, risks and documentation impact. Primary owns guard and acceptance.

### M502-B-RED-01 — First Red handoff requires test correction

Lease `M502-B-red-01` verified and freshly **closed-compliant**, exit 0, receipt `0ab52654eb1ff99d4a6becd7d4fd65a84d7fead4129363e0947264e4b953e91a`. Only outcome test creation and helper modification occurred; A's pair test hash stayed unchanged. Exact prepared B command exited 1 at agreed missing `target-correlation.ts` and `compare-finding.ts`; all 13 B and 11 A bodies were blocked at module linking, with syntax checks passing. This is honest module absence, not executed behavior.

Primary inspected both complete test files and identified remaining test-boundary issues before Green: raw policy fixtures widen literal/fact types or use incomplete `Mutable` shapes against the frozen typed classifier; comparable-result accesses need explicit union narrowing; unavailable invalid/withheld message-key capture must follow G4's insufficient-evidence branch rather than available-contradiction semantics. The helper's controlled result needs comparable-pair narrowing before reading outcome. These are corrections to the existing contract, not new production decisions. The unaccepted initial boundary is superseded by the ordinary test correction below.

Initial hashes: outcome `74b38d923f93d7599b5586177108bb4a33abec56369a27f48b476362d945844c`, helper `8bfc1f6e8fba99c371e0de0c050c96434a4f55b9dbce2a8eba7d3423490b2e2d`. Primary canonical 176-path fingerprint is `7da7228704f2cb768c3d2069494d460acbc4006d7df1c58235f991f0392bcc25`; the worker's differing fingerprint is not accepted. Exact file hashes and lease evidence match, with no observed drift. Use the ordinal Python selection including `vite.config.ts` for all subsequent identities.

### M502-B-PACKET-03 — Complete Red correction assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-B`; assignment/lease `M502-B-red-02`; phase red; attempt 2; correction parent `M502-B-red-01`; role `test_worker`; guard owner `b_test`; digest supplied from verified guard start. Owner is the same persistent `/root/b_test`.

Owning ExecPlan this document; observable acceptance unchanged B/G1–G5; exact authority/readiness/production placement/dependency/reuse/refactor/non-goals are PACKET-01 and PACKET-02, without expansion. TDD Applicable; current/preflight M502-B-PREFLIGHT-01; previous Red M502-B-RED-01 is not accepted for Green and is invalidated by correction. B test owner `/root/b_test`; accepted A pair test remains protected. Relevant paths the same two B test files. Named correction: make policy fixture types and result narrowing satisfy frozen signatures, correct unavailable message-key reason, and recompute canonical evidence identity. No production signature widening to accommodate tests. S3 and fresh review after Green remain required.

Allowed files exactly `tests/comparison-outcome.test.ts` and `tests/helpers/comparison-fixtures.ts`; allowed roots None; forbidden file `tests/comparison-pair.test.ts`; forbidden roots `src`, `docs`, `evaluation`, `fixtures`, `.codex`, `.agents`; all other writes and Git mutation forbidden. Preserve complete assertions and helper policy; no skip/focus, stub, or production adapter.

Working directory repository root. Exact README preparation/cache-disabled wrapper; focused `$m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts`, capture exit immediately and require agreed missing-module Red. Both test files must pass prepared `--check`. Strict remains the G7 task command; known absent modules still block full strict, so use the existing A `ComparisonObservation` type explicitly to validate policy fixture construction and intentionally cast only adverse policy inputs, without introducing a production stub. Expected M502-B-RED-02: complete corrected unchanged-scope test contract, honest linking failure/blocked bodies, new exact hashes and canonical fingerprint. Baseline 176-path `7da7228704f2cb768c3d2069494d460acbc4006d7df1c58235f991f0392bcc25`; environment Node 24.20.0/Python 3.14.6, unchanged dependencies/config. Effects only two test files and previously bound read-only/memory-only checks; restore mocks/environment; no filesystem cleanup.

Budget: this consumes the sole ordinary Red correction; one conditional final correction remains only with recorded material progress/new evidence. Maximum identical failure twice without new evidence or two no-diff handoffs still stop; no budget reset. Validate coherent corrected Red once. Stop on unresolved semantic/typing contradiction, path/effect expansion or exhausted allowance. Handoff exact identity/digest, changes, decisive prepared results and blocked assertions, canonical hashes/fingerprint, residual risks/documentation impact. Guard stays primary-owned.

### M502-B-RED-02 — Corrected test boundary accepted

Lease `M502-B-red-02` verified and freshly **closed-compliant**, exit 0, receipt `f616a10b182fa2bab8b06be65fcde20c95fb36dcae9a048c8d6ecf7c00e4ba85`; only the outcome test changed from the preceding terminal baseline. Primary inspected explicit rule/fact fixture types, deliberate adverse-policy casts, discriminant narrowing and corrected unavailable-message expectations. Prepared B Red and both syntax checks produce the expected results: two missing-production-module failures, 13 B and 11 A bodies unexecuted. No production stub, skip/focus or non-module failure exists. Corrected Red is accepted; one ordinary test correction is consumed.

Accepted hashes: outcome `f76b96b6c5115a654a0da53e0f5217e30bc43d82d594ad5118e4aac87af7093d`, helper `8bfc1f6e8fba99c371e0de0c050c96434a4f55b9dbce2a8eba7d3423490b2e2d`, A pair `bdfb94234d371216285fc65caac5ee2ad7b549d78a37ed2d03915bbe373290b8`. Primary canonical 176-path identity is `82deb481fb5c1b11cfc19865e59631502d53acad803375859498f817df687f3d`. The differing worker fingerprint was independently reproduced as literal backslash-zero/backslash-n separators, rather than NUL/LF; it is an encoding/reporting error, not tree drift, and is replaced by the primary value. Future fingerprint callers use `chr(0)` and `chr(10)` to remove this ambiguity.

### M502-B-PACKET-04 — Complete Green assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-B`; assignment/lease `M502-B-green-01`; phase green; attempt 1; correction parent None; role `code_worker`; guard owner `b_code` (agent `/root/b_code`); digest supplied from verified guard start.

Owning ExecPlan this document. Observable acceptance B and frozen G1–G5: exact admission/profile/correlation/classification order, bounded evidence/result, conservative binary and contrast outcomes, independent of downstream data. Exact requirements/ADR/Verification/BHV/SPEC anchors in PACKET-01 and Context and Orientation remain controlling. Readiness/freeze M502-G-ACCEPT-01; prerequisite M502-A-ACCEPT-01; TDD Applicable; current/preflight M502-B-PREFLIGHT-01; accepted corrected Red M502-B-RED-02. Test owner `/root/b_test`; accepted boundary includes its two hashed files and protected A pair test, all unchanged during Green.

Production responsibility placement: new `target-correlation.ts` owns exact cross-bucket match disposition and minimized observation projection; new `finding-outcome.ts` owns evidence sufficiency, native outcome/arithmetic and fixed application-authored reason wording; new `compare-finding.ts` composes admitted sources, profile gate, correlation/classification and bounded result; existing `comparison-contract.ts` receives frozen result/match/outcome type additions only. Dependency direction composer to pure comparison owners; each reuses existing pure domain readers/types. Reuse A readers/profile and existing rule/check vocabularies, create only the three current cohesive policy owners. Private helpers and directly needed projection/wording exports within new owners are permitted; no A behavior refactor or scanner/service/I/O dependency. Relevant boundaries are those four source files plus protected tests. Non-goals C/service integration, production fixture binding, persistence/API/client, new dependencies, frozen artifacts and later tasks. Named uncertainties None; contradictions return to primary. Risk S3 identity/evidence/false-resolution; fresh critical barrier and independent strict required before C.

Allowed files exactly `src/server/comparison/target-correlation.ts`, `src/server/comparison/finding-outcome.ts`, `src/server/comparison/compare-finding.ts`, `src/server/comparison/comparison-contract.ts`; allowed roots None. Forbidden files all other paths; forbidden roots `tests`, `docs`, `evaluation`, `fixtures`, `.codex`, `.agents`; no Git mutation. Accepted tests cannot be weakened or edited.

Working directory repository root. Exact README preparation/cache-disabled G7 wrapper. Focused `$m105Node --experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts`, immediate exit check; strict `$m105Node node_modules/typescript/bin/tsc --project tsconfig.json`, immediate exit check. Complete G7 suite/build at D. Expected M502-B-GREEN-01: all 24 A/B test bodies execute/pass unchanged, strict passes, complete G1–G5 behavior and cohesive source diff. Baseline canonical 176-path `82deb481fb5c1b11cfc19865e59631502d53acad803375859498f817df687f3d`; post-write identity uses Python ordinal relative paths, `chr(0)`, lowercase SHA-256, `chr(10)`, including source/test TS/TSX and package.json/package-lock.json/tsconfig.json/vite.config.ts. Environment Node 24.20.0/Python 3.14.6, unchanged lock/config. Side effects only four source paths and bound pure/read-only test execution with memory-only mocks; no browser/service/model/network/cache/other output. Cleanup restore environment; no filesystem removal.

Maximum Green turns initial plus one ordinary and one conditional final correction with recorded learning/progress; maximum corrections two under that gate. Maximum repeated decisive failure twice without new evidence; no-diff handoffs two. Validate coherent Green and after actual refactor; reuse exact accepted Red identity. Stop for test contradiction, changed binding/cohesion/path/effect or exhausted allowance; do not widen production signatures for test typing. Handoff identity/digest, actual changes, commands/results, protected hashes/canonical fingerprint, RETAINED/REFACTORED/RECONCILE disposition, risks/documentation impact. Primary owns lease closure/evidence acceptance.

### M502-B-GREEN-01 — Policy implementation ready for critical review

Lease `M502-B-green-01` verified and freshly **closed-compliant**, exit 0, receipt `186c67633f0e4b5751d410de12a1870d6b79e79cbe6418399e3fd95c29bace68`. The only changes are three B policy modules and result types in the existing contract owner. All three accepted test/helper hashes and the frozen companion remain unchanged. The prepared focused command executes and passes all 24 A/B tests; worker strict and independent primary prepared strict both pass. Primary inspected the complete source diff and responsibility placement: **RETAINED**, with projection/correlation, classification/wording and orchestration in their assigned cohesive owners. No service or browser behavior is claimed.

Primary independently confirms canonical 179-path fingerprint `9f9410f6de0b40ed0e5f144cd5e50e95f8436ca37282f3d2f8f0384e4484c516`. Initial Green turn consumed; no Green correction used. Fresh B critical review remains required before C. Review must challenge exact all-bucket identity, unavailable/contradictory evidence, contrast boundaries, minimized output and controlled binding. Review is read-only with no Git mutations, network/browser/model operations or retained-file changes; memory-only risk-critical probes may use the prepared runner. One supported review correction loop remains under the existing worker budgets.

### M502-C-PACKET-01 — Transient operation preflight (dispatch after B acceptance)

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-C`; assignment `M502-C-preflight-01`; phase preflight; attempt 1; lease/digest/correction parent None; persistent role `test_worker`, owner `/root/c_test`.

Owning ExecPlan this document. Observable acceptance C/G6: the real service invokes the internal comparison executor while candidates live, publishes the same independently admitted terminal snapshot, exposes comparison only after truthful successful publication without stop/abort/deadline, and preserves current HTTP/run outputs. Frozen G5/G7 supplies three real controlled pairs using unchanged six source buffers and actual stable-key/locator/element-kind assertions. Exact authority anchors Context and Orientation, REQ-COMP-001–008 within M5-02, REQ-EVID-002/008/010, REQ-SCAN-005, lifecycle, ADR-0021/0024, BHV-06 and SPEC-006. Readiness M502-G-ACCEPT-01; prerequisites A and B accepted; dispatch is forbidden before B review acceptance. TDD Applicable: new execution/lifetime behavior. Current evidence M502-B-GREEN-01, preflight None, accepted C tests None; A/B tests protected.

Production placement: `src/server/local-service/rescan-operation.ts` retains validated baseline selection in PreparedRescan; new `src/server/local-service/rescan-comparison.ts` owns exact single-snapshot envelope admission, transient computation and settlement projection; `src/server/service.ts` owns reservation and calls that executor, projecting outcome only; `src/server/local-service/scan-operation.ts` exports/renames its dependency type only. Dependency direction service to rescan executor to existing publisher and pure comparison; no comparison-to-service edge. Reuse existing publication, validation and scanner; bounded creation of one current operation owner. Permitted local refactor private helpers inside the new executor; no publisher behavior changes or production observer/cache/framework.

Future test scope: `tests/rescan-comparison.test.ts`, `tests/rescan-service.test.ts`, `tests/rescan-api.test.ts`, `tests/rescan-integration.test.ts`, `tests/helpers/m105-walking-skeleton-harness.ts`, `tests/helpers/comparison-fixtures.ts`. Preserve existing integration and A/B assertions; helper extensions may expose fixed hash-verified bytes and wrap the existing managed browser mechanism for exact frozen fixtures, with current consumers only. Preflight must identify whether both helper changes are actually needed. Primary found one additional existing raw terminal injection in rescan-api.test.ts at its disconnected-client commitment case; migrate only that test-owned injection to the explicit envelope and retain all assertions. Add its existing module-mock command to C focused verification, before scanner/browser effects; no API production behavior changes. Unit tests own adversarial reflection, create/publication failures, exception containment, cancellation/deadline and mutation/differing-descriptor snapshot witnesses; integration owns actual native scans and no private HTTP/durable escape. Non-goals public comparison, persistence/UI, later tasks, new scanner policy, source fixture changes, dependencies, external target/model operations. Named uncertainty existing injectable test envelopes and exact uncovered behavior. S3 for publication/identity/reentry/lifetime; fresh critical reviewer and independent strict before D.

Read-only preflight: allowed files/roots None; all writes and Git mutations forbidden. Inspect exact source/tests and reuse current runner evidence. No browser/service/scratch effects during preflight. Future commands are G7 C internal, service, scanner and integration rows in that order, sequential, each through exact README preparation/wrapper; scanner/integration use scanner scratch. Strict and complete task suite remain G7. Expected M502-C-PREFLIGHT-01 supported classification and complete test boundary with exact missing callable/import. Environment Node 24.20.0, Python 3.14.6, retained Chromium 151.0.7922.34, unchanged dependency/config files; canonical baseline 179-path `c666e8ebb44b0af533039e1cb544c596076797441344ac8cc0c36a9c7d13c17d` after B correction. Future owned integration child exactly `temp/m105-integration/m502-comparison`, create only if absent under ordinary empty shared scratch; fixed fixture bytes are read-only. No cleanup or generated output during preflight.

Budget one read-only preflight; unchanged writer phase chains initial plus one ordinary and one conditional final correction only with documented progress/new evidence; two identical decisive failures without new evidence or two no-diff handoffs stop. One supported review correction loop. First-module Red only for agreed new executor, complete assertions and verified runner; blocked assertions remain unexecuted. Stop on binding/semantics/effect expansion, missing owner decision or exhausted allowance. Handoff classification, exact coverage/callables, proposed complete assertions and any path/command reconciliation, evidence identity and risks/documentation impact. No agents or writes.

### M502-B-REVIEW-01 — Narrow policy mismatch requires correction

Fresh critical review independently passed 648 public bucket/locator combinations, 28 font-boundary cases, 36 exact-margin cases, three downstream-state comparisons and 76 controlled identity/order/integrity cases. Source fingerprint and protected hashes match. It found one Minor frozen-G4 mismatch: a pass or incomplete baseline followed by an incomplete later observation returns `native-incomplete`, although unsupported baseline/bucket combinations require `conflicting-evidence`. Public admission selects only violation baselines, so this cannot cause public false resolution. It still violates the selected acceptance contract and must be corrected before C.

This is a routine in-task correction under the bug-recording rules, not a separate defect record. The sole supported B review correction loop is now used. New independent executable evidence justifies the conditional final test correction: prior correction resolved fixture typing and reason expectations, all 24 tests then passed, and the newly isolated unsupported-bucket gap is distinct. No allowance resets. Red attempt 3 is the last permitted test write turn; Green ordinary correction attempt 2 remains available. Other B behavior and frozen semantics stay unchanged.

### M502-B-PACKET-05 — Final conditional Red correction

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-B`; assignment/lease `M502-B-red-03`; phase red; attempt 3; correction parent `M502-B-red-02`; persistent test owner `/root/b_test`, guard owner `b_test`; digest supplied after verified start.

Owning plan this document; exact authority, responsibilities, dependencies, reuse, non-goals and S3 risk remain B PACKET-01/03 and frozen G4. Observable correction only: unsupported baseline native pass/incomplete followed by later incomplete must be inconclusive/conflicting-evidence; actual violation to incomplete remains native-incomplete. Current accepted preflight M502-B-PREFLIGHT-01; current Green/review M502-B-GREEN-01/M502-B-REVIEW-01. TDD Applicable; source now exists, so this must be executed assertion Red, never module absence. Accepted boundary is invalidated only by the narrow added regression assertion, reaccepted before Green. Preserve every existing assertion.

Guard allowed files remain exactly `tests/comparison-outcome.test.ts` and `tests/helpers/comparison-fixtures.ts`, matching the correction parent; the semantic assignment permits changing only the outcome test, and the helper hash must stay unchanged. No allowed roots. Forbid `tests/comparison-pair.test.ts`, all source/docs/evaluation/fixtures/config/agent writes, and all Git mutations. No helper/production edits or agents. The first guard-start request attempted a narrower scope and was rejected before a lease existed; the reconciled request preserves the parent scope without expanding the semantic assignment or resetting its attempt. Exact README preparation/wrapper and B focused command with module mocks; require the new expected-reason mismatch, all unaffected assertions pass. Then prepared strict must pass. Environment unchanged; baseline 179-path `9f9410f6de0b40ed0e5f144cd5e50e95f8436ca37282f3d2f8f0384e4484c516`. Effects only the named test and memory-only/read-only pure checks; no browser/service/network/cache/output; restore environment/mocks. Record new test hash and canonical fingerprint using actual NUL/LF via chr(0)/chr(10).

This consumes the conditional final Red correction justified above; no Red turns remain afterward. The sole supported review correction loop is active. Stop on unresolved contract, unexpected failure, path/effect change or exhausted budget. Handoff exact identity/digest, actual assertion failure and unaffected results, strict result, protected hashes and residual risk. Primary owns lease closure and acceptance.

### M502-B-RED-03 — Final corrected regression boundary accepted

Lease red-03 verified and freshly closed-compliant, receipt `bb80ab5501bc9d3e393f259109177ad92281ea0b7f60a3b45e3bb7db14f6b8bf`; only the outcome test changed. Primary inspected the two added unsupported-baseline assertions and preserved violation-to-incomplete expectation. Prepared B command exits 1: 23 tests pass, the new pass-to-incomplete assertion fails with native-incomplete versus conflicting-evidence; the subsequent incomplete-baseline assertion is authored but blocked by that first loop failure. Strict passes. Revised test boundary accepted with outcome hash `a0d70ca57f5b3dce97cccc0234f9821c9977cb8bdbcf51244995ad422379e5c4`, unchanged pair/helper, canonical 179-path `cde4d79405195341eb7131baebf52785012f61d72746ca07ed9928e58ecdfb86`. No Red allowance remains.

### M502-B-PACKET-06 — Ordinary Green review correction

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-B`; assignment/lease `M502-B-green-02`; phase green; attempt 2; correction parent `M502-B-green-01`; persistent role code_worker, owner `/root/b_code`, guard owner `b_code`; verified digest supplied at dispatch.

Owning plan this document. Exact authority/readiness/requirements, source placement, dependencies, reuse, local-refactor limits, non-goals and S3 route remain PACKET-04 and G1–G5. Observable correction is M502-B-REVIEW-01 only; test owner b_test and revised accepted boundary M502-B-RED-03 supersede the earlier outcome hash. TDD Applicable with executed reason-mismatch Red. Preserve all tests. No unresolved semantics or architecture decision. Correct the unsupported baseline guard before later-incomplete handling while preserving supported binary reversal and actual violation-to-incomplete behavior. No wider refactor.

Guard scope remains the same four source files as green-01: comparison-contract.ts, target-correlation.ts, finding-outcome.ts and compare-finding.ts under src/server/comparison. Semantic correction should touch only finding-outcome.ts. No allowed roots; forbidden roots tests/docs/evaluation/fixtures/.codex/.agents; all unlisted writes and Git mutation forbidden. Exact README preparation/cache-disabled B focused command and strict from PACKET-04; all 24 tests including both new assertions must execute/pass. Environment/effects/cleanup unchanged pure scope. Canonical baseline 179-path `cde4d79405195341eb7131baebf52785012f61d72746ca07ed9928e58ecdfb86`; hash protected tests and actual NUL/LF fingerprint at handoff.

This uses the ordinary Green correction and active sole supported review correction loop. One conditional final Green turn exists only on demonstrated progress/new evidence; no Red turns remain. Stop for contradiction, binding/effect changes or exhausted allowance. Handoff exact identity/digest, diff/cohesion, tests/strict, hashes and residual risks; primary verifies and closes lease, independently checks strict, and obtains complete B re-review.

### M502-B-GREEN-02 — Reviewed reason correction verified

Green-02 verified and freshly closed-compliant, receipt `356b1f71c49ff226391a967606752433ffd31bc6f2667fd39ba64294d667f44f`. Only finding-outcome.ts changed: unsupported baseline handling now precedes later incomplete, preserving supported binary reversal. Primary inspected the branch change, confirmed protected test hashes and independently passed prepared strict. Worker prepared focused command passes all 24 tests, including both new assertions. Canonical 179-path identity `c666e8ebb44b0af533039e1cb544c596076797441344ac8cc0c36a9c7d13c17d` matches. Cohesion RETAINED. Complete B re-review is the remaining barrier; no further Red or review correction loop remains.

### M502-B-ACCEPT-01 — Correlation and outcome barrier accepted

Primary accepts fresh critical re-review **PASS**, no remaining findings. Independent reviewer verified the exact one-branch correction and 83 targeted rule/bucket checks, including the original defect, supported reversal and insufficient positive evidence. The previous 791 risk-focused cases remain applicable to unchanged behavior. Primary accepts the actual source diff, revised immutable test boundary, prepared 24-test/strict evidence, matching 179-path fingerprint and terminal compliant receipt recorded in GREEN-02. Cohesion RETAINED. B's review loop and final Red turn are consumed; no open correction remains. No browser/service behavior is claimed by B. C may now preflight under PACKET-01, including the inspected API injection migration.

### M502-C-PREFLIGHT-01 — Operation boundary missing

Persistent C test owner returns **MISSING**, accepted after inspection of the actual candidate-discarding service path and absent executor, PreparedRescan and dependency-type exports. No writes or mutable effects occurred. A corrected prepared read-only probe confirms the boundary; an earlier stale native exit-code check after a PowerShell inspection is excluded from evidence. Fresh B runner/profile evidence remains valid. Exact first-module import is `../src/server/local-service/rescan-comparison.ts` / `executeRescanComparison`; complete new assertions must initially remain unexecuted at module linking.

Both helper extensions are necessary: the comparison fixture owner exposes only fixed verified endpoint metadata and the exact served buffer; the existing managed browser helper gains the bounded six-state route and actual DOM identity assertion. No new harness framework or production adapter is justified. The complete proposed test contract covers single snapshot/data-descriptor access, malformed candidate envelopes with preserved scan truth, comparator exceptions, publisher failures, stop/abort/deadline precedence, successful-commit reentry, service outcome-only projection, exact baseline selection and all three real pairs with no HTTP/disk escape. Existing service/API injections must explicitly migrate. C remains within frozen G6/G7.

### M502-C-PACKET-02 — Complete operation Red assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-C`; assignment/lease `M502-C-red-01`; phase red; attempt 1; correction parent None; persistent test owner `/root/c_test`, guard owner `c_test`; digest supplied after verified start.

Owning plan this document. Observable acceptance, exact authorities, readiness G/B acceptance, production placement/dependency/reuse/refactor limits, non-goals and S3 risk remain C PACKET-01 and frozen G5–G7. Current/preflight M502-C-PREFLIGHT-01; TDD Applicable; accepted C tests None until handoff. Preserve all A/B assertions and existing integration/service/API behavior assertions; helper additions invalidate helper evidence identity only, which must be reaccepted with passing A/B tests before Green. New tests directly import the missing executor and require no production stub. The accepted preflight test boundary above is complete; unknown/contradictory expectations return to primary.

Allowed files exactly `tests/rescan-comparison.test.ts`, `tests/rescan-service.test.ts`, `tests/rescan-api.test.ts`, `tests/rescan-integration.test.ts`, `tests/helpers/comparison-fixtures.ts`, `tests/helpers/m105-walking-skeleton-harness.ts`. No allowed roots. Forbidden files `tests/comparison-pair.test.ts`, `tests/comparison-outcome.test.ts`; forbidden roots src/docs/evaluation/fixtures/.codex/.agents. All unlisted writes and Git mutations forbidden. No agents. Read every edited existing file completely.

The new internal suite stays memory-only using the existing module-mock mechanism for comparator exceptions; no production seam. Use existing controlled scan harness and repository for the integration witness, owning only create-only `temp/m105-integration/m502-comparison`. Serve exactly the verified buffer, abort other requests, and assert declared key/locator/kind inside the actual browser scan; no raw archives/screenshots. Preserve existing built-client case. New integration cleanup validates resolved ordinary owned descendants and closes resources before removal, preserves uncertain residue and shared parents. Existing service/API regression roots keep their established withReviewSandbox contracts.

Working directory repository root. Every command uses exact README preparation and cache-disabled wrapper with immediate exit inspection. Red internal command G7 must fail only at agreed absent executor and report all blocked bodies unexecuted; integration import may likewise block both existing/new bodies without any browser effect. Prepared `--check` for every changed test/helper must pass. Run B's prepared 24-test regression to reaccept unchanged policy behavior after helper extensions. Service/API migration checks may run their G7 commands sequentially and must report honest expected envelope-contract failures, not count them as passing; existing temporary service roots/listeners and cleanup are the only permitted effects there. No scanner/browser execution until Green because the new integration callable is absent. Strict still has absent-module/export limitations; do not add a stub or widen signatures. Full C focused sequence and independent strict are Green obligations; complete suite/build D.

Baseline canonical 179-path `c666e8ebb44b0af533039e1cb544c596076797441344ac8cc0c36a9c7d13c17d`; Node 24.20.0/Python 3.14.6/retained Chromium unchanged. Record all six test-path hashes, protected pair/outcome hashes and new canonical source/test/config fingerprint with actual chr(0)/chr(10). Original companion/manifests/fixtures remain read-only. Environment and mocks must restore; no cache/acquisition/public target/model/original-run effects.

Budget initial Red plus one ordinary and one conditional final correction only on recorded progress/new evidence; two repeated decisive failures without new evidence or two no-diff handoffs stop. One supported review correction loop. Validate at the coherent boundary, not assertion by assertion. Stop for changed semantics, path/command/effect expansion or exhausted allowance. Handoff identity/digest, complete authored assertion inventory, exact commands/results and blocked bodies, hashes, actual source preservation and resource cleanup, risks/documentation impact. Primary owns lease and acceptance.

### M502-C-RED-01 — No-write stop reconciled

The test worker returned BLOCKED before writing after comparing active.json's own payload digest with the dispatched contract digest. Primary inspected guard `_require_contract_pin`, the immutable contract.json and freshly reran verify: the actual contract digest is the dispatched `df7b970ccf541ed601d01c2cf37565cdd2ca653d43016b6e71f604789a8fc637`; active.json's digest identifies a different smaller pointer payload and is not the contract pin. There was no drift or guard violation. The untouched lease freshly closed-compliant, receipt `b60d439d040a7616e331101e747bb69c2b9018add0f23d1122b73384264735eb`. No tests or source changes occurred.

This is one no-diff handoff and consumes the initial Red turn; primary does not reset the budget or reopen the lease. The same owner receives the ordinary correction with exact verified contract identity and unchanged work. The next no-diff handoff or a repeated decisive failure without new evidence would stop under the recorded budget.

### M502-C-PACKET-03 — Reconciled Red assignment

Milestone Assignment Packet v2. All capsule, authority, acceptance, responsibility/dependency/reuse/refactor, exact six allowed paths, forbidden paths, command/effect/cleanup, environment, fingerprint and handoff fields of C PACKET-02 remain unchanged and are incorporated here. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-C`; assignment/lease `M502-C-red-02`; phase red; attempt 2; correction parent `M502-C-red-01`; persistent test owner `/root/c_test`, guard owner `c_test`; new verified contract digest supplied at dispatch. Read immutable `logs/agent-flow-leases/v2/M502-C-red-02/contract.json` for the contract pin, not active.json's pointer checksum. Primary owns guard mutations; worker may inspect both without interpreting unlike digests as drift.

TDD Applicable; preflight M502-C-PREFLIGHT-01; accepted C tests still None. Author the complete unchanged PACKET-02 test boundary and its decisive Red/compilation/A/B regression evidence. This is the sole ordinary Red correction; afterward only a conditional final turn remains on recorded material progress/new evidence. One prior no-diff handoff, zero test edits. Do not restart counts. All other stop conditions and the one supported review correction loop are unchanged. No new source, semantics, effects or paths are authorized.

### M502-C-RED-02 — Authored boundary needs the conditional correction

Lease red-02 verified and freshly closed-compliant, receipt `5a93e62c550bfa01eed603fce70e26b147d0d88c715ac0d61a1f10501faf2161`; exactly the six authorized test/helper paths changed. Prepared internal command has the exact absent executor failure; four test bodies are blocked. All six syntax checks and B24 pass; migrated service/API report expected old-contract failures (1/7 and 5/6 passing), with cleanup complete. Canonical 180-path identity `ba2803a45cf311c32678413583d92e993169b984e5d1a831ba76c9da6f76ba4e` records this unaccepted boundary.

Primary source inspection found decisive remaining issues: a raw terminal incorrectly expects successful publication despite having no envelope run descriptor; globally replacing the comparator with an invalid success object fails to test real candidate admission or snapshot equivalence; several promised reflection/failed-envelope/prepare assertions are absent. Real integration uses the old synthetic run profile and URL instead of the frozen production profile, bypasses the required real repository/owned child, omits declared-profile binding, and the page evaluation closes over a host-only fixture variable that is unavailable in the browser. These are test defects, not production decisions, and Green cannot start against them.

A conditional final Red correction is justified by material progress (complete injection migrations, six authored paths, clean effects and passing protected policy tests) plus specific new primary inspection evidence. The no-write first turn was a distinct checksum interpretation error; red-02 produced a diff, so this is not two consecutive no-diff handoffs. No allowance resets. This final correction must complete the frozen acceptance contract, not enlarge it.

### M502-C-PACKET-04 — Conditional final Red correction

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-C`; assignment/lease `M502-C-red-03`; phase red; attempt 3; correction parent `M502-C-red-02`; persistent test owner `/root/c_test`, guard owner `c_test`; new verified contract digest supplied at dispatch. All unchanged capsule fields in PACKET-02/03 remain binding: exact authority G5–G7, source responsibility/dependency/reuse/refactor boundaries, six allowed paths, two protected tests, forbidden roots, commands, effects, cleanup, environment and handoff. Current preflight M502-C-PREFLIGHT-01; current Red02 is unaccepted and superseded; TDD Applicable. Baseline canonical 180-path `ba2803a45cf311c32678413583d92e993169b984e5d1a831ba76c9da6f76ba4e`.

Required corrections: (1) raw terminal returns existing result-validation/scan-unavailable, while independently valid enveloped terminal survives malformed comparison data; (2) use actual comparator behind a spy and scope only its intentional exception injection, with valid run/terminal fixtures checked by existing matchTerminalRun, explicit candidate rule/count/malformed cases, differing-descriptor and mutation-at-later-reflection witnesses asserting comparator input and published evidence are the same detached snapshot, failed scan envelopes, no run/candidate getter invocation, stop reentry at reflection and commit, and actual prepareRescan baseline selection/detachment; (3) real integration uses prepareScanRequest/prepareRunningRun and actual executeScan for the baseline, actual repository create/finish and prepareRescan, then actual executeRescanComparison/executeRescanScan for the later state; fixed m105 URL and full declared production profile, exact same verified fixture buffers, actual stable-key/locator/kind observations, shared controlled binding and exact bounded-result consistency. Own only create-only m502-comparison child with baseline byte preservation, no private durable fields, exact request/launch counts, restored mocks and ownership-checked cleanup; preserve existing built-client case; (4) pass expected locator explicitly as the browser evaluate argument and assert pinned Chromium version; (5) use proper discriminant narrowing and domain-validated fixtures, not casts concealing unsupported assumptions. Original sources and protected A/B behavior must stay intact.

Before handoff inspect the complete test contract against preflight, run prepared syntax and B regression, repeat only affected expected Red checks, and run strict to expose all diagnostics not attributable to the agreed absent executor/types. Do not fabricate a production stub; identify true missing-export limitations explicitly. No browser/scanner run until Green. All new browser and repository assertions are authored but blocked at missing-module resolution. This is the final permitted C Red turn; no further Red corrections remain afterward. Existing Green/review budgets remain unused. Stop on unresolved contract, scope/effect changes or exhausted budget. Primary inspects actual files and closes lease before acceptance.

### M502-C-RED-03 — Complete corrected boundary accepted

Primary inspected all six C test/helper paths, including corrected native profile, actual repository integration, descriptor mutation, real comparator spy, candidate count/rule failures, original-source binding and cleanup forwarding. During the active final turn, concrete fixture and harness defects were corrected before handoff; no additional write turn or budget was created. Fresh verify/close is compliant, receipt `4143f16aadab9517acba76cca0a30c34650e6f695fe8e7d918da274419314cdb`; four paths changed in red-03 and service/API remained byte-identical from red-02. No forbidden/Git drift occurred.

Prepared internal Red is exactly the absent executor, with all four authored bodies unexecuted. Six syntax checks and B24 pass. Strict reports only the five absent module/type-export diagnostics, not test errors. Unchanged service/API expected Red and clean resource evidence remain applicable. Browser/integration assertions are authored but unexecuted. Primary confirms canonical 180-path `019da975cabac5b9c0eb3546e94700527960b1105281aa1e6ac55c014b93f368` and accepts this revised boundary. No C Red turns remain.

Protected C hashes: internal `efec15686584657d1d115726d0f26a0cfc9c2cf44e85268074a136c08008c43a`; service `d53db0dee22ffa17792f08e5d5a95eeafbbfa6b8963f077600ecd066aee8f541`; API `fa5d381bd009072a08ec2df7f7344b6c8d3b77d583c62677f4ac520b669bf449`; integration `efb0eed0ce24b8c9418c0714a9cdbb014ae1f981b0d7c390ac2cf0c1cca80452`; comparison helper `30b304f7a8ee6565f369e964d8035bb30891d3c66ad980cc60a0eaee9f1699f8`; browser helper `912cf79976f211ad4c9d32f8b7a1007cb326575c0a447cca32d29c7598a87436`. Pair/outcome hashes remain B accepted values.

### M502-C-PACKET-05 — Complete transient-operation Green assignment

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; slice `M502-C`; assignment/lease `M502-C-green-01`; phase green; attempt 1; correction parent None; role code_worker, owner `/root/c_code`, guard owner `c_code`; new verified contract digest supplied at dispatch.

Owning plan this document. Observable acceptance C/G6 plus real G5/G7 integration; exact requirement/ADR/BHV/SPEC anchors and scope in C PACKET-01 and Context and Orientation. Readiness G/A/B accepted; TDD Applicable; preflight M502-C-PREFLIGHT-01; accepted corrected test boundary M502-C-RED-03 owned by `/root/c_test`, immutable during Green. All six C paths and A/B tests are protected. Named uncertainties None: test or contract contradictions return to primary, never weaken tests or widen policy.

Placement: rescan-operation.ts adds exported PreparedRescan and retains already validated baseline/Finding; new rescan-comparison.ts owns one-time run descriptor extraction, independent terminal admission, exact envelope validation without rereading run, synchronous bounded comparison, reference release and publisher settlement projection; service.ts retains reservations/capability retirement and invokes that executor, returning outcome only; scan-operation.ts exports/renames Dependencies to ScanOperationDependencies without behavior change. Direction service to operation owner to existing publisher/pure comparison/scanner. Reuse existing matchTerminalRun, startScanOperation, compareFinding and executeRescanScan; no new publisher, observer, cache, persistence/API/client field or framework. Private helpers inside new owner are the only permitted local refactor; source responsibility boundaries must remain cohesive. S3 identity/publication/reentry/lifetime requires fresh critical review and independent strict before D.

Allowed files exactly `src/server/local-service/rescan-operation.ts`, `src/server/local-service/rescan-comparison.ts`, `src/server/local-service/scan-operation.ts`, `src/server/service.ts`; no allowed roots. Forbidden roots tests/docs/evaluation/fixtures/.codex/.agents; all other files and all Git mutation forbidden. No agents. Read complete existing targets. Preserve the exact single admitted snapshot despite later envelope traps; catch comparison exceptions without inventing scan failure/uncertain cleanup; recheck stop after reflection; expose bounded comparison only after successful durable outcome with stop/abort/deadline suppression. Release raw envelope/candidates after calculation, retaining no collection in results or service state. Successful commit then stop remains a completed scan with comparison shutdown.

Working directory repository root. Exact README preparation/cache-disabled wrapper. Run G7 C internal, C service, C API, C scanner and C integration sequentially with immediate exit checks, stopping on unexpected failure. Scanner/integration use scanner scratch and pre/post empty-directory checks; existing built client is already present and unchanged. Then strict command. Complete suite/build are D. Expected evidence all accepted C assertions execute/pass, real six frozen states produce three declared resolved comparisons through actual scanner/repository/executor, existing client output stays unchanged, all scratch/resource ownership checks pass. Baseline180 `019da975cabac5b9c0eb3546e94700527960b1105281aa1e6ac55c014b93f368`; environment Node24.20.0/Python3.14.6/retained Chromium151.0.7922.34, unchanged lock/config. Mutable verification effects only existing test-owned roots/listeners/browser scratch and create-only m502-comparison child; no original retained runs, external public targets/model calls/acquisition/archive/cache. Tests own path-verified cleanup and resource closure, restore environment/mocks; preserve uncertain residue. Never remove shared scratch parents or stop developer-owned processes.

Budget initial Green plus ordinary and conditional final correction only with recorded progress/new evidence; no C Red allowance remains. Two identical decisive failures without new evidence or two no-diff handoffs stop. One supported C review correction loop remains. Validate coherent Green; do not repeat broader checks without drift/failure. Handoff identity/digest, exact diff/responsibility fit RETAINED/REFACTORED/RECONCILE, commands/results/native integration and cleanup, protected hashes/canonical source-test-config identity using actual chr(0)/chr(10), risks/documentation impact. Primary inspects and closes lease, independently checks strict and routes review.

### M502-C-GREEN-01 — Real transient integration ready for critical review

Initial C Green implements exactly the four assigned source paths. Primary inspected the complete new executor and existing-file diff: one independently admitted terminal snapshot feeds comparison and the unchanged publisher; envelope reflection never rereads run, stop is checked after reflection, unexpected comparison errors preserve scan truth, raw references are released, and service projects only outcome. Publisher changes are type-only. Responsibility fit **RETAINED**; no broader refactor or speculative mechanism. All protected C/A/B test hashes remain unchanged.

Prepared focused checks pass: internal 4, service 7, API 6, scanner 105 and integration 2 (124 total across five files). The integration includes the preserved built-client scenario and actual six frozen buffers producing all three declared resolved comparisons through the production scanner, real repository and comparison executor. Exact target-key/locator/kind and profile checks, baseline bytes, durable/HTTP non-escape and cleanup assertions passed. Scanner execution preceded the final stop-check refinement; that pure executor change cannot affect the scanner-only suite, whose evidence is reused. Internal/service/API and integration pass on final source. Worker strict and independent primary prepared strict pass. No actual public target/model/acquisition/original-run action occurred.

Fresh verify/close is compliant, receipt `f185f31b890280ac5b61ca871663ea000e7c88df01a32a54f2862336cfac8ea4`, with only new rescan-comparison.ts and the three named existing source modifications. Primary confirms canonical181 `4c0ed8590cc1675fba2ae26a48be02c3cd803a1c2e07f608b64c8ca44101bf96` and all six protected hashes; all three shared scratch parents are ordinary and empty and the owned child is absent. C initial Green consumed; no Green corrections used. Fresh critical review remains before D; review is read-only and reuses valid focused/strict/browser evidence, adding only missing or risk-critical probes under prepared commands. No writes/Git mutation/public target/model calls or acquisition. One supported C review correction loop remains, with no C Red turn remaining.

### M502-C-ACCEPT-01 — Transient integration barrier accepted

Fresh `critical_reviewer` `/root/c_review` returns PASS with no findings after source and evidence inspection plus 30 prepared memory-only adversarial cases. Reflection stop points, candidate accessors/throws, postcommit suppression and detached references pass. The reviewer confirms canonical181, eight protected A/B/C hashes, nine companion/source/fixture identities, compliant Green receipt and empty scratch; it reuses valid 124-test, strict and native integration evidence. A reviewer-only hash assertion initially confused normalized lease hashes with raw-byte test identities, stopped before product probes, and passed after correcting that assertion; no source drift or product failure occurred.

Primary accepts actual four-path source diff, cohesive ownership, preserved tests, fresh terminal receipt and complete review. C is retired with all three Red turns consumed, initial Green only, no Green correction or review loop consumed. No active lease exists. D may run the full maintained suite plus new tests, strict/build, preservation and documentation checks. The service still returns scan outcomes only; comparison is internal and operation-local. M5-03 remains unselected.

### M502-D-ENTRY-01 — Integrated verification and closure boundary

Primary owns D; no application writes are planned. Reuse accepted G/A/B/C semantics and focused evidence at canonical181 `4c0ed8590cc1675fba2ae26a48be02c3cd803a1c2e07f608b64c8ca44101bf96`. Execute the maintained README preparation, independent strict and client build against the inspected ordinary three-file `dist/client`, followed by all 40 current test files sequentially with existing module-mock and scratch groups. No acquisition, actual public scan, model call, original-run mutation or Git write is authorized. Preserve all scratch parents and verify exact cleanup and frozen identities. Reconcile capability/command/lifecycle/navigation/status documentation; obtain a different fresh integrated critical reviewer over I1–I10 before Complete/archive.

### M502-D-VERIFY-01 — Complete regression and invariant reconciliation

At unchanged canonical181 `4c0ed8590cc1675fba2ae26a48be02c3cd803a1c2e07f608b64c8ca44101bf96`, primary ran fresh strict TypeScript (exit 0), inspected the exact ordinary three-file `dist/client` inventory and prior hashes, and built the client with the maintained native-loader command (exit 0, 67 modules). No client source or dependency changed. Each session used the exact README preparation and environment-restoring cache-disabled wrapper.

The complete updated README inventory ran sequentially, preserving existing flags, scratch groups and immediate exit checks: **805 tests passed across 40 files**, all exits 0. Browser-free ordinary tests: 538 in 24 files; module-mock tests: 58 in seven files; scanner/integration: 122 in three files; UI/checkpoints: 87 in six files. New inventory entries are comparison-pair, comparison-outcome and rescan-comparison; rescan-integration includes the additional actual six-state comparison witness. No capture flag, model/provider call or actual public target was used. All three scratch parents remained ordinary and empty, including removal of only the owned comparison child; environment restoration passed.

Fresh byte checks confirm the companion hash, both original manifests, all six fixture hashes and thirteen generation source references. The original files are unchanged in Git. The first generic hash checker treated uppercase hexadecimal references as unequal; lowercasing representation resolves that checker-only discrepancy. Source identity remains unchanged. Native integration proves the three declared failing-to-corrected pairs; reverse binary and contrast arithmetic/adverse witnesses remain policy-only.

| Invariant | Accepted evidence and limitation |
|---|---|
| I1 | G reviewed directed companion and unchanged original inputs; fresh D hashes and scoped diff; no later task or public/model operation. |
| I2 | A pair admission and C independent single-snapshot admission; valid mismatches bypass correlation; malformed comparison data preserves valid scan truth. |
| I3 | A exact 16-field profiles, ordered mismatch codes and downstream/count exclusions; B composition tests. |
| I4 | B all-bucket exact matching and controlled metadata/direction gate; C actual stable-key/locator/kind observations. |
| I5 | B native positive minima and uniqueness, bounded evidence projection; C no candidate/comparison HTTP or disk leakage. |
| I6 | B binary persistence, policy-only reversal and sufficient contrast margin ordering; corrected unsupported-baseline reason and critical re-review. |
| I7 | A/B detached frozen data and downstream independence; C preserved baseline bytes and truthful bounded rationale; no later-only classification. |
| I8 | C real envelope consumption, failure/abort/deadline/commit tests and 30 independent adversarial cases; existing publisher retained. Reference release is not physical memory erasure. |
| I9 | Three declared pairs through actual preparation/scanner/repository/executor plus shared controlled binding, re-executed during D; original history stays separate. |
| I10 | Protected tests and compliant terminal leases; actual cohesive diff; all 805 tests, strict/build and cleanup pass. Different final review and final documentation/status/archive gate remain pending. |

Candidate documentation updates reconcile README capabilities and all 40 test commands, lifecycle implementation wording, and documentation navigation. Existing UI scope and requirement/ADR semantics remain unchanged. Eight-document validation passes 648 local links/anchors, excluding fenced templates; all 25 changed/new files pass UTF-8, trailing-whitespace and final-newline checks, and `git diff --check` passes. A temporary primary-only encoding error in two documentation writes was corrected and the actual diff rechecked; no product file changed. Final roadmap/plan/progress/index status and archive movement are intentionally pending the different integrated review.

### M502-D-REVIEW-01 — Checkout reproducibility correction required

Different fresh integrated critical reviewer `/root/d_review` found one Major: the new raw-hash-bound companion has no explicit text/eol rule while this checkout uses `core.autocrlf=true`. Current LF bytes hash to the accepted companion identity, but an ordinary Windows checkout may produce CRLF and invalidate `loadDefinition` even though logical JSON is unchanged. Primary reproduces unspecified `text`/`eol` and the active setting. Existing `.gitattributes` pins only the seven original RD-003 inputs. This violates I1/I9/I10 and blocks closure; passing current-tree tests does not prove checkout reproducibility.

Primary reconciles D's preservation scope to add only the exact companion LF attribute. This is a non-behavioral configuration correction to the new task-owned definition, not a change to frozen bytes, runtime policy, tests or Accepted architecture. It uses D's sole integrated review correction loop and does not reopen or reset any A/B/C worker chain. The bug index permits this contained task correction without a separate bug record. Source/test evidence remains valid, but checkout-preservation evidence must be replaced. One guarded setup turn is authorized; no setup correction allowance is added.

### M502-D-PACKET-01 — Preserve companion checkout bytes

Milestone Assignment Packet v2. Workflow `M5-02-20260917`; task `M5-02`; work-slice `M502-D`; assignment/lease `M502-D-setup-01`; phase setup; attempt 1; correction parent None; role `code_worker`, persistent agent `/root/c_code`, guard owner `c_code`; contract digest supplied after primary start and verification. Owning ExecPlan this document. Observable acceptance: `/evaluation/m502-comparison-v1.json text eol=lf` is explicit in the existing attributes file, and current/future checkout bytes preserve the frozen companion without a Git metadata write.

Authorities: Context and Orientation's REQ-EVID-002, REQ-COMP-001/002, REQ-EVAL-004/005, M5-02 Verification, BHV-06/SPEC-006, and I1/I9/I10. Readiness/freeze G-ACCEPT-01 and D-VERIFY-01; defect D-REVIEW-01. TDD Not applicable: exact Git text-conversion configuration has no application behavior to Red-test; replacement evidence is explicit attributes, read-only checkout-filter output over existing LF blob bytes at the new path, preserved companion hash and focused comparison tests. Preflight and accepted test boundary/owner: None for setup; all existing tests remain protected.

Relevant path `.gitattributes`. Production responsibility placement: None — no application-source responsibility changes. Dependency/runtime/interface edges: None. Reuse: extend the existing exact-path byte-preservation policy with one companion entry, optionally one concise purpose comment. Structural refactor: None. Non-goals: all source/test/fixture/definition edits, dependencies, general Git settings, staging/index/object writes, checkout/renormalization, actual target/model operations. Named uncertainty: only whether the exact-path attribute prevents checkout conversion; existing Git filter mechanism provides direct read-only evidence. Risk S3 because frozen input identity; same integrated critical reviewer rechecks the full invariant contract plus this correction.

Allowed files: `.gitattributes`; allowed roots None. Forbidden files: all except that allowlisted path; explicitly package.json, package-lock.json, tsconfig.json, vite.config.ts. Forbidden roots: src, tests, fixtures, evaluation, corpus, docs, .codex, .agents. Working directory repository root. Focused checks: `git check-attr text eol -- evaluation/m502-comparison-v1.json`; Python read-only `git cat-file --filters --path=evaluation/m502-comparison-v1.json HEAD:evaluation/rd003-scan-v1.json` under `core.autocrlf=true,false,input` and `core.eol=lf,crlf`, comparing each output to the exact existing LF blob; the committed blob is a checkout-filter witness, not a substitute companion identity. Verify the actual companion's raw hash unchanged. Run prepared cache-disabled A/B focused command `--experimental-test-module-mocks --test --test-timeout=120000 tests/comparison-pair.test.ts tests/comparison-outcome.test.ts`. Task-level full 805 tests, strict/build are reusable because no runtime/test/build input changes; no broad repeat without new evidence. Relevant-tree canonical181 and companion hash remain D-VERIFY-01; .gitattributes baseline raw SHA256 `a8f04c4c4311baa6f9cf55c13d93bcf026a8d0cfc5c065b26cd82c1256987660`. Environment existing Git/core.autocrlf=true, Node24.20.0 and README wrapper; no acquisition. Effects: only the allowed text edit, memory-only filter/probe output and ordinary focused tests; no scratch creation or cleanup needed.

Budget: one setup write turn, zero setup corrections, D's sole integrated review correction loop consumed. No test preflight/Red/Green is fabricated or reset. Stop on failure, unexpected scope/effects, frozen-byte change or guard issue; primary triages within existing authority and cannot renew budgets. Cohesion disposition None for setup. Handoff states exact identity/digest, changed paths, commands/exits, protected hashes, outcome, unexpected state, residual risk and documentation impact. Primary inspects actual diff, verifies/closes lease and obtains final re-review before closure.

### M502-D-SETUP-01 — Checkout-byte preservation verified

The single authorized setup turn appended only `/evaluation/m502-comparison-v1.json text eol=lf`; all seven existing entries remain unchanged. Primary inspected the exact one-line diff, verified the lease and freshly closed it compliant: receipt `051f3c1b3e327d61c0b9708f392f5ac5c79f0916ccb2acd05adb5979b05a7f95`. No forbidden or Git metadata drift occurred. New `.gitattributes` raw hash is `cd67e408508680c431d69b2a1847b9d600f6aaff210e4fd9d8593d3810b1ddd7`; canonical181 and actual companion hash remain unchanged.

Explicit attributes now report `text: set`, `eol: lf`; all six read-only checkout-filter combinations preserve the witness bytes exactly. The 24 prepared focused A/B tests pass, as does `git diff --check`. TDD replacement evidence establishes checkout conversion without fabricating a behavioral Red or writing Git objects/index. All 805 full-suite tests, strict/build and native integration remain reusable against unchanged executable inputs and current bytes. Cohesion None for this configuration-only correction. D's sole setup turn and integrated correction loop are consumed; final re-review remains before closure.

The initial integrated review also independently passed ten memory-only cross-slice cases covering all three rules, insufficient evidence, profile mismatch, publication failure, postcommit abort and detachment; it found no other semantic or cohesion defect and verified all eleven prior terminal receipts. The new setup receipt is the twelfth. The Major's loader/source-integrity failure was reproduced with memory-only CRLF substitution, preserving the on-disk companion.

### M502-FINAL-01 — Integrated review and task closure

Different fresh integrated `critical_reviewer` `/root/d_review` returns **PASS** on its sole re-review, with no remaining Blocker, Major or Minor across G1–G7 and I1–I10. It independently confirms the exact one-rule correction, explicit LF attributes, all six checkout-filter combinations, unchanged companion/canonical181, compliant setup receipt, no active lease and passing diff check. The sole Major is resolved. Its ten integrated memory-only cases and the accepted 24 focused tests, complete 805-test suite, strict/build and actual six-state scanner integration remain valid and reusable.

Primary accepts the actual integrated diff, all twelve compliant terminal receipts and required research/implementation/final reviews. The README inventory exactly matches every current test file once; rebuilt client artifacts match their prior three hashes. Current source/test/configuration identity remains canonical181, and the separate `.gitattributes` identity is recorded in D-SETUP-01. Original manifests, fixtures, generation references and retained runs remain unchanged. All three shared scratch parents are ordinary and empty, the owned comparison child is absent, no lease is active, and HEAD remains `4309a8f05d56032241c5a9418467f5505a455680`. No commit or push occurred.

Documentation impact: Updated README capabilities and 40-file commands, lifecycle implementation wording, documentation navigation, roadmap status, this plan, progress summary and both indexes. Preserved requirement/ADR semantics, stable identifiers and historical evidence. Candidate documentation passed link/anchor, UTF-8, formatting, command-inventory and consistency checks plus `git diff --check`; final status/archive reconciliation passes 647 local links/anchors across eight documents, formatting checks for all 28 changed/new files, exact 22-Complete/6-Not-started status consistency and renewed `git diff --check`. Inbound and relative archive links are repaired. M5-02 is Complete after Verification and documentation-gate acceptance; the plan moves to `completed/`. Twenty-two roadmap tasks are Complete, six remain Not started, and none is active. M5-03 is unselected.

Limits: comparison is internal and transient; no durable or visible comparison is implemented. Controlled scans and policy-only vectors are distinct from public-site qualification, accessibility/conformance claims or remediation causality. Checkout-filter proof uses an existing LF blob at the companion path and unchanged actual companion bytes, not a fresh checkout or new platform qualification. Reference release is not physical memory erasure. No actual public-page scan, provider/model call, acquisition or original retained-run mutation occurred.

## Revision Note

2026-09-17: created for owner-requested M5-02 planning after Complete M5-01. Recorded current verification, pair-definition gap, conservative native-evidence rules, separate guarded owners, transient integration and M5-03 boundary. No implementation or later task is selected.

2026-09-17: recorded independent critical planning PASS without findings and reconciled living state, progress and documentation validation. Future G and execution authority remain unchanged.

2026-09-17: recorded owner execution authorization, fresh prerequisites, two critical research dimensions, bounded controlled-identity follow-up, corrected DRAFT READY and fresh pre-draft PASS. Authored G's task-owned literals and comparison-only companion without changing historical inputs. Final artifact review and implementation remain pending.

2026-09-17: completed G and guarded A–C, full D verification and different integrated review; corrected companion checkout preservation through the sole D review loop, reconciled documentation and archived the Complete task. M5-03 remains unselected.
