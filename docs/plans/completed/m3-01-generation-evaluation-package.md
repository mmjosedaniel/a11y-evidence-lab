# Freeze the generation evaluation package

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M3-01](../../DEVELOPMENT_ROADMAP.md#m3-01--freeze-the-generation-evaluation-package), Complete. Verification and the documentation gate passed; this plan is archived.
- **Accepted artifacts:** [M301-FREEZE-01](#m301-freeze-01--exact-static-package-and-verification) records the safe manifest and nine exact ignored local files. [M301-FINAL-01](#m301-final-01--final-review-and-documentation-closure) accepts the different final review and resolved documentation follow-up.
- **Read this archive:** [M301-OWNER-02](#m301-owner-02--accepted-controlled-input-scope-and-resolved-contract) owns the resolved contract; [M301-COMMAND-02](#m301-command-02--resolved-preparation-validation-and-closure-callers) contains its read-only verification block; [Idempotence and Recovery](#idempotence-and-recovery) governs missing or changed inputs. Earlier planning, blocked-state and creation instructions are retained history, not current authorization.
- **Evidence:** Mandatory DRAFT READY; corrected fresh pre-draft PASS; different final PASS WITH FOLLOW-UPS with F1 resolved; full 96-negative static packet; fresh strict TypeScript and all 449 tests; source/input preservation and documentation checks.
- **Scope remaining:** None within M3-01. Runtime validation/context fit, Local configuration/capacity, Groq availability and the six generation executions remain unproved downstream. Actual M2-04 retrievals remain abstained.
- **Allowance and ownership:** Research/analyst/pre-draft allowances are consumed; one final documentation correction cycle was used and one remains unused. No active lease or implementation-worker write occurred. Primary authored the static evidence and authority/status documents.
- **Next:** M3-02 requires separate owner selection. No later task, model operation, commit, publish or push is authorized by this closure.

## Progress

- [x] (2026-09-09 18:35Z) Reviewed roadmap-wide state, controlling generation/evaluation authorities, M2-04 closure, current domain/retrieval boundaries, test inventory and maintained commands; accepted M301-PLAN-01.
- [x] (2026-09-09 18:35Z) Drafted the task-scoped plan and planning-only activation; no execution artifact or production behavior is claimed.
- [x] (2026-09-09 18:44Z) Accepted fresh independent planning PASS, corrected progress-table formatting, and passed documentation/preservation checks; see M301-PLAN-REVIEW-01.
- [x] (2026-09-09 18:53Z) Received exact-task execution authorization; recorded actual clean Git entry and read-only scope in M301-EXEC-01. Fresh checks subsequently passed below.
- [x] (2026-09-09 19:01Z) Passed fresh strict TypeScript and 119 focused tests; completed both initial research reports and pure reconstruction of retained G1–G3 abstentions. Full package eligibility remains unresolved; see M301-RESEARCH-01.
- [x] (2026-09-09 19:02Z) Accepted mandatory analyst OWNER DIRECTION for the exact eligibility exception; retained unaffected research proposals without declaring a partial freeze.
- [x] (2026-09-09 19:09Z) Accepted fresh R3 PASS for the stopped answer and documentation handoff, with no findings; package checkpoints and task completion remain pending.
- [x] (2026-09-09 19:46Z) Complete A: owner exception, both follow-ups, DRAFT READY and fresh pre-draft PASS after the single supported outline correction.
- [x] (2026-09-09 20:06:17 UTC) Complete B: exact static package, full 96-negative packet and different final R3 acceptance; documentation finding F1 resolved.
- [x] (2026-09-09 20:06:17 UTC) Accepted documentation closure, marked M3-01 Complete and archived this plan. M3-02 remains Not started and requires separate selection.

## Surprises & Discoveries

- M2-04 passed gold-hit verification but none of its three real retrieval sets was generation-eligible. G1 lacked interpretation; G2 and G3 lacked a criterion. Its [accepted observations](m2-04-retrieval-checkpoint.md#m204-b-accept-01--bounded-checkpoint-observations) explicitly preserve all three abstentions. A gold hit is not complete guidance support.
- The existing `EvidencePath` and canonical passage references already provide bounded reference vocabulary. Reuse them; M3-01 need not invent independently identified evidence, proposal, invocation or review records.
- `REQ-SEC-021` excludes raw prompts and provider payloads from tracked documentation/public evidence even when planning an evaluation. Package provenance must distinguish safe tracked definitions from local exact assembled inputs; synthetic origin is not a blanket exemption.
- The execution-entry checkout is clean but three M2-04 test/helper byte hashes differ from its preserved preparation receipt. LF normalization does not restore the first differing identity; no cause or byte-equivalent reuse is inferred. The M2-04 449-test result remains historical; M301-RESEARCH-01 first supplied narrower fresh evidence, and M301-SYNTHESIS-02 subsequently supplied the fresh complete 449-test regression.

## Decision Log

- Decision: Create a planning-only activation, preserving M2-04 completion and all later-task statuses. Rationale: the owner requested an ExecPlan, not execution. Date/Author: 2026-09-09 / primary.
- Decision: Put the three-case eligibility/provenance question before package drafting. Rationale: relabeling real incomplete retrieval as supported, adding gold passages to production ranking, or weakening required roles would contradict accepted evidence and exceed this task. No case-construction option is selected by this plan. Date/Author: 2026-09-09 / primary.
- Decision: Use a static, primary-authored freeze with existing validators and direct checks, not a new evaluation runner. Rationale: this task creates no application behavior; runtime output validation belongs to M3-02. Date/Author: 2026-09-09 / primary.
- Decision: Stop B at OWNER DIRECTION, preserving the completed research and actual abstentions. Rationale: REQ-GEN-003, REQ-RETR-004/006 and ADR-0014 require completed supported retrieval; existing evaluation authority does not accept an exception for independently assembled inputs. This is an authority decision, not a runtime proof to defer or a reason to consume another research allowance. Date/Author: 2026-09-09 / primary.

## Outcomes & Retrospective

M3-01 is Complete. The safe manifest freezes three explicitly controlled canonical-support cases, six common-provider bindings, the exact local inputs/instruction/schema/no-call definition, material controls, rubric and failure/recovery interpretation before model outputs. The owner-approved exception preserves production eligibility and the three actual retrieval abstentions. R3 synthesis, corrected pre-draft review, different final package review, the full static packet, strict/449-test regression and documentation closure pass. No application behavior, model execution or downstream proof is claimed.

The decisive corrections improved integrity-failure precedence and distinguished applicable Local configuration from unavailable Groq internals. The original authority stop, failed verification ordering and minor stale status are preserved as history. Static support and validated structure remain separate from actual retrieval and human assessment of groundedness.

## Purpose / Big Picture

Make later generation observations interpretable: one reviewer can identify exactly what each of the three controlled cases supplied, what both providers were asked to return, what support was acceptable, and how success and failure will be judged before seeing an answer. The output is a frozen evaluation definition, not an LLM result, implemented validator, eligible production run or model-capacity claim.

## Context and Orientation

The two baseline records below describe their original planning/execution entry states. The accepted owner exception and completed freeze supersede their unresolved eligibility and package-absence statements; use [Current state](#current-state) for the final disposition.

### M301-EXEC-01 — execution baseline and research contract

Execution enters at clean HEAD `c219da3c8fea86d5bed8c8a4b2abd9c3373c964e`, branch `codex/m3-01-freeze-generation-evaluation-package`, with an empty staged diff and no active lease file under `logs/agent-flow-leases/v2/`. The planning commit is intentionally present; the earlier planning HEAD is historical. The manifest and reserved local package directory are absent. Existing `temp`, `logs`, and `node_modules` are ordinary directories. No model or credential state was inspected. The operator selects Astra High for primary coordination; available custom-role metadata and the three role TOMLs agree on critical researcher Astra High and analyst/research reviewer Astra XHigh. No configuration changes are selected.

Entry SHA-256 identities: `package.json` = `01C0B39BD9141588543BD520DBB7A160EAC2D95414EEF54D36F394FE0FB7BA5C`; `package-lock.json` = `38CE94019310A5EFDBA9BB5565ABE6EB13F678FA18FCD9D87B1C5DC21B30748D`; installed `node_modules/.package-lock.json` = `D40F65DEBC6312706A3BB92979285D8234AD781EBAF92BB0E4B44E68DD37F7BA`; corpus manifest = `87D8867ED1138BFB38E6C44EED67A79B0A43BDAF2532A056AFAFAED828202DA9`; passages = `29EA0033E825A0FA20430AA42E9718B87A87AA59EDADDA0A3876A0A7BE871850`; M2-01 gold = `A7D38CACF98BF263CDD53012350708BE987699DF21B4C0F1BB7F32BC1892E29B`; RD-003 manifest = `13C9722BE9EA2E3B0AAF020EA91F429A701180A83814FE7AB21BAF2DDAD57459`. These checkout-byte hashes supplement, never replace, canonical normalized corpus identities. Protected tracked source, tests, fixtures, corpus, evaluation inputs and executable configuration remain the entry tree.

The one non-ranking discovery pass identifies two provenance candidates: C1, explicitly labeled controlled supported packages composed from unchanged canonical inputs with no production-path insertion; C2, retain actual abstentions and stop the final freeze for the smallest owner-controlled eligibility decision. Current authority has not accepted C1. Storage candidates are restricted by REQ-SEC-021 to the planned safe tracked manifest plus ignored exact local definitions; publishing raw inputs is disqualified. Primary sources for shared-provider expressibility are official Groq Structured Outputs and Ollama structured-output/API documentation. No other provider or contract framework is a candidate.

Freeze the common comparison matrix before research: authority fit; three-case eligibility; exact canonical references and required roles within `k = 3`; selected-only data minimization; shared-provider schema expressibility; reproducibility and failure/recovery meaning; smallest maintenance burden. A hard-gate failure disqualifies the option, with no ranking of forbidden alternatives. Research identity `M301-R3-01` projects A's six decisions and I1–I7. Dimension 1 owns provenance/reference eligibility; dimension 2 owns output/instruction/privacy/storage and provider expressibility. At most two read-only critical-researcher reports of 1,000 useful words plus necessary contract tables, each with one targeted follow-up; one mandatory analyst synthesis and one correction; one fresh pre-draft reviewer with one supported correction; a different fresh final reviewer with the existing two-cycle ceiling. No output inspection, repository writes by research roles, additional agents, model execution, corpus/ranking/sufficiency changes, or owner-controlled scope changes. Repository evidence is reusable only while the relevant entry identities and claim remain unchanged. Primary retains repository fact gathering and sole authoritative writes. The next barrier is combined-evidence synthesis; no artifact drafting before DRAFT READY and fresh pre-draft PASS.

### M301-PLAN-01 — current project state

Planning began at clean HEAD `10edd950d82e74e2fdfd31978dc16c7315ba8b25`, with 220 tracked files. Twelve roadmap tasks are Complete: RD-001–RD-003, M1-01–M1-05 and M2-01–M2-04. M3-01 is the only task selected here; fifteen subsequent tasks remain Not started. This commit is historical entry evidence, never a hard-coded executable HEAD condition.

The implemented application has a loopback service, exact-three-rule Playwright/axe scan, validated single-file aggregate, React target/results UI, closed 8-source/16-passage corpus, local exact-vector retrieval, canonical citation inspection and deterministic sufficiency/abstention. M2-04's [final review](m2-04-retrieval-checkpoint.md#m204-final-01--integrated-review-and-task-closure) passed the 449-test regression, strict/build, actual retrieval and bounded visual checkpoint. The embedding capacity gate passed in M2-02. Neither proves Qwen capacity or generation quality. Full-detail 200% visual verification remains with M6-03; no new UI is planned here.

Fresh planning checks on the unchanged implementation passed pinned Node `v24.20.0`, independent strict TypeScript, and 119 tests across `run-contract`, `scan-normalization`, `retrieval-contract` and `finding-sufficiency`. The full 449-test/build/real-browser results above are retained M2-04 evidence, not fresh planning executions. No generation adapter, proposal validator, review or comparison behavior exists in the current source. The bug index contains no recorded bugs. No model/runtime state was probed or changed.

Read [project requirements](../../PROJECT_REQUIREMENTS.md) for status semantics, [roadmap](../../DEVELOPMENT_ROADMAP.md) for ownership, and these exact authorities for execution:

- [Evaluation and acceptance](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md): `REQ-EVAL-001`, `004`–`008`, freeze boundary, controlled profiles and compact rubric; `002`, `003`, `009` constrain distinct observations, gold identity and reporting. [OD-009](../../requirements/DELIVERY_READINESS_AND_OPEN_DECISIONS.md) fixes the non-promotable six-execution manifest.
- [Evidence and review](../../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#generated-explanations-and-remediation-proposals): `REQ-GEN-001`–`006`, `008`–`010`, supported profile facts and `REQ-REV-009`; [information lifecycle](../../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md) owns nested references, invocation existence and proposal-only review.
- [Provider execution](../../requirements/generation-provider-and-model-lifecycle/GENERATION_PROVIDER_EXECUTION.md): `REQ-LLM-005`, `008`, `009`, `011`, `015`, `019`; [privacy](../../requirements/quality-security-and-operations/PRIVACY_AND_SECURITY.md): `REQ-SEC-002`–`007`, `013`–`016`, `021`. Derived [SPEC-003/SPEC-004](../../specs/SPEC.feature) and [HS-008/HS-009](../../specs/HARD_SPEC.feature) illustrate, but do not add, these contracts.
- [ADR-0003](../../architecture/decisions/ADR-0003-initial-local-generation-evaluation-preset.md) selects `qwen3.5:4b` only for capacity screening; [ADR-0014](../../architecture/decisions/ADR-0014-groq-as-mvp-external-generation-provider.md) fixes Groq `openai/gpt-oss-20b` with strict Structured Outputs. [Local feasibility](../../LOCAL_MVP_FEASIBILITY.md) and ADR-0004 govern later capacity, not a new M3-01 model experiment.

Reuse [RD-003 inputs](../../../evaluation/rd003-scan-v1.json), [M2-01 gold](../../../evaluation/m201-corpus-v1.json), [manifest](../../../corpus/wcag22-mvp-v1/manifest.json) and [passages](../../../corpus/wcag22-mvp-v1/passages.json), without changing them. Source responsibilities are already separate: [run types](../../../src/server/domain/run-contract/run-types.ts), [evidence/reference types](../../../src/server/domain/finding-analysis-types.ts), [evidence assessment](../../../src/server/domain/finding-sufficiency.ts), [support policy](../../../src/server/retrieval/support-policy.ts), and [citation resolution](../../../src/server/retrieval/citation-resolution.ts). Inspect their actual exports before using them. M2-04's [fixture helper](../../../tests/helpers/m204-checkpoint-fixture.ts) is synthetic seed evidence, not a new native scan or supported result for all profiles.

## Scope and Non-Goals

Freeze exactly three one-Finding case definitions, shared by Local and Groq, their eligible minimized packages, canonical gold/support references, one application-owned output contract, one instruction variant and provenance, material generation controls, one compact rubric, prohibited claims and failure interpretation. Keep scanner, retrieval, structural validity, semantic groundedness and remediation usefulness as separate observations. Retain one shared deterministic incomplete-guidance/no-call case rather than duplicating it per provider.

No source/test/dependency/configuration changes, model/credential setup, downloads, provider probes or calls, embedding requests, public scans, browser proof, fabricated model outputs, or mutations of retained runs are planned. M3-02 implements shared generation and validation; M3-03 owns Local configuration/capacity, M3-04 Groq setup/current availability, M3-05 integration, and M6-02 the six fixed executions. Their first inspected product-model outputs must respect this freeze. Do not spend their execution allowances here.

Do not add a prompt-management system, schema/code-generation framework, new provider, registry, retry/fallback, tuning, role-aware reranking, extra corpus/fixtures, scoring platform, release-grade provenance system or generalized safety classifier. Preserve Deferred prompt-injection hardening and all other Proposed/Deferred boundaries.

## Plan of Work

### M301-RESEARCH-01 — initial evidence barrier

Primary accepted the scope and underlying source basis of `M301-R3-01/D1` (`eligibility_research`, critical researcher) and `M301-R3-01/D2` (`contract_research`, critical researcher), both read-only and within their initial-report budgets. D1 returned **BLOCKED** at the eligibility authority boundary; D2 returned **RESEARCH COMPLETE** for reusable A2–A6 proposals only. Neither result accepts the contract or authorizes B. Both reports were supplied to the mandatory `decision_analyst` (`decision_synthesis`). No researcher follow-up has been spent.

**D1 evidence and comparison.** D1-E01 is REQ-EVAL-001/003/007/008 and OD-009: fixed shared cases, pre-output freeze and separate observations. D1-E02 is REQ-GEN-003 plus REQ-RETR-004/006: eligibility requires complete evidence and completed supported retrieval, with actual query embedding and exact top-three ranking. D1-E03 is [M204-B-ACCEPT-01](m2-04-retrieval-checkpoint.md#m204-b-accept-01--bounded-checkpoint-observations): all actual cases abstained. D1-E04 is the [temporary-substitute rule](../../DEVELOPMENT_ROADMAP.md#temporary-substitutes-and-real-integration-rule); D1-E05 is the existing support/evidence validators, which validate supplied data but do not prove its ranking provenance; D1-E06 is the M2-01 gold's expected-subset provenance and M2-04's expressly controlled S case. C1 could establish static canonical role completeness but has no explicit exception allowing it to replace actual completed retrieval in the fixed six executions. Honest labeling alone does not supply that authority. C2 preserves existing evidence and rules while stopping the final freeze. Both candidates retain the same minimization, shared-contract and recovery criteria; those independent properties cannot cure C1's authority gap. No exact C1 triple or fabricated scores were constructed after that finding.

**D2 evidence and reusable proposals.** D2-E1–E5 map respectively to generation/review requirements, provider requirements and ADR-0003/0014, privacy/lifecycle requirements, evaluation requirements, and existing evidence/citation types and validators. Current official [Groq Structured Outputs](https://console.groq.com/docs/structured-outputs) (D2-E6) documents required properties and closed objects for strict schema output; [Ollama Structured Outputs](https://docs.ollama.com/capabilities/structured-outputs) and [chat API](https://docs.ollama.com/api/chat) (D2-E7) document schema-valued `format`. D2-E8 comprises the [Groq API reference](https://console.groq.com/docs/api-reference) and [Ollama Modelfile reference](https://docs.ollama.com/modelfile) for material controls. Access date: 2026-09-09. Basic structural expressibility is supported; exact schema acceptance and operational availability are unproved. No provider call occurred.

The following are **unaccepted research fragments**, retained for resumption, not executable instructions, final literals, a partial freeze, or authority for downstream implementation:

| A item | Reusable finding / candidate | Remaining M3-01 semantics |
| --- | --- | --- |
| 2 Input/reference | Explicit selected `findingId`, rule/native result, checks and profile evidence projection; canonical required passages/notices. Evidence references use existing `EvidencePath` and selected available-reference membership; citations must belong to the exact supplied canonical profile set. No target/locator, raw records, scores, vectors, siblings or history. | Bind authorized provenance and exact per-case fields/bytes; verify the complete minimized projection. |
| 3 Output/instruction | Candidate closed all-required proposal fields: `type`, `findingId`, `findingSummary`, `evidenceReferences`, `userImpact`, `remediation`, `passageIds`, `evidenceSufficiency`, `confidence`, `uncertainty`, `assumptions`, `blockingManualJudgment`, `postChangeVerificationReminder`. Keep existing application abstention separate. Consider inline evidence/passage markers with exact set correspondence, categorical confidence and required uncertainty. Candidate bounds: prose 1,000 UTF-16 units, remediation 2,000, 0–5 assumptions of 500 each. | Exact reference/marker grammar, complete field definitions/bounds, instruction bytes, all sampling/reasoning/default semantics and material controls require resolution before DRAFT READY. Proposed temperature 0/output ceiling 4096 and non-streaming are not selected. |
| 4 Interpretation | Mechanical structure/reference failures remain distinct from reviewer semantic edit/rejection. Image judgment concerns purpose/context/treatment; label judgment concerns clarity/association/instructions; contrast judgment concerns classification/exceptions/background/design and omitted states. Later verification reminder stays non-blocking. | Exact mechanical prohibited-claim rule and vectors remain unfrozen. A lexical filter alone cannot cover certification/legal/whole-page/site/complete-SC or automation-only fix claims; preserve the semantic rubric and its limits. |
| 5 Storage | Safe tracked manifest plus ignored exact local definitions, explicit byte identities, ordinary contained paths, no overwrite of prior evidence, missing/mismatch blocks use, identical reconstruction or new version. | All exact filenames/encoding/creation/reconstruction/negative-check/receipt slots remain unfilled and unexecuted until the authority and contract barrier passes. No local package exists. |
| 6 Decide now/prove later | M3-02 proves runtime validation/minimization/no-truncation fit; M3-03 binds acquired Qwen/runtime/context/offload and proves capacity; M3-04 binds Groq and proves operational availability. | M3-01 eligibility and output/instruction/control/interpretation semantics stay here, not deferred as downstream proof. Later exact configurations must bind before output inspection; changes preserve prior evidence. |

**Fresh verification and retention limits.** The prepared pinned-Node strict command and four-file focused test command in Concrete Steps passed independently, with 119 passes and no failures. No tests or implementation were edited. The existing M2-04 preparation receipt still hashes to `79E9D69F75CA10842CE02B43342F6FCED8E206E81F2AE28A55713322686202DC`. Its 67 source, 3 build, 2 corpus and 2 package identities match the current files. Three of 24 test/helper hashes differ: `tests/retrieval-service.test.ts` is now `AADB964D9438C0D0CAD9D460604FECF67A29BB418C233059F666DD729C561569`; `tests/helpers/m204-checkpoint-fixture.ts` is `4106376E89AD4BFBE83A0B8A30D1CC6FAF5B32C0F5AC996956EBD8E148AFB617`; `tests/helpers/m204-retrieval-checkpoint.ts` is `C21C9E6B7B5FBDDD2DD6FC5B53EED0511F1667E20B3CBCC208DFECB03FD4C098`. They belong to the intentional clean entry, not this task's edits. Exact full-suite evidence identity is therefore not established; retain the 449-test result as historical M2-04 completion evidence, not current M3-01 suite acceptance. This partial static research handoff does not close M3-01 or need a browser-heavy suite. Any later complete-task regression must satisfy the applicable full-suite rule.

Primary's read-only reconstruction freshly validated each retained aggregate, projected its native selected Finding into the exact existing seven-field contract, reassessed evidence, resolved canonical citations and confirmed incomplete support, `abstained`, and no provider invocation. G1 lacks interpretation; G2/G3 lack criterion. Canonical run SHA-256 values still match the archive: G1 `9292D3C1EC28466FB5FEB6CAEBBC23F39C570D6EFC4C2D3C2767351BF57F4C8E`; G2 `0471BFE4BF569CF3BF16E746AE4A0F394B2F0280A256D9777F8C8C489971077D`; G3 `AB4950B5A2B79260189249A38B524C35FDC83256F803D27EFAFFBA781F5F4147`. This is reconstruction of retained results, not fresh retrieval. The initial broad hash assertion failed; a normalization hypothesis also failed and was rejected. An initial reconstruction caller incorrectly supplied a workflow-extended Finding to the native-only validator and failed before reconstruction; the corrected explicit native projection passed. None changed a file, evidence identity, product behavior, or research budget. A shell inspection's empty-pipe syntax error was corrected without effects.

The smallest proof is direct inspection of a compact manifest and exact local inputs, canonical reconstruction with existing pure functions, and a small set of in-memory adverse variants. No reusable runner, browser harness or live model is necessary. Contract tables may describe future validator behavior without implementing that validator. **TDD: Not applicable**: these are authoritative evaluation definitions and decision artifacts, not executable production behavior. Replacement evidence is structural, semantic, preservation and negative checking below.

### M301-SYNTHESIS-01 — owner-controlled eligibility stop

The mandatory `decision_analyst` (`decision_synthesis`, `M301-R3-01/ANALYST`) returned **OWNER DIRECTION** after auditing both reports, current authorities and official provider-schema documentation. Primary accepts the bounded disposition: C1 is not authorized by existing authority; C2 preserves the record but is not a completing package alternative. [ADR-0014's Decision](../../architecture/decisions/ADR-0014-groq-as-mvp-external-generation-provider.md#decision) independently reinforces REQ-GEN-003 and REQ-RETR-004/006 by requiring both adapters to receive input only after complete evidence and completed supported retrieval. The fixed synthetic evaluation context does not waive that requirement. No further researcher is needed to repeat that gap.

**Concrete owner clarification requested:** Authorize a documented evaluation-only exception allowing the six fixed generation cases to use independently assembled canonical, support-complete inputs, with those executions demonstrating generation/provider behavior while actual retrieval abstentions and production eligibility rules remain unchanged. This would permit evaluating generation against explicitly controlled guidance, not claiming that actual ranking returned it or that generation completed the real end-to-end retrieval path. It would not authorize model execution, a production substitute, ranking/corpus changes, fabricated scores, or later roadmap tasks.

If approved, reconcile the exact evaluation exception in the owning evaluation/decision authorities, with explicit cross-references where REQ-GEN-003/ADR-0014 eligibility would otherwise conflict, before accepting C1. This request is not approval of that amendment and does not pre-approve a new mechanism. A route that instead changes real retrieval or inserts a production-path substitute needs its separate owner-controlled authority. If the owner declines the exception, keep the final package blocked pending separately authorized work that establishes eligible actual retrieval.

**Unchanged execution route after direction:** Refresh the actual baseline, scope the authority amendment, and use remaining research/analyst allowance only for materially new evidence or completion of unresolved A semantics. Exact per-case inputs, reference grammar, schema, instruction, controls/defaults, prohibited-claim policy and storage/validation/recovery commands remain M3-01 work before DRAFT READY. No proposal in M301-RESEARCH-01 is delegated to M3-02 to decide. DRAFT READY and fresh pre-draft PASS must precede B; a different fresh final reviewer must inspect the complete package and evidence after B. A review of this stopped answer is not either package checkpoint. Retain existing evidence and all budgets.

The complete current invariant packet is reconciled for this partial disposition; it is deliberately not a package PASS:

| Invariant | Current actual evidence / disposition | Remaining acceptance |
| --- | --- | --- |
| I1 Completeness | **Blocked:** no authorized three eligible cases, shared final contract or six valid bindings. Analyst confirms A1 owner decision and A2–A6 unresolved semantics. | Owner direction; analyst DRAFT READY; both future package reviews; primary. |
| I2 Honesty/authority | **Preserved:** actual G1–G3 freshly reconstructed as abstained; no C1 selection, ranking/corpus/sufficiency change, false runtime/capacity claim or output inspection. | Fresh blocked-answer review now; future package reviews and primary. |
| I3 Identity/support | **Partial:** unchanged production validators and 119 focused tests; all three retained actual records validate and resolve exact canonical passages, with incomplete support. No controlled eligible package or its adverse vectors exist. | Authorized case construction and complete positive/negative packet; package reviewers. |
| I4 Privacy/ownership | **Preserved boundary, package unproved:** only safe documentation changed; no raw prompt/payload, credential, local package or worker write. D2 identifies closed projection constraints. | Exact projection/storage checks; package reviewers and primary. |
| I5 Output/judgment | **Pending:** content requirements and shared-schema expressibility researched; exact structure, markers, bounds, controls, judgment/reminder and mechanical-policy definitions are not frozen. | Analyst and both package reviews; direct structural/semantic vectors. |
| I6 Failure/no-call | **Partial:** current actual abstention/no-invocation reconstruction and existing pure tests pass; required distinctions retained. Future generation/context-fit/prerequisite/validation negative cases unexecuted. | Complete static policy/negative packet now owned by M3-01; runtime proof downstream. |
| I7 Freeze/recovery | **Pending:** no manifest/local files, freeze hashes or receipt; no prior evidence overwritten. All generation runtime/adapter/capacity proofs remain explicitly unproved. Full 449-test reuse is not claimed. | Exact command/byte/recovery slots, package checks/reviews and task closure. |

Documentation impact at this checkpoint is limited to the living plan, roadmap and plan/progress navigation/status. No requirements, ADRs, specifications, completed-plan evidence, production source, tests, dependencies, fixtures or corpus need mutation to record the blocker. The task remains incomplete, and its complete authoritative suite and final freeze verification are not represented as passed.

**M301-STOP-CHECKS-01:** Primary's direct checks passed strict UTF-8/no-BOM/final-newline validation on the five changed documents, all 196 local Markdown links/anchors, syntax parsing of all three PowerShell blocks, exact roadmap counts (12 Complete, 1 Blocked, 15 Not started), package absence and `git diff --check`. The 222 tracked-file inventory has exactly the five intended documentation edits; all 217 others retain their recorded entry bytes. HEAD/branch remain M301-EXEC-01, the staged diff is empty and no lease is active. The prepared fresh runtime is Node `v24.20.0` (executable SHA-256 `5C976096E04E5C2C1F091938926234CC9FBEBFE9787DDD149351B3B0ECC707B5`); compiler launcher SHA-256 is `2219F428A7E55AAF1F7AD85B9B0F0CF5078AEB76CCC9A7C6036C92D48F492FFD`; installed dependency lock remains the entry identity. These are partial-handoff documentation and preservation checks, not task completion. Primary inspected the actual diff for bounded documentation ownership and retained all original evidence. No cleanup, commit, publication or push occurred.

**M301-STOP-REVIEW-01:** A fresh `critical_research_reviewer` (`blocked_disposition_review`, `M301-R3-01/STOP-REVIEW`) returned **PASS for the stopped answer and documentation handoff**, with no Blocker, Major or Minor findings. Reviewed plan SHA-256: `506A618D1213CDA213A6DA3482B967421F5AB5902E59D792023E1F86C006EC27`. It reviewed the complete plan, five-file diff, proposed owner question and I1–I7; independently reproduced the recorded pure reconstruction and 72 artifact/two package matches, confirmed all three historical test/helper mismatches, package absence, current runtime/compiler/dependency-lock identities, empty index/no active lease and `git diff --check`, and corroborated the limited official provider-schema claims. It reused unchanged fresh strict/119-test and documentation evidence. Primary accepts that verdict and its underlying evidence. This acceptance entry and current-state reconciliation add no material semantics and require no new package review or renewed budget. M3-01 remains Blocked; neither package checkpoint nor task Verification has passed. Documentation impact: Updated this plan, roadmap, plan index, task progress record and progress index; requirements, ADRs, application behavior and earlier evidence are unchanged.

### M301-OWNER-02 — accepted controlled-input scope and resolved contract

On 2026-09-09 the owner approved the narrow evaluation-only exception requested in M301-SYNTHESIS-01. The [evaluation authority](../../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md#controlled-generation-input-exception) owns this Accepted exception; the generation requirement module and ADR-0014 link it. It applies only to the fixed six generation executions, preserves actual retrieval abstentions and production eligibility, and grants no model call, production substitute or later integration/capacity exemption. M3-01 resumes In progress under the original execution allowance.

Both researchers completed their sole targeted follow-up. D1-E07 is this owner authority; D1-E08/E09 independently reproduced canonical support and native completeness using the existing pure validators; D1-E10 rejected seven definition/evidence mutations per case and altered corpus bytes. D2-E9 independently exercised the bounded lexical policy and a known semantic blind spot. D2-E10 checked [Ollama thinking](https://docs.ollama.com/capabilities/thinking), its [Qwen3.5 renderer](https://github.com/ollama/ollama/blob/main/model/renderers/qwen35.go) and [parser tests](https://github.com/ollama/ollama/blob/main/model/parsers/qwen3_test.go), accessed 2026-09-09; these support selecting think=false, not a claim about an installed runtime. D2-E11's closed upstream issue is historical caution, not current reproduction. The [Groq API reference](https://console.groq.com/docs/api-reference), [strict-output documentation](https://console.groq.com/docs/structured-outputs) and [Ollama chat reference](https://docs.ollama.com/api/chat), accessed 2026-09-09, support the request-shape selections below. Actual runtime behavior remains unproved.

**A1/A2 — exact cases and minimization.** Choose C1 under the new authority. The three ordered triples are criterion, interpretation, remediation; each ID belongs to its unchanged M2-01 gold. Use G1/G2/G3's retained selected Finding, whose native projection matches its original seed. These are synthetic seeded native evidence, not new scans. Every actual workflow remains abstained. The scoped identity is (case, source run, finding-0), never the reused finding ID alone.

| Case / failing fixture / target | Retained source | Exact ordered passage IDs |
| --- | --- | --- |
| informative-image-alt / informative-image-alt-v1-failing / rd3-image | G1 | wcag22-sc111; understanding111-intent; h37-text-alternative |
| form-input-label / form-input-label-v1-failing / rd3-email | G2 | wcag22-sc412; understanding412-intent; h44-explicit-label |
| text-contrast / text-contrast-v1-failing / rd3-text | G3 | wcag22-sc143; understanding143-threshold-measurement; g18-contrast-remediation |

A native validation input has exactly findingId, ruleId, nativeResult, state=unprocessed, checks, locator, evidence, projected without mutating the retained run. The model-visible root has exactly finding and guidance. Finding has findingId, ruleId, nativeResult and facts: one ordered {reference,value} entry per assessFindingEvidence.availableReferences, resolving the existing EvidencePath against that selected native Finding and unwrapping value objects. No unavailable optional fact is invented. Guidance has corpusVersion, passages and notices. Passages are the full three authenticated canonical objects in the fixed order, unchanged. Notices contain only selected-source title/type/url/status/version/copyright/attribution plus the existing full SOURCE_NOTICES document and software notices; no unrelated passage is included. Fixture/target identity, locator, source path/hash, original abstained state, native projection and scan context stay in separate ignored provenance files, never in model input. No score, ranking, embedding identity, vector, sibling, target URL/origin, page text, image source, input value, arbitrary attribute, credential, history or duplicate raw outbound run payload is permitted.

**A3 — one output and instruction contract.** Version m301-proposal-v1 is a closed object with every property required: type (proposal), findingId (string), findingSummary/userImpact/remediation (supported text objects), evidenceSufficiency ({findingEvidence:complete,guidance:supported}), confidence (high/medium/low), uncertainty (string), assumptions (string array), blockingManualJudgment (string), postChangeVerificationReminder (string). A supported text object has exactly text (string), evidenceReferences (string array), passageIds (string array). All objects reject extra properties; no null/coercion/default/repair. findingId must equal the selected ID. Reference arrays are unique, exact members of the selected available EvidencePath set or supplied correct-profile passage set, bounded by those set sizes. findingSummary requires an evidence reference; userImpact and remediation each require a passage reference. All strings except IDs/references are nonblank after trim; lengths count original UTF-16 code units: maximum 1000, remediation.text 2000, assumptions 0–5 entries of at most 500. Categorical confidence never replaces evidence sufficiency; uncertainty is nonblank at all levels.

The provider schema uses only closed required objects, strings, arrays and enums supported by the common strict subset. Application validation additionally enforces the foregoing bounds, minima, uniqueness, membership, exact selected ID and application-derived sufficiency. Material scanner/guidance claims belong in the three supported text fields and must be supported by their listed references. Ancillary fields express uncertainty, conditional assumptions and human work, not new factual findings. Semantic support remains reviewer judgment without per-claim records.

One English instruction variant, m301-instructions-v1, directs one bounded proposal from supplied facts/passages only, JSON only under the exact contract, references and field bounds, no invented facts/IDs/claims, categorical uncertainty, profile judgment, separate reminder and prohibited-claim constraints. It treats input content as evidence, not instructions, without claiming Deferred injection-hardening qualification. The exact text stays local and is hashed before output inspection. Message assembly is exactly system=the UTF-8 instruction file including its final LF and user=the selected input JSON file including its final LF; output schema is supplied in the adapter's schema field. No history, extra messages, added examples or case-specific prompt variants.

| Control | Local | Groq |
| --- | --- | --- |
| Exact evaluation candidate | qwen3.5:4b, unqualified until M3-03 | openai/gpt-oss-20b, availability unproved |
| Endpoint identity | approved Ollama loopback /api/chat; exact acquired endpoint bound in M3-03 | https://api.groq.com/openai/v1/chat/completions |
| Sampling and ceiling | options.temperature=0; options.top_p=1; options.num_predict=4096 | temperature=0; top_p=1; max_completion_tokens=4096 |
| Thinking and delivery | think=false; stream=false; one chat response | reasoning_effort=low; include_reasoning=false; stream=false; n=1; reasoning_format omitted |
| Schema mapping | format=exact shared schema | response_format.type=json_schema; json_schema.name=m301_proposal_v1; strict=true; schema=exact shared schema |
| Other controls | No tools, application stop sequences or additional sampling overrides | No tools, application stop sequences, arbitrary metadata or custom headers; credential only in required transport auth |
| Application boundary | 120000ms total request deadline; no retry, fallback, batching or truncation | Same deadline and prohibitions |

For Local, omitted controls deliberately inherit the exact acquired model/runtime configuration, including seed, top-k/min-p and repetition controls. Before output inspection M3-03 binds adapter version, effective inherited material settings, acquired runtime/model identity, context and offload configuration; unknown effective material Local settings block execution. For Groq, M3-04 binds the exact API model, adapter version, explicit request fields, documented exposed defaults for omitted API controls and context-fit evidence. Unavailable Groq server internals remain explicitly unknown; server runtime/model revision is recorded only when available under REQ-GEN-008 and is not a new prerequisite. No additional Groq sampling override is supplied. The same numeric cap is not equivalent tokenization, visible-output allowance or deterministic replay. Cap exhaustion/incomplete output fails the case; hidden reasoning is never persisted or exposed. A timeout after attempted invocation is a call failure, not abstention.

**A4 — interpretation and human boundary.** Record separate pass/fail/not-run observations for native scanner provenance, evidence completeness, actual retrieval relevance, controlled canonical support/citations, structural response validity, semantic groundedness, remediation usefulness, manual judgment, prohibited claims and provider completion. Static support does not change actual retrieval observations. No aggregate/ranking/percentage. Each happy case requires a valid structure, correctly supported material claims, useful actionable bounded remediation and preserved uncertainty/judgment/reminder; an invalid/failed response fails the fixed case. A later reviewer edit does not retroactively make the original answer successful.

| Rubric mapping | Acceptable interpretation |
| --- | --- |
| REQ-GEN-001/002/008; EVAL-004/008 | Exact selected Finding and nested evidence/canonical citation membership; field support must actually substantiate each material claim |
| REQ-GEN-003/004; EVAL-002 | Complete evidence and controlled support before call; separate confidence and uncertainty without pretending this proves production eligibility |
| REQ-GEN-001/004; REV-009 | Image: establish purpose/informative versus decorative and suitable equivalent wording. Label: establish understandable, appropriate visible label wording and actual association. Contrast: establish meaningful text, applicable threshold/exceptions and visual context. Minimized facts alone do not decide these |
| REQ-GEN-001/004; REV-009 | Separate post-change rescan and relevant human verification reminder is visible and non-blocking; it is not a completed action |
| REQ-GEN-005/006/009/010; EVAL-007 | No certification, legal compliance, whole-page/site accessibility, complete SC conformance/non-conformance or automation-only fix claim; one reviewer confirms support and resolves unsupported claims by edit or rejection |

Mechanical policy m301-prohibited-v1 applies to each prose leaf independently after NFKC, lowercase, whitespace collapse to one space and trim, preserving original content. The exact five JavaScript Unicode regex sources from D2-E9 will be frozen in the manifest: certif*/conform*/complian* words; whole/entire page/site/website accessible declarations; page/site/website is/are accessible declarations; finding/issue/violation is/was/has been fixed/resolved/remediated; automated/automatic/scanner/axe evidence/results/changes/checks/scans (alone) prove/confirm a/the fix/resolution. This deliberately overrejects negation and direct completed-fix language; it misses paraphrases, split assertions, punctuation variants, homoglyphs and other languages. Lexical success never proves semantic safety. Exact regex literals are bound in the verification command below before artifact creation.

Missing required evidence or valid incomplete/missing/conflicting guidance produces pre-call application-authored abstention. Canonical/hash/unknown/wrong-profile/duplicate/excess-passage failures are integrity failures, never abstention. Missing selected-mode prerequisites or inability to fit the unchanged full input/schema plus output budget fails before invocation. Authentication, quota/rate-limit, timeout/network/provider and invalid-response failures after an attempted call retain their bounded failure class and non-secret invocation. A valid-shaped unsupported answer goes to human edit/reject, never post-call abstention. A new ordinary run is required for any later attempt; no in-place retry or second execution hidden in the six-case manifest.

The one shared no-call vector uses the same complete image native Finding with only wcag22-sc111 and understanding111-intent. Expected support=incomplete, missingRoles=[remediation], reason=incomplete-guidance, providerCalled=false, no ProviderInvocation, proposal or review decision. The local expected explanation names incomplete guidance and missing remediation, and supplies manual-investigation guidance. No fake RetrievalResult is serialized.

**A5 — exact storage and recovery.** Primary creates only evaluation/m301-generation-v1.json and these ignored local files under temp/m301-generation-freeze-v1/: informative-image-alt.input.json, informative-image-alt.provenance.json, form-input-label.input.json, form-input-label.provenance.json, text-contrast.input.json, text-contrast.provenance.json, instructions.txt, output-schema.json, no-call.json. All are new exclusive paths, ordinary in-repository directories/files, no links, UTF-8 without BOM, LF only and exactly one final LF. JSON uses JSON.stringify(value,null,2) plus LF; insertion order is frozen by the exact bytes, not a promise of canonical JSON. No file is overwritten after freeze. Creation uses primary apply_patch only; no helper or executable repository file is added.

The safe manifest records version/freeze UTC, authority/provenance, source hashes, fixture/target/rule/criterion/passages and input/provenance hash references, six unique (case,mode) bindings to the same input/schema/instruction bytes, the closed field and validation policy, exact material controls, rubric, prohibited regexes, failure policy, one no-call reference and pending downstream proofs. It contains no raw prompt, model input, native full record or credentials. The plan records its hash as the freeze receipt without a self-hash cycle. Keep all exact local files for downstream work; no cleanup is selected. Recovery requires surviving original bytes or a byte-identical reconstruction from retained synthetic native sources, canonical snapshot and exact original instruction/schema, verified against every frozen hash. A manifest alone cannot recover lost instruction text: if exact recovery is unavailable, stop use and explicitly freeze a new version before output inspection while preserving prior evidence. Material configuration/input/contract changes create new evidence, never silently update this version.

**A6 — implementation boundary.** These are fully selected static semantics. M3-02 must prove actual validation, minimization, no-call/no-retry and context-fit behavior; M3-03 must bind actual Qwen digest/runtime/quantization/template/parser/effective settings/context/offload and pass capacity; M3-04 must bind actual Groq adapter/request/current availability. None is proved here. Their separate actual-integration obligations remain under the ordinary production gate. Missing downstream evidence blocks those executions, not this static definition. No first output may be used to tune this freeze.


### M301-SYNTHESIS-02 — accepted drafting readiness

The mandatory decision analyst's sole bounded correction returned **DRAFT READY** on 2026-09-09 for M301-OWNER-02's complete A1–A6 semantics. It independently reproduced all three retained source hashes, native/seed equality, complete evidence, canonical gold/role mappings and unchanged actual abstentions; B artifacts remained absent. Primary accepts the synthesis and underlying evidence. Its two reported verification limitations were already corrected during that active review: M301-COMMAND-02 now asserts the exact four source path/hash objects and exact ordered nine local filenames. This strengthens existing requirements and consumes no new research allowance. All researcher and analyst allowances are exhausted. Fresh pre-draft PASS still precedes B, followed by a different final reviewer.

Fresh independent strict TypeScript and all 449 tests in the maintained thirteen-suite sequence passed with zero failures, cancellations, skips or todo cases. Counts by suite: run-contract 66; run-repository 68; local-service 69; scan-normalization 20; retrieval-contract 12; embedding-retrieval 28; retrieval-service 18; finding-sufficiency 21; finding-guidance-api 5; scan-page 88; walking-skeleton 15; target-results-ui 30; finding-guidance-ui 9. The existing client build matches retained hashes, Chromium 1234 is present, and all scanner/UI/integration scratch directories are empty afterward. A denied CIM process read stopped only that prerequisite attempt; direct Get-Process executable-path inspection subsequently excluded unknown/project-runtime Node processes before regression, without escalation or termination. Unrelated CUA runtime processes were preserved. No generation/embedding, download, new public scan or application edit occurred.

The primary's fresh protected-file check found all 215 entry tracked files outside the seven currently changed documentation paths byte-identical, including all source/tests/configuration, corpus, fixtures and gold. Progress-index content has returned to its intentional entry In progress state. Entry comparisons include intentional planning documentation, not a clean-tree assumption. The manifest and nine local files remain absent; no freeze evidence is claimed by this checkpoint.


### M301-PREDRAFT-01 — supported outline correction

The fresh critical research reviewer returned REVISE on plan hash 1835176563383020A013C7D5C7147266692578B5BA3ABB0098CBF76861DEBDFE, with two Major findings and no Blocker/Minor findings. Its reproduced combined-defect counterexample invalidated the command's blanket integrity-before-abstention claim: unavailable checks previously returned incomplete-evidence before rejecting an unknown passage. It also identified overly broad Local/Groq configuration wording. The reviewer independently reconfirmed retained/canonical provenance and package absence, and reused unchanged strict/449-test evidence.

Primary consumed the single supported outline correction: canonical passage/count/profile integrity is now checked before evidence sufficiency, and every invalid-guidance mutation is also tested with incomplete evidence. Fresh pure reproduction passed all 15 combined-defect rejections across three cases, while valid guidance plus incomplete evidence still returns incomplete-evidence. The complete future packet's expected negative count is 96. Local acquired configuration binding is now expressly distinct from Groq's exposed API/default/context-fit binding; unavailable Groq internals are unknown, not a new gate. No source/runtime behavior changed and no B artifact was created. Return the complete corrected contract for the same reviewer's fresh verdict; the research and analyst budgets remain exhausted.


**Accepted pre-draft review:** The same fresh reviewer returned PASS for the complete corrected contract, plan hash 9E7645BF2C3A928D5ED72E5031996267552854CA2E4BEABD8D620C197B761D40, with no outstanding findings. It independently reproduced all positive/native/canonical cases, missing/incomplete guidance and evidence controls, 15 invalid-guidance faults and all 15 combined faults; confirmed package absence and whitespace checks; and reused unchanged strict/449-test evidence. Primary accepts A and this evidence. The exact B artifacts and 96-negative packet still require separate final acceptance. All I1–I7 contract definitions now pass; artifact-level evidence remains pending.

### M301-FREEZE-01 — exact static package and verification

Primary authored the safe [m301-generation-v1 manifest](../../../evaluation/m301-generation-v1.json) at 2026-09-09T19:47:51Z, after mandatory DRAFT READY and corrected pre-draft PASS. Manifest SHA-256: 63770583A97E1D0517337474DADC706C1047D78E026E22C7A745CE7674ED9E6B. The manifest records all nine local file hashes, four source identities, three case definitions and six shared provider bindings. No product-model output has been inspected.

Exact local inputs have SHA-256: image 6651E5C75388E845CA1573E49F54147C24AA44D63EE255DA3BA9A20441B17097; label 48EB42DDCBB0C42EBA64A9AA0FD2202D8770211FEDACBFE569084C3E6B23C6C8; contrast 2E356A2380926E50EEC3F9143FB47258B8CF03900814EBF0D123ECB0BEAD0EAE. Shared instructions: 1755087AA9B09C40BFB181F96740984091962F17EC1174B916D5B0ECA5B59A05. Shared schema: 86E6F43CE861EA0E48C07C56A3C43764F618C9E35721C86A2D41F181A8659704. No-call definition: 1B07D5D764AD99BB028DB1AEF767B9BB9D0685661B5AD761731D60F93394B6DD. Exact source/native and separate local provenance are reconstructed by the maintained inline caller; no payload is copied into tracked evidence.

The complete M301-COMMAND-02 packet passed: three complete supported controlled cases, six byte-identical common bindings, nine exact UTF-8/LF local files, 96 adverse checks including all 15 combined-defect rejections, preserved actual abstentions and runtimeGeneration=NOT RUN. The primary inspected the authored content: selected-only facts, complete canonical texts/notices, separate native/target provenance, schema, instruction fidelity, bounded controls, human judgment/reminder, rubric and failure semantics. Source hashes and build/test identities remain unchanged. Creation guards confirmed exclusive ordinary ignored local paths, unignored manifest, empty index and no active lease; no helper, source/test/dependency or Git metadata write occurred.

I1–I7 now have static artifact evidence. Runtime validators, actual context fit, Local acquired settings/capacity and Groq operational availability remain unproved exactly as A6 requires. The different fresh final package review and documentation closure are still pending; this receipt alone does not complete M3-01.


### M301-FINAL-01 — final review and documentation closure

The different fresh critical research reviewer returned **PASS WITH FOLLOW-UPS** on the complete static package: manifest hash 63770583A97E1D0517337474DADC706C1047D78E026E22C7A745CE7674ED9E6B and plan hash A303876CAEE41F8F7B13D51A54B5E1AAC57F96577E4699BA00E1DC5D81A9CE8F. It inspected all nine complete local files, exact instruction fidelity, shared contract and controls, authority/provenance, failure policy, full I1–I7 and documentation changes. It independently reproduced the complete 96-negative packet, byte/control audit, retained actual abstentions, ignored ordinary paths, empty index/no lease and whitespace checks, and reused unchanged strict/449-test evidence. No Blocker or Major finding exists.

The sole Minor finding FINAL-F1 was stale roadmap top-level Blocked/no-package wording. Primary corrected it to the actual package/review state before closure. This documentation-only follow-up occupies the first final correction cycle; one remains unused. There is no material package revision and no invariant evidence is invalidated. The reviewer explicitly leaves ordinary final status/link/archive reconciliation to primary; no new research or approval is required.

Primary additionally checked all ten artifact files for strict UTF-8, no BOM, one LF terminator, exact JSON serialization and ordinary single-link file identity; all pass. The shared instruction/schema versions and actual authored manifest/instruction bytes agree with the reviewed definitions. The exact nine local files remain ignored and retained. The tracked manifest has no dedicated LF checkout attribute: later checkout conversion can change its hash, and existing mismatch/reconstitution rules block silent reuse. No cross-checkout byte equivalence is claimed or configuration change selected.

Documentation impact: Updated the evaluation authority, REQ-GEN-003 and ADR-0014 only for the owner-approved fixed-evaluation exception; added the safe manifest and exact ignored files; updated roadmap, owning plan, plan/progress indexes and progress record, plus documentation/evaluation navigation. Root capability and developer-command summaries remain accurate because no runtime behavior changed. No new ADR, requirement ID, dependency, implementation helper, test, source or Git write was added. Existing ADR indexes remain accurate; the clarification introduces no new architecture decision or changed provider.

Task Verification is satisfied by the reviewed static freeze, three identical common-provider definitions, exact selected-only packages, explicit change/recovery policy, fresh strict/449-test regression, preserved inputs and independent final acceptance after F1. Before completion, the documentation gate passed nine documents, 460 local links/anchors, four PowerShell blocks, current status/UTF-8/format and git diff --check. All 214 entry files outside the eight changed documentation paths remained byte-identical before the final progress-index status update. Primary accepts the gate and performs the final Complete/archive/link reconciliation as the same closure. Runtime validation, context fit, Local acquisition/configuration/capacity, Groq availability and all six model executions remain unproved downstream; the exception preserves separate actual integration obligations and all original retrieval abstentions.


**Final archive reconciliation:** Nine changed documentation files pass UTF-8/no-BOM/final-newline checks, 460 local Markdown links/anchors resolve, and all four PowerShell blocks parse. Roadmap and mirrors agree on 13 Complete and 15 Not started, with no active task; the old plan path is absent and all navigation uses the archive. All 213 protected entry files outside the nine intended documentation paths remain byte-identical. The reviewed manifest and all nine local file hashes remain unchanged; the staged diff is empty and git diff --check passes. Primary accepts final documentation closure and M3-01 completion without rerunning unchanged runtime evidence.

### A — resolve and freeze the package contract

After execution authorization, primary refreshes Git status, dependency closure, mapped authorities, protected file identities and command prerequisites. Treat repository fact gathering as R0; the consequential contract choices use the R3 route below. Research only unresolved evidence; do not repeat corpus acquisition or completed M2 verification.

Resolve these six items before `DRAFT READY`:

1. **Eligible-case provenance.** Establish whether a clearly labeled, independently constructed controlled supported package can satisfy the fixed generation case definition without masquerading as actual vector retrieval or entering a production path. Compare that bounded option with stopping for an owner-controlled retrieval/evaluation decision. Audit `REQ-EVAL-001/003/007/008`, `REQ-GEN-003`, and the roadmap's temporary-substitute rule together. Exact selected passages must satisfy all three required roles, match canonical metadata/text and fit `k = 3`; never alter historical G1–G3, invent cosine scores, bypass the existing sufficiency gate or claim controlled success repairs real retrieval. If current authority cannot support three eligible cases, record `OWNER DIRECTION`; do not silently change ranking, gold, inputs or requirements. Unaffected output/rubric definition work may continue, but the final freeze cannot pass.
2. **Input/reference contract.** Freeze exact one-Finding allowlisted facts and references, stable controlled target/fixture provenance kept outside model-visible content, exact guidance and notices where applicable, evidence completeness and support assertions derived from current validators. Keep target URL/origin/locator, page/element text, input values, siblings, raw records, vectors and history out of the model-visible package. Preserve local provenance without duplicating an outbound payload in `run.json`.
3. **Output/instruction contract.** Freeze field names, types, required/optional rules, bounds, evidence and citation reference grammar, exact proposal discriminator, confidence categories and uncertainty, assumptions, user impact, remediation, evidence sufficiency, blocking profile judgment and separate non-blocking reminder. Distinguish an adapter candidate from the application-owned abstention. Freeze one instruction variant, material generation controls and any deliberately defaulted control with documented semantics, not unspecified values. Establish compatibility of the shared contract with the accepted strict Groq schema using current primary documentation, without calls or adopting a schema framework.
4. **Interpretation.** Define a field-to-requirement rubric and expected profile-specific manual judgment. Mechanical rejection covers malformed structure, nonexistent or wrong-Finding evidence references, unresolved/invented citations and defined prohibited claims. A structurally valid but semantically unsupported claim is a reviewer edit/reject issue, not a post-call abstention. Prohibit certification, legal compliance, whole-page/site accessibility, complete SC conformance/non-conformance, and claiming a fix from automated change alone. Freeze exact mechanical policy and its limits; keyword presence is not proof of complete semantic safety. No percentages or aggregate ranking.
5. **Storage/provenance.** Keep one safe tracked manifest at `evaluation/m301-generation-v1.json` and this plan's explanation; do not add another specification document. Reserve `temp/m301-generation-freeze-v1/` for exact local assembled packages/instructions that cannot be copied into tracked evidence under `REQ-SEC-021`. Before any write, select the exact child filenames, encoding/byte identity, contents, approved synthetic provenance, retention and reconstitution procedure; verify ignored/untracked status and ordinary in-repository paths. The safe manifest links versioned identities and validation expectations without raw prompts/payloads. Loss of an exact local package blocks later calls until verified identical reconstruction or a new freeze. A proposal to publish excluded content requires owner direction, not an inferred synthetic-data exception.
6. **Decide now / prove later.** Complete case, output, instruction, control and interpretation semantics now. M3-02 later proves runtime validators, minimization and no-truncation context-fit behavior; M3-03 later records exact acquired Qwen digest/runtime/quantization/context/offload and capacity; M3-04 proves fixed Groq mapping/current availability. Record those as explicitly unproved prerequisite slots, not unresolved M3-01 semantics or a capacity PASS. Before their first output is inspected, the downstream task must bind exact configuration to this package, recheck fit and freeze any material amendment as new evidence. No adapter may choose a different contract or silently shorten the package.

Only after mandatory synthesis and the fresh contract checkpoint may primary author B. Ordinary task literals do not require a new ADR; significant architecture changes or owner-controlled scope/interpretation changes stop at their existing approval route.

### B — author, verify and record the freeze

Primary is sole writer of the authoritative static manifest, local evaluation definitions and living documentation, between leases. No researcher/reviewer writes and no worker receives evidence-document ownership. **Responsibility placement: None — no application-source responsibility changes.** Existing pure validators remain dependencies of verification, never consumers of a new product configuration. No structural refactor is permitted.

Write only after the exact command/storage slots below and A's six items pass their barrier. Inspect every case and contract field against its authority and canonical source; verify both modes reference identical application inputs and output semantics. Run the complete invariant packet, independent strict checking, and fresh final `critical_research_reviewer` review of the whole artifact and evidence. Primary accepts the actual artifacts and results, not just the report. Preserve unresolved runtime proof as later work and do not mark an unproven case successful.

At closure update the task row, plan/progress indexes and task progress, plus the maintained README/evaluation navigation only where the delivered artifact changes their instructions or current statements. Read any newly affected document completely before editing. Mark Complete and archive only after M3-01 Verification and the documentation gate pass. A later task still requires selection.

If inspection demonstrates a necessary executable helper, first reconsider direct checks. Do not improvise code under primary evidence ownership: revise the work-slice contract with exact paths/commands and apply the [worker-first workflow](../../../.codex/execplan-implementation-workflow.md) and [write lease](../../../.codex/write-lease-guard.md). A non-behavioral setup uses guarded `code_worker`; real production behavior belongs to a separately selected task with independent test/Green ownership. No helper is currently planned or authorized by this planning request.

## Decision Review Contract

**Owner/artifact:** M3-01; the safe manifest, exact local package and task-owned freeze explanation. **Tier: R3**, triggered by identity/reference binding, closed provider data boundaries and integrity of evidence interpretation, not by model selection or the number of cases. The procedure and planning evidence table below preserve the contract used during execution. The initial owner-direction stop was resolved by [M301-OWNER-02](#m301-owner-02--accepted-controlled-input-scope-and-resolved-contract); [M301-FINAL-01](#m301-final-01--final-review-and-documentation-closure) records completed acceptance. Historical pending cells and allowances do not reopen this task or renew its budgets.

One bounded non-ranking discovery pass may identify candidate package provenance/storage representations and primary provider-schema sources. Freeze the viable candidate set and common comparison matrix before comparative research. Compare authority fit, case eligibility, reference correctness, payload minimization, shared-provider expressibility, reproducibility and smallest maintenance burden. A hard-gate failure disqualifies an option; do not rank a forbidden option as a compromise. Required artifact-local outputs are A's six resolved items, input/output/provenance mappings, rubric, failure interpretation and the entire invariant packet.

Use at most two read-only `critical_researcher` reports: (1) eligible-case provenance/canonical references and unchanged retrieval boundary; (2) shared output/instruction/minimization/storage contract and provider expressibility. Each receives [Research Assignment Capsule v1](../../../.codex/README.md#research-assignment-capsule-v1), exact authorities, shared evidence identity, no-output/no-write permission, a useful-detail limit of 1,000 words plus necessary contract tables, and one targeted follow-up round. Independent dimensions may run concurrently while primary resolves repository facts. Do not add a routine third researcher or drafter.

Wait at one evidence barrier. Mandatory `decision_analyst` receives the combined evidence and contract, with one synthesis and one bounded correction, and returns exactly `DRAFT READY`, `RETURN FOR RESEARCH`, or `OWNER DIRECTION`. R3 requires a fresh `critical_research_reviewer` pre-draft checkpoint even after DRAFT READY; permit its one supported outline correction. Primary drafts only after PASS, then a different fresh `critical_research_reviewer` performs final semantic/evidence review. Use configured role pins from the [model policy](../../../.codex/README.md#model-and-reasoning-policy); no policy or model migration is selected here.

Final-artifact corrections have the workflow's two-cycle ceiling. Re-run the entire packet and complete artifact review after every material R3 revision. Reuse researchers only within their remaining follow-up allowance; a new identity never resets a budget. Stop for repeated decisive gaps without new evidence, two unsupported synthesis returns, exhausted allowance, authority conflict or expanded scope. Primary completes unaffected authorized work, records precise missing owner direction and does not present REVISE/BLOCKED as acceptance-ready.

| Invariant | Trigger / expected result | Evidence at planning | Responsible acceptance |
| --- | --- | --- | --- |
| I1 Completeness | All A items resolved; exactly three eligible case definitions, one shared contract and six future case/provider bindings; no model output used to select them | Pending | Analyst; both R3 reviewers; primary |
| I2 Honesty and authority | Actual M2-04 G1–G3 stay abstained; controlled inputs labeled; no ranking/corpus/sufficiency change, new product path, approval or capacity claim | M2-04 closure and M301-PLAN-01 | Both R3 reviewers; primary |
| I3 Identity and support | Each case references exactly one complete Finding; every passage reconstructs from the unchanged corpus; all roles present; unknown/wrong-profile/altered references fail | Existing 119-test boundary; package pending | Researcher 1; both R3 reviewers |
| I4 Privacy and ownership | Selected-only allowlist; forbidden data excluded; adapter credentials never model-visible/retained; tracked artifacts contain no raw prompts/payloads; primary owns evidence writes | Authorities/current source inspected; package pending | Researcher 2; both R3 reviewers |
| I5 Shared output and judgment | Both modes use the same structure and reference policy; confidence is categorical, abstention distinct; blocking judgment/reminder and semantic-review boundary retained | Pending literal freeze | Analyst; both R3 reviewers |
| I6 Failure and no-call meaning | Incomplete/missing/conflicting evidence guidance abstains; integrity/context-fit/prerequisite/call/validation errors remain their distinct failures; no retry/fallback or fabricated invocation | Existing abstention evidence; generation rules unimplemented | Both R3 reviewers; primary |
| I7 Freeze and recovery | Exact local inputs and safe provenance agree; tampering or missing files blocks use; material changes preserve old versions/evidence; all later configuration proofs explicitly pending | Pending exact paths/commands and freeze | Both R3 reviewers; primary |

## Concrete Steps

### Retained-result reconstruction caller

This read-only caller is the final corrected reconstruction used in M301-RESEARCH-01. It reads the retained preparation and three canonical run files; it imports only existing pure production validators and Node built-ins, writes no files and makes no model/browser request. Run from the repository root after loading the maintained README definitions block into the calling PowerShell session. Exit nonzero or an assertion failure invalidates only this reconstruction claim and returns control to primary; it grants no repair or retry of actual retrieval. Expected: three exact abstention rows and 72 artifact plus two package hash matches. The original broad test-hash reuse assertion and native/workflow-shape caller failures are preserved in M301-RESEARCH-01; this narrower caller supersedes them only for current reconstruction.

```powershell
Invoke-M105Command {
@'
import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
import { validateRun } from './src/server/domain/run-contract.ts';
import { assessFindingEvidence } from './src/server/domain/finding-sufficiency.ts';
import { resolveCitations } from './src/server/retrieval/citation-resolution.ts';
const hash=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex').toUpperCase();
const prep=JSON.parse(fs.readFileSync('temp/m204-retrieval-checkpoint/preparation.json','utf8'));
assert.equal(hash('temp/m204-retrieval-checkpoint/preparation.json'),'79E9D69F75CA10842CE02B43342F6FCED8E206E81F2AE28A55713322686202DC');
let checked=0;
for(const group of ['source','build','corpus']) for(const [p,h] of Object.entries(prep.baselineIdentity[group])) { assert.equal(hash(p),h,p); checked++; }
for(const [key,p] of [['packageJson','package.json'],['packageLock','package-lock.json']]) assert.equal(hash(p),prep.baselineIdentity.configuration[key]);
const m=fs.readFileSync('corpus/wcag22-mvp-v1/manifest.json'), p=fs.readFileSync('corpus/wcag22-mvp-v1/passages.json');
for(const [id,role] of [['g1','interpretation'],['g2','criterion'],['g3','criterion']]) {
 const path=`temp/m204-retrieval-checkpoint/${id}/runs/m204-${id}/run.json`;
 const r=JSON.parse(fs.readFileSync(path,'utf8')); assert.ok(validateRun(r).ok);
 const f=r.scan.findings[0]; const native={findingId:f.findingId,ruleId:f.ruleId,nativeResult:f.nativeResult,state:'unprocessed',checks:f.checks,locator:f.locator,evidence:f.evidence}; assert.equal(assessFindingEvidence(native).state,'complete');
 const c=resolveCitations(native,f.retrieval.result,m,p); assert.ok(c.ok); assert.equal(c.support.state,'incomplete'); assert.deepEqual(c.support.missingRoles,[role]); assert.equal(f.state,'abstained'); assert.equal(f.result.providerCalled,false); assert.equal(f.providerInvocation,undefined);
 console.log(JSON.stringify({case:id,sha256:hash(path),state:f.state,missingRoles:c.support.missingRoles,canonicalPassages:c.value.passages.map(x=>x.passageId)}));
}
console.log(JSON.stringify({preparationArtifactHashesMatched:checked,packageHashesMatched:2}));
'@ | & $m105Node --input-type=module -
if ($LASTEXITCODE -ne 0) { throw 'Retained evidence check failed' }
}
```

### Static package preparation (not yet executable)

**Historical planning section:** Its unresolved slots were completed by [M301-COMMAND-02](#m301-command-02--resolved-preparation-validation-and-closure-callers). The original heading and steps remain for traceability; they are not the current verification entry point.

Run from `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`. Load only the definition block in [README development command preparation](../../../README.md#development-command-preparation) into the actual calling PowerShell session; do not replay the adjacent dependency-restore command. Missing runtime/dependencies stop for reconciliation, not installation.

```powershell
git status --short
if ($LASTEXITCODE -ne 0) { throw 'Git status failed' }
$m301EntryHead = git rev-parse HEAD
if ($LASTEXITCODE -ne 0) { throw 'HEAD lookup failed' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'Whitespace check failed' }
```

Record current intentional HEAD, branch, index/dirty paths and relevant file hashes. Classify pre-existing changes and preserve them; no clean-tree or uncommitted-plan requirement is assumed. Recheck all controlled fixtures, corpus/gold, source/tests and package/configuration inputs against the accepted entry. Historical commits and raw checkout hashes are not substituted for the corpus validator's own normalized-content identity.

```powershell
Invoke-M105Command {
  if ((& $m105Node --version) -ne 'v24.20.0' -or $LASTEXITCODE -ne 0) { throw 'Pinned Node required' }
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test tests/run-contract.test.ts tests/scan-normalization.test.ts tests/retrieval-contract.test.ts tests/finding-sufficiency.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Contract checks failed' }
}
```

Expected current result: strict PASS and 119 passing tests; changes in that count require explanation, not editing tests to match it. These checks make no model request. Reuse unaffected M2-04 suite/build evidence by full evidence identity; task closure follows the complete authoritative-suite rule, preparing any required broader command and its effects before execution. Do not blindly launch the browser-heavy suite merely to draft documentation.

These **unresolved future command slots are not executable permission**. Primary fills exact commands, arguments, expected results and caller/error boundaries in this section during A, before package writes or any optional worker lease:

| Slot | Binding content required before use |
| --- | --- |
| Package creation/storage | Exact tracked manifest and local child filenames; UTF-8/newline representation; ordinary-path and ignored/untracked checks; whether paths must be absent; primary `apply_patch` writes; no overwrite of existing evidence |
| Canonical reconstruction | Exact pinned-Node command/imports and evidence/JSON inputs; existing evidence, retrieval and citation validators; selected-Finding and role checks; no model/browser import effects |
| Negative checks | Exact in-memory mutations for missing roles/evidence, wrong/unknown references, altered passage, sibling/forbidden field, incomplete output definition and mismatched version/hash; expected rejection or gate failure, without modifying frozen files |
| Freeze receipt | Exact safe fields, UTC freeze time, versions, hashes and references; verify all three local packages are present and identical across provider bindings; mark downstream runtime/adapter slots unproved |
| Regression/closure | Exact applicable full-suite command, prerequisites and generated scratch effects, or documented unchanged-evidence reuse where permitted; independent strict command above; manifest/Markdown/link/status checks and `git diff --check` |
| Retention/recovery | Preserve exact inputs needed downstream and earlier failures; no default deletion. Any later cleanup names only validated task-owned disposable files and its effects; no broad temp, run, model, dependency or workspace cleanup |

No bootstrap, dependency restore, live retrieval or generation command is needed. M301-COMMAND-02 resolves the stale-suite identity issue with only the maintained regression tests' temporary service/browser effects; no manual browser proof or new public scan is selected. Other effects require scope/command triage.

### M301-COMMAND-02 — resolved preparation, validation and closure callers

**Read-only reuse after closure:** Load the maintained README definitions and use only the verification code block below, with its stated input/path prerequisites. The creation and full-regression paragraphs record completed M3-01 work; they do not require recreating files or rerunning the task to inspect an unchanged freeze. Apply the existing evidence-identity and recovery rules if inputs differ.

These slots supersede the earlier unresolved future table for the selected static route. Load only the README definitions into the actual PowerShell caller as described above. Every Node invocation uses Invoke-M105Command and the pinned executable. Primary creates the nine files named in M301-OWNER-02 and the safe manifest using apply_patch after pre-draft PASS. JSON bytes are JSON.stringify(value,null,2) plus LF; no existing file is overwritten. Source reconstruction uses the exact pure imports below; primary captures candidate data in memory and authors the reviewed static definitions, never a reusable runner.

Before creation, resolve each reserved target through Assert-M105OrdinaryPath -AllowMissing. Require the manifest and entire versioned local directory absent, no active lease, empty staged diff and unchanged protected files. Require git check-ignore --quiet for each of the nine reserved local children and git ls-files --error-unmatch to fail for each. These Git calls are read-only. Require the manifest path not ignored. No cleanup, acquisition or source/test edit is selected. Any failed path/hash/definition assertion stops affected creation for primary triage.

The following exact one-shot Node caller is the complete package verification after authoring and after each material correction. It reads only the fixed corpus/gold, three retained run/seed pairs and the manifest's nine exact local files. Ordinary-path and ignore checks are repeated before readback. It writes nothing and imports no network/browser/model adapter. Assertions cover canonical reconstruction, native completeness, all three roles, exact selected-only projection, full notice preservation, closed schema, reference/bound rules, byte identity, six common bindings and the shared no-call outcome. Synthetic shape vectors do not count as generated answers or prove semantic support. All negative mutations stay in memory; a failure at an unrelated import/parse does not pass a negative assertion. Expected final summary: 3 cases, 6 bindings, 9 files, 96 negative checks, including 15 combined incomplete-evidence/invalid-guidance rejections, unchanged actual abstentions and runtimeGeneration=NOT RUN.

```powershell
Invoke-M105Command {
@'
import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
import {validateRun} from './src/server/domain/run-contract.ts';
import {assessFindingEvidence} from './src/server/domain/finding-sufficiency.ts';
import {parseCorpusCatalog} from './src/server/retrieval/corpus-validation.ts';
import {findPassageReference,PROFILE_REQUIRED_ROLES} from './src/server/retrieval/corpus-identity.ts';
import {SOURCE_NOTICES} from './src/server/retrieval/source-notices.ts';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));
const hash=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex').toUpperCase();
const cat=parseCorpusCatalog(fs.readFileSync('corpus/wcag22-mvp-v1/manifest.json'),fs.readFileSync('corpus/wcag22-mvp-v1/passages.json'));
assert.ok(cat.ok); const catalog=cat.value; assert.deepEqual(catalog.manifest.unresolvedConflicts,[]);
const gold=read('evaluation/m201-corpus-v1.json');
const triples=[['wcag22-sc111','understanding111-intent','h37-text-alternative'],['wcag22-sc412','understanding412-intent','h44-explicit-label'],['wcag22-sc143','understanding143-threshold-measurement','g18-contrast-remediation']];
const factValue=(n,r)=>{const v=r.split('.').reduce((v,k)=>v[k],n); return typeof v==='object'&&'value'in v?v.value:v;};
const cases=gold.cases.map((g,i)=>{
 const sourcePath=`temp/m204-retrieval-checkpoint/g${i+1}/runs/m204-g${i+1}/run.json`;
 const run=read(sourcePath); assert.ok(validateRun(run).ok); const f=run.scan.findings[0];
 const native={findingId:f.findingId,ruleId:f.ruleId,nativeResult:f.nativeResult,state:'unprocessed',checks:f.checks,locator:f.locator,evidence:f.evidence};
 assert.deepEqual(native, Object.fromEntries(Object.keys(native).map(k=>[k,read(`temp/m204-retrieval-checkpoint/g${i+1}/seed.json`).scan.findings[0][k]])));
 const assessment=assessFindingEvidence(native); assert.equal(assessment.state,'complete');
 assert.equal(f.state,'abstained'); assert.equal(f.result.providerCalled,false); assert.equal(f.providerInvocation,undefined);
 const passages=triples[i].map(id=>catalog.passages.find(p=>p.passageId===id));
 passages.forEach(p=>{assert.ok(g.goldPassageIds.includes(p.passageId));assert.deepEqual(p.ruleIds,[g.ruleId]);assert.deepEqual(p.successCriteria,[g.successCriterion]);});
 assert.deepEqual(passages.map(p=>findPassageReference(p.passageId).guidanceRole),PROFILE_REQUIRED_ROLES[g.ruleId]);
 const notices={sources:[...new Set(passages.map(p=>p.sourceTitle))].map(title=>{
  const s=catalog.manifest.sources.find(s=>s.title===title);
  return Object.fromEntries(['title','type','url','status','version','copyright','attribution'].map(k=>[k,s[k]]));
 }),full:SOURCE_NOTICES};
 return {caseId:g.profile,fixtureRevision:g.fixtureRevision,targetKey:g.targetKey,ruleId:g.ruleId,successCriterion:g.successCriterion,passageIds:triples[i],
 input:{finding:{findingId:native.findingId,ruleId:native.ruleId,nativeResult:native.nativeResult,facts:assessment.availableReferences.map(reference=>({reference,value:factValue(native,reference)}))},guidance:{corpusVersion:catalog.identity.version,passages,notices}},
 provenance:{version:'m301-generation-v1',caseId:g.profile,evidenceOrigin:'synthetic-seeded-native-evidence',fixtureRevision:g.fixtureRevision,targetKey:g.targetKey,sourcePath,sourceSha256:hash(sourcePath),sourceRunId:run.runId,sourceWorkflowState:f.state,sourceProviderCalled:false,scanContext:run.scan.context,native,assessment,guidanceOrigin:'independently-assembled-canonical-evaluation-only',support:'supported'}};
});
const sourcePaths=['corpus/wcag22-mvp-v1/manifest.json','corpus/wcag22-mvp-v1/passages.json','evaluation/m201-corpus-v1.json','evaluation/rd003-scan-v1.json'];

const manifest=read('evaluation/m301-generation-v1.json');
assert.equal(manifest.version,'m301-generation-v1'); assert.equal(manifest.status,'frozen-before-model-output');
assert.ok(Number.isFinite(Date.parse(manifest.frozenAt)));
assert.deepEqual(manifest.corpusIdentity,catalog.identity); assert.equal(manifest.cases.length,3);
const schema=read(manifest.shared.outputSchema.path);
const patterns=["\\b(?:certif[a-z]*|conform[a-z]*|complian[a-z]*)\\b","\\b(?:whole|entire)[ -]+(?:page|site|website)[ -]+(?:is[ -]+)?(?:(?:fully|completely)[ -]+)?accessible\\b","\\b(?:page|site|website)[ -]+(?:is|are)[ -]+(?:(?:fully|completely)[ -]+)?accessible\\b","\\b(?:finding|issue|violation)[ -]+(?:is|was|has been)[ -]+(?:fixed|resolved|remediated)\\b","\\b(?:automated|automatic|scanner|axe(?:-core)?)[ -]+(?:evidence|results?|changes?|checks?|scans?)[ -]+(?:alone[ -]+)?(?:prove|proves|confirm|confirms)[ -]+(?:a[ -]+|the[ -]+)?(?:fix|resolution)\\b"];
assert.deepEqual(manifest.prohibitedClaims.patterns,patterns); assert.equal(manifest.prohibitedClaims.flags,'u');
const expectedSchema={"type":"object","properties":{"type":{"type":"string","enum":["proposal"]},"findingId":{"type":"string"},"findingSummary":{"type":"object","properties":{"text":{"type":"string"},"evidenceReferences":{"type":"array","items":{"type":"string"}},"passageIds":{"type":"array","items":{"type":"string"}}},"required":["text","evidenceReferences","passageIds"],"additionalProperties":false},"userImpact":{"type":"object","properties":{"text":{"type":"string"},"evidenceReferences":{"type":"array","items":{"type":"string"}},"passageIds":{"type":"array","items":{"type":"string"}}},"required":["text","evidenceReferences","passageIds"],"additionalProperties":false},"remediation":{"type":"object","properties":{"text":{"type":"string"},"evidenceReferences":{"type":"array","items":{"type":"string"}},"passageIds":{"type":"array","items":{"type":"string"}}},"required":["text","evidenceReferences","passageIds"],"additionalProperties":false},"evidenceSufficiency":{"type":"object","properties":{"findingEvidence":{"type":"string","enum":["complete"]},"guidance":{"type":"string","enum":["supported"]}},"required":["findingEvidence","guidance"],"additionalProperties":false},"confidence":{"type":"string","enum":["high","medium","low"]},"uncertainty":{"type":"string"},"assumptions":{"type":"array","items":{"type":"string"}},"blockingManualJudgment":{"type":"string"},"postChangeVerificationReminder":{"type":"string"}},"required":["type","findingId","findingSummary","userImpact","remediation","evidenceSufficiency","confidence","uncertainty","assumptions","blockingManualJudgment","postChangeVerificationReminder"],"additionalProperties":false};
assert.deepEqual(schema,expectedSchema);
const verifyBytes=(bytes,expected)=>{
 assert.equal(crypto.createHash('sha256').update(bytes).digest('hex').toUpperCase(),expected);
 assert.ok(!bytes.subarray(0,3).equals(Buffer.from([239,187,191])));
 const s=new TextDecoder('utf8',{fatal:true}).decode(bytes); assert.ok(!s.includes('\r'));
 assert.ok(s.endsWith('\n')&&!s.endsWith('\n\n')); return s;
};
const files=[];
for(const c of manifest.cases) files.push(c.input,c.provenance);
files.push(manifest.shared.instructions,manifest.shared.outputSchema,manifest.shared.noCall);
assert.equal(new Set(files.map(f=>f.path)).size,9);
assert.deepEqual(files.map(f=>f.path),[
 'informative-image-alt.input.json','informative-image-alt.provenance.json',
 'form-input-label.input.json','form-input-label.provenance.json',
 'text-contrast.input.json','text-contrast.provenance.json',
 'instructions.txt','output-schema.json','no-call.json'
].map(name=>'temp/m301-generation-freeze-v1/'+name));
assert.deepEqual(manifest.sources,[{"path":"corpus/wcag22-mvp-v1/manifest.json","sha256":"87D8867ED1138BFB38E6C44EED67A79B0A43BDAF2532A056AFAFAED828202DA9"},{"path":"corpus/wcag22-mvp-v1/passages.json","sha256":"29EA0033E825A0FA20430AA42E9718B87A87AA59EDADDA0A3876A0A7BE871850"},{"path":"evaluation/m201-corpus-v1.json","sha256":"A7D38CACF98BF263CDD53012350708BE987699DF21B4C0F1BB7F32BC1892E29B"},{"path":"evaluation/rd003-scan-v1.json","sha256":"13C9722BE9EA2E3B0AAF020EA91F429A701180A83814FE7AB21BAF2DDAD57459"}]);
for(const f of files) {
 assert.ok(f.path.startsWith('temp/m301-generation-freeze-v1/'));
 const bytes=fs.readFileSync(f.path); verifyBytes(bytes,f.sha256);
 assert.throws(()=>verifyBytes(Buffer.concat([bytes,Buffer.from('x')]),f.sha256));
}
for(const s of manifest.sources) assert.equal(hash(s.path),s.sha256);
const classify=(native,passages,g)=>{
 assert.ok(passages.length<=3); assert.equal(new Set(passages.map(p=>p.passageId)).size,passages.length);
 for(const p of passages) {
  assert.deepEqual(p,catalog.passages.find(x=>x.passageId===p.passageId));
  assert.deepEqual(p.ruleIds,[g.ruleId]); assert.deepEqual(p.successCriteria,[g.successCriterion]);
 }
 const a=assessFindingEvidence(native); if(a.state!=='complete') return 'incomplete-evidence';
 if(!passages.length) return 'missing';
 const roles=passages.map(p=>findPassageReference(p.passageId).guidanceRole);
 return PROFILE_REQUIRED_ROLES[g.ruleId].every(r=>roles.includes(r))?'supported':'incomplete';
};
const matchesSchema=(v,s)=>{
 if(s.type==='object') {
  assert.ok(v&&typeof v==='object'&&!Array.isArray(v)); assert.deepEqual(Object.keys(v).sort(),s.required.slice().sort());
  for(const k of s.required) matchesSchema(v[k],s.properties[k]);
 } else if(s.type==='array'){assert.ok(Array.isArray(v)); for(const x of v) matchesSchema(x,s.items);}
 else {assert.equal(typeof v,'string'); if(s.enum) assert.ok(s.enum.includes(v));}
};
const prohibited=s=>patterns.some(p=>new RegExp(p,'u').test(s.normalize('NFKC').toLowerCase().replace(/\s+/gu,' ').trim()));
const validateCandidate=(v,c)=>{
 matchesSchema(v,schema); assert.equal(v.findingId,c.input.finding.findingId);
 const refs=c.input.finding.facts.map(f=>f.reference), ids=c.passageIds;
 const prose=(s,max)=>{assert.ok(s.trim().length>0&&s.length<=max);assert.ok(!prohibited(s));};
 for(const name of ['findingSummary','userImpact','remediation']){
  const vfield=v[name]; prose(vfield.text,name==='remediation'?2000:1000);
  for(const [key,allowed] of [['evidenceReferences',refs],['passageIds',ids]]){
   assert.equal(new Set(vfield[key]).size,vfield[key].length);assert.ok(vfield[key].length<=allowed.length);
   for(const r of vfield[key]) assert.ok(allowed.includes(r));
  }
  assert.ok(name==='findingSummary'?vfield.evidenceReferences.length:vfield.passageIds.length);
 }
 for(const name of ['uncertainty','blockingManualJudgment','postChangeVerificationReminder']) prose(v[name],1000);
 assert.ok(v.assumptions.length<=5); for(const a of v.assumptions) prose(a,500);
};
let negatives=0;
for(let i=0;i<cases.length;i++){
 const c=cases[i], entry=manifest.cases[i], g=gold.cases[i];
 assert.equal(entry.caseId,c.caseId); assert.deepEqual(entry.passageIds,c.passageIds);
 for(const k of ['fixtureRevision','targetKey','ruleId','successCriterion']) assert.equal(entry[k],c[k]);
 assert.deepEqual(read(entry.input.path),c.input); assert.deepEqual(read(entry.provenance.path),c.provenance);
 assert.deepEqual(entry.availableEvidenceReferences,c.input.finding.facts.map(f=>f.reference));
 assert.equal(classify(c.provenance.native,c.input.guidance.passages,g),'supported');
 assert.equal(classify(c.provenance.native,c.input.guidance.passages.slice(0,2),g),'incomplete'); negatives++;
 const incomplete=structuredClone(c.provenance.native); incomplete.checks={unavailable:'missing'};
 assert.equal(classify(incomplete,c.input.guidance.passages,g),'incomplete-evidence'); negatives++;
 for(const mutate of [
  ps=>{ps[0].passageId='unknown';},ps=>{ps[0].text+=' altered';},
  ps=>{ps[0]=cases[(i+1)%3].input.guidance.passages[0];},
  ps=>{ps[1]=ps[0];},ps=>ps.push(ps[0])
 ]) {const ps=structuredClone(c.input.guidance.passages);mutate(ps);assert.throws(()=>classify(c.provenance.native,ps,g));negatives++;
 assert.throws(()=>classify(incomplete,ps,g));negatives++;}
 for(const key of ['siblingFindings','targetUrl','locator','inputValue','credential','history']) {
  const injected={...c.input,[key]:'synthetic-forbidden'};
  assert.throws(()=>assert.deepEqual(injected,c.input));negatives++;
 }
 // Hand-authored shape vectors only: no semantic or model-success evidence.
 const v={type:'proposal',findingId:'finding-0',findingSummary:{text:'Synthetic shape vector.',evidenceReferences:[c.input.finding.facts[0].reference],passageIds:[]},
 userImpact:{text:'Synthetic shape vector.',evidenceReferences:[],passageIds:[c.passageIds[1]]},
 remediation:{text:'Synthetic shape vector.',evidenceReferences:[],passageIds:[c.passageIds[2]]},
 evidenceSufficiency:{findingEvidence:'complete',guidance:'supported'},confidence:'low',uncertainty:'Human context is required.',assumptions:[],
 blockingManualJudgment:'Request the profile judgment.',postChangeVerificationReminder:'Rescan after a change.'};
 validateCandidate(v,c);
 for(const mutate of [
  x=>delete x.uncertainty,x=>x.extra='x',x=>x.findingId='finding-1',x=>x.confidence='95%',
  x=>x.findingSummary.evidenceReferences=['evidence.unknown'],x=>x.remediation.passageIds=['unknown'],
  x=>x.userImpact.passageIds=[],x=>x.findingSummary.evidenceReferences.push(x.findingSummary.evidenceReferences[0]),
  x=>x.uncertainty=' ',x=>x.remediation.text='a'.repeat(2001),x=>x.evidenceSufficiency.guidance='incomplete',
  x=>x.remediation.text='The finding is fixed.',x=>x.blockingManualJudgment=null
 ]) {const bad=structuredClone(v);mutate(bad);assert.throws(()=>validateCandidate(bad,c));negatives++;}
}
assert.equal(manifest.executions.length,6);
assert.equal(new Set(manifest.executions.map(e=>e.caseId+'/'+e.mode)).size,6);
for(const c of manifest.cases) for(const mode of ['local','groq']) {
 const e=manifest.executions.find(e=>e.caseId===c.caseId&&e.mode===mode);assert.ok(e);
 assert.equal(e.inputSha256,c.input.sha256);assert.equal(e.instructionSha256,manifest.shared.instructions.sha256);
 assert.equal(e.outputSchemaSha256,manifest.shared.outputSchema.sha256);
}
const noCall=read(manifest.shared.noCall.path);
assert.deepEqual(noCall.native,cases[0].provenance.native);
assert.deepEqual(noCall.passageIds,cases[0].passageIds.slice(0,2));
assert.equal(classify(noCall.native,noCall.passageIds.map(id=>catalog.passages.find(p=>p.passageId===id)),gold.cases[0]),'incomplete');
assert.deepEqual(noCall.expected,{support:'incomplete',missingRoles:['remediation'],reason:'incomplete-guidance',providerCalled:false,providerInvocation:false,proposal:false,reviewDecision:false});
assert.ok(noCall.explanation.includes('remediation'));assert.ok(noCall.manualInvestigation.trim());
for(const s of ['finding is fixed','whole-page is accessible','Scanner results alone prove the fix','ＣＥＲＴＩＦＩＥＤ','not conformant']) assert.ok(prohibited(s));
for(const s of ['Verify purpose with a human.','Provide accessible labels.','page offers access to everyone']) assert.ok(!prohibited(s));
const wrongVersion={...manifest,version:'wrong'};assert.throws(()=>assert.equal(wrongVersion.version,'m301-generation-v1'));negatives++;
assert.equal(parseCorpusCatalog(fs.readFileSync('corpus/wcag22-mvp-v1/manifest.json'),Buffer.from('altered')).ok,false);negatives++;
assert.throws(()=>fs.readFileSync('temp/m301-generation-freeze-v1/missing.input.json'));negatives++;
console.log(JSON.stringify({staticPacket:'PASS',cases:cases.length,bindings:manifest.executions.length,localFiles:files.length,negativeChecks:negatives,actualStates:cases.map(c=>c.provenance.sourceWorkflowState),runtimeGeneration:'NOT RUN',manifestSha256:hash('evaluation/m301-generation-v1.json')}));

'@ | & $m105Node --input-type=module -
if ($LASTEXITCODE -ne 0) { throw 'Static package verification failed' }
}
```

For closure, the historical three test/helper hash mismatches prevent complete-suite reuse. Therefore run the maintained thirteen-suite command in README's Build and verify section, sequentially, after the definitions block and independent strict tsc command above. Each suite uses --test-timeout=120000; the nine browser-free files precede scan-page/walking-skeleton under m103-scan scratch and target-results-ui/finding-guidance-ui under m104-ui scratch. The integration tests also use temp/m105-integration. Inspect those exact ordinary directories and require them empty before/after; create only a missing directory with New-Item -ItemType Directory after path validation. Confirm the existing client build identities from retained preparation and installed Chromium 1234; no rebuild/download is selected while unchanged build identity passes. Confirm no application service or overlapping test process. These maintained tests may start temporary loopback services and scan intercepted project-owned fixture responses; they are required regression, not new public scans or manual browser proof. A failure stops the sequence for primary triage, without installing or repairing anything implicitly. Expected total: 449 passing tests, no failed/cancelled/skipped/todo cases. Capture bounded suite totals, not raw page/model content.

Finally inspect the actual diff and every frozen file; compare protected tracked hashes, run git diff --check, JSON parse/UTF-8/LF checks, affected Markdown link/anchor checks, PowerShell code-block parsing and roadmap/plan/progress consistency. Receipt: UTC frozenAt, complete artifact hashes and accepted invariant/reviewer results in this plan; the safe manifest has no self-hash. Missing exact local files, wrong version or mismatched bytes blocks use. Retain all files and earlier evidence; no deletion command is authorized.


## Validation and Acceptance

A passes only with complete evidence, mandatory DRAFT READY, fresh pre-draft PASS and primary reconciliation of all six decisions and I1–I7. An unresolved eligibility authority or material contract semantic cannot be deferred as runtime proof. Owner-controlled decisions remain owner-controlled.

B passes only when exact packages are frozen before product-model outputs, every case is demonstrably complete/supported under the accepted provenance route, all input/reference and privacy checks pass, both modes bind the same contract, and the rubric/failure policy is complete. Inspect exact artifacts, not a receipt alone. Negative evidence must fail the intended boundary, not merely parse or throw for an unrelated missing import. Hand-authored output vectors, if needed to illustrate validation, are explicitly synthetic and do not count as model success; production runtime validation remains unimplemented.

Recheck protected inputs and current status, full applicable invariants, independent strict TypeScript, applicable regression evidence and fresh final R3 review. Resolve every finding and reconcile the reviewed artifact, verdict, requirements, remaining runtime gates and next action. `PASS WITH FOLLOW-UPS` advances only when no item conflicts with a hard gate or definition of done. Complete the [documentation closure gate](../../README.md#task-closure-documentation-gate), including links, JSON syntax, UTF-8/newlines, command parsing, status consistency and `git diff --check` before marking Complete.

There is no test preflight/Red/Green or lease receipt to fabricate for primary-authored decision documents. If the optional helper contingency is actually selected, its revised contract must supply packet/lease/role identities, non-TDD or characterization justification, actual diff inspection and proportional fresh implementation review under the existing workflow.

## Idempotence and Recovery

Inspection and pure checks are safe to repeat; drafting never overwrites an earlier frozen/evaluated version. Before resumption read Current state, applicable A/B contract, affected authorities, evidence and history as required by PLANS.md. Refresh the real HEAD and identities after an intentional commit or intervening edit; a commit alone is not a failure, and historical HEAD is never an executable receipt condition.

Preserve unrelated changes, retained runs, dependencies, runtime/model installations, corpus and fixtures. No recursive deletion or Git mutation is planned. An unexpected path, reference mismatch, secret/raw-content risk or active lease stops affected writes; primary reconciles between leases without reverting others. Missing local exact inputs block downstream evaluation until identical authorized reconstruction is proven; otherwise create a new version and preserve the old evidence. Never use output-informed tuning, a new researcher identity, or reclassification as routine recovery to bypass a gate or replenish allowance.

## Artifacts and Notes

Deliverables are the safe evaluation manifest, nine exact ignored local files, this plan's decision/verification history, reconciled authority notes and task/navigation/progress documentation. M301-FREEZE-01 owns the byte receipt. No standalone research report, command framework, raw agent transcript or telemetry ledger was added; raw inputs remain outside tracked evidence. Final review and documentation closure are accepted in M301-FINAL-01.

## Interfaces and Dependencies

M2-04 is the completed prerequisite. Existing scanner, corpus, gold, native evidence and support/citation contracts are read-only inputs. M3-01 supplies a frozen case and application-output specification immediately consumed by separately authorized M3-02, not a new runtime API. Local/Groq protocol mapping and operational availability remain with their adapters; no new package, library or service is selected. The agent workflow is repository coordination only, not product-runtime orchestration.

### M301-PLAN-REVIEW-01 — accepted planning readiness

On 2026-09-09 a fresh read-only `critical_reviewer` returned **PASS** for pre-execution planning readiness, with no outstanding findings. Named S3 triggers were selected-Finding privacy, canonical evidence/reference identity and freeze recovery. It reviewed the complete plan and five-file planning surface against controlling authorities, M2-04 closure, the R3 route, command gates, preservation and YAGNI. A minor progress-table separator was corrected by primary and verified by the reviewer. The reviewed plan's SHA-256 was `AD092CDE1FA05482698A093D22DB04BF7993C3F0AF61010820EC491CBA445587`; the subsequent acceptance/status entry changes no execution contract.

Primary accepts that verdict and the underlying evidence: fresh strict TypeScript and 119 focused tests; all affected local Markdown links/anchors, both PowerShell blocks, strict UTF-8, final-newline and whitespace checks pass. Of 220 entry tracked files, all 217 outside the three intended existing-document edits remain byte-identical. Only those edits and two new M3-01 documents exist; the staged diff is empty. Roadmap counts reconcile as 12 Complete, 1 planning-only In progress and 15 Not started. The reviewer independently confirmed Git state, no active lease and absence of the future manifest/local package; it did not repeat model, browser or application execution.

Documentation impact: Updated only this plan, the roadmap planning activation, plan index and matching progress record/index. No requirement, ADR, specification, completed task or application behavior changed. This PASS permits bounded research only after a separate execution request; it is not the future R3 contract checkpoint, package freeze or M3-01 completion.

## Revision note

2026-09-09 / primary: Created the planning-only M3-01 ExecPlan from current M2-04 closure and fresh pure checks. Made the real-retrieval eligibility gap and safe exact-package storage explicit gates; kept implementation, provider execution and speculative tooling outside scope. Recorded independent planning PASS, corrected index formatting and accepted documentation/preservation checks without changing the future execution contract.

2026-09-09 / primary, historical execution checkpoint: Recorded exact-task execution authorization and current intentional baseline, two bounded critical-researcher reports, mandatory OWNER DIRECTION, fresh strict/119-test evidence, actual-abstention reconstruction, full-suite identity limits, remaining budgets and concrete eligibility clarification. Kept research literals proposed, all B effects gated, prior history preserved and M3-01 blocked rather than frozen.

2026-09-09 / primary, closure: Recorded the accepted owner exception, completed both bounded research follow-ups and analyst correction, corrected the pre-draft integrity/configuration findings, froze and verified the exact static package, accepted different final review with its stale-status finding resolved, passed the documentation gate and archived the Complete task. All runtime/capacity/provider-execution obligations remain downstream and no later task is selected.
