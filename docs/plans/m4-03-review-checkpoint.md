# Integrate and verify human review

This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.

## Current state

- **Task:** [M4-03](../DEVELOPMENT_ROADMAP.md#m4-03--integrate-and-verify-human-review), In progress for owner-requested current-state review and planning only. No execution or human decision is authorized by this planning request.
- **Latest evidence:** [M403-PLAN-01](#m403-plan-01--accepted-planning-readiness): independent critical planning PASS, fresh focused/source checks and five-document validation. M4-02 is Complete. Two retained actual proposals remain valid and pending; no retained source was changed. M3-03 remains independently Blocked on capacity.
- **Pending:** owner execution authorization. Future execution starts with [G](#g--freeze-the-checkpoint-evidence-and-command-contract), including authentic-source/isolated-case binding, finite commands and the human-decision boundary, then [A/B/C](#a--accept-existing-coverage-and-the-minimum-checkpoint-caller).
- **Active lease:** None. Future research, writer and actual-case allowances have not started; [budgets](#plan-of-work) cannot be renewed by a new agent or case ID.
- **Next:** on an owner execution request, enter G. Semantic confirmations and exact reviewer decisions remain separate human inputs before each final submission. Follow [commands](#concrete-steps), [acceptance](#validation-and-acceptance) and [recovery](#idempotence-and-recovery).

## Progress

- [x] (2026-09-14 20:26Z) Review current state, M4-02 closure, task authorities and review/browser seams; strict TypeScript and 16 focused tests pass. Both retained actual sources validate and match their historical hashes; M403-ENTRY-01.
- [x] (2026-09-14 20:41Z) Accept the planning draft after independent critical PASS with no findings and five-document validation; M403-PLAN-01. No execution, source-case preparation or human decision occurred.
- [ ] After execution authorization, accept G's evidence/case/command contract and owner-input boundary.
- [ ] Accept A's existing coverage and any strictly necessary characterization/helper work through the worker-first route.
- [ ] Complete B's real-proposal human decisions, blocked acceptance, abstention-no-review, application-owned readback and bounded accessibility evidence.
- [ ] Accept C's full verification, different fresh integrated review and documentation closure; only then mark M4-03 Complete and archive this plan.

## Surprises & Discoveries

- [M4-02 final acceptance](completed/m4-02-accessible-review-ui.md#m402-final-01--integrated-review-and-task-closure) records all 706 tests in 31 files, current strict/build, all three synthetic browser-to-service-to-disk decisions, a committed lost-response case, final desktop/narrow evidence and integrated critical PASS. Recreating those tests with another synthetic fixture would not establish M4-03's real-proposal human-review checkpoint.
- There are two known real pending proposals from M3-05, one per provider, both for `color-contrast`. Each aggregate has 35 Findings and preserves its siblings. Three mutually exclusive final actions cannot be applied sequentially to one canonical pending proposal. This plan therefore gates isolated evidence branches rather than resetting decisions or silently ordering new generation calls.
- M3-05 records semantic limitations in the actual proposals. In particular the Local proposal's blocking-judgment prose contains `false`; this is not a human finding that judgment is complete or unnecessary. Mechanical validation never justifies automatic support confirmation or selecting an acceptance outcome to satisfy a test.
- M4-02 omitted native 200% testing at explicit owner direction [for that task only](completed/m4-02-accessible-review-ui.md#m402-b-visual-02--final-focus-proof-and-owner-directed-zoom-omission). It did not pass zoom or amend global accessibility requirements/M6 scope. Preserve that history; G must bind M4-03's applicable visual evidence without treating the omission as a global waiver.

## Decision Log

- Decision: select only M4-03 planning. Rationale: the owner explicitly requested this plan and Complete M4-02 satisfies the prerequisite; no actual review is implied. Date/author: 2026-09-14 / primary.
- Decision: plan an evidence-first checkpoint, with no application changes by default. Rationale: transport, validation, persistence and accessible interaction already exist. Extend only a demonstrated missing test-side verification capability; a production defect requires a bounded correction contract. Date/author: 2026-09-14 / primary.
- Decision: preserve retained originals and plan finite isolated branches from authentic proposal bytes, subject to G's provenance/interpretation freeze. Rationale: independent review outcomes must not overwrite one another or manufacture new model executions. Copies are controlled review branches, not new real generation runs. Date/author: 2026-09-14 / primary.
- Decision: separate product-owner semantic decisions from agent verification. Rationale: a research/review agent may inspect evidence or recommend wording but cannot stand in for the required person or infer their confirmation from task execution authorization. Date/author: 2026-09-14 / primary.

## Outcomes & Retrospective

Planning is complete with independent critical readiness PASS and documentation validation. Existing review behavior has controlled verification; the actual-proposal human checkpoint remains pending. The frontend-quality skill limits visual proof to the existing interaction and source distinctions, without a redesign or new UI owner. Planning has not approved any proposal, established semantic truth, passed capacity, consumed generation evaluation or reopened a completed task.

## Purpose / Big Picture

Demonstrate that a person can inspect an authentic validated proposal, resolve or reject unsupported claims, apply a valid blocking judgment, and save one final decision through the current browser/service path. Verify approve, edit-and-accept and reject as independent outcomes, prove invalid acceptance is blocked and abstention has no review action, and validate durable readback without altering original evidence or siblings. This is a review-transition checkpoint, not a new model-quality evaluation or an automatic ground-truth label.

## Context and Orientation

Use the [authority map/task router](../README.md#read-by-task), [global requirement semantics](../PROJECT_REQUIREMENTS.md) and the owning roadmap row. Selected authorities:

| Authority | Checkpoint obligation |
| --- | --- |
| [BHV-05 and compact review portion of REQ-EVAL-002](../requirements/evaluation-and-release/EVALUATION_AND_ACCEPTANCE.md) | Individual human decision; review transitions observed separately from generation groundedness, retrieval quality or other dimensions. Shared review checks are provider-independent and add no generation execution. |
| [SPEC-005](../specs/SPEC.feature), [HS-010](../specs/HARD_SPEC.feature) | All three final actions, material-support and blocking-judgment acceptance gates, original/edited distinction, non-blocking reminder, action/time and no sibling/page-level decision. |
| [REQ-REV-001, 008, 009](../requirements/EVIDENCE_AND_REVIEW_WORKFLOW.md#human-review-and-manual-checks) | The controlling Accepted semantics behind those examples: one person, one current decision, complete edited proposal only for edit-and-accept, bounded optional note and no per-claim/history machinery. |
| [ADR-0021](../architecture/decisions/ADR-0021-single-file-run-aggregate.md), [lifecycle](../requirements/INFORMATION_AND_WORKFLOW_LIFECYCLE.md) | Validated application-owned single-file evidence; immutable original/context, final-decision integrity and last-valid-file truth. OD-026 still defers user-facing run reopening. |
| [Application accessibility](../requirements/quality-security-and-operations/APPLICATION_ACCESSIBILITY.md), [M4-02 presentation](../ui/ANALYZE_AND_RESULTS_PRESENTATION.md#m4-02-selected-finding-review), [ADR-0012](../architecture/decisions/ADR-0012-react-as-initial-user-interface-library.md) | Automated keyboard/focus/announcement checks and manual visual review of the existing source-separated interface; browser presentation is not durable authority. No screen-reader speech or support-matrix claim. |

All task-applicable Must semantics are Accepted; deferred reopening, release and post-MVP behavior remain excluded. Entry has eighteen Complete tasks and M3-03 Blocked. This activation makes M4-03 In progress and leaves eight later tasks Not started. M4-02's 706-test closure is accepted historical evidence, not a fresh full regression in this turn.

### Current implementation and reusable evidence

`src/server/domain/review-contract.ts`, the review operation/repository transition and `src/client/finding-review-admission.ts` already enforce one request-correlated final decision. `/api/finding-review` is the real same-origin endpoint used by `src/client/main.tsx`. The browser uses a 30000-ms local deadline, no automatic recovery read or retry, and retains the mutation block for an unknown save. A primary readback for evidence/recovery is not a new user-facing reopen feature.

`ProposalReviewForm.tsx`, `ProposalEditor.tsx`, `ReviewDecision.tsx`, `ProposalContent.tsx`, the shared status region and existing selection/focus coordination are **REUSE_AS_IS**. So are native evidence, authenticated guidance/citations, final labels and current CSS. No rendered change or new component is planned. The skill applies to evidence inspection; if a visible defect requires correction, first freeze its frontend-visual packet and reuse disposition.

`tests/finding-review-ui.test.ts` and `tests/helpers/m402-review-browser.ts` already exercise built UI → actual review endpoint → actual service → disposable disk. They stage earlier scan/guidance/generation responses to reach the pending proposal; only review reaches the actual mutation path. They currently synthesize proposals and automatically select test confirmations. Reuse their mechanics and exact cleanup approach, **not** their synthetic content or automatic confirmations as human evidence. `m401-review-sandbox.ts`, review fixtures and the existing UI harness provide test-owned boundaries; no general evaluation harness is needed.

### Authentic source candidates

These identities are already recorded in the M3-05 archive. Entry read-only validation confirms each original is still complete, has one pending `color-contrast` proposal, no decision, and 35 Findings. Content was not copied into tracked planning documents.

| Source | Original aggregate and selected Finding | Entry SHA-256 |
| --- | --- | --- |
| Local, [H observation](completed/m3-05-generation-checkpoint.md#m305-h-observation-01--successful-local-diagnostic-observation) | `data/runs/run-b785db0b-5ed0-4baa-b62f-9be6181d5e72/run.json`; `50dcefee-7e74-4253-a373-c5bbf2ab839b` | `df21550331a276095d3d5780eec301d88285af2f91fcc4414a37848a830726aa` |
| Groq, [corrected observation](completed/m3-05-generation-checkpoint.md#m305-f-observation-02--corrected-local-failure-and-groq-proposal) | `data/runs/run-d4d06856-39d7-4196-b607-8e2f2815b302/run.json`; `f0a4f4ff-5beb-4676-9055-0ea763523e70` | `61eb5b6fccc59083850ea147838a1049ab42090e75fc82a607c5874663d5960b` |

A candidate is not yet selected or semantically approved. G chooses only the minimum suitable source(s); it does not repeat review once per provider or require three rule profiles. If the person cannot honestly approve an unchanged original, the approve case remains unfulfilled. Do not modify that original, pressure the reviewer, fabricate support or silently substitute an edit under the approve label. Complete unaffected proof and request the needed owner direction without automatically generating another candidate.

## Scope and Non-Goals

Include source/proposal provenance, bounded isolated review cases, the three actual human-authorized final actions, blocked acceptance, no-review abstention, immutable-original/sibling checks, application-owned durable readback and proportional accessibility/visual proof. Preserve the fixed review API/shape, source bytes, invocation versions and evidence hierarchy.

Exclude new product behavior by default, new scans/retrieval/generation, credentials or acquisition, changing any source under `data/runs`, model/corpus/prompt changes, the six generation-evaluation executions, provider ranking, capacity, statistical scoring, support qualification, new IDs/fields in canonical runs, approval history, audit graphs, per-claim records, automatic semantic judgments, run reopening, comparison, source-code remediation, new libraries/frameworks, screenshot services or telemetry. Copies and test-side staged navigation must never be reported as fresh scan/retrieval/generation or three independent model outputs.

## Plan of Work

Follow the [agent workflow](../../.codex/README.md), [worker-first procedure](../../.codex/execplan-implementation-workflow.md), [ADR-0024](../architecture/decisions/ADR-0024-milestone-slice-tdd-with-independent-ownership.md), [write guard](../../.codex/write-lease-guard.md) and visual evidence rules in the [frontend-quality skill](../../.agents/skills/frontend-quality/SKILL.md).

The primary owns authority, evidence interpretation, human-input requests, integration, status, documentation and lease open/terminal close. Research/review roles remain read-only. Any ordinary test/helper or production edit is delegated. One lease per worktree; primary documentation/guard maintenance occurs between leases. Inspect actual diff, complete command/environment/tree identity, cohesion and terminal receipt before handoff acceptance. Only useful independent read-only work may overlap; no parallel writers.

For A, retain one `test_worker` for read-only preflight and any passing characterization. If production behavior is already covered, reuse evidence rather than inventing Red/Green. For an actual production gap, record a bounded correction slice first: `test_worker` owns Red, separate `code_worker` or `frontend_code_worker` owns Green, accepted tests remain unchanged, and the complete packet/leases/review rules apply. No primary production patch or silent expansion follows from discovering a defect.

Writer budgets retain the workflow's one preflight, coherent characterization or Red/Green, at most three writes per unchanged role/phase chain (initial, ordinary correction, conditional final correction with required learning record), and one review correction loop. Stop dependent work on exhausted budget, changed binding fields, two repeated decisive gaps without material new evidence or two no-diff handoffs. IDs and replacement agents do not renew allowances. Primary triages within remaining authority; only actual owner-controlled decisions or expanded/exhausted authority require owner direction.

Actual B allowance, available only after execution authorization and the applicable gates: **three final review POSTs total**, one for each independently isolated action case; at most one final POST per case. Blocked UI attempts must produce zero POSTs. One controlled abstention case is provider-independent. A rejected, failed, lost or uncertain final attempt consumes its case allowance; read-only diagnosis/readback does not renew it. Existing repeatable synthetic regression remains separate and cannot satisfy or reset the human-case allowance.

### G — Freeze the checkpoint evidence and command contract

**M403-G; R3; TDD: Not applicable.** Primary-owned evidence/command decision work, no production writes or implementation lease. Replacement evidence: exact authority/source inspection, complete invariant/case contract and mandatory research checkpoints. Application responsibility change: None.

Freeze the exact proposal source(s), originals' hashes and no-review state, each case's isolated run root, copied bytes and expected selected Finding, temporary-versus-retained evidence destinations, and readback predicates before any actual-case preparation. Default preservation route is three independent byte-identical copies of selected authentic aggregates in separate exclusive test roots, keeping original run/Finding IDs and provenance unchanged. Case labels belong in this plan/local evidence, never canonical run fields. Duplicate IDs across isolated roots are controlled branches, not new application runs or proposal versions. G must verify this interpretation satisfies the real-proposal checkpoint and openly label the staged pre-review pipeline; no source under `data/runs` is writable. If another route would mutate originals, add generation calls or change the authority interpretation, stop for owner direction.

Freeze the smallest way to present copied actual inputs in the current built UI without a product reopen feature. Reuse M4-02's test-side stage replay where compatible: earlier Analyze/guidance/generation requests are intercepted and supplied explicitly identified projections from the authentic source plus its authenticated corpus view; they are not executed. The actual pending aggregate supplied to the UI and service must match the selected source exactly. Only `/api/finding-review` may pass through as a real mutation. Validate every projection and reject unexpected calls. Do not change the original evidence or reference set merely to fit a harness fixture.

Freeze one compact case table before observing outcomes: source/hash, case/root, required action, expected permitted transition, unchanged-original/sibling comparisons, no-call predicates, negative attempts, readback and cleanup. Use existing evidence for exhaustive invalid/malformed/unknown-response cases; add only a missing checkpoint witness. Do not create another generation manifest or turn public observations into evaluation gold.

**Human-input boundary:** execution authorization permits preparation, not semantic approval. Present the selected actual proposal, its cited guidance/evidence and limitations to the person. Before any final POST, the person must either operate the existing controls themselves or explicitly authorize the exact action, judgment/reason, note and complete edit for the identified proposal. Approval/edit require their support confirmation for the resulting proposal; rejection does not. Agent suggestions are proposals only. Any material change to the payload/source invalidates the prior confirmation. One concise owner input may cover all ready case payloads; do not invent per-claim records, reviewer identities or extra approval stages. A synthetic test's `supports-proposal`/checked box is not reusable human evidence.

Freeze exact A/B/C commands and effect slots, existing browser/build/runtime identity, safe ownership/cleanup and bounded visual samples before preflight or effects. The M4-02 zoom omission is historical and task-specific. Bind one applicable current review visual path including native zoom/reflow under the unchanged accessibility authority, or obtain explicit owner direction if a required mechanism is unavailable; do not invent a global waiver or silently claim the omission was a pass. Keep the separate M2-03/M6-03 full-guidance zoom item untouched.

### A — Accept existing coverage and the minimum checkpoint caller

**M403-A; read-only preflight then characterization if needed; S3.** Authorities: BHV-05, SPEC-005, HS-010 and shared-review REQ-EVAL-002, with G's source/ownership contract. Primary issues a complete Milestone Assignment Packet v2. Expected classification is existing behavior with a missing authentic-source verification route; preflight must confirm instead of assuming missing production behavior.

First reuse the existing review-contract, review-admission/API, service/repository and UI suites. If they and a bounded direct procedure meet G, make no code change. If the source-bound caller is missing, `test_worker` may add passing characterization and the smallest test-side helper under a fresh lease. Candidate paths: `tests/review-checkpoint.test.ts`, `tests/helpers/m403-review-checkpoint.ts`; minimal changes to `tests/helpers/m402-review-browser.ts` only if a concrete common mechanism warrants local reuse without altering its existing synthetic tests. Freeze exact paths and tests before the lease. Never copy actual proposals into tracked fixtures.

Test responsibility is a bounded review-checkpoint driver: validate supplied source bindings, prepare exact isolated copies, stage only the agreed earlier responses, accept externally supplied human-authorized input, drive the unchanged real review route, collect validated readback and close only owned resources. Reuse `m401-review-sandbox.ts` and current browser/service primitives; do not create a generic runner, polling system or new UI/endpoint. Synthetic characterization proves the driver cannot dispatch an unapproved payload or mutate originals; it is not human-case evidence. Default application-source responsibility change: **None**. Passing characterization requires no fake Green owner.

Replacement evidence where TDD is not applicable includes structural path/provenance/no-call checks and the existing behavior's passing characterization. If preflight finds a missing/regressed application behavior, consult the [bug index](../bugs/README.md), freeze a separate bounded standard or frontend-visual correction packet with exact source/test owners and Red target, and pause only affected observations. Do not delegate an open-ended repair or widen the current helper lease. New dependencies, significant architecture or expanded behavior need owner direction.

Advance after actual diff/test relevance/cohesion inspection, accepted focused checks/independent strict TypeScript, compliant terminal leases where writes occurred and fresh `critical_reviewer` acceptance of the exact caller and original-preservation/no-call boundary. The review includes source-bound authorization handling, not an agent's substitute judgment of the proposal.

### B — Observe the real-proposal review checkpoint

**M403-B; primary-operated bounded observation, with a person supplying final decisions; TDD: Not applicable.** No production responsibility change. Replacement evidence is the actual built UI, real review service/disk transition, human-authorized payload and corresponding validated readback. Follow G's accepted command/case freeze; no observation may invent commands or expand targets.

Prepare and validate each exclusive case root and pending copy; inspect the actual original/guidance/evidence with the person before its final input. Exercise the following provider-independent cases, not a provider/profile matrix:

| Case | Required observation |
| --- | --- |
| Approve | Person confirms support for the unchanged original and supporting judgment or reasoned N/A. One final request saves `accepted` with the exact action/judgment/note and server time; original remains unchanged. |
| Edit and accept | Person approves a complete resolving edit with valid references and confirms its support/judgment. Exactly one final request saves `edited-and-accepted`; full reviewer content is distinct from the immutable original. |
| Reject | Person chooses rejection with an honest judgment disposition and optional bounded feedback. One final request saves `rejected`; no accepted plan or acceptance confirmation/edit is fabricated. |
| Blocked acceptance | Before a final decision on a still-pending branch, demonstrate missing support confirmation and unresolved judgment, then N/A without reason. Errors are accessible, no final POST/decision occurs and the pending bytes remain unchanged. These deliberate negative inputs are test probes, not the person's semantic assessment. |
| Abstention has no review | Reuse the existing controlled incomplete-guidance/abstention witness once. Show explanation, available evidence and no provider-call/review action; no decision or actual generation occurs. Preserve its synthetic provenance rather than calling it a real-proposal outcome. |

For each final case compare captured request, real endpoint response, `LocalService.readRun` and validated on-disk aggregate. The selected decision/state must agree, generation/original/retrieval/analysis/parent and all siblings must be unchanged, and `decidedAt` must be server-owned. Preserve complete before/after hashes and minimized evidence locally. Verify source originals remain byte-identical after every case. Do not reset, overwrite or replay a completed case.

Automated keyboard, focus and shared-announcement checks cover the accepted action/selection path and existing adverse cases. Manually inspect one authentic proposal-bearing path through editor/error/final decision at the bounded desktop/narrow/native-zoom samples frozen in G. Reuse M4-02's current rendering and focus evidence where identities and content coverage suffice, but do not describe synthetic screenshots as showing actual proposals. Record exactly which proof is authentic-content review, controlled negative behavior or reused regression. Do not claim unobserved screen-reader speech or whole-app/browser support.

If the person declines approval, cannot establish support or needs an unresolved manual judgment, preserve that outcome and finish unaffected cases. Do not choose a misleading N/A reason, fabricate acceptance, substitute an edited result for approve or generate another proposal. Ask only for the concrete missing owner decision or changed scope needed to finish the remaining branch.

### C — Integrated review and documentation closure

**M403-C; primary evidence integration; TDD: Not applicable.** No new production module. Run the current full authoritative suite (entry: 31 files) plus any accepted checkpoint characterization, independent strict TypeScript and the client build. Reuse unchanged evidence under its complete identity; broaden or repeat only for changed/stale evidence, failure or unresolved risk. Preserve mandatory independent risk-critical checks.

Obtain a different fresh integrated `critical_reviewer` for the complete I1–I10 packet, actual-case provenance, human-input binding, original preservation, real readback, no-call evidence, visual observations and exact cleanup. It may assess validation/provenance but cannot create a missing human confirmation. Add no independent semantic panel, inter-rater agreement or aggregate score.

Apply the [documentation closure gate](../README.md#task-closure-documentation-gate): reconcile plan, roadmap/progress and their navigation, current overview/review-checkpoint instructions and materially affected UI verification limits. State separately the observed transitions, remaining semantic limitations and evidence boundaries. Preserve M4-02's omitted zoom and M3-05's historical failures; no retroactive qualification or M6 credit. Requirements/ADRs need no change unless an explicit accepted amendment actually occurs. Run proportional link/command/configuration/encoding/whitespace checks and `git diff --check`. Mark Complete/archive only after every task Verification item and this gate pass; do not start M5 automatically.

## Decision Review Contract

**Identity:** M403-G, R3, future checkpoint procedure freeze. **Triggers:** authenticity/identity of replayed evidence, irreversible independent decisions, exact original preservation and recovery after possible commit. **Artifact:** one accepted case/command subsection in this ExecPlan, not a new evaluation framework, ADR or ledger. **Boundary:** primary may accept compatible technical literals; person supplies semantic decisions, and only the owner can expand actual calls, mutation targets, requirements or budgets.

At most one bounded non-ranking discovery pass may resolve current caller/data/visual-mechanism gaps. Then freeze the minimal candidate procedures and evidence dimensions before comparison: direct reuse versus a small test-side adapter for isolated authentic-input branches. Existing review semantics and source content are fixed, not candidates. Hard gates are original/source integrity, honest staged-versus-actual provenance, real UI-to-service-to-disk review, one authorized decision per branch, meaningful human support/judgment inputs, no new model operations, accessible evidence and bounded cleanup. Prefer no new helper when the existing caller can satisfy those gates.

Budget: one `critical_researcher` report covering the coherent authentic-input/decision-to-disk evidence dimension, one bounded follow-up; one mandatory `decision_analyst` contract/synthesis audit plus one bounded correction; fresh `critical_research_reviewer` pre-draft checkpoint; primary-only authoritative drafting; different fresh `critical_research_reviewer` final review. No drafter or extra quality panel. Allow one pre-draft correction and at most two final-artifact correction cycles within the recorded research budget. If critical coverage cannot fit, reconcile the capsule before adding assignments; no respawn resets limits.

Project this contract and I1–I10 into Research Assignment Capsules with exact source/authority links, evidence identity and remaining budget. Synchronize before synthesis. The analyst returns exactly `DRAFT READY`, `RETURN FOR RESEARCH` or `OWNER DIRECTION`; DRAFT READY accepts neither the final artifact nor any person's proposal decision. Final review checks the complete primary-written contract and explicit pending runtime/human obligations. Repeated decisive gaps twice without material new evidence, exhausted allowance, changed authority or a missing owner-controlled choice stop dependent work. Preparing a useful inspection packet and completing unaffected checks remain authorized; no new permission ritual is needed for routine in-scope preparation.

## Concrete Steps

Working directory: `C:/Users/mmjos/Desktop/workbeanch/a11y-evidence-lab`. Read and dot-source the maintained [README preparation](../../README.md#development-command-preparation) in each command session. Do not replay archived task leases, setup grants, process kills, source mutations or Git constants.

### M403-ENTRY-01 — Planning evidence

At clean HEAD `ae0850df450f89715f73d3089c2778126b665414`, no active `logs/agent-flow-leases/v2/active.json` exists. On 2026-09-14 20:26 UTC, independent strict TypeScript and all **16** tests in review-contract and finding-review-admission passed with no failures/skips. A read-only Node caller used the current `validateRun` on both identified actual sources; each was complete with 35 Findings and exactly one pending contrast proposal/no review. Their hashes match the source table and M3-05 evidence. No provider call, app/browser launch, semantic review or retained-run write occurred. This is focused planning evidence, not a fresh full-suite or checkpoint result.

Additional read-only planning validation confirms the M3-01 manifest still has SHA-256 `63770583a97e1d0517337474dadc706c1047d78e026e22c7a745ce7674ed9e6b`; all thirteen `path`/`sha256` references match their exact bytes. No evaluation input or outcome was changed.

After the README preparation, these exact repeatable checks perform no application operation:

```powershell
git status --short
git rev-parse HEAD
if (Test-Path -LiteralPath 'logs/agent-flow-leases/v2/active.json') { throw 'Resolve existing lease before continuing' }
Invoke-M105Command {
  & $m105Node node_modules/typescript/bin/tsc --project tsconfig.json
  if ($LASTEXITCODE -ne 0) { throw 'Strict TypeScript failed' }
  & $m105Node --test --test-timeout=120000 tests/review-contract.test.ts tests/finding-review-admission.test.ts
  if ($LASTEXITCODE -ne 0) { throw 'Planning review checks failed' }
}
git diff --check
```

The entry commit is historical evidence, not an executable future equality gate. Before execution accept the then-current intentional HEAD, expected dirty paths, relevant source/test/configuration/build identities, frozen manifest/references, source-original hashes, runtime/environment, working directory and exact commands. A committed plan need not be dirty; reconcile genuine drift and affected evidence instead of requiring an old parent HEAD in receipts/closure. Preserve unrelated changes and all existing records.

### Future effect and command slots

These unresolved slots are frozen in G before the applicable preflight, lease or primary effect; they do not permit improvising a command after review.

| Slot | Exact freeze required |
| --- | --- |
| M403-CMD-PREFLIGHT / CHARACTERIZE | Read-only coverage command, exact test/helper paths if necessary, passing characterization/negative witness, strict checker, runtime, flags and maximum durations. No actual source-case final POST. |
| M403-CMD-SOURCE / PREPARE | Exact selected sources and byte hashes, ordinary ancestor/reparse/alias checks, new exclusive isolated roots, create-only byte copies, retained-original protection and validation; no rewrite of IDs/proposals or missing-input regeneration. |
| M403-CMD-PRESENT | Current build/service/browser, exact controlled stage projections, authenticated guidance view, permitted intercepted routes versus the sole real review mutation, source/run identity and request counters. No product reopen route or new actual upstream execution. |
| M403-CMD-DECIDE / READBACK | Person-authorized exact source/action/payload binding, per-case one-POST allowance, endpoint/body and deadline, service-owned read and aggregate comparison, original/sibling/time predicates, minimized local result paths and evidence retention. Human waits are not held-open write leases. |
| M403-CMD-NEGATIVE / VISUAL | Exact blocked/no-POST and controlled abstention inputs; keyboard/announcement/axe checks; one installed browser and bounded desktop 1366×900/narrow 390×844 sample, applicable real native-200 mechanism, screenshot/identity destinations and ownership. No artificial zoom claimed as native proof. |
| M403-CMD-REGRESSION | Maintained 31-file inventory plus accepted new characterization, current mock flags, strict/build, sequential ordering, no competing service/browser test, known output/scratch and exact environment restoration. Existing capture flags are absent unless explicitly frozen for the bounded visual caller. |
| M403-CMD-CLEANUP / GUARD / DOCS | Exact owned services/tabs/handles and shutdown order, temporary versus retained evidence, safe exact-path removals/final absence checks, guard open/terminal callers/protected tests/attempt lineage, documentation links/anchors/command parsing/UTF-8/newline/whitespace and `git diff --check`. |

Missing runtime/browser/source prerequisites stop the dependent effect; no download, model call or fabricated replacement input is authorized. Reuse corrected M4-01/M4-02 cleanup patterns, preserving original failures and exact retained roots when secondary cleanup fails. Keep outcome records local/content-safe; never write credentials, full private transcripts or actual proposal bodies into tracked fixtures or logs.

## Validation and Acceptance

G uses this cumulative invariant packet; every actual-case and visual result is pending at planning. For each row record exact fixture/source, expected result, command/source/build/environment identity, actual evidence or remaining gap and the responsible reviewer under stable checkpoint IDs. Research reviews check contract completeness; A's critical reviewer accepts caller integrity; different C review accepts the integrated evidence.

| ID | Trigger and expected result | Evidence owner |
| --- | --- | --- |
| I1 | Task/dependency/freeze inspection: only M4-03, M4-02 Complete, source/evaluation/model bindings unchanged; no M3-03/M6/provider-quality or support claim. | Primary, G/C reviewers |
| I2 | Source and isolated branch preparation: pending bytes/proposal/provenance match authentic origin; copies are labeled controlled branches, never fresh upstream runs; retained originals stay byte-identical throughout. | G/A checks, A/C reviewers |
| I3 | Exact source/payload semantic input: a person confirms accepted-result support and a supporting or reasoned-N/A judgment; rejection needs no positive assertion. No automated check or generic execution request supplies the person's judgment. | Primary records human input; C reviews traceability |
| I4 | Three separate final branches: exactly one approved action/request per case, correct terminal state/action/time, full edit only for edit-and-accept, no accepted plan after rejection; original result/invocation remain unchanged. | B request/response/readback, C reviewer |
| I5 | Missing support, unresolved judgment and empty N/A reason: accessible blocking errors, zero final POSTs and no decision. Controlled abstention remains explanatory/non-reviewable, with no invocation or review decision. | Existing coverage plus bounded B witnesses, A/C reviewers |
| I6 | Real review endpoint/service and validated readback: browser result equals canonical decision; parent, native evidence, retrieval/analysis and every sibling remain unchanged. Direct fixture mutation cannot stand in for publication. | B service/disk evidence, C reviewer |
| I7 | Keyboard, selection, form-to-decision focus and shared announcements: current behavior remains operable and names the reviewed Finding without changing or stealing focus from siblings. Original/edited/reminder/source labels remain distinct and no certification claim appears. | Current automated coverage and B browser proof, C reviewer |
| I8 | Authentic-content manual visual path: readable full original/edit/decision/citations, non-color status and visible focus at bounded desktop/narrow and applicable native zoom. Historical omitted zoom is not a pass or global waiver; no speech/support-matrix claim. | Primary visual observation, C reviewer |
| I9 | Failure/lost response or cleanup uncertainty: last valid record determines truth; no blind retry, reset branch or new grant. Read-only primary recovery may establish state without a new UI feature; exact owned cleanup preserves source and retained evidence. | A adverse characterization/reused M4-02 proof, B/C acceptance |
| I10 | Actual diff/cohesion, accepted characterization or justified no-write route, terminal leases, full current suite, strict/build, finite allowances, exact cleanup and documentation consistency pass. No roadmap completion before all required evidence and human inputs exist. | Primary, C reviewer |

Do not require all three rule profiles or both providers for these shared transitions. Do not rerun generation or create statistically meaningful quality claims. If only synthetic transitions pass, report that useful evidence but leave M4-03 incomplete. A reviewer may legitimately reject a proposal or be unable to confirm support; the resulting limitation is evidence, not a test condition to manipulate away.

## Idempotence and Recovery

Read-only inspection is repeatable. Source originals under `data/runs` are never write targets. An isolated branch has one final decision and cannot be reset/reviewed again; preparing another copy after failure does not reset its consumed attempt allowance. G's three branches are planned independent cases, not retries or a proposal-version graph.

A lost reply can follow a committed decision. Stop submissions, preserve browser uncertainty and read the selected isolated aggregate through the existing service to establish canonical truth; do not infer no-write from the transport error. This is evidence recovery, not a product recovery read, and does not grant a second final POST. If it cannot establish the required observation, mark that case unresolved and request only the necessary new authority after unaffected work is complete.

Validate all absolute paths and ancestors before copy/removal; refuse unexpected contents or links. Close owned browser/context/service/handles before removing only the exact disposable case resources. Retain required before/after evidence separately under the frozen content-safe path; do not delete source runs or existing M3/M4 proof roots, kill unrelated processes/Ollama or use Git reset. Report exact retained paths and uncertainty on cleanup failure.

Any worker stop requires terminal lease inspection and primary triage before another writer or document edit. Corrections use remaining named budgets, invalidate affected evidence and receive fresh acceptance. Human decisions can remain pending without keeping a worker lease active. If support, a significant scope decision, native-zoom access or a prerequisite cannot be resolved within existing authority, finish unaffected checks and ask a concise evidence-backed question; never silently waive the requirement.

## Artifacts and Notes

M403-ENTRY-01 and M403-PLAN-01 own planning evidence. Future accepted records are M403-G-ACCEPT-01, M403-A-ACCEPT-01, M403-B-OBSERVATION-01 and M403-FINAL-01, created only when their evidence exists. Keep a compact case table, command/identity bindings, human-authorized payload references, actual results, failure/cleanup facts and review verdicts here. Full proposal/edit evidence stays in the authorized local case records; no copied private conversation, per-claim checklist, new telemetry ledger or separate report document is planned.

Planning changes only this plan, roadmap activation, plan index, progress index and one concise M4-03 progress record. Requirements, ADRs, source, tests, dependencies and current runtime behavior are unchanged. Execution documentation impact follows the actual evidence at C.

### M403-PLAN-01 — Accepted planning readiness

On 2026-09-14 20:41 UTC, the fresh independent `critical_reviewer` returned **PASS** for readiness to enter G after execution authorization, with no Blocker, Major or Minor finding. Reviewed plan SHA-256 was `1619c4a5538622bcbfa320925dd63113366d52a2a50a324797e620db37380027` at entry HEAD `ae0850df450f89715f73d3089c2778126b665414`, before this verdict/living-state maintenance. The review covered the complete plan, five planning paths, selected authorities and actual review/helper seams. No execution contract or semantic decision was accepted.

The reviewer independently reproduced strict TypeScript, all 16 focused tests and both source validations/hashes: 35 Findings each, exactly one pending contrast proposal and no decision. Primary accepted the actual five-file diff and checked all sixteen required sections, the one PowerShell block, local paths/anchors, strict UTF-8/no BOM, final newline, trailing whitespace and `git diff --check`; final living-record/link maintenance is checked with the same procedure. Primary's frozen manifest/thirteen references remain unchanged. No active lease, production edit, original mutation, browser/service launch or provider call occurred.

Source suitability for honest unchanged approval, exact case/caller commands, native-zoom mechanism, full regression and actual human/browser/service results remain explicitly pending. M4-03 stays In progress for planning only; M4-02 stays Complete and M3-03 stays independently Blocked. Documentation impact is the five named planning/status/navigation files; no requirement, ADR or runtime change.

## Interfaces and Dependencies

Reuse existing React review controls, `/api/finding-review`, `LocalService.reviewFinding/readRun`, the strict review/aggregate validators, repository safe writer, pinned browser/axe tests, current build and test sandbox. A minimal test-only source-bound driver is the sole anticipated possible addition; exact necessity/ownership is resolved by A preflight, after G freezes its contract. No new application interface, persistence schema, dependency or model operation is needed by default.

M4-03 proves the human review checkpoint, not generation semantics, capacity or release. M5 and M6 remain separately selected tasks; M3-03 stays Blocked until its own gate is met.

## Revision Note

2026-09-14: created for the owner's M4-03 current-state review and planning request after M4-02 completion. Recorded unchanged real pending sources, source-preserving independent review branches, genuine human-input gates, reuse-first characterization/observation workflow, finite effects and proportional visual proof. Preserved M4-02's task-specific zoom omission and independent capacity/evaluation boundaries. No actual decision, source mutation or new upstream operation occurred.

2026-09-14: recorded independent critical planning PASS and verified five-document closure; updated living status and the concise progress summary without changing future execution authority or the case contract.
